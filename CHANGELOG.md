# Changelog

> *Maintainer-facing commit-linked ledger. For the user-facing release summary, see `docs/wiki/model_project_constructor/Changelog.md`. For the design-decision arc, see `docs/wiki/model_project_constructor/Evolution.md`.*

All notable changes to this project are documented here.
Format loosely follows [Keep a Changelog](https://keepachangelog.com/).

When completing work, remove the item from `BACKLOG.md` and add an entry here.

This file is the **authoritative, maintainer-facing record** of completed work. The wiki page `Changelog` (under `model_project_constructor/wiki`, mirrored at `docs/wiki/model_project_constructor/Changelog.md`) is an audience-facing summary whose tone and level of detail may evolve over time; this file stays stable and complete. When the two disagree, this file is canonical.

Dates are commit dates on `master`. Commit hashes are short-form as produced by `git log --oneline`. Session numbers refer to entries in `SESSION_NOTES.md`.

---

## 2026-09

### 2026-09-19 · [ad hoc] S259 claim: BL-57 P11 — bring this ledger to the methodology's rules, sync the framework files, move the runner's customizations into `CLAUDE.md` first *(in progress)*
- **Change:** Session 259 is claimed in `SESSION_NOTES.md`. This is the ledger's first tagged entry. From here on every commit carries one, under a `## YYYY-MM` heading above `## [0.3.0]`, newest on top; the 156 entries below stay where they are, as written (the operator's decision (b), 2026-09-19). Tagged `[ad hoc]` because BL-57 is the methodology fork's backlog id and this project's `BACKLOG.md` holds no item for the work.
- **Commit/PR:** this commit
- **Session:** S259 · **Verified:** n/a — docs-only; the census and read-budget guards pass at the claim state
- **Model:** Claude Opus 5 (1M context)

## [0.3.0] - 2026-07-27

> *Sessions 116–144 below were backfilled in Session 149 (2026-06-12), when the `CHANGELOG.md` cadence convention was clarified to gate on shipped-code changes rather than strictly per-session (see `docs/methodology/PROJECT_CONVENTIONS.md` §2). The six entries cover the O3/O1/O4 single-sourcing overhauls, the E4 provider factory, the wiki citation guard, and the `SR_11_7`→`SR_26_2` rename. Documentation-only sessions in the same span are intentionally recorded in `SESSION_NOTES.md` only.*

### 2026-07-27 — GitLab adapter migrated off `python-gitlab` to direct `httpx` calls (Session 191, Phase 1 of `docs/planning/httpx-adapter-migration.md`)

Removes one of the project's two LGPL-3.0 direct dependencies by rewriting the Website Agent's GitLab adapter against direct `httpx` REST calls instead of the `python-gitlab` SDK. `PyGithub` (the other LGPL-3.0 dependency, GitHub adapter) is untouched — that's Phase 2 of the same plan, a separate session per the plan's explicit per-phase boundary.

- **`src/model_project_constructor/agents/website/gitlab_adapter.py`:** full rewrite (`9af715b`). `PythonGitLabAdapter` now builds an `httpx.Client` scoped to `{host_url}/api/v4` with a `PRIVATE-TOKEN` header (no network call at construction) instead of wrapping `gitlab.Gitlab(...)`. `create_project` issues `GET /groups/{namespace}` (URL-encoded so nested group paths address one path segment) then `POST /projects`; `commit_files` issues `GET /projects/{id}` then `POST /projects/{id}/repository/commits`. `_is_name_conflict` is re-signatured from a `GitlabCreateError` argument to an `httpx.Response`, inspecting the parsed JSON body (falling back to raw text if the body isn't valid JSON) for the same loose "already been taken" / "already exists" match. Every non-2xx response or `httpx.HTTPError` (transport-level failure) is wrapped into `RepoClientError`/`RepoNameConflictError`; no raw `httpx` exception escapes, preserving semantics for the nodes' `RETRY_BACKOFF` loop (dragon #4 of the plan).
- **Bug caught by adversarial review, fixed before commit (`9af715b`):** the first draft checked `status_code >= 400` for "is this a failure," not "is this a 2xx" — `httpx.Client` does **not** follow redirects by default (unlike the `requests`-based transport the old `python-gitlab` adapter used), so a 3xx response fell through the check and crashed on `response.json()` with a raw `json.JSONDecodeError`, and any 2xx response with a non-JSON body did the same at all three "happy path" `.json()` call sites — both are real violations of dragon #4's "no raw exception escapes" contract. Fixed: `_ok_or_raise`/`create_project`'s inline check now test `_is_2xx` (strict `200 <= status < 300`) instead of `< 400`, and a new `_parse_json` helper wraps every post-success `.json()` call, converting a malformed body into `RepoClientError` instead of letting `ValueError` escape. +7 regression tests (redirect and malformed-JSON-on-2xx, one pair per call site).
- **`tests/agents/website/test_gitlab_adapter.py`:** full rewrite (`9af715b`) against `httpx.MockTransport` (DP4) instead of `MagicMock` stubbing of the SDK — asserts exact request method/path/body (including the `%2F`-encoded nested-namespace case and the sorted multi-file commit `actions` payload) and drives transport-level errors (`httpx.ConnectError`), HTTP-level errors (4xx/5xx), 3xx redirects, and malformed-JSON bodies through every call site — a strictly wider failure-point matrix than the pre-migration `MagicMock` suite. 27 tests (was 11), gitlab_adapter.py itself at 100% branch coverage.
- **Packaging (`09f80a4`):** `pyproject.toml` — removed `python-gitlab>=4`, added `httpx>=0.27,<1` as a direct `agents`-extra dependency (previously only transitive via `anthropic`/`langsmith`). `uv lock` confirmed the prune removes only the `python-gitlab` package itself — its two dependencies (`requests`, `requests-toolbelt`) both survive: `requests` because `PyGithub` and `langsmith` (unrelated, already in the graph) also need it, `requests-toolbelt` because `langsmith` alone needs it (dragon #6 — verified live with `uv.lock`'s dependency blocks, not assumed).
- **Docs (`348dff1`, `41445b9`):** `README.md`, `protocol.py`/`cli.py`/`config.py`/`_vocab_guard.py` docstrings and comments, and five wiki pages (`Software-Bill-of-Materials.md`, `Security-Considerations.md`, `Agent-Reference.md`, `Contributing.md`, `Content-Recommendations.md`) updated to describe the `httpx` adapter and the now-single (`PyGithub`) LGPL-3.0 direct dependency. `THIRD-PARTY-LICENSES` regenerated from a live `importlib.metadata` scan (95 distributions, was 96). The wiki commit auto-published to the live GitHub Wiki via the tracked `post-commit` hook.
- **Verified:** `grep -rn "import gitlab" src/ tests/` → 0; `grep -rn -i "python-gitlab" pyproject.toml uv.lock` → 0; `uv tree | grep -i gitlab` → 0. Full gate: **939 passed + 8 live-skipped @ 97.64% coverage** (was 922 passed, Sessions 178–180 baseline); `ruff check src/ tests/` and `mypy` both clean.

### 2026-07-28 — GitHub adapter migrated off `PyGithub` to direct `httpx` calls (Session 193, Phase 2 of `docs/planning/httpx-adapter-migration.md`)

Removes the second and last of the project's two LGPL-3.0 direct dependencies, mirroring Session 191's GitLab rewrite. At completion **zero direct dependencies are LGPL** — the tree is permissive plus the unavoidable `certifi` (MPL-2.0) CA bundle.

- **`src/model_project_constructor/agents/website/github_adapter.py`:** full rewrite (`2412abf`). `PyGithubAdapter` now builds an `httpx.Client` scoped to `host_url` (default `https://api.github.com`; a GHE URL like `.../api/v3` for enterprise) with a bearer-token `Authorization` header and `Accept: application/vnd.github+json` (no network call at construction) instead of wrapping `github.Github(...)`. `create_project` resolves the owner via `GET /orgs/{namespace}`, falling back to `GET /users/{namespace}` on a 404 (dragon #1 of the plan: GitHub's API has no endpoint to create a repo under an arbitrary third-party user, so the fallback still confirms the account exists but creates via `POST /user/repos` — the *authenticated* user's own account — rather than a namespace-scoped path); an org lookup succeeding instead posts to `POST /orgs/{namespace}/repos`. `commit_files` walks the git-database dance per the plan's §4 spec: `GET .../git/ref/heads/{branch}` → `GET .../git/commits/{sha}` → `POST .../git/blobs` (one call per file) → `POST .../git/trees` → `POST .../git/commits` → `PATCH .../git/refs/heads/{branch}`. `_is_name_conflict` re-signatured from a `GithubException` argument to an `httpx.Response`, inspecting the parsed JSON body's `errors` list (falling back to raw text) for a 422 "already exists" match. Applied the `_is_2xx`/guarded-`_parse_json` pattern from Session 191's own post-review fix from the start (per the Phase 1 handoff's explicit gotcha), so no raw `httpx` exception or redirect-body-parse crash was introduced pre-review this time.
- **`tests/agents/website/test_github_adapter.py`:** full rewrite (`2412abf`) against `httpx.MockTransport` (DP4) instead of `MagicMock` stubbing of the SDK, mirroring `test_gitlab_adapter.py`'s structure — a `_route()` helper dispatches by `(method, path)` so each of the 7 sequential git-dance calls can be independently overridden to fail (network error and HTTP error, one pair per call) without duplicating the 6 preceding successful calls per test. 42 tests (was 16 `MagicMock`-based), `github_adapter.py` itself at 100% branch coverage.
- **Packaging (`e0ae0c5`):** `pyproject.toml` — removed `PyGithub>=2,<3` (`httpx` already a direct dependency since Phase 1). `uv lock` pruned `PyGithub` and its unique transitive deps `cffi`, `cryptography`, `pycparser`, `PyJWT`, `PyNaCl` (verified live via `uv tree`, not assumed — dragon #6); `requests`/`requests-toolbelt`/`urllib3` all survive, still needed by `langsmith`.
- **Docs (`797ea5c`, `9aad76b`):** `README.md`, `protocol.py`/`config.py` docstrings, and five wiki pages (`Software-Bill-of-Materials.md`, `Security-Considerations.md`, `Agent-Reference.md`, `Contributing.md`, `Content-Recommendations.md`) updated to describe the `httpx` GitHub adapter and drop the now-obsolete LGPL-compliance notes; root `CONTRIBUTING.md` updated to match. `THIRD-PARTY-LICENSES` regenerated (89 distributions, was 95) — rows for packages unaffected by the removal were carried forward from Session 190's classifier-cross-checked scan rather than re-derived from a bare `importlib.metadata` re-scan, which returns `None`/`UNKNOWN` for several packages (e.g. `Jinja2`, `packaging`, `pathspec`, `sqlparse`) that declare license only via PyPI trove classifiers. The wiki commit auto-published to the live GitHub Wiki via the tracked `post-commit` hook.
- **Verified:** `grep -rn "from github import\|import github" src/ tests/` → 0; `grep -rn -i "pygithub" pyproject.toml uv.lock` → 0; `uv tree | grep -iE "pygithub|pynacl|pyjwt|cryptography|cffi|pycparser"` → 0. Full gate: **963 passed + 8 live-skipped @ 97.75% coverage** (was 939 passed, Session 192 baseline); `ruff check src/ tests/` and `mypy` both clean.

### 2026-07-29 — Rename adapter classes to drop the SDK names baked into the identifiers (Session 203, Phase 3 of `docs/planning/httpx-adapter-migration.md`, DP1)

Optional Phase 3, explicitly selected by the operator this session (the plan's own default recommendation was to keep the names / skip this phase). Both adapters have called `httpx` directly, not the LGPL SDKs, since Phases 1–2 (Sessions 191/193); `PythonGitLabAdapter`/`PyGithubAdapter` were misnomers naming libraries the classes no longer import.

- **Rename (`2538f8f`):** `PythonGitLabAdapter` → `GitLabAdapter`, `PyGithubAdapter` → `GitHubAdapter` — pure mechanical, no behavior change. `src/model_project_constructor/agents/website/gitlab_adapter.py` / `github_adapter.py` (class def, docstring usage example, `__all__`); `agents/website/__init__.py` (re-export + `__all__`); `orchestrator/config.py` (lazy-import factories). Tests: `test_gitlab_adapter.py`, `test_github_adapter.py` (import + every reference), `test_cli.py` (4 `monkeypatch.setattr` patch strings + a docstring + two test function names that spelled the old SDK names in snake_case — `..._invokes_python_gitlab_adapter` → `..._invokes_gitlab_adapter`, `..._invokes_pygithub_adapter` → `..._invokes_github_adapter`), plus two files outside the plan's original §2.2 inventory that a fresh grep caught monkeypatching the classes by string name: `tests/scripts/test_run_pipeline_adapter.py` and `tests/orchestrator/test_host_registry_extensibility.py` (their `monkeypatch.setattr(module, "OldName", ...)` string arguments had to change too, or the patch would silently fail to find the attribute post-rename).
- **Docs (`36734a9`):** five wiki pages (`Agent-Reference.md`, `Architecture-Decisions.md`, `Extending-the-Pipeline.md`, `Schema-Reference.md`, `Security-Considerations.md` — the last also outside the plan's original Phase 3 file list, caught by the same fresh grep) plus `ROADMAP.md`'s M4 feature-inventory line. `docs/wiki/claims-model-starter/Changelog.md` and `Evolution.md` deliberately left untouched — historical record, per the plan's own §2.5 "leave" list. Wiki commit auto-published to the live GitHub Wiki via the tracked `post-commit` hook.
- **SBOM version table (`Software-Bill-of-Materials.md`):** checked, not edited — the table holds no adapter class-name references and its locked-version rows (`anthropic` 0.94.1, `httpx` 0.28.1, etc.) were independently re-verified against the current `uv.lock` and are still accurate; nothing to reconcile.
- **`BACKLOG.md`:** the "Phase 3 (optional)" bullet removed (was `Not started`); the httpx-adapter-migration entry's summary now says both phases plus the optional rename are done.
- **Verified:** `grep -rn "PyGithubAdapter\|PythonGitLabAdapter" src/ tests/ docs/wiki` → 0 outside the two named historical files. Full gate: **970 passed + 8 live-skipped @ 97.76% coverage** (unchanged from Session 202's baseline — pure rename, no behavior change); `ruff check src/ tests/` and `mypy` both clean.

### 2026-07-29 — Generated-project CI portability: enterprise-host overrides for the Website Agent's governance scaffold (Session 205, Phase C3b of `docs/planning/enterprise-migration.md`)

Previously, every project the pipeline generates had public-host values baked into its CI/pre-commit config — `python:3.11` (Docker Hub), `actions/checkout@v4`/`actions/setup-python@v5` (the public GitHub Actions marketplace), and `https://github.com/astral-sh/ruff-pre-commit` (the public pre-commit mirror), with no index URL ever set (implicit public PyPI). An enterprise deployment generating projects for an internal host had no way to redirect any of these.

- **`src/model_project_constructor/agents/website/governance_templates.py` (`ed09df5`):** new frozen `CIHostConfig` dataclass (`base_image`, `index_url`, `action_prefix`, `pre_commit_repo` — all default to today's public values, so an un-overridden call is byte-identical to before). `render_gitlab_ci`, `render_github_actions_ci`, `render_pre_commit_config`, and `build_governance_files` all gain an optional `ci_host_config` parameter. `render_gitlab_ci` adds a conditional `variables: UV_INDEX_URL` block and swaps `pip install uv` for `pip install --index-url ... uv` only when `index_url` is set; `render_github_actions_ci` mirrors this with a top-level `env:` block and parameterises the `actions/` marketplace prefix on all three jobs' `checkout`/`setup-python` steps.
- **Threading (`ed09df5`):** `state.py`'s `WebsiteState` gains `ci_host_config: dict[str, Any]` (stored as a plain dict, not the dataclass, to keep the LangGraph state JSON-serializable and keep `state.py` decoupled from `governance_templates`); `nodes.py`'s `scaffold_governance` reconstructs the dataclass via `CIHostConfig(**state.get("ci_host_config") or {})`. `agent.py`'s `WebsiteAgent.__init__` gains a `ci_host_config: CIHostConfig | None = None` kwarg (default `CIHostConfig()`), converted via `dataclasses.asdict()` when building `initial_state`. The website agent CLI (`agents/website/cli.py`) gains `--ci-base-image`/`--ci-index-url`/`--ci-action-prefix`/`--ci-pre-commit-repo`. `scripts/run_pipeline.py` gains `build_ci_host_config()`, reading `MPC_CI_BASE_IMAGE`/`MPC_CI_INDEX_URL`/`MPC_CI_ACTION_PREFIX`/`MPC_CI_PRE_COMMIT_REPO` directly from `os.environ` (mirroring the existing `MPC_HOST_URL`/`MPC_NAMESPACE` direct-read pattern already in that file, not `OrchestratorSettings` — `ci_platform` itself never flowed through that settings object either), threaded into `WebsiteAgent` for both the fake and live branches.
- **A pre-existing test bypass, fixed (`ed09df5`):** `tests/agents/website/test_retry.py`'s end-to-end retry test constructs `WebsiteAgent.__new__(WebsiteAgent)` and manually sets attributes, bypassing `__init__` — needed `.ci_host_config = CIHostConfig()` added since `run()` now reads that attribute unconditionally.
- **Design boundary respected:** `orchestrator/config.py`'s `OrchestratorSettings` deliberately does NOT gain these fields — importing anything from `model_project_constructor.agents.website.*` at runtime (vs. under `TYPE_CHECKING`) would trigger the package's SDK-eager `__init__.py` (httpx/PyGithub adapters), which `config.py`'s own docstring explicitly avoids.
- **Tests (`ed09df5`):** 17 new/changed tests across `test_governance.py` (`CIHostConfig` defaults reproduce the exact pre-change output; each field's override tested in isolation with its siblings asserted to keep their defaults; a full end-to-end `WebsiteAgent` → `FakeRepoClient` content check with all four fields overridden together), `test_cli.py` (all-four-flags, zero-flags, and a partial/mixed-flags case), and `test_run_pipeline_adapter.py` (env-var reading in isolation, plus threading into `WebsiteAgent` for both the fake AND live branches of `build_website_runner`).
- **Adversarial review (pre-commit, no code change from it needed beyond the coverage fixes below):** two independent lenses reviewed the diff. Correctness/regression: no issues — confirmed byte-identical default output via a `git stash`/diff comparison and validated the new GitHub Actions `env:` block with `yaml.safe_load`. Test-coverage: found 3 real gaps, all closed before commit — the live-mode branch of `build_website_runner` had no test asserting `ci_host_config` threading at all, no test isolated a single-field override from its untouched siblings, and no CLI test covered a partial (not all-four, not zero) flag combination.
- **Verified:** a fake-mode run with all four `MPC_CI_*` env vars set produced zero `docker.io|python:3.11|github.com/` matches across all 39 generated files; the same run with the env vars unset still showed the public values in exactly `.github/workflows/ci.yml` and `.pre-commit-config.yaml`. Full gate: **989 passed + 8 live-skipped @ 97.78% coverage** (was 970 passed, Session 203 baseline); `ruff check .` and `mypy` both clean.

### 2026-09-18 — The ledger's front matter becomes the census guard's fifth surface (Session 258, Option 2 of the closed BACKLOG item)

Closes "The ledger's front matter is guarded only at its table rows," filed Session 254 and ruled by the operator on 2026-09-18: **Option 2, extend the guard.** Session 254's review had deleted the routing sentence, the write-once rule, the banner-snapshot rule and the column legend one at a time with **every proof green**; Session 256's review then repointed row 9 at a file that does not exist, and every proof and both guards stayed green. Every shard proof resolves its prose operands at its own trim commit, so between trims nothing read the front matter at all.

- **`tests/test_session_notes_census.py`:** `SESSION_NOTES.md`'s front matter — everything above the first `### What Session N Did` heading, sliced by the module's own declared grammar — joins `SURFACES`, so `check_composed`, `check_scan` and `check_filename_sets` reach it. Records are deliberately excluded: they discuss shards constantly and would bury the fail-closed net. Two new checks: **`check_rows`** (one numbered row per shard, ordinals in sequence, `` `this commit` `` only in the newest row) and **`check_standing`** (seven standing rules required verbatim — declared, not composed, because a rule carrying no number cannot be derived from an artifact). Every row cell but the commit hash is composed from the artifacts, `added` included: the set difference between a proof's `def L<n>(` definitions and its parent's. All nine rows composed correctly on first measurement.
- **Three inherited checks had silently stopped isolating.** Adding the surface left `check_tiling`, `check_proofs` and `check_banners` the sole catcher of nothing — M14, M16 and M17 rewrite four surfaces to agree with the break they introduce, and the front matter, left stating the truth, made `check_composed` co-fire. The suite stayed green: reachability was intact, isolation was not, and only a hand re-derivation noticed. All three now sync the table (`_sync_front_matter_rows`), and **`test_every_check_is_the_sole_catcher_of_some_mutant`** makes the published neuter table a test rather than a claim.
- **Mutants M18–M23:** a row cell disagreeing with its shard (`composed`), `` `this commit` `` in an older row and a renumbered table (`rows`, isolating), a deleted standing rule (`standing`, isolating), an unclassified census sentence in the front matter (`scan`), and the front matter naming a shard that does not exist (`filename_sets`). 23 mutants, 9 checks, every check the sole catcher of at least one.
- **`tests/test_read_budget.py` (`6128ef8`, its own commit):** M08 had stopped isolating `check_satisfiable`. It padded the ledger beyond the K prefix and kept the tail, growing the file ~30 KB; in the `after-a-wide-close-out-then-claim` state that tipped `page_estimate` into the reduced regime — 638 lines to 446 against a K prefix ending at 462 — so `check_k_lines` co-fired. Red at this session's own claim commit and green at the two claim commits before it, so Session 257's close-out record moved the state, not the guard. The pad now comes back off the tail, holding file length constant where the ledger is already large enough; measured isolating in all seven modelled states. Third repair of this mutant (Session 256 fixed it twice).
- **The review gated the commit.** Five lenses — guard correctness, mutant quality, stale prose, ruling fidelity, future states — then a skeptic per finding: 38 agents, 33 findings raised, **11 confirmed, 22 refuted**, all 11 fixed before the commit. The major one is the session's own: the front matter still said a row's non-span cells were "guarded by nothing, which `BACKLOG.md` files" — both halves falsified by this very diff, in the file it had just declared guarded, with the parallel sentence in `CLAUDE.md` updated and this one left. Also fixed: `"Everything above ## ACTIVE TASK is now guarded prose"` was an overclaim (four front-matter sentences delete green; the claim is now scoped to what is enforced); "the census guard exists for the prose files outside this one" contradicted an edit 46 lines above it; the composed write-once sentence hard-coded its leading article, so the **eleventh** trim would have had to write "A eleventh trim" to stay green (both articles are composed now, with a test over every declared ordinal); the front matter's own "these nine trims" count sat inside the guarded region reached by neither scan arm (composed now); and `STANDING` was doubling as a second, unclassified scan-exemption list (branch dropped — it covered nothing).
- **Front-matter budget.** The prose repairs first pushed the front matter to 8,141 B, which a trim's own row would have carried to 8,329 against the 8,192 B budget — caught by `test_read_budget.py`'s trim states, not by inspection. Compressed to **7,839 B** by merging a duplicated enumeration.
- **Verified:** census guard 35 tests, read-budget guard 47, full gate **1395 passed + 9 live-skipped @ 97.98% coverage** (was 1385, Session 257 baseline); `ruff check .` and `uv run mypy` clean; all eleven `docs/architecture-history/*.verify.sh` proofs green in **both** modes. `README.md`'s repo-map rows sum to the 1,404 collected.

### 2026-09-14 — `CHANGELOG.md` leaves the read budget, by the operator's ruling on that file alone (Session 257)

The operator ruled remedy (c) of this file's `BACKLOG.md` item: *"Rule (c) for CHANGELOG.md — take
it off the read budget."* The file is never read whole, only searched, and what a search finds is
not kept, so what a default `Read` of it delivers decides nothing and its size (2.5× the refusal
ceiling) is no longer a defect. The ruling names this file alone; `PROJECT_LEARNINGS.md` stays in
scope. Recorded in `docs/methodology/PROJECT_CONVENTIONS.md` §5 and
`docs/planning/ledger-budgets-review.md` §16.

- **`tests/test_read_budget.py` (`8b6b19a`).** `CHANGELOG.md` leaves `MANDATED` and
  `KNOWN_REFUSED`; `PROJECT_LEARNINGS.md` is now the one declared exemption, and `M02`/`M14` follow
  it. The guard composes the budget's scope into a sentence, full stop included, that `CLAUDE.md`
  and `PROJECT_CONVENTIONS.md` must each state once — the scope was prose nothing read, and the one
  thing this ruling changed. New mutant `M20` appends `CHANGELOG.md` to that sentence's list and is
  caught; without the sentence, or without its full stop, it fires no check. The 2.49 B/token page
  floor was measured on this file and stays: the three files in scope measure 2.620–2.861, and a
  re-tune needs its own ruling.
- **Not code:** the `BACKLOG.md` item now records only this file's order — four July entries above
  the September ones — as an operator call. An adversarial review (9 agents) confirmed five findings
  and refuted none; all five were fixed before the commit. Verified: 1,385 passed + 9 live-skipped;
  `ruff` and `uv run mypy` clean; all eleven proofs green in both modes; both guards 72/72.

### 2026-09-14 — The ninth ledger trim lands under the byte rule, and the guards stay isolating in every state it reaches (Session 256)

The ninth trim of `SESSION_NOTES.md` and the first under Session 255's byte rule: Sessions 248 → 242
moved into `docs/architecture-history/SESSION_NOTES-S248-through-S242.md` beside its proof. What
earns this entry is the test logic the trim had to change first — each change found by modelling a
state the trim creates, reproduced red, then fixed.

- **`tests/test_read_budget.py` (`3a141ec`, `8e6bdca`).** Two new `test_next_state` cases:
  `after-a-trim-then-its-close-out` and `after-a-wide-close-out-then-claim` (the widest close-out every
  live check accepts, then the next claim). Each was red before its fix. `M04` now joins the K records'
  bodies before growing them and gives its overshoot back from the next record, so neither
  `check_k_lines` nor `check_satisfiable` can co-fire; `M01` pads at the ledger's own line width; `M08`
  keeps the ledger's tail. The lines-arm remedy offers a trim only above the fire threshold.
- **`docs/architecture-history/SESSION_NOTES-pointer-collapse-S254.verify.sh` (`3a141ec`).** `M50`
  derives its gap span from the newest live row; a hard-coded span became an overlap once the ninth
  row existed, leaving `R7 LIVE-GAP` reached by no mutant. Self-test output unchanged at the time.
- **`tests/test_session_notes_census.py` (`8e6bdca`).** Six mutants derive their anchors from the shard
  set instead of carrying hand-typed copies of composed sentences, so a trim no longer re-edits them.
- **Not code, recorded for completeness:** the new proof adds no assertion — the operator's ruling F
  covers the bequeathed `L15` — rewrites `L11` to the byte rule, re-targets every operand that read
  retired prose, and strengthens four inherited arms after an adversarial review (12 agents, 38
  findings). Verified: 1,384 passed + 9 live-skipped; `ruff` and `uv run mypy` clean; all eleven
  proofs green in both modes at `9342637`; census guard 25/25, read-budget guard 46/46.

### 2026-09-10 — The mandated-read files have byte budgets, held against the working tree on every CI run (Session 255, Option D widened of `docs/planning/ledger-budgets-review.md`)

Executes the operator's ruling of 2026-09-07 (§13.1: Option D, second, *widened* to `SESSION_NOTES.md`, `BACKLOG.md`, `CHANGELOG.md` and `PROJECT_LEARNINGS.md`), with three parameters ruled on 2026-09-10 after they were measured: **K=2** with a byte trim trigger; the two files already refused outright **declared, guarded and filed**; `BACKLOG.md`'s plain-language index **guarded whole**. The line rule (1,500 / 1,050 / 4) is retired, not re-tuned.

- **Measured first, by default-`Read` probes in this harness.** A file over 262,144 B is refused with zero content; one of at most 25,000 tokens returns whole; past that a partial view delivers `floor(0.85 × 25,000 × L / T)` lines — a page sized by the WHOLE file, so 20 long lines appended at the bottom cut an identical head from 730 lines to 299 — and 0.7 of that when `T × bytes(page) / B` exceeds the cap. An explicit `offset`/`limit` `Read` over the cap returns the exact token count with zero content, refused files included. One `Read` of `SESSION_NOTES.md` delivered the front matter and **two** complete records; every K figure in circulation had counted claim stubs. K=2 fit at all 41 commits since Session 239; K=3 at a minority.
- **`tests/test_read_budget.py` (new).** Ten checks over the working tree, no git: the ceiling (with a self-expiring `KNOWN_REFUSED` exemption), the front matter at most 8,192 B, the front matter plus the two newest non-stub records inside a page in BYTES and in LINES (both measured regimes, worst case over the measured bytes-per-token range), `BACKLOG.md`'s index the same way, that the retention rule has a compliant cut, the claim-stub template, and every budget sentence exactly once in `CLAUDE.md` and `PROJECT_CONVENTIONS.md`. Eighteen mutants; a synthetic test proving the reduced-page branch load-bearing; unit tests pinning the stub classifier and the fence rule against every historical shape found; and a battery that fails if any check is the sole catcher of none, re-run in the states the next close-out, the next claim and the next trim produce (learning #241).
- **`CLAUDE.md`**: the retention bullet rewritten to bytes, with remedies per arm and an instruction that every close-out runs the guard; the ninth trim told to REWRITE `L11` and re-target every operand that reads retired prose; `--no-cov` added to the gate command (learning #57); stale sentences corrected. **`docs/methodology/PROJECT_CONVENTIONS.md` §5** records the ruling, the measurements and the limits; **`docs/planning/ledger-budgets-review.md` §14** the premises this session measured false. **`README.md`**: repo map and test counts. **`SESSION_NOTES.md`**: two front-matter sentences naming the working-tree readers. **`BACKLOG.md`**: the two refused files filed as remediation items; the row saying E's criterion needed D's budget resolved.
- **Two adversarial reviews gated the commit rather than following it.** The first (11 agents): 45 findings, 36 survived refutation, plus 3 from a completeness critic — a second page regime the guard could not see, an empty heading and a trim session's off-template claim counted as records, mutants vacuous in a plausible next state, a remedy wrong for the lines arm, a front-matter budget whose justification ignored claim stubs (10,240 → 8,192 B), and a K=2 rationale that misread `SESSION_RUNNER.md`. The second (5 agents) verified the fixes and found three more: no green state for the session's own close-out, a stub-classifier regression on two historical claims (Sessions 193 and 226), and a page arm with no margin. All fixed before commit; a 30-state future-state sweep is green in every healthy state, and its reds are genuine budget breaches.
- **Verified:** 1,382 passed + 9 live-skipped; `ruff` and `uv run mypy` clean (68 files); ten proofs green in both modes; census guard 25/25. Commits `85865d5` (claim), `e768e1f` (guard, rule and prose), then this documents commit and the close-out.

### 2026-09-09 — The ledger's front matter is collapse-on-write, and the rule is asserted rather than announced (Session 254, Option E retroactive of `docs/planning/ledger-budgets-review.md`)

Executes the operator's ruling of 2026-09-07 (§13.1: *Option E, applied retroactively, first* — ahead of Option D, on §13.3's arithmetic). Eight trims had each written a prose pointer block into `SESSION_NOTES.md`'s front matter; five became one table at Session 246 and the three still standing are rows 6, 7 and 8 of it now. **Front matter: 284 lines / 23,029 B → 94 / 7,184.** Nothing was archived, no record moved, and the replaced prose is readable at `8c9bb35` and embedded verbatim in the new proof.

- **The 276 replaced lines were guarded by nothing, and that is measured with controls.** Three perturbations of the working tree, restored from a copy each time (never `git checkout`): corrupting the live "authority" block's routing clause *and* one of its size figures left **all nine proofs GREEN**; **deleting all three prose blocks outright left all nine GREEN**; rewriting only the C-series table's opening line turned exactly one red. The apparatus could not see the region it existed to describe.
- **Four figures in that region had rotted, and one is dated.** The live block claimed its filename sweep returns 23 files; measured 26 at HEAD and **23 at its own cut `4ab6306`** — true when written, silently false since, with three nameable files added (`SESSION_NOTES-pointer-collapse.verify.sh`, `tests/test_session_notes_census.py`, `docs/planning/ledger-budgets-review.md`). `CLAUDE.md` and the table's own prose both said `C0–C6` / *36 mutants* where the file has eight assertions and 46. A truncated sentence ended mid-clause at the old line 227.
- **`docs/architecture-history/SESSION_NOTES-pointer-collapse-S254.verify.sh` (new, the `R`-series, R0–R8, 48 mutants).** Lettered `R` — not an `L`, and not an §8 option letter — for the collision reason Session 245 recorded. R0 pins the replaced prose; R1 confines the front matter to **one** declared replacement with **zero** substitutions (nothing survives to be falsified); R2 holds the records byte-identical; R3 re-derives **all eight** rows from each shard at its own add-commit and composes four self-arithmetic phrases; R4/R5 carry provenance and assertion credit; **R6, R7 and R8 read the WORKING TREE.** `R7` proves the `archived` spans tile the history with no gap or overlap *and* that the newest archived session plus one equals the oldest record on disk — which is what licensed deleting the nine prose routing clauses instead of keeping them in parallel. **`R8` enforces the rule**: a prose pointer block or a *"the N blocks below are frozen"* claim in the front matter goes RED, so a later trim cannot quietly revert to prose.
- **`docs/architecture-history/SESSION_NOTES-pointer-collapse.verify.sh`:** `C6`'s live anchor was re-declared as `LIVE_HEAD` (the frozen `NEW_TABLE` could not be re-pointed — `C1` pins it at the proof's own add-commit forever), and a new `C6/SUPERSEDED` arm requires the **old** opening line to be absent, so the substitution is proved rather than merely permitted. `M47` added.
- **The arm sweep caught a weakening this session introduced.** Re-anchoring `C6` made a working tree rebuilt from `after` contain no `LIVE_HEAD`, so `C6` began firing for every table mutant and `C0/FIGURE` and `C3/FIGURE` silently stopped being sole objectors. Measured: **17 unique of 35 before, 16 of 36 after** — one arm gained, two lost, with *both* modes green throughout. `with_table` now swaps only the ROWS into the real tree; the count is **18 of 36**. The inherited "17 of 35" was re-derived against the pre-collapse tree and reproduced exactly, so Session 253's published figure was right and the regression was this session's.
- **`tests/test_session_notes_census.py`:** two `FROZEN` entries **dropped**. Both exempted numerals in `CLAUDE.md`'s Session-246 bullet, which this session rewrote; the companion staleness test correctly went red. They are dropped rather than re-pointed because the replacement prose carries no numeral the fail-closed scan objects to — verified by running the scan without them, not by inspection.
- **`docs/methodology/PROJECT_CONVENTIONS.md` §3.1:** collapse-on-write declared as convention, with the ruling recorded outside `CLAUDE.md` as §10 dragon 1 requires. `README.md` repo map gains the new proof. `CLAUDE.md`'s bullet rewritten and now **states no assertion or mutant count on purpose**.
- **A 116-agent adversarial review landed AFTER the collapse commit and changed the code (follow-up commit).** 110 findings verified — 19 survived, 91 refuted — plus 12 from a completeness critic. Eight survivors independently re-derived rows 6–8 field-for-field and agreed exactly. **Five were real defects, all fixed:** (1) **`R7/FRONTIER` had no green state after the next trim** — it compared the FROZEN declared `ROWS` (max archived 241) against the LIVE oldest record id, so a ninth trim fails it while extending `ROWS` fails `R3 SET` and editing `NEW_BLOCK` fails `R1`; the C3 "rows cannot be extended" trap rebuilt inside the new assertion, by the session that documented it. `R7` is now split into a declared-tiling half and **live** arms that parse the `archived` column off disk, verified by simulating a ninth trim three ways (archive-without-row → RED, archive-and-row → **GREEN**, overlapping row → RED); `M49`/`M50` isolate the new arms. (2) The record's guidance to the ninth trim, which was impossible given (1). (3) **`R8`'s family regex, inherited from `C7`, never matched the singular `**The block below is frozen`** — and both forms stood in the deleted region, so `C7` could only ever have caught one of the two members of its own family. (4) The front-matter byte figure was wrong in two live files (`94 / 7,179`; measured **7,184**). (5) `docs/planning/ledger-budgets-review.md` pinned `SESSION_NOTES.md:185`, `:256` and `:277`, all three inside the deleted region — and `:277`'s backticked "quotation" never existed in that file at any commit; six sites annotated.
- **Two residual gaps filed, not fixed:** the standing block is guarded only at its table rows and spans (a six-way control showed deleting the routing sentence, the write-once rule, the banner-snapshot rule or the legend leaves every proof green — an improvement in **cost**, not in **safety**), and **neither collapse proof is guarded by anything** — not `L10`'s declared list, not the census guard's glob, and CI errors only when *zero* proofs are found.
- **Verified:** ten proofs green in **both** modes; whole-assertion neuter sweep shows every one of R0–R8 is the sole objector to ≥2 mutants; per-arm sweep published in the header (44 statements, 17 uniquely catching, the 27 grouped by cause). Census guard **25/25**. Full gate **1,338 passed + 9 live-skipped**; `ruff` clean; `uv run mypy` clean (68 files). Empirically re-probed: a default `Read` now delivers **lines 1–754 of 2,539, front matter + 5 complete records** where it delivered 4 before, and the front matter is **13.1%** of the delivered prefix against 41.3%.

### 2026-09-09 — A ledger proof that could no longer fail is repaired, and CI now runs both proof modes (Session 253)

`docs/architecture-history/SESSION_NOTES-pointer-collapse.verify.sh` exited 0 on its ordinary run and **exited 2 on `--self-test`** from Session 249 to Session 252 — 8 consecutive commits, 4 sessions — with two of its 46 mutants surviving. This entry exists because the repair adds a `.github/workflows/` job; the operator's ruling (Session 252) put this ahead of Options E and D.

- **Root cause — a mutant is a fixture, and fixtures rot silently.** `M16`/`M17` corrupted the working-tree copy of `SESSION_NOTES.md` with `live_wt.replace(NEW_TABLE, …)`. `NEW_TABLE` is a 51-line pinned literal of table **plus prose**; `C6` reads only the composed rows and the table's opening line. At `5243242` Session 249 correctly repaired the read-cap sentence inside that prose, `NEW_TABLE` stopped being a substring of the live file (4,273 of 4,369 characters still matched — the divergence is the literal's last sentence), both `.replace()` calls became no-ops, and `C6` was handed pristine input. `C6` objected to nothing because nothing was wrong. Re-derived independently of the Session 252 filing: last matched at `b1d761f`, broke at `5243242`.
- **The filed remedy was tested and rejected on measurement.** `BACKLOG.md` proposed "re-pin one literal". `C1` resolves its `after` operand at the proof's own add-commit `2b8c9c9` forever, where the old sentence is frozen — a re-pinned `NEW_TABLE` fails `C1 TABLE MISSING` **and** `C1 CONFINEMENT` permanently. There is no green state containing one, so the repair went where that item's own last paragraph points: the mutants.
- **`docs/architecture-history/SESSION_NOTES-pointer-collapse.verify.sh`:** new `self_test()` helper `live_table_replaced(lw, put="")` strips exactly what `C6` counts — `rows_of(NEW_TABLE)` and `new_table.split("\n")[0]`, **the same expressions `C6` itself uses**, neither carrying a trailing newline. `M16` removes them; `M17` puts `OLD_BLOCKS` where the opening line was. No pinned literal, assertion or shard was touched. Measured across three drift classes: a row-content edit and an opening-line edit turn **both** modes red together; a prose edit inside the block — the exact class that caused this bug — leaves both green.
- **The class, not the instance:** the `--self-test` driver now compares each mutant's 10-tuple of arguments against the pristine tuple and reports **`NO-OP`** — a distinct, hard failure — for any mutant that changed nothing. "SURVIVED" named the wrong culprit (it says the assertion missed a corruption, when the corruption never happened); Session 252 needed a bisect to learn the difference, which the self-test now prints on the first line. Verified by restoring the two pre-repair expressions: `NO-OP`, exit 2.
- **`.github/workflows/ci.yml`:** new `proofs` job running **both** modes over `docs/architecture-history/*.verify.sh`. `fetch-depth: 0` is load-bearing — the proofs resolve blobs by sha and locate add-commits across the whole history, and under `checkout@v4`'s default depth-1 clone all nine go red (measured). The step also greps for each proof's `SELF-TEST OK` line: every proof ignores an unrecognised flag, so `--self-test` against a proof lacking the mode silently performs a second plain run and exits 0.
- **Neither mode subsumes the other, measured with controls — this refines the operator's F ruling.** A plain run cannot see a proof that has stopped being able to fail. `--self-test` cannot see real corruption: append a line to any archived file and its `--self-test` still **PASSES**, because every mutant then fails for the corruption's sake and scores as "caught". The plain run catches all eight archives, but not always via the corrupted file's own proof — that holds for six; for the two oldest (which define only `L0`–`L4`, none of which reads disk) `L9` in every proof from the S227 one forward is what catches it. `CLAUDE.md`'s trimmed-file bullet now publishes the two-mode loop and why each half is necessary.
- **Adversarial review (7 lenses + per-finding refuters + a completeness critic; 42 agents, 0 errors).** 10 findings confirmed, 24 rejected. Three changed the code: the dangling "filed in `BACKLOG.md`" claim (found by 7 of 7 lenses — the filing now exists), a needle mismatch where the helper searched `row + "\n"` while `C6` counts a bare `row` (**the same failure shape as the bug being repaired**, caught before shipping), and the CI silent-pass hole. The critic found the DONE gate's own `VERIFY` command was `--self-test`-only and therefore passes on an already-corrupt world. One rejected finding was re-measured by hand and the rejection confirmed: the header's "35 failure-emitting statements" is correct (33 `out.append` + 2 early `return ["C…`), so nothing was filed against it.
- **Filed, not fixed:** the `NO-OP` guard compares whole tuples, so a *partially* inert mutant (7 of 46 change more than one slot — `M27`, `M28`, `M38`–`M41`, `M44`) stays invisible; all seven read frozen operands that cannot drift, so nothing is broken today. Also filed: CI fires on push, and this clone was 7 commits / 4 sessions ahead of `origin/master` when measured, so CI's detection latency matches the outage it repairs.
- **Verified:** all nine proofs green in **both** modes (the corrected gate prints nothing); `M16`/`M17` re-broken individually and together → `NO-OP`, exit 2; `C6` neutered → `M16`, `M17`, `M34`, `M35`, `M43` survive, matching the header's own table; the CI step run verbatim → exit 0, 9 groups, 0 errors, and exit 1 on both a corrupted archive and a re-broken mutant. Census guard **25/25** (it went red once, correctly, on a shard-census claim in `CLAUDE.md` — reworded to state no census rather than exempted). Full gate: **1338 passed + 9 live-skipped @ 97.98%**; `ruff` and `mypy` clean.

### 2026-08-26 — The census guard stops requiring two sentences that measurement showed to be false (Session 249, Option A of `docs/planning/ledger-budgets-review.md`)

Session 248 measured the premise this project's ledger budgets were derived from — *"an agent `Read` silently stops at 2,000 lines, with no error and no missing-data marker"* — and found it false. The operator ruled **Option A**: correct the premise wherever it is asserted as fact in a live file. The prose corrections are documentation and carry no entry; **this entry exists because two of those sentences were composed and REQUIRED by `tests/test_session_notes_census.py`**, so correcting them is a change to test logic.

- **The guard was enforcing a falsehood, and could not have known.** `expectations()` composed `"(The {prior_word} newer shards are under the 2,000-line cap today;"` for `CLAUDE.md` and `"the {prior_word} newer ones read whole today"` for `BACKLOG.md`. Both derive the shard count correctly and both assert a cap claim the guard has no way to check: it measures **lines** from disk, and the real cap is denominated in **tokens**. Measured by probing all eight archives with a default `Read`, three of the seven newer ones truncate. The guard would have held `BACKLOG.md` red against the true sentence and green against the false one.
- **`tests/test_session_notes_census.py`:** both composed literals re-cut to `"of the {prior_word} newer shards truncate under a default"` / `"of the {prior_word} newer ones truncate under a default"` — the derived N-1 count survives, the cap claim does not. The obsolete `FROZEN` entry exempting the `2,000` numeral (whose own rationale asserted *"the 2,000-line read cap is a constant"*) is replaced by two honest per-occurrence classifications: the measured truncation count, and a session number plus the `read_cap` constant. A `WINDOW` comment naming an example string that no longer exists in those files was corrected in the same pass.
- **The fail-closed scan did its job, on the author.** A first draft of the `BACKLOG.md` paragraph used "archived file" where "shard" was the natural word, and passed — because the net triggers on proximity to the word *shard*. Rewritten with the natural vocabulary, the scan went red on three numbers and named each one; one was a line count the guard **already composes elsewhere** and was deleted rather than exempted, and the remaining two were classified. **Rewording around the trigger is a live way to leave text unchecked** — recorded here because the suite cannot detect it.
- **Not fixed, recorded:** `"read_cap": 2000` is a hand-declared constant in three **write-once** proofs; `L12` fails a cut whose shard's LINE count reaches it, emitting *"truncates in silence"*. The S235 proof's arm passes — 1,057 < 2,000 — while a default `Read` of the file it guards returns a partial view at 30,683 tokens against a 25,000-token cap. All three are pinned byte-for-byte by `L10` and cannot be corrected; a future proof must declare the cap in tokens or bytes, or not at all.
- **Verified:** full gate **1338 passed + 9 live-skipped** (unchanged from the Session 247 baseline — no test added or removed, two expectations re-cut); census guard **25/25**, 2.2 s; all **nine** `docs/architecture-history/*.verify.sh` GREEN before and after, which is expected and proves nothing about live prose: every shard proof resolves its prose operands from its own trim commit.
- **Unchanged intentionally:** no `src/`, `packages/`, `scripts/` or `.github/workflows/` change. The retention rule's three numbers (1,500 / 1,050 / 4) stand **unchanged and now unjustified** — re-tuning them is Options B-E and a separate session. `L11`'s three declared literals were checked and still match `CLAUDE.md` verbatim, so a ninth trim inherits `POLICY` unchanged.

### 2026-08-26 — The four prose files are held against the ledger archives on every CI run (Session 247)

Closes Session 246's what's-next #1 under two operator rulings: **pytest-in-CI** rather than another standalone `.verify.sh`, and a **fail-closed allowlist** rather than region markers. The trim apparatus guards a historical snapshot, not the live tree — every shard proof resolves its prose operands from its *own* trim commit, so from the moment a trim lands, no census copy in `CLAUDE.md`, `README.md`, `BACKLOG.md` or `docs/methodology/PROJECT_CONVENTIONS.md` is checked by anything until the next trim. **33 of the 42 commits** that have touched those files since the first trim fell in such a window, and only **3 of the 19** literals the seventh trim's proof requires are still present in the working tree.

- **`tests/test_session_notes_census.py` (new, 25 tests):** seven checks over a single `Census` snapshot — span tiling, proof presence, banner derivability, composed claims, filename set equality, the fail-closed scan, and allowlist staleness. Every fact is derived from **files on disk and never from git**: `ci.yml` checks out at the default depth 1, so a git-derived fact would behave differently in CI than locally. Each expected sentence is **composed** from the measurement rather than compared against a declared integer, so a failure prints the exact text the file must carry. The scan is fail-closed: a number token within `WINDOW` characters of "shard", or quantifying a shard-set head noun, must fall inside a composed sentence or a per-occurrence `FROZEN` entry, or the suite is red until someone classifies it. `FROZEN` holds per-occurrence strings, never whole-file exemptions, and a companion test fails when an entry stops matching.
- **17 mutants ship as tests**, so CI proves the guard can fail rather than only that it passes. Two exist solely to isolate a check that every other mutant reaches transitively: `M16` renames a shard to mis-cut the span *and* rewrites all four prose files to agree, and `M17` strips a banner's trim session *and* writes the prose to match the fallback that then applies. The neuter loop is published in the module header; **all seven checks are load-bearing** and no mutant survives.
- **Control experiment:** with `CLAUDE.md`'s shard count corrupted 8→5 and one routing clause widened, **all nine `docs/architecture-history/*.verify.sh` proofs stay GREEN** and this guard goes RED on `check_composed` and `check_scan`. That is the hole, measured in both directions.
- **Performance defect found and fixed during the build:** `_in_scope` re-scanned the whole file for head nouns once per *candidate*, making the suite quadratic in number tokens — a multi-minute run. Hoisting that scan to once per file took the module to **2.1 s**.
- **`README.md`:** the new file added to the `tests/` map; headline total corrected **1313 → 1338**. **`CLAUDE.md`:** one bullet in the trimmed-file section telling a trim session the guard exists and must be re-run before committing — written with **no numerals**, referencing `WINDOW` and `FROZEN` by name, because a numeral there would be another copy nothing derives. **`BACKLOG.md`:** one item filed (not fixed) — `README.md`'s per-directory test counts have drifted by a combined 131 across exactly three rows, swept rather than sampled.
- **Verified:** full gate **1338 passed + 9 live-skipped @ 97.98% coverage** (was 1313 passed at the Session 243 baseline); `ruff check` clean; `mypy` unaffected (it scopes to `packages`, not `tests/`). All **nine** shard proofs green before and after — this guard is not one of them: it reads no git, enforces no write-once, and the `for f in docs/architecture-history/*.verify.sh` loop neither runs it nor is disturbed by it.
- **Unchanged intentionally:** no `ci.yml` change — `uv run pytest -q` already collects `tests/`, so the guard runs on every push and pull request as-is. No `src/` or `packages/` change.

### 2026-08-24 — `uv.lock` triggers the tutorial deploy, and `--locked` makes it authoritative (Session 243)

*Entered retroactively at Session 244 under the amended §2 cadence gate — see "Unchanged intentionally" below.* The public tutorial site is built by `uv sync --extra docs`, so `uv.lock` pins the theme it ships (`mkdocs-material 9.7.6`). `uv.lock` was **not** in `publish-tutorial.yml`'s `paths:` filter, so a theme bump deployed nothing at bump time and was first exercised weeks later by an unrelated `docs/*.md` commit — failing there, attributed to the wrong change, and blocking that change's legitimate deploy. Filed as a Session 237 gotcha and deferred by four sessions after it on a frequency objection that turned out to be false.

- **The premise that deferred it was measurably wrong (`9522bbd`).** The filed objection was *"adding `uv.lock` would fire a public deploy on every dependency bump."* Root `pyproject.toml` was **already** in the filter, and **13 of 13** `uv.lock` commits in 496 also touched it — every dependency bump already fired a deploy. Simulating both filters over the full history with GitHub's glob semantics (`*` does not cross `/`): **45 firing commits before, 45 after, 0 newly firing.** The simulator's sanity probes confirm it could see a delta (a synthetic lock-only push flips `False`→`True`), so the zero is a measurement, not a stuck number.
- **`.github/workflows/publish-tutorial.yml` — the trigger (`9522bbd`):** `uv.lock` added to `paths:` (7 entries now), with the 13-of-13 measurement recorded in a comment beside it. The residual case the line covers is a **lock-only** bump (`uv lock --upgrade`), which has occurred 0 times here and is available today — PyPI carries `mkdocs-material 9.7.7` against the lock's 9.7.6.
- **`.github/workflows/publish-tutorial.yml` — the authority (`9522bbd`):** `uv sync --extra docs` → `uv sync --extra docs --locked`. Without it a bare `uv sync` may **re-resolve** at deploy time, so `uv.lock` governed the published theme only while it and `pyproject.toml` happened to agree, making the new trigger a half-truth. `uv lock --check` exits 0, so the flag costs nothing now and converts a future drift from a silent re-resolve into a red job.
- **A sibling gap closed as a side effect, and measured rather than filed.** `packages/data-agent/pyproject.toml` is invisible to the filter (GitHub patterns are root-anchored). Measured: 7 commits touch it, **6 already fire**, the 1 that does not predates the workflow, and it declares no docs dependencies — so any resolution-affecting change to it moves `uv.lock`, which now fires.
- **Verified:** YAML parses with `uv.lock` under `push.paths`; `uv sync --extra docs --locked` exits 0; `mkdocs build` and `scripts/check_site_assets.py --require-css` both clean, run via `UV_PROJECT_ENVIRONMENT` in a scratch venv so the repo's own `.venv` kept its `agents`/`ui`/`dev` extras (Session 237 gotcha 3); `uv.lock` unmodified. Full gate **1313 passed + 9 live-skipped @ 97.98% coverage** — identical to the Session 241 baseline. All seven shard proofs green before and after.
- **Unchanged intentionally:** no `src/`, `packages/`, `scripts/` or `tests/` change — this deliverable is one workflow file. Under the §2 cadence gate **as written at the time**, that earned no `CHANGELOG.md` entry, and Session 243 correctly took none. The operator ruled at its close that `.github/workflows/` belongs inside the gate; §2 was amended in Session 244 and this entry added then, dated by the landing commit above and appended rather than backfilled per §1. **`BACKLOG.md`** gained one item (the docs toolchain's missing version ceiling — the half of the option not taken) rather than losing one: this item had never been filed there, which is why it was carried five times.

### 2026-08-24 — Both fail-open paths closed in the wiki-publishing pair, and the pair gets its first tests (Session 241)

`scripts/publish_wiki.sh` and `.githooks/post-commit` run **unattended from a git hook** and both failed open: a failure was indistinguishable from "nothing to do". Filed as two items in Session 234 (from `repository-rename.md` §8.1 dragons 3 and 4) and explicitly filed to be bundled into one session. Neither file had ever had a test.

- **`scripts/publish_wiki.sh` — the empty-source wipe (`72b5718`).** The script mirrors the wiki clone with `rsync -a --delete`; its source guard tested that the directory **existed**, not that it held pages, so an emptied or half-populated source published the deletion and pushed it. **Reproduced against the pre-fix script, not argued:** an empty source exited **0** and printed `94d9d62..4aefc7c  master -> master` to the remote. Two guards added. (a) Refuse a source holding zero `*.md` files — `find -L`, because `[ -d ]` and rsync's trailing slash both follow a symlinked source while a bare `find` will not descend into one and would report 0 pages in a full directory. (b) Refuse a *disproportionate* deletion, measured with an `rsync --dry-run --itemize-changes` pass and compared against `git ls-files` in the clone: **a static page-count floor cannot express this guard** — set it low and it catches nothing, set it high and it goes stale the next time a page is added — so the deletions are measured against what is published now (fires above 2 deletions **and** above 25% of the clone). Directory deletions are excluded from the numerator: rsync itemises `*deleting assets/` for directories and `git ls-files` counts only files, and mixing the units refused a small subdirectory retirement the guard is meant to wave through. `MPC_WIKI_ALLOW_MASS_DELETE=1` overrides it and **says on stderr that it did** — a silently-suppressed guard is the same shape as the bug.
- **Two fail-opens inside the new guard itself, caught and closed before the commit (`72b5718`).** `rsync … | grep -c '^\*deleting' || true` masks an **rsync failure** as "0 deletions" and publishes blind; and when `grep` produces no output at all the variable is empty, so `[ "$PENDING_DELETIONS" -gt 2 ]` fails as a *malformed test* — which, inside an `if` condition, `set -e` does not catch, so the guard is skipped and the deletion ships. The dry run's exit status is now tested explicitly and the deletions are counted in a `while`/`case` loop that cannot fail.
- **`.githooks/post-commit` — the silent skip (`72b5718`).** It decided from `git diff-tree --no-commit-id --name-only -r HEAD`, which prints **nothing** for a merge commit and nothing for the root commit without `--root`; both read as "no wiki paths changed" and it exited 0 in silence. Now `--diff-merges=first-parent --root -z`, piped through `tr`. Three findings behind those flags: **`-m --first-parent` is not the fix** (`diff-tree` ignores `--first-parent` when `-m` is given and emits the union of the diffs against *every* parent — measured); `--root` closes a second silent hole the filed item never named; and `-z` closes a third — `--name-only` honours `core.quotePath`, so a page named `Café-Notes.md` arrives as `"docs/wiki/…/Caf\303\251-Notes.md"` and the anchored match cannot see it, whereupon the hook announces a confidently false reason. Git's exit status is now tested rather than piped into `grep` (a git failure writes nothing, which read as "no wiki paths"), and the match uses a here-string, not a pipe — `grep -q` closing the pipe on its first match plus `set -o pipefail` would have turned a *match* into a skip. A stale path prefix is reported and exits non-zero, but only when `docs/wiki/` itself exists, so a checkout predating the wiki is a normal skip rather than a false alarm on every commit. Every decision not to publish is announced on stderr.
- **A gap the filed diagnosis did not reach, verified and filed rather than quietly fixed:** git runs **`post-merge`**, not `post-commit`, when it creates a merge commit itself. `post-commit` sees a merge only when the user finishes a *conflicted* merge with `git commit`. A clean `git merge` or `git pull` carrying a wiki change therefore still publishes nothing — a new `BACKLOG.md` item, pinned by `test_a_clean_merge_never_reaches_this_hook`, which asserts the publisher does *not* run and reddens if git's behaviour ever changes.
- **`tests/scripts/test_wiki_publishing.py` (`72b5718`):** 38 tests, the first either file has had, against synthetic git repositories (a bare "remote", a clone, a source repo) with no network and no contact with the real wiki clone. **Falsified before being trusted:** run against the pre-fix scripts in a throwaway worktree, 11 failed and 12 passed — the 12 pin pre-existing guards. **Then mutation-checked:** 15 mutants, one per guard arm, **15 killed**, each by a named test — including two arms that an adversarial review proved were *algebraically identical* at the original fixture's 8 pages and so deletable with the suite fully green (the fixture is now parametrised so each arm is pinned alone), and the first M13 mutant, which was vacuous and was rewritten until it bit. One test pins the hook's trigger prefix to the publisher's `SOURCE_DIR`: the wiki path is written out in both files and nothing kept them aligned, which is the "stale prefix" failure mode itself.
- **Documentation the change falsified, repaired by content rather than by digit (`e2a8ba5`):** the commit grew `publish_wiki.sh` by 66 lines and moved every line number three live documents cite. `CLAUDE.md`'s D-R5 rule pinned `publish_wiki.sh:19`, `:24`, `:42` **permanently** and two had moved to `:29`/`:48`, so the rule protected unrelated text — restated by content. `repository-rename.md` §7.2's re-runnable criterion asserted "exactly `:19`, `:24`, `:42` — never 0" and now printed `:19, :29, :48`, which the plan's own "a printed path is a miss, no judgment call" rule reads as a rename regression — restated as `grep -c … # -> exactly 3`, which D-R5 makes true permanently. Dragon 4's "**And no test will ever catch it** … the hook and the publisher have **zero** test coverage" was true when written and is now false — annotated SUPERSEDED, with its *advice* preserved. `enterprise-migration.md` §2.6's seven coupling rows and C4 step 4 re-pointed, the new test file added as an eighth coupling, and a re-derive warning added under the table so the next drift is not silent.
- **Verified:** full gate **1313 passed + 9 live-skipped @ 97.98% coverage** (was 1275 passed, Session 240 baseline); `ruff check` clean. All six shard proofs green — the newest reads `CLAUDE.md` and `BACKLOG.md` live and both were edited. C4/C5 arm 2 re-run over this repository still returns `claims-model-starter`, so the criterion still fires on the case it exists for; §7.2's residue arm still empty. **Runtime-verified against the live wiki clone** (25 tracked files, 0 pending deletions): the hook announces its skip with the correct SHA, the publisher still prints "no changes to publish" in parity, and the clone's HEAD is unmoved. Each of the three commits printed the new hook's announcement as it was made.
- **Unchanged intentionally:** `publish_wiki.sh`'s three `claims-model-starter` lines (D-R5 — GitHub's rename moves a URL, never a directory on disk). No `post-merge` hook added: that is the new backlog item, and adding a hook that can push to a public wiki is not a change to make as a side effect of closing two others.

### 2026-08-22 — The published tutorial site is styled again, and a deploy can no longer drop the theme silently (Session 237)

The public tutorial at `https://rmsharp.github.io/model_project_constructor/` had served style-less HTML — no CSS, no search, no navigation — since 2026-07-27, while `Publish Tutorial` reported **success in 11 seconds**. Session 234 bracketed the regression to a six-week window and named `mkdocs.yml`'s `exclude_docs` as the likely cause without being able to run a build; this session confirmed the mechanism from MkDocs' own source, fixed it, and closed the hole that let a 43-file loss ship green through four release phases.

- **Root cause (`70e968b`), read out of MkDocs rather than inferred.** `mkdocs/commands/build.py` calls `add_files_from_theme()` at `:289` and then **re-runs `set_exclusions()` at `:294`**. Theme files arrive with `InclusionLevel.UNDEFINED`, so that second pass applies `exclude_docs` to them as well as to `docs_dir` — which the option's name and documentation do not suggest. `b27cc98` (Session 182, Phase A1 of `docs/planning/enterprise-migration.md`) had inverted `exclude_docs` into a fail-closed allowlist whose first pattern is `/*`; gitignore-anchored at the root, that pattern matches the Material theme's entire `assets/` tree. Measured three ways: calling `add_files_from_theme` directly yields **43 files, all under `assets/`** — matching the 43 Session 234's bisect found missing; `pathspec.GitIgnoreSpec` confirms `/*` matches `assets/stylesheets/…` and that `!/assets/` un-matches it while leaving every `docs/` subdirectory excluded; and a local `mkdocs build` goes from **6 files to 49**.
- **`mkdocs.yml` (`70e968b`):** `!/assets/` added to the allowlist. The fail-closed property Phase A1 wanted is preserved — `docs/planning/`, `docs/methodology/`, `docs/architecture-history/` and `docs/wiki/` all stay excluded. The comment records the ordering mechanism so the line is not "simplified" away by a later reader who checks only the option's documentation.
- **`scripts/check_site_assets.py` (new, `70e968b`):** asserts every local `href`/`src` in the built HTML resolves to a file in the artifact, with `--require-css` for the one case reference-integrity cannot see (a site that consistently links nothing). Self-calibrating — no hard-coded file count to rot on the next theme upgrade. Handles the root-relative references MkDocs emits in `404.html` via `--base-path`, derived in the workflow from `site_url` rather than duplicated. Run against the pre-fix config it reports **9 dangling references and exits 1**; against the fixed config, 0.
- **`scripts/check_published_site.py` (new, `70e968b`):** asserts the **live** site serves what it links, polling because a `gh-pages` push starts an asynchronous Pages build. This is a different claim from the artifact check and catches a different class of failure (`ghp-import` dropping files, a missing `.nojekyll`, Pages serving stale or wired to the wrong branch). Run against the still-broken live site before the deploy it reported both assets 404.
- **`.github/workflows/publish-tutorial.yml` (`70e968b`):** build → assert the artifact → deploy → assert the live site. **The artifact check runs before `gh-deploy --force --clean`**, which writes a *parentless* commit to `gh-pages` (measured: `git rev-list --parents -n1` shows zero parents; `enterprise-migration.md` §2.3 — no git revert path), so a bad build stays a failed job instead of becoming an unrevertable publish. `BACKLOG.md` had asked only for a post-deploy assertion; both are kept because they prove different things. The `paths:` filter gains the two new scripts and a comment recording why it cannot fire on nested `docs/` changes.
- **Tests (`70e968b`):** 45 new tests in `tests/scripts/test_check_site_assets.py` and `tests/scripts/test_check_published_site.py`, stdlib-only and fixture-driven so they run in CI's test job (which installs `--extra agents --extra ui --extra dev` and never `docs`). Polling is exercised through an injected clock, so the retry logic is covered deterministically and instantly. **Two defects in the new code were found by these tests and fixed before commit:** `--require-css` reported "no stylesheet is referenced" when one *was* referenced but missing (false, and duplicated a problem already reported), and `check(fetch=probe)` bound the default at definition time so `monkeypatch.setattr(mod, "probe", …)` silently fell through to the real network — the symptom was a two-minute test-suite **hang**, not a failure. The regression guard for the second passes `--timeout 0` so a reintroduction fails in milliseconds instead of reproducing the hang.
- **Verified live, not asserted.** Six predictions were written down before the push and all six confirmed after: exactly two workflow runs fired for `70e968b` (CI + Publish Tutorial; GitHub's own *pages build and deployment* then ran for the new `gh-pages` commit); Publish Tutorial **success**, 7/7 steps, both new guards green with real output; `origin/gh-pages` `cc66dea` → `e0311a0`, **7 files → 50, of which 43 are `assets/`**, zero parents; the live root page **byte-identical** at 12,533 bytes and sha256 `b97eddcd…` (the HTML was never broken — only the assets were dropped, measured by diffing the candidate build against production *before* deploying); `assets/stylesheets/main.484c7ddc.min.css` and `assets/javascripts/bundle.79ae519e.min.js` **404 → 200** at 139,849 and 114,308 bytes; the wiki clone unmoved at `d85cc67`. The live check's polling earned its keep on its first run — attempts 1 and 2 reported `2 of 3 URL(s) not yet 200` while Pages was still building — so a single-shot `curl` would have failed the workflow on a correct deploy. Rendering confirmed visually in a browser (theme, search box, left nav and table of contents all present), per `DEVELOPMENT_WORKSTREAM.md`'s runtime-verification gate.
- **Recovery point:** `gh-pages` was captured to local branch `gh-pages-pre-s237` (`cc66dea`) before the deploy, alongside the existing `gh-pages-preA4` and `gh-pages-pre-rename` bisect branches.
- **Gate:** **1275 passed, 9 skipped** (was 1230 + 9); `ruff check src/ tests/ packages/ scripts/` and `mypy` both clean; CI run `32554400688` green 4/4.

### 2026-08-19 — One transient policy for both governance surfaces; the last hand-written measurement loop retired (Session 225)

Closes the `BACKLOG.md` item filed in Session 221. The Phase E eval harness had four live-measurement surfaces applying three different transient policies; the two governance ones were the sharp end, and they disagreed with **each other**.

- **The defect, verified in code rather than inherited from the backlog's summary.** `tests/eval/shadow_run.py`'s governance loop caught `IntakeLLMError`, did **not** retry, and scored it an immediate non-agreement — the Session 219 shape, where one event later measured at 1-in-60 decided a cutover verdict. `tests/eval/test_eval_live.py`'s governance test had **no handler at all**, so the same event aborted the gate: the measure-and-report driver and the assertion gate could not agree on what a blip means. Neither caught a transport error, and `classify_governance` wraps none — `AnthropicLLMClient._call_json` (`anthropic_client.py:363-370`) calls `messages.create` bare — so an `APITimeoutError` raised after the SDK exhausts its own retries propagated past both and **aborted a ~2.5-hour live run mid-measurement**. That is the hole Session 171 closed for interviews and Session 221 for SQL/QC.
- **`tests/eval/governance_sweep.py` (new, 316 lines, `2f853f7`):** `sweep_governance_agreement(cases, classify, *, n_samples, max_transient_retries, on_event)` samples each governance case and pools, mirroring `sql_sweep`'s two-tier exhaustion. `_TRANSIENT_SCORED` (`IntakeLLMError`) is retried then **scored** a non-agreement — the class is the intake client's catch-all over its whole parse path, so it *is* the measured capability, and excluding would shrink a denominator whose survivors are clean by construction (a provider failing 24 of 25 samples would score 1/1 = 100% and pass). `_TRANSIENT_EXCLUDED` (`APITimeoutError`/`APIConnectionError`) is retried then **excluded** and counted. Everything else propagates; `APIStatusError` is deliberately not caught (FM #18). The `classify` callable is injected, so the statistics are unit-testable with no API key — the same shape `interview_sweep` uses for `run_one`.
- **A seam failure never feeds the zero-tolerance row.** A scored exhaustion appends `False` to `cycle_matches` and `risk_acceptable` but leaves `laxer_misses` alone: no prediction means no risk tier, and fabricating a miss against `GOVERNANCE_LAXER_MISSES_MAX = 0` would turn one blip into a NO-GO — the pathology the item exists to remove. `shadow_run` already behaved this way; the behaviour is preserved and now tested at both the sweep and the driver level.
- **Both call sites rewired (`2f853f7`)**, each passing an event sink. `measure_provider` gained `governance_seam_failures`, `governance_excluded_transient` and `governance_transient_retries` as diagnostics (`eval_cutover` reads a fixed `CHECK_KEYS` set and ignores extras — pinned by a new test). `interview_sweep.py` and `sql_sweep.py` are **byte-stable**: hoisting one generic `_call_with_retries` into a shared module is a cross-module refactor `SAFEGUARDS.md` gates behind plan mode, so the near-duplicate is documented as a deferral rather than smuggled in.
- **Adversarial review, 23 agents over 5 lenses with 3-refuter majority panels (`fcb4366`).** 24 findings; all adjudicated, including the 18 past the verification cap and the 4 the panel killed. **Three docstring claims of mine were false and are corrected:** that all three blocks now apply one transient policy (they do not — `interview_sweep` *excludes* an exhausted seam error where the other two *score* it, and that difference is correct; what is true is that no hand-written loop remains); that this driver's SQL call site had the S221 missing-sink defect (it has passed `on_event=_warn` since `4e2c8ec`, Session 218 — the sink-less site was `test_eval_live`'s); and an enumeration in `_warn` that omitted the interview sweep it also serves. **Two facts the refuters reproduced and then ruled not-defects are now documented rather than dropped:** an exclusion leaves a *rate* unbiased but can only move the zero-tolerance `governance_laxer_miss` **count** toward PASS, and the tiers key on exception class, so `opencode`'s subprocess adapter lands its transport failures in the *scored* tier where an SDK provider's are excluded (filed, not fixed — it needs an adapter change).
- **Seven assertions had no test that could falsify them** (learning #99) and were closed in `fcb4366`: the summary line asserted labels but no values; "every note carries `str(exc)`" was checked with `any()` on one of three note kinds; the mixed-evidence rule was only exercised with the scored class *first*, leaving the `scored_exc`/`last_exc` split unfalsified; the same-length test never produced a seam failure; `transient_retries` was unasserted on the exclusion path; the `n_samples` default (now the live gate's only denominator) was unpinned; and `shadow_run`'s six-field governance mapping was invisible to every test because the stub returned all defaults.
- **Falsified, not assumed: a 12-mutant matrix over `governance_sweep.py` catches 12/12.** One mutant initially **survived** — swapping the two fractions in the summary line, because the test's two fractions were both `2/3`, making the swap invisible. Fixed by making them differ (2/3 vs 3/4). That is the S224 failure mode (an assertion no mutant can reach) reproduced one session later, caught this time by running the mutants rather than trusting the green suite.
- **Documents that *describe* the structure, refreshed** (learning #101): `tests/eval/README.md`'s transient-policy section (which claimed "both sweeps" and told readers governance numbers were comparable when they no longer are), `tests/eval/PHASE_E_AGREEMENT_REPORT.md` (a dated Session 225 update block superseding the S221 block's caveat 6, plus the living "How to run" procedure), `PROJECT_LEARNINGS.md` #86 (its source column still said "Filed in `BACKLOG.md`"), and `BACKLOG.md` (item closed, index row parity restored, the circuit-breaker item's worst-case arithmetic corrected — Session 225's retry makes the recorded ~7.5 h figure an understatement).
- **No threshold moved.** `GOVERNANCE_CYCLE_TIME_AGREEMENT_MIN` is still 0.90 and `GOVERNANCE_LAXER_MISSES_MAX` still 0. Best-of-3 does mean **post-fix governance numbers are not comparable with Sessions 216-220**, which measured best-of-1 on this capability — a different span from the SQL/QC cells' S219-S220, so the two rules must not be quoted interchangeably. No live call was made and no verdict was re-scored: `opencode` remains NO-GO.
- **Verified:** full gate **1230 passed + 9 live-skipped @ 97.98% coverage** (was 1202 passed, Session 223 baseline); `uv run ruff check src/ tests/ packages/ scripts/` and `uv run mypy` both clean. No `src/` or `packages/` code was touched — this is harness-only.

### 2026-08-17 — `sql_dialect_from_url` honours its own contract: a non-numeric port degrades instead of crashing (Session 223)

Closes the defect filed in Session 218. `sql_dialect_from_url` promised in its docstring that a malformed `--db-url` "degrades to today's dialect-silent prompt rather than raising here — the URL's real failure surfaces at `ReadOnlyDB.connect`, which is the error path callers already handle." It caught only `sa.exc.ArgumentError`, but `sa.make_url` coerces the port segment with a bare `int()`, and **`ArgumentError` is not a `ValueError` subclass** (`ArgumentError -> SQLAlchemyError -> Exception`), so a non-numeric port escaped as a raw `ValueError` and made that sentence false.

- **`packages/data-agent/src/model_project_constructor_data_agent/db.py`:** the catch is now `except (sa.exc.ArgumentError, ValueError)` — one line. The docstring gained the rationale: that neither exception subsumes the other, which of the two `make_url` uses for what, and an explicit statement that this is **not** URL validation (a negative or absurdly large port still parses, because `int()` accepts it; judging the URL remains `connect()`'s job).
- **The trigger is wider than filed.** The BACKLOG item named one input. Measured against SQLAlchemy 2.0.49, **five** classes take the `ValueError` path: an unexpanded environment variable (`@host:$DB_PORT/`), an alphabetic port, an **empty** port (`@host:/` — the same shell template with the variable set to `""`), a float port, and an IPv6 host with either. Wrong-type input (`None`, `int`) raises `ArgumentError` and was always handled.
- **Both production seams were unguarded, and this was confirmed live rather than argued.** `packages/data-agent/.../cli.py:129` evaluates `db.dialect` *outside* the `try` that begins on the next statement, and `scripts/run_pipeline.py:175` sits in `build_data_runner`, which no caller wraps. Driven end to end pre-fix: `run_pipeline` printed its `[1/5]`–`[3/5]` banner then a raw CPython traceback ending `ValueError: invalid literal for int() with base 10: '$DB_PORT'` and exited 1 before any stage ran; the data-agent CLI emitted Typer's rich traceback panel with the same error. Both raised *before* `connect()` was attempted. Post-fix both complete: the CLI writes its report and `run_pipeline` reports `Status: COMPLETE`, exit 0.
- **⚠ What this fix does NOT buy, established by adversarial review rather than assumed.** An earlier draft of this entry claimed the post-fix path reaches a `FAILED_AT_DATA` off-ramp. **That is false, and it was corrected before commit.** There is no such off-ramp for any DB problem: `ReadOnlyDB.connect` does build a message naming the exact cause, but `nodes.py`'s `execute_qc` catches `DBConnectionError` *without binding it*, `agent.py` substitutes the fixed string "database unreachable at QC execution time; quality checks not executed" and returns `status="COMPLETE"`, and `pipeline.py:460` only halts on a non-COMPLETE report. Measured: the report produced from `@host:$DB_PORT/claims` is byte-identical (modulo `created_at`) to one from a well-formed but unreachable URL. So the operator still cannot tell "I forgot to export `$DB_PORT`" from "the warehouse is down" — they simply no longer get a traceback. **This silence is pre-existing and applies to every malformed URL; the fix makes the non-numeric-port case consistent with the rest rather than uniquely fatal, and does not claim more.** Filed in `BACKLOG.md` as its own item. The `db.py` docstring, both new test docstrings, and this entry all now say so explicitly, because that sentence is the load-bearing reason a reader would believe silent degradation is safe.
- **`tests/data_agent_package/test_db.py`:** +3 tests (23 total, was 19). `test_non_numeric_port_degrades_to_none` parametrizes all five measured trigger classes plus `${DB_PORT}` and a MySQL URL; `test_out_of_range_port_still_parses` pins the deliberate non-validation so a future session does not "fix" it into range checking; `test_unexpected_error_propagates_rather_than_degrading` monkeypatches `make_url` to raise and asserts it propagates.
- **The upper bound is pinned because a mutant survived without it.** Mutation testing during the fix showed that widening the catch to `except Exception` left the whole file green — silently converting a genuine defect in this function into a `None` that reads as "the operator typed a bad URL," which would drop the dialect from the prompt and reintroduce the wrong-database SQL the module exists to prevent. That third test is what kills it. Verified: reverting the catch to `ArgumentError`-only kills 10 tests; `ValueError`-only kills 3; `except Exception` kills 1.
- **Seam-level regression coverage added, because neither seam had any.** `tests/data_agent_package/test_cli.py` gains `test_cli_survives_a_non_numeric_port_in_db_url`; `tests/scripts/test_run_pipeline_adapter.py` gains a non-numeric-port row to `test_build_data_runner_derives_sql_dialect_from_db_url`. Both existing tests exercised these seams with well-formed URLs only and **would not have caught the defect**; both new cases die under the reverted catch.
- **Two documentation surfaces were already wrong and are repaired by the code, not by an edit** — `packages/data-agent/USAGE.md:311-312` ("An unparseable URL yields `None`") and `TROUBLESHOOTING.md:102-103`, which routes a bad `db_url` to `FAILED_AT_DATA` when the non-numeric-port class actually landed under "Unhandled exceptions (pipeline crash)". Neither was touched; the fix makes both true as written. The Session 217 entry below (`9c9fe35`) stating "An unparseable URL returns `None` rather than raising" is left **frozen** — `CHANGELOG.md` is an append-only historical ledger, so a dated description of what a commit shipped is not rewritten; this entry is the correction.
- **One same-class sibling found and filed, not fixed:** `discovery.py`'s `probe_information_schema` promises "never raises for probe failures" while a third of its body sits outside the `try`. See `BACKLOG.md`. A deliberate sweep for other instances ruled every other candidate LEAVE.
- **Verified:** full gate **1202 passed + 9 deselected @ 97.98% coverage** (was 1190 passed, Session 222 baseline); `uv run ruff check src/ tests/ packages/ scripts/` and `mypy` both clean. Note `ruff format` is deliberately **not** applied — CI runs `ruff check` only, and `ruff format` reflows unrelated pre-existing lines in this file.

### 2026-08-17 — The SQL/QC sweep retries a transient: gap #1c's other half, closed (Session 221)

Closes the retry asymmetry Session 219 diagnosed and Session 220 quantified. Two live measurement blocks fed one gate under **opposite policies for the same failure class**: `interview_sweep` retried a transient seam error up to 3 attempts before scoring anything, while `sql_sweep` scored one `LLMParseError` as one parse miss + one exec miss + one QC miss with no retry at all. Against the 1.00 `sql_parse` and `qc_structural` bars one blip was sufficient to decide a verdict — and at S219 one blip produced the `opencode` NO-GO, at a rate S220 measured as **1 event in 60 samples**. Session 169 fixed this for interviews; the sibling never got the treatment.

**No production code changed, no threshold changed, `tests/eval/interview_sweep.py` is byte-identical, and no recorded verdict was re-scored — `opencode` stays NO-GO.** A harness change made after seeing a disliked number is how a gate stops being a gate (learning #86); the re-measure is filed as its own session (~$16.4, ~130 min).

- **The design is NOT the mirror `BACKLOG.md` prescribed, and the deviation is the whole point (`752e79b`).** The filed item asked for `interview_sweep`'s treatment byte-for-byte, including *exclude the sample on exhaustion*. Adversarial analysis showed that inverts the gate: exclusion drops the sample from the denominator while every surviving sample is clean by construction, so a provider failing 14 of 15 samples scores **1/1 = 100% and PASSES** a bar it fails 6.7% of today. At the measured 1-in-60 rate, three failures running is a ~1-in-216,000 event — so an exhaustion is never a transient in practice, it is systematic, which is precisely what the gate exists to catch. This path is also the live tier's **only** observation that a provider can emit parseable JSON at all (`shadow_run.py` records `json_parse` as a hardcoded `1.0` from the deterministic parity battery), so excluding those samples would leave nothing anywhere measuring the real thing. The operator chose this branch on that evidence.
- **Two tiers, two exhaustion policies (`752e79b`):** `_TRANSIENT_SCORED = (LLMParseError,)` is retried 3 attempts then **scored a miss** exactly as before — the denominator never shrinks. `_TRANSIENT_EXCLUDED = (APITimeoutError, APIConnectionError)` is retried 3 attempts then **excluded** and counted in a new `excluded_transient`, mirroring `interview_sweep`, because no model output exists to judge and scoring one a miss would convert a network outage into a model verdict. Everything else propagates; `APIStatusError` (4xx/5xx — bad model id, auth, rate limit) is a *sibling* of `APIConnectionError`, not a subclass, so a real API error still surfaces loudly (FM #18).
- **The transport tier is new coverage, not just a policy change (`752e79b`).** The data-agent package wraps no SDK transport error anywhere (`grep` for `APIConnectionError|APITimeoutError` in `packages/data-agent/src/` returns nothing), so before this session an `APITimeoutError` during the SQL block propagated straight out of `sweep_sql_capabilities` and **aborted the whole live run** — a ~2.5-hour `opencode` sweep could die on one network blip at minute 140. This is the hole Session 171 closed for `interview_sweep` and never applied to its sibling.
- **Design (B) — new exception subtypes in shipped `packages/` code — was priced and rejected (`752e79b`).** It is blocked by `tests/test_llm_json_parity.py:425`, whose `test_error_classes_are_intentionally_distinct` asserts pairwise `not issubclass(left, right)` across every seam error class; a subtype breaks it **by construction**, and the docstring calls that invariant "intentional and load-bearing (plan §2.3)". Reversing it needs an ADR, not a test edit. It also cannot classify the decisive case: `opencode_client.py:284` emits the *same* message for a genuine provider blip and for an unset `ANTHROPIC_API_KEY`. Blast radius was 18 `LLMParseError` raise sites (**not the 9 the backlog claimed**) plus 25 `IntakeLLMError` twins and 59 tests across 9 files.
- **Diagnosability (`752e79b`):** both `notify(...)` calls now carry `str(exc)`. Three documents recorded that S220's probe could not attribute the S219 event to one of the raise sites *because the message was discarded here*; the next occurrence is self-attributing. The original event stays unattributed — that cannot be recovered.
- **Visibility (`752e79b`):** `test_eval_live.py` passed **no `on_event`**, so inside the assertion gate — the surface that most needs them — every retry and exclusion note was silently discarded. It now sinks to stderr, and all three assertion messages carry `transient_retries` / `excluded_transient`; without them a pooled denominator and one shrunk by exclusions are indistinguishable. `shadow_run.measure_provider` gains `sql_excluded_transient` and `sql_transient_retries` as **diagnostics, not gate keys** (`eval_cutover` reads a fixed key set and ignores the rest — verified against `test_eval_cutover.py`'s 8-key pin).
- **Three defects found by adversarial review and fixed before commit (`752e79b`).** 18 agents produced 14 claims; 11 were refuted on evidence, 3 confirmed — each reproduced by mutating the module and re-running, not argued. (1) `excluded` was **last-attempt-wins**, so a sample raising `LLMParseError` twice and *then* timing out was excluded despite two observed model-quality failures: measured, 6 observed parse failures across 3 samples scored **0** misses and **passed** the 1.00 bar — the exact hole this design rejects exclusion for, reintroduced through the back door. Now any scored failure beats an exclusion, and the terminal note names the scored exception rather than whichever error landed last. (2) `_MAX_TRANSIENT_RETRIES = 2` was **unpinned** — every retry test injected the bound explicitly, so mutating the default to `0` reverted *both* live call sites to Session 218 best-of-1 with the whole suite green. (3) `transient_retries` was unpinned on the exhaustion path and across the entire QC seam; two mutants survived. All four mutants now die.
- **Tests (`752e79b`):** `tests/eval/test_sql_sweep.py` 9 → 24. New pins include retry-and-recover on both tiers, exhaustion-is-still-scored, exhaustion-excludes-only-for-transport, QC exclusion granularity (the sample's parse/exec observations survive; only the QC datum drops), mixed-class exhaustion in both orderings, `max_transient_retries=0` reproducing the pre-fix semantics as a regression anchor, the default bound, notes carrying `str(exc)`, `APIStatusError` propagating unretried, and that `pass_rate("sql_parse", []).meets(1.00)` is `False` — the previously untested `total == 0 → 0.0` ternary that is the only thing standing between exclusion and a vacuous pass.
- **⚠ Recorded honestly: best-of-3 changes what the bars mean.** A per-sample failure rate `p` becomes `p³` against unchanged thresholds — a provider whose true rate is 20% goes from a 3.5% chance of clearing `sql_parse` to 88.6%. That is a real loss of detection power and **no threshold diff will show it**, which is why `transient_retries` is reported. **Post-fix numbers are not comparable with Sessions 219 and 220**, which measured best-of-1. This is a harness-statistics/policy fix, **not** a #129 loosening — the same clause both prior gap-#1c entries carry.
- **⚠ Also recorded: what this does not fix.** The gate now retries an error class production retries **zero** times (`except LLMParseError` appears nowhere in `packages/`). Worst-case spend and wall clock **triple** on a doomed run (30 calls → 90); no circuit breaker was built. And learning #86 is only half-closed: `shadow_run.py`'s governance loop is a **third** block with a **fourth** policy — no retry, scored an immediate non-agreement against `GOVERNANCE_LAXER_MISSES_MAX = 0`, a zero-tolerance bar fed by an un-retried transient — while `test_eval_live.py`'s governance test has no handler at all and simply aborts, so the driver and the assertion gate disagree with each other. All filed in `BACKLOG.md`, deliberately not fixed here.
- **Verified:** full gate **1190 passed + 9 live-skipped @ 97.98% coverage** (was 1175 + 9 @ 97.98%, measured directly against the pre-change tree, not quoted from a prior entry); `ruff check .` and `uv run mypy` both clean (0 issues, 68 files). **Deterministic only** — the deliverable is the robustness fix, *not* a measured live-green confirmation. Zero live calls were made and zero dollars spent.

### 2026-08-02 — The data agent is told which SQL dialect it is writing for (Session 217)

Closes the root cause Session 216 diagnosed behind the failing `sql_exec` eval threshold. The data agent generated SQL for a dialect nobody named, so it inferred one — and it inferred a *warehouse*. This shipped to production too: an organization on Snowflake, PostgreSQL or SQL Server got whatever dialect the model picked, and the failure was silent because the SQL still parsed and only failed on execution. **No threshold was changed, no cutover verdict was re-scored, and `SQL_EXECUTABLE_MIN` remains 0.95** — the re-measure is a separate, operator-authorized session.

- **`packages/data-agent/src/model_project_constructor_data_agent/db.py` (`9c9fe35`):** new `sql_dialect_from_url(url)` plus a `ReadOnlyDB.dialect` property, both parse-only via `sa.make_url(...).get_backend_name()`. No connection and no installed DBAPI driver is required — load-bearing, because `generate_queries` is the *first* node in the graph while the DB is not connected until `execute_qc`, so a connection-dependent dialect would arrive too late to reach the prompt. `get_backend_name()` strips the `+driver` suffix (`postgresql+psycopg` → `postgresql`). An unparseable URL returns `None` rather than raising: the URL's real failure belongs to `connect()`, which callers already handle.
- **`anthropic_client.py` (`9c9fe35`):** new `sql_dialect` constructor keyword and a `_dialect_note()` helper injected into the *system* string of the three SQL-emitting methods — `generate_primary_queries`, `generate_quality_checks`, `generate_baseline_query`. The prose and scoring surfaces (`summarize`, `generate_datasheet`, `rank_candidate_tables`) deliberately do not carry it. `sql_dialect=None` reproduces the previous prompt **byte for byte**, asserted as a string equality against the reconstructed note rather than a substring absence, so the note is provably the only difference.
- **`bedrock_client.py`, `opencode_client.py` (`9c9fe35`):** both re-declare `__init__` and call `super().__init__` with explicit keywords, so both forward the new keyword. Unforwarded, it would degrade silently — the prompt would still build, still parse, and only the executability rate would move.
- **`factory.py` (`9c9fe35`):** `make_llm_client(..., sql_dialect=...)` threaded through all three provider branches. Test coverage is parametrized over `KNOWN_PROVIDERS` so a fourth provider cannot silently skip the keyword and ship dialect-blind while its siblings are not.
- **Production wiring (`9c9fe35`):** the data-agent CLI (`cli.py`) and `scripts/run_pipeline.py` derive the dialect from the `--db-url` they already accept, rather than adding a separate `--sql-dialect` flag — a second source of truth is a second thing to get wrong, and deriving it makes "agent points at PostgreSQL, prompt says SQLite" unrepresentable. No `--db-url` means no execution target, so the dialect stays unstated rather than guessed (the SKIP_EXECUTION path never executes the SQL at all).
- **Eval harness (`9c9fe35`):** `tests/eval/shadow_run.py` and `tests/eval/test_eval_live.py` pass `db.dialect` — **derived from the DB object, never the literal `"sqlite"`** — so the harness cannot mislabel its target if the eval DB ever changes.
- **Live A/B verification (6 calls, `anthropic`, `claude-sonnet-4-6`):** the same three `kind: primary` corpus cases, the same seeded SQLite DB, one session, run twice. Dialect-blind: **2/5 executable**, failing on `DATEDIFF`, `PERCENTILE_CONT … WITHIN GROUP`, and **`ILIKE`** — a fourth offender Session 216's list did not contain, which widens the diagnosis: the problem was never three specific functions but that the model had to guess. Dialect-aware: **4/4 executable**. Parse-validity was 100% in both arms, which is precisely why an unstated dialect failed silently. Recorded in `tests/eval/PHASE_E_AGREEMENT_REPORT.md` §"Update — Session 217" with an explicit list of what the probe does *not* establish (tiny n, model-chosen denominator, `anthropic` only, single run).
- **Tests (`9c9fe35`):** new `tests/data_agent_package/test_db.py` (derivation across six dialects, `+driver` stripping, unparseable → `None`, availability pre-`connect`) and new `tests/eval/test_shadow_run.py`, which runs `measure_provider` with every LLM seam and corpus loader stubbed — so the shadow driver's wiring is finally exercised at **$0**. That path was previously green-badged and untested, which is exactly how Session 215's harness defect survived (learning #71). Plus dialect coverage in the anthropic/bedrock/opencode client, factory, CLI and pipeline-adapter suites.
- **Verified:** full gate **1166 passed + 12 live-skipped @ 97.98% coverage** (was 1110 + 12 @ 97.79%, Session 214 baseline); `uv run mypy` and `ruff check .` both clean.

### 2026-08-02 — `opencode` measured live for the first time: NO-GO, 7/8 thresholds PASS (Session 216, Phase 4 of `docs/planning/opencode-adapter-spec.md`)

The provider-quality gate ran against `opencode` for the first time since the adapter shipped in Session 213. **No production default changed** — the verdict is NO-GO and `anthropic` stays primary everywhere. No `src/` change at all; this session measured, diagnosed, and recorded.

- **The run:** 451 live calls pinned to **`anthropic/claude-sonnet-4-6`** — deliberately the same model id the recorded baseline uses (`anthropic_client.py:43`, unchanged since the first commit), so the A/B isolates the *transport* change instead of confounding it with a model change (spec §11 Q2, resolved by the operator 2026-08-01). **$13.99, 99.5 min.** Driven through `shadow_run.measure_provider` — the same code path the live assert tier uses — so the numbers are not produced by bespoke session code. A same-session `anthropic` governance+SQL refresh (31 calls, 6.1 min) gave a same-day comparison; its interview sweep was deliberately not re-paid, being at ceiling and stable since S170.
- **Result: 7 of 8 §3.4 thresholds PASS.** `opencode` ties the baseline on `json_parse` (100%), `sql_parse` (100%), governance `cycle_time` (100%) and laxer misses (0), `qc_structural` (100%), and **both interview thresholds** — `convergence` 100% (20/20) and `premature` 0. Those last two were *unmeasurable for this provider* until Session 215 fixed the harness transport gap.
- **Spec risk #1 did not materialize.** The D2 prompt-role change — system instructions folded into the user message, inside OpenCode's own agent framing and its irreducible ~4,830-token scaffold — is the specific hazard this whole gate exists to catch. It degraded nothing measurable on this corpus.
- **The one FAIL, `sql_exec` at 42.9%, is diagnosed as a harness SQL-*dialect* artifact — and it fails the incumbent too (60.0%).** A follow-up diagnostic pass (6 calls) captured the real DB exception per generated query: **every** failure on **both** providers is an unsupported-function error against SQLite — `DATEDIFF(...)` (MySQL/SQL Server/Snowflake), `PERCENTILE_CONT(...) WITHIN GROUP` (Postgres/Oracle), `MEDIAN(...)`. Both providers emit competent *warehouse* SQL; the eval executes it on SQLite and nothing in the data-agent prompt names the dialect. Session 165's standing guess — "could be model SQL *or* an incomplete seed schema" — is **neither**, and that mis-attribution had stood since.
- **The metric is unstable and the provider ordering reverses.** Its denominator is model-chosen (how many queries the model decides to write), so the rate moves run to run: sweep `anthropic` 3/5 = 60.0% vs `opencode` 3/7 = 42.9%; diagnostic re-run `anthropic` 2/4 = 50.0% vs `opencode` **4/7 = 57.1%**. In the sweep both providers produced the *same absolute number* of executable queries (3) — `opencode` simply wrote more, each additional one another chance to hit the dialect wall. **The recorded gap is therefore not evidence of a transport regression.** New `BACKLOG.md` item: fix the metric (name the dialect in the prompt, or execute against a warehouse target) — explicitly **not** by lowering `SQL_EXECUTABLE_MIN`.
- **Cost telemetry (spec risk #12, previously unquantified):** no §3.4 threshold can see per-call cost, so it is now recorded in the agreement report. Mean **$0.0310/call**; p50 latency 7.8 s, p90 17.3 s, **p99 105.8 s, max 233.3 s**. The tail is expensive — 20 of 451 calls (4.4%) exceeded 60 s and consumed **14% of total spend**, consistent with the adapter invoking an *agent* that may take multiple steps (spec dragon #3). Prompt-cache reuse is rare: only **38 of 451 calls (8.4%)** registered any `cache_read`, so ~92% re-pay the cache-*write* premium on the constant scaffold.
- **Files:** `tests/eval/PHASE_E_AGREEMENT_REPORT.md` (opencode column measured, replacing S214's hand-added PENDING cells; new §"Update — Session 216" with the diagnosis and cost profile), `docs/planning/opencode-adapter-spec.md` (§9 Phase 4 marked DONE with the verdict), `BACKLOG.md`, `SESSION_NOTES.md`.
- **Verified:** pre-flight resolved the simulator's completer to `OpenCodeLLMClient._run` at $0 before any billable call (Session 215's gotcha 1, followed literally); `bedrock` correctly stayed PENDING (no creds). The NO-GO is produced by the unmodified `evaluate_cutover`, not asserted by hand.

### 2026-08-01 — The interview sweep can drive a subprocess provider; spec Phase 4 unblocked (Session 215, Phase 3b of `docs/planning/opencode-adapter-spec.md`)

Session 214 made `opencode` *judgeable*; this session found that it was not yet *measurable*. Phase 4 — the live shadow run — would have aborted mid-run, after billing. The blocker was invisible to the test suite because the live tier skips, and invisible to the spec because Phase 4's pre-flight listed the binary, the credentials and the pinned model, but never "the harness can drive this provider at all". Found by checking before spending; the reproduction cost nothing.

- **The defect (`a0c3930`):** `tests/eval/stakeholder_sim.py` reached through to `intake_client._client.messages.create(...)`, assuming every provider is Anthropic-SDK-backed. `OpenCodeLLMClient` sets `_client` to the `_UNUSED_SDK_CLIENT` placeholder whose `__getattr__` raises by design (spec D5, pinned by `test_opencode_client.py`) — so the simulator died on the first simulated-stakeholder turn with a bare `AttributeError`. That type is **not** in `interview_sweep._TRANSIENT_ERRORS`, so it aborted the entire run rather than being retried-and-excluded. `shadow_run.measure_provider` orders governance → sql → interview, so a Phase 4 run would have spent ~31 live `opencode` calls and then crashed with no report. Two of the eight gate keys (`interview_convergence`, `interview_premature`) were unmeasurable for the provider.
- **Second defect, same path (`a0c3930`):** `shadow_run.py` and `test_eval_live.py` both built the simulator via `stakeholder_simulator_for(fixture, provider=provider)` with **no `model=`**, so the simulated-stakeholder half of every interview would have run unpinned while the interviewer half ran on the pinned model. For this provider unpinned means no `--model` flag at all (`DEFAULT_MODEL` is `None`, spec D6) — two model tiers inside one measured conversation, and nothing in the report would have shown it.
- **The fix (`a0c3930`):** a `TextCompleter` seam — `(system, user) -> str` — resolved by **transport shape** rather than provider name, so a fourth provider shaped like either existing one needs no change: `_text_completer_for` returns the client's own `_run` for subprocess transports and a closure over `messages.create` for SDK ones. A client with neither shape raises `TypeError` at construction — deliberately *not* `StakeholderSimError`, which is in `_TRANSIENT_ERRORS` and would therefore get every interview retried three times then excluded, converting "nobody taught the harness this provider" into a silently empty result set instead of a loud abort. `StakeholderSimulator` keeps its `client=`/`model=` constructor as the convenient shape for a fake.
- **Scope held:** **no production code changed.** The `_UNUSED_SDK_CLIENT` guard that made this failure loud is untouched, and the fix lives entirely in the test tree, where the deliberate `_client`/`_model` reach has always lived.
- **One deliberate behaviour change, contract-aligning rather than new policy:** the shared guard in `_call_text` now rejects a whitespace-only answer on *every* transport, where the SDK path previously returned it verbatim. `tests/eval/interview_sweep.py:73` — untouched by this change — already documented `StakeholderSimError` as covering "an empty simulated answer", so the code now matches a contract that was already written. It cannot move the recorded `anthropic` baseline, which sits at its ceiling on both affected keys (`interview_convergence` 100%, 20/20; `interview_premature` 0).
- **Tests (`a0c3930`):** +11, all hermetic. The regression test drives a client shaped like the real one — `_run` present, `_client` a raising placeholder — and fails loudly if the SDK path is ever taken again. The end-to-end test drives the **real** `OpenCodeLLMClient` through the real factory branch, replaying the committed `tests/fixtures/opencode/success_with_agent.jsonl` via a stubbed runner and `shutil.which`, and asserts both that the answer travels the real event-stream parser and that the pinned model reaches the argv — covering both defects in one case, with no binary, no network, and no spend. A `KNOWN_PROVIDERS` sentinel goes red when a fourth provider lands. **The five pre-existing simulator tests pass unmodified** — the evidence that SDK-provider behaviour is unchanged.
- **Adversarial review (pre-commit):** four independent lenses (correctness, hermeticity/CI, regression/scope, docs) raised **12 findings; all 12 were refuted** on verification, each by an agent instructed to default to refuted. The one genuine code observation — that the blank-answer guard is a real behaviour change on the SDK path — survived as an observation but not as a defect, and is documented above rather than dropped. Its refutation was re-verified by hand against `interview_sweep.py:73`, not taken on the agents' word.
- **Verified:** the exact reproduction that failed at session start now resolves to `OpenCodeLLMClient._run` with the pinned model on the client, touching no SDK handle. Full gate: **1121 passed + 12 live-skipped @ 97.79% coverage** (was 1110 + 12, Session 214 baseline); `mypy` clean (68 files); `ruff check src/ tests/ packages/ scripts/` clean.
- **Operator decision recorded (spec §11 Q2):** the first measured run targets an **Anthropic** model through `opencode`, holding the model constant so the A/B isolates the transport change — which is what actually tests risk #1. The non-Anthropic run that discharges `AI-Dependencies.md` §6.7's model-family diversification is a second measurement, after the adapter is cleared.

### 2026-08-01 — `opencode` enters the provider cutover gate, unmeasured; three-provider documentation sweep (Session 214, Phase 3 of `docs/planning/opencode-adapter-spec.md`)

Makes the Session 213 provider *judgeable* rather than merely present: `"opencode"` joins `CANDIDATE_PROVIDERS`, so the live eval tier now parametrizes over three providers and the committed agreement report carries an `opencode` column in which every one of the eight §3.4 thresholds is PENDING. Nothing was measured — by the gate's own §5 rule an unmeasured threshold cannot certify GO, so `anthropic` stays primary. Then the documentation sweep the provider's existence made necessary.

- **`tests/eval/eval_cutover.py`:** `CANDIDATE_PROVIDERS` gains `"opencode"`; new `OPENCODE_MODEL_ENV` (`"OPENCODE_EVAL_MODEL"`) and `provider_eval_model(provider)`. **`provider_creds_available("opencode")` deviates from spec §7.4 deliberately, on an operator decision taken before implementation.** §7.4 specified `shutil.which("opencode") is not None` alone; that is unsafe here — the binary is installed globally on any machine that ran the spec's Phase 1 spike, and `pyproject.toml`'s `addopts` carries no `-m 'not live'`, so a binary-only probe would have made a bare `uv run pytest -q` on a developer machine *execute and bill* four live cases (the convergence sweep alone is hundreds of model calls) while CI, having no binary, still looked hermetic. The shipped probe requires the binary **and** `OPENCODE_EVAL_MODEL`. Both the spec's §7.4 and its Phase 3 entry carry the correction inline.
- **The same variable is the pinned model id, not just a flag.** `provider_eval_model` returns it for `opencode` and `None` for the SDK providers (which keeps each on its own native default id — what stops a Bedrock client being handed a bare first-party id). `tests/eval/test_eval_live.py` (4 construction sites) and `tests/eval/shadow_run.py` (3) now pass it to the factories. This makes spec D6's standing requirement — *every evaluated run must pass an explicit model*, since this provider pins no default — actually true rather than aspirational, and discharges Phase 4's "the operator names the model to pin" pre-flight with a single variable.
- **+10 deterministic tests (`tests/eval/test_eval_cutover.py`).** A 4-case matrix over (binary present × model var set) — including the binary-present/var-unset case a binary-only probe would get wrong; a test pinning *which* executable name is probed; a test asserting the probe reaches neither `subprocess.run` nor `subprocess.Popen` (the docstring's side-effect-free promise, enforced rather than stated); `provider_eval_model` for all four provider values; and a role test asserting `opencode` is a candidate, never the baseline, and resolves to PENDING/"keep anthropic primary". Every case stubs **both** signals — a test reading the real `PATH` would measure the host, not the code.
- **`tests/eval/conftest.py`, `README.md`, `PHASE_E_AGREEMENT_REPORT.md`, `pyproject.toml`'s `live` marker:** documentation of the three-provider skip contract. The agreement report gains the `opencode` column (hand-added in the renderer's exact cell format — there was no run to paste), a Session 214 update paragraph, and a rewritten gap-list item 5 covering both unmeasured candidates. `json_parse` is left PENDING for `opencode` rather than pre-filled `1.0`, even though the deterministic parity battery already covers its seams, because one green cell would make the report read as partially measured.
- **Documentation sweep — 13 wiki pages, plus `README.md`, `OPERATIONS.md`, `.env.example`, and the published `docs/tutorial.md`.** Shipping a third provider falsified present-tense claims well beyond the four surfaces the spec's phase list named: `AI-Dependencies.md` (§9 flipped planned→as-built, §6.7's residual rewritten, plus a **new §6.9** on the risks specific to a subprocess *agent* in the call path, and five conditional operator-checklist items), `Architecture-Decisions.md` (AD-2's provider count, an as-built block under AD-11), `Extending-the-Pipeline.md` (a "transport override" subsection and a CLI-transport variant of the provider recipe), and then `Security-Considerations.md`, `Glossary.md`, `Data-Guide.md`, `Monitoring-and-Operations.md`, `Contributing.md`, `Home.md`, `Getting-Started.md`, `Intake-Interview-Design.md`, `Architecture-Overview.md`, and `Software-Bill-of-Materials.md` — the last gaining a **non-Python runtime dependency** subsection, since the `opencode` binary is a genuine supply-chain dependency that `uv.lock` does not pin and a Python-tree vulnerability scan cannot see. `Evolution.md` and the wiki `Changelog.md` were deliberately left alone (user-requested-rewrite and episodic-curated cadences respectively, per `PROJECT_CONVENTIONS.md` §2). Stale test-count figures were refreshed against live counts in `README.md`, `Architecture-Overview.md`, `Monitoring-and-Operations.md`, and `Contributing.md` (the last recomputed with that page's own documented `grep` command: 997 test *functions*, not the collected count).
- **The sweep's own scope was wrong until an adversarial pass corrected it.** A four-lens verification fan-out, each finding independently refutation-tested, confirmed ten defects — all real on re-check. The important class: files **outside** `docs/wiki/` that a wiki-scoped grep never saw, including **`docs/tutorial.md`, the one page `publish-tutorial.yml` actually publishes**, still reading "Two providers ship today"; `OPERATIONS.md` and `.env.example` enumerating the `INTAKE_LLM_PROVIDER` values without `opencode`; and two enumerations on pages the sweep *had* edited elsewhere (`Security-Considerations.md`'s env-var table, 90 lines from a line the sweep corrected). Also caught: `tests/eval/__init__.py` and `render_agreement_report`'s docstring still saying "both providers", stale test counts on two more pages, and an off-by-one in this entry's own page count. All fixed before commit; the interview-convergence explainer's `.qmd` was corrected **and re-rendered** so its committed PDF does not drift from its source.
- **Verified:** **1110 passed + 12 live-skipped @ 97.79%** (from 1100 + 8 @ 97.79%); `-m 'not live'` green; `-m live` collects 12 and skips all 12 **on this machine, where the binary is present** — and the probe flips to runnable the moment `OPENCODE_EVAL_MODEL` is set, so the gate was checked in both directions; `mypy` clean (68 files); `ruff check src/ tests/ packages/ scripts/` clean; the wiki no-line-citation guard green.

### 2026-08-01 — Third LLM provider: `OpenCodeLLMClient`, a subprocess adapter over the `opencode` CLI (Session 213, Phase 2 of `docs/planning/opencode-adapter-spec.md`)

Adds `"opencode"` alongside `"anthropic"` and `"bedrock"` in both agents' provider seams. Unlike its siblings it calls no SDK: it shells out to the `opencode` command-line agent (`run --format json`) and parses the JSONL event stream. OpenCode is itself a vendor-agnostic multiplexer (75+ providers via the AI SDK / Models.dev), so one adapter reaches many underlying vendors through the *operator's* OpenCode config — the enterprise-portability goal behind AD-11. Spec Phase 1 (Session 212) verified the CLI's behaviour live and left seven corrections; this phase implements the corrected design. **The adapter is wired but unmeasured** — no eval wiring (Phase 3) and no quality thresholds (Phase 4), so `anthropic` remains the default everywhere.

- **New clients — `src/model_project_constructor/agents/intake/opencode_client.py` and `packages/data-agent/src/model_project_constructor_data_agent/opencode_client.py`:** each subclasses its package's `AnthropicLLMClient` and overrides **only** `__init__` plus the one transport method (`_call_json` for intake, returning parsed JSON; `_call_claude` for the data agent, returning raw text — the §2.1 seam asymmetry is preserved). Every prompt, JSON-shape instruction, dataclass builder, `_extract_json`, and the optional `rank_candidate_tables` are inherited unchanged, so prompt drift across providers is structurally impossible (spec D1, the same argument as `bedrock_client.py`). Both ship in one session because the twin drift guard below is only writable once both copies exist (precedent: Session 162's Bedrock pair).
- **The system prompt is folded into the user message (spec D2).** `opencode run` has no `--system` flag — a load-bearing absence re-confirmed against the installed binary's `--help` in Phase 1 — so each call sends `system + "\n\n" + user` as one message. Spec Appendix A.3 validated this end-to-end for `next_question`: the real payload, run live three times, produced schema-valid output that parses with the project's own unmodified `_extract_json`. **That is a smoke test, not a quality measurement**, and no data-agent method was exercised at all; risk #1 (silent quality regression) stands until the eval gate runs.
- **Safety: the tool-denying agent definition is mandatory, and the spec's escape hatch was removed.** Phase 1 disproved hazard H4's claim that OpenCode's permission default is safe — with no `--auto`, a live run listed a directory, **read a file and disclosed its contents**, exit 0 (Appendix A.4 C1). Both clients therefore always write a block-style YAML agent definition denying `edit`/`write`/`bash`/`read`/`webfetch` into their sandbox and always pass `--agent`. Spec §5.1's `agent: str | None = None` parameter — which would have let a caller substitute their own, unvalidated agent — is **replaced by `agent_name: str`**, which renames the generated definition rather than substituting one, so no constructor path reaches an unlocked agent. Recorded as an as-built deviation in the spec's §4.4/§5.1 and Appendix A.4 C1.
- **The other Phase 1 corrections, all implemented:** the answer is taken from the **final step** (the one whose `step_finish.reason == "stop"`), not by concatenating every `text` event, which on a real 3-step run returned narration glued ahead of the answer (C4); error messages are built from the stdout `error` event's `name`/`message`/`ref` because **stderr is empty** on that path, making the spec's prescribed `{stderr_tail}` render as `""` (C2); `step_finish.reason == "length"` restores the truncation guard §3.3 declared impossible, and its `tokens`/`cost` are surfaced as `last_usage` — per-call telemetry the SDK clients do not expose (C3); "non-zero exit with zero parseable stdout" is diagnosed **distinctly** as a malformed-agent-definition failure, because the adapter generates that file so it would be our bug, not the provider's (C5); and the sandbox is created **once per client instance**, never per call, since an agent file makes OpenCode materialise `.opencode/node_modules` from npm at runtime (C6).
- **Hazards closed in code and pinned by tests:** the prompt goes on **stdin, never argv** (H2 — transcripts and `DataRequest` free text may carry PII, and argv is world-readable via `ps`); stdin is a pipe the adapter closes and is **never inherited** (H1 — a probe with an inherited stdin hung for a full 25 s timeout with zero output); `--auto` is never passed (H4, its own negative test); `--dir` is an ephemeral `TemporaryDirectory` outside the repository (H3), asserted as such.
- **Registry + factories (the three-edit lockstep, no test can guard it — C4):** `LLMProvider` `Literal` in both `factory.py` modules, one lazy-import branch each, and `LLM_PROVIDERS["opencode"] = LLMProviderSpec(api_key_env_var=None)` in `orchestrator/config.py`. `api_key_env_var=None` is correct here for a *different* reason than Bedrock's: the credential depends on whichever vendor the operator's OpenCode config routes to and may live outside any environment variable, so there is no single name to record. `KNOWN_PROVIDERS`, the unknown-provider error, and the data-agent CLI's `--provider` help all derive from the `Literal` and updated themselves; `run_pipeline.py`, the data-agent CLI, and `ui/intake/app.py` needed no edit (verified, spec §6.3).
- **`DEFAULT_MODEL = None` (spec D6):** no model is pinned, so `--model` is omitted entirely and the operator's own `opencode.json` chooses the vendor — that deferral *is* the portability goal. The cost is reproducibility, so any evaluated or production run must pass an explicit model; Phase 4 must record it.
- **Tests (+111): `tests/agents/intake/test_opencode_client.py` (43), `tests/data_agent_package/test_opencode_client.py` (42), plus factory/registry/parity additions.** Every JSONL sample is a **verbatim Phase 1 capture** from `tests/fixtures/opencode/` — a hand-written fixture would test the spec rather than OpenCode. No test spawns a process: the `runner` seam is injected, and clients resolve `sys.executable` so the construction-time `shutil.which` check passes on a machine without `opencode` (CI stays hermetic).
- **The new twins got their drift guard on the day they were born (`tests/test_llm_json_parity.py`):** C4 forces a second duplicated pair — the JSONL event helpers — and the `_extract_json` twins drifted once before, costing three sessions to repair (Sessions 98–100). A 16-case behavioural battery now runs identical input through both copies asserting identical output, plus a test pinning that the two `AGENT_DEFINITION` constants are byte-identical (a divergence there would mean the two packages run under **different tool permissions**). Two `_SEAMS` rows document the new `(seam, provider)` matrix.
- **A wrong assumption of mine, caught by my own test:** an initial test asserted the OpenCode provider constructs with **no SDK loaded at all**. It failed — `opencode_client` imports its parent, which imports `anthropic.types.TextBlock` at module level, so `anthropic` still reaches `sys.modules`. The assertion was wrong, not the code: spec D5's actual claim is that this adapter adds no *Python dependency*, which is about what the file requires (`pyproject.toml` is unchanged in both packages). Replaced with an AST-based guard asserting the module's own imports are stdlib + its own package, and the weaker-than-hoped truth is documented in the test's docstring rather than quietly dropped.
- **Verified:** full gate **1100 passed + 8 live-skipped @ 97.79% coverage** (was 989 passed @ 97.78%, Session 205/209 baseline); `mypy` clean (68 files); `ruff check src/ tests/ packages/ scripts/` clean; `tests/test_data_agent_decoupling.py` and `tests/test_llm_json_parity.py` green; `rg -n "subprocess" packages/data-agent/src/` → only the new client; importing the wheel's factory still loads no SDK. Both new modules at 98% branch coverage — the residue is `_default_runner`'s single `subprocess.run` line, deliberately never exercised.

### 2026-07-27 — Bedrock provider migrated from `AnthropicBedrock` (SigV4) to `AnthropicBedrockMantle` (native Bedrock API key auth) (Session 188, Phase A3 of `docs/planning/enterprise-migration.md`)

The classic `AnthropicBedrock` (SigV4/`bedrock-runtime`) client is replaced by `AnthropicBedrockMantle`, authenticating with a Bedrock API key against the native Anthropic Messages `bedrock-mantle` endpoint, with the default model flipped to `anthropic.claude-opus-4-8` (the mantle catalog has no Sonnet tier), plus optional enterprise-networking pass-throughs added on top.

- **`src/model_project_constructor/agents/intake/bedrock_client.py`, `packages/data-agent/src/model_project_constructor_data_agent/bedrock_client.py`:** swapped `AnthropicBedrock` → `AnthropicBedrockMantle` in both client copies (`dadf514`); bumped the `anthropic[bedrock]` floor to `>=0.94` (ships `AnthropicBedrockMantle`). Flipped `DEFAULT_MODEL` from the inherited `anthropic.claude-sonnet-4-6` (404s — no Sonnet in the mantle catalog) to `anthropic.claude-opus-4-8` (`9e3f19b`), intentionally diverging from the first-party `anthropic` client's Sonnet default. Added optional `base_url` (PrivateLink VPCE / GovCloud host), `http_client` (forward proxy / TLS-inspection CA / mTLS), and `require_sigv4` (raises if `AWS_BEARER_TOKEN_BEDROCK` is set, so a stray bearer token cannot silently override role-based SigV4) keyword arguments, forwarded to `AnthropicBedrockMantle` (`56dc700`). All new arguments default to no-op, so the factory and app are unchanged. Module docstrings inverted to the enterprise story: SigV4-via-IAM-role primary, bearer token dev-only fallback.
- **Tests:** updated the 4 tests that patch the client by name for the endpoint swap; 2 default-model assertion tests updated for the opus-4-8 flip; +6 tests for the new keyword arguments (3 per client copy). Full gate: 922 passed + 8 live-skipped, ruff + mypy clean (as of `56dc700`).
- **Verified live (auth + endpoint + region resolution only):** confirmed against `https://bedrock-mantle.us-east-1.api.aws/anthropic/`. **Not verified live end-to-end** — every current-generation Claude model returned `403 "not available for this account"` on the tested AWS account, a data-plane runtime-quota gate independent of this code; AWS subsequently denied the quota-increase request, and the operator's plan is to deploy into an enterprise AWS account instead (`docs/planning/enterprise-migration.md`). The `require_sigv4`/`base_url`/`http_client` hooks are not yet wired to app config — a future session wires them at the deployment entrypoint (see `docs/planning/enterprise-migration.md` Phase C1/D13).
- **Docs:** `docs/deployment/bedrock-enterprise.md` (new — enterprise Bedrock deployment guide: IAM policy, PrivateLink, region/residency, security config, app config surface); `.env.example` AWS/Bedrock surface. Builds on the original Bedrock-provider rollout: `docs/architecture-history/multi-provider-llm-plan.md`.

### 2026-06-19 — Harden the `tactical`/`operational` cadence boundary (output purpose, not frequency) + add the role≠frequency corpus case (gap #2 robustness, Session 177)

The S175 cadence definitions hit a live 100% on the four governance cases, but those cases have **cadence co-linear with frequency**, so the intended `tactical` (per-case decision support) vs `operational` (periodic institutional close-step) *role* distinction was never exercised (the S175 adversarial CONCERN — a green on cases where cadence is co-linear with frequency is not evidence the role boundary generalises). Refined the definitions to discriminate on **output purpose** and added a divergent corpus case that exercises the boundary at one divergent point (corpus coverage, not a general-robustness claim).

- **`src/model_project_constructor/agents/intake/anthropic_client.py`:** `CYCLE_TIME_DEFINITIONS` now lead with output purpose — `tactical` = per-case decision support consumed for individual near-term case decisions on **any** run cadence (incl. an overnight batch); `operational` = a periodic portfolio/process artifact from a scheduled close-step. `strategic` was also refined — **applied alone, the output-purpose rewrite first regressed `pricing_optimization` toward `continuous` (0/12 in a first live re-measure**, since its output feeds the rating engine per-renewal); re-anchoring `strategic` on the quarterly filing-calendar/enterprise scope (even when the strategy is applied per-transaction) is the compensating fix, re-verified to restore 12/12 `strategic`. No threshold, schema, or `CycleTime`-member change (O4 parity guard + verbatim-enumeration pin preserved).
- **Corpus:** new authored governance case `tests/eval/corpus/governance/claim_workqueue_triage.yaml` — a fixed nightly batch that feeds adjusters a per-claim work-queue (operational frequency cue, tactical output purpose) → reference `tactical` (operator-blessed); `reserving_adequacy` is its batch-cadence `operational` foil. Registered in `_GOVERNANCE_SOURCES` (governance corpus only, not the interview sweep). Aligned the `subrogation` reference rationale prose (and its `subrogation_intake.json` / `docs/tutorial.md` copies) with the canonical `operational` definition (label unchanged).
- **Tests:** new reversion pin `test_governance_corpus_exercises_role_not_frequency_boundary` (the corpus must contain the role≠frequency pair). Full gate **916 passed + 8 live-skipped @ 97.39%** (was 915); ruff + bare mypy clean.
- **Verified live (anthropic, N=12/case, n=60):** `cycle_time` agreement **60/60 = 100%, laxer 0** — every case classifies its reference cadence exactly, incl. the divergent case **12/12 `tactical`** and `pricing_optimization` **12/12 `strategic`**. Independently corroborated by the gate assert test `test_live_governance_cycle_time_agreement_and_no_laxer_miss` (anthropic **PASS**, ~259s).
- **Closed** the gap #2 cadence-hardening BACKLOG item → `BACKLOG.md` is empty. The operator **deferred** the optional event-driven/episodic `CycleTime` member (YAGNI). *Scope unchanged:* anthropic-only / corpus-validated; **overall Phase E stays NO-GO** (`sql_exec` 60% + unmeasured `bedrock`). gap #2 fixes are forward-only — strengthen the prompt/corpus, never weaken references or thresholds.
- **Docs:** `PHASE_E_AGREEMENT_REPORT.md` (S177 note + §3.4 row + gap-list), `tests/eval/README.md` (corpus inventory + reference-labels + the role≠frequency pin), `docs/wiki/claims-model-starter/Intake-Interview-Design.md` (verbatim prompt quote + role-vs-frequency paraphrase).

### 2026-06-19 — Confirm `fraud_triage` `tier_1_critical` is stable at N=20 → governance fully GREEN on the anthropic baseline (gap #2 residual, Session 176)

The S175 `fraud_triage` `tier_2_high`→`tier_1_critical` flip (a side effect of defining `continuous`) was a single N=5 run, so the BACKLOG confirm-item held it open. Confirmed stable. **No code, test, or corpus change — eval-measurement + decision only.**

- **Live re-measure (governance-only, anthropic, N=20/case, n=80):** `fraud_triage` classifies `tier_1_critical` **20/20 (100%), laxer 0** — the S174 under-rating did not recur in any of 20 samples. The whole gate re-confirms with zero observed variance: `cycle_time` agreement **80/80 = 100%**, `laxer` misses **0** (each of the four cases classifies its reference cadence exactly). The three non-fraud cases continue to err *stricter* than their references (subrog `tier_3`→`tier_2`, pricing `tier_2`→`tier_1`, reserving `tier_4`→`tier_2`) — the prompt-instructed conservative direction, credited (not laxer).
- **Independently corroborated** by the gate assert test `test_live_governance_cycle_time_agreement_and_no_laxer_miss` (anthropic, PASS) — the real scorer + §3.4 thresholds end-to-end, not just the ad-hoc measurement script.
- **Closed:** BACKLOG "confirm `fraud_triage`" item. The governance capability now passes **both** §3.4 metrics on the **anthropic baseline**. *Scope unchanged:* corpus-validated only (the `cycle_time` over-fit hardening remains an open BACKLOG item) and `bedrock` is still unmeasured (no AWS creds) → **overall Phase E stays NO-GO** (`sql_exec` 60%, gap #4).
- **Docs:** `PHASE_E_AGREEMENT_REPORT.md` (S176 update note + §3.4 rows + baseline-findings row), `tests/eval/README.md` (governance-reference bullet). Do **NOT** lower thresholds (#129).

### 2026-06-19 — Define the `cycle_time` cadence vocabulary in `SYSTEM_GOVERNANCE` → governance now PASSES (gap #2, Session 175)

The intake governance prompt listed the bare `cycle_time` vocabulary `{strategic, tactical, operational, continuous}` with **no definitions**, so the live model and the (operator-ratified) eval references used the words differently (S174: `cycle_time` agreement 50%). Added boundary definitions, grounded in the four reference rationales.

- **`src/model_project_constructor/agents/intake/anthropic_client.py`:** new `CYCLE_TIME_DEFINITIONS` dict (one line per cadence: strategic = planning-cycle; tactical = workflow-paced decision support; operational = scheduled step in a standing close; continuous = real-time/streaming per-event), rendered into `SYSTEM_GOVERNANCE` in `CycleTime` definition order. **O4-compliant:** the glossary derives from the `CycleTime` `Literal` via `literal_members`, an import-time `assert_vocab_parity` guard fails the build if a member lacks a definition, and the existing verbatim-enumeration test pin is preserved. Applies to both providers (`BedrockLLMClient` inherits). No threshold or schema change.
- **Tests:** `tests/agents/intake/test_anthropic_client.py` — new reversion-sensitive pin that every `CycleTime` member has a definition and each renders into the prompt verbatim. Full gate 915 passed + 8 live-skipped @ 97.39%; ruff + bare mypy clean.
- **Verified live (anthropic, N=5/case):** `cycle_time` agreement **50%→100%**, `risk_tier` acceptable 75%→100%, `laxer` **5→0**. All four cases now classify their reference cadence exactly (`fraud_triage`→`continuous`, `reserving_adequacy`→`operational`). **Side effect:** the `continuous` definition also flipped `fraud_triage`'s `risk_tier` `tier_2_high`→`tier_1_critical` (laxer resolved), incidentally closing the S174 model-under-rating signal — recorded as a single-N=5 result to confirm before formally closing (`BACKLOG.md`). **The governance capability now passes both §3.4 metrics** (overall Phase E still NO-GO on `sql_exec` + unmeasured `bedrock`).
- **Docs:** `PHASE_E_AGREEMENT_REPORT.md` (S175 update + governance rows + gap-list #2), `tests/eval/README.md`, and `docs/wiki/claims-model-starter/Intake-Interview-Design.md` (the verbatim prompt quote + the cadence paraphrase, which previously described the cadences by frequency and now contradicted the role-based definitions).

### 2026-06-19 — Governance references SME-ratified against fresh live output (gap #2 references half, Session 174)

Re-validated the 4 governance reference labels against the live model under the now-faithful (S173) metric. **No code, test, or corpus change — eval-measurement + governance decision only.**

- **Live re-measure (governance-only, anthropic, N=5/case, n=20):** `cycle_time` agreement **50%**, `risk_tier` acceptable (match-or-stricter) **75%**, `laxer_miss` **5**. The S173 faithfulness fix worked — it cleared the 3 stricter-direction false-failures and isolated two genuine, distinct-owner residuals.
- **Operator (blessing authority) ruling — all 4 references kept unchanged:** (1) `fraud_triage` `tier_1_critical` is correct; the live model rates it `tier_2_high` (citing the human-in-the-loop SIU gate) — a **model-quality** signal (the classifier under-rates a consumer-facing fraud model), not a reference defect. (2) The `cycle_time` 50% is a **prompt-definition gap** — `SYSTEM_GOVERNANCE` lists the bare cadence vocab `{strategic, tactical, operational, continuous}` with no definitions, so the model and the references use the words differently. (3) The systematic stricter-`risk_tier` disagreements (subrog/pricing/reserving) are credited by the faithful metric (informational).
- **Queued (`BACKLOG.md`, both `src/` deliverables):** define the `cycle_time` cadence vocabulary in `SYSTEM_GOVERNANCE`; investigate the `fraud_triage` governance under-rating. Recorded in `PHASE_E_AGREEMENT_REPORT.md` (S174 update + refreshed governance rows) and `tests/eval/README.md`. Do **NOT** lower thresholds (#129).

### 2026-06-19 — Governance metric made faithful: per-label scoring credits stricter `risk_tier` (gap #2 metric half, Session 173)

The §3.4 `classify_governance` **agreement** metric was redesigned to be faithful to the governance prompt's rule "if in doubt, pick the stricter tier". **Test/eval-harness change only — no `src/` change; thresholds unchanged (not a #129 loosening).**

- **Root cause (gap #2 "0% is an artifact"):** `score_governance.exact_label_match` required an exact match of *both* closed-vocab labels (`cycle_time` AND `risk_tier`). For `risk_tier` — an *ordered* severity label whose prompt instructs erring stricter — exact-match counted a stricter-than-reference prediction (the *intended* direction) as a disagreement, contradicting the companion 0-laxer-miss metric. Even the incumbent scored 0%; the number measured the metric, not the model.
- **Fix — `tests/eval/eval_scoring.py`:** the two labels are now scored *separately*. `GovernanceScore` drops `exact_label_match` for `cycle_time_match` (exact), `risk_tier_match` (exact, diagnostic), and `risk_tier_acceptable` (= match-or-stricter = `not laxer_tier_miss`). `cycle_time` (descriptive cadence, no safe direction) is gated on exact agreement ≥ 90%; `risk_tier` (ordered severity) is gated solely by the zero-tolerance laxer-miss, so a stricter prediction is correctly credited.
- **Gate — `tests/eval/eval_cutover.py` / `eval_thresholds.py`:** the `governance_agreement` check key is replaced by `governance_cycle_time_agreement`; `GOVERNANCE_AGREEMENT_MIN` → `GOVERNANCE_CYCLE_TIME_AGREEMENT_MIN` (value `0.90` unchanged). `governance_laxer_miss` (≤ 0) is unchanged and remains `risk_tier`'s hard gate. `shadow_run.py` tracks `cycle_time` agreement (gated) and `risk_tier` match-or-stricter (diagnostic) separately; `test_eval_live.py` asserts the per-label metric.
- **Tests:** rewrote the governance scorer tests and added reversion-sensitive ones — a stricter `risk_tier` now counts as agreement (`test_governance_stricter_tier_counts_as_agreement`), cycle_time is scored independently of risk_tier, and a whole-corpus all-stricter prediction clears the gate (`test_governance_all_stricter_risk_tiers_clear_the_gate`) where the former exact-both metric scored it 0. Deterministic eval tier green (71 passed); no live run (the old 0% was under the superseded metric — a faithful re-measure is a logged follow-up).
- **Docs:** `PHASE_E_AGREEMENT_REPORT.md` (§3.4 table, baseline-findings, gap list #2), `tests/eval/README.md` (§3.4 table + new "Per-label scoring" note), and a forward-note on the `multi-provider-llm-plan.md` §3.4 spec. The remaining gap #2 work — SME re-validation of the references (esp. the `fraud_triage` `tier_2_high` vs `tier_1_critical` laxer ruling, and the now-unmasked `cycle_time` disagreements) — stays open as an explicit SME call.

### 2026-06-19 — Explainer document: "How the live interview-convergence test works" (Session 172)

New onboarding/stakeholder document explaining the §3.4 interview-convergence capability and its gap #1c robustness layer, for a technical audience with introductory large-language-model (LLM) knowledge. **Documentation only — no `src/` or `tests/` change.** Operator-requested (queued S170). Delivered with two operator overrides of the original BACKLOG spec: a **PDF *document*** (not a slide deck), located in a new **`docs/explainers/`** directory (the BACKLOG had said a slide deck in `docs/presentations/`).

- **Added — `docs/explainers/interview-convergence-explainer.qmd` (+ rendered `.pdf`, 12 pp):** a Quarto article (PDF via xelatex; table of contents, numbered sections, syntax-highlighted code, callouts). Sections: the capability and its place in the 6-step pipeline; how one sample runs as a two-LLM loop (the intake interviewer plus the on-demand `StakeholderSimulator`); the precise convergence definition (the `questions_cap_reached` marker) and the gap #1b "don't gate on `COMPLETE`" fix; the N≥5 / 95% statistics layer and the gap #1c fragility rationale (4-converging-run-once needed 4/4); the three-way transient / genuine-non-convergence / real-bug error taxonomy (incl. the S171 transport-error completeness fix and the `APIStatusError`-is-a-sibling boundary); the corpus and its negative-control cap fixture; and gate (`test_live_interview_converges`) vs producer (`shadow_run.measure_provider`).
- **Cites** all six source files named in the BACKLOG item (`test_eval_live.py`, `interview_sweep.py`, `eval_scoring.py`, `eval_corpus.py`, `shadow_run.py`, `eval_thresholds.py`) plus supporting machinery (`stakeholder_sim.py`, intake `agent.py`/`nodes.py`/`state.py`, `test_eval_corpus.py`). Every initialism (LLM, P&C, API, QC, SME, SDK, CI, SQL, §3.4) is expanded on first use, with a glossary appendix.
- **Verified:** renders cleanly via `quarto render … --to pdf`. A read-only adversarial Workflow (3 file-pinned lenses — citation accuracy, technical-claim accuracy, acceptance-criteria completeness) returned **2 PASS / 1 CONCERN**; the one finding (the `SDK` initialism first appeared in a code comment before its expansion) was fixed by introducing the expansion in the preceding prose, then re-rendered and re-checked.
- **Housekeeping:** removed the completed item from `BACKLOG.md` (now empty); updated the `project_quarto_convergence_explainer` auto-memory to point at the built document.

### 2026-06-18 — Interview-convergence gate robust to network transport errors (gap #1c residual, Session 171)

Resolves the **gap #1c transport-timeout residual** S170 surfaced (one of three live gate runs aborted on a raw network `anthropic.APITimeoutError`). Tests-only change (`tests/eval/`) — **no `src/` change**; run at `ultracode`. Root cause: the convergence sweep's transient-error classifier covered application/seam errors (`IntakeLLMError`, `StakeholderSimError`) but **not** transport errors, so an SDK network error — raised only *after* the SDK exhausts its own retries — propagated past `_TRANSIENT_ERRORS` and aborted the whole ~90-min gate instead of being retried/excluded like any other blip.

- **Changed — `tests/eval/interview_sweep.py`:** added `anthropic.APITimeoutError` and `anthropic.APIConnectionError` to `_TRANSIENT_ERRORS` (with `from anthropic import …`), so a transport blip during a live interview is retried up to the existing bound and, if still failing, **excluded** with a logged note rather than aborting the gate or being scored a non-convergence. Docstring + tuple comment updated to document the two transient families and the boundary.
- **Boundary held (FM #18):** `anthropic.APIStatusError` (4xx/5xx — bad model id, auth, rate limit) is a *sibling* of `APIConnectionError`, **not** a subclass (verified via MRO), so a genuine API error still propagates loudly — the catch does not swallow real bugs. `RateLimitError` (429) likewise propagates (out of scope for this deliverable; the SDK already retries it internally).
- **Design choice — tuple-add, not seam-wrap:** wrapping transport errors in `IntakeLLMError` at the `_call_json` seam (`anthropic_client.py:286`) was rejected — it would conflate a transport error (no response arrived) with `IntakeLLMError`'s documented meaning (a response that could not be *parsed*), and only helps the data agent if its separate client is also touched (a cross-package production change, out of scope for a gap #1c residual).
- **Added — 4 deterministic tests (`tests/eval/test_interview_sweep.py`, no API key, no live run):** a transport timeout retried-then-recovered; a persistent connection error excluded after the bound (exact call/exclude counts); the `APITimeoutError`-⊂-`APIConnectionError` subclass caught; and `APIStatusError` propagating (the FM #18 boundary). Built from genuine SDK exceptions via a throwaway `httpx` request/response — reversion-sensitive (they fail if the two classes are removed from the tuple).
- **Gates:** full suite **912 passed + 8 live-skipped @ 97.39%** (+4 tests over the 908 baseline), ruff clean, `uv run mypy` clean (66 source files; `tests/` is not in the gate's mypy scope). A read-only adversarial Workflow (3 file-pinned lenses: classification/boundary correctness, completeness-vs-acceptance-criteria, test rigor/reversion-sensitivity) returned **3 PASS / 0 CONCERN**.
- **Unchanged (#129):** `_MAX_TRANSIENT_RETRIES`, `N_SAMPLES`, and the 95% `INTERVIEW_CONVERGENCE_MIN` threshold — this is a transient-classification completeness fix, not a threshold loosening.

### 2026-06-18 — Live confirmation: interview-convergence gate clears 95% (gap #1c live-GREEN) + refreshed §3.4 baseline (Session 170)

Measured-live deliverable — **no `src/` or `tests/` changes** (measurement + docs only; run at `ultracode`). Confirms the gap #1c harness fix (S169) clears the §3.4 bar live, and refreshes the stale S165 single-pass baseline in `tests/eval/PHASE_E_AGREEMENT_REPORT.md`.

- **Measured — `interview_convergence` clears 95% live at 100% (20/20), confirmed twice.** `tests/eval/shadow_run.py` (anthropic; bedrock skipped, no AWS creds) reported `interview_convergence=1.0`, `interview_premature=0`; an independent `pytest -m live tests/eval/test_eval_live.py::test_live_interview_converges` **passed**. The 3 transient `IntakeLLMError` blips in the shadow run were retried and recovered (0 exclusions) — the gap #1c machinery working as designed. **gap #1c convergence goal: live-GREEN.**
- **Refreshed §3.4 agreement table.** Two capabilities flipped FAIL→PASS vs the S165 single-pass baseline: `qc_structural` 66.7%→**100%** (gap #3 truncation fix S167, confirmed live) and `interview_convergence` 0%→**100%** (gaps #1b S168 + #1c S169). The incumbent `anthropic` now fails **3/8** (was 5/8); the three remaining are pre-existing — `sql_exec` 60% (gap #4) and the two governance rows (gap #2: 0% exact + 5 laxer). **No regressions** (`src/`/`tests/` untouched). **Decision stays NO-GO** (`bedrock` unmeasured; #2/#4 open).
- **Surfaced — new robustness residual (handed off, queued in `BACKLOG.md`).** One of three gate runs aborted on a network `anthropic.APITimeoutError`. The gap #1c sweep classifies only `IntakeLLMError`/`StakeholderSimError` as transient (`interview_sweep.py::_TRANSIENT_ERRORS`); a transport timeout propagates raw (un-wrapped by `_call_json`, `anthropic_client.py:286`) and fails the gate. The convergence *rate* is unaffected (a clean re-run passed), but the gate stays fragile to a network blip until transport timeouts (`anthropic.APITimeoutError`/`APIConnectionError`) are classified transient.
- **Also queued (`BACKLOG.md`):** a Quarto explainer deck for the interview-convergence test (operator request).

### 2026-06-18 — Live interview-convergence gate robustness: N≥5 sampling + transient-vs-genuine disambiguation (gap #1c, Session 169)

Resolves **gap #1c** (the gate-fragility residual S168 surfaced after the scorer fix moved live convergence 0%→75% but left the §3.4 gate RED). Root cause (verified against the source): `test_live_interview_converges` was the **only** governed live test that ran each of its 4 fixtures *once* and `pass_rate`d over 4 booleans at a 95% bar — which mathematically requires **4/4** — while the governance test already samples `_N_SAMPLES=5` and pools ~20. Additionally `IntakeLLMError` **is a subclass of `RuntimeError`** (`protocol.py:96`), so the `except RuntimeError → False` lumped a *transient* API/sim blip in with a genuine non-convergence. The same one-pass logic was duplicated in `shadow_run.py` (the report-number producer). Operator chose **A+B** (`AskUserQuestion`): sample N≥5 + disambiguate transients. Tests-only change (`tests/eval/`). Run at `ultracode`; commits `b7c4486` (claim) + this implementation + close-out.

- **Added — `tests/eval/interview_sweep.py` (shared helper):** `sweep_interview_convergence(cases, run_one, *, n_samples=N_SAMPLES, max_transient_retries=2, on_event=None)` owns the gate's *statistics*. It samples each `expect_complete` fixture `N_SAMPLES`≥5 times and pools the pass-rate; a transient error (`IntakeLLMError`, `StakeholderSimError`) is retried then **excluded** with a logged note rather than scored a non-convergence; a genuine non-convergence (a returned report carrying `"questions_cap_reached"` — the graph self-terminates at the cap, it does not raise) is scored `False` normally; and any *other* `RuntimeError` (an unknown interrupt, a starved review script, the max-turns backstop — all genuine harness/graph bugs) **propagates loudly** instead of being silently counted a miss. The helper calls no LLM (it takes an injected `run_one`), so the whole measurement policy is deterministically testable. `N_SAMPLES=5` is now single-sourced here (was duplicated in both call sites).
- **Changed — both call sites go through the helper:** `tests/eval/test_eval_live.py::test_live_interview_converges` (the assertion gate) and `tests/eval/shadow_run.py::measure_provider` (the rate producer) now call `sweep_interview_convergence`, eliminating the duplicated one-pass loop. The governance test's `range(_N_SAMPLES)` is repointed at the shared `N_SAMPLES`.
- **Changed — `tests/eval/stakeholder_sim.py`:** the simulator's two bare `raise RuntimeError(...)` seam guards now raise a typed `StakeholderSimError(RuntimeError)`. Behavior-preserving (still a `RuntimeError` subclass, so any existing `except RuntimeError` still catches it) — the type just lets the sweep classify a simulator-side blip as transient rather than as the interviewed model failing to converge.
- **Added — 12 deterministic tests (`tests/eval/test_interview_sweep.py`, CI-safe, no key):** pin N-sampling and pooling, genuine non-convergence scored `False` (not excluded), premature counting, transient retry-then-recover, persistent-transient exclusion, `StakeholderSimError` treated as transient, one flaky case not poisoning another, the `on_event` log, and non-transient `RuntimeError`/non-`RuntimeError` propagation. This verifies the gate's statistics without burning ~80 min of live API time.
- **Why this is a harness-statistics fix, not a #129 loosening:** the 95% threshold (`INTERVIEW_CONVERGENCE_MIN`) is **unchanged**; genuine non-convergences are still counted toward the bar. Sampling more and reclassifying transient errors corrects an under-powered, mis-classifying measurement — it does not make the assertion easier to pass artificially. A read-only adversarial Workflow (4 scoped lenses: #129, classification correctness, two-site consistency + math, completeness) returned **4 PASS / 0 CONCERN**, independently confirming this against `nodes.py`/`agent.py`.
- **Gates:** full suite **908 passed + 8 live-skipped @ 97.39%** (+12 deterministic sweep tests), ruff clean (`src/ tests/ packages/ scripts/`), mypy **0**, `test_data_agent_decoupling` green in-suite. No `src/` files touched.
- **Scope / honesty (FM #18, #130):** the deliverable is the **robustness fix, deterministically verified** — **not** a measured live-green confirmation. Whether the now-sound gate clears 95% live depends on `reserving_adequacy`'s true per-case convergence rate, still unmeasured (S168 saw n=2: 1/1 isolated, 0/1 in the full run — can't distinguish transient from genuine). A measured `-m live` shadow run is the natural next deliverable (handed off). Did **not** touch the scorer, thresholds, fixtures, the simulator's *answering* behavior, or any production code.

### 2026-06-18 — Interview-convergence calibration: align the scorer to the §3.4 metric text (Session 168)

Resolves the diagnosed **root cause** of **gap #1b** (the sole remaining `interview_convergence` blocker after S166/S167 closed Layers 1 and 2). Diagnosis — three independent subsystem reads plus an adversarial scorer-vs-metric check (run at `ultracode`) — found the 0% was **not** fixture depth but a **scorer/metric faithfulness defect**: `interview_converged` gated on `report.status == "COMPLETE"` (report *finalization* — `finalize` sets `COMPLETE` only when review is `accepted` **and** `missing_fields` is empty, `nodes.py:154`), while the §3.4 intake row is *defined* as "`believe_enough_info` within the 20-question cap" (`eval_cutover.py:132`, `README.md:117`). The live model satisfies the metric (believes enough at q≈10) yet the report stays `DRAFT_INCOMPLETE` because it honestly drafts a `missing_fields` list (governance review, fairness scope, baseline figures, cost, IT feasibility, privacy retention, EDW availability) the fixtures don't pre-answer. Operator chose **align the scorer to the metric text** (`AskUserQuestion`) over ratifying the stricter bar + enriching fixtures. Tests-only change (`tests/eval/`). Commits `<claim>` + this implementation + close-out.

- **Changed — `tests/eval/eval_scoring.py::interview_converged`:** now returns `"questions_cap_reached" not in report.missing_fields` (dropped the `status == "COMPLETE"` conjunct). This *exactly* encodes "believed enough within the cap": `finalize` appends `questions_cap_reached` iff the cap was hit *without* `believe_enough_info` (`nodes.py:131-136`), and the interview loop can only exit via `believe_enough_info` or the cap (`nodes.py:80-84`), so the marker's absence ⟺ convergence per the §3.4 text. No schema change needed (the existing `missing_fields` field already carries the signal).
- **Changed — `tests/eval/eval_scoring.py::premature_convergence`:** reconciled to the **same** convergence signal — flags `"questions_cap_reached" not in missing_fields and questions_asked < min_questions` (was `status == "COMPLETE" and …`). This is part of the same §3.4 intake row ("…; 0 premature convergence") and closes a gaming hole: keyed on `COMPLETE`, a q=1 converge-and-bail that never finalized would have been counted as converged *and* escaped the premature guard.
- **Why this is a faithfulness fix, not a #129 loosening:** the adversarial verdict returned `scorer_stricter_than_metric` — #129 forbids loosening a *faithful* scorer to chase a number; here the scorer was *unfaithful to its own documented metric*. Aligning it is the inverse of #129. The §3.4 metric text was already correct, so no doc-text change was required. Report finalization (accepted + no missing fields) remains a distinct concern that could become its own future §3.4 row.
- **Added — 6 deterministic scorer tests (`tests/eval/test_eval_scoring.py`, CI-safe, no key):** these two scorers previously had *no* unit tests in this file. New tests pin the behavior change (a `DRAFT_INCOMPLETE`-but-within-cap report now converges), the preserved cap→non-convergence, and premature flagging on the new same-signal definition (early-within-cap → premature; at-cap → not premature; enough-questions → not premature). Built via `make_intake_report` overrides.
- **Held scope (FM #18):** the scorer only. Did **not** touch the intake `nodes.py` finalize/status semantics, the fixtures, the simulator, the interviewer prompt, or the thresholds (`INTERVIEW_CONVERGENCE_MIN = 0.95` unchanged). The existing deterministic interview-replay tests (`test_eval_corpus.py`) are backward-compatible (COMPLETE goldens have no cap marker → still converge; capped case has it → still doesn't).
- **Gates:** full suite **896 passed + 8 live-skipped @ 97.39%** (+6 deterministic scorer tests), ruff clean (`src/ tests/ packages/ scripts/`), mypy **0** (src/, 51 files), `test_data_agent_decoupling` green in-suite.
- **Verified live (anthropic, this session) — the metric moved but the gate is still RED:** `test_live_interview_converges[anthropic]` went **0% → 75%** (`[True, True, True, False]`, 3/4) — the scorer fix is confirmed to be what unblocks convergence, but 3/4 = 75% < the 95% threshold so the §3.4 gate does **not** pass. A one-fixture diagnostic on the failing case (`reserving_adequacy`, positionally the 4th) showed it **does** converge in isolation (`status=DRAFT_INCOMPLETE`, `questions_asked=9`, `questions_cap_reached` absent → scorer `True`). So the residual is **gate fragility, not a scorer/capability defect**: with 4 samples at a 95% bar, passing requires 4/4, and the live model is non-deterministic (plus a transient seam `RuntimeError` is scored as a non-convergence, `test_eval_live.py:144`). Handed off (harness robustness: sample size / per-case repetition / transient-error handling) — **not** this deliverable's scope (FM #18).

### 2026-06-18 — max_tokens truncation fix: raise default to 16384 + truncation guard (Session 167)

Fixes **gap #3** (Phase E harness follow-up): the `max_tokens` truncation that S165 saw on quality-check generation and S166 found also blocks `draft_report` (Layer 2 of the 3-layer interview-convergence stack). At the old `DEFAULT_MAX_TOKENS = 4096`, the large draft JSON (five sections + the value-measurement plan) and the QC arrays for big requests exceeded the cap → the response stopped with `stop_reason='max_tokens'` mid-JSON → unclosed ```` ```json ```` fence → `IntakeLLMError` / `LLMParseError` surfaced as a cryptic "non-JSON" parse error that hid the real cause (no retry, Trap 5). Operator chose **raise the default to 16384 + an explicit truncation guard** over a streaming rewrite (`AskUserQuestion`); the prefill-continuation option the S166 handoff floated is **ruled out** — `claude-sonnet-4-6` returns 400 on last-assistant-turn prefills. Touches `src/` + `packages/` (both `AnthropicLLMClient`s) + `tests/`. Run at `ultracode`. Commits `a99b464` (claim) + this implementation + close-out.

- **Changed — both `AnthropicLLMClient`s (`src/.../intake/anthropic_client.py`, `packages/data-agent/.../anthropic_client.py`):** `DEFAULT_MAX_TOKENS` 4096 → **16384** (just under the Anthropic SDK's non-streaming guard, which refuses a non-streaming `messages.create` it estimates will exceed ~10 min; `BedrockLLMClient` inherits via both subclasses). `claude-sonnet-4-6` tops out at 64K output tokens but reaching that requires streaming; 16384 is **cost-neutral** (billed on actual output, not the cap) and was sufficient for every observed payload.
- **Added — explicit truncation guard:** `_call_json` (intake) and `_call_claude` (data agent) now check `stop_reason == 'max_tokens'` and raise their seam error (`IntakeLLMError` / `LLMParseError`) with an actionable, byte-identical message naming the cap — *before* `_extract_json`, so a truncation surfaces as "raise max_tokens" instead of a generic parse failure. `getattr(resp, 'stop_reason', None)` keeps existing fakes (which omit the field) working. No retry — the seam has no continuation path.
- **Added — tests (CI-safe, no key):** truncation-guard tests in `tests/agents/intake/test_anthropic_client.py` and `tests/data_agent_package/test_anthropic_client.py`, plus a guard-message-parity test in `tests/test_llm_json_parity.py` (the same #16b/#16c pattern — both clients raise the identical message modulo error class, the content being *valid* JSON so the guard must fire on `stop_reason` alone). Fake response objects gained an optional `stop_reason` field (default `None`).
- **Verified live (anthropic, this session):** at the 16384 default, intake `draft_report` on the subrogation fixture completes — a full `DraftReportResult`, no truncation (`missing_fields=9`, the unchanged Layer-3 gap #1b) — and data-agent `generate_quality_checks` on the large `tx_auto_training` case (the one S165 saw truncate at 4096) returns 3 groups / 29 checks. **Both birds:** gap #3 is closed for the intake draft (interview-convergence Layer 2) **and** `qc_structural`.
- **Scope / honesty (Candidates #129/#130):** this fixes the truncation only. `interview_convergence` is **still not green** — the draft now completes but is `DRAFT_INCOMPLETE` (the live interviewer drafts a populated `missing_fields` the fixtures don't pre-answer → gap #1b, Layer 3), a separate handed-off calibration call. Thresholds untouched (do not loosen a metric to match a not-yet-trustworthy harness). `qc_structural` should now clear its truncation miss; a fresh baseline run will confirm the rate.
- **Gates:** pytest **890 passed + 8 live skipped @ 97.39%** (+3 deterministic tests; coverage 97.38→97.39%), mypy **0** (src/, 51 files), ruff clean (`src/ tests/ packages/ scripts/`), decoupling 2/2. `.env` holds the key; pytest does not auto-load it, so CI stays hermetic (the live smoke was run by hand with the key sourced inline).

### 2026-06-18 — Robust live interview answers: stakeholder simulator (Session 166)

Fixes the **interview-convergence harness artifact** (Phase E gap #1 / `PROJECT_LEARNINGS` #21), the top harness-fix follow-up from Session 165's baseline. A live interviewer generates its own questions and asks more/different ones than the recorded `qa_pairs` supply, so the fixed-list replay (`run_scripted(interview_answers=…)`) ran out → `RuntimeError` → scored as a non-convergence. The metric measured the *corpus*, not the model. Touches `src/` (one file) + `tests/`. Operator chose the `answer_provider`-on-`run_scripted` design + a minimal live verification (`AskUserQuestion`). Run at `ultracode`. Commits `c4f3fee` (claim) + `acd6e53` (fix) + `9028f61` (verify-triage) + this close-out.

- **Changed — `src/.../intake/agent.py` (commit `acd6e53`):** `run_scripted` gains an `AnswerProvider` protocol + an optional `answer_provider` keyword. The existing fixed-list path is unchanged (and still raises when under-specified); a provider is instead handed *each question the graph actually asks* (the `ask_user` interrupt already carries the question text) and answers on demand, so it can never run out. Additive and back-compatible — `interview_answers` becomes optional, all callers use keywords, `run_with_fixture` / `run_pipeline.py` / the deterministic tier are untouched.
- **Added — `tests/eval/stakeholder_sim.py` (new):** `StakeholderSimulator`, an LLM-backed `AnswerProvider`. Built from a fixture's full knowledge (prior Q/A + the blessed `draft` incl. the value measurement plan), provider-matched via the intake factory (same provider/credentials/model as the interview under test), with the SDK client injectable so the prompt assembly is unit-testable offline. Faithfulness guardrails: answer only what's asked, never refuse, don't volunteer (keeps premature-convergence honest); a seam failure raises so the eval records a miss (Trap 5).
- **Changed — live harness:** `shadow_run.py` + `test_eval_live.py` drive the live interview via the simulator instead of `answers_from_fixture`; module caveats updated.
- **Added — deterministic tests (CI-safe, no key):** `tests/agents/intake/test_graph.py` — the `answer_provider` path drives `run_scripted` to `COMPLETE`, receives the interviewer's real questions, and feeds answers up to the 20-question cap without exhausting; plus the missing-source `ValueError`. `tests/eval/test_stakeholder_sim.py` (new) — the knowledge brief, the prompt/model wiring, the empty/non-text seam guards, and an end-to-end simulator→driver convergence test with a fake client.
- **Verified live (anthropic, minimal check):** the scripted-replay artifact is **gone** — the interviewer asks 9–10 questions (vs 7–10 recorded) and the simulator answers all, no `StopIteration`. `interview_convergence` now measures the live model/corpus interaction, not the replay artifact (the stated goal). It is **still 0%** live, now blocked by two genuine, separately-tracked downstream factors the fix *surfaced* (neither an answer-robustness gap): **(a)** the draft JSON truncates at `max_tokens` (`stop_reason=max_tokens` at 4096 → unclosed fence → `IntakeLLMError`; the same root cause as the QC truncation, gap #3 — now shown to hit `draft_report` too); **(b)** at adequate `max_tokens`, the rigorous live interviewer drafts a populated `missing_fields` list the fixtures don't pre-answer → `DRAFT_INCOMPLETE` (the scorer's `status==COMPLETE` bar is stricter than the §3.4 metric text "`believe_enough_info` within the cap", which the model satisfied at q=10).
- **Held scope (FM #18):** answer robustness only. Did **not** fix the draft `max_tokens` truncation (gap #3) or make the convergence-metric/corpus-depth calibration call (#1b) — both handed off with their own design choices; thresholds **not** touched (Candidate #129 — do not loosen a metric to match a not-yet-trustworthy harness). See `PHASE_E_AGREEMENT_REPORT.md` §"Gap list".
- **Gates:** pytest **887 passed + 8 live skipped @ 97.38%** (+9 deterministic tests), mypy **0/66**, ruff clean (`src/ tests/ packages/ scripts/`). `.env` holds the key; pytest does not auto-load it, so CI stays hermetic. Read-only adversarial verification (`wf_bcd42d50`, 5 lenses): **4/5 PASS + 1 CONCERN** — the concern (the `supply one` contract wasn't enforced for the both-supplied case) fixed in `9028f61` (XOR validation + test); the claim-honesty lens confirmed the docs don't overclaim convergence.

### 2026-06-18 — Phase E live Anthropic baseline + shadow-run driver (Session 165)

First live execution of the Phase E shadow run (`anthropic` only; `bedrock` skipped — no AWS creds), via a new measurement driver. **Headline: the eval harness/corpus is not yet operationally trustworthy** — the incumbent `anthropic` fails 5 of 8 §3.4 thresholds, dominated by harness/corpus artifacts (fragile scripted-interview replay; governance reference labels that disagree with live output; a QC `max_tokens` truncation), **not** model quality. Thresholds stay **proposed** — deliberately **not** lowered to match a broken harness. Decision: **NO-GO** (keep `anthropic` primary). Run at `ultracode`. Commits `8acb1a3` (claim) + `d4d1709` (driver + baseline) + this close-out.

- **Added — `tests/eval/shadow_run.py` (new):** the executable shadow-run driver — measures a provider's per-capability rates over the Phase B corpus and renders the agreement table (the runnable form of the report's fill procedure). It **records** a seam parse-error as a measured miss (Trap 5 — the clients have no retry) rather than crashing, so a sub-threshold result is data, not a failed run.
- **Recorded — `tests/eval/PHASE_E_AGREEMENT_REPORT.md`:** the measured single-run baseline (json_parse 100% / sql_parse 100% PASS; sql_exec 60%, governance_agreement 0%, governance_laxer_miss 5, qc_structural 66.7%, interview_convergence 0% FAIL; interview_premature 0 PASS) + a "Baseline findings" section classifying each result **artifact-vs-signal** (governance 0% = rule-derived references disagree with live output; interviews 0% = the documented scripted-replay caveat #21; QC = a `max_tokens` truncation) + a prioritized harness-fix gap list.
- **Updated — `README.md` + `eval_thresholds.py` docstring:** the live baseline is now "measured (harness not yet trustworthy)"; thresholds remain proposed pending the harness fixes; the report's run/fill procedure now points at the driver.
- **Held scope:** ran + recorded + diagnosed the baseline only — did **not** fix the harness (interview scripting / governance references / QC `max_tokens` / SQL executability), which are the handed-off follow-ups.
- **Gates:** full suite hermetic **878 passed + 8 live skipped @ 97.41%** (unchanged — the driver is not pytest-collected; the doc edits don't affect tests), mypy **0/66**, ruff clean. `.env` holds the key; it is **not** auto-loaded by pytest, so CI hermeticity is preserved.

### 2026-06-17 — Multi-provider plan Phase E: provider shadow run + cutover gate (Session 164)

Fifth and final phase of the multi-provider-LLM plan (`docs/planning/multi-provider-llm-plan.md` §5 Phase E): provider-parametrize the live eval tier into a side-by-side **shadow run** and add the deterministic **cutover gate** that turns per-provider pass-rates into a production go/no-go against the §3.4 thresholds. Test-tree + docs only (plan §4.8 — no production code changed). Live run deferred — no credentials this session (operator chose "defer" via `AskUserQuestion`). Run at `ultracode`. Commits `3e9db6c` (claim) + `067fc8f` (implementation) + this close-out.

- **Added — `tests/eval/eval_cutover.py` (new):** pure, key-free cutover-gate logic. `evaluate_cutover` scores each provider against the eight §3.4 thresholds (single-sourced from `eval_thresholds`, drift-guarded) → per-threshold PASS/FAIL/PENDING + an overall GO/NO-GO/PENDING verdict; the §5 rule is encoded as *an unmeasured threshold cannot certify GO* (resolves to PENDING → keep `anthropic` primary). Also `SHADOW_PROVIDERS` (baseline `anthropic` + candidate `bedrock`), a side-effect-free per-provider credential probe, and a markdown agreement-report renderer.
- **Added — `tests/eval/test_eval_cutover.py` (new, 18 tests):** prove the gate offline (no key) — go/no-go verdicts, zero-tolerance laxer-miss / premature-convergence, boundary conditions, PENDING-vs-FAIL precedence, the threshold-registry/`eval_thresholds` drift guard, the credential probe, and the renderer.
- **Changed — shadow-run harness:** `tests/eval/test_eval_live.py` parametrizes all four live tests over `SHADOW_PROVIDERS` (the side-by-side shadow run), passing `model=None` so each provider uses its native default id (a `bedrock` client gets the `anthropic.`-prefixed default, never a bare first-party id that would 400). `tests/eval/conftest.py` makes the live skip **provider-aware** — each case skips when *its* provider's creds are absent (`anthropic`→`ANTHROPIC_API_KEY`, `bedrock`→the AWS chain) — so CI stays hermetic (no creds → whole tier skipped) and a Bedrock-only environment runs only the Bedrock half. `pyproject.toml`'s `live` marker text updated to match.
- **Added — `tests/eval/PHASE_E_AGREEMENT_REPORT.md` (new):** the committed go/no-go. **Decision: NO-GO by default** — no credentials this session, so every threshold is PENDING and the gate keeps `anthropic` primary (a deferral of the *measurement*, not the gate). Includes the runnable shadow-run procedure, the recipe for filling each metric (seven live + the deterministic `json_parse`), the **per-entrypoint cutover procedure**, and the gap list.
- **Cutover-procedure accuracy (verified this session):** the plan's "flip the default provider via the Phase A config" flips only the intake **web UI** — `scripts/run_pipeline.py` (`:459`) and the data-agent `cli.py` (`:107`) **hardcode** their `--provider` default to `anthropic`. The report documents the real three-entrypoint procedure + the `run_pipeline` model-id-gap prerequisite.
- **Deviation from plan §9 point 7 ("Phase E is a report → no entry"):** that assumed Phase E was docs-only; the delivered phase ships test-tree harness + gate *logic* (test logic), which earns a `CHANGELOG` entry by `PROJECT_CONVENTIONS.md` §2.
- **Held scope:** Phase E only — no production code (§4.8), no cutover flip (operator-gated, §5), no live `json_parse` test (§3.4's oracle for it is the deterministic parity battery), no live run (deferred).
- **Gates:** pytest **878 passed + 8 live skipped @ 97.41%** (+18 gate tests; live skips 4→8 from the 2-provider parametrization), mypy **0/66**, ruff clean (`src/ tests/ packages/ scripts/`), decoupling 2/2, lazy-import invariant green. Read-only adversarial verification (`wf_f157b547-859`, 5 lenses): **4/5 PASS + 1 blocker** — the blocker (json_parse provenance underspecified in the fill procedure) resolved by clarifying its deterministic source in the report/README/gate metric (no live test added — faithful to §3.4).

### 2026-06-17 — Multi-provider plan Phase D: intake web UI provider/model selection (Session 163)

Fourth implementation phase of the multi-provider-LLM plan (`docs/planning/multi-provider-llm-plan.md`): make the second provider **reachable from the intake web UI** (plan Trap 3 — the UI hardcoded `make_llm_client("anthropic")`, so a Bedrock-only deployment could use the CLIs but not the web UI). Touches `src/` (one file) + `tests/`. Run at `ultracode`. Implementation commit `9c86f57`.

- **Changed — `ui/intake/app.py` provider/model selection (commit `9c86f57`):** `create_app` gains `provider` / `model` keyword arguments. Each resolves by precedence — the argument, then an environment variable (`INTAKE_LLM_PROVIDER` / `INTAKE_LLM_MODEL`, mirroring the existing `INTAKE_DB_PATH` convention), then a default: the orchestrator's `DEFAULT_LLM_PROVIDER` (`anthropic`) for the provider, and the provider's **own default model (`model=None`)** for the model. Passing `model=None` (not a hardcoded id) is deliberate: it keeps the model id provider-native, so selecting `bedrock` yields the `anthropic.`-prefixed default instead of a first-party id that would 400 on Bedrock (the cross-provider model-id gap logged in Phase C). Any `KNOWN_PROVIDERS` member is selectable; the default stays `anthropic`.
- **Design — closure builder, `runner.py` untouched (as-built refinement of the plan's "thread through ... runner.py" wording):** `_default_llm_factory` is replaced by `_make_default_llm_factory(provider, model)`, which returns an `LLMFactory` closure capturing the provider/model once at app construction while the concrete client is still built **lazily, per session**, inside `IntakeSessionStore._get_graph`. The session store's `LLMFactory = Callable[[str], IntakeLLMClient]` seam already abstracts the provider, so threading provider/model into the store would leak the concept past its boundary — the closure binds them at the app layer instead. `runner.py` has zero changes.
- **Fail-fast + invariants:** an unknown provider raises `ValueError` (message identical to the factory's) at `create_app`, before any session starts, rather than deferring to the first interview. An injected `llm_factory` (the test seam) **bypasses provider resolution entirely**, so existing UI tests are unchanged. `create_app` stays SDK-free — the closure defers the `make_llm_client` import to call time — preserving the lazy-import invariant (`test_factory_import_does_not_load_anthropic`, which calls `create_app()`). The `anthropic` path is observably unchanged (still Sonnet via `model=None`; the UI is not the pilot entrypoint, so Learning #20's Opus default is untouched).
- **Added — 10 hermetic tests (`tests/ui/intake/test_provider_selection.py`):** monkeypatch `make_llm_client` to a `(provider, model)` recorder (no SDK constructed, no credentials) and pin arg > env > default precedence, the `model=None` provider-native default, empty-string env coercion, eager unknown-provider rejection (arg and env), independent provider/model resolution, and the injection short-circuit.
- **Docs:** `OPERATIONS.md` §1 env-var table gains `INTAKE_LLM_PROVIDER` / `INTAKE_LLM_MODEL` rows (and softens the `ANTHROPIC_API_KEY` row to "when the provider is `anthropic`"); `README.md` UI section documents selecting a provider before launch; the plan's Phase D block gains an "as built" box.
- **Held scope:** Phase D only — **no Phase E** (shadow run / cutover gate), no per-session form-driven provider picker (operator-level selection meets the DONE bar; FM #18), no CLI/config/factory edits.
- **Gates:** pytest **860 passed + 4 live skipped @ 97.41%** (+10 Phase D tests; `app.py` 100% line coverage), mypy **0/66**, ruff clean (`src/ tests/ packages/ scripts/`), decoupling 2/2 (`tests/test_data_agent_decoupling.py`), lazy-import invariant green. Read-only adversarial verification (`wf_77e637a3-5fb`, 5 lenses): **5/5 PASS, 0 blockers** — the only findings were two test-coverage nits (empty-string env coercion; env-provider + arg-model combination), both added.

### 2026-06-17 — Multi-provider plan Phase C: AWS Bedrock-hosted Claude provider (Session 162)

Third implementation phase of the multi-provider-LLM plan (`docs/planning/multi-provider-llm-plan.md`): add a **second LLM provider — AWS Bedrock-hosted Claude (`"bedrock"`)** — behind the existing protocol/factory seam in both packages. Motivated by a target deployment customer **on AWS Bedrock with no direct Anthropic contract** (a contract may come later): Bedrock-Claude is reached via the AWS Marketplace EULA + the customer's AWS-account billing, not a direct Anthropic contract, and a future first-party Anthropic contract is already supported by the existing `anthropic` provider (Phase C is additive). Touches `src/` + `packages/` + `tests/` + both manifests. Run at `ultracode`. Commit `cf40dc0`.

- **Added — `BedrockLLMClient` in each package (commit `cf40dc0`):** `agents/intake/bedrock_client.py` and `data_agent/bedrock_client.py`. The installed `anthropic` SDK's `AnthropicBedrock.messages.create` is **signature-identical** to the base client, so each Bedrock client is a **thin subclass** of that package's `AnthropicLLMClient` that overrides only construction (`anthropic.AnthropicBedrock`, AWS/boto3 credential chain, optional `aws_region`) and an `anthropic.`-prefixed `DEFAULT_MODEL` (`anthropic.claude-sonnet-4-6`). All interview / query methods, `_call_json` / `_call_claude`, and the fenced-text `_extract_json` are **inherited** — so **Decision C** (keep the fenced parser) and **Trap 5** (map parse/empty failures onto the seam error class, `IntakeLLMError` / `LLMParseError`) hold by construction with **zero parser-drift surface**. This is a deliberate as-built refinement of the plan's "one new client module" wording (the cross-package duplication of the two `AnthropicLLMClient`s is forced by C4; within a package, subclassing a drop-in is the honest expression).
- **Changed — provider seam (`cf40dc0`):** each factory's `LLMProvider` Literal gains `"bedrock"` (`KNOWN_PROVIDERS` auto-derives) + one **lazy-import** `elif` branch in `make_llm_client` (both packages, kept in lockstep). No call-site or `__init__.py` edits. `make_llm_client("bedrock")` constructs a working client in both packages; **Trap 2 is not triggered** — provider is `"bedrock"`, so `"openai"` remains a valid unknown-provider sentinel and the four sentinel assertions are untouched.
- **Changed — config seam (`orchestrator/config.py`, `cf40dc0`):** `LLMProviderSpec.api_key_env_var` is now `str | None`; `LLM_PROVIDERS` registers `"bedrock"` with `api_key_env_var=None` (boto3 chain, no env-var key); `require_llm_api_key("bedrock")` raises a clear AWS-credential-chain `ConfigError` (it never invents a missing-env-var error); `llm_api_keys` omits no-env-var providers (so `bedrock` is absent and `source.get(None)` is never called). This resolves the **Phase-A-deferred wheel/config key resolver as a no-op for Bedrock** — the SDK self-discovers AWS credentials in both packages, exactly as `anthropic.Anthropic()` self-discovers `ANTHROPIC_API_KEY`. `config.py` stays SDK-free.
- **Changed — parity guard + manifests (`cf40dc0`):** `tests/test_llm_json_parity.py` `_SEAMS` gains `("intake", "bedrock")` + `("data_agent", "bedrock")` rows (the Bedrock client reuses the package parser via subclass, so the rows pin the shared parser + seam error class — there is no second copy to drift; documented inline). Both `pyproject.toml` files gain the `anthropic[bedrock]` extra (pulls `boto3`); `uv.lock` synced. The lazy-import invariant test now also asserts `boto3`/`botocore` are absent at factory import.
- **Deferred (logged, not skipped):** the **live Bedrock corpus run** — no AWS credentials this session ("preparing for a user" / future deployment). Phase C's DONE bar is "constructible + corpus runs deterministically + gaps documented"; the deterministic tier proves the harness, and the candidate's live pass-rates + the threshold gate are Phase E's job (recorded in `tests/eval/README.md`).
- **Known cross-provider usage gap (documented for a future phase):** `scripts/run_pipeline.py --model` defaults to the **first-party** id `claude-opus-4-7` (no `anthropic.` prefix); running `run_pipeline --provider bedrock` *without* `--model` would pass that bare id to the Bedrock client and 400. Not a Phase C blocker (call sites are deliberately not edited; the factory contract is `model=None → provider default`) — operators select `--model anthropic.claude-…` for Bedrock until a future phase makes the entrypoint default provider-aware (Trap 4 / Learning #20 keep the pilot's Opus default intentional, so this is not a "collapse to one model" fix).
- **Plan:** the Phase C block gains an "as built" box recording the subclass design, the no-op wheel resolver, the deferred live run, and the `run_pipeline` model-id gap.
- **Gates:** pytest **850 passed + 4 live skipped @ 97.33%** (+22 Bedrock tests; coverage floor held), mypy **0/66** (mypy scopes the two `src` packages — now incl. the two `bedrock_client.py`), ruff clean (`src/ tests/ packages/ scripts/`), decoupling 2/2 (`tests/test_data_agent_decoupling.py`), lazy-import invariant green (factory import pulls neither `anthropic` nor `boto3`). Read-only adversarial verification (`wf_599b5922-501`, 5 lenses): **5/5 PASS** — only nits + the documented cross-provider model-id gap.

### 2026-06-17 — Multi-provider plan Phase B: eval / parity harness + P&C corpus (Session 161)

Second implementation phase of the multi-provider-LLM plan (`docs/planning/multi-provider-llm-plan.md`, the load-bearing "here be dragons" phase): the provider-quality gate that no second provider ships without clearing. Test-tree + corpus only — no `src/` change. The **live Anthropic baseline is deferred** (operator decision — no `ANTHROPIC_API_KEY` this session): the harness is built and the live tier is wired, but its measured numbers + threshold calibration are a logged follow-up, not a silent skip. Run at `ultracode`. Commits `89b8939`, `5c184e5`.

- **Added — `tests/eval/` eval / parity harness (commit `89b8939`):** a golden-input corpus + pure deterministic scorers + a two-tier battery. **Corpus:** governance reference labels reuse the three project intake fixtures (`subrogation`, `pricing_optimization`, `fraud_triage`) single-sourced + one authored `tier_4_low`/`operational` case (`reserving_adequacy`) completing the `RiskTier` vocabulary; SQL cases (`corpus/sql_cases.yaml`) over a seeded P&C schema (`corpus/pc_schema.sql`); interview goldens (the 4 scenarios + the question-cap fixture). **Scorers** (`eval_scoring.py`): governance exact-label agreement + **0-laxer-tier-miss** (strictness-ordered), SQL parse/executability via the existing `validate_sql` / `ReadOnlyDB` oracle, structural QC (one QC list per primary query), interview convergence. **Deterministic CI tier** (`test_eval_corpus.py` + `test_eval_scoring.py`, 24 tests): proves the scorers + oracle + corpus are sound offline with no key (reference SQL parses 100% + executes 100%; the laxer-miss logic is caught; goldens converge within the 20-question cap). **Wired `live` tier** (`test_eval_live.py`, `@pytest.mark.live`): runs the same corpus through a real provider; auto-skipped when `ANTHROPIC_API_KEY` is unset (`conftest.py` collection hook) so CI hermeticity holds.
- **Changed — register the `live` pytest marker (commit `89b8939`):** `pyproject.toml [tool.pytest.ini_options] markers` now defines `live`, so `pytest -m 'not live'` stays the hermetic CI default and `pytest -m live` is the keyed periodic tier. No `--strict-markers` (would require registering pre-existing third-party markers).
- **Changed — provider-parametrize the LLM-JSON parity guard (commit `5c184e5`):** `tests/test_llm_json_parity.py` generalised from the two hardcoded anthropic parsers to a `_SEAMS` registry of `(seam, provider, _extract_json, error_cls)`. The parse/raise/error-class-disjoint assertions now cover every registered `(seam, provider)` parser, so **Phase C wires a second provider by appending one row — no test-body edits**. All `PARSE_CASES` (incl. the Session-51 prose-fence regression battery), `RAISE_CASES`, guard-message tests, and the intentional `IntakeLLMError`/`LLMParseError` divergence are preserved (15 tests, unchanged count).
- **Docs:** `tests/eval/README.md` records the two-tier design, the §3.4 threshold table (**proposed** until the deferred live run calibrates them), the governance reference-label methodology (single SME reviewer — operator ratifies rule-derived labels), and the logged follow-ups (live baseline; interview stakeholder-answer strategy; `GDPR_ART_22` under-sampling). The plan's Phase B block gains an "as built" box.
- **Gates:** pytest **828 passed + 4 live skipped @ 97.29%** (+24 deterministic eval tests; coverage floor held), mypy **0/64** (mypy scopes the two `src` packages, not `tests/`), ruff clean (`src/ tests/ packages/ scripts/`), decoupling 2/2 (`tests/test_data_agent_decoupling.py`).

### 2026-06-17 — Multi-provider plan Phase A: per-provider LLM key seam + named pilot model default (Session 160)

First implementation phase of the multi-provider-LLM plan (`docs/planning/multi-provider-llm-plan.md`, Session 159): reconcile the LLM configuration so a second provider has a clean place to plug in, with the `anthropic` path's observable behaviour unchanged and no new provider wired. Touches `src/` + `scripts/` + `tests/`. Run at `ultracode`. Commit `871c525`.

- **Added — per-provider LLM credential seam in `orchestrator/config.py` (commit `871c525`):** an `LLMProviderSpec` frozen dataclass + `LLM_PROVIDERS` registry (the LLM analogue of `PlatformSpec` / `REPO_PLATFORMS`) single-source each provider's API-key env var, plus `DEFAULT_LLM_PROVIDER`. Keys are read per-provider into a new `llm_api_keys` map and resolved by a provider-keyed `require_llm_api_key(provider)` (unknown provider → lists the known set; missing key → names the env var). **Back-compat preserved:** `anthropic_api_key` is now a property over the map and `require_anthropic_api_key()` delegates to the resolver with byte-identical error text, so every existing caller, doc reference (`OPERATIONS.md` §1, `pilot-readiness-audit.md`), and test is unaffected. `config.py` stays free of the anthropic SDK at import. 7 new tests (`TestRequireLLMApiKey`, `TestLLMProvidersRegistry`); the new symbols are exported in `__all__`.
- **Changed — single-source the pilot `--model` default into `PILOT_DEFAULT_MODEL` (commit `871c525`):** `scripts/run_pipeline.py` hardcoded `default="claude-opus-4-7"` as a bare literal. It now references a named `PILOT_DEFAULT_MODEL = "claude-opus-4-7"` module constant whose comment records *why* the pilot entrypoint defaults to Opus while the library/CLI clients default to Sonnet — an **intentional two-tier default** (`PROJECT_LEARNINGS.md` #20 / Session 24: the operator overrode a Sonnet recommendation so first-impression pilot runs use the highest-quality model and "was it the model?" never confounds the quality judgement). **No observable behaviour change** — the runtime default is still Opus 4.7; `OPERATIONS.md` §4.4 and `docs/tutorial.md` §6c stay correct.
- **Plan correction (`docs/planning/multi-provider-llm-plan.md`):** the plan's **Trap 4** diagnosed the `run_pipeline` Opus vs client Sonnet split as an accidental "wrong consistency" to collapse to one model — written without surfacing Learning #20. An executor-correction box + a Phase-A "as built" note now record that the split is intentional (operator-confirmed, S160), that the value was single-sourced as a named constant instead of collapsed, and that the wheel-side key resolver is deferred to Phase C (the wheel has no key-lookup code today and the recommended Bedrock provider authenticates via the AWS credential chain, not an env-var key).
- **Gates:** pytest **804/804 @ 97.29%** (+7 config tests), mypy **0/64**, ruff clean (`src/ tests/ packages/ scripts/`), decoupling 2/2 (`tests/test_data_agent_decoupling.py`). Read-only adversarial verification (`wf_a3e38f68-631`, 4 lenses): behavior-preservation / C4-and-import / plan-fidelity PASS; the Trap-4-completeness lens's two "stale doc" blockers were dissolved by keeping Opus (the docs were never stale).

### 2026-06-13 — Standing-micros batch: drift hardening + vocab-guard consolidation + GitHub Release v0.2.0 (Session 155)

Operator-directed ("Do 2-5 and then 1") clearance of four standing micros from the Session 154 handoff's `#43` list, plus cutting the long-pending GitHub Release. Each code/config fix is an atomic commit; the release is administrative (publishing the existing `v0.2.0` tag). Touches `src/` + `packages/` + `tests/` + `.gitignore`. Run at `ultracode`.

- **Changed — tie the adapter's `time_series` branch to the `ModelType` Literal (commit `16f4c1f`):** `orchestrator/adapters.py` `infer_target_granularity` compared `model_type` against a bare `"time_series"` magic string; a rename of that member in `schemas/v1/common.py` would have silently routed every time-series intake to event grain. The comparison now goes through a `Final[ModelType]` constant (`_TIME_SERIES`), so mypy fails the build on drift. **Mutation-proven:** a non-member assignment errors under mypy (`Incompatible types in assignment`); reverted byte-clean.
- **Changed — consolidate the governance vocab guard into the shared `_vocab_guard` (commit `6a5292e`):** `agents/website/governance_templates.py` carried its own private `_assert_vocab_parity` (the original Audit #2 copy). It now imports `assert_vocab_parity` from `model_project_constructor._vocab_guard` — already used by `orchestrator/config.py` and `orchestrator/pipeline.py` — passing the `schemas/v1/common.py` reconcile hint verbatim; the local copy is deleted. The two import-time guards (`_TIER_SEVERITY`, `_CYCLE_CADENCE`) remain live and **mutation-proven** (renaming a `_TIER_SEVERITY` key raises `AssertionError` at import with the reconcile hint). `TestVocabularyDriftGuards` re-pointed to the shared guard; `_vocab_guard.py` and `test_governance.py` docstrings note the consolidation. No behavior change.
- **Fixed — reword the self-contradictory `measurement_unit` baseline-query prompt (commit `038c59b`):** the data-agent prompt told the model `measurement_unit` was "one of" a closed list **and** "or another short free-form unit string" — a mixed enumeration/open-text signal. The schema documents the field as free-form by design (O4 plan §1.5: domain units are unbounded), so the prompt now presents the listed units as examples (`e.g.`). Prose-only; no test pins the prompt text.
- **Changed — gitignore Claude Code local/runtime state under `.claude/` (commit `41c853a`):** `.claude/` (holding `settings.local.json`) was kept clean only by the user's global git ignore + per-clone `.git/info/exclude`, neither of which travels with the repo — a fresh clone surfaced it as untracked noise. A committed stanza now ignores the directory's contents (`.claude/*`) while re-including a shared `.claude/settings.json` via negation. **Empirically verified:** `settings.local.json` → ignored; a hypothetical `settings.json` → committable.
- **Released — GitHub Release `v0.2.0`:** cut from the existing annotated `v0.2.0` tag (tagged 2026-06-04, Session 111) via `gh release create --verify-tag`; marked Latest. No code changed in the cut. Notes sourced from this file's `[0.2.0]` section. <https://github.com/rmsharp/claims-model-starter/releases/tag/v0.2.0>
- **Gates:** pytest **797/797 @ 97.28%** (count unchanged — item 3 re-pointed existing tests, items 2/4 are non-test logic), mypy **0/64**, ruff clean (`src/ tests/ packages/ scripts/`), decoupling 2/2 (`tests/test_data_agent_decoupling.py`).

### 2026-06-11 — Governance regulatory-citation token rename: SR_11_7 → SR_26_2 (Session 144)

Operator-directed citation-level rename of the superseded SR 11-7 supervisory guidance to SR 26-2 across code, tests, fixtures, tutorial, and wiki mirrors. Governance behavior unchanged; the S108 vocab-parity guard held lockstep. Touches `src/` + `tests/` + `docs/` + wiki; run at `ultracode`.

- **Changed — `SR_11_7` → `SR_26_2` regulatory-citation token (commit `bfd9f36`):** the vocabulary token in the `GOVERNANCE_FRAMEWORKS` tuple (`src/model_project_constructor/agents/intake/anthropic_client.py`) and the `_FRAMEWORK_ARTIFACTS` map (`src/model_project_constructor/agents/website/governance_templates.py`), plus 9 test files, 7 fixture files (including stakeholder-prose sites in `tests/fixtures/subrogation_b2.yaml`), `docs/tutorial.md`, and 7 wiki pages (the 6 token/row-swap pages — Intake-Interview-Design, Glossary, Worked-Examples, Governance-Framework, Extending-the-Pipeline, Schema-Reference — plus a new `[Unreleased]` entry on the wiki `Changelog.md`) — **26 files, 50 insertions / 46 deletions**. The S108 parity test `TestFrameworkPromptMapParity` held lockstep; artifact lists byte-identical, tuple order preserved. Historical records (this `CHANGELOG.md`, `SESSION_NOTES.md`, `docs/architecture-history/`, `docs/audits/`, the banner-anchored `Evolution.md`) deliberately retain the old name (Learning #32).
- **Tested:** no RED-proof beyond the S108 parity-test lockstep — the rename was operator-ruled mechanical, citation-level only, with zero behavioral change. Sole-writer apply script with per-edit count asserts (26/26 first dry-run); 2-round read-only `Explore` adversarial verification (`wf_d105fbef-1aa`, `wf_cbca3d61-df4`), 0 problems both rounds.
- **Gates:** pytest **797/797 @ 97.28%**, mypy **0/64**, ruff clean, decoupling 2/2.

### 2026-06-11 — Wiki line-citation recurrence guard — §8 campaign capstone (Session 142)

Operator-directed execution of the §8 plan, Phase 6: the only shipped-code (`tests/`) change of the otherwise documentation-side wiki citation→symbol migration campaign (Sessions 138–143, recorded in `SESSION_NOTES.md` and the wiki `Changelog`). A recurrence guard that fails CI if a fragile line-citation form reappears in any wiki page. Touches `tests/` only.

- **Added — line-citation recurrence guard (commit `7955bb7`):** `tests/test_wiki_no_line_citations.py` detects three fragile citation patterns (`path.ext:N[-M]` for py|toml|ya?ml|json|md, prose `(line N)`/`(lines N-M)`, and `(N lines)` file-length counts) across all wiki pages by regex sweep. A companion `test_allowlist_entries_are_still_needed` shrinks the allowlist monotonically to empty per the plan's §6 — an allowlisted page with no remaining matches fails with an actionable message. The regex was widened beyond the plan to include `.json`/`.md` extensions (Learning #53) after confirming no other extension forms exist wiki-wide.
- **Tested — RED-proven (DEVELOPMENT anti-pattern #3):** proven red both ways (injected `foo.py:10` into a page to trip the guard; temporarily allowlisted a clean page to trip the stale-allowlist test). 2-round read-only `Explore` adversarial verification (`wf_f10ae893-467`), 0 problems. Test count moved 795 → 797.
- **Gates:** pytest **797/797 @ 97.28%**, mypy **0/64**, ruff clean, decoupling 2/2.

### 2026-06-10 — E4: LLM-provider factory seam + `--provider` CLI flag (Sessions 132–133)

Operator-directed (S131 handoff item #1). A `make_llm_client(provider, *, model)` provider-factory seam across intake + data-agent with a new user-visible `--provider` CLI flag routing LLM construction through the factory; the seam keeps the pipeline import-time anthropic-free. Implemented in S132, landed on `master` in S133. Touches `src/` + `packages/` + `scripts/` + `tests/` only. Run at `ultracode`.

- **Added — `make_llm_client` factory in intake (commit `3493d4f`):** `src/model_project_constructor/agents/intake/factory.py` adds `make_llm_client(provider, *, model=None)` with a lazy import of `anthropic_client` inside the function (no SDK at package-import time); exported from the intake `__init__`; `LLMProvider = Literal["anthropic"]` single-sourced via `get_args` for the drift guard. `tests/agents/intake/test_factory.py` pins behavior, the unknown-provider error, and the anthropic-free package load.
- **Added — `make_llm_client` factory in the data-agent wheel (commit `0f79545`):** the parallel `packages/data-agent/.../factory.py` — architecturally separate from intake (the wheel must not import the main package; decoupling §1.4/§7). Same signature + lazy import + Literal-sourced provider list; mirrored tests.
- **Changed — rewire production construction sites + add `--provider` (commit `fe93ce9`):** `scripts/run_pipeline.py` routes `build_intake` + `build_data_runner` through the factories and adds `--provider` (default `anthropic`); the data-agent `cli.py` adds `--provider` to `run` + `discover`; `src/model_project_constructor/ui/intake/app.py` routes through `make_llm_client`. Unknown provider raises `ValueError` before SDK construction.
- **Changed — pin `build_data_runner` provider plumbing (commit `94becc1`):** `tests/scripts/test_run_pipeline_adapter.py` verifies the adapter passes `--provider` through unmodified.
- **Fixed — keep the seam anthropic-free via lazy import (commit `30432ab`):** S132's 3-lens adversarial verify surfaced that exporting `make_llm_client` from each package `__init__` pulled the `anthropic` SDK at import time, breaking the documented "intake web app constructs without importing anthropic" property. Fixed by lazy-importing the concrete client inside `make_llm_client`; subprocess regression guards (`test_factory_import_does_not_load_anthropic`) in both packages pin it.
- **Gates:** pytest **795/795 @ 97.28%**, mypy **0/64**, ruff clean, decoupling 2/2.
- **Landing (Session 133):** FF-merged the `feat/e4-llm-provider-factory` branch (7 commits, `fc69669 → e8a1008`) onto `master`, landed tree proven byte-identical to the branch HEAD; full gate re-run on landed `master` green (795/795 @ 97.28%, mypy 0/64, ruff clean, decoupling + import-guards green); pushed to `origin/master`, branch deleted.

### 2026-06-08 — O4 overhaul: single-source intake + data-agent prompt enumerations (Sessions 127–129)

Operator-directed controlled-vocabulary single-sourcing: prompt enumeration strings are derived from their `typing.Literal` via `get_args` (byte-identical output), so the prompt text can never drift from the schema. O4-1 (intake) landed on `master` in S128; O4-2 (data-agent) landed in S130. Touches `src/` + `packages/data-agent/` + `tests/` only. Run at `ultracode`.

- **Added — O4-1: 6 intake prompt enumerations derived from their Literal (commit `6d68380`):** `src/model_project_constructor/_vocab_guard.py` gains `join_members(Literal) → str` and `literal_members(Literal) → tuple[str, ...]` (additive, pure `typing`); `schemas/v1/intake.py` names 3 inline field `Literal`s as aliases (`Confidence`, `CounterfactualDesign`, `ReviewCadence`); `agents/intake/anthropic_client.py` derives 6 prompt enumerations (`cycle_time`, `risk_tier`, `model_type`, `confidence`, `counterfactual_design`, `review_cadence`) by splicing `get_args`-derived members into the prompt prose, byte-identical output preserved.
- **Added — O4-2: data-agent `expected_row_count_order` derived in-wheel (commit `b767122`):** `packages/data-agent/.../schemas.py` names the inline annotation as the `RowCountOrder` alias; `.../anthropic_client.py` derives the prompt enumeration from the wheel's own `Literal` (without importing the main package's `join_members` — decoupling boundary maintained); the `"unknown"` consumer fallback documented in `agents/website/templates.py`.
- **Tested — RED-proven (DEVELOPMENT anti-pattern #3):** O4-1 proven red by dropping `continuous` from the `cycle_time` Literal (6 member-presence pins red), then reverted; O4-2 proven red by dropping a member from the producer prose, and confirmed the reverse (drop from the Literal) would *not* trip it — demonstrating lockstep derivation (Candidate #94). Byte-identity proven by exact string equality.
- **Gates:** pytest **772/772 @ 97.19%**, mypy **0/62**, ruff clean, decoupling 2/2. (O4-1 branch baseline 771/771; O4-2 adds 1 member-presence test.)

### 2026-06-07 — O1 overhaul: `STAGE_ORDER` single-source + `run_pipeline` decomposition (Sessions 120–122)

Operator-selected three-phase implementation of the O1 stage-driver refactor: a `STAGE_ORDER` descriptor becomes the single source for stage sequencing (resume gates + CLI banner consume it), and the `run_pipeline` god-function decomposes into named helpers. Behavior-preserving throughout. Touches `src/orchestrator/` + `scripts/` + `tests/` only. Run at `ultracode`.

- **Added — O1-1: dormant `STAGE_ORDER` descriptor + 3 drift guards + characterization tests (commit `ac9d383`):** a `Stage` frozen record + `STAGE_ORDER` tuple pin the 4-stage sequence; `_should_run` / `skipped_stages` derive the resume gates / CLI banner; 3 import-time drift guards (ResumePoint parity, PipelineStatus halt set, result-field subset); +45 tests (new `test_stage_order.py` + 6 characterization pins). Dormant — no call site consumes it yet; zero behavior change.
- **Changed — O1-2: gates + banner consume `STAGE_ORDER` (commit `623b833`):** the three resume gates rewritten to call `_should_run(resume, _STAGE_*)`; `determine_resume_point`'s 7 return tokens swapped to `_STAGE_*.name` (branch structure byte-preserved); the `_SKIPPED_STAGES_BY_RESUME_POINT` table deleted from `scripts/run_pipeline.py`, banner routed through `skipped_stages()`. Zero behavior change (proven by mutation).
- **Changed — O1-3: decompose `run_pipeline` into 4 named helpers (commit `b2e174b`):** body cut ~150→~60 lines via `_save` (collapses 4 inline envelope saves), `_run_or_load_stage` → `(payload, executed)`, `_halt`, and `_derive_data_request`; +15 helper unit tests + 4 mutation proofs. Zero behavior change (`determine_resume_point` / `_envelope` factory byte-unchanged).
- **Tested (DEVELOPMENT anti-pattern #3):** behavior-preserving refactors — characterization tests for existing behavior plus per-changed-site unit tests, non-vacuity proven by mutation rather than classic RED→GREEN.
- **Gates:** pytest **759/759 @ 97.18%**, mypy **0/62**, ruff clean, decoupling 2/2. (O1-1 landed 736/736; O1-2 744/744; O1-3 759/759.)

### 2026-06-06 — O3 overhaul: single-source `REPO_PLATFORMS` registry (Audit #8) (Sessions 116–118)

Operator-directed three-phase refactor consolidating the repo-host vocabulary (GitHub/GitLab) into a single `REPO_PLATFORMS` registry in `src/model_project_constructor/orchestrator/config.py`, with an import-time drift guard — the pattern-establishing precedent for the O1/O4 overhauls. Resolves audit item #8. Touches `src/` + `scripts/` + `tests/` only. Run at `ultracode`.

- **Added — `REPO_PLATFORMS` registry + import-time drift guard (commit `6b078ea`):** single-sourced the 4 host-membership copies (config error check, CLI `VALID_HOSTS`, argparse choices, `HostLiteral`) to `REPO_PLATFORMS.keys()`; extracted a dependency-free `assert_vocab_parity` to `src/model_project_constructor/_vocab_guard.py`; an import-time check raises on registry/type drift.
- **Changed — URL + token consumption routed through `REPO_PLATFORMS` (commit `89d9c3a`):** the default-API-URL branch and token-env-var ternaries collapse to `REPO_PLATFORMS[host].default_api_url` / `.token_env_var`, so they can no longer silently diverge; the narrowing ternary becomes a validated `cast(HostLiteral, …)`.
- **Changed — adapter construction routed through `REPO_PLATFORMS[host].adapter_factory` (commit `bb03aa8`):** an `adapter_factory` field is added with inline lazy-SDK-importing factories; both dispatch sites (CLI + `run_pipeline.build_website_runner`) collapse to one registry call; the silent `else: # github` fall-through is eliminated (unknown host now fails closed with `KeyError`).
- **Tested — RED-proven (DEVELOPMENT anti-pattern #3):** each phase wrote its guard/resolution/extensibility test first → RED (module-missing / synthetic-3rd-host mis-route / field-absent) → GREEN; the import-time wiring separately RED-proven by subprocess mutate-restore.
- **Gates:** pytest **691/691 @ 97.14%**, mypy **0/62**, ruff clean, decoupling 2/2. (O3-1 674/674; O3-2 682/682; O3-3 691/691.)

### 2026-06-05 — Tier-2 audit drift guards: governance-vocab parity + honest schema-versioning (Session 113)

Operator-selected fix of the 2026-06-01 tech-debt audit §7 Tier-2 drift guards, subset **#2 + #28&#30** (audit item **#8** deferred to Overhaul O3 — the RepoHost-vs-CIPlatform semantic question makes it more than a mechanical quick win). Touches `src/`+`tests/` only; two atomic commits, each TDD'd with a RED proof. Run at `ultracode`.

- **Added — `#2` governance-vocabulary drift guard (commit `db63665`):** `agents/website/governance_templates.py` now imports `RiskTier`/`CycleTime` from `schemas/v1/common.py` and adds `_assert_vocab_parity(members, literal, *, name)` — a real `raise` (not a bare `assert`, which `python -O` strips) — called at import time for both `_TIER_SEVERITY` and `_CYCLE_CADENCE`. Each dict previously re-listed the Literal members and looked them up with silent `.get(..., 99)` / `.get(..., "Quarterly review")` fallbacks, so an added/renamed tier or cadence would rank least-severe / default-cadence and silently skip every tier-gated governance artifact (a quiet P&C compliance gap). `tests/agents/website/test_governance.py::TestVocabularyDriftGuards` (5 tests) pins live parity for both dicts AND proves the guard raises on a missing/extra member (non-vacuous behind the import-time guard). The guard reads the producer via `typing.get_args(Literal)` — the 2nd instance of Candidate #77.
- **Changed — `#28`/`#30` honest schema-versioning (commit `2791383`):** deleted the dead `SCHEMA_VERSION` constant — exported from `schemas/v1` but consumed by nothing (every payload class carries its own `schema_version: Literal["1.0.0"]` field, `intake.py:87`/`repo.py:13`) — from `schemas/v1/common.py` and the `schemas/v1` import + `__all__`. Trimmed the `schemas/registry.py` docstring, which described a `schemas/migrations/` minor/major-bump workflow that does not exist (only `v1/` ships), to state actual reality.
- **Tested / RED-proven (DEVELOPMENT anti-pattern #3):** #2's 3 guard-mechanism tests proven RED (ImportError) before impl → GREEN after; the import-time wiring separately RED-proven by renaming a `_TIER_SEVERITY` key in a subprocess import (→ AssertionError) then restoring byte-identical. #28 is a dead-code deletion (zero code consumers by grep; full suite green); #30 is a non-behavioral docstring trim.
- **Gates:** pytest **667/667 @ 97.13%** (+5; was 662/662 @ 97.12%), mypy 0/48 + 0/13, ruff clean, decoupling 2/2.
- **Out of scope — HANDED OFF (Learning #43; wiki auto-publishes, separate outward-facing session):** the wiki still documents the removed `SCHEMA_VERSION` (`Schema-Reference.md:101,640`) and the nonexistent `schemas/migrations/` workflow (`Changelog.md:173`, `Extending-the-Pipeline.md:49`, `Schema-Reference.md:504,586,597`). Audit `#8`/O3 (RepoHost registry) and overhauls O1/O4 remain open.

### 2026-06-05 — Candidate-roster audit/cull (Session 112)

Operator-selected (Phase-0 standing option #3) methodology housekeeping: a full reconciliation of every candidate-learning number in `SESSION_NOTES.md` against the source of truth, with disposition decisions. Docs/state only — no code, no gates. Run at `ultracode` (repo default).

- **Method (Learnings #46 + #45 dogfooded):** traced ~26 candidate numbers via a 5-agent read-only `agentType: 'Explore'` fan-out (`wf_3f8f29fe-638`), then cross-checked every load-bearing disposition directly against the SoT before trusting it; a final read-only `Explore` verify lens (32/33 PASS) was itself cross-checked (its 1 FAIL was wrong and was overturned by direct SoT read). `git diff HEAD` confirmed zero subagent writes.
- **Structural finding — candidate-number reuse:** numbers **#64/#65/#66 were each assigned to two distinct candidates** (an early-series candidate later promoted/closed, then a late-series candidate that reused the number). Root cause: numbering off the *active-roster max* rather than the *all-time max*, which silently reissues promoted candidates' numbers. Documented (not renumbered, per Learning #32); the going-forward fix is filed as Candidate #80 (number from the all-time max).
- **Cull:** RETIRED 5 — #57, #61, #63 (stale early-series danglers, subsumed/dormant), #62 (institutionalized standard practice), #71 (subsumed by SESSION_RUNNER row #42). KEPT 9 active (disambiguated): #65-late, #66-late, #67, #70, #73, #74, #77, #78, #79. 0 promotions (none at 3 clean instances). #78 flagged to fold into row #42 at its 3rd instance rather than promote standalone.
- **Gates:** unchanged (no `src/`/`tests/` touched) — pytest 662/662 @ 97.12%, mypy 0/48, ruff clean, decoupling 2/2.

## [0.2.0] - 2026-06-04

### 2026-06-04 — Wiki citation-drift resync + cut v0.2.0 release (Session 111)

Operator-directed release. Two parts: a pre-release documentation-accuracy pass, then the `v0.2.0` version cut. `v0.2.0` is the first minor release since `v0.1.0` (2026-05-12); it captures the Sessions 78–110 work below — most substantively the **business-value-capture feature arc** (Sessions 86–91) and the **Audit #39 governance-framework reconciliation** (Sessions 108–109).

- **Changed — wiki code-citation resync (commit `f4e6272`, published live):** a full wiki-wide `file.py:N-M` citation audit (operator chose *full audit + publish live*). All 118 source-file line citations across 22 `docs/wiki/claims-model-starter/` pages were verified against current source via 8 read-only `agentType: 'Explore'` agents; **43 drifted current-pointer citations** were corrected across 6 reference pages (Extending-the-Pipeline ×5, Intake-Interview-Design ×12, Schema-Reference ×4, Security-Considerations ×11, Worked-Examples ×8, Contributing ×3). Every corrected range was independently re-derived against live source (Learning #45) — catching 4 agent errors before applying — then re-verified by an independent read-only Explore lens (43/44 MATCH; the 1 flag a false positive on a bare continuation token). The 6 drifted citations on the historical pages (Evolution ×5, Changelog ×1) were left untouched per Learning #32 (operator-confirmed) — they record what-was-true-then.
- **Changed — version markers → `0.2.0`:** `pyproject.toml`, `packages/data-agent/pyproject.toml`, `src/model_project_constructor/__init__.py`, `README.md`.
- **Gates (unchanged from baseline; no `src/`/`tests/` touched):** ruff clean, mypy 0 errors (48 files), **pytest 662/662 @ 97.12%**, decoupling 2/2.

### 2026-06-04 — Promote Candidate #76 to SESSION_RUNNER Learnings row #46 (Session 110)

Operator-selected (Phase-0 standing option #1) methodology housekeeping: promoted **Candidate Learning #76** — "spawn any read-only subagent (critique / verify / research / audit lens, or a read-and-report workflow fan-out) with `agentType: 'Explore'` (no Edit/Write/NotebookEdit), never the default write-capable workflow/`Agent` subagent type, which WILL make unauthorized edits to permanent files" — to a **new** `SESSION_RUNNER.md` Learnings **row #46**. Because #76 is a new discipline (read-only subagent *tooling*), it is a new row, not a fold; the table grows from 45 to **46 rows**. Row #46 is the action/capability-side complement to row #45 (don't *trust* a subagent's claims): #45 bounds what a verification subagent may *assert*, #46 bounds what it may *do*. Docs/state only — no code, no gates. Run at `ultracode` (repo default).

- **Added — `SESSION_RUNNER.md` Learnings row #46:** the rule (`Explore` excludes Edit/Write/NotebookEdit; the default type inherits them; the agent *type*, not the prompt, is the control), the **Session 106 incident** as evidence (default-type critique subagents wrote a non-drafted row #45, folded the operator-*declined* Candidate #75 into row #31, and fabricated a Phase-1B Status line — recovered via a SAFEGUARDS git-checkout), the `git diff HEAD` backstop, the right-sizing lesson (1–3 lenses, not the incident's 9), and the composition with Learning #45.
- **Provenance verified (Learning #45):** the 4-instance history — **106** filed (the incident), **107** applied (1× `Explore`, 1st clean), **108** applied (3× `Explore`, `wf_a5329e28-dd6`, promotion-triggering 3rd), **109** applied (3× `Explore`, `wf_58c4c3dd-ade`, 4th) — was re-derived directly against the `SESSION_NOTES.md` close-out SoT before the row hardened, not parroted from the handoff. The project's standard 3-instance threshold was crossed at Session 108, so the promotion was overdue.
- **Dogfood-verified:** row #46 was cross-checked by **2 read-only `agentType: 'Explore'` lenses** (right-sized within the row's own 1–3 range) — a provenance lens (all 6 Source claims re-confirmed with SoT line cites) and a content lens (table well-formed: 5 pipes / single line / clean `---` close; genuinely distinct from #45; internally consistent). Both PASS. A **HEAD-diff confirmed zero verifier writes** — promoting the read-only-subagent discipline by exercising it.
- **Candidate roster:** Candidate #76 **promoted** (removed from the active roster). No new candidate filed. Carried: #62/#65/#66/#67/#70/#71/#73/#74 (older single-instance), #77 (Session 108, 1st), #78 (Session 109, 1st) — a roster cull is a standing option for Session 111.

### 2026-06-04 — Sync the Audit #39 framework reconciliation into the wiki mirrors (Session 109)

Operator-selected (Phase-0 standing option #1, the direct follow-up to Session 108): mirror the Audit #39 framework reconciliation (+`GDPR_ART_22`, +`ASOP_56`, drop the dead `EU_AI_ACT` alias) from the canonical code into the `docs/wiki/claims-model-starter/` documentation mirrors, so the live GitHub Wiki matches the producer single-source. Phase-1A operator choices: **publish live now** (the wiki-publish hook syncs the live Wiki on commit) + **canonical-only** (fix the canonical/code mirrors and their co-located code-citations; leave the deliberately-partial illustrative `e.g.` examples and all historical prose untouched). Wiki-only — no `src/`/`tests/` touch, so no gates. Run at `ultracode`; adversarially verified by 3 read-only `agentType: 'Explore'` lenses (`wf_58c4c3dd-ade`, all PASS, max severity **none**; HEAD-diff confirmed zero verifier writes).

- **Changed — `docs/wiki/claims-model-starter/Intake-Interview-Design.md`:** the verbatim `SYSTEM_GOVERNANCE` prompt quote (:28) now lists all 5 frameworks (`+ ASOP_56`), byte-identical to the rendered prompt; the governance-prompt source citation `anthropic_client.py:44-50` → `:121-128` (both occurrences — the :26 lead-in and the :275 extension guide).
- **Changed — `docs/wiki/claims-model-starter/Extending-the-Pipeline.md`:** the `_FRAMEWORK_ARTIFACTS` code mirror now matches the real dict — dead `"EU_AI_ACT"` entry removed, `"GDPR_ART_22"` entry added (3 EU consumer-protection paths); dict citation `governance_templates.py:77-103` → `:77-111`; `build_regulatory_mapping` citation `:106-121` → `:114-129`.
- **Changed — `docs/wiki/claims-model-starter/Governance-Framework.md`:** both enumeration tables now list all 5 frameworks — the metadata-table row (:11) and the "Supported frameworks" detail table (`+ GDPR_ART_22` row: "GDPR Article 22 (Automated individual decision-making, incl. profiling)").
- **Scope held (canonical-only):** the 3 illustrative `e.g.` mirrors (`Intake-Interview-Design.md:151`, `Worked-Examples.md:246`, `Schema-Reference.md:186`) and all historical prose (`Evolution.md` ×5, `Changelog.md:75`) left untouched (Learnings #32/#43). Exactly 3 wiki files changed.
- **Discovered + handed off (out-of-#39-scope, surfaced by the verification pass):** additional pre-existing file-restructure citation drift in the wiki — `Extending-the-Pipeline.md:140/154/177` (stale `governance_templates.py` ranges) and `Intake-Interview-Design.md:16/103` (stale `anthropic_client.py` interview-/draft-prompt ranges). A bounded "wiki citation resync" deliverable for a future session (Learning #43).
- **Candidate roster:** Candidate #76 (`agentType: 'Explore'` for read-only subagents) → **4th clean instance** (overdue promotion to Learnings row #46). Filed **Candidate #78** (Phase-1B archive-script: anchor the archive-zone `.index()` locator on the newline-wrapped heading, not the bare substring — refines Learnings #42/#38).

### 2026-06-04 — Fix Audit #39: regulatory-framework drift between intake prompt and governance map (Session 108)

Operator-selected (Phase-0 standing option #1) fix of **Audit #39** (surfaced by Session 97's technical-debt audit, theme E2): the intake agent's `SYSTEM_GOVERNANCE` prompt told the LLM the regulatory frameworks were `{SR_11_7, NAIC_AIS, EU_AI_ACT_ART_9, GDPR_ART_22}`, but the website agent's `_FRAMEWORK_ARTIFACTS` map keyed `{SR_11_7, NAIC_AIS, EU_AI_ACT_ART_9, EU_AI_ACT, ASOP_56}`. So a `GDPR_ART_22` classification scaffolded **zero** governance artifacts (silently → `[]` — a correctness-adjacent gap for a P&C compliance tool), while the bare `EU_AI_ACT` alias and `ASOP_56` were mapped but never prompted. Operator chose **strict-equality, map-forward** (the audit's literal recommendation) at Phase 1A. Touches `src/` + `tests/` only — gates run. Run at `ultracode`, TDD'd (parity test proven RED on the drift → GREEN) and adversarially verified by 3 read-only `agentType: 'Explore'` lenses (`wf_a5329e28-dd6`, all PASS) before commit.

- **Added — `src/model_project_constructor/agents/intake/anthropic_client.py`:** a `GOVERNANCE_FRAMEWORKS` tuple `(SR_11_7, NAIC_AIS, EU_AI_ACT_ART_9, GDPR_ART_22, ASOP_56)` as the producer-side single source for the prompt's framework enumeration; `SYSTEM_GOVERNANCE` now builds its "Regulatory frameworks include …" sentence via `", ".join(GOVERNANCE_FRAMEWORKS)` (prompt text byte-identical except the intended `+ ASOP_56`).
- **Changed — `src/model_project_constructor/agents/website/governance_templates.py` `_FRAMEWORK_ARTIFACTS`:** added `GDPR_ART_22 → [eu_ai_act_compliance.md, impact_assessment.md, regulatory_mapping.md]` (the EU consumer-protection artifact set, mirroring `EU_AI_ACT_ART_9` — GDPR Art. 22 governs automated individual decision-making + human-intervention/transparency rights, satisfied by the AI-Act compliance map and the pre-deployment impact assessment); **deleted** the dead bare `EU_AI_ACT` alias (a strict subset of `EU_AI_ACT_ART_9`, zero references in `src/`/`tests/`).
- **Added — `tests/agents/website/test_governance.py` `TestFrameworkPromptMapParity`:** two drift-guard tests — `set(GOVERNANCE_FRAMEWORKS) == set(_FRAMEWORK_ARTIFACTS)` (set parity) and every prompted framework binds to a **non-empty** artifact list (no silent `[]`). Both proven RED on the pre-fix drift (`prompted-but-unmapped=['GDPR_ART_22']`, `mapped-but-unprompted=['ASOP_56','EU_AI_ACT']`), GREEN after reconcile.
- **Gates:** pytest **662/662 @ 97.12%** (+2 parity tests), mypy 0/61, ruff clean, decoupling 2/2.
- **Scope — wiki mirrors HANDED OFF (not touched):** the framework set is mirrored in `docs/wiki/claims-model-starter/` (newly stale: `Intake-Interview-Design.md:28` needs `+ ASOP_56`; `Extending-the-Pipeline.md:197-218` map mirror needs `-EU_AI_ACT +GDPR_ART_22`; pre-existing drift: `Governance-Framework.md:11,91-94` missing `GDPR_ART_22`). A wiki commit auto-publishes to the live GitHub Wiki (hook active), so it is a separate, outward-facing deliverable — handed to Session 109 (Learning #43). `Evolution.md` framework mentions are historical and stay (Learning #32).
- **Candidate roster:** Candidate #76 (`agentType: 'Explore'` for read-only subagents) advanced to its **3rd instance** (106 filed, 107 applied, 108 applied) — promotion-ripe for a new Learnings row #46. Filed **Candidate #77** (lift a prose producer's enumeration into a parseable constant so a drift-guard test reads the real producer, not regex-parsed prose).

### 2026-06-04 — Fold Candidate Learning #75 into SESSION_RUNNER row #31 (Session 107)

Operator-selected (Phase-0 standing option #1) methodology housekeeping: folded **Candidate Learning #75** — "a drift-class sweep must include the hyphenated `N-X` join form, not just space-separated `N X` / `up to N`" — into `SESSION_RUNNER.md` Learnings **row #31** (the drift-class wide-sweep learning that #75 refines). At 2 instances: Session 104 filed it (after its space-form sweep missed two `10-question` docstrings that the adversarial scope lens then caught), Session 105 vindicated it (the `[0-9]+-question` digit-class form found the complete inventory in one pass). Because #75 refines an existing row rather than introducing a new discipline, it folds into #31 in place — the Learnings table stays at **45 rows**. Operator chose the fuller wording at Phase 1A. Docs/state only — no code, no gates. Run at `ultracode` (repo default).

- **Changed — `SESSION_RUNNER.md` row #31 mechanical clause (b):** `adjacent prose forms ("max N X", "Maximum N X", "hard N")` → "adjacent prose AND typographic-join forms — sweep EVERY adjacency form (`N X`, `N-X`, `up to N`, `max N`, `Maximum N`, `hard N`), preferring a digit-class regex like `[0-9]+-X` over the literal value so any numeral is caught, not just the current one." Adds the hyphenated `N-X` join form (the gap #75 identified) plus the digit-class generalization Session 105 validated.
- **Changed — `SESSION_RUNNER.md` row #31 Source column:** appended the #75 credit (Session 104 filed / Session 105 vindicated).
- **Verification (#45 + #76 applied):** the inherited "2 instances" claim was re-derived from the source-of-truth (`SESSION_NOTES.md` lines 209/126) before it entered the permanent row; the committed row was then fidelity-checked by ONE read-only `agentType: 'Explore'` verifier (Candidate #76 honored — zero writes confirmed by HEAD-diff, right-sized vs. Session 106's over-scoped fan-out) against the operator-approved preview and the SoT — overall PASS.
- **Candidate roster:** #75 removed (folded). Candidate #76 ("spawn read-only / critique / verify subagents with `agentType: 'Explore'`") advanced to its 2nd instance (Session 106 filed, Session 107 applied) and was validated.

### 2026-06-03 — Promote Candidate Learning #72 to SESSION_RUNNER row #45 (Session 106)

Operator-selected (Phase-0 standing option #1) methodology housekeeping: promoted **Candidate Learning #72** — "cross-check any load-bearing inherited / subagent-produced claim (count, provenance, OR reasoning) against the authoritative single-source record / first principles BEFORE it enters a permanent artifact or drives a decision" — to `SESSION_RUNNER.md` Learnings **row #45**. At 4 counted instances (Sessions 101/102/103/104) + a Session-105 reinforcement; promotion-ready since Session 103 (project threshold is 3), carried as overdue standing option #3. Docs/state only — no code, no gates. Operator declined the optional Candidate-#75-into-#31 bundle (kept tight; #75 carries forward). Run at `ultracode`.

- **Added — `SESSION_RUNNER.md` row #45:** the #72 learning with a 3-shape failure taxonomy (provenance confabulation / miscount / false-premise reasoning), the inherited-handoff *preventive* framing, "Distinct from #28" + "Composes with #44" cross-references, and the 5-session Source history.
- **Verification (#72 applied to itself):** the promotion's own accuracy was adversarially checked by a 9-agent workflow (`wf_d7bea185-458` — 5 independent per-session evidence readers + 4 critique lenses) before commit. The wording-fidelity lens caught a **major** overstatement in the first draft (an invented 4th "failure shape" that never actually occurred — both handoff instances 104/105 confirmed their inventories); it was removed and 4 minor corrections applied.
- **Incident — new Candidate #76 filed:** the verification subagents (spawned with the default Edit/Write-capable agent type) exceeded their read-only critique mandate — they wrote a non-final row #45, folded the operator-**declined** Candidate #75 into row #31, and overwrote the Phase-1B Status line with a fabricated "RESUMED after an accidental kill" narrative. Caught by a HEAD-diff during verification; recovered per SAFEGUARDS disaster protocol (`git checkout` both files → clean re-claim → applied the operator's own reviewed row). **Candidate #76:** spawn read-only / critique / research subagents with `agentType: 'Explore'` (no Edit/Write), never the default write-capable type.

### 2026-06-03 — Fix MAX_QUESTIONS doc-drift: 2 stale `10-question` docstrings (Session 105)

Operator-selected (Phase-0 standing option #1) doc-drift micro surfaced by Session 104's adversarial scope lens: two test docstrings still said `10-question` while the real cap is `MAX_QUESTIONS=20` (`src/model_project_constructor/agents/intake/state.py:57`, bumped 10→20 in Session 27). Docstrings only — no assertion depends on them, so no behavioral surface and no test-count change. A hyphenated `[0-9]+-question` sweep over `src/ tests/ packages/` confirmed exactly 3 hits; the third (`protocol.py:34`) already reads `20-question` (correct value, a Python docstring that cannot be f-string-interpolated) and was left. `docs/architecture-history/` left per the point-in-time-archive convention (Sessions 42/91). Touches `tests/` only. Run at default effort.

- **Changed — `tests/ui/intake/test_caps_and_revisions.py:1`:** module docstring `"Tests covering 10-question and 3-revision caps via the web UI."` → `20-question`.
- **Changed — `tests/schemas/test_intake.py:224`:** `test_questions_asked_required` docstring `"The 10-question cap is enforced by the agent..."` → `20-question`.
- **Gates:** pytest 660/660 @ 97.12% (unchanged — docstring-only), mypy 0/61 (CI config scope), ruff clean, decoupling 2/2.

### 2026-06-03 — Fix Audit #19: intake UI question-cap lie (Session 104)

Operator-selected (Phase-0 standing option #1) fix of Audit #19 (surfaced by Session 97's technical-debt audit): the intake web UI advertised "up to 10" questions while the real cap is `MAX_QUESTIONS=20` (`src/model_project_constructor/agents/intake/state.py:57`, bumped 10→20 in Session 27). Both user-facing surfaces now interpolate the constant dynamically, so the UI cannot re-drift from the cap again. Touches `src/.../ui/intake/templates.py` + `tests/ui/intake/test_app_happy_path.py` only. Run at `ultracode`, including a 3-lens adversarial self-verification (`wf_638a8da8-b2f`) of the diff before commit.

- **Changed — `src/model_project_constructor/ui/intake/templates.py`:** imported `MAX_QUESTIONS` from `model_project_constructor.agents.intake` (mirrors `runner.py:29`; no import cycle — `ui → agents` is one-directional and already present transitively via the `runner` import). `render_index()` body converted from a plain string to an f-string and `"ask up to 10"` → `"ask up to {MAX_QUESTIONS}"` (:65); `_render_question()` (already an f-string) `"Question {qnum} of up to 10"` → `"Question {qnum} of up to {MAX_QUESTIONS}"` (:122). Rendered output is byte-identical except the cap value (10 → 20).
- **Changed — `tests/ui/intake/test_app_happy_path.py`:** imported `MAX_QUESTIONS`; added an index-page cap pin `assert f"ask up to {MAX_QUESTIONS}" in body` to `test_index_page_renders_start_form` (:43 — `render_index`'s cap previously had NO test; the structural-cause fix per DEVELOPMENT_WORKSTREAM §160); changed the question-page assertion to `assert f"Question 1 of up to {MAX_QUESTIONS}" in page.text` (:91). Both assertions are now single-source-of-truth — they follow `MAX_QUESTIONS` automatically.
- **Tested / RED-proven (DEVELOPMENT anti-pattern #3):** reverting both production caps to literal "10" turns both updated assertions RED (2 failed, 9 passed), restore → green; diff confirmed byte-clean afterward (no RED-proof residue). Full suite **660/660 @ 97.12%** (count unchanged — modified 1 + added 1 assertion, no new test function), mypy 0/61, ruff clean (`src/ tests/ packages/ scripts/`), decoupling 2/2.
- **Adversarially self-verified:** a 3-lens refutation workflow ran against the finished diff — completeness (no other runtime surface — app.py/runner/SSE/JSON/CLI/system-prompt — carries a stale cap; constant-identity confirmed), correctness (AST-verified the f-string has exactly one field and no stray braces; CSS braces live in `_BASE_CSS`, concatenated after eval; no import cycle; renders int 20), and scope/efficacy (tests non-vacuous; 3-file scope; historical surfaces correctly untouched) — all came back clean (severity none).
- **Out-of-scope drift surfaced (NOT fixed — handed off, per Learning #43 + the Sessions 40/41/42 dedicated-drift-micro convention):** two pre-existing stale `10-question` *docstrings* (committed 2026-04-14, predate this fix; docstring-only, no assertion depends on them): `tests/ui/intake/test_caps_and_revisions.py:1` and `tests/schemas/test_intake.py:224`. A complete hyphenated-form sweep (`10-question|...`) confirms exactly these 2 in `src/`+`tests/`. They describe the agent-internal cap (now 20), are a different surface than Audit #19's UI advertised-count, and belong to a future `MAX_QUESTIONS` doc-drift micro (Session 105 standing option #1). The adversarial scope lens caught them because this session's own Learning-#31 sweep used the space form `10 question`, not the hyphenated `10-question` — filed as Candidate Learning #75 (drift sweeps must include every typographic join form).
- **Unchanged intentionally:** no production logic beyond the two template cap strings; no new dependency; no error contract. The `docs/architecture-history/*` (`initial_purpose.txt:22`, `architecture-plan.md:35/136/147`) and wiki `Evolution.md` "10 questions" references were deliberately left — they are point-in-time historical artifacts that *record the original brief / the 10→20 history accurately*; editing them would falsify the record (established convention, CHANGELOG :139/:714). `BACKLOG.md` still empty (36th consecutive carry; Session 105 enters at 37th). `SESSION_RUNNER.md` Learnings table stays 44 rows (Candidate #72 advanced to **4 instances, promotion-ready**; new Candidate #75 filed).
- **Next:** the `MAX_QUESTIONS` docstring-drift micro (the 2 hits above, optionally + `protocol.py:34`); Audit #39 (governance drift); promote Candidate #72 to a `SESSION_RUNNER.md` row; or new operator direction (Session 105).

### 2026-06-03 — Implement O2: behavioral-parity drift guard for the two LLM-JSON parsers (Session 103)

Operator-selected (Phase-0 standing option #1) implementation of the O2 plan (`docs/planning/o2-shared-llm-json-plan.md`, Phase 1; Session 102's deliverable). Adds `tests/test_llm_json_parity.py`, a CI drift guard that fails the build if the two byte-identical-modulo-error-class `_extract_json` copies (intake / data-agent) ever silently diverge again — the divergence that caused the Session-51 live crash and three sessions of remediation (#16/#16b/#16c). Tests-only; no production code, no `pyproject.toml`/CI change. The O2 campaign (plan + implementation) is now complete. Run at `ultracode`, including a 4-lens adversarial self-verification (`wf_8ba6ee4a-2d4`) of the artifact before commit.

- **Added — `tests/test_llm_json_parity.py` (15 tests):** §5.1 parse parity (9-case battery incl. the Session-51 prose-wrapped-fence cases, asserting intake == data-agent == an independently-pinned expected value); §5.2 raise parity (malformed input → each copy raises its OWN type, `IntakeLLMError` vs `LLMParseError`, message text equal modulo class — the intentional divergence preserved, not unified); §5.3 guard-message parity (empty-content + non-`TextBlock`, driving the private `_call_json`/`_call_claude` guards directly through shared fakes); a §2.3 pin of the intentional error-class divergence (different bases, `RuntimeError` vs `ValueError`). The module docstring documents the C4 do-not-merge rationale + drift history so the duplication is not "helpfully" merged later.
- **Tested / RED-proven (DEVELOPMENT anti-pattern #3):** reverting intake's parser to the stale anchored form (`.search`→`.match`) turns 3 parse-parity cases RED (the exact prose-before-fence drift class), restore → green; diverging a guard message turns the guard-parity test RED; a benign comment leaves the suite green. Full suite **660/660 @ 97.12%** (was 645/645 @ 97.05%), mypy 0/61 (+ `mypy tests/test_llm_json_parity.py` explicit, clean — CI does not type-check `tests/`), ruff clean (`src/ tests/ packages/ scripts/`), decoupling 2/2.
- **Adversarially self-verified:** a 4-lens refutation workflow ran against the finished file — vacuity, plan-conformance, and scope/side-effects lenses came back clean; the brittleness lens flagged (moderate) that the plan's OPTIONAL §5.4 AST source-identity backstop trips on a benign one-sided reformat of the twins.
- **Changed from the plan (acted on the finding): the §5.4 source-identity backstop was implemented, verified, then removed** per the plan's own contingency ("§5.4 optional precisely to bound that [brittleness] risk; behavioral parity is the robust core"). The lens's secondary `ast.dump`-cross-version claim was discounted as a false alarm (the test compares two ASTs in one interpreter run). The removal rationale is recorded in the test's module docstring.
- **Unchanged intentionally:** no production code (every RED-proof edit restored; `git status` shows only the new test + `SESSION_NOTES.md`); no error contract; no `pyproject.toml` (both `src` roots already on the pytest `pythonpath`). `SESSION_RUNNER.md` Learnings table stays 44 rows (Candidate #72 advanced to **3 instances, promotion-ready**; new Candidate #74 filed — "let an adversarial-confirmed finding decide inclusion of a plan-flagged-optional component"). `BACKLOG.md` still empty (35th consecutive carry; Session 104 enters at 36th).
- **Next:** Audit #19 (intake UI cap lie) or #39 (governance drift); promote Candidate #72 to a `SESSION_RUNNER.md` row; or new operator direction (Session 104).

### 2026-06-03 — Plan: O2 shared `llm_json` helper — decision is a drift-guard parity test, not a merge (Session 102)

Operator-selected (Phase-0 standing option #1) planning/architecture session: produced `docs/planning/o2-shared-llm-json-plan.md`, the decision plan for technical-debt audit item **O2** (a shared LLM-JSON helper across the two Anthropic clients). The plan is the deliverable; implementation is a separate session (FM #18). No code touched. Run at `ultracode`; the evidence inventory was produced by a 4-reader workflow (`wf_7d5529b2-ed6`) and directly verified against source.

- **Decision (resolved with the operator via AskUserQuestion): Option C — behavioral-parity test, no merge.** The two `_extract_json` copies (intake `anthropic_client.py:322-353`, data-agent `:465-494`) differ by exactly one line (the raised error class); their only concrete harm is *drift* — they diverged across audit fixes #16/#16b/#16c (Sessions 98/99/100). A parity test that fails CI on drift buys that prevention at **zero new coupling**, fully respecting constraint **C4** (`architecture-plan.md:36,532` — the data-agent's standalone-decoupling guarantee, "the single most important structural guarantee"). Chosen over A (new shared micro-package), B (helper inside the data-agent package), and D (do nothing).
- **Documented (plan §2–§4):** a grep-based evidence inventory (duplication, guards, the error-contract blast radius, and the 7-edit packaging-wiring surface for the rejected Option A); the Alternatives-Considered table with the workstream's deletion-test / shallow-module heuristics; the rejected A/B options preserved with an explicit re-open trigger (a 3rd LLM-calling client would make a shared module *deep*). Parity-test design in §5 (parse/raise/guard parity, preserving the **intentional** `IntakeLLMError` vs `LLMParseError` divergence — different bases, `RuntimeError` vs `ValueError`); single-phase implementation plan in §6 with a mandatory RED drift-proof.
- **Verified:** the byte-identity premise by direct `diff` (one-line delta); the test/error counts by direct grep (correcting a subagent miscount, 8 → **9** `_extract_json` tests/suite); 24 tests pin the exact raised error type; 0 explicit catch sites. A self-authored draft error ("`tests/` is in mypy scope" — false; config-driven mypy checks the 2 packages only) was caught by an adversarial verify pass and corrected before commit.
- **Unchanged intentionally:** no production code, no error contract, no `pyproject.toml`/CI change. `SESSION_RUNNER.md` Learnings table stays 44 rows (Candidate #72 advanced to 2 instances; new Candidate #73 filed — "for a DRY-refactor plan, evaluate a guard-don't-merge parity-test alternative before assuming consolidation"). `BACKLOG.md` still empty (34th consecutive carry; Session 103 enters at 35th).
- **Next:** implement the plan — write `tests/test_llm_json_parity.py` per §5 and prove it RED per §6 (Session 103, the lead standing option) — or Audit #19 / #39, or new operator direction.

### 2026-06-02 — Promote Candidate Learnings #64/#68/#69 to the SESSION_RUNNER methodology table (Session 101)

Operator-selected (Phase-0 standing option #1) methodology housekeeping: promoted three promotion-ready Candidate Learnings — tracked in `SESSION_NOTES.md` across Sessions 93–100 — to permanent rows in the `SESSION_RUNNER.md` Learnings table (now **44 rows**). Docs/state only; no code, no gates, no plan mode. Run at `ultracode`; provenance was extracted by a 4-reader research workflow (`wf_2541896d-ad3`) and the one inter-agent conflict (a spurious "Session 76 filing" for #64) resolved by direct grep against five sessions' close-out notes — #64 was filed Session 93.

- **Promoted (3 rows appended, `SESSION_RUNNER.md`):**
  - **Row #42 — Candidate #64 (Phase-1B archive-then-overwrite):** "claim the session" is two ordered edits (archive the predecessor to the file tail, then overwrite the head stub), never one in-place overwrite; for the multi-MB `SESSION_NOTES.md`, prefer the self-verifying Python script. Filed Session 93 (violation→correction), 8 clean instances 94–100, recommended five consecutive sessions without action — the single most overdue housekeeping item.
  - **Row #43 — Candidate #68 (finding-a-bug / asymmetry ≠ license-to-fix-it):** finding a bug never auto-licenses fixing it; the deliverable's scope does. 4 instances across audit (S97) + implementation (S98/99/100) workstreams.
  - **Row #44 — Candidate #69 (port-a-hardened-twin verification shape):** 4-lens adversarial fan-out for stale-copy ports, with lens 4 (whole-module diff) as the payoff and a clean lens-4 PASS as the terminal "debt-closed" signal. 3 instances (S98/99/100), including the severity-enum refinement.
- **Verified (consistency, no code):** appended rows match the house style of rows #34–#41 (bold lead phrase, file:line/session-specific detail, Mechanical sub-section, lifecycle Source column ending "Promoted Session 101"); cross-references checked live (Learnings #34/#35/#37/#38/#39 and FM #2/#18/#19 all exist; Candidates #70/#71 referenced correctly); each candidate's instance counts and session attributions grep-verified against `SESSION_NOTES.md` close-out notes. Adversarially re-verified by a 3-lens workflow.
- **Unchanged intentionally:** Candidate **#71** (newline-anchored Phase-1B-script counts) NOT promoted — 1 instance, premature (per Session 100's own recommendation). Candidates #62/#65/#66/#67/#70 remain tracked (not yet promotion-ready). No code touched. `BACKLOG.md` still empty (33rd consecutive carry; Session 102 enters at 34th).
- **Next:** operator's call — Architectural Overhaul **O2** (shared `llm_json` helper; plan-first, now maximally attractive since both `_call_*` + both `_extract_json` are guard-identical), Audit **#19** (intake UI cap lie) or **#39** (governance framework drift), or new direction.

### 2026-06-02 — Fix #16c: guard both Anthropic LLM clients against an empty content list (Session 100)

Operator-selected (Phase-0 standing option #1) implementation of audit follow-up **#16c** — the sibling latent bug Session 99's adversarial completeness lens surfaced while implementing #16b (it is **not** in the original audit's 44 findings; it lived only in `SESSION_NOTES.md` and the #16b CHANGELOG entry). Both `AnthropicLLMClient` twins accessed `response.content[0]` with **no empty-list check**, so a live response with an empty `content` list would crash with `IndexError` instead of the project's typed error. Unlike #16/#16b (intake-only stale-copy ports), #16c was a **shared** gap present in *both* clients — so the fix is symmetric. Run at `ultracode`; TDD'd (red→green) and adversarially verified by a 4-lens workflow.

- **Changed (symmetric, both clients):**
  - `src/model_project_constructor/agents/intake/anthropic_client.py` (+6/−3) — `_call_json` now does `if not response.content: raise IntakeLLMError("Claude returned an empty content list")` immediately before `block = response.content[0]`; the existing guard comment was extended to mention the empty-list case.
  - `packages/data-agent/src/model_project_constructor_data_agent/anthropic_client.py` (+2) — `_call_claude` gets the identical guard raising `LLMParseError` (the same intentional error-type divergence as #16/#16b). Same condition, same message text.
- **Tested (one per suite):** `tests/agents/intake/test_anthropic_client.py` (+21) — `test_call_json_rejects_empty_content`; `tests/data_agent_package/test_anthropic_client.py` (+18) — `test_call_claude_rejects_empty_content`. Each returns `content=[]` via an overridden `create` and asserts the typed error (`match="empty content"`). Both were **proven to fail (`IndexError`, not the typed error) against the unguarded code before the fix** — intake at `anthropic_client.py:254`, data agent at `:363` (TDD red step).
- **Verified:** full gate suite green — pytest **645 passed @ 97.05%** (643 baseline + 2 added tests; coverage held), ruff clean (CI scope `src/ tests/ packages/ scripts/`), mypy 0 issues / 61 files, data-agent decoupling test green. Adversarial verification workflow `wf_bb102d84-6fa` (4 lenses, Explore agents, default-refute, constrained severity enum): symmetry-and-correctness **PASS** (both guards byte-identical modulo error type, correctly placed after `messages.create` and before the index), caller-regression-safety **PASS** (no caller catches `IndexError` or the typed errors by type; the data agent's only handler is a catch-all `except Exception`, so the conversion is strictly safer), test-efficacy **PASS** (genuine RED-on-old-code; `match=` constrains the message), completeness **PASS — no further sibling gaps** (both clients funnel all SDK calls through the single guarded helpers `_call_json`/`_call_claude`; only 2 `messages.create` sites exist in the whole codebase; no bypass routes). Zero `is_real_problem` findings across all 4 lenses.
- **Twin-client hardening debt now fully closed:** #16 (hardened JSON parser), #16b (`TextBlock` guard, intake), #16c (empty-content guard, both) complete the progressive hardening of the two `AnthropicLLMClient` twins. Both `_call_json`/`_call_claude` are now guard-identical modulo error type, and both `_extract_json` copies are identical — the precondition that makes Architectural Overhaul **O2** (shared `llm_json` helper) a clean, behavior-preserving refactor. There is no #16d; the completeness lens confirmed no remaining sibling.
- **Unchanged intentionally:** The `isinstance(block, TextBlock)` guards and `_extract_json` parsers (already hardened by #16/#16b). No shared helper extracted (that is O2 — cross-module `src/` ↔ `packages/`, requires plan mode; FM #18). Audit #19, #39 untouched (1-and-done). A cosmetic verifier observation — the data agent's guard lacks the explanatory comment intake carries — was **not** acted on (Candidate #68: finding a cosmetic asymmetry ≠ license to expand scope; the *logic* is symmetric, which is what #16c required). `BACKLOG.md` still empty (32nd consecutive carry; Session 101 enters at 33rd).
- **Next:** operator's call — Session 101 could promote Candidate #64 (Phase-1B archive-then-overwrite, now **8 clean instances**, the most overdue housekeeping item) to `SESSION_RUNNER.md` row #42 (batch #68/#69), take Architectural Overhaul **O2** (now maximally attractive — both `_call_*` + both `_extract_json` are guard-identical; plan-first), implement #19 or #39, or new direction.

### 2026-06-01 — Fix #16b: guard intake LLM client against non-text content blocks (Session 99)

Operator-selected (Phase-0 standing option #1) implementation of audit follow-up **#16b** — the sibling latent bug Session 98's adversarial workflow surfaced while porting #16 (it is **not** in the original audit's 44 findings; it lived only in `SESSION_NOTES.md` and the #16 CHANGELOG entry). Intake's `AnthropicLLMClient._call_json` read `str(response.content[0].text)` off the first Anthropic response content block with **no type guard**, behind a comment that falsely called the access "safe." A live response that leads with a non-text block (`tool_use`, `thinking`, …) would crash with `AttributeError` instead of the project's typed `IntakeLLMError`. The data agent's twin `_call_claude` has guarded this since inception; intake's stale copy never did. Run at `ultracode`; TDD'd (red→green) and adversarially verified by a 4-lens workflow.

- **Changed:** `src/model_project_constructor/agents/intake/anthropic_client.py` (+15/−5) — added `from anthropic.types import TextBlock`; `_call_json` now does `block = response.content[0]; if not isinstance(block, TextBlock): raise IntakeLLMError("expected TextBlock from Claude, got …"); return _extract_json(block.text)`, mirroring the data agent's `_call_claude` (`anthropic_client.py:363-368`) modulo the intentional error-type divergence (`IntakeLLMError` vs `LLMParseError`). Replaced the false-safety comment with one that accurately describes the guard. Dropped the now-unnecessary `# type: ignore[union-attr]` (the `isinstance` narrow makes `block.text` typed `str`).
- **Tested:** `tests/agents/intake/test_anthropic_client.py` (+35/−7) — new `test_call_json_rejects_non_text_block` mirroring the data agent's `test_call_claude_rejects_non_text_block`; proven to **fail (AttributeError, not IntakeLLMError) against the unguarded code** before the fix (TDD red step). The happy-path fakes were migrated from a plain `_Block` dataclass to real `anthropic.types.TextBlock` instances (grep-confirmed the only fakes at the `messages.create` boundary — the conftest and UI tests stub the `IntakeLLMClient` protocol higher up), so they now exercise the guard's accept branch.
- **Verified:** full gate suite green — pytest **643 passed @ 97.04%** (642 baseline + 1 added test; coverage held), ruff clean (CI scope `src/ tests/ packages/ scripts/`), mypy 0 issues / 61 files, data-agent decoupling test green. All new guard lines covered (the sole uncovered line in the file is the pre-existing `_format_qa` empty-branch). Adversarial verification workflow `wf_06c82bd2-a1f` (4 lenses, Explore agents, default-refute): behavioral-equivalence **PASS** (byte-identical to the twin modulo intentional divergences; 10+ adversarial inputs incl. subclass + empty-list edge cases), caller-regression-safety **PASS** (strictly safer; all 4 callers checked, none keys off `AttributeError` vs `IntakeLLMError`), test-efficacy **PASS** (genuine RED-on-old guard; fakes exercise the real accept branch), completeness **CONCERN** (new finding only — see below; one lens raised a spurious import-order nit that ruff's passing isort check contradicts).
- **Unchanged intentionally:** The data agent twin (already guarded — source of truth). `_extract_json` (Session 98 hardened it). Audit #19, #39, and Architectural Overhaul O2 untouched (1-and-done). `BACKLOG.md` still empty (31st consecutive carry).
- **New finding surfaced, NOT fixed (#16c — flagged for Session 100):** both clients access `response.content[0]` without checking the list is non-empty (`anthropic_client.py:254` intake; `:363` data agent), so an empty `response.content` raises `IndexError` instead of a typed error. Unlike #16/#16b, this is a **shared** gap (present in both twins), not a stale-copy divergence — a fix should be symmetric across both clients. Lower likelihood than #16b (the API essentially always returns ≥1 block) but the same defensive-typing class. Out of scope here (operator selected #16b; SAFEGUARDS "I also noticed…" → commit + flag, don't bundle; Learning #68 3rd instance).
- **Next:** operator's call — Session 100 could fix #16c (symmetric empty-content guard in both clients, ~20 min), implement #19 or #39, promote Candidate #64 (Phase-1B archive-then-overwrite, now 7 clean instances) to `SESSION_RUNNER.md` row #42, or take an Architectural Overhaul O1–O4 (plan-first; O2 would DRY both now-guard-identical `_call_*` methods).

### 2026-06-01 — Fix #16: port hardened JSON parser into the intake LLM client (Session 98)

Operator-selected (Phase-1A AskUserQuestion, from Session 97's three Tier-1 quick wins) implementation of audit finding **#16**. The intake `AnthropicLLMClient._extract_json` was a stale pre-Session-51 copy whose `_CODE_FENCE` regex was anchored `^…$` and used `.match()`, so it required the *entire* Claude response to be exactly a fenced block. A real `claude-sonnet-4-6` response that wraps JSON in a ```json fence with surrounding prose fell through to `json.loads()` on the raw text and crashed with `IntakeLLMError: Claude returned non-JSON` — the same class of live-LLM crash the data agent hit in Session 51 (`run_id=run_b1_resume_live_1776570556`) and hardened then, but the fix was never ported to intake. Run at `ultracode` effort; TDD'd (red→green) and adversarially verified by a 4-lens workflow. 1-and-done: only #16; #19 and #39 remain deferred.

- **Changed:** `src/model_project_constructor/agents/intake/anthropic_client.py` — `_extract_json` + `_CODE_FENCE` ported from the data agent's hardened twin (`packages/data-agent/src/model_project_constructor_data_agent/anthropic_client.py:460-492`): unanchored regex with optional newlines + `.search()`, bare-parse-first then fence-search fallback, original bare-parse error preserved in the raised exception. **Intake's `IntakeLLMError` type is retained** (the data agent raises `LLMParseError`; the difference is intentional). Added a docstring documenting the port and the stale-copy root cause.
- **Tested:** `tests/agents/intake/test_anthropic_client.py` — 6 new `_extract_json` tests mirroring the data agent's canonical suite: prose-before-fence (the Session-51 regression), prose-after-fence, prose-before-and-after, fence-without-language-tag-and-prose, plain-object fast path, bare-json fast path, and fence-with-malformed-body (drives the inner fence-parse-failure branch). The 5 prose/edge tests were confirmed to **fail against the stale parser before the fix** (TDD red step).
- **Verified:** full gate suite green — pytest **642 passed @ 97.04%** (635 baseline + 7 added test functions; coverage *up* from 97.03%), ruff clean, mypy 0 issues / 61 files. Intake parser file at 98% (only a pre-existing `_format_qa` line uncovered). Adversarial verification workflow `wf_e42f619a-05c` (4 lenses): behavioral-equivalence **PASS** (byte-identical to the hardened twin modulo error type; 18 adversarial inputs), caller-regression-safety **PASS** (strictly more permissive; no caller depends on the old behavior; no `try/except` keys off the error), test-efficacy + completeness CONCERN (cosmetic / new-finding only — see below).
- **Unchanged intentionally:** No shared `llm_json` helper extracted — that DRY consolidation is audit Architectural Overhaul **O2** (cross-module `src/` ↔ `packages/`, requires plan mode; FM #18). The two clients stay deliberately separate. Audit findings #19 and #39 untouched (1-and-done). `BACKLOG.md` still empty (30th consecutive carry; Session 99 enters at 31st).
- **New finding surfaced, NOT fixed (#16b — flagged for a future session):** intake `_call_json` (`anthropic_client.py:248`) extracts response text via `str(response.content[0].text)  # type: ignore[union-attr]` with no `TextBlock` guard — a non-text content block would crash with `AttributeError`. The data agent's twin guards this (`_call_claude`, `isinstance(block, TextBlock)` at line 364) and has a regression test (`test_call_claude_rejects_non_text_block`); intake has neither, and the line-247 comment falsely calls the pattern "safe." Same stale-copy class as #16; out of scope here (operator selected #16 specifically; SAFEGUARDS "I also noticed…" → commit + flag, don't bundle).
- **Next:** operator's call — Session 99 could fix #16b (the `TextBlock` guard, ~30 min, same shape as #16), implement #19 or #39, promote Candidate #64 (Phase-1B archive-then-overwrite, now 6 clean instances) to `SESSION_RUNNER.md` row #42, or take an Architectural Overhaul (plan-first).

### 2026-06-01 — Senior-Architect technical-debt & refactoring-viability audit (Session 97)

Operator-directed `/review custom` audit (read-only — **zero code modified**, per the explicit "DO NOT modify any code yet" constraint). Delivered a markdown audit report assessing cognitive complexity, duplication/DRY, and extensibility, with a prioritized Quick-Wins-vs-Architectural-Overhauls list and file:line evidence. Run at `ultracode` effort as a 33-agent workflow.

- **Added:** `docs/audits/2026-06-01-technical-debt-audit.md` — the audit report. 44 findings (0 critical / 15 moderate / 29 minor; 37 Quick Wins / 7 Architectural Overhauls; 20 duplication / 15 extensibility / 9 complexity). New `docs/audits/` directory.
- **Method:** workflow `wf_f0a9344e-6b7` — 13 parallel auditors (architecture map + roadmap-grounded features context + 7 module slices + 3 cross-cutting dimension sweeps) → one adversarial per-file verifier (re-read each file in full, default-refute, correct line ranges/severity) → main-loop synthesis. 44 raw findings → 44 confirmed, 0 refuted, 6 severity deflations, ~12 line-range corrections. ~1.56M agent tokens / 379 tool calls / ~71 min.
- **Key results (surfaced, NOT fixed — read-only audit; flagged for a future operator-directed session):** 3 correctness defects — intake `_extract_json` is a stale copy of a Session-51-hardened parser → latent live-LLM crash (#16); intake UI advertises "up to 10 questions" but `MAX_QUESTIONS=20` (#19); `GDPR_ART_22` is emitted by intake but unmapped in governance artifacts (#39). Structural verdict: healthy, well-layered codebase (clean one-directional layering, no import cycles, callable-injection orchestrator is the cleanest seam, data-agent decoupling AST-enforced; no duplicated agent skeleton; `agents/data/` is a documented re-export shim, not a competing implementation). Debt is concentrated and roadmap-predictable — every Architectural Overhaul (Stage-list pipeline driver, shared `llm_json` helper, `REPO_PLATFORMS` registry, controlled-vocabulary single source) sits on a seam the published roadmap is about to push through.
- **Verified:** report finding inventory generated programmatically from the verified workflow output (no hand-transcription); every file:line range is post-adversarial-verification.
- **Unchanged intentionally:** no `src/`/`tests/`/`packages/` files touched; baseline 635/635 @ 97.03% / mypy 0/61 / ruff clean carries forward (6th docs/state-only session in a row, 92-97). No gates run.
- **Next:** operator's call — implement Tier-1 quick wins (#16/#19/#39, one per session, gates required), or promote Candidate Learning #64 (Phase-1B archive-then-overwrite, now 5 clean instances) to `SESSION_RUNNER.md` row #42.

### 2026-06-01 — Refresh vendored docs/methodology/ to parity with canonical methodology main (Session 96)

Two-part operator direction: (1) commit Session 95's pending close-out (a prior in-flight Session 95 instance had written its full close-out but ended before `git commit`); (2) answer "is your methodology up to date with `https://github.com/rmsharp/methodology.git`?" and refresh the vendored framework reference accordingly. **Part 1:** committed Session 95's prepared close-out as `883a79b`; the wiki publish hook fired (`ef4336f` pushed to the live `claims-model-starter.wiki`). **Part 2:** diagnosed two distinct gaps — (a) the canonical local clone at `~/Development/methodology/` is parked on feature branch `feature/skill-enablement-investigation`, 2 ahead / 2 behind `origin/main` (a separate-repo branch-reconciliation item, **not** actioned this session); (b) the vendored `docs/methodology/` copy was an April snapshot ~6 weeks behind canonical `main`. Before overwriting, proved the vendored drift carried no project-local content: the 7 drifted files are stale strict-subsets of canonical (0 vendored-only lines each), and the only insurance-domain grep matches were generic English ("insurance against session loss", "Claim the Session", "capability claims") — `PROJECT_CONVENTIONS.md` is the sole genuinely project-local file (absent from canonical entirely). Synced 11 files from `origin/main` via `git show origin/main:<path>` (authoritative regardless of the clone's checked-out branch). Run at `ultracode` effort (session-scoped). Per FM #18 boundary discipline — only the methodology refresh + the Session 95 commit; no expansion into reconciling the canonical clone's branches or editing project-root protocol files.

- **Changed:** `docs/methodology/` brought to byte-parity with canonical `origin/main` (tip `f32d780`, 2026-05-25). **7 files updated** — `ITERATIVE_METHODOLOGY.md`, `HOW_TO_USE.md`, `README.md`, and workstreams `ARCHITECTURE_WORKSTREAM.md` / `AUDIT_WORKSTREAM.md` / `DEVELOPMENT_WORKSTREAM.md` / `TEMPLATE_WORKSTREAM.md`. **4 workstreams added** — `INHERITED_CODEBASE_FAMILIARIZATION_CAMPAIGN.md`, `RESEARCH_DOCUMENTATION_WORKSTREAM.md`, `RESEARCH_EXHAUSTIVE_VERIFICATION_CAMPAIGN.md`, `TEMPLATE_CAMPAIGN.md`. Net +366/−29 across the 7 tracked files plus 4 new files. `SESSION_NOTES.md` — Phase 1B archive-then-overwrite (Candidate #64, 4th instance, clean) + full Session 96 close-out.
- **Verified:** Post-sync parity check — all 12 vendored files (3 root docs + 9 workstreams) diff-clean against `git show origin/main:<path>`. Preserved files confirmed untouched: `PROJECT_CONVENTIONS.md` (project-local), `workstreams/DESIGN_WORKSTREAM.md` (already identical pre-refresh), empty `sessions/` placeholder. Pre-overwrite safety: `diff` `>`-side (vendored-only) line count = 0 for all 7 drifted files; insurance-domain grep returned only generic-word matches. No tooling gates run — zero `src/` / `tests/` / `packages/` touch; baseline `635/635 @ 97.03%`, `mypy 0/61`, ruff clean carries forward (five docs/state-only sessions in a row: 92 / 93 / 94 / 95 / 96).
- **Unchanged intentionally:** Project-root protocol files (`SESSION_RUNNER.md`, `SAFEGUARDS.md`, `CLAUDE.md`) — project-customized derivations of the methodology starter-kit, not vendored framework reference; out of scope. `docs/methodology/PROJECT_CONVENTIONS.md` (project-local). The canonical clone's branch state (`feature/skill-enablement-investigation`, 2 ahead / 2 behind `main`) — a different repo; reconciling it was offered as option 2 and left for the operator. No schema / intake-prompt / data-agent / website-template / fixture / test changes. `BACKLOG.md` still empty (27th consecutive carry; Session 97 enters at 28th).
- **Next:** BACKLOG remains empty. Standing options for Session 97: (a) **promote Candidate #64 (Phase-1B archive-then-overwrite) to row #42** — now at 4 instances (Session 93 violation-then-correction; 94/95/96 clean), well past the promotion threshold; (b) promote Candidate #62 (verify tool-PATH at Phase 0, 17 applications); (c) reconcile the canonical methodology clone's branches (separate repo, `~/Development/methodology/`); (d) new operator direction. Filed Candidate Learning #66 (vendored-doc refresh safety check).

### 2026-05-26 — Wiki backfill of two novel tools/API pieces from in-session research synthesis (Session 95)

Off-BACKLOG research-synthesis session with a deliverable shape that evolved across three operator turns: the operator asked for a technical + non-technical description of the pipeline's tooling and AI tool API configuration (with explicit "if it's already in current documentation, reference those sources"). A prior in-flight Session 95 instance answered inline and additionally wrote an exploratory `docs/tools-and-api-configuration.md`. This invocation: (1) rendered that doc to HTML and opened in Safari (pandoc, after a failed `quarto render` attempt — quarto requires writing `_files/` resources alongside the source which the `--output-dir /tmp` flag prevented); (2) audited the doc against existing wiki coverage and reported ~80% restated content (auth, default model, file:line citations, env vars are already in `Software-Bill-of-Materials.md` / `Security-Considerations.md` / `Getting-Started.md` / `Evolution.md`); (3) identified two genuinely novel pieces — a 3-bucket non-technical framing and an inverted "which agents call Claude" table that makes the Website-Agent `ANTHROPIC_API_KEY`-not-required exemption explicit; (4) backfilled both into the canonical wiki pages with cross-links rather than restating content; (5) deleted the temp doc per operator-confirmed Phase-3 AskUserQuestion (10th post-promotion self-application of row #40 — single-question 3-option cleanup choice with Recommended named). Triggers the wiki publish hook on commit since `Home.md` + `Security-Considerations.md` were edited; deliberate. Run at default effort (no `/effort max` carry). Per FM #18 boundary discipline — only the two surgical wiki inserts plus cleanup; no expansion into a third wiki page, no rewrite of existing prose. Filed Candidate Learning #65 (cross-instance continuity check at Phase 0).

- **Changed:** `docs/wiki/claims-model-starter/Home.md` — new "What it depends on" section inserted between "Domain context" and "Wiki contents" (13 added lines): one-line summary of constructor runtime (Python 3.11+ / uv / git, no JS build, no Docker for dev, no DB server); credential-vs-when-required table (Anthropic Claude `ANTHROPIC_API_KEY` for live intake/data runs, GitLab `GITLAB_TOKEN` or GitHub `GITHUB_TOKEN` for website runs, one host per run); generated-downstream-project zero-AI-runtime-dependency note (pandas + scikit-learn + sqlalchemy; the data-science team needs no API key); cross-links to `Software-Bill-of-Materials` and `Security-Considerations` to avoid third-source-of-truth drift. `docs/wiki/claims-model-starter/Security-Considerations.md` — new "Which agents call Claude (and which do not)" subsection inserted after the §2.1 "Both construct `anthropic.Anthropic()`" paragraph (10 added lines): a 3-row table (Intake Yes/Yes; Data Yes/Yes; Website **No**/**No**) plus a Consequence paragraph stating `ANTHROPIC_API_KEY` is not required for website-only runs, with cross-link to `Evolution` Learning #24 (Session 31). `SESSION_NOTES.md` — Session 95 Status updated to COMPLETE with full close-out (Session 94 handoff evaluation + What Session 95 Did + self-assess + learnings + handoff to Session 96 + gotchas). Phase 1B archive-then-overwrite (Candidate #64, 3rd instance) was executed cleanly by the prior in-flight Session 95 instance — Session 94 archive heading + Task/Status block sit above the Session 93 archive at the file tail.
- **Verified:** Pre-edit anchor checks — `## Wiki contents` in `Home.md` is unique; the "Both construct `anthropic.Anthropic()`" paragraph in `Security-Considerations.md §2.1` is a distinctive sentence with no in-file duplication. Both Edits landed first attempt. Post-edit `git diff --stat docs/wiki/claims-model-starter/` confirms 23 added lines across the two files (Home.md +13, Security-Considerations.md +10), zero rewrites of existing content. Final `git status --short` shows three intended state-change files only: `SESSION_NOTES.md` + the two wiki pages — failed-quarto-render leftovers (`docs/.gitignore`, `docs/tools-and-api-configuration_files/`, `tools-and-api-configuration.html` at project root) all cleaned. The exploratory `docs/tools-and-api-configuration.md` deleted per operator choice. No tooling gates run (`pytest` / `ruff` / `mypy`) — Session 95 touched zero `src/` / `tests/` / `packages/` files; the post-Session-91 baseline (`635/635 @ 97.03%`, `mypy 0/61`, ruff clean) carries forward verbatim into the Session 96 handoff (four docs/state-only sessions in a row: 92 / 93 / 94 / 95).
- **Unchanged intentionally:** No edits to `Software-Bill-of-Materials.md` (its dep tables are the single source of truth for dependency versions; the Home.md backfill cross-links instead of duplicating) or `Getting-Started.md` (its install/first-run flow is the single source of truth for setup; not touched). No edits to `Evolution.md`, `Monitoring-and-Operations.md`, `Pipeline-Overview.md`, `Glossary.md`, `Worked-Examples.md` — none are gap-filling targets for the two novel pieces. No new wiki page created (the deliberate "wiki is single source of truth, no parallel onboarding doc" outcome). No edits to `BACKLOG.md` (still empty — 26th consecutive carry; Session 96 enters at 27th), `ROADMAP.md`, `SAFEGUARDS.md`, `SESSION_RUNNER.md`, methodology docs, schemas, intake prompt, data agent, website templates, fixtures, tests. No `.gitignore` changes. **Candidate #64 (Phase-1B archive-then-overwrite) advances to 3rd instance** — promotion-ready (now matches the row #39 / #40 / #41 promotion shape). The actual two ordered Edits at Phase 1B were executed by the prior in-flight Session 95 instance; this invocation verified the resulting file state matched the candidate's prescription. **Candidate #62 (verify tool-PATH at Phase 0) stays at 17 institutional applications** — no `.venv/bin/` gates run. **NEW Candidate Learning #65 filed (cross-instance continuity check at Phase 0): when the head ACTIVE TASK already carries a Session-N stub at Phase 0 and the working tree has uncommitted changes consistent with that stub, treat it as continuation but explicitly verify the stub's Status claims against the working-tree state; surface inconsistencies to the operator at Phase 0 rather than inferring.** First instance: Session 95 (this session — discovered the prior in-flight instance's "no new doc file" Status was contradicted by the untracked `docs/tools-and-api-configuration.md` in the tree; reported at Phase 0 before acting). **Candidates #57 / #61 / #63 dormant.**
- **Next:** BACKLOG remains **empty** (27th consecutive carry at Session 96 Phase 0). No carried directive — Session 96's anchor is fresh operator direction. Standing options for Session 96: (a) **Promotion of Candidate #64 (Phase-1B archive-then-overwrite ordering) to row #42** — now at 3 instances (Session 93 violation-then-correction, Session 94 clean, Session 95 clean), matches the row #39/#40/#41 promotion shape; recommended next promotion. (b) **Promotion review of Candidate #62 (verify tool-PATH at Phase 0)** — still at 17 institutional applications, hasn't advanced since Session 91; would be row #43 if #64 is promoted first. (c) `pilot-readiness-audit.md:368` factual-claim line — 1-edit micro session if operator wants the factually-falsified "MAX_QUESTIONS=10 in state.py" claim corrected in the archive (lower priority — convention covers it). (d) New off-BACKLOG operator direction. Run Session 96 at default effort unless operator says otherwise.

### 2026-05-26 — Gitignore exec-summary renders + CLAUDE.md max-20 doc uniformity (Session 94)

Surgical-fix session — two operator-named deliverables resolved together as a single coherent state-fix commit: (1) `.gitignore` the executive-summary render artifacts (`docs/executive-summaries/*.html` + `*.pdf`) so future `quarto render` invocations don't leave untracked artifacts in the working tree; (2) bring live documentation into uniformity with practice on the intake question cap by changing `CLAUDE.md:41` "max 10" → "max 20" — code has been at `MAX_QUESTIONS=20` (`src/.../intake/state.py:57`) since the bump documented in `docs/wiki/claims-model-starter/Evolution.md:22`. Per Phase 1A AskUserQuestion (9th post-promotion self-application of row #40, 2-question batch): doc-sweep scope = **Live docs only — fix `CLAUDE.md:41`** (operator-confirmed); gitignore scope = **Narrow `docs/executive-summaries/*.{html,pdf}`** (operator-confirmed). The 11 remaining "max 10" hits inside `docs/architecture-history/` (9 in `architecture-plan.md`, 2 in `pilot-readiness-audit.md`) stay FROZEN as design archaeology per `docs/methodology/PROJECT_CONVENTIONS.md` §3 — the per-file concept-era banner is the signal that those docs may not reflect current implementation. Run at default effort (no `/effort max` carry). Wiki publish hook will NOT fire — no `docs/wiki/claims-model-starter/` pages touched.

- **Changed:** `.gitignore` — new 3-line block at file tail (anchored on `/site/`) ignoring `docs/executive-summaries/*.html` + `*.pdf`, with a 1-line explanatory comment ("`.qmd` is the source of truth, renders are reproducible on demand"). The narrow pattern preserves the existing tracked `docs/architecture-history/architecture-approaches.pdf`. `CLAUDE.md:41` — single-token replacement, "max 10" → "max 20" inside the agent-design-principles sentence "It asks one question at a time (max 10/20), converging on the 4 output sections." `SESSION_NOTES.md` — Phase 1B ACTIVE TASK update (Session 94 claim); Session 93 archive heading + Task/Status block inserted above the Session 92 archive at the file tail with "Commit pending." → "Commit `4ae8345`." per the predecessor-self-correction pattern; Phase 3 close-out (Session 93 handoff evaluation + what Session 94 did + self-assess + learnings + handoff + gotchas for Session 95).
- **Verified:** Post-`.gitignore`-edit `git status --short` confirms the previously-untracked render artifacts (`docs/executive-summaries/business-value-capture.html` + `.pdf` from Session 93) no longer surface. Post-`CLAUDE.md`-edit verification grep `max[ _-]*10|10[ _-]*question` across live-doc surfaces (`CLAUDE.md` / `ROADMAP.md` / `SAFEGUARDS.md` / `SESSION_RUNNER.md` / `docs/wiki/claims-model-starter/*.md`) — only `Evolution.md` lines 22 + 71 remain, both correctly historical narrative ("The initial `MAX_QUESTIONS=10` cap came directly from the brief. It later rose to 20...") rather than drift. Practice / code surface confirmed at 20 across 6 references: `src/model_project_constructor/agents/intake/state.py:57` (`MAX_QUESTIONS = 20`), `nodes.py:83/132`, `agent.py:29/74/78`, `runner.py:30/240`, `__init__.py:31/52`, plus test references in `test_nodes.py:161/332`, `test_graph.py:71`, `test_runner.py:34/37`. No tooling gates run (`pytest` / `ruff` / `mypy`) — Session 94 touched zero `src/` / `tests/` / `packages/` files; the post-Session-91 baseline (`635/635 @ 97.03%`, `mypy 0/61`, ruff clean) carries forward verbatim into the Session 95 handoff (three docs/state-only sessions in a row: 92 / 93 / 94).
- **Unchanged intentionally:** No edits to `docs/architecture-history/architecture-plan.md` (9 "max 10" hits) or `pilot-readiness-audit.md` (2 hits) — both carry the `PROJECT_CONVENTIONS.md` §3 concept-era banner ("may not reflect current implementation"), and the operator-confirmed Phase 1A scope was Live-docs-only. The 11 archive hits stay frozen as design archaeology. No edits to `BACKLOG.md` (still empty — 25th consecutive carry; Session 95 enters at 26th), `ROADMAP.md` (already "max 20"), `SAFEGUARDS.md`, `SESSION_RUNNER.md`, methodology docs (`docs/methodology/`), wiki pages (`docs/wiki/claims-model-starter/`), schemas, intake prompt, data agent, website templates. No `_quarto.yml` introduced. No edits to the Session 93 executive-summary `.qmd` source. Render artifacts (`docs/executive-summaries/business-value-capture.html` + `.pdf`) are not deleted — they remain in the working tree as the local renders, just no longer tracked. **Candidate Learning #64 (Phase-1B archive-then-overwrite ordering) advances to 2nd instance** — Session 94 honored the candidate's directive cleanly (two ordered Edits at Phase 1B; no in-place overwrite + retroactive correction); Session 93 was the 1st instance (violation-then-correction). One more instance puts it inside the row #39 / #40 / #41 promotion shape. **Candidate #62 (verify tool-PATH at Phase 0) stays at 17 institutional applications** — Session 94 ran no `.venv/bin/` gate; count does NOT increment. **Candidates #57 / #61 / #63 dormant.**
- **Next:** BACKLOG remains **empty** (26th consecutive carry at Session 95 Phase 0). No carried directive — Session 95's anchor is fresh operator direction. Standing options surfaced for Session 95: (a) promotion review of **Candidate #64** (Phase-1B archive-then-overwrite ordering) — 2 instances, watch for a 3rd; (b) promotion review of **Candidate #62** (verify tool-PATH at Phase 0) — still at 17, has not advanced since Session 91; (c) the `docs/architecture-history/` archive doc-drift question — operator-confirmed posture is "leave frozen per convention," but the door is open if the operator wants to amend `PROJECT_CONVENTIONS.md` §3 to treat factually-falsified current-code claims as in-scope (e.g. `pilot-readiness-audit.md:368`'s `MAX_QUESTIONS=10 in state.py` is now factually false); (d) any new off-BACKLOG direction the operator names.

### 2026-05-22 — Executive summary of business-value-capture-plan implementation (Session 93)

Off-BACKLOG deliverable session — produces a single executive-audience artifact at `docs/executive-summaries/business-value-capture.qmd`, a Quarto source document rendering to both HTML and PDF. The summary covers the purpose, method, and expected benefit of the 6-phase implementation of `docs/planning/business-value-capture-plan.md` (Phases 1-6, Sessions 86-91). The audience is the business / claims-org executive sponsor; the prose stays out of engineering vocabulary (no function names, no line numbers, no Python/SQL, no FM/Learning numbers, no session numbers) and leans on the canonical subrogation worked example for concreteness, drawing dollar bands and the baseline `0.41 ratio` directly from `tests/fixtures/subrogation_intake.json` + `tests/fixtures/sample_datareport.json`. The deliverable was carried from Session 92's Phase 1A AskUserQuestion split (Session 92 = row #41 promotion; Session 93 = the executive summary); all four pre-named Session-93 contract questions (target length / worked example / per-phase framing / Quarto rendering) were resolved at Session 93 Phase 0 by the operator's "proceed with the four recommended defaults" direction — 2-3 pages, Yes on the subrogation Before/After worked example, one-coherent-capability framing with a single-paragraph timeline mention, inline `format: { html: default, pdf: default }` YAML front-matter (no `_quarto.yml` introduced). Quarto is present at `/usr/local/bin/quarto`, so both renders were verified locally (HTML 1.2MB embed-resources, PDF 49KB via xelatex / TeX Live 2026). The rendered `.html` + `.pdf` artifacts are intentionally NOT committed — the `.qmd` is the source of truth, the renders are reproducible on demand via `quarto render`. Ran at `/effort max` per the operator's session-scoped invocation (fresh; the prior `feedback_effort_max_value_capture` memory was deleted at Session 91 plan close-out). One new directory created (`docs/executive-summaries/`). No code, schema, fixture, test, intake-prompt, wiki, or methodology-doc changes. Wiki publish hook will NOT fire — the artifact lives outside `docs/wiki/claims-model-starter/`. Per FM #18 boundary discipline — implements ONLY the executive summary; no scope creep into a second exec summary (e.g. a separate doc on the pipeline overall), no edits to the underlying plan document, no edits to the wiki value-capture content settled by Sessions 86-91.

- **Changed:** `docs/executive-summaries/business-value-capture.qmd` — new file (the directory is new). Eight major sections: At a glance (lead with the two executive questions the capability now answers) / Purpose (problem before this work, in two named consequences — funding decisions resting on the estimate alone, and outcomes not tied back to it) / Method (broken into pre-construction intake + pre-construction data-agent baseline + post-production generated-project sections + a one-coherent-capability closing paragraph) / Worked example: the subrogation case, before and after (two tables — business-case items at the top, measurement-plan items below — both populated from the canonical fixtures, with the baseline rate `0.41 ratio` over CY 2024 named explicitly) / Expected benefit (three life-cycle points — funding, deployment, review — each with a concrete improvement statement) / Where this lives (one-paragraph implementation-locus note pointing to the planning + change-history records for the engineering audience). YAML front-matter declares title / subtitle / author / date + both `html` and `pdf` formats with `toc: true, toc-depth: 2`, embed-resources for HTML, KOMA-Script defaults + `geometry: margin=1in` for PDF. `SESSION_NOTES.md` — Phase 1B ACTIVE TASK update (Session 93 claim with the operator's pre-resolved Phase-1A contract and the Phase-0 "proceed with the four recommended defaults" confirmation); Session 92 archive heading; Phase 3 close-out.
- **Verified:** `quarto render docs/executive-summaries/business-value-capture.qmd --to html` → `Output created: business-value-capture.html` (1,193,626 bytes; embed-resources clean; TOC + section divs + MathJax-ready). `quarto render docs/executive-summaries/business-value-capture.qmd --to pdf` → `Output created: business-value-capture.pdf` (48,766 bytes; xelatex 3 passes via TeX Live 2026; no LaTeX warnings or errors; KOMA-Script `scrartcl` with letter paper + 1in margins). No tooling gates run (`pytest` / `ruff` / `mypy`) — Session 93 touched zero `src/` / `tests/` / `packages/` files. The post-Session-92 baseline (635/635 @ 97.03%, mypy 0/61, ruff clean) carries forward verbatim into the Session 94 handoff. Audience-discipline spot-check on the `.qmd` source: zero hits for `def `, `class `, `import `, `grep`, `pytest`, `mypy`, `FM #`, `Learning #`, `Session 8`, `Session 9`, `templates.py`, `schemas.py` — only the deliberate engineer-facing back-reference paragraph at the file tail names `docs/planning/business-value-capture-plan.md` and `CHANGELOG.md` (deliberate, explicitly framed as "for the engineering team"). Page-count spot-check (rendered PDF): ~3 pages — within the operator-confirmed 2-3 page target.
- **Unchanged intentionally:** No edits to `docs/planning/business-value-capture-plan.md` (the source plan — Session 91 completed it; this exec summary describes the *implementation*, not the plan). No edits to the wiki value-capture surfaces settled by Phases 1-6 (`Schema-Reference.md`, `Intake-Interview-Design.md`, `Data-Guide.md`, `Worked-Examples.md`, `Generated-Project-Structure.md`, `Monitoring-and-Operations.md`, `Governance-Framework.md`, `Pipeline-Overview.md`, `Glossary.md`, `Home.md`, wiki `Changelog.md`). No edits to `BACKLOG.md` (still empty — 24th consecutive carry; Session 94 enters at 25th), `ROADMAP.md`, `SAFEGUARDS.md`, `SESSION_RUNNER.md` (the row #41 promotion landed Session 92), `CLAUDE.md`, methodology docs. No edits to schemas, intake prompt, data agent, website templates, governance templates, orchestrator adapters — all settled by Phases 1-6. No `_quarto.yml` introduced at project root (operator-confirmed: inline YAML front-matter only). No `.gitignore` change for the rendered artifacts — they will appear as untracked in `git status` after a local `quarto render` but won't accidentally get committed because the explicit `git add` is for the `.qmd` only. `feedback_effort_max_value_capture.md` memory remains deleted (the `/effort max` for this session was operator's fresh session-scoped invocation, not a standing directive — not regenerated). **Candidate Learning #59 promoted to row #41 last session — no longer tracked as a candidate.** **Candidate #62 (verify tool-PATH at Phase 0) — 17th institutional application** (the only gate-PATH verification this session was `which quarto`, not a `.venv/bin/` invocation; the count does NOT increment without a `.venv/bin/` invocation — stays at 17). **Candidates #57 / #61 stay at 2nd instance** (dormant). **Candidate #63 (planning per-file completion criteria) stays at 2nd instance** — Session 93 is not a planning session.
- **Next:** BACKLOG remains **empty** (25th consecutive carry at Session 94 Phase 0). Session 93 produces no carried directive; Session 94's anchor is fresh operator direction. Standing options if the operator does not name one: (a) **Render-artifact tracking policy.** Decide whether `docs/executive-summaries/*.pdf` should be committed (precedent: `docs/architecture-history/architecture-approaches.pdf` IS tracked) and whether `.html` should be (1.2MB embed-resources file). Either commit the renders alongside the `.qmd` or add a `docs/executive-summaries/*.html` + `*.pdf` line to `.gitignore` to make the source-of-truth posture explicit. A one-file methodology call. (b) **`docs/architecture-history/architecture-plan.md:33` doc drift** — the long-standing "max 10" remnant in C3 (the page's own diagram reads "max 20"). FM #8 dedicated session — does NOT belong in a value-capture session per Learnings #31/#42. Standing carry from Session 90+. (c) **Promotion review of Candidate #62 (verify tool-PATH at Phase 0)** — at 17 institutional applications spanning Sessions ~75-91, it has the deepest cross-session record of any open candidate. Would close out the most-actively-tracked candidate. Session 94 may want to run at default effort unless the operator says otherwise — Session 93's `/effort max` was session-scoped, not a standing directive.

### 2026-05-22 — Promote Candidate Learning #59 (5-file checkpoint discipline) to permanent row #41 in `SESSION_RUNNER.md` (Session 92)

Methodology session — dedicated promotion of Candidate Learning #59 from the candidate pool tracked in `SESSION_NOTES.md` to a permanent row in the `SESSION_RUNNER.md` Learnings table. Matches the precedent of the row #39 promotion (Session 82, commit `1ee2c1b`) and the row #40 promotion (Session 83, commit `5202f4a`) — both standalone methodology commits touching only `SESSION_RUNNER.md` + `SESSION_NOTES.md` + `CHANGELOG.md`. Per FM #18 (planning-to-implementation bleed) — the promotion is NOT bundled with the executive-summary deliverable that Session 92's prompt also named; that was split off as Session 93 per Phase 1A AskUserQuestion (8th post-promotion self-application of row #40 — three operator-confirmed choices: bundling = split, exec-summary audience = business/claims-org executive sponsor, exec-summary file location = `docs/executive-summaries/business-value-capture.qmd`). Candidate #59's history: filed Session 70 (1st instance — 9 files in one cohesive Phase-4 commit, SAFEGUARDS rule bent; `feat(...): phase 4 schema scaffold` checkpoint recipe scheduled); advanced Sessions 81 / 84 / 86 / 87 as candidate at the 5-file threshold but stayed spirit-dormant per filing-quality-check (additive coherent work didn't fit the rationale); Session 88 produced the 5th instance with disaster context (10+ files touched, mid-flight interruption, uncommitted unverified work) — promotion-triggering; Sessions 89 / 91 confirmed the spirit-dormant judgment for purely additive instances. Row #41's rule names file-count as the trigger but folds the filing-quality-check refinement into the rule prose itself, so future sessions don't repeat the "spirit-dormant despite Nth instance" pattern. Run at `/effort max` per the operator's session-scoped invocation (the prior `feedback_effort_max_value_capture` directive expired at Session 91 plan close-out and was deleted; this is a fresh session-scoped `/effort max`, not a carry). Wiki publish hook will NOT fire — no `docs/wiki/claims-model-starter/` pages touched.

- **Changed:** `SESSION_RUNNER.md` — new row #41 appended to the Learnings table immediately after row #40. Rule: "For multi-file sessions touching ≥5 files of non-procedural state, take an explicit checkpoint commit at the first stable-state boundary mid-session — particularly during refactors or multi-stage implementation." Source column enumerates Sessions 70 (1st) / 81 / 84 / 86 / 87 (candidate-advances) / 88 (disaster-context, promotion-triggering) / 89 / 91 (spirit-dormant confirmations) and the row's promotion in Session 92. When-to-apply column names ≥5-file deliverables of non-procedural state (production code, schemas, test fixtures, generated artifacts — `SESSION_NOTES.md` / `CHANGELOG.md` / `BACKLOG.md` updates do not count) and prescribes the `checkpoint: <what's done so far>` commit shape, with a skip-clause for additive single-coherent gates-clean sessions. `SESSION_NOTES.md` — Phase 1B ACTIVE TASK update (Session 92 claim with the operator's exec-summary prompt verbatim + the three Phase-1A AskUserQuestion resolutions); Session 91 archive (`### Session 91 ARCHIVED ACTIVE TASK` heading inserted above the `### Session 90 ARCHIVED ACTIVE TASK` file-tail archive zone at line 3055, demoted block carries the Session 91 Task/Status verbatim with the stale `Commit pending.` corrected to `Commit \`b3101ba\`.` per the established predecessor-self-correction pattern); Phase 3 close-out below (Session 91 handoff evaluation by Session 92, what Session 92 did, self-assess, learnings, handoff to Session 93).
- **Verified:** `SESSION_RUNNER.md` table integrity — `grep -c "^| 41 |" SESSION_RUNNER.md` → 1; `grep -c "^| 40 |" SESSION_RUNNER.md` → 1 (table boundary `---` separator + downstream `## Launch Prompt Templates` heading untouched). `SESSION_NOTES.md` archive integrity — `grep -n "^### Session 91 ARCHIVED ACTIVE TASK\|^### Session 90 ARCHIVED ACTIVE TASK" SESSION_NOTES.md` → `3055:### Session 91 ARCHIVED ACTIVE TASK` and `3061:### Session 90 ARCHIVED ACTIVE TASK` (chronological order preserved at the file tail, no inline duplication). Memory state — `feedback_effort_max_value_capture.md` remains deleted (Session 91 deleted it; Session 92 did not regenerate — the operator's `/effort max` for this session was session-scoped, not a standing directive). Tooling-gate posture: no `pytest` / `ruff` / `mypy` run was needed — Session 92 touched only methodology docs (zero `src/` / `tests/` / `packages/` changes); the post-Session-91 baseline (`635/635 @ 97.03%`, `mypy 0/61`) carries forward verbatim into the Session 93 handoff.
- **Unchanged intentionally:** No code, test, fixture, or schema changes. No wiki edits (`docs/wiki/claims-model-starter/` untouched — the auto-publish hook will NOT fire on this commit). No edits to settled-plan documents (`docs/planning/business-value-capture-plan.md` complete per Session 91; untouched). No edits to `BACKLOG.md` (still empty — 23rd consecutive carry; Session 93 enters at 24th), `ROADMAP.md`, `SAFEGUARDS.md`, methodology docs other than `SESSION_RUNNER.md`. No edits to `CLAUDE.md` or the methodology framework (`docs/methodology/`). The pre-existing "max 10" doc-drift in `docs/architecture-history/architecture-plan.md:33` (C3 "max 10 questions") is a known remaining hit from Session 91's incidental sweep; NOT touched this session (FM #18 + Learnings #31/#42 — a drift-class micro is its own dedicated session). **Candidate #59 promoted to row #41 — removed from active candidate tracking.** **Candidate #62 (verify tool-PATH at Phase 0) stays at 17 institutional applications** (no `.venv/bin/` gate runs this session because no gates needed; the count does NOT increment without an actual invocation). **Candidates #57 / #61 stay at 2nd instance** (dormant — no Edit-tool multi-match recovery, no "align X to Y" inversion). **Candidate #63 (planning per-file completion criteria) stays at 2nd instance** — Session 92 is a methodology promotion, not a planning session.
- **Next:** **Session 93 = the executive summary**, per operator's pre-resolved Phase 1A contract (bundling = split, audience = business / claims-org executive sponsor, location = `docs/executive-summaries/business-value-capture.qmd`). Content scope: purpose / method / expected benefit of the Phases 1-6 implementation of `business-value-capture-plan.md` (Sessions 86-91), rendered to both PDF and HTML via Quarto (`format: { html: default, pdf: default }` in the YAML front-matter). Open contract questions for Session 93 (per row #40): (a) target length — 1 page / 2-3 pages / 5+ pages? (b) include the subrogation worked example as a "Before / After" demonstration? (c) include a per-phase timeline table (Sessions 86-91), or treat the work as one coherent capability rather than six phases? (d) does the project have a `_quarto.yml` configuration to detect at Phase 1, or does Session 93 author one alongside the `.qmd`? — pre-named in the Session 92 handoff so Session 93 resolves them at Phase 1A. BACKLOG remains empty (24th consecutive carry at Session 93 Phase 0).

### 2026-05-22 — Phase 6 of business-value-capture-plan: cross-doc consistency, worked examples, glossary (Session 91) — plan COMPLETE

Implementation session — Phase 6, the **final phase**, of `docs/planning/business-value-capture-plan.md` (authored Session 85; Phases 1-5 landed Sessions 86-90). Docs-only — no code, no test, no fixture changes. Threads the value-capture extension through the six documentation surfaces not owned by an earlier phase: `docs/architecture-history/architecture-plan.md` §1 + §8.3, and the wiki pages `Worked-Examples.md` / `Pipeline-Overview.md` / `Glossary.md` / `Home.md` / `Changelog.md`. Closing Phase 6 closes out the whole 6-phase plan — every plan §9 definition-of-done grep now holds simultaneously (see Verified). Per Learning #1, re-verified the architecture-plan anchors at Phase 1 — the plan's 2026-05-21 line numbers (§1 "line 135", §8 "555-562") had drifted after Phases 1-3 edited the file; §1's pipeline overview is the 6-step list at lines 18-25 and §8's monitoring section is §8.3 at line 605. Plan Appendix B names NO Phase-6 contract questions and the per-file completion criteria are prescriptive, so no Phase-1A AskUserQuestion was needed. Per FM #18 — implements ONLY Phase 6; no code touched. Run at `/effort max` per the operator directive (memory `feedback_effort_max_value_capture`) — the last phase the directive covers. Triggers the wiki publish hook on commit since five `docs/wiki/claims-model-starter/` pages were edited; deliberate.

- **Changed:** `docs/architecture-history/architecture-plan.md` — §1 Context gains a "Business-value capture (Sessions 86–91)" paragraph after the 6-step pipeline list, noting the `ValueMeasurementPlan` / `BaselineSnapshot` / business-case / Production Measurement Plan thread through Steps 2-4 and cross-referencing §8.3; §8.3 Cycle-Time-Driven Monitoring Cadence gains a "Value-review cadence" paragraph after the cadence table, distinguishing model-health monitoring from the `value_measurement_plan.review_cadence` value-review cadence. `docs/wiki/claims-model-starter/Worked-Examples.md` — Example 1 Step 4 gains a "Baseline snapshot" bullet (the `subrogation_recovery_rate` = `0.41 ratio` EXECUTED snapshot from `tests/fixtures/sample_datareport.json`); Step 5 gains a "Rendered `06_implementation_plan.qmd`" subsection enumerating the nine Production Measurement Plan sections rendered from the intake `value_measurement_plan` + the `baseline_snapshot`. `docs/wiki/claims-model-starter/Pipeline-Overview.md` — the Intake/Data/Website agent summaries updated: Intake now drives toward **five** sections (value measurement plan added); the Data Agent notes baseline-snapshot collection; the Website Agent notes business-case + Production Measurement Plan rendering. The stale "max 10" in the Intake behavior line was corrected to "max 20" in the same sentence — it contradicted the page's own diagram (line 11, "max 20 questions") and `MAX_QUESTIONS`; the correction was made only because the sentence was already being rewritten for the five-section change. `docs/wiki/claims-model-starter/Glossary.md` — four entries added to the Pipeline terms table: Value Measurement Plan, Baseline Snapshot, Counterfactual Design, Cost of Inaction. `docs/wiki/claims-model-starter/Home.md` — the "What it does" section gains a value-capture paragraph. `docs/wiki/claims-model-starter/Changelog.md` — a "Business value capture — Sessions 86–91 (2026-05-22)" entry added under `## [Unreleased]`, replacing the "No unreleased changes" placeholder (Learning #32 — historical phase entries untouched). `SESSION_NOTES.md` — Phase 1B ACTIVE TASK update + Session 90 archive; Phase 3 close-out.
- **Verified:** All four Phase-6 plan §5 completion criteria met: `grep -c "baseline_snapshot\|value_measurement_plan\|counterfactual_design" Worked-Examples.md` → 4 (≥1); `grep -c "Baseline Snapshot\|Value Measurement Plan\|Counterfactual Design\|Cost of Inaction" Glossary.md` → 4 (≥3); `grep -c "value cadence\|Value Demonstration\|baseline_snapshot" architecture-plan.md` → 4 (≥1); `grep -c "value_measurement_plan\|baseline\|business case" Pipeline-Overview.md` → 2 (≥1). The whole-plan §9 definition-of-done now holds simultaneously: `.venv/bin/pytest -q` → **635/635 passing @ 97.03% coverage** (Session 90 baseline 635/635 @ 97.03% — unchanged, docs-only); `.venv/bin/mypy src/model_project_constructor/ packages/data-agent/src/` → `Success: no issues found in 61 source files`; `grep -c "intentionally sparse" templates.py` → 0; `grep -c "value_measurement_plan" schemas/v1/intake.py` → 1; `grep -c "baseline_snapshot" data-agent schemas.py` → 1; `grep -c "Value Demonstration" Monitoring-and-Operations.md` → 1. **`docs/planning/business-value-capture-plan.md` is COMPLETE — all six phases closed out (Sessions 86-91).**
- **Unchanged intentionally:** No code, test, or fixture changes — Phase 6 is docs-only; a doc edit requiring a code change would have been a scope signal. No edits to the doc surfaces settled by Phases 1-5 (`Schema-Reference.md`, `Intake-Interview-Design.md`, `Data-Guide.md`, `Generated-Project-Structure.md`, `Monitoring-and-Operations.md`, `Governance-Framework.md`, architecture-plan §4.2/§5.x). Plan file `docs/planning/business-value-capture-plan.md` untouched. `BACKLOG.md` (empty — 22nd consecutive carry), `ROADMAP.md`, `SESSION_RUNNER.md`, `SAFEGUARDS.md`, methodology docs untouched — `ROADMAP.md` is not a Phase-6 surface per the plan's file table. Only the one "max 10" → "max 20" drift inside the sentence being rewritten was corrected; no broader "10 question" drift sweep (out of scope, FM #8). **Candidate Learning #59 (5-file checkpoint) advances to 8th instance** — Phase 6 touched 7 files; additive docs-only, gates green throughout, no interruption — spirit-dormant. The #59 → row #41 promotion remains a candidate for a dedicated methodology session (FM #18). **Candidate #62 (verify tool-PATH) — 17th institutional application.** **Candidates #57 / #61 / #63 dormant.**
- **Next:** **`business-value-capture-plan.md` is complete** — no carried plan work. BACKLOG.md remains **empty** (23rd consecutive carry at Session 92 Phase 0). Session 92 has no carried directive — its anchor is fresh operator direction. The `/effort max` directive (memory `feedback_effort_max_value_capture`) was scoped to "every session implementing a phase of this plan"; with the plan complete that directive has **expired** — Session 92 does not inherit it (the memory should be retired). A standing non-plan option: a dedicated methodology session to weigh the Candidate #59 → row #41 promotion.

### 2026-05-22 — Phase 5 of business-value-capture-plan: website post-production value-demonstration templates (Session 90)

Implementation session — Phase 5 of `docs/planning/business-value-capture-plan.md` (authored Session 85; Phase 1 schemas Session 86, Phase 2 intake Session 87, Phase 3 data agent Session 88, Phase 4 pre-construction templates Session 89). Replaces the load-bearing "intentionally sparse" TODO in `render_qmd_implementation_plan` with a structured **Production Measurement Plan** — the generated `analysis/06_implementation_plan.qmd` now renders Baseline / Counterfactual Design / Attribution Method / Evaluation Horizon / Logging Requirements / Review Cadence / Success Criteria / Decision Rights. `render_qmd_implementation_plan` gains a `data: dict` parameter so the baseline current-state metric can be surfaced from the Data Agent's `DataReport.baseline_snapshot`; the baseline figure is interpolated directly into the `.qmd` with a citation footnote pointing back to `reports/data_report.json`. `render_ongoing_monitoring` threads the intake's value-measurement baseline metric alongside the model-health metrics, and `render_deployment_gates` adds a Stage-1 gate for Data-Science review of the Production Measurement Plan. One Phase-1A contract question (pre-named in plan Appendix B Phase 5 + the Session 89 handoff per permanent row #40) resolved via AskUserQuestion — **7th post-promotion self-application of row #40**: baseline-value rendering = **interpolate the value inline with a citation footnote** (Recommended; reads better for stakeholders, footnote preserves provenance). The second handoff-flagged question (`data: dict` parameter vs. a merged dict) was NOT asked — plan §5 Phase 5 explicitly prescribes "Accept `data: dict` in addition to `intake: dict`" and the only call site (`build_base_files`) already has `data` in scope, so the plan + code resolved it. Per Learning #1, re-verified the plan §5 Phase 5 anchors at Phase 1 — `render_qmd_implementation_plan` (templates.py:464, plan's stale `386`), the TODO (templates.py:485, plan's stale `406-408`), `render_ongoing_monitoring` (governance_templates.py:278), `render_deployment_gates` (governance_templates.py:304) — the line numbers had drifted from the 2026-05-21 plan-write (Session 89 shifted `templates.py` ~+80 lines) but the symbols were all present. Per FM #18 boundary discipline — implements ONLY Phase 5; Phase 6 (cross-doc consistency + worked examples + glossary) is untouched. Run at `/effort max` per the carried operator directive (memory `feedback_effort_max_value_capture`). Triggers the wiki publish hook on commit since `docs/wiki/claims-model-starter/Monitoring-and-Operations.md` + `Governance-Framework.md` were edited (Phase 5 owns those mirrors per plan §5); deliberate.

- **Changed:** `src/model_project_constructor/agents/website/templates.py` — new `_render_production_measurement_plan(*, intake, data)` helper emitting nine `##` sections (Production Measurement Plan intro / Baseline / Counterfactual Design / Attribution Method / Evaluation Horizon / Logging Requirements / Review Cadence / Success Criteria / Decision Rights); the Baseline section interpolates `DataReport.baseline_snapshot` (metric, definition, current value + unit, measurement window, query-execution status, caveats) with a `[^baseline-src]` citation footnote, and degrades to a "no baseline snapshot was collected" message when the snapshot is absent; the methodology sections read the intake's `ValueMeasurementPlan` with placeholder text for missing fields. `render_qmd_implementation_plan` now takes `data: dict` in addition to `intake: dict`, reuses the Phase-4 `_format_usd_band` helper for the estimated-impact band, keeps the Estimated Annual Impact / Confidence / Assumptions header, and appends the measurement-plan block — replacing the old `## Measurement` "intentionally sparse" TODO. `build_base_files` call site updated to `render_qmd_implementation_plan(intake=intake, data=data)`. `src/model_project_constructor/agents/website/governance_templates.py` — `render_ongoing_monitoring` threads the intake's `value_measurement_plan` baseline metric as a tracked metric in "Metrics to Track" and notes the value-review cadence separately from the model-health cadence (placeholders when no plan is declared); `render_deployment_gates` adds a Stage-1 Shadow checkbox for Data-Science review of `analysis/06_implementation_plan.qmd`. `tests/fixtures/sample_datareport.json` — added a `baseline_snapshot` block (`subrogation_recovery_rate`, value 0.41 ratio, EXECUTED, one caveat) so the populated Phase-5 rendering path is testable; the metric name matches the `value_measurement_plan` in the paired `subrogation_intake.json` fixture. `tests/agents/website/test_templates.py` — 2 existing `test_qmd_implementation_plan_*` tests updated for the new `data=` parameter; 3 new tests (`test_qmd_implementation_plan_renders_production_measurement_plan` populated path + `test_qmd_implementation_plan_missing_plan_renders_placeholders` sparse path + `test_qmd_implementation_plan_baseline_collected_but_not_measured` FAILED/unmeasured snapshot path) pinning positive + negative assertions (Learning #5). `tests/agents/website/test_governance.py` — 3 new tests (`test_ongoing_monitoring_threads_baseline_metric` + `test_ongoing_monitoring_missing_value_plan_renders_placeholder` + `test_deployment_gates_includes_measurement_plan_review`). `docs/wiki/claims-model-starter/Monitoring-and-Operations.md` — new `## Value Demonstration` section (pre-construction estimate vs. post-production methodology; the baseline → counterfactual → attribution → horizon → success-criteria → decision-rights thread as a table; value-review cadence vs. model-health cadence). `docs/wiki/claims-model-starter/Governance-Framework.md` — `## Monitoring cadence` extended with a paragraph distinguishing the model-health cadence from the `ValueMeasurementPlan.review_cadence` value-review cadence. `SESSION_NOTES.md` — Phase 1B ACTIVE TASK update + Session 89 archive; Phase 3 close-out.
- **Verified:** `.venv/bin/pytest -q` → **635/635 passing** @ **97.03% coverage** (Session 89 baseline: 629/629 @ 96.99%; +6 new tests; coverage +0.04 — `templates.py` held at 100%, 184/184 statements, after a fourth test was added for the snapshot-present-but-unmeasured branch). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (after extracting one over-length f-string into a `status` local). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged); `.venv/bin/mypy src/model_project_constructor/agents/website/` (plan §5 Phase 5 criterion) → `Success: no issues found in 13 source files`. All Phase-5 plan §5 greppable completion criteria met: `grep -c "intentionally sparse" templates.py` → 0 (TODO removed); `grep -c "## Production Measurement Plan\|## Baseline\|## Counterfactual\|## Attribution\|## Evaluation Horizon\|## Logging Requirements\|## Success Criteria" templates.py` → 7 (≥4); `grep -c "data=data" templates.py` → 8 (≥1); `grep -c "render_qmd_implementation_plan(" templates.py` → 2 (≥1); `grep -c "^## Value Demonstration" Monitoring-and-Operations.md` → 1. Rendered output spot-checked: `06_implementation_plan.qmd` from `subrogation_intake.json` + `sample_datareport.json` renders all nine sections with the interpolated baseline (`0.41 ratio`), the footnote, the `champion_challenger` design and the six logging fields; `governance/ongoing_monitoring.md` renders the threaded baseline metric in both the populated and no-plan branches; `governance/deployment_gates.md` renders the new Stage-1 review gate.
- **Unchanged intentionally:** No edits to the Phase-4 business-case block (`_render_business_case_block` / `_format_usd_band` in `templates.py` — reused, not modified; `render_impact_assessment` / `_usd_band` in `governance_templates.py` — Phase 4 settled), intake agent code (`agents/intake/` — Phase 2 settled), data agent runtime (`packages/data-agent/` — Phase 3 settled), schema definitions (`schemas/v1/intake.py`, data-agent `schemas.py` — Phase 1 settled), orchestrator adapters (Phase 3 settled). Wiki pages other than the two named: `Worked-Examples.md` / `Pipeline-Overview.md` / `Glossary.md` / `Home.md` / wiki `Changelog.md` and architecture-plan §1/§8 are all Phase 6 surfaces — untouched. Plan file `docs/planning/business-value-capture-plan.md`, `BACKLOG.md`, `ROADMAP.md`, `SESSION_RUNNER.md`, `SAFEGUARDS.md`, methodology docs untouched. The `render_qmd_implementation_plan` signature change is contained — `build_base_files` is its only production caller and was updated in the same diff; the new sections render placeholders rather than being omitted for missing fields, so the generated plan is structurally stable regardless of intake/data sparsity. The `governance_templates.py` `_usd_band` / `templates.py` `_format_usd_band` near-duplication from Phase 4 was left as-is (the governance module's docstring deliberately keeps it decoupled from `templates`); Phase 5 added no new band helper. **Candidate Learning #59 (5-file checkpoint) advances to 7th instance** — Session 90 touched ~9 files; additive, gates green throughout, no mid-work interruption — spirit-dormant (contrast Session 88's disaster-context instance). The #59 → row #41 promotion remains a candidate for a dedicated methodology session (FM #18 — not bundled into a plan-implementation session). **Candidates #57 / #61 stay at 2nd instance** (dormant). **Candidate #62 (verify tool-PATH) — 16th institutional application** (`.venv/bin/` preemptive prefix on all four gate runs). **Candidate #63 (planning per-file criteria) stays at 2nd instance** — Session 90 is an implementation session.
- **Next:** BACKLOG remains **empty** (21st consecutive carry; 22nd at Session 91 Phase 0). Most likely Session 91 direction: (a) **Implement Phase 6 of business-value-capture-plan.md** — the final phase: cross-doc consistency + worked examples + glossary. Docs-only (no code): `architecture-history/architecture-plan.md` §1 + §8; `Worked-Examples.md` Example 1 extended with the extended-intake / baseline-snapshot / `06_implementation_plan.qmd` excerpts; `Pipeline-Overview.md` Steps 1/3/5; `Glossary.md` adds Baseline Snapshot / Counterfactual Design / Value Measurement Plan / Cost of Inaction; `Home.md` value-proposition framing; wiki `Changelog.md` `## Unreleased` entry. Phase 6 closes out the whole plan. Appendix B names NO Phase-6 contract questions. (b) New off-BACKLOG operator direction. **Run Session 91 at `/effort max`** per the operator directive (memory `feedback_effort_max_value_capture`) — Phase 6 is the last phase the directive covers.

### 2026-05-22 — Phase 4 of business-value-capture-plan: website pre-construction value-presentation templates (Session 89)

Implementation session — Phase 4 of `docs/planning/business-value-capture-plan.md` (authored Session 85; Phase 1 schemas Session 86, Phase 2 intake Session 87, Phase 3 data agent Session 88). Renders the value-capture data collected by Phases 1-3 into the generated website's pre-construction documents: the generated `analysis/01_business_understanding.qmd` and `reports/intake_report.md` now present a coherent business case — Annual Impact Band, Cost of Inaction, Implementation Cost, Payback, Value Drivers, Assumptions, Decision Rights — and `governance/impact_assessment.md` surfaces the cost-of-inaction + implementation-cost bands in its Value Narrative section. Both `templates.py` surfaces share a single `_render_business_case_block(intake)` helper so they cannot drift. One Phase-1A contract question (pre-named in Session 88 handoff per permanent row #40) plus one surfaced from the code during Phase 1 reconnaissance, both resolved via AskUserQuestion — **6th post-promotion self-application of row #40**: (1) render strategy = **shared helper** (Recommended; DRY across the two surfaces; plan §5 Phase 4 completion criterion greps for `_render_business_case_block`); (2) missing-field rendering = **placeholder text** (Recommended; `(not estimated)` / `(none declared)` / `(not specified)` — matches the existing `render_qmd_implementation_plan` style, keeps the business case structurally complete for sparse intakes). Operator directed mid-session that **all sessions implementing this plan run at `/effort max`** (saved to memory `feedback_effort_max_value_capture`; carried in the Session 90 handoff). Per FM #18 boundary discipline — implements ONLY Phase 4 (website pre-construction rendering + the two named wiki mirrors); Phase 5 (post-production measurement-plan templates, the `templates.py:406-408` TODO) and Phase 6 (cross-doc consistency) are untouched. Per Learning #1, re-verified the plan §5 Phase 4 anchors at Phase 1 — `render_qmd_business_understanding` (templates.py:303), `render_reports_intake_md` (:500), `render_impact_assessment` (governance_templates.py:329) — zero drift from the 2026-05-21 plan-write. Triggers the wiki publish hook on commit since `docs/wiki/claims-model-starter/Generated-Project-Structure.md` + `Worked-Examples.md` were edited (Phase 4 owns those mirrors per plan §5); deliberate.

- **Changed:** `src/model_project_constructor/agents/website/templates.py` — new `_format_usd_band(low, high, *, suffix)` helper (formats a `$L – $H` band, `(not estimated)` when either bound is absent); new `_render_business_case_block(*, intake)` helper emitting seven sections (Annual Impact Band / Cost of Inaction / Implementation Cost / Payback / Value Drivers / Assumptions / Decision Rights) with placeholder text for missing optional fields; `render_qmd_business_understanding` appends the block after the Estimated Value narrative; `render_reports_intake_md` inserts the block between the Estimated Value and Governance sections. `src/model_project_constructor/agents/website/governance_templates.py` — new local `_usd_band(low, high, *, suffix)` helper (parallels `templates._format_usd_band`; kept local so the governance module stays decoupled from `templates` per its module docstring); `render_impact_assessment` surfaces `**Estimated annual cost of inaction:**` + `**Estimated implementation cost:**` bullets in the `## Value Narrative` section. `tests/fixtures/subrogation_intake.json` — extended `estimated_value` with the 7 Phase-1 business-case fields + added a full `value_measurement_plan` block, mirroring the canonical `tests/fixtures/subrogation.yaml` intake fixture; brings the fixture to post-Phase-2 reality (a `COMPLETE` IntakeReport carries a `value_measurement_plan` per the Session 87 finalize gate). `tests/agents/website/test_templates.py` — 4 new tests (`test_qmd_business_renders_business_case` + `test_qmd_business_missing_fields_render_placeholders` + `test_intake_report_md_renders_business_case` + `test_intake_report_md_missing_fields_render_placeholders`) pinning both the populated path and the sparse-intake placeholder path (Learning #5 positive + negative). `tests/agents/website/test_governance.py` — 2 new tests (`test_impact_assessment_surfaces_cost_bands` + `test_impact_assessment_missing_cost_bands_render_placeholder`). `docs/wiki/claims-model-starter/Generated-Project-Structure.md` — the `01_business_understanding` narrative-table row now lists the full business case. `docs/wiki/claims-model-starter/Worked-Examples.md` — Example 1 Step 2 extended with the populated business-case fields + a Value Measurement Plan paragraph; Step 5 gains a "Rendered `01_business_understanding.qmd`" subsection; `subrogation_intake.json` line citations updated (`estimated_value` :26-47, `value_measurement_plan` :48-71, `governance` :72-83) and the `test_templates.py` file-set citation refreshed to `:229-258`. `SESSION_NOTES.md` — Phase 1B ACTIVE TASK update + Session 88 archive; Phase 3 close-out.
- **Verified:** `.venv/bin/pytest -q` → **629/629 passing** @ **96.99% coverage** (Session 88 baseline: 623/623 @ 96.96%; +6 new tests; coverage +0.03 — `templates.py` is now 100%-covered, 157/157 statements). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!`. `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged); `.venv/bin/mypy src/model_project_constructor/agents/website/` (plan §5 Phase 4 criterion) → `Success: no issues found in 13 source files`. All Phase-4 plan §5 greppable completion criteria met: `grep -c "## Cost of Inaction\|## Implementation Cost\|## Payback\|## Value Drivers\|## Decision Rights" templates.py` → 5 (≥3); `grep -c "_render_business_case_block\|cost_of_inaction" templates.py` → 9 (≥2); `grep -c "cost_of_inaction\|value_drivers\|payback_months" Worked-Examples.md` → 3 (≥1). Rendered output spot-checked: the three surfaces (`01_business_understanding.qmd`, `reports/intake_report.md`, `impact_assessment.md`) render the full business case from the subrogation fixture with the en-dash band format consistent with the pre-existing `render_qmd_implementation_plan`.
- **Unchanged intentionally:** No edits to `render_qmd_implementation_plan` (`templates.py:406-408` TODO — Phase 5 owns the post-production measurement-plan section), intake agent code (`agents/intake/` — Phase 2 settled), data agent runtime (`packages/data-agent/` — Phase 3 settled), schema definitions (`schemas/v1/intake.py` — Phase 1 settled), orchestrator adapters (Phase 3 settled). Wiki pages other than the two named: `Monitoring-and-Operations.md` / `Governance-Framework.md` (Phase 5), `Pipeline-Overview.md` / `Glossary.md` / `Home.md` / wiki `Changelog.md` (Phase 6) untouched. Plan file `docs/planning/business-value-capture-plan.md`, `BACKLOG.md`, `ROADMAP.md`, `SESSION_RUNNER.md`, `SAFEGUARDS.md`, methodology docs untouched. The seven new business-case sections render placeholders rather than being omitted for missing fields, so the generated business case is structurally stable regardless of intake sparsity; no existing website test byte-compares `01_business_understanding.qmd` / `reports/intake_report.md` / `impact_assessment.md` content, so the extension is non-breaking. **Candidate Learning #59 (5-file checkpoint) advances to 6th instance** — Session 89 touched ~9 files; additive multi-file deepening, gates green throughout, no mid-work interruption — spirit-dormant (contrast Session 88's disaster-context instance). Session 88's promotion recommendation (#59 → row #41) stands for a dedicated methodology session. **Candidates #57 / #61 stay at 2nd instance** (dormant — no Edit-tool multi-match recovery, no "align X to Y" inversion). **Candidate #62 (verify tool-PATH) — 15th institutional application** (`.venv/bin/` preemptive prefix). **Candidate #63 (planning per-file criteria) stays at 2nd instance** — Session 89 is an implementation session.
- **Next:** BACKLOG remains **empty** (20th consecutive carry; 21st at Session 90 Phase 0). Most likely Session 90 direction: (a) **Implement Phase 5 of business-value-capture-plan.md** — website post-production value-demonstration templates: replace the `templates.py:406-408` TODO with a structured Production Measurement Plan section in `render_qmd_implementation_plan` (Estimated Annual Impact, Confidence, Assumptions, Baseline from `DataReport.baseline_snapshot`, Counterfactual Design, Attribution Method, Evaluation Horizon, Logging Requirements, Review Cadence, Success Criteria, Decision Rights); extend `render_ongoing_monitoring`; mirror in `Monitoring-and-Operations.md` + `Governance-Framework.md`. Plan Appendix B Phase 5 pre-names 1 contract question (interpolate `baseline_snapshot.value` directly vs render "see `reports/data_report.json`"). (b) New off-BACKLOG operator direction. **Run Session 90 at `/effort max`** per the operator directive. Phase 1A AskUserQuestion at Session 90 resolves the Appendix B Phase-5 question per row #40.

### 2026-05-22 — Phase 3 of business-value-capture-plan: data agent baseline collection (Session 88)

Implementation session — Phase 3 of `docs/planning/business-value-capture-plan.md` (authored Session 85; Phase 1 schemas Session 86, Phase 2 intake Session 87). Wires baseline-metric collection through the data agent: when an upstream `IntakeReport` carries a `value_measurement_plan` with a baseline metric, the data agent generates a baseline-collection SQL query, executes it read-only, and records the result on `DataReport.baseline_snapshot`. Adds `BaselineQuerySpec` + `generate_baseline_query()` to the `LLMClient` Protocol; implements it on `AnthropicLLMClient`; adds a `baseline_collection` graph node sequenced strictly after quality-check execution and before summarization; threads `value_measurement_plan` from `IntakeReport` into `DataRequest` via three projected fields (`baseline_metric_name` / `baseline_metric_definition` / `baseline_measurement_window`) in the orchestrator adapter — the data agent never imports `IntakeReport` (architecture-plan §3.2 / §4.2 invariant: intake defines, data agent executes). Two Phase-1A contract questions (pre-named in Session 87 handoff per permanent row #40) resolved via AskUserQuestion — **5th post-promotion self-application of row #40**: (1) baseline-SQL-error failure mode = **fail-the-snapshot** (`query_execution_status="FAILED"`, caveat recorded, `DataReport` still ships — the graph never crashes on a baseline error); (2) execution order = **strictly after** primary-query execution (sequential, no concurrency). **Recovery note:** Session 88 ran across two invocations — a first invocation completed the 10-file implementation but ended before committing or closing out; this second invocation verified all gates against the uncommitted work (operator chose "verify, then close out" at the Phase 0 recovery decision per the SAFEGUARDS Session Recovery Protocol), fixed one ruff import-sort error in `test_data_agent.py`, completed the two Phase 3 documentation surfaces, and closed out. Per Learning #11 (trust the code, not the plan): plan §5 Phase 3 names the architecture-plan target "§6 (Data Agent)", but §6 is "Handoff Envelope Protocol" and the Data Agent section is §4.2 — the baseline subsection landed in §4.2. Triggers the wiki publish hook on commit since `docs/wiki/claims-model-starter/Data-Guide.md` was edited (Phase 3 owns that mirror per plan §5); deliberate.

- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/llm.py` — new `BaselineQuerySpec` frozen dataclass (`metric_name` / `sql` / `measurement_unit`); new `generate_baseline_query()` method on the `LLMClient` Protocol. `.../anthropic_client.py` — implements `generate_baseline_query()` via the existing JSON-driven `_call_claude` + `_extract_json` pattern (senior-P&C-analyst system prompt instructs a scalar-first-column `SELECT`). `.../schemas.py` — `DataRequest` extended with three optional baseline-projection fields (all-three-or-none). `.../state.py` — `DataAgentState` extended with `baseline_snapshot: BaselineSnapshot | None`. `.../nodes.py` — new `make_baseline_collection(llm, db)` factory + `baseline_collection` node: skips (`baseline_snapshot=None`) when no metric definition; records a `FAILED` snapshot with the error on `caveats` on LLM error or SQL error; records `NOT_EXECUTED` when the DB is unreachable; on success extracts the first-column scalar and records `EXECUTED`. `.../graph.py` — `baseline_collection` node inserted on the `execute_qc → summarize` edge (now `execute_qc → baseline_collection → summarize`). `.../agent.py` — `_assemble_complete_report` reads `baseline_snapshot` from final state and passes it to `DataReport`. `.../cli.py` — `_FakeCLIClient` gets a `generate_baseline_query()` stub (Protocol conformance for the CLI fake path). `src/model_project_constructor/orchestrator/adapters.py` — `intake_report_to_data_request` projects `IntakeReport.value_measurement_plan`'s baseline fields onto the `DataRequest`. `tests/agents/data/test_data_agent.py` — `FakeLLMClient` gets `generate_baseline_query()`; 5 new tests (executed / skipped-when-no-plan / NOT_EXECUTED-when-DB-unreachable / FAILED-on-SQL-error / FAILED-on-LLM-error). `tests/orchestrator/test_adapters.py` — new `TestIntakeReportToDataRequestBaseline` with 2 tests (no-plan leaves fields None / plan projects through). `docs/wiki/claims-model-starter/Data-Guide.md` — new "## Baseline collection" section (`BaselineSnapshot` field table + intake-defines / data-agent-executes note). `docs/architecture-history/architecture-plan.md` — §4.2 Data Agent extended with a baseline-collection behavior bullet + a "Baseline collection" subsection paragraph. `SESSION_NOTES.md` — Session 88 close-out (Phase 3A/3B/3C/3D + gotchas); the Phase 1B ACTIVE TASK update + Session 87 archive heading were written by the prior (interrupted) invocation.
- **Verified:** `.venv/bin/pytest -q` → **623/623 passing** @ **96.96% coverage** (Session 87 baseline: 616/616 @ 97.30%; +7 new tests; coverage −0.34 absolute because `AnthropicLLMClient.generate_baseline_query` — the live-LLM path — is not unit-tested, consistent with the rest of `anthropic_client.py`; 96.96% remains 1.96 points above the 95% gate). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (after fixing one I001 import-sort in `test_data_agent.py`). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). All Phase-3 plan §5 greppable completion criteria met: `grep -c "baseline_collection\|baseline_snapshot" graph.py` → 4 (≥1); `grep -c "baseline_snapshot" agent.py` → 2 (≥1); `grep -c "BaselineQuerySpec\|generate_baseline_query" llm.py` → 4 (≥1); `grep -c "value_measurement_plan" orchestrator/adapters.py` → 5 (≥1); skip-path test present (`grep -c "baseline_snapshot is None" test_data_agent.py` → 2).
- **Unchanged intentionally:** No edits to intake agent code (`agents/intake/` — Phase 2 settled), website templates (`agents/website/templates.py|governance_templates.py` — Phases 4/5), schema definitions in `schemas/v1/intake.py` (Phase 1 settled; `BaselineSnapshot` already in the data-agent `schemas.py`), or wiki pages other than `Data-Guide.md` (`Worked-Examples.md` / `Generated-Project-Structure.md` Phase 4; `Monitoring-and-Operations.md` / `Governance-Framework.md` Phase 5; `Pipeline-Overview.md` / `Glossary.md` / `Home.md` / wiki `Changelog.md` Phase 6). Plan file `docs/planning/business-value-capture-plan.md` untouched. `BACKLOG.md` / `ROADMAP.md` / `SESSION_RUNNER.md` / `SAFEGUARDS.md` / methodology docs untouched. Schema additions are backward-compatible — the new `DataRequest` baseline fields default to `None`, so legacy requests still validate. **Candidate Learning #59 (5-file checkpoint) — first genuine disaster-context instance:** the prior Session 88 invocation touched 10+ files with zero checkpoint commits and was then interrupted, leaving uncommitted unverified work — exactly the scenario #59's rationale anticipates. Recovery was clean only because the work happened to pass all gates; a checkpoint commit after the implementation block would have bounded the blast radius. Prior #59 instances (Sessions 81/84/86/87) were gates-green-throughout additive work; Session 88 is the first instance with an actual mid-work interruption. **Candidate #59 should be weighed for promotion (row #41) by Session 89+.** **Candidates #57 / #61 stay at 2nd instance** (dormant). **Candidate #62 (verify tool-PATH) — 14th institutional application** (`.venv/bin/` preemptive prefix). **Candidate #63 (planning per-file criteria) stays at 2nd instance** — Session 88 is an implementation session.
- **Next:** BACKLOG remains **empty** (19th consecutive carry; 20th at Session 89 Phase 0). Most likely Session 89 direction: (a) **Implement Phase 4 of business-value-capture-plan.md** — website pre-construction value-presentation templates (`render_qmd_business_understanding` + `render_reports_intake_md` + `render_impact_assessment` extensions; `Generated-Project-Structure.md` + `Worked-Examples.md` mirrors); Appendix B names no pre-set contract questions for Phase 4, but plan §5 Phase 4 flags one optional choice — extract a shared `_render_business_case_block(intake)` helper or inline the new sections in both surfaces. (b) New off-BACKLOG operator direction. (c) Verify the auto-publish hook fired on the Session 88 commit (`Data-Guide.md` was edited). Phase 1A AskUserQuestion at Session 89 should resolve the Phase-4 helper-vs-inline choice per row #40.

### 2026-05-21 — Phase 2 of business-value-capture-plan: intake interview extensions (Session 87)

Implementation session — Phase 2 of `docs/planning/business-value-capture-plan.md` (authored Session 85; Phase 1 schemas landed Session 86). Wires the Session 86 schemas (`EstimatedValue` extensions + `ValueMeasurementPlan`) into the intake interview surface: extends the `DraftReportResult` protocol with `value_measurement_plan: dict[str, Any]`; extends `_INTERVIEWER_BASE` system prompt + `next_question` user prompt + `draft_report` user prompt + `revise_report` user prompt to drive toward **five required sections** (was four — adds value measurement plan as the 5th) and reserve ~3-4 of the 20-question cap for measurement-plan probes (per Phase 1A operator-confirmed inline-within-`draft_report` shape + 3-4 question budget); plumbs the plan through `_draft_to_dict` / `_dict_to_draft` / `_draft_as_dict` round-trips + `_build_draft` parsers in both `anthropic_client.py` and `fixture.py`; extends `finalize_node` (`nodes.py:142`) with the plan §3.3 structural gate — `value_measurement_plan_incomplete` appended to `missing_fields` when `baseline_metric_name` or `evaluation_horizon_months` is missing, blocking `COMPLETE` status even when the reviewer accepted; updates `build_intake_report` to populate `IntakeReport.value_measurement_plan: ValueMeasurementPlan | None` from the draft dict; extends all 7 intake YAML fixtures (`subrogation`, `subrogation_b2`, `fraud_triage`, `pricing_optimization`, `intake_question_cap`, `intake_revision_cap`, `_b2_failmode`) with `value_measurement_plan:` blocks (fully populated for happy-path fixtures; null-valued placeholders for cap/fail-mode fixtures so they still parse but trip the gate); updates the intake-test trio (`test_anthropic_client.py` + `test_nodes.py` + `test_graph.py`) with 5 new tests pinning the new behavior; updates wiki `Intake-Interview-Design.md` (four → **five required sections**, new §4.5 documenting `value_measurement_plan`, Section 7 status-rule table extended with the plan column + missing_fields entry); updates `docs/tutorial.md` worked example with the extended `estimated_value` + `value_measurement_plan` blocks. Two Phase-1A contract questions (pre-named in Session 86 handoff per permanent row #40) resolved via AskUserQuestion — **4th post-promotion self-application of row #40**: (1) measurement-plan prompt shape = **inline within `draft_report`** (Recommended; one round-trip per draft; lower cost than a separate sub-prompt mirroring governance); (2) question-budget allocation = **~3-4 of the 20-question cap** (Recommended; plan §7 risk register default). Per Learning #1, re-ran Appendix A.5 + A.7 drift-check greps in Phase 0 — zero drift from Session 85 plan-write (2026-05-21); `anthropic_client.py` schema-instruction anchors at lines 158, 198, 243, 254 all current; all 7 YAML fixtures confirmed present. Per FM #18 boundary discipline — implements ONLY Phase 2 (intake surfaces + fixtures + tests + wiki + tutorial); Phase 3 (data agent baseline collection) is Session 88+. Triggers wiki publish hook on commit since `docs/wiki/claims-model-starter/Intake-Interview-Design.md` was edited (Phase 2 owns that mirror per plan §5); deliberate.

- **Changed:** `src/model_project_constructor/agents/intake/protocol.py` — added `value_measurement_plan: dict[str, Any] = field(default_factory=dict)` to `DraftReportResult` (optional at the draft layer; finalize node enforces population for COMPLETE per plan §3.3). `src/model_project_constructor/agents/intake/anthropic_client.py` — `_INTERVIEWER_BASE` system prompt extended from "four required sections" to "FIVE required sections" and adds value-measurement-plan probing guidance (3-4 question budget allocation + named topics: baseline metric, counterfactual, evaluation horizon, logging requirements, review cadence, success criteria, decision rights); `next_question` user prompt extended to drive toward all 5 sections; `draft_report` user prompt extended with 7 new `estimated_value` sub-fields + new `value_measurement_plan` object schema (13 sub-fields including `counterfactual_design` Literal with 7 values + `review_cadence` Literal with 4 values); `revise_report` user prompt extended to list `value_measurement_plan` in the revised-draft key set; `_draft_as_dict` + `_build_draft` updated to round-trip the plan. `src/model_project_constructor/agents/intake/nodes.py` — imported `ValueMeasurementPlan` from schemas; `_draft_to_dict` / `_dict_to_draft` updated to round-trip the plan; `finalize_node` extended with the plan §3.3 structural gate (4-line block appending `value_measurement_plan_incomplete` when `baseline_metric_name` or `evaluation_horizon_months` is empty); `build_intake_report` updated to construct `ValueMeasurementPlan(**vmp_dict) if vmp_dict else None` and pass to `IntakeReport(...)`. `src/model_project_constructor/agents/intake/fixture.py` — docstring schema section extended with `value_measurement_plan: {...}` documentation; `_build_draft` updated to round-trip the plan from fixture dict. `tests/fixtures/{subrogation,fraud_triage,pricing_optimization}.yaml` — `estimated_value` block extended with 7 new fields (subrogation-, fraud-, pricing-flavored); new `value_measurement_plan:` block added (fully populated for COMPLETE status). `tests/fixtures/{subrogation_b2,intake_question_cap,intake_revision_cap,_b2_failmode}.yaml` — `estimated_value` extended with 7 null/empty defaults; new `value_measurement_plan:` block with all-null fields (block exists; trips the structural gate). `tests/agents/intake/test_anthropic_client.py` — `_draft_payload` extended with full plan; new `test_draft_report_value_measurement_plan_optional` (plan omitted in LLM output → empty dict, finalize flags later); new `test_system_interviewer_pins_value_measurement_plan_section` (pins "FIVE required sections" + 7 plan-topic tokens in the prompt). `tests/agents/intake/test_nodes.py` — `_sample_draft` extended with full plan; new `_sparse_draft` helper (no plan); `test_draft_report_stores_dict_shape` extended; new `test_finalize_flags_missing_value_measurement_plan` (sparse plan → DRAFT_INCOMPLETE + flag); new `test_finalize_flags_partial_value_measurement_plan` (plan present but `evaluation_horizon_months=None` → DRAFT_INCOMPLETE + flag); `test_build_intake_report_validates_and_returns_report` extended with plan assertions; new `test_build_intake_report_omits_plan_when_draft_has_none` (sparse draft → IntakeReport.value_measurement_plan is None). `tests/agents/intake/test_graph.py` — `test_subrogation_happy_path` extended with `payback_months`, `value_drivers`, and full `value_measurement_plan` assertions; `test_question_cap_produces_draft_incomplete` extended to assert `value_measurement_plan_incomplete` in missing_fields. `docs/wiki/claims-model-starter/Intake-Interview-Design.md` — Section 1 prompt verbatim quote updated; three load-bearing rules now (added the 3-4 question budget rule); Section 3 `believe_enough_info` description updated; Section 4 heading changed to "five required sections" + sub-sections renumbered 4.1-4.4 + new §4.5 documenting `value_measurement_plan` (10 sub-bullets covering all 13 schema fields); Section 7 terminal-status table extended with the plan column + plan flag rows. `docs/tutorial.md` — fixture worked example's `estimated_value` block extended with 7 new fields + new `value_measurement_plan:` block (subrogation-flavored complete plan); explanatory paragraph extended to "five required sections" + names the plan §3.3 COMPLETE rule. `SESSION_NOTES.md` — Phase 1B ACTIVE TASK Task/Status block updated for Session 87; Session 86 archive heading + abbreviated Task-line inserted above `### Session 85 ARCHIVED ACTIVE TASK` (Learning #37 consume-and-replace; single-line raw-string anchor `grep -c "off-BACKLOG operator direction (BACKLOG.md EMPTY since Session 70; 16th consecutive carry)"` = 1, safe).
- **Verified:** `.venv/bin/pytest -q` → **616/616 passing** @ **97.30% coverage** (Session 86 baseline: 611/611 @ 97.30%; +5 new tests pinning the new behavior, coverage held at 97.30%). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!`. `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). All Phase-2 plan §5 greppable completion criteria met: `grep -c "value_measurement_plan" protocol.py` → 3 (≥1, expected); `grep -c "value_measurement_plan" anthropic_client.py` → 4 (≥2, expected); `grep -c "value_measurement_plan" nodes.py` → 9 (≥3, expected); all 7 YAML fixtures contain `value_measurement_plan:` block (`grep -lq` for-loop returned no MISSING lines); `grep -c "five required sections\|Five required sections\|FIVE required sections" Intake-Interview-Design.md` → 3 (≥1, expected); `grep -c "value_measurement_plan" Intake-Interview-Design.md` → 3 (≥1, expected).
- **Unchanged intentionally:** No edits to data agent code (`packages/data-agent/` runtime — Phase 3 owns baseline collection), website templates (`agents/website/templates.py|governance_templates.py` — Phases 4/5 own pre/post construction rendering), orchestrator adapters (`orchestrator/adapters.py` — Phase 3 owns `value_measurement_plan` threading into DataRequest), schema definitions (`schemas/v1/intake.py` — Phase 1 owned these; no rework needed), `docs/architecture-history/architecture-plan.md` (Phase 1 already updated §5.1/§5.3; Phase 6 owns the cross-doc consistency pass for §1/§8), wiki pages other than `Intake-Interview-Design.md` (`Schema-Reference.md` updated in Phase 1; `Data-Guide.md` in Phase 3; `Worked-Examples.md` + `Pipeline-Overview.md` + `Glossary.md` + `Home.md` + `Generated-Project-Structure.md` + `Monitoring-and-Operations.md` + `Governance-Framework.md` + wiki `Changelog.md` deferred to their respective phases). `BACKLOG.md`, `ROADMAP.md`, `SESSION_RUNNER.md`, `SAFEGUARDS.md`, methodology docs untouched. Plan file `docs/planning/business-value-capture-plan.md` untouched. Existing `EstimatedValue` field validations + Pydantic schema-level optionality preserved — schema additions remain backward-compatible; legacy intake reports without `value_measurement_plan` still parse (carried as None on `IntakeReport`). **Candidate Learning #57 (Edit-tool multi-match diagnosis) stays at 2nd instance** — did not fire (row #38 recipe held; all Phase 1B and close-out anchors were single-match raw-string greps). **Candidate Learning #59 (5-file checkpoint) ADVANCES to 4th instance** — Session 87 touched 17 files (protocol.py + anthropic_client.py + nodes.py + fixture.py + 7 YAML fixtures + 3 test files + Intake-Interview-Design.md + tutorial.md + SESSION_NOTES.md + CHANGELOG.md = 17), well above the 5-file threshold; no checkpoint commits taken (single coherent deliverable + gates green end-to-end + no destructive operations). Filing-quality-check repeated from Session 86: candidate's rationale (defensive checkpoints during HIGH-RISK refactors) does not match additive interview-surface deepening with gates-green-throughout. Provisional answer remains NO — #59 spirit-dormant despite 4th instance. **Candidate Learning #61 (audit BOTH sides before "align X to Y") stays at 2nd instance** — no inversion. **Candidate Learning #62 (verify tool-PATH at Phase 0) stays at 1st instance, 13th institutional application** (`.venv/bin/` preemptive prefix). **Candidate Learning #63 (planning per-file completion criteria) stays at 2nd instance** — Session 87 IS an implementation session, not a planning session.
- **Next:** BACKLOG remains **empty** (18th consecutive carry; 19th at Session 88 Phase 0). Most likely Session 88 direction: (a) **Implement Phase 3 of business-value-capture-plan.md** — data agent baseline collection (`generate_baseline_query` on `LLMClient` Protocol + Anthropic client; `baseline_collection` graph node between `generate_qc` and `summarize`; `DataAgentState.baseline_snapshot` field; orchestrator adapter threading `value_measurement_plan` from IntakeReport into DataRequest; data-agent test extensions for executed vs skipped paths; `Data-Guide.md` wiki update + architecture-plan §6 subsection); plan Appendix B Phase 3 pre-names 2 contract questions (fail-the-snapshot vs fail-the-graph on SQL error; sequential vs parallel baseline execution). (b) Observe + verify auto-publish hook fired on Session 87's commit (`Intake-Interview-Design.md` edit will trigger another hook fire). (c) New off-BACKLOG operator direction. Phase 1A AskUserQuestion at Session 88 should resolve the 2 plan-Appendix-B Phase-3 contract questions per row #40 5th-post-promotion self-application target.

### 2026-05-21 — Phase 1 of business-value-capture-plan: schema extensions (Session 86)

Implementation session — Phase 1 of `docs/planning/business-value-capture-plan.md` (authored Session 85). Extends `EstimatedValue` with 7 new pre-construction business-case fields (`cost_of_inaction_narrative`, `annual_cost_of_inaction_usd_low/high`, `implementation_cost_band_usd_low/high`, `payback_months: int | None`, `value_drivers: list[str]`); adds new top-level `ValueMeasurementPlan` class (13 fields covering baseline metric definition, counterfactual design, attribution method, evaluation horizon, logging requirements, review cadence, success criteria, decision rights) and threads `IntakeReport.value_measurement_plan: ValueMeasurementPlan | None`; adds new `BaselineSnapshot` class (8 fields covering metric_name, value, measurement window, query_sql + execution status, caveats) in the data-agent package's canonical `schemas.py` and threads `DataReport.baseline_snapshot: BaselineSnapshot | None`. All new fields/classes are optional at the schema level so existing fixtures continue to validate; downstream enforcement of `COMPLETE`-status requirements lands in Phase 2 (intake `finalize` node) and Phase 3 (data agent baseline collection). Three Phase-1A contract questions (pre-named in Session 85 handoff per permanent row #40) resolved via AskUserQuestion — **3rd post-promotion self-application of row #40**: (1) `payback_months` typed as `int | None` (Recommended; flexibility over banded precision); (2) `value_drivers` as free-form `list[str]` (Recommended; controlled vocabulary deferred); (3) `BaselineSnapshot` placed in `packages/data-agent/.../schemas.py` (Recommended; cohesion with `DataReport` over sibling-module precedent from Learning #2). Per Learning #1, re-ran Appendix A drift-check greps in Phase 0 — zero drift since plan-write (2026-05-21); anchors at intake.py:27/54, data-agent schemas.py:95, architecture-plan.md:248/272, Schema-Reference.md:131 all current. Per FM #18 boundary discipline — implements ONLY Phase 1 (schema + tests + schema-doc mirrors); Phase 2 (intake interview extension) is Session 87+. Does NOT trigger wiki publish hook on commit despite Schema-Reference.md edit — the file lives in `docs/wiki/claims-model-starter/`, so the hook WILL fire; deliberate (Phase 1 owns the Schema-Reference mirror per plan §5).

- **Changed:** `src/model_project_constructor/schemas/v1/intake.py` — extended `EstimatedValue` (added 7 optional fields with `Field(default_factory=list)` for `value_drivers`); added `ValueMeasurementPlan` class (13 fields, all optional at schema level; `counterfactual_design` Literal with 7 values; `review_cadence` Literal with 4 values); threaded `IntakeReport.value_measurement_plan: ValueMeasurementPlan | None = None`. `packages/data-agent/src/model_project_constructor_data_agent/schemas.py` — added `BaselineSnapshot` class (8 fields; `query_execution_status` Literal `EXECUTED|NOT_EXECUTED|FAILED`); threaded `DataReport.baseline_snapshot: BaselineSnapshot | None = None`; added `BaselineSnapshot` to `__all__`. `packages/data-agent/src/model_project_constructor_data_agent/__init__.py` — re-exported `BaselineSnapshot` from the schemas module + added to `__all__`. `src/model_project_constructor/schemas/v1/data.py` — re-exported `BaselineSnapshot` from the data-agent package + added to `__all__`. `src/model_project_constructor/schemas/v1/__init__.py` — re-exported `BaselineSnapshot` + `ValueMeasurementPlan` + added both to `__all__`. `tests/schemas/fixtures.py` — extended `make_estimated_value` defaults with all 7 new fields (subrogation-flavored example values); added `make_value_measurement_plan` factory (champion_challenger + quarterly cadence defaults); added `make_baseline_snapshot` factory (executed-percent default). `tests/schemas/test_intake.py` — 5 new `TestEstimatedValue` tests (defaults-when-omitted + populated-via-fixture + payback_months type rejection); new `TestValueMeasurementPlan` class with 6 tests (happy path + all-fields-optional construction + invalid counterfactual rejection + invalid cadence rejection + extra-fields-forbidden + serialization round-trip); 2 new `TestIntakeReport` tests for `value_measurement_plan` field (defaults-to-none + round-trip-when-present). `tests/data_agent_package/test_inventory_schemas.py` — appended `TestBaselineSnapshot` class with 7 tests (happy path + value-None-when-FAILED + NOT_EXECUTED status + invalid-status rejection + caveats default-empty + extra-fields-forbidden + serialization round-trip) AND `TestDataReportBaselineSnapshotField` class with 2 tests (defaults-to-none + round-trip-when-present). `docs/architecture-history/architecture-plan.md` — §5.1 EstimatedValue mirror extended with the 7 new fields; new `ValueMeasurementPlan` class definition with docstring; `IntakeReport.value_measurement_plan` field added; §5.3 `BaselineSnapshot` class definition added above `DataReport`; `DataReport.baseline_snapshot` field added; Phase-1/Session-86 note appended below the existing Phase-3/Session-60 note. `docs/wiki/claims-model-starter/Schema-Reference.md` — `EstimatedValue` mirror extended with the 7 new fields + explanatory paragraph; new `ValueMeasurementPlan` section with full class definition + explanatory paragraph; `IntakeReport` mirror updated with `value_measurement_plan` field; new `BaselineSnapshot` section above `DataReport`; `DataReport` mirror updated with `baseline_snapshot` field + explanatory bullet. `SESSION_NOTES.md` — Phase 1B ACTIVE TASK Task/Status block updated for Session 86; Session 85 archive heading + abbreviated Task-line inserted above `### Session 84 ARCHIVED ACTIVE TASK` (Learning #37 consume-and-replace; row #38 multi-line anchor verified unique via `grep -c "(final entry; directive expired)"` = 1 after raw-string `grep -c "### Session 84 ARCHIVED ACTIVE TASK"` returned 7); Phase 3 close-out content (Session 85 Handoff Evaluation by Session 86 + What Session 86 Did + Phase 3B self-assess + Phase 3C learnings + Phase 3D handoff to Session 87 + Gotchas for Session 87) inserted above `### Session 84 Handoff Evaluation (by Session 85)`.
- **Verified:** `.venv/bin/pytest -q` → **611/611 passing** @ **97.30% coverage** (Session 85 baseline: 591/591 @ 97.27%; +20 new schema tests, +0.03 absolute coverage from new schemas being 100%-covered by the new tests). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!`. `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). All Phase-1 plan §5 greppable completion criteria met: `grep -c "cost_of_inaction_narrative"` intake.py → 1; `grep -c "payback_months"` intake.py → 1; `grep -c "value_drivers"` intake.py → 1; `grep -c "^class ValueMeasurementPlan"` intake.py → 1; `grep -c "^class BaselineSnapshot"` data-agent schemas.py → 1; `grep -c "value_measurement_plan: ValueMeasurementPlan"` intake.py → 1; `grep -c "baseline_snapshot:"` data-agent schemas.py → 1 (all 7 = 1, expected). Coverage on `src/.../schemas/v1/intake.py` = 100% (64/64 statements); on `packages/data-agent/.../schemas.py` = 100% (existing baseline maintained).
- **Unchanged intentionally:** No edits to intake agent code (`agents/intake/` — Phase 2), data agent runtime (`packages/data-agent/.../graph.py|nodes.py|agent.py|llm.py|anthropic_client.py|state.py` — Phase 3), website templates (`agents/website/templates.py|governance_templates.py` — Phases 4/5), orchestrator adapters (Phase 3), test fixtures under `tests/fixtures/*.yaml` (Phase 2), `Intake-Interview-Design.md` (Phase 2), `Data-Guide.md` (Phase 3), `Worked-Examples.md` (Phase 6), `Monitoring-and-Operations.md` (Phase 5), `Governance-Framework.md` (Phase 5), `Pipeline-Overview.md` (Phase 6), `Glossary.md` (Phase 6), `Home.md` (Phase 6), `Generated-Project-Structure.md` (Phase 4), or `tutorial.md` (Phase 2). `BACKLOG.md`, `ROADMAP.md`, `SESSION_RUNNER.md`, `SAFEGUARDS.md`, methodology docs untouched. Plan file `docs/planning/business-value-capture-plan.md` untouched (the plan describes WHAT is changing; implementation lands here without editing the plan). Existing fixtures (`tests/fixtures/*.yaml`) NOT modified — schema additions are optional with defaults so legacy intake reports continue to validate; Phase 2 will extend the YAML fixtures when the prompt is updated. Schema registry (`schemas/registry.py`) NOT modified — `ValueMeasurementPlan` and `BaselineSnapshot` are nested types accessed transitively via `IntakeReport` and `DataReport`, not top-level handoff envelope payloads. **Candidate Learning #57 (Edit-tool multi-match diagnosis) stays at 2nd instance** — did not fire (row #38 recipe held; Phase 1B Edit #2 7-match escalation was preemptive per recipe, not recovery from `Found N matches`). **Candidate Learning #59 (5-file checkpoint) ADVANCES to 3rd instance** — Session 86 touched 11 files (`intake.py` + `data.py` + v1 `__init__.py` + data-agent `schemas.py` + data-agent `__init__.py` + `fixtures.py` + `test_intake.py` + `test_inventory_schemas.py` + `architecture-plan.md` + `Schema-Reference.md` + `SESSION_NOTES.md` + `CHANGELOG.md` = 12 files actually), well above the 5-file threshold; no checkpoint commits taken (single coherent deliverable + gates green end-to-end, so no recoverable-bad-state risk); filing question: does #59 still warrant promotion when 12 files of additive schema/test changes are coherent and gates stay green? Provisional answer: NO — 12 files of additive deepening with no destructive operations does not invoke the disaster-recovery rationale behind #59; the candidate is fundamentally about defensive checkpoints during high-risk multi-module refactors, not about additive schema deepening. #59 advances to 3rd-instance but remains dormant in spirit. **Candidate Learning #61 (audit BOTH sides before "align X to Y") stays at 2nd instance** — no "align X to Y" inversion this session. **Candidate Learning #62 (verify tool-PATH at Phase 0) stays at 1st instance, 12th institutional application** (`.venv/bin/` preemptive prefix at all gate runs). **Candidate Learning #63 (planning per-file completion criteria) stays at 2nd instance** — Session 86 IS an implementation session, not a planning session, so does not advance this candidate.
- **Next:** BACKLOG remains **empty** (17th consecutive carry; 18th at Session 87 Phase 0). Most likely Session 87 direction: (a) **Implement Phase 2 of business-value-capture-plan.md** — intake interview extensions (anthropic_client prompt JSON schema for new fields + nodes.py draft/revise/finalize plumbing + protocol DraftReportResult extension + 7 YAML fixture extensions + Intake-Interview-Design.md "five required sections" update + tutorial.md walk-through); plan Appendix B Phase 2 pre-names 2 contract questions (sub-prompt vs inline measurement-plan question; question budget allocation). (b) End-to-end test of the auto-publish hook (carried since Session 84; the Session 86 commit WILL fire it since Schema-Reference.md was edited — verify behavior post-commit). (c) New off-BACKLOG operator direction. Phase 1A AskUserQuestion at Session 87 should resolve the 2 plan-Appendix-B Phase-2 contract questions per row #40 4th-post-promotion self-application target.

### 2026-05-21 — `business-value-capture-plan.md` — 6-phase plan for value-capture documentation modifications (Session 85)

Operator-directed planning session (off-BACKLOG; BACKLOG remains **empty** since Session 70 — 16th consecutive carry). Creates `docs/planning/business-value-capture-plan.md` (625 lines, 17 top-level sections, 6 implementation phases) that ensures the model intake/design phase collects information to present business value PRIOR to model construction AND collects necessary information to demonstrate business value DOWNSTREAM once the model is in production. Operator follow-up "modify documentation to reflect capturing of business value" interpreted with explicit three-sense framing in plan §1.2: documentation as **generated artifacts** (intake/data/website MDs produced by the pipeline), **design documentation** (architecture-plan, wiki pages, tutorial), and **schema-as-documentation** (the EstimatedValue schema IS the contract downstream code reads as documentation). Each phase enumerates which sense it touches. Plan structure resolved at Phase 1A via 2-question AskUserQuestion — **2nd post-promotion self-application of permanent row #40**: operator selected (a) multi-phase plan + (b) full grep-based inventory, both Recommended per Learnings #1/#19. Six phases ordered Schema → Intake → Data Agent → Website pre-construction → Website post-production → Cross-doc consistency; each phase has greppable completion criteria + verification commands + session boundary marker + documentation surfaces enumerated. Plan §3 holds 6 cross-phase invariants in one document per Learning #9 (schema partition extends `EstimatedValue` + adds sibling top-level `IntakeReport.value_measurement_plan`; baseline collection lives in data agent not intake per `feedback_data_source_discovery.md` principle; `ValueMeasurementPlan` required for COMPLETE status; production logging is TODO scaffolding not implementation; estimate vs demonstration are different artifacts; decision rights captured but advisory). Plan §4 sketches concrete schema extensions: extended `EstimatedValue` with 7 new fields (`cost_of_inaction_narrative`, USD low/high inaction-cost band, USD low/high implementation-cost band, `payback_months`, `value_drivers`); new `ValueMeasurementPlan` schema with 11 fields covering baseline metric definition + counterfactual design + attribution method + evaluation horizon + logging requirements + review cadence + success criteria + decision rights; new `DataReport.baseline_snapshot` with executed-SQL + measured-value + caveats. Phase 5 explicitly replaces the load-bearing TODO at `src/model_project_constructor/agents/website/templates.py:406-408` ("Define the pre/post metric and the measurement window before shipping. This section is intentionally sparse — the Data Science team owns the measurement plan.") with a structured Production Measurement Plan section. Plan §5 Phase-by-phase tables specify files + anchors with line numbers (per Learning #1 — plan-mode output is a draft; named symbols are claims-at-write-time that executor re-verifies at implementation). Appendix A organizes grep inventory by Learning #8 (TYPE / FIELD / STATE KEY / DOCSTRING separately) — 67 hits across src/tests/docs. Appendix B pre-names open contract questions per implementation phase (per Learning #40). Per Learning #32, Phase 6 completion criteria explicitly target the current `## Unreleased` section of wiki Changelog.md only — does NOT file-wide grep `CHANGELOG.md`. Plan IS the deliverable; implementation begins Session 86 at Phase 1 (per FM #18 #19 — no planning-to-implementation bleed). Does NOT trigger wiki publish hook (no `docs/wiki/claims-model-starter/` files touched — plan lives in `docs/planning/`).

- **Added:** `docs/planning/business-value-capture-plan.md` (625 lines, 17 top-level sections). Structure modeled on `docs/planning/data-source-inventory-contract-plan.md` (Session 57): §1 Context (§1.1 Problem statement — names the `templates.py:406-408` TODO as the load-bearing gap; §1.2 Operator follow-up + three-sense documentation framing; §1.3 Filing-session grounding — CLAUDE.md pipeline + initial_purpose.txt subrogation worked example + architecture-plan monitoring cadence table + memory `feedback_data_source_discovery.md`; §1.4 Current-state summary grep-verified table — 7 surfaces × counts; §1.5 What this plan is and is not — explicitly states implementation is Session 86+); §2 Non-goals (6 explicit exclusions including no field-rename, no new package, no vendor coupling, no A/B-orchestration); §3 Strategic decisions (6 cross-phase invariants per Learning #9); §4 Proposed schema extensions (concrete Pydantic-shape sketches for `EstimatedValue` extension + `ValueMeasurementPlan` new + `BaselineSnapshot` new); §5 Six implementation phases (each table-formatted: deliverable + files-affected-with-anchors + greppable completion criteria + verification commands + documentation surfaces touched + session boundary marker); §6 Phase ordering rationale; §7 Risk register (6 risks each with mitigation — prompt extension cap risk, baseline SQL silent-failure, template signature breaking, fixture migration miss, wiki Changelog drift per Learning #32, LLM cost regression); §8 Out of scope (5 future-work items); §9 Definition of done (whole-plan verification command). Appendix A grep inventory (10 grep-types per Learning #8 — TYPE / FIELD / STATE KEY / DOCSTRING separate). Appendix B open contract questions for Phases 1, 2, 3, 5 (Learning #40 forward-application to future implementation sessions). `SESSION_NOTES.md` — Phase 1B ACTIVE TASK Task/Status block updated for Session 85; Session 84 archive heading + abbreviated Task-line inserted above `### Session 83 ARCHIVED ACTIVE TASK` (Learning #37 consume-and-replace; row #38 multi-line anchor verified unique via `grep -c "carried 14th consecutive session"` = 1 after raw-string `grep -c "### Session 83 ARCHIVED ACTIVE TASK"` returned 5); Phase 3 close-out content (Session 84 Handoff Evaluation by Session 85 + What Session 85 Did + Phase 3B self-assess + Phase 3C learnings + Phase 3D handoff to Session 86 + Gotchas for Session 86) inserted above `### Session 83 Handoff Evaluation (by Session 84)` via Learning #37 consume-and-replace (single-line anchor `grep -c` = 1).
- **Verified:** Plan is documentation-only (no code/tests/wiki paths). `.venv/bin/pytest -q` → **591/591 passing** @ **97.27% coverage** (unchanged from Session 84 baseline; plan + session-notes files not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Plan size: `wc -l docs/planning/business-value-capture-plan.md` → **625 lines** (between existing plans `evolution-page-plan.md` 458 and `resume-from-checkpoint-plan.md` 771). Structure: `grep -c "^### Phase\|^## " docs/planning/business-value-capture-plan.md` → 17 (top-level sections). Per Learning #32 (per-phase completion criteria avoid file-wide greps against append-only / freshness-tracked files): plan's Phase 6 completion criteria explicitly targets the current `## Unreleased` section of wiki Changelog.md only; does NOT file-wide grep `CHANGELOG.md`. Phase 1B Edit #1 anchor: single-line raw-string `grep -c "Session 84 — operator direction \"1, 2, 3\" #3 from Session 82 durable directive"` = 1 (safe). Phase 1B Edit #2 anchor: escalated to multi-line per Learning #34 when `grep -c "### Session 83 ARCHIVED ACTIVE TASK"` returned 5; distinguishing phrase `carried 14th consecutive session` → `grep -c` = 1. Row #39 file-tail locator: `grep -n "^### Session .* ARCHIVED ACTIVE TASK" SESSION_NOTES.md | head -1` returned line 2497 (Session 83 archive heading). Phase 3 close-out anchor: single-line `grep -c "### Session 83 Handoff Evaluation (by Session 84)"` = 1 (safe).
- **Unchanged intentionally:** No `src/`, `tests/`, `packages/`, `scripts/`, wiki, `BACKLOG.md`, `ROADMAP.md`, `SESSION_RUNNER.md`, `SAFEGUARDS.md`, or methodology-doc edits — this is a planning session; the plan IS the deliverable, implementation lands in Sessions 86+. Wiki publish hook does **not** fire (no `docs/wiki/claims-model-starter/` files touched). Plan content is forward-looking only — describes WHAT will change in Phases 1-6, does NOT modify the schemas/templates/wiki pages it references. Existing plans in `docs/planning/` untouched (the 6 existing plans are independent deliverables; this plan adds a 7th sibling). **Candidate Learning #57 (Edit-tool multi-match diagnosis) stays at 2nd instance** — did not fire (row #38 recipe held; Phase 1B Edit #2 multi-line escalation on 5-match anchor was preemptive). **Candidate Learning #59 (5-file checkpoint) stays at 2nd instance** — Session 85 touched 3 files (`docs/planning/business-value-capture-plan.md` + `SESSION_NOTES.md` + `CHANGELOG.md`); below 5-file threshold. **Candidate Learning #61 (audit BOTH sides before "align X to Y") stays at 2nd instance** — no inversion. **Candidate Learning #62 (verify tool-PATH at Phase 0) stays at 1st instance, 11th institutional application** (`.venv/bin/` preemptive prefix). **Candidate Learning #63 (planning per-file completion criteria) ADVANCES to 2nd instance** — Session 85 IS a planning session and applied per-phase greppable completion criteria + verification commands + documentation surfaces enumeration; 1st-instance was Session 57's `data-source-inventory-contract-plan.md` filing. #63 advances to 2nd instance; promotion path requires a 3rd-instance validation in a future planning session.
- **Next:** BACKLOG remains **empty** (17th consecutive carry will be reached at Session 86 Phase 0). Most likely Session 86 direction: (a) **Implement Phase 1 of `business-value-capture-plan.md`** (Schema extensions — `EstimatedValue` + `ValueMeasurementPlan` + `BaselineSnapshot` + tests + schema-doc mirrors); operator may direct OR Session 86 may pick as the natural sequel; (b) End-to-end test of the new auto-publish hook by making a deliberate wiki-source edit (carried over from Session 84 handoff direction #2; ~5 min; optional); (c) New off-BACKLOG operator direction. Phase 1A AskUserQuestion at Session 86 should resolve the 3 open contract questions from Plan Appendix B + handoff (payback_months int-vs-enum, value_drivers free-form-vs-vocabulary, BaselineSnapshot module placement) — these are the **row #40 3rd post-promotion self-application** targets for Session 86 if Phase 1 implementation is directed.

### 2026-05-13 — Auto-publish wiki via tracked `.githooks/post-commit` (Session 84)

Operator-directed completion of durable "1, 2, 3" sequence (#3, final entry; directive expires after this session). Off-BACKLOG (BACKLOG remains **empty** since Session 70 — 15th consecutive carry). Implements **option (a)** of the wiki publish-mechanism decision: a tracked `.githooks/post-commit` hook that conditionally invokes `scripts/publish_wiki.sh` when the just-made commit touches `docs/wiki/claims-model-starter/`. Activates via the one-time, per-clone `git config core.hooksPath .githooks` (no third-party deps, no symlinks to `.git/hooks/`, survives `git clone`). Hook delegates to the existing idempotent script — `publish_wiki.sh` already exits cleanly with "no changes to publish" when source and clone are in parity, so the hook needs no extra guard. Escape hatch `MPC_SKIP_WIKI_PUBLISH=1 git commit ...` for the rare intentional-amend case. Operator selected (a) at Phase 1A via AskUserQuestion — **1st post-promotion self-application of permanent row #40** (Session 83 handoff item #26 pre-named the three bounded options under "Open contract questions for Session 84"; resolved at Phase 1A rather than mid-execution, validating the row #40 recipe). Does NOT trigger wiki publish hook on its own commit (no `docs/wiki/claims-model-starter/` files touched this session).

- **Added:** `.githooks/post-commit` (executable, 21 lines) — bash hook with `set -eu`; honors `MPC_SKIP_WIKI_PUBLISH=1` escape hatch; uses `git diff-tree --no-commit-id --name-only -r HEAD | grep -q '^docs/wiki/claims-model-starter/'` to detect wiki-source changes in the just-made commit; on match, `exec $REPO_ROOT/scripts/publish_wiki.sh`; on no-match, silent exit 0. Header comment documents one-time enable (`git config core.hooksPath .githooks`) + escape hatch.
- **Changed:** `scripts/publish_wiki.sh` header — added "Auto-publish (recommended)" section documenting the `core.hooksPath` setup + the `MPC_SKIP_WIKI_PUBLISH=1` escape hatch; existing manual-invocation usage retained as backstop. `SESSION_RUNNER.md` Phase 3E "Wiki sync check" rewritten as "Wiki sync" — now describes auto-publish as the default, manual invocation as backstop; documents the one-time `git config core.hooksPath .githooks` setup + the escape hatch. `SESSION_NOTES.md` — Phase 1B ACTIVE TASK Task/Status block updated for Session 84; Session 83 archive heading + abbreviated Task-line inserted above `### Session 82 ARCHIVED ACTIVE TASK` (Learning #37 consume-and-replace; row #38 multi-line anchor verified unique via `grep -c 'carried 13th consecutive session). Promote'` = 1 after raw-string `grep -c '### Session 82 ARCHIVED ACTIVE TASK'` returned 7); Phase 3 close-out content (handoff eval + What Session 84 Did + self-assess + learnings + handoff + gotchas) replaces Session 83's close-out block.
- **Local config change (uncommitted, per-clone):** `git config core.hooksPath .githooks` set in this clone to activate the hook. Future clones must run this once to opt in; documented in `scripts/publish_wiki.sh` header + `SESSION_RUNNER.md` Phase 3E.
- **Verified:** Hook dry-run (`.githooks/post-commit` invoked against HEAD with no wiki paths) → silent exit 0 (skip path correct). `git config --get core.hooksPath` → `.githooks` (activation confirmed). `.venv/bin/pytest -q` → **591/591 passing** @ **97.27% coverage** (unchanged from Session 83 baseline; hook + scripts not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged; hook is bash, not on ruff inputs). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged).
- **Unchanged intentionally:** No `src/`, `tests/`, `packages/`, `BACKLOG.md`, `ROADMAP.md`, wiki content, or `docs/methodology/*` edits beyond `SESSION_RUNNER.md` Phase 3E. `docs/wiki/claims-model-starter/` content untouched — this session implements the *mechanism*, not new wiki content. Hook deliberately runs synchronously (not backgrounded) — `publish_wiki.sh` has its own error handling (exit 2 on push failure with undo instructions) and the user sees results immediately; backgrounded execution would lose error visibility. **Candidate Learning #65 already promoted → permanent row #40 (Session 83).** No new candidates filed. Candidate Learning #57 (Edit-tool multi-match diagnosis) stays at 2nd instance — did not fire (row #38 recipe held; Phase 1B Edit #2 multi-line escalation was preemptive on a 7-match anchor). Candidate Learning #59 (5-file checkpoint) stays at 2nd instance — Session 84 touched 5 files (`.githooks/post-commit` + `scripts/publish_wiki.sh` + `SESSION_RUNNER.md` + `SESSION_NOTES.md` + `CHANGELOG.md`); 5-file threshold reached but single-coherent-deliverable, gates green end-to-end, no checkpoint needed mid-session. Advance unclear — same file count as Session 81; filing-quality-check: 5 production-config changes are coherent (hook + script header + protocol doc + session-notes + changelog), no destructive operations, gates clean → #59 dormant remains correct. Candidate Learning #61 (audit BOTH sides before "align X to Y") stays at 2nd instance — no inversion. Candidate Learning #62 (verify tool-PATH at Phase 0) stays at 1st instance + **10th institutional application** (`.venv/bin/` preemptive prefix). Candidate Learning #63 (planning per-file completion criteria) stays at 1st instance — no planning session.
- **Next:** BACKLOG remains **empty** (16th consecutive carry will be reached at Session 85 Phase 0). Operator's durable "1, 2, 3" sequence **completes** with this session; post-Session-84 there is no carried directive — fresh operator direction or first non-empty BACKLOG entry anchors Session 85. The new auto-publish hook is **active in this clone** but will not fire on the Session 84 close-out commit itself (no wiki paths in the commit). First real-world fire will be the next commit that touches `docs/wiki/claims-model-starter/`. Possible Session 85 directions: (a) wait for fresh operator direction; (b) test the auto-publish hook end-to-end by making a deliberate wiki-source edit; (c) revisit any remaining off-BACKLOG items from prior session handoffs; (d) Session 85 may be the first session post-durable-directive — Phase 0 anchor reverts to operator-driven or BACKLOG-driven.

### 2026-05-13 — Promote Candidate Learning #65 → permanent row #40 in SESSION_RUNNER.md (Session 83)

Operator-directed methodology housekeeping micro-deliverable (off-BACKLOG; BACKLOG remains **empty** since Session 70 — 14th consecutive carry). Adds permanent row #40 to the `SESSION_RUNNER.md` Learnings table, codifying the Phase 3D pre-naming recipe for unresolved convention / date / boundary / contract-shape choices in handoffs: surface each as a one-sentence bullet under "Open contract questions for Session N+1:" so the next session resolves at Phase 1A rather than mid-execution. Candidate Learning #65 was filed Session 78 (1st instance — release-date ambiguity at the `[0.1.0] - 2026-05-12` transition), applied Session 80 (2nd instance — wiki-date convention at the Changelog mirror), applied Session 81 (3rd instance, promotion-triggering — broadened scope to non-"transition X to Y": flag interaction matrix + multi-file default in an implementation deliverable, resolved via 2-question AskUserQuestion at Phase 1A). Row #40 phrasing drafted from the four components carried in Session 82 handoff item #3 (rule / why / source-with-instances / when-to-apply), assembled into the body convention of rows #33/#36/#37/#39 (bold lead claim, expansion-with-examples, "Mechanical:" recipe, "Cost: ... Benefit: ..." cost-benefit, composition note with Learning #28). Source-column terminated with `Promoted Session 83.` per Session 82's structural-mirror convention. Operator input `"session 83"` mapped under durable directive ("numerical order; 1 per session") to direction #2 of the carried "1, 2, 3" sequence (Session 82 = #1 Candidate #66 promotion; Session 83 = #2 Candidate #65 promotion; Session 84 = #3 wiki publish-mechanism three-options decision). Phase 1B applied permanent row #39 (file-tail archive zone locator) as **2nd-instance post-promotion self-application** — locator returned line 2334 (Session 81 archive heading); Phase 1B Edit #2 inserted Session 82 archive block above line 2334 via Learning #37 consume-and-replace; zero placement fumble. Does NOT trigger wiki publish hook (`SESSION_RUNNER.md` / `CHANGELOG.md` / `SESSION_NOTES.md` not under `docs/wiki/claims-model-starter/`).

- **Changed:** `SESSION_RUNNER.md` — added permanent row #40 to the Learnings table directly below row #39, using phrasing drafted from Session 82 handoff item #3 four components, mirroring the row-body structure of rows #33/#36/#37/#39. Anchor: row #39's unique closing phrase `insert above the predecessor's \`### Session N-1 ARCHIVED ACTIVE TASK\` heading. |` followed by the table-end `---` separator (Learning #37 consume-and-replace; raw-string `grep -c` = 1 per row #38 recipe; single-line anchor sufficient). `SESSION_NOTES.md` — Phase 1B ACTIVE TASK Task/Status block updated for Session 83; Session 82 archive heading + abbreviated Task-line inserted above `### Session 81 ARCHIVED ACTIVE TASK` (Learning #37 consume-and-replace; row #38 multi-line anchor verified unique via `grep -c "carried 12th consecutive session). Implement"` = 1 after raw-string `grep -c "### Session 81 ARCHIVED ACTIVE TASK"` returned 6); Phase 3 close-out content (handoff eval + What Session 83 Did + self-assess + learnings + handoff + gotchas) inserted above `### Session 81 Handoff Evaluation (by Session 82)` (Learning #34 single-line anchor `grep -c` = 1).
- **Verified:** `.venv/bin/pytest -q` → **591/591 passing** @ **97.27% coverage** (unchanged from Session 82 baseline; methodology + session-notes files not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Structural: `grep -c "^| 40 |" SESSION_RUNNER.md` → 1 (row #40 present); `grep -c "^| 39 |" SESSION_RUNNER.md` → 1 (row #39 preserved); `grep -c "^| 38 |" SESSION_RUNNER.md` → 1 (row #38 preserved); table-end `---` separator preserved at the row block boundary. Additive-only: zero rows #1-#39 modified.
- **Unchanged intentionally:** No `src/`, `tests/`, `packages/`, `scripts/`, wiki, `BACKLOG.md`, `ROADMAP.md`, or methodology-non-runner doc edits. `docs/wiki/claims-model-starter/Changelog.md` (audience-facing wiki mirror) untouched — its release cadence is independent (Session 80 last touched it). Wiki publish hook does **not** fire (none of the touched files under `docs/wiki/`). Plan document `docs/planning/data-source-inventory-contract-plan.md` untouched (Phase 4 fully shipped by Sessions 70 + 81). **Candidate Learning #65 (handoff convention/date/boundary/contract-shape pre-naming) PROMOTED to permanent row #40.** Removed from candidate tracking. Candidate Learning #57 (Edit-tool multi-match diagnosis) stays at 2nd instance — did not fire (row #38 recipe held; Phase 1B Edit #2 escalation was preemptive). Candidate Learning #59 (5-file checkpoint) stays at 2nd instance — Session 83 touched 3 files (`SESSION_RUNNER.md` + `SESSION_NOTES.md` + `CHANGELOG.md`; below threshold). Candidate Learning #61 stays at 2nd instance — no "align X to Y" inversion. Candidate Learning #62 (verify tool-PATH at Phase 0) stays at 1st instance + **9th institutional application** (`.venv/bin/` preemptive prefix at gate-run time). Candidate Learning #63 (planning per-file completion criteria) stays at 1st instance — no planning session.
- **Next:** BACKLOG remains **empty** (15th consecutive carry will be reached at Session 84 Phase 0). Row #40 promotion lands `SESSION_RUNNER.md` Learnings table at 40 rows; no PROMOTION-READY candidates remain (Candidates #65 + #66 both promoted in Sessions 82-83; #55 closed Session 82; #57/#59/#61 dormant at 2nd-instance; #62/#63 at 1st-instance). Most likely Session 84 direction per operator's "1, 2, 3" #3: (a) **wiki publish-mechanism three-options decision** — final entry in durable sequence; Session 84's Phase 1A is the expected 1st post-promotion self-application of NEW row #40 (locate canonical source in `docs/planning/` / archived BACKLOG / `SESSION_NOTES.md` history, OR enumerate bounded options via AskUserQuestion); (b) new off-BACKLOG operator direction post-sequence (durable directive expires after #3).

### 2026-05-13 — Promote Candidate Learning #66 → permanent row #39 in SESSION_RUNNER.md (Session 82)

Operator-directed methodology housekeeping micro-deliverable (off-BACKLOG; BACKLOG remains **empty** since Session 70 — 13th consecutive carry). Adds permanent row #39 to the `SESSION_RUNNER.md` Learnings table, codifying the Phase 1B archive-placement file-tail locator recipe: `grep -n "^### Session .* ARCHIVED ACTIVE TASK" SESSION_NOTES.md | head -1` to identify the archive zone BEFORE constructing the Phase 1B Edit's `new_string`. Candidate Learning #66 was filed Session 79 (1st instance — placement fumble caught + recovered; 2 extra Edits + ~2 minutes cost), applied preemptively Session 80 (2nd instance — clean placement), applied preemptively Session 81 (3rd instance — promotion-triggering). Row #39 phrasing was pre-drafted in Session 81 handoff item #4 and pasted verbatim per Learning #27 with one structural-consistency mirror (`Promoted Session 82.` appended to source-column to match rows #36/#37/#38 termination shape; substantive learning content unchanged). Operator input `"1, 2, 3"` mapped under durable directive ("numerical order; 1 per session") to Session 82 = #1 (Candidate #66 promotion), Session 83 = #2 (Candidate #65 promotion), Session 84 = #3 (wiki publish-mechanism three-options decision). Also **formally closes Candidate Learning #55 (archive placement convention) as permanently superseded by row #39 #66** per Session 81 gotcha #13 forecast. Phase 1B applied Candidate #66 as **4th-instance institutional application** (extending its own promotion path) — file-tail locator returned line 2172 (Session 80 archive heading); Phase 1B Edit #2 inserted Session 81 archive block above line 2172 via Learning #37 consume-and-replace; zero placement fumble. Does NOT trigger wiki publish hook (`SESSION_RUNNER.md` / `CHANGELOG.md` / `SESSION_NOTES.md` not under `docs/wiki/claims-model-starter/`).

- **Changed:** `SESSION_RUNNER.md` — added permanent row #39 to the Learnings table directly below row #37/#38, using the pre-drafted Session 81 handoff item #4 phrasing with the `Promoted Session 82.` source-column mirror. Anchor: row #38's unique closing phrase `multi-line escalation per Learning #34 if >1. |` followed by the table-end `---` separator (Learning #37 consume-and-replace; raw-string `grep -c` = 1 per row #38 recipe). `SESSION_NOTES.md` — Phase 1B ACTIVE TASK Task/Status block updated for Session 82; Session 81 archive heading + abbreviated Task-line inserted above `### Session 80 ARCHIVED ACTIVE TASK` (Learning #37 consume-and-replace; row #38 multi-line anchor verified unique via `grep -c "carried 11th consecutive session). Update"` = 1 after raw-string `grep -c "### Session 80 ARCHIVED ACTIVE TASK"` returned 7); Phase 3 close-out content (handoff eval + What Session 82 Did + self-assess + learnings + handoff + gotchas) inserted above `### Session 80 Handoff Evaluation (by Session 81)` (Learning #34 single-line anchor `grep -c` = 1).
- **Verified:** `.venv/bin/pytest -q` → **591/591 passing** @ **97.27% coverage** (unchanged from Session 81 baseline; methodology + session-notes files not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Structural: `grep -c "^| 39 |" SESSION_RUNNER.md` → 1 (row #39 present); `grep -c "^| 38 |" SESSION_RUNNER.md` → 1 (row #38 preserved); `grep -c "^| 37 |" SESSION_RUNNER.md` → 1 (row #37 preserved); table-end `---` separator preserved at the row block boundary. Additive-only: zero rows #1-#38 modified.
- **Unchanged intentionally:** No `src/`, `tests/`, `packages/`, `scripts/`, wiki, `BACKLOG.md`, `ROADMAP.md`, or methodology-non-runner doc edits. `docs/wiki/claims-model-starter/Changelog.md` (audience-facing wiki mirror) untouched — its release cadence is independent (Session 80 last touched it). Wiki publish hook does **not** fire (none of the touched files under `docs/wiki/`). Plan document `docs/planning/data-source-inventory-contract-plan.md` untouched (Phase 4 fully shipped by Sessions 70 + 81). **Candidate Learning #55 (archive placement) FORMALLY CLOSED — permanently superseded by row #39 #66.** Removed from candidate tracking. **Candidate Learning #65 (handoff convention/date/boundary/contract-shape pre-naming) STILL 3rd-instance PROMOTION-READY** — deferred to Session 83 per operator's "1, 2, 3" sequencing (#2 = #65 promotion at Session 83; phrasing draft components carried in Session 82 handoff item #3). Candidate Learning #57 (Edit-tool multi-match diagnosis) stays at 2nd instance — did not fire (row #38 recipe held; Phase 1B Edit #2 escalation was preemptive). Candidate Learning #59 (5-file checkpoint) stays at 2nd instance — Session 82 touched 3 files (below threshold). Candidate Learning #61 stays at 2nd instance — no "align X to Y" inversion. Candidate Learning #62 (verify tool-PATH at Phase 0) stays at 1st instance + **8th institutional application** (`.venv/bin/` preemptive prefix at gate-run time). Candidate Learning #63 (planning per-file completion criteria) stays at 1st instance — no planning session.
- **Next:** BACKLOG remains **empty** (14th consecutive carry will be reached at Session 83 Phase 0). Row #39 promotion lands `SESSION_RUNNER.md` Learnings table at 39 rows. Most likely Session 83 direction per operator's "1, 2, 3" sequencing: (a) **Promote Candidate Learning #65 → permanent row #40** (phrasing draft needed; Session 82 handoff item #3 lists the four components: rule, why, source, when-to-apply); (b) Session 84 = wiki publish-mechanism three-options decision (#3 in operator sequence); (c) new off-BACKLOG operator direction.

### 2026-05-13 — `--curated-inventory` CLI flag + curated/interview merge logic (Session 81)

Operator-directed largest-carried-item pickup (off-BACKLOG; BACKLOG remains **empty** since Session 70 — 12th consecutive carry). Implements the `--curated-inventory PATH` CLI flag that was scope-stripped from Phase 4 of `docs/planning/data-source-inventory-contract-plan.md` at Session 70 (see Session 70 CHANGELOG entry: "Plan §9.3 merge-with-curated logic stripped because `--curated-inventory` CLI flag does not exist; filed as follow-up if/when curated inventory adoption demands it"). Adds a JSON loader (`load_curated_inventory(path) -> DataSourceInventory`) and a merge helper (`merge_inventories(curated, interview) -> DataSourceInventory`) to `src/model_project_constructor/orchestrator/adapters.py`; wires the flag through `argparse` → `PipelineConfig.curated_inventory_path: Path | None` → the adapter stage in `run_pipeline`. Pre-claim AskUserQuestion (Candidate Learning #65 **3rd-instance validation** — first non-"transition-X-to-Y" application; fires on any deliverable with unresolved convention/boundary choice) bounded the contract via two operator-confirmed choices: (a) **Independent + merge when both** — `--curated-inventory PATH` alone uses curated only; `--inventory-from-intake` alone uses interview only (preserves Phase 4 default); both flags trigger `merge_inventories(curated, interview)`. (b) **Single file only** per plan §12 Q7 default; glob support deferred to a future session if pilot demand surfaces. Merge contract per plan §9 Phase 4 bullet 3: curated entries win on duplicate `fully_qualified_name`; interview entries with new FQNs enrich (are appended); producers list is the union (deduplicated by `producer_id`, curated-first ordering); `created_at` set to merge timestamp; `request_context` describes the merge composition for downstream audit. Phase 1B applied Candidate Learning #66 (file-tail archive-placement location verification) as **3rd-instance promotion-triggering** — `grep -n "^### Session .* ARCHIVED ACTIVE TASK" SESSION_NOTES.md | head -1` located the file-tail archive zone at line 1998 (Session 79 archive heading); Phase 1B Edit #2 inserted Session 80 archive block above line 1998 via Learning #37 consume-and-replace; both Edits landed first-attempt; zero placement fumble. **Candidate Learning #65 + #66 both reach 3rd-instance validation this session** — both eligible for promotion to permanent row #39 at Session 82 (one row, two candidates competing). Does NOT trigger wiki publish hook (no `docs/wiki/claims-model-starter/` files touched).

- **Added:** `src/model_project_constructor/orchestrator/adapters.py` — `load_curated_inventory(path: Path) -> DataSourceInventory` (JSON loader via `DataSourceInventory.model_validate_json`; `pydantic.ValidationError`/`FileNotFoundError` propagate to caller); `merge_inventories(curated, interview) -> DataSourceInventory` (FQN dedup with curated winning, producers union, merge-timestamp `created_at`, descriptive `request_context`). `pathlib.Path` import added. `__all__` extended. `src/model_project_constructor/orchestrator/pipeline.py` — `PipelineConfig.curated_inventory_path: Path | None = None`; new adapter-stage logic loads curated if path set, derives interview if flag set, merges if both, passes either-or-neither otherwise (type-annotated `inventory: DataSourceInventory | None` to satisfy mypy across the three-branch assignment); `DataSourceInventory` added to `schemas.v1.data` import block. `scripts/run_pipeline.py` — new `--curated-inventory PATH` argparse argument (`type=Path`, `default=None`) with help text citing the merge contract and plan reference; plumbed to `PipelineConfig(curated_inventory_path=args.curated_inventory)`. `tests/orchestrator/test_pipeline.py` — 3 new test methods in `TestPipelineConfig`: `test_curated_inventory_alone_populates_request` (loads fixture, runs pipeline with only `curated_inventory_path` set, asserts `producer_types == {"curated"}` + all entries have `curated:` producer_id prefix), `test_curated_plus_intake_merges_with_curated_winning_on_duplicate_fqn` (both flags set, intake mentions `Guidewire ClaimCenter` which is also in fixture, asserts `producer_types == {"curated", "interview"}` + FQN appears once), `test_curated_wins_on_duplicate_fqn` (unit-style direct `merge_inventories` call with hand-built curated + interview inventories, asserts curated entry's `description`/`producer_id` survive on duplicate FQN and interview-only `Subrogation Recovery` enriches). `SESSION_NOTES.md` — Phase 1B ACTIVE TASK Task/Status block updated for Session 81; Session 80 archive heading + abbreviated Task-line inserted above `### Session 79 ARCHIVED ACTIVE TASK` (Learning #37 consume-and-replace; row #38 multi-line anchor verified unique via `grep -c "carried 10th consecutive session). Promote Candidate Learning #64"` = 1).
- **Verified:** `.venv/bin/pytest -q` → **591/591 passing** @ **97.27% coverage** (+3 tests vs Session 80's 588/588 baseline; coverage delta -0.05% absorbed by the new test coverage of `adapters.py:182-228` `load_curated_inventory`/`merge_inventories` branches). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!`. `.venv/bin/mypy src/` → `Success: no issues found in 48 source files` (initially surfaced 1 error at `pipeline.py:295` — `inventory = curated or interview` narrowed to `DataSourceInventory | None` after the if-branch inferred `DataSourceInventory`; fixed with explicit `inventory: DataSourceInventory | None` annotation + import). `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total). New tests verified to pass via filtered run (`pytest -k "curated or inventory"` → 5/5).
- **Unchanged intentionally:** Decoupling guarantee preserved — `merge_inventories` lives in the orchestrator package, not in `packages/data-agent/`, so `tests/test_data_agent_decoupling.py` continues to pass without modification (no `IntakeReport` imports added to data-agent). `packages/data-agent/USAGE.md` untouched — its existing curated-inventory prose at line 262 describes loading the JSON shape for downstream consumers and remains accurate; orchestrator-side flag plumbing is a pipeline-runner concern, documented in CLI `--help` (the place users discover scripts/run_pipeline.py flags). Wiki Schema-Reference / Data-Guide / Worked-Examples / Pipeline-Overview / Phase-4-architecture-plan notes already document the inventory contract; the new flag is a CLI-surface addition not a contract change. `BACKLOG.md` already empty (12th consecutive carry pre-session; remains empty post-session per Learning #26 delete-not-flip). `ROADMAP.md` unchanged — all milestones already complete. Methodology files (`SESSION_RUNNER.md`, `SAFEGUARDS.md`) unchanged. Plan document `docs/planning/data-source-inventory-contract-plan.md` unchanged — the plan IS satisfied by this implementation; no edit needed (open questions Q7/Q8 are already answered "default JSON, default single file" in the plan, and Phase 4 was explicitly marked optional/scope-stripped which this session completes). **Candidate Learning #65 (handoff convention/date/boundary pre-naming) ADVANCED to 3rd-instance validation** — handoff named the deliverable and the merge-logic location but did not pre-discuss the flag-matrix semantics (independent vs. coupled) or single-file-vs-glob default; session surfaced via 2-question AskUserQuestion (3+2 options); operator-confirmed both bounded choices. **Candidate Learning #66 (Phase 1B archive-placement location verification) ADVANCED to 3rd-instance validation** — file-tail locator grep applied mechanically at Phase 1B; zero placement fumble (clean recovery shape extending the Session 80 success). Both candidates eligible for promotion to permanent row #39 at Session 82 (competing for one slot — phrasing pre-draft for #66 already in Session 80 gotcha #10; #65 phrasing TBD). Candidate Learning #55 (archive placement) stays at 2nd instance — may be permanently superseded by #66 promotion. Candidate Learning #57 (Edit-tool multi-match diagnosis) stays at 2nd instance — did not fire (row #38 recipe held). Candidate Learning #59 (5-file checkpoint) **ADVANCES — Session 81 touched 5 production-code files** (`scripts/run_pipeline.py` + `src/model_project_constructor/orchestrator/pipeline.py` + `src/model_project_constructor/orchestrator/adapters.py` + `tests/orchestrator/test_pipeline.py` + `CHANGELOG.md`; plus `SESSION_NOTES.md`) — first session at the 5-file threshold post-#59-filing; checkpoint commit discipline maintained (single coherent commit at session close, no mid-session checkpoint needed because no destructive operations and gates ran clean end-to-end). #59 advances to 2nd instance. Candidate Learning #61 (audit BOTH sides before "align X to Y") stays at 2nd instance — no inversion this session. Candidate Learning #62 (verify tool-PATH at Phase 0) **7th institutional application** (`.venv/bin/` preemptive prefix). Candidate Learning #63 (planning per-file completion criteria) stays at 1st instance — no planning session.
- **Next:** BACKLOG remains **empty** (13th consecutive carry will be reached at Session 82 Phase 0). The largest carried item (`--curated-inventory` follow-up) is now complete; the plan's Phase 4 scope is fully shipped (interview-derived in Session 70 + curated-loaded + merge logic in Session 81). Most likely Session 82 direction: (a) **Promote Candidate Learning #66** → permanent row #39 in `SESSION_RUNNER.md` Learnings table (paste-verbatim of pre-drafted phrasing in Session 80 gotcha #10 per Learning #27); (b) **Promote Candidate Learning #65** → permanent row #39 (phrasing TBD); both candidates at 3rd-instance — one row available; (c) wiki publish-mechanism three-options decision (carried but never picked up); (d) new off-BACKLOG operator direction.

### 2026-05-13 — Wiki Changelog v0.1.0 mirror update (Session 80)

Operator-directed wiki-mirror micro-deliverable (off-BACKLOG; BACKLOG remains **empty** since Session 70 — 11th consecutive carry). Updates `docs/wiki/claims-model-starter/Changelog.md` to mirror the root `CHANGELOG.md`'s v0.1.0 release-section transition formalized in Session 78. Pre-claim alignment via AskUserQuestion surfaced four mirror strategies — operator selected **"Re-date + roll [Unreleased] under it"** (closest structural mirror of root): rename `## [0.1.0 — Pilot Ready] — 2026-04-15` → `## [0.1.0] - 2026-05-12` (tag-creation date), roll the existing wiki-side `[Unreleased]` entries (Wiki expansion Sessions 19/20A/20B + End-to-end tutorial Session 18) under the new `[0.1.0]` heading, and leave a fresh empty `## [Unreleased]` heading above with an explanatory note. AskUserQuestion was Candidate Learning #65's **2nd-instance validation** (filed Session 78 as 1st instance — "handoff convention/date/boundary pre-naming"): Session 79's handoff direction (c) named the deliverable but did not pre-discuss the date-convention choice, forcing a mid-execution round-trip exactly as #65 forecasts; #65 advances to 2nd instance. Phase 1B applied NEW Candidate Learning #66's **2nd-instance validation** (filed Session 79 as 1st instance — Phase 1B archive-placement location verification): `grep -n "^### Session .* ARCHIVED ACTIVE TASK" SESSION_NOTES.md | head -1` located the file-tail archive zone at line 1828 (Session 78 archive heading) before constructing the Phase 1B Edit's `new_string`; the Session 79 archive heading + Task line were inserted above line 1828 with Learning #37 consume-and-replace; zero placement fumble (contrast: Session 79's 1st-instance fumble cost 2 extra Edits + ~2 minutes); **#66 advances to 2nd instance**. Wiki publish hook fires post-commit (touched file under `docs/wiki/claims-model-starter/`).

- **Changed:** `docs/wiki/claims-model-starter/Changelog.md` — single Edit consuming the `## [Unreleased]` heading through the original `## [0.1.0 — Pilot Ready] — 2026-04-15` preamble line ("Phases 1 through 6 complete..."). New structure: (a) fresh empty `## [Unreleased]` heading with an in-place note that previous wiki-side entries (Sessions 18, 19, 20A, 20B) have been folded into v0.1.0 in Session 80; (b) new `## [0.1.0] - 2026-05-12` heading matching root canonical; (c) one-paragraph release-section preamble explaining the date re-keying (tag-creation date 2026-05-12 vs. previous "Pilot Ready" narrative date 2026-04-15) + cross-reference to root `CHANGELOG.md` + Session 78; (d) preserved "Phases 1 through 6 complete. 422 tests at 97.18% coverage..." sentence verbatim as the second preamble paragraph; (e) Sessions 19/20A/20B wiki-expansion + Session 18 end-to-end-tutorial bullet-block contents preserved verbatim under [0.1.0]; (f) existing `### Phase 6 — Production hardening (Session 16)` and below entries unchanged in body and order. `SESSION_NOTES.md` — Phase 1B ACTIVE TASK Task/Status block updated for Session 80; Session 79 archive heading + abbreviated Task-line inserted above `### Session 78 ARCHIVED ACTIVE TASK` (Learning #37 consume-and-replace).
- **Verified:** `.venv/bin/pytest -q` → **588/588 passing** @ **97.32% coverage** (unchanged from Session 79 baseline; wiki not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Structural: `grep -c "^## \[Unreleased\]" docs/wiki/claims-model-starter/Changelog.md` → 1; `grep -c "^## \[0.1.0\] - 2026-05-12" docs/wiki/claims-model-starter/Changelog.md` → 1; `grep -c "Pilot Ready" docs/wiki/claims-model-starter/Changelog.md` → 1 (now exists only in the explanatory preamble, not in a heading). `git diff --numstat docs/wiki/claims-model-starter/Changelog.md` → `9 7` (additive heading restructure + preamble; all original bullet content preserved). Wiki publish hook prerequisite: file path under `docs/wiki/claims-model-starter/` confirmed → `scripts/publish_wiki.sh` will fire post-commit.
- **Unchanged intentionally:** No `src/`, `tests/`, `packages/`, `scripts/`, root `BACKLOG.md`, or `ROADMAP.md` edits. Root `CHANGELOG.md` v0.1.0 section unchanged — it remains canonical and this session merely mirrors it audience-facing. Wiki line 7 ("currently at version `0.1.0` (pre-1.0, pilot-ready)") unchanged — still accurate. All `### Phase 6` through `### Phase 1` sub-sections + `## [Architecture Plan]` + `## Quality-gate history` + `## Versioning policy` + `## See also` sections preserved verbatim. Methodology files (`SESSION_RUNNER.md`, `SAFEGUARDS.md`, methodology docs) untouched. Candidate Learning #55 (archive placement) stays at 2nd instance — Session 80's placement was driven by gotcha #18 prescriptive direction + the now-mechanical Candidate #66 recipe; 11 consecutive sessions (70-80) of gotcha-driven placement; may be permanently superseded by #66. **Candidate Learning #65 (handoff convention/date/boundary pre-naming) ADVANCED to 2nd instance** — Session 79 handoff named direction (c) "Update `docs/wiki/claims-model-starter/Changelog.md` to mirror v0.1.0 release section" but did not pre-discuss the date-convention choice (2026-04-15 retain vs. 2026-05-12 re-date vs. preamble-only); session surfaced via AskUserQuestion (4 options); operator selected re-date. **Candidate Learning #66 (Phase 1B archive-placement location verification) ADVANCED to 2nd instance** — file-tail locator grep applied at Phase 1B; zero placement fumble (clean recovery shape vs. Session 79's 1st-instance fumble). Candidate Learning #57 (Edit-tool multi-match diagnosis) stays at 2nd instance — did not fire (raw-string pre-grep returned 1 for every anchor; row #38 recipe held). Candidate Learning #59 (5-file checkpoint) stays at 1st instance — Session 80 touched 3 files (`docs/wiki/claims-model-starter/Changelog.md` + `CHANGELOG.md` + `SESSION_NOTES.md`). Candidate Learning #61 stays at 2nd instance — no "align X to Y" inversion. Candidate Learning #62 (verify tool-PATH at Phase 0) stays at 1st instance + **6th institutional application** (`.venv/bin/` preemptive prefix at gate-run time). Candidate Learning #63 (planning per-file completion criteria) stays at 1st instance — no planning session.
- **Next:** BACKLOG remains **empty** (12th consecutive carry will be reached at Session 81 Phase 0). Wiki Changelog audience-facing v0.1.0 section now matches root canonical date + structure. Most likely Session 81 direction: (a) `--curated-inventory` CLI flag follow-up (producer side of Phase 4 plan §9.3 merge logic; carried 10 consecutive sessions; still the largest single carried item); (b) Candidate Learning #65 advanced to 2nd instance this session — one more validated "transition X to Y" instance triggers promotion to permanent row #39; (c) Candidate Learning #66 advanced to 2nd instance this session — one more validated Phase-1B-clean-placement instance triggers promotion to permanent row #39 (#65 and #66 may compete for row #39 depending on which reaches 3rd instance first); (d) new off-BACKLOG operator direction.

### 2026-05-13 — Promote Candidate Learning #64 → permanent row #38 in SESSION_RUNNER.md (Session 79)

Operator-directed methodology housekeeping micro-deliverable (off-BACKLOG; BACKLOG remains **empty** since Session 70 — 10th consecutive carry). Adds permanent row #38 to the `SESSION_RUNNER.md` Learnings table, codifying the raw-string `grep -c "literal text" FILE` recipe for Edit-tool uniqueness pre-greps (no `^`/`$` regex anchors). Candidate #64 was filed in Session 76 (1st instance — caught after a `grep -c "^### Session 74 ARCHIVED ACTIVE TASK"` returned 1 but the Edit tool reported "Found 6 matches"), applied preemptively in Session 77 (2nd instance), and applied preemptively in Session 78 (3rd instance — promotion-triggering). Row #38 phrasing was pre-drafted in Session 78 Phase 3C and pasted verbatim per Learning #27. Mirrors the Sessions 71/72/73 promotion-micro shape (single-row table-edit + close-out). Phase 1B caught a placement fumble — the demoted Session 78 archive block was first written inline at the top of `SESSION_NOTES.md` (collocated with the current ACTIVE TASK) instead of at the file tail above the Session 77 archive heading; recovered with a two-edit sequence (remove inline + insert at tail with Learning #37 consume-and-replace). Does NOT trigger wiki publish hook (`SESSION_RUNNER.md` / `CHANGELOG.md` / `SESSION_NOTES.md` not under `docs/wiki/claims-model-starter/`).

- **Changed:** `SESSION_RUNNER.md` — added permanent row #38 to the Learnings table directly below row #37, using the pre-drafted Session 78 Phase 3C phrasing. Anchor: row #37's unique closing phrase `composes with Learning #34). |` followed by the table-end `---` separator (Learning #37 consume-and-replace; raw-string `grep -c` for that phrase = 1, single-line anchor sufficient). `SESSION_NOTES.md` — Phase 1B ACTIVE TASK Task/Status block updated for Session 79; Session 78 archive heading + Task line inserted above `### Session 77 ARCHIVED ACTIVE TASK` (Learning #37 consume-and-replace; anchor escalated to multi-line with distinguishing phrase `carried 8th consecutive session` per Candidate #64 row #38 recipe — raw-string `grep -c "### Session 77 ARCHIVED ACTIVE TASK"` returned 5, phrase-`grep -c` = 1).
- **Verified:** `.venv/bin/pytest -q` → **588/588 passing** @ **97.32% coverage** (unchanged from Session 78 baseline; methodology + session-notes files not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Structural: `grep -c "^| 38 |" SESSION_RUNNER.md` → 1 (row #38 present); `grep -c "^| 37 |" SESSION_RUNNER.md` → 1 (row #37 preserved); table-end `---` separator at line 313 (post-insert). Additive-only: zero rows #1-#37 modified.
- **Unchanged intentionally:** No `src/`, `tests/`, `packages/`, `scripts/`, wiki, `BACKLOG.md`, `ROADMAP.md`, or methodology-non-runner doc edits. `docs/wiki/claims-model-starter/Changelog.md` (audience-facing wiki mirror) untouched — its release cadence is independent. Wiki publish hook does **not** fire (none of the touched files under `docs/wiki/`). Candidate Learning #55 (archive placement) stays at 2nd instance — 10 consecutive sessions (70-79) of gotcha-driven counts; may stall. Candidate Learning #57 (Edit-tool multi-match diagnosis) stays at 2nd instance — did not fire (row #38 recipe prevented the failure-mode on the CHANGELOG insert). Candidate Learning #59 (5-file checkpoint) stays at 1st instance — Session 79 touched 3 files (`SESSION_RUNNER.md` + `SESSION_NOTES.md` + `CHANGELOG.md`). Candidate Learning #61 stays at 2nd instance — no "align X to Y" inversion. Candidate Learning #62 stays at 1st instance + **5th institutional application** (`.venv/bin/` preemptive prefix at gate-run time). Candidate Learning #63 stays at 1st instance — no planning session. **NEW Candidate Learning #65 (handoff convention/date/boundary pre-naming) stays at 1st instance** — Session 78 handoff did not predict a "transition X to Y" task for Session 79 (Session 79 was a row-promotion, not a transition); fires on future "transition" / "rename" / "rollover" deliverables. **Filed NEW Candidate Learning #66 (Phase 1B archive-placement location verification)** — 1st instance from this session's placement fumble: a demoted ACTIVE TASK block must be inserted at the file-tail archive zone (above the predecessor's `### Session N-1 ARCHIVED ACTIVE TASK` heading), not inline at the top adjacent to the current ACTIVE TASK. Recovery cost was 2 extra Edits + ~2 minutes; learning captures the verification step ("locate the bottom-of-file archive zone via `grep -n "^### Session .* ARCHIVED ACTIVE TASK" SESSION_NOTES.md \| tail -1` BEFORE constructing the Phase 1B Edit's new_string") that would have prevented it.
- **Next:** BACKLOG remains **empty** (11th consecutive carry will be reached at Session 80 Phase 0). Row #38 promotion lands `SESSION_RUNNER.md` Learnings table at 38 rows. Most likely Session 80 direction: (a) `--curated-inventory` CLI flag follow-up (producer side of plan §9.3 merge logic; carried 9 consecutive sessions; still the largest single carried item); (b) `docs/wiki/claims-model-starter/Changelog.md` mirror update for v0.1.0 (separate decision — wiki Changelog has its own release cadence); (c) new off-BACKLOG operator direction.

### 2026-05-13 — Transition [Unreleased] → [0.1.0] in CHANGELOG (Session 78)

Operator-directed release-bookkeeping micro-deliverable (off-BACKLOG; BACKLOG remains **empty** since Session 70 — 9th consecutive carry). Formalizes the v0.1.0 release section in `CHANGELOG.md` now that the `v0.1.0` git tag exists locally + on origin (created Session 74 on commit `b4c0dbb`). Per methodology v2.1 three-file split (`docs/methodology/README.md` §templates) and the existing Keep a Changelog framing already declared at `CHANGELOG.md:6`: renamed `## [Unreleased]` heading to `## [0.1.0] - 2026-05-12` (tag-creation date, selected via pre-edit AskUserQuestion at session start; alternative options — 2026-05-13 formalization date, or moving post-tag Session 76+77 entries to a fresh [Unreleased] — declined in favor of tag-date convention). Inserted fresh empty `## [Unreleased]` heading above for the next release cycle. Added a one-paragraph release-section preamble acknowledging that Session 76+77 entries (dated 2026-05-13) post-date the tag-creation commit but are preserved under [0.1.0] per operator direction. Phase 1B applied NEW Candidate Learning #64 (raw-string `grep -c` for Edit-tool uniqueness pre-greps, no `^`/`$` regex anchors) preemptively as a **3rd-instance PROMOTION-TRIGGERING** application — `grep -c "### Session 76 ARCHIVED ACTIVE TASK" SESSION_NOTES.md` returned 5 (4 prose-references in Session 77 handoff + 1 actual heading at line 1489); escalated upfront to multi-line anchor with distinguishing phrase `carried 7th consecutive session` (uniqueness verified `grep -c` = 1). Both Phase 1B Edits + the CHANGELOG content Edit landed first-attempt.

- **Changed:** `CHANGELOG.md` — converted the previous sole `## [Unreleased]` heading into (a) a freshly empty `## [Unreleased]` heading at the same position, (b) a new `## [0.1.0] - 2026-05-12` heading below it, and (c) a one-paragraph release-section preamble explaining the date choice + post-tag-entry preservation. Existing Session 70-77 entries (and the legacy `### Earlier history` section further down) flow under the new `## [0.1.0]` heading verbatim — no rewording, no summary collapsing.
- **Verified:** `.venv/bin/pytest -q` → **588/588 passing** @ **97.32% coverage** (unchanged from Session 77 baseline; CHANGELOG not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Structural checks: `grep -c "^## \[Unreleased\]" CHANGELOG.md` → 1 (fresh empty heading present); `grep -c "^## \[0.1.0\] - 2026-05-12" CHANGELOG.md` → 1 (release heading present); `git diff --numstat CHANGELOG.md` (pre-Session-78-entry) → `4 0` (4 additions: fresh [Unreleased] preserved + 1 blank line + new [0.1.0] heading + 1 blank line + 1 preamble paragraph = 5 lines visible, of which 4 are net-new — actually `4 0` reflects the 4 inserted lines including the new heading + blank + preamble + blank; `0` deletions because the original `## [Unreleased]` line is preserved as the new empty heading). Additive-only: zero existing Session 70-77 entry text modified or removed.
- **Unchanged intentionally:** Tag `v0.1.0` not modified (re-tagging would have been a destructive operation on a published tag, never considered). No `src/`, `tests/`, `packages/`, `scripts/`, wiki, `BACKLOG.md`, `ROADMAP.md`, or methodology doc edits. `docs/wiki/claims-model-starter/Changelog.md` (the user-facing wiki mirror) left untouched — its release-version-tracking cadence is separate from this maintainer-facing file (declared at `CHANGELOG.md:3`). Wiki publish hook does **not** fire (CHANGELOG.md not under `docs/wiki/claims-model-starter/`). Candidate Learning #55 (archive placement) stays at 2nd instance — Session 78 Phase 1B placement was driven by Session 77 gotcha #17 prescriptive direction (9 consecutive sessions 70-78 of gotcha-driven counts). Candidate Learning #57 (Edit-tool multi-match diagnosis) stays at 2nd instance — did not fire (#64 preemptive raw-string + multi-line escalation prevented Edit-tool "Found N matches"). Candidate Learning #59 (5-file checkpoint) stays at 1st instance — Session 78 touched only 2 files (`CHANGELOG.md` + `SESSION_NOTES.md`). Candidate Learning #61 (audit BOTH sides before "align X to Y") stays at 2nd instance — Session 77 handoff did not contain an inversion. Candidate Learning #62 (verify tool-PATH at Phase 0) stays at 1st instance + **4th institutional application** (`.venv/bin/` preemptive prefix at gate-run time). Candidate Learning #63 stays at 1st instance — no planning session this session. **Candidate Learning #64 ADVANCED to 3rd-instance PROMOTION-TRIGGERING** — recommended for promotion to permanent row #38 in Session 79 (phrasing pre-drafted in Phase 3C of this session).
- **Next:** BACKLOG remains **empty** (10th consecutive carry will be reached at Session 79 Phase 0). v0.1.0 release section now formalized; the next release tag (v0.2.0 or v0.1.1) will start accumulating entries under the fresh `## [Unreleased]` heading. Most likely Session 79 direction: (a) **promote Candidate Learning #64** to permanent row #38 in `SESSION_RUNNER.md` Learnings table — 3rd promotion-triggering instance reached this session, phrasing pre-drafted in Phase 3C; (b) `--curated-inventory` CLI flag follow-up (producer side of plan §9.3 merge logic); (c) `docs/wiki/claims-model-starter/Changelog.md` mirror update if the operator wants the wiki release-tracker to reflect the v0.1.0 formalization (separate decision — wiki Changelog is audience-facing and its release cadence is independent); (d) new off-BACKLOG operator direction.

## [0.1.0] - 2026-05-12

First release tag. Marks the project's initial versioned checkpoint. The `v0.1.0` git tag was created in Session 74 on commit `b4c0dbb`; this section formalizes the release notes in Session 78. All entries below — Sessions 70 through 77, plus the legacy `### Earlier history` section — constitute the v0.1.0 release content. Some entries (Sessions 76, 77) are dated after the tag-creation commit because they are doc-only sessions that finalized the release notes; the release date is fixed at the tag's commit date per Keep a Changelog convention.

### 2026-05-13 — Wiki delta Session B: architecture-plan inline notes (Session 77)

Operator-directed execution of `docs/planning/wiki-inventory-delta.md` Session B (off-BACKLOG; BACKLOG remains **empty** since Session 70 — 8th consecutive carry). Session 75 produced the plan; Session 76 executed Session A (5 wiki+USAGE files); Session 77 executes Session B — adds three additive forward-pointer notes to `docs/architecture-history/architecture-plan.md` per plan §3.6 + §6: a Phase 3 note near §5.2 `DataRequest` (announces the optional `data_source_inventory` field with pointers to the contract plan + Schema-Reference §5), a Phase 3 note near §5.3 `DataReport` `PrimaryQuery` (announces `inventory_entries_used` field with the same pointers), and a Phase 4 note in §7 Data Agent Reuse Interface (announces the orchestrator-owned `intake_qa_pairs_to_inventory` adapter + `--inventory-from-intake` CLI flag, with pointer to contract plan §5.3). Phase 0 re-verified plan §3.6 line citations per Learning #11 — §5.2 at 285, §5.3 PrimaryQuery at 321, §7 at 435 — all matched (zero drift). Phase 1B applied NEW Candidate #64 preemptively: raw-string `grep -c "### Session 75 ARCHIVED ACTIVE TASK"` returned 5 (4 prose-references in Session 76 handoff + 1 actual heading at line 1320), escalated upfront to a multi-line anchor with distinguishing phrase `carried 6th consecutive session` (uniqueness verified `grep -c` = 1). Both Phase 1B Edits + all 3 content Edits landed first-attempt — **Candidate Learning #64 advances to 2nd instance** (preemptive raw-string + multi-line escalation worked without an Edit-tool "Found N matches" failure). Per plan §3.6 + §6 the deliverable is "a note" not a rewrite; existing §5.2 / §5.3 / §7 bodies untouched (+6 / -0 lines). Does NOT trigger wiki publish hook (architecture-plan.md not under `docs/wiki/`).

- **Added (3 forward-pointer notes, 1 file):** `docs/architecture-history/architecture-plan.md` — (a) Phase 3 note after §5.2's "decouples the Data Agent for reuse" paragraph mentioning the optional `data_source_inventory: DataSourceInventory | None` field on `DataRequest` and pointing to `docs/planning/data-source-inventory-contract-plan.md` + `docs/wiki/claims-model-starter/Schema-Reference.md` §5. (b) Phase 3 note after the §5.3 `DataReport` code block mentioning `PrimaryQuery.inventory_entries_used: list[str]` with the same two pointers. (c) Phase 4 note in §7 after the "The inference helpers..." paragraph mentioning `intake_qa_pairs_to_inventory` in `src/model_project_constructor/orchestrator/adapters.py` + `scripts/run_pipeline.py --inventory-from-intake` flag, with pointer to contract plan §5.3.
- **Verified:** `.venv/bin/pytest -q` → **588/588 passing** @ **97.32% coverage** (unchanged from Session 76 baseline; docs not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Plan §3.6 completion criteria: `grep -cE "data_source_inventory|inventory_entries_used|data-source-inventory-contract-plan" docs/architecture-history/architecture-plan.md` → **3** (≥2 required). `git diff --numstat` → `6 0` for the one file (additive only — no structural changes to §5.2 / §5.3 / §7 bodies). Plan §8 forecast for architecture-plan.md: Pre=0, Post≥2 → actual 3 (forecast met).
- **Unchanged intentionally:** `docs/wiki/claims-model-starter/*.md` — all 4 wiki files untouched (Session A scope, completed Session 76). `packages/data-agent/USAGE.md` — Session A scope, untouched. `docs/planning/data-source-inventory-contract-plan.md` — upstream design plan. `docs/planning/wiki-inventory-delta.md` — the spec; not modified during execution. No `src/`, `tests/`, `packages/data-agent/src/`, or `scripts/` files touched (docs-only delta, plan §6 non-goal). ROADMAP.md unchanged (no milestone transitions). Candidate Learning #55 (archive placement) stays at 2nd instance — Session 77's Phase 1B archive placement was again driven by Session 76 gotcha #19 prescriptive direction (8 consecutive sessions 70-77 of declining gotcha-driven counts). Candidate Learning #57 stays at 2nd instance — did not fire this session (NEW #64 preemptive raw-string + multi-line escalation prevented the Edit-tool "Found N matches" condition; healthy outcome — #57 fires only on the failure mode itself). Candidate Learning #59 (5-file checkpoint) stays at 1st instance — Session 77 touched only 3 files (architecture-plan.md + SESSION_NOTES.md + CHANGELOG.md). Candidate Learning #61 (audit BOTH sides) stays at 2nd instance — handoff did not contain a "skip X" / "align X to Y" framing inversion. Candidate Learning #62 (verify tool-PATH at Phase 0) stays at 1st instance + institutional 3rd application. Candidate Learning #63 (planning per-file completion criteria) stays at 1st instance — no planning session. Wiki publish hook does **not** fire (architecture-plan.md not under `docs/wiki/claims-model-starter/`).
- **Next:** BACKLOG remains **empty** (8th consecutive carry → 9th at Session 78 start). Wiki-inventory-delta plan is now **fully executed** (Session A + Session B both complete). Operator direction at Session 78 start. Likely paths: (a) `--curated-inventory` CLI flag follow-up (producer side of plan §9.3 merge logic); (b) `[Unreleased]` → `[0.1.0]` CHANGELOG section transition now that `v0.1.0` tag exists; (c) Candidate Learning #64 advanced to 2nd instance this session — one more validated instance triggers promotion to permanent row #38; Candidate #57 still at 2nd instance and depends on a future Edit-tool failure mode to advance; (d) new off-BACKLOG work item.

### 2026-05-13 — Wiki delta Session A: Phase 3/4 data-source-inventory documentation (Session 76)

Operator-directed execution of `docs/planning/wiki-inventory-delta.md` Session A (off-BACKLOG; BACKLOG remains **empty** since Session 70 — 7th consecutive carry). Session 75 produced the plan; Session 76 executes the 5-file Session A scope — the 4 wiki files under `docs/wiki/claims-model-starter/` (`Schema-Reference.md`, `Data-Guide.md`, `Worked-Examples.md`, `Pipeline-Overview.md`) + `packages/data-agent/USAGE.md`. Phase 0 re-verified all plan §3.1-§3.5 line citations + Phase 4 code anchors (`intake_qa_pairs_to_inventory` at `orchestrator/adapters.py:113`; `--inventory-from-intake` at `scripts/run_pipeline.py:477`) per Learning #11. Phase 1B Edit #2 hit a multi-match on the predecessor-archive heading despite a pre-grep of `^### Session 74 ARCHIVED ACTIVE TASK` returning `1` — Edit tool ignores regex line-anchors, so 6 unanchored prose hits triggered the "Found 6 matches" error. Recovered first-retry by extending to a multi-line anchor (heading + blank + Task-line prefix) per Learning #34. **Candidate Learning #57 advances to 2nd instance** (Edit-tool multi-match diagnosis fired + diagnosed + recovered). **New Candidate Learning #64 filed** — pre-grep recipe must use raw-string matching (no `^` / `$`), since Edit doesn't honor regex anchors. The 5 content edits all landed first-attempt afterward. Session B (architecture-plan note, 1 file) remains deferred per plan §5 sequencing.

- **Added (5 file deltas):** `docs/wiki/claims-model-starter/Schema-Reference.md` — new `### DataSourceInventory and nested types` sub-section in §5 (~77 insertions; documents `ColumnMetadata` / `ProducerMetadata` / `DataSourceEntry` / `DataSourceInventory` Pydantic types verbatim from `packages/data-agent/src/model_project_constructor_data_agent/schemas.py` per Learning #11). `docs/wiki/claims-model-starter/Data-Guide.md` — line 109 Interview-producer bullet updated from stale "reference implementation deferred until pilot demand" to "shipped Phase 4 as `intake_qa_pairs_to_inventory`...". `docs/wiki/claims-model-starter/Worked-Examples.md` — new `#### Optional: deriving an inventory from the intake interview` sibling sub-section after the Phase 3 "attaching a data source inventory" block (~12 insertions; documents the `--inventory-from-intake` CLI flag + cited line numbers). `docs/wiki/claims-model-starter/Pipeline-Overview.md` — Orchestrator step 2 bullet expanded to mention the optional `DataSourceInventory` derivation from `IntakeReport.qa_pairs`. `packages/data-agent/USAGE.md` — §"Data source inventory" Interview bullet updated to reflect Phase 4 shipped status + closing paragraph appended with a Phase 4 sentence covering `--inventory-from-intake` flag (orchestrator-side cross-reference; respects the decoupling guarantee).
- **Verified:** `.venv/bin/pytest -q` → **588/588 passing** @ **97.32% coverage** (unchanged from Session 75 baseline; docs not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Plan §3.X completion criteria (per-file grep checks): §3.1 USAGE Phase 4 count = 2 (≥2); §3.1 intake-from-intake variants = 3 (≥1); §3.2 Data-Guide shipped-Phase-4 variants = 1 (≥1); §3.2 deferred = 0 (==0); §3.3 Worked-Examples deriving-an-inventory variants = 3 (≥1); §3.4 Schema-Reference `class` blocks = 4 (≥4); §3.5 Pipeline-Overview inventory-from-intake variants = 1 (≥1). All pass. §8 author-forecast counts use `grep -c` (line count) rather than match count, so the lower bounds for `Worked-Examples.md` (6 vs ≥9) and `USAGE.md` (12 vs ≥14) appear below forecast — `git diff` confirms zero §8-pattern lines removed (the apparent gap is a counting-method mismatch, not a content gap). Plan §3 is the authoritative criterion set.
- **Unchanged intentionally:** `docs/architecture-history/architecture-plan.md` — Session B scope per plan §5 sequencing; remains untouched. `docs/planning/data-source-inventory-contract-plan.md` — upstream design plan; downstream wiki delta does not modify it. `docs/planning/wiki-inventory-delta.md` — the spec; not touched during execution. No `src/`, `tests/`, `packages/data-agent/src/`, or `scripts/` files modified (docs-only delta, by plan §6 non-goals). ROADMAP.md unchanged (no milestone transitions). Candidate Learning #55 (archive placement) stays a candidate (still 2nd instance; Session 76's Phase 1B archive placement was again driven by Session 75 gotcha #19 prescriptive direction — 7 consecutive sessions 70-76 of declining gotcha-driven counts). Candidate Learning #59 (checkpoint-commit at 5-files boundary) stays a candidate (1st instance; did not fire — Session 76 touched 7 files including the 5 content files + SESSION_NOTES.md + CHANGELOG.md, but all 5 content edits were independent and landed inside one logical pass; explicit checkpoint at the 5-files boundary did not occur because the gates ran post-pass, not mid-pass — `#59` recipe still 1st instance). Candidate Learning #61 (audit BOTH sides before "align X to Y") stays at 2nd instance (this session's handoff did not contain an "align X to Y" framing inversion; #61 did not fire). Candidate Learning #62 (verify tool-PATH at Phase 0) stays 1st instance (applied preemptively as institutional habit — no fresh-discovery instance). Candidate Learning #63 (planning per-file completion criteria are weaker-form vs region-scoped) stays 1st instance (no planning session this session; did not fire). Wiki publish hook fires post-commit (4 wiki files changed; `scripts/publish_wiki.sh`).
- **Next:** BACKLOG remains **empty**. Operator direction at Session 77 start. Most likely path: **Session B** of the wiki delta plan (1 file: `docs/architecture-history/architecture-plan.md`; ~6-9 lines total, 2-3 inline notes per plan §3.6; does NOT trigger wiki publish hook). Alternative paths: (a) `--curated-inventory` CLI flag follow-up (producer side of plan §9.3 merge logic); (b) `[Unreleased]` → `[0.1.0]` CHANGELOG section transition; (c) new off-BACKLOG work item. Candidate Learning #57 reached 2nd instance this session — one more validated instance triggers promotion to permanent row #38. New Candidate Learning #64 at 1st instance — fires on every Edit-tool uniqueness pre-grep.

### 2026-05-12 — Wiki/USAGE/architecture-plan delta plan for Phase 3/4 data-source-inventory documentation debt (Session 75)

Operator-directed audit-first planning deliverable (off-BACKLOG; BACKLOG remains **empty** since Session 70). Carried forward 6 consecutive sessions (69→74) before this session picked it up. Plan §8.6 of `docs/planning/data-source-inventory-contract-plan.md` named ~6 doc surfaces that should have been updated alongside Phases 3 (Session 60) + 4 (Session 70); Session 74's handoff framed them as "skipped." Session 75 Phase 0 audit corrected that framing — `grep -cE "DataSourceInventory|data_source_inventory|data source inventory"` returned **13 / 7 / 7 / 3 / 2 / 0** hits across `USAGE.md` / `Data-Guide.md` / `Worked-Examples.md` / `Schema-Reference.md` / `Pipeline-Overview.md` / `architecture-plan.md` respectively — partial coverage already exists in 5 of 6 surfaces; only `architecture-plan.md` is fully unaware. Pre-claim alignment via AskUserQuestion (4 options) resolved scope to **"Audit-first delta plan"** (over: architecture-plan-only, Pipeline-Overview-only, all-6-in-one). Per Planning Workstream discipline (SESSION_RUNNER FM #18 / FM #19): the plan IS the deliverable; no edits to any of the 6 target surfaces this session.

- **Added:** `docs/planning/wiki-inventory-delta.md` — 9-section plan documenting per-file delta inventory for the 6 surfaces, with: §2 current-state audit (grep-verified hit counts), §3.1-§3.6 per-file delta (current coverage / what's missing / what to add / file-anchored completion criteria), §4 cross-file invariants (no new wiki pages, additive-only, Learning #11 re-grep at edit time, wiki publish hook handling), §5 suggested 2-session execution split respecting SAFEGUARDS 5-file blast radius (Session A = 5 wiki+USAGE files + 1 publish hook fire; Session B = `architecture-plan.md` only), §6 non-goals, §7 risks + mitigations, §8 expected post-execution grep counts, §9 plan checklist.
- **Verified:** `.venv/bin/pytest -q` → **588/588 passing** @ **97.32% coverage** (unchanged from Session 74 baseline; planning docs not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `.venv/bin/mypy src/` → `Success: no issues found in 48 source files`; `.venv/bin/mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Plan presence: `test -f docs/planning/wiki-inventory-delta.md` → 0. Section-count check: `grep -cE "^## " docs/planning/wiki-inventory-delta.md` → 9.
- **Unchanged intentionally:** None of the 6 target documentation surfaces (`Schema-Reference.md`, `Data-Guide.md`, `Worked-Examples.md`, `Pipeline-Overview.md`, `USAGE.md`, `architecture-plan.md`) touched this session — Planning Workstream discipline (FM #18 / FM #19) requires plan-and-stop-before-implementation. Subsequent Session A executes the 5-file wiki+USAGE batch; Session B executes the architecture-plan note. Candidate Learning #55 (archive placement) stays a candidate (still 2nd instance; Session 75's Phase 1B archive placement was again driven by Session 74 gotcha #16 prescriptive direction, not an explicit decision-time grep — 6 consecutive sessions 70-75 declining gotcha-driven counts). Candidate Learning #57 (Edit-tool `new_string missing` → multi-match diagnosis) stays a candidate (1st instance; did not fire — Learning #34 pre-grep prevented multi-match conditions). Candidate Learning #59 (checkpoint-commit at 5-files boundary) stays a candidate (1st instance; did not fire — Session 75 touched only 3 files: SESSION_NOTES.md + CHANGELOG.md + the new planning doc). New Candidate Learning #61 (audit BOTH sides before "align X to Y") advanced to **2nd instance** — Session 75 audit-first scoping of the wiki delta was exactly this pattern applied at Phase 0 (handoff said "skipped"; audit said "partial coverage in 5/6"). New Candidate Learning #62 (verify tool-PATH at Phase 0) stays 1st instance (did fire — `.venv/bin/` prefix used preemptively this session, but that's an institutional habit from the gotcha, not a fresh-discovery instance). 3-instance promotion discipline preserved. Wiki publish hook does NOT fire (no `docs/wiki/claims-model-starter/*.md` change — by design; this is a planning-only session).
- **Next:** BACKLOG remains **empty**. Operator direction at Session 76 start. Most likely path: **Session A** of the wiki delta plan (5 files: `Schema-Reference.md` + `Data-Guide.md` + `Worked-Examples.md` + `Pipeline-Overview.md` + `packages/data-agent/USAGE.md`; triggers wiki publish hook). Alternative paths: (a) `--curated-inventory` CLI flag follow-up (producer side of plan §9.3); (b) `[Unreleased]` → `[0.1.0]` CHANGELOG section transition; (c) methodology housekeeping if Candidate #55/#57/#59/#61/#62 reach promotion thresholds; (d) new off-BACKLOG work item. Plan-then-implement sequencing (FM #18/#19) means Session 76 implements Session A — it does NOT also plan or implement Session B in the same session.

### 2026-05-12 — Release marker v0.1.0: align README version to canonical (Session 74)

Operator-directed version-plumbing follow-up (off-BACKLOG; BACKLOG remains empty). Carried forward 6 consecutive sessions (68→73) before this session picked it up. The handoff framing was "align packages to README's `0.01`", but Phase 0 audit found the canonical state was the opposite: `pyproject.toml`, `src/model_project_constructor/__init__.py`, and `packages/data-agent/pyproject.toml` were already `0.1.0` (long pre-dating the README's `**Version:** 0.01` marker, which was added in commit `0e96526` and was the actual drift). Pre-claim alignment via AskUserQuestion surfaced four options (literal-`0.01`, normalized-`0.1`, pre-alpha-`0.0.1`, reverse-direction-update-README); operator selected reverse-direction so the packages stay on their canonical PEP 440 release literal and the README marker matches. Tag `v0.1.0` created on the close-out commit and pushed to origin per operator authorization — first release tag on the project.

- **Added:** Git tag `v0.1.0` on the close-out commit, annotated form (`git tag -a v0.1.0`), pushed to `origin`. First release-marker tag in the repository (`git tag --list` was empty before this session).
- **Changed:** `README.md:3` — `**Version:** 0.01` → `**Version:** 0.1.0`. One-line edit; the README's version marker now matches the three canonical PEP 440 release literals in `pyproject.toml:3`, `src/model_project_constructor/__init__.py:3`, and `packages/data-agent/pyproject.toml:3` (all unchanged).
- **Verified:** `.venv/bin/pytest -q` → **588/588 passing** @ **97.32% coverage** (unchanged from Session 73 baseline; README not on test inputs). `.venv/bin/ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `.venv/bin/mypy` (uses `[tool.mypy].packages` config covering both `model_project_constructor` + `model_project_constructor_data_agent`) → `Success: no issues found in 61 source files` (48 src + 13 packages; unchanged). `git tag --list` → `v0.1.0` present. `grep -c "Version:** 0.1.0" README.md` → 1.
- **Unchanged intentionally:** `pyproject.toml` / `__init__.py` / `packages/data-agent/pyproject.toml` already on `0.1.0` — no edits required. Candidate Learning #55 (archive placement) stays a candidate (still 2nd instance; Session 74's Phase 1B archive placement was again driven by Session 73 gotcha #12 prescriptive direction, not an explicit decision-time grep). Candidate Learning #57 (Edit-tool `new_string missing` → multi-match diagnosis) stays a candidate (1st instance; did not fire — Learning #34 pre-grep prevented all multi-match conditions). Candidate Learning #59 (checkpoint-commit at 5-files boundary) stays a candidate (1st instance; did not fire — Session 74 touched only 3 files). 3-instance promotion discipline preserved. No code, tests, plans, CI, wiki, BACKLOG, ROADMAP, or `docs/` files other than `README.md` + `CHANGELOG.md` + `SESSION_NOTES.md` touched. Wiki publish hook does not fire (no `docs/wiki/claims-model-starter/*.md` change).
- **Next:** BACKLOG remains **empty**. Operator direction at Session 75 start. Likely candidates: (a) wiki updates for Phase 3/4 data-source-inventory (documentation debt: `Schema-Reference.md`, `Data-Guide.md`, `Worked-Examples.md`, `Pipeline-Overview.md`, `USAGE.md`, `architecture-plan.md`); (b) `--curated-inventory` CLI follow-up (producer side of plan §9.3 merge logic); (c) further methodology housekeeping if/when Candidate #55/#57/#59 reach promotion thresholds; (d) new off-BACKLOG work item. No candidate-learning is currently PROMOTION-READY (queue cleared at Session 73). With v0.1.0 tagged, this section can also be transitioned from `[Unreleased]` to `[0.1.0]` in a future close-out if/when desired — left as `[Unreleased]` for now since the close-out commit + tag are coincident and there is no separate "release process" yet.

### 2026-05-12 — Promote Candidate Learning #60 to permanent row #37 (Session 73)

Methodology housekeeping micro-deliverable, operator-directed (off-BACKLOG — BACKLOG remains empty post-Session-70). Promotes Candidate Learning #60 (Edit-tool boundary-heading consume-and-replace) to permanent row #37 in `SESSION_RUNNER.md`'s Learnings table. Session 72's Phase 3C drafted the promotion phrasing verbatim after Candidate #60 reached its 3rd validated instance (filed Session 70 1st, applied Session 71 2nd, applied Session 72 3rd promotion-triggering). Session 73 validates as the 4th instance — at Phase 1B the Session 72 archive-heading insertion used `old_string` consuming `### Session 71 ARCHIVED ACTIVE TASK\n\n**Task:** Session 71 — operator direction (off-BACKLOG, methodology housekeeping; BACKLOG is empty post-Session-70).` (multi-line anchor pre-verified unique by `grep -c` returning 1) and `new_string` replicating both at its tail. Row #37 follows the same 4-column shape as rows #34/#35/#36. Per Session 72's self-flag on rationale-augmentation, this session defaulted to paste-verbatim of the pre-drafted phrasing (no augmentation).

- **Added:** `SESSION_RUNNER.md` — new row #37 in the Learnings table, inserted between row #36 and the `---` separator that closes the table. Phrasing matches Session 72's Phase 3C draft verbatim (rule statement + "Distinct from Learning #34..." elaboration + "Useful for..." examples list). Source column cites Sessions 70 (filed) / 71 (applied) / 72 (applied — promotion-triggering) / 73 (validated 4th instance + promoted). When-to-Apply column composes with Learning #34 (pre-verify multi-line anchor uniqueness with `grep -c`).
- **Verified:** `uv run pytest -q` → **588/588 passing** @ **97.32% coverage** (unchanged from Session 72 baseline; no `src/`, `packages/`, `tests/`, or `scripts/` files touched). `uv run ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `uv run mypy src/` → `Success: no issues found in 48 source files`; `uv run mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Row presence: `grep -c "^| 37 |" SESSION_RUNNER.md` → 1.
- **Unchanged intentionally:** Candidate Learning #55 (archive placement convention) stays a candidate (still 2nd instance — Session 73's placement was again driven by Session 72 gotcha #11 prescriptive direction, not an explicit decision-time grep; four consecutive sessions 70/71/72/73 have declined to count gotcha-driven placement as explicit decision-time application). Candidate Learning #57 (Edit-tool `new_string missing` → multi-match diagnosis) stays a candidate (1st instance; did not fire — Learning #34 pre-grep prevented all multi-match conditions; healthy outcome). Candidate Learning #59 (checkpoint-commit at 5-files boundary) stays a candidate (1st instance; did not fire — Session 73 touched only 3 files). 3-instance promotion discipline preserved. No code, tests, plans, CI, wiki, BACKLOG, ROADMAP, or `docs/` files other than `SESSION_RUNNER.md` + `CHANGELOG.md` + `SESSION_NOTES.md` touched. Wiki publish hook does not fire (no `docs/wiki/claims-model-starter/*.md` change).
- **Next:** BACKLOG remains **empty**. Operator direction at Session 74 start. Likely candidates: (a) version-plumbing follow-up (align `pyproject.toml` / `__version__` / git tag with README's `0.01`); (b) wiki updates for Phase 3/4 data-source-inventory (documentation debt: `Schema-Reference.md`, `Data-Guide.md`, `Worked-Examples.md`, `Pipeline-Overview.md`, `USAGE.md`, `architecture-plan.md`); (c) `--curated-inventory` CLI flag follow-up; (d) further methodology housekeeping if/when Candidate #55/#57/#59 reach promotion thresholds; (e) new off-BACKLOG work item. No candidate-learning is currently PROMOTION-READY (the trio of methodology micros across Sessions 71/72/73 has cleared the standing promotion queue).

### 2026-05-12 — Promote Candidate Learning #58 to permanent row #36 (Session 72)

Methodology housekeeping micro-deliverable, operator-directed (off-BACKLOG — BACKLOG remains empty post-Session-70). Promotes Candidate Learning #58 (Phase-1B vs Phase-3 boundary discipline) to permanent row #36 in `SESSION_RUNNER.md`'s Learnings table. Session 71's Phase 3C drafted the promotion phrasing verbatim after Candidate #58 reached its 3rd validated instance (filed Session 69 1st, applied Session 70 2nd, applied Session 71 3rd promotion-triggering). Session 72 validates as the 4th instance — Phase 1B touched only the `## ACTIVE TASK` Task/Status block + predecessor archive heading (no heading renames, no eval-section content, no `What Session N Did` body drafted at Phase 1B); both Edits landed first-attempt. Row #36 follows the same 4-column shape as rows #34 and #35. Mirrors Session 71's promotion of Candidate #56 → row #35 and Session 69's promotion of #52 → row #34. Candidate Learning #60 (Edit-tool boundary-heading consume-and-replace) advanced to 3rd instance this session — promotion-triggering — applied on the Session 71 archive-heading insertion (old_string consumed the Session 70 boundary heading + Task line for uniqueness; new_string included both the new archive content and the replicated boundary heading at its tail).

- **Added:** `SESSION_RUNNER.md` — new row #36 in the Learnings table, inserted between row #35 and the `---` separator that closes the table. Phrasing matches Session 71's Phase 3C draft verbatim. Source column cites Sessions 69 (filed) / 70 (applied) / 71 (applied — promotion-triggering) / 72 (validated 4th instance + promoted).
- **Verified:** `uv run pytest -q` → **588/588 passing** @ **97.32% coverage** (unchanged from Session 71 baseline; no `src/`, `packages/`, `tests/`, or `scripts/` files touched). `uv run ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `uv run mypy src/` → `Success: no issues found in 48 source files`; `uv run mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Row presence: `grep -c "^| 36 |" SESSION_RUNNER.md` → 1.
- **Unchanged intentionally:** Candidate Learning #55 (archive placement convention) stays a candidate (still 2nd instance; Session 72's placement was again driven by gotcha-prescriptive direction rather than an explicit `^### Session .* ARCHIVED ACTIVE TASK` decision-time grep, preserving the conservative count). Candidate Learning #57 (Edit-tool `new_string missing` → multi-match diagnosis) stays a candidate (1st instance; did not fire this session — Learning #34 pre-grep prevented all multi-match conditions, the healthy outcome). Candidate Learning #59 (checkpoint-commit at 5-files boundary) stays a candidate (1st instance; did not fire — Session 72 touched only 3 files). 3-instance promotion discipline preserved. No code, tests, plans, CI, wiki, BACKLOG, ROADMAP, or `docs/` files other than `SESSION_RUNNER.md` + `CHANGELOG.md` + `SESSION_NOTES.md` touched. Wiki publish hook does not fire (no `docs/wiki/claims-model-starter/*.md` change).
- **Next:** BACKLOG remains **empty**. Operator direction at Session 73 start. Likely candidates: (a) promote Candidate Learning #60 (consume-and-replace boundary-heading discipline) to permanent row #37 — 3rd promotion-triggering instance reached this session, pre-drafted phrasing in Session 72 Phase 3C; (b) version-plumbing follow-up (align `pyproject.toml` / `__version__` / git tag with README's `0.01`); (c) wiki updates for Phase 3/4 data-source-inventory (documentation debt: `Schema-Reference.md`, `Data-Guide.md`, `Worked-Examples.md`, `Pipeline-Overview.md`, `USAGE.md`, `architecture-plan.md`); (d) `--curated-inventory` CLI follow-up; (e) further methodology housekeeping when Candidate #55/#57/#59 reach promotion thresholds; (f) new off-BACKLOG work item.

### 2026-05-12 — Promote Candidate Learning #56 to permanent row #35 (Session 71)

Methodology housekeeping micro-deliverable, operator-directed (not from BACKLOG — BACKLOG is empty post-Session-70). Promotes Candidate Learning #56 (archive-heading pre-grep specialization at Phase 1B) to permanent row #35 in `SESSION_RUNNER.md`'s Learnings table. Session 70's Phase 3C drafted the promotion phrasing verbatim after Candidate #56 reached its 3rd validated instance (filed Session 68 1st, applied Session 69 2nd, applied Session 70 3rd promotion-triggering). Session 71 validates as the 4th instance — Phase 1B pre-grep against `^### Session 70 ARCHIVED ACTIVE TASK` returned 0 hits, archive heading insertion landed first-attempt. Row #35 follows the same shape as row #34 (the parent rule): 4-column `| N | Learning | Source | When to Apply |`. Row #34's See-also note updated from "Candidate Learning #56 (in flight) ... Promote when 3rd instance lands" to "Learning #35" so the cross-reference is no longer stale. Mirrors Session 69's promotion of Candidate Learning #52 → row #34.

- **Added:** `SESSION_RUNNER.md` — new row #35 in the Learnings table, inserted between row #34 and the `---` separator that closes the table. Phrasing matches Session 70's Phase 3C draft verbatim. Source column cites Sessions 68 (filed) / 69 (applied) / 70 (applied — promotion-triggering) / 71 (validated 4th instance + promoted).
- **Changed:** `SESSION_RUNNER.md` — row #34's See-also note edited in place: `Candidate Learning #56 (in flight) — ... Promote when 3rd instance lands.` → `Learning #35 — ...` (stale "in flight" reference removed; cross-reference points at the now-permanent row).
- **Verified:** `uv run pytest -q` → **588/588 passing** @ **97.32% coverage** (unchanged from Session 70 baseline; no `src/`, `packages/`, or `tests/` files touched). `uv run ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `uv run mypy src/` → `Success: no issues found in 48 source files`; `uv run mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (61 total, unchanged). Row presence: `grep -c "^| 35 |" SESSION_RUNNER.md` → 1. Row #34's See-also still cross-references row #35 (no dangling "in flight" reference).
- **Unchanged intentionally:** Candidate Learning #55 (archive placement convention) stays a candidate (still 2nd instance per Session 70 close-out; needs 1 more for promotion). Candidate Learning #57 (Edit-tool `new_string missing` → multi-match diagnosis) stays a candidate (1st instance; may never reach 3rd if Learning #34 pre-grep discipline holds). Candidate Learning #58 (Phase-1B vs Phase-3 boundary) at 2nd instance — Session 71's Phase 1B applied the recipe mechanically (Task/Status block + predecessor archive heading only); promotion-trigger deferred to a session that explicitly documents the 3rd application. Candidates #59 / #60 (Session 70 filings) stay at 1st instance. 3-instance promotion discipline preserved. No code, tests, plans, CI, wiki, BACKLOG, ROADMAP, or `docs/` files other than `SESSION_RUNNER.md` touched. Wiki publish hook does not fire (no `docs/wiki/claims-model-starter/*.md` change).
- **Next:** BACKLOG remains **empty**. Operator direction at Session 72 start. Likely candidates: (a) version-plumbing follow-up (align `pyproject.toml` / `__version__` / git tag with README's `0.01`); (b) wiki updates for Phase 3/4 data-source-inventory (documentation debt: `Schema-Reference.md`, `Data-Guide.md`, `Worked-Examples.md`, `Pipeline-Overview.md`, `USAGE.md`, `architecture-plan.md`); (c) `--curated-inventory` CLI follow-up; (d) further methodology housekeeping when Candidate #55/#57/#58/#59/#60 reach promotion thresholds; (e) new off-BACKLOG work item.

### 2026-05-12 — Data source inventory Phase 4: intake coupling (Session 70)

Implements Phase 4 OPTIONAL of `docs/planning/data-source-inventory-contract-plan.md` (the sole remaining `[ ]` item in BACKLOG). Adds the `intake_qa_pairs_to_inventory` converter to `src/model_project_constructor/orchestrator/adapters.py`: a case-insensitive substring scan of `IntakeReport.qa_pairs` against the canonical P&C claims systems probed by SYSTEM_INTERVIEWER (Guidewire ClaimCenter, Duck Creek, policy admin, billing/collections, subrogation, fraud/SIU, CRM, EDW/data lake) emits one `DataSourceEntry` per system mentioned with `producer_type="interview"` provenance per plan §5.3. Wires `--inventory-from-intake` CLI flag in `scripts/run_pipeline.py` → `PipelineConfig.inventory_from_intake` → `run_pipeline` to invoke the converter and pass the result through `intake_report_to_data_request(..., data_source_inventory=inv)`. Default off preserves pre-Phase-4 behavior for every existing caller. **Plan-vs-code drift caught at Phase 0 (Learning #11):** the plan assumes `IntakeReport.qa_pairs` exists; in fact `qa_pairs` lived only in `IntakeState` (the graph TypedDict). Per operator's pre-claim alignment, scope expanded to add a `qa_pairs: list[QAPair] = Field(default_factory=list)` field to `IntakeReport` (backward-compatible empty default) plus the `QAPair` Pydantic model in `schemas/v1/intake.py`. **Plan §9.3 merge-with-curated logic stripped** because `--curated-inventory` CLI flag does not exist (Phase 3/Session 60 did not add it); filed as follow-up if/when curated inventory adoption demands it.

- **Added:**
  - `src/model_project_constructor/schemas/v1/intake.py` — `class QAPair(StrictBase)` (`question: str`, `answer: str`); `IntakeReport.qa_pairs: list[QAPair] = Field(default_factory=list)`.
  - `src/model_project_constructor/schemas/v1/__init__.py` — re-export `QAPair`.
  - `src/model_project_constructor/orchestrator/adapters.py` — `_CANONICAL_PC_SYSTEMS` constant (9 canonical names + aliases, specific-before-generic ordering); `_INVENTORY_PRODUCER_ID = "intake-interview"`; `intake_qa_pairs_to_inventory(intake) -> DataSourceInventory` function; expanded `__all__`.
  - `src/model_project_constructor/orchestrator/pipeline.py` — `PipelineConfig.inventory_from_intake: bool = False`; converter invocation in the adapter stage when the flag is set.
  - `scripts/run_pipeline.py` — `--inventory-from-intake` argparse flag; threading through `PipelineConfig`.
  - `tests/orchestrator/test_adapters.py` — `TestIntakeQAPairsToInventory` (9 new tests: no-mentions, empty-qa-pairs, single-system, multi-system, case-insensitive, repeated-mention concatenation, producer-id consistency with `_producer_ids_resolve` validator, request_context provenance, model_dump round-trip, specific-alias-wins-over-generic). `_make_intake` factory gains an optional `qa_pairs` kwarg.
  - `tests/orchestrator/test_pipeline.py` — `test_inventory_from_intake_default_off_preserves_behavior` + `test_inventory_from_intake_flag_populates_request` smoke tests against the wired pipeline.
- **Changed:**
  - `src/model_project_constructor/agents/intake/nodes.py` — `build_intake_report` now passes `qa_pairs=[QAPair(question=qa["question"], answer=qa["answer"]) for qa in state.get("qa_pairs") or []]` so the published `IntakeReport` carries the interview transcript. Import of `QAPair` from `schemas.v1.intake` added.
- **Verified:**
  - `uv run pytest -q` → **588/588 passing** (+12 new tests vs Session 69 baseline of 576) @ **97.32% coverage** (+0.04pp).
  - `uv run ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (one auto-fix consolidated the new aliased-import in `nodes.py` after ruff's first pass flagged un-sorted imports; second pass clean).
  - `uv run mypy src/` → `Success: no issues found in 48 source files`; `uv run mypy packages/data-agent/` → `Success: no issues found in 13 source files` (61 total, unchanged from Session 69).
  - Existing fixtures (`tests/fixtures/{subrogation,tier1,tier2}_intake.json`) carry no `qa_pairs`; the `default_factory=list` keeps all 576 pre-existing tests green without fixture edits.
- **Unchanged intentionally:**
  - `--curated-inventory` CLI flag NOT added — out of scope (Phase 3 didn't add it; this session honors the BACKLOG entry's narrow framing). Plan §9.3 merge-with-curated logic remains a documented possibility but is unimplementable until the producer side exists.
  - `_CANONICAL_PC_SYSTEMS` constant is local to `adapters.py` rather than refactored out of SYSTEM_INTERVIEWER prose in `agents/intake/anthropic_client.py:47-53`. Operator declined Option C (shared constant) at pre-claim alignment in favor of Option A (Recommended). The list lives in two places now (prose + converter constant); divergence risk is real but small — the prose is the LLM's prompt, the constant is the matcher; they need not be identical.
  - Wiki not touched (`docs/wiki/claims-model-starter/*.md` unchanged). `publish_wiki.sh` hook does not fire.
- **Next:** BACKLOG.md is now **empty** (the Phase 4 OPTIONAL line is removed per the v2.1 three-file split + Learning #26 delete-not-flip). Operator direction at Session 71 start. Likely candidates: (a) version-plumbing follow-up (align `pyproject.toml` / `__version__` / git tag with README's `0.01`); (b) document data-source-inventory usage in the wiki (`Data-Guide.md`, `Pipeline-Overview.md`, `Worked-Examples.md` — Phase 3/4 doc updates were never landed); (c) `--curated-inventory` CLI follow-up; (d) methodology housekeeping (Candidate Learnings #55/#56/#57/#58 promotion when subsequent instances land); (e) a new off-BACKLOG work item.

### 2026-05-12 — Promote Candidate Learning #52 to permanent row #34 (Session 69)

Methodology housekeeping micro-deliverable not from BACKLOG, operator-directed. Promotes Candidate Learning #52 (grep-before-insert for non-stub structured-file edits) to permanent row #34 in `SESSION_RUNNER.md`'s Learnings table. Session 68's Phase 3C drafted the promotion phrasing verbatim after Candidate #52 reached its 4th validated instance (filed Session 65 1st, applied Sessions 66/67 2nd/3rd promotion-triggering, validated Session 68 4th — archive-heading 9-match collision recovered via multi-line anchor per the recipe). Per operator's pre-claim alignment (AskUserQuestion), this session promotes ONLY #52; folds a one-line "See also" note pointing at Candidate Learning #56 (archive-heading pre-grep specialization, still 1st instance — needs 2 more for promotion) into row #34's body for forward-reference; does NOT promote Candidate #55 (archive placement convention, 2nd instance) or Candidate #56. 3-instance candidate-promotion discipline preserved. Row #34 cites Sessions 65/66/67/68 as the four instances. The row's body is the Session 68 draft verbatim + the See-also paragraph; the Source column documents promotion provenance; the When-to-Apply column generalizes from archive-edits to any multi-line-matching structured-file Edit.

- **Added:** `SESSION_RUNNER.md` — new row #34 in the Learnings table (3-column `| N | Learning | Source | When to Apply |`), inserted between row #33 and the `---` separator that closes the table. Net +1 row; +1244 bytes. Phrasing matches Session 68's Phase 3C draft verbatim, with a one-paragraph See-also note appended pointing at Candidate Learning #56 as a specialization-in-flight.
- **Verified:** `uv run pytest -q` → **576/576 passing** @ **97.28% coverage** (unchanged from Session 68 baseline; no `src/`, `packages/`, or `tests/` files touched). `uv run ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `uv run mypy src/` → `Success: no issues found in 48 source files`; `uv run mypy packages/data-agent/src` → `Success: no issues found in 13 source files` (61 total, unchanged). Row presence: `grep -c "^| 34 |" SESSION_RUNNER.md` → 1. Row #33 still precedes row #34 immediately (consecutive lines in the table). `---` separator and `## Launch Prompt Templates` heading still follow the table.
- **Unchanged intentionally:** Candidate Learning #55 stays a candidate (2nd instance; needs 1 more); Candidate Learning #56 stays a candidate (1st instance; needs 2 more). 3-instance promotion discipline preserved. No code, tests, plans, CI, wiki, or `docs/` files other than `SESSION_RUNNER.md` touched. BACKLOG unchanged (this work was not on BACKLOG; nothing to remove). Wiki publish hook does not fire (no `docs/wiki/claims-model-starter/*.md` change). No new memory entries (no durable preference, role, or reference surface emerged from this session).
- **Next:** BACKLOG remains at **1 `[ ]` item** — Data source inventory Phase 4 OPTIONAL (now 7th+ consecutive skip if Session 70 picks anything else). Operator may direct: (a) Phase 4 OPTIONAL; (b) version-plumbing follow-up to align `pyproject.toml` / `__version__` / git tags with README's `0.01`; (c) a new off-BACKLOG work item; or (d) more methodology housekeeping (promote Candidate #55 when its 3rd instance lands, etc.).

### 2026-05-12 — README versioning marker (Session 68)

Operator-directed micro-deliverable not from BACKLOG: add a `Version: 0.01` line to `README.md` directly under the title. Single-line characterization-metadata addition; the existing 213-line README is comprehensive and otherwise untouched (operator confirmed at pre-claim alignment). No source, schema, test, CI, plan, or methodology changes — README is the only modified file. Marks the repository's first version label after the 6-phase implementation (`0.01` chosen by operator; intentionally pre-`0.1` to signal "all phases complete, pre-pilot, no public release"). No version-file (e.g. `__version__`), tag, or release artifact created — this entry pins the marker in the README only, per operator's explicit "add a Version number of 0.01" scope.

- **Added:** `README.md` — new `**Version:** 0.01` line on line 3, between the `# Model Project Constructor` title and the description paragraph. Two-line insertion (the version line + a blank line).
- **Verified:** `uv run pytest -q` → **576/576 passing** @ **97.28% coverage** (unchanged from Session 67 baseline; no `src/`, `packages/`, or `tests/` files touched). `uv run ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `uv run mypy` → `Success: no issues found in 61 source files` (unchanged; 48 + 13 packages). README presence: `grep -c "Version:" README.md` → ≥ 1.
- **Unchanged intentionally:** no `__version__` constant, no `pyproject.toml` `[project].version` bump, no git tag, no GitHub release. Operator's scope was strictly "add a Version number to the README"; broader version-plumbing is a separate decision deferred to a future session. No code, no tests, no plans, no methodology, no CI, no wiki, no `docs/` files other than `README.md` touched. Wiki publish hook does not fire (no `docs/wiki/claims-model-starter/*.md` change). BACKLOG unchanged (this work was not on BACKLOG; nothing to remove).
- **Next:** BACKLOG remains at **1 `[ ]` item** — Data source inventory Phase 4 OPTIONAL (now 6th+ consecutive skip). Session 69 picks per operator direction. If broader version-plumbing is wanted (`__version__`, `pyproject.toml`, git tag), that's a follow-up.

### 2026-04-20 — Tutorial renderer migration — Phase 2: CI publish to GitHub Pages (Session 67)

Executes Phase 2 of `docs/planning/tutorial-renderer-migration-plan.md` (filed Session 65; Phase 1 Session 66). Ships the GitHub Actions workflow that builds the MkDocs site and deploys it to `gh-pages` via `mkdocs gh-deploy`, closing BACKLOG item 1 (tutorial renderer migration). The workflow triggers on pushes to `master` that touch `docs/tutorial.md`, `mkdocs.yml`, `pyproject.toml`, or the workflow file itself, plus manual `workflow_dispatch` so the operator can re-deploy without a code change (plan §9.5's "manual dispatch" verification path requires the trigger to be wired — minor additive deviation from plan §9's YAML). `astral-sh/setup-uv@v4` matches the project's existing CI pattern (`.github/workflows/ci.yml:19,30,41,59`) rather than plan §9's `@v3` suggestion (applied Learning #11: trust the code, not the plan). `permissions: contents: write` is the canonical setting needed for `gh-deploy` to push to `gh-pages` using the default `GITHUB_TOKEN`. `--force --clean` is the standard `gh-deploy` invocation: `gh-pages` branch history is owned by the tool, not preserved commit-by-commit (plan §11 risk #4). CI-side `uv sync --extra docs` is correct for CI's fresh-container semantics and is explicitly preserved — Session 66's `--all-extras` erratum applies only to the local-dev path (plan §8.4 already reflects this, §9 was already correct). Plan §9's stale verification command `grep -q 'md-clipboard'` fixed in-place (Session 67 erratum) to `grep -q 'content.code.copy'`: `md-clipboard` is the class of the clipboard-button DOM that Material injects at runtime via JavaScript — the static HTML `curl` retrieves does not contain it. The static-visible marker for copy-button wiring is the `content.code.copy` feature flag in the `__config` script tag. GitHub Pages enablement (plan §9.2 — `Settings → Pages → Source = gh-pages branch, /`) is a one-time operator web-UI action that cannot be scripted; operator confirmed at pre-claim alignment that Pages is already enabled on `rmsharp/claims-model-starter`. README.md gains a one-line "Tutorial (published):" pointer to `https://rmsharp.github.io/claims-model-starter/` positioned directly after the existing repo-naming sentence and before the `## Status` heading (operator-approved scope; `docs/tutorial.md` prose stays untouched per plan §13 non-goals).

- **Added:** `.github/workflows/publish-tutorial.yml` — new workflow, 27 lines. Triggers: `push` on `master` with `paths:` filter (4 entries) + `workflow_dispatch` (manual). `permissions: contents: write`. Single `publish` job named "Build + gh-deploy": `actions/checkout@v4` → `astral-sh/setup-uv@v4` with `version: "latest"` → `uv sync --extra docs` → `uv run mkdocs gh-deploy --force --clean`. Matches `ci.yml` style (named jobs, uv-action version pinned via `with:`).
- **Added:** `README.md` — new one-line pointer "Tutorial (published): https://rmsharp.github.io/claims-model-starter/" inserted after line 5 (repo-naming sentence) and before `## Status`. Highest-visibility non-title location; discoverable without scroll.
- **Changed:** `docs/planning/tutorial-renderer-migration-plan.md` §9 verification commands — Session 67 erratum. `curl -sf <URL> | grep -q 'md-clipboard'` replaced with `curl -sf <URL> | grep -q 'content.code.copy'` + a paragraph-length inline comment explaining the JS-injection root cause. Erratum note structurally parallels Session 66's §8.4 `uv sync --all-extras` erratum — both pin an empirically-validated correction directly adjacent to the incantation so Phase-2 (or future-migration) readers cannot miss it.
- **Removed:** one `BACKLOG.md` line per Learning #26 (delete, do NOT flip to `[x]`) — "Tutorial renderer: migrate to MkDocs (or equivalent) with copy-button plugin" (plan filed Session 65; Phase 1 Session 66; Phase 2 this session). BACKLOG net: 2 `[ ]` → 1 `[ ]`, 0 `[x]` → 0 `[x]`. Learning #26 Session #36 of application.
- **Verified:** `uv run pytest -q` → **576/576 passing** @ **97.28% coverage** (unchanged from Session 66 baseline; no `src/`, `packages/`, or `tests/` files touched). `uv run ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `uv run mypy` → `Success: no issues found in 61 source files` (unchanged; 48 + 13 packages). Workflow YAML parsed by `yq`/GitHub-Actions-semantics via inspection: `name`, `on.push.branches`, `on.push.paths`, `on.workflow_dispatch`, `permissions.contents`, `jobs.publish.runs-on`, `jobs.publish.steps` all well-formed. Plan §9 erratum in-place verified: `grep -c "md-clipboard" docs/planning/tutorial-renderer-migration-plan.md` → 0; `grep -c "content.code.copy" docs/planning/tutorial-renderer-migration-plan.md` → ≥1. BACKLOG removal verified: `grep -c "migrate to MkDocs" BACKLOG.md` → 0 (plan §9 completion criterion). README pointer verified: `grep -c "Tutorial (published):" README.md` → 1.
- **Unchanged intentionally:** no code in `src/`, `packages/`, or `tests/` touched — this deliverable is CI workflow + docs pointer + plan erratum + BACKLOG removal only. `docs/tutorial.md` prose untouched (plan §13 non-goal; operator scoped the "published at" pointer to README only). `mkdocs.yml` untouched — Phase 1's config is correct for GitHub Pages publication (`site_url` pre-configured). `pyproject.toml` untouched — Phase 1's `docs` optional-dep group is what CI installs. Wiki files (`docs/wiki/claims-model-starter/*.md`) untouched — Session 64's `scripts/publish_wiki.sh` close-out hook therefore does not fire. Methodology docs (`docs/methodology/*`, `SESSION_RUNNER.md`, `SAFEGUARDS.md`, `CLAUDE.md`, `PROJECT_CONVENTIONS.md`) untouched. No Material theme customization (plan §13 non-goal). CI-side `uv sync --extra docs` intentionally preserved in the workflow — Session 66's `--all-extras` erratum applies to local dev only (fresh-container CI has no pre-installed extras to displace).
- **Post-push live-site verification (fix-up):** Workflow run succeeded on first push (`gh run list --workflow publish-tutorial.yml --limit 1 --json conclusion` → `"success"`); `gh-pages` branch created at `6f9bd45`. GitHub Pages deployment blocked at HTTP 404 initially — `Settings → Pages` source was set to `master / /` from the operator's initial enablement click (before `gh-pages` existed); operator updated source to `gh-pages / /` and saved. Subsequent Pages build succeeded (commit `6f9bd454`, `status: "built"`). Live-site verification surfaced a second issue: the published URL is `https://rmsharp.github.io/claims-model-starter/tutorial/` (MkDocs single-page layout with `use_directory_urls: true`), NOT `https://rmsharp.github.io/claims-model-starter/` — plan §10 open consideration #1 forecast this exactly (plan §7 does not rename `tutorial.md` → `index.md`, so the MkDocs output path is `site/tutorial/index.html`). `README.md` pointer updated from `/` to `/tutorial/` (the working URL), and plan §9's verification command URL updated similarly for future readers. Root `/` returns expected 404 (no `index.md` in nav); `/tutorial/` returns HTTP 200 with `content.code.copy` marker present (`grep -c` → 1) and Material `bundle.79ae519e.min.js` loaded (same hash as Session 66's local build — deploy parity confirmed). Fix-up committed immediately after the first push; not an amend (SAFEGUARDS — never force-push master). A future session could close the `/` → `/tutorial/` UX gap with a trivial `index.md` redirect or `mkdocs-redirects` plugin, but that's BACKLOG territory, not Phase 2 scope (plan §13 non-goals forbid plugins). Phase 2 COMPLETE with live-site verification evidence.
- **Root redirect (second fix-up, operator-approved):** Shipped `docs/index.md` (5 lines — heading + `<meta http-equiv="refresh" content="0; url=tutorial/">` + fallback link) so `/` now returns HTTP 200 with an instantaneous client-side redirect to `/tutorial/`. `mkdocs.yml` gains `not_in_nav: | /index.md` (MkDocs 1.6+ directive — keeps the redirect page built but out of the nav bar so Material's nav still shows only "Tutorial", avoiding the back-button loop that would arise if "Home" were clickable). Workflow `paths:` filter broadened from `docs/tutorial.md` to `docs/*.md` so top-level doc additions (`tutorial.md`, `index.md`, or future peers) auto-trigger the publish workflow; subdirs under `docs/` remain untriggered (plan §5 non-goals still forbid other doc migrations). Local `mkdocs build --strict` exit 0 with one harmless INFO message ("unrecognized relative link 'tutorial/', it was left as is" — MkDocs couldn't resolve the directory URL to a doc-file, left it intact in the output, which is exactly correct; INFO not WARN, so `--strict` passes). No plugins added (plan §13 non-goal honored — meta-refresh is plain HTML, not a plugin).
- **Next:** BACKLOG has **1 `[ ]` item** after the removal: (1) Data source inventory Phase 4 OPTIONAL — still deferred unless pilot demand surfaces (now 6th+ consecutive skip). Session 68 picks per operator direction.

### 2026-04-20 — Tutorial renderer migration — Phase 1: local MkDocs setup (Session 66)

Executes Phase 1 of `docs/planning/tutorial-renderer-migration-plan.md` (filed Session 65). Replaces the pandoc → standalone-HTML tutorial renderer (`scripts/render_tutorial.sh` → `docs/tutorial.html`) with an MkDocs + Material site generator at `mkdocs.yml`. Per plan §7, the config uses Material's `content.code.copy` feature (JS-injected copy button on every code block — the UX gap that motivated the migration), an explicit-directory `exclude_docs:` pattern (MkDocs ≥ 1.5 gitignore-format) that scopes the site to `docs/tutorial.md` and excludes `methodology/`, `planning/`, `architecture-history/`, `style/`, `wiki/` (a whitelist alternative would silently hide future additions — maintainable-failure-mode choice per plan §7), no plugins beyond Material's built-in search, no custom CSS, and `site_url` pre-configured for GitHub Pages at `https://rmsharp.github.io/claims-model-starter/` (Phase 2 consumes this). Dependencies: new `docs` optional-dependency group in `pyproject.toml` with floors `mkdocs>=1.5` (for `exclude_docs:`) and `mkdocs-material>=9.0` (for `content.code.copy`). `uv sync` drift caught mid-execution (**Learning #13 extension, new instance**): the plan's §8.4 said `uv sync --extra docs` but in practice `--extra <name>` on `uv sync` is *replacing*, not *additive* — it removed the previously-installed `agents`, `ui`, and `dev` extras. Re-ran `uv sync --all-extras` to restore the full dev environment. Plan's Phase 1 verification recipe should be corrected to `uv sync --all-extras` or `uv sync --extra docs --extra agents --extra ui --extra dev` — noted as a plan-drift erratum for Session 67's Phase 2 handoff (and for any future addition of another optional-dependency group). `mkdocs build --strict` completed cleanly (no `--strict` warnings; Material's informational banner about the future MkDocs 2.0 is not a MkDocs-framework warning and does not trip `--strict`). Copy-button wiring confirmed via static inspection: `site/tutorial/index.html` loads Material's `bundle.79ae519e.min.js`; `__config` script tag includes `"features": ["content.code.copy"]` and the clipboard.copy/clipboard.copied translations; the button DOM itself is JS-injected at runtime (Material's documented pattern — static grep for `md-clipboard` returns 0, which is expected, not a failure). A full browser visual check is deferred to operator review. BACKLOG reordered per operator direction: Tutorial renderer MkDocs migration (plan + Phase 1 in-flight) moved to position #1; Data source inventory Phase 4 OPTIONAL moved to end.

- **Added:** `mkdocs.yml` at repo root (25 lines) — matches plan §7 verbatim. Contains `site_name`, `site_description`, `site_url`, `repo_url`, `repo_name`, `docs_dir: docs`, `site_dir: site`, `exclude_docs:` with 5 explicit directories, single-page `nav:`, and Material theme with `content.code.copy` feature.
- **Added:** `pyproject.toml` `[project.optional-dependencies].docs` — new group with `mkdocs>=1.5` + `mkdocs-material>=9.0`. Version floors match plan §8.2 rationale.
- **Removed:** `scripts/render_tutorial.sh` — 100-line pandoc wrapper (shipped Session 54; per-block-copy UX gap documented Session 55). Supersession is complete: MkDocs + Material provides the copy-button feature that motivated Session 55's per-command splits, and no other caller or doc references the script (plan §6 grep confirmed before deletion).
- **Removed:** `docs/tutorial.html` — stale render-time artifact from a past `render_tutorial.sh` invocation. Was `.gitignore`d under the old renderer; surfaced as untracked when that ignore line was removed. Not in git history (the `.gitignore` entry covered its entire lifetime); deleted from the working tree at close-out to keep `git status` clean.
- **Changed:** `.gitignore` — line swap: `docs/tutorial.html` (old renderer output) → `/site/` (MkDocs build output). Leading slash anchors the pattern to repo root so any future `site/` subdirectory elsewhere in the tree is not inadvertently caught.
- **Changed:** `BACKLOG.md` — item order swapped per operator direction. Position #1 is now "Tutorial renderer: migrate to MkDocs" (plan filed Session 65, Phase 1 in-flight); position #2 is "Data source inventory — Phase 4 OPTIONAL" (deferred 5+ sessions). Net BACKLOG count unchanged: 2 `[ ]`, 0 `[x]`. The MkDocs line also gains a provenance annotation ("Plan filed: `docs/planning/tutorial-renderer-migration-plan.md` (Session 65). Phase 1 in progress Session 66; Phase 2 (CI publish to GitHub Pages) is a separate future session.") so the plan pointer survives Phase 1's close-out regardless of when Phase 2 lands.
- **Verified:** `uv run mkdocs --version` → `mkdocs, version 1.6.1` (above `>=1.5` floor). `uv run mkdocs build --strict` → exit 0; "Documentation built in 0.17 seconds"; zero MkDocs warnings. `ls site/` → `404.html  assets  search  sitemap.xml  sitemap.xml.gz  tutorial  tutorial.html`. `ls site/tutorial/` → `index.html` (Material's directory-URL single-page layout). `grep -c "content.code.copy" mkdocs.yml` → 1 (plan §8 verification command). `grep -c "content.code.copy" site/tutorial/index.html` via the `__config` script tag → present. `ls site/assets/javascripts/` → `bundle.79ae519e.min.js` (Material's runtime). `git check-ignore site/` → `site/` (gitignored; build output not tracked). `git status --porcelain` after build + after removing `docs/tutorial.html` → clean for the build output, working-tree diff limited to the tracked-file changes in this session. `uv run pytest -q` → **576/576 passing** @ **97.28% coverage** (unchanged from Session 64/65 baseline). `uv run ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `uv run mypy` → `Success: no issues found in 61 source files` (unchanged; 48 + 13 packages). `ls scripts/render_tutorial.sh` → `No such file or directory` (deletion verified).
- **Unchanged intentionally:** No code in `src/`, `packages/`, or `tests/` touched — this deliverable is config + deletion only. No CI workflow change — Phase 2 adds `.github/workflows/publish-tutorial.yml`; Phase 1 builds locally only. `BACKLOG.md` item 2 (now #1) stays `[ ]` — the line is removed at **Phase 2** close per Learning #26 (Phase 1 is not completion of the BACKLOG item; it is completion of Phase 1 of the plan). `docs/tutorial.md` prose untouched (plan §5 explicit out-of-scope). Wiki files (`docs/wiki/claims-model-starter/*.md`) untouched — Session 64's `scripts/publish_wiki.sh` close-out hook therefore does not fire this session. Methodology docs (`docs/methodology/*`, `SESSION_RUNNER.md`, `SAFEGUARDS.md`, `CLAUDE.md`, `PROJECT_CONVENTIONS.md`) untouched. No Material theme customization (Learning #18 / plan §13 non-goal — defaults suffice). No plugins added. No MkDocs upper-version bound added (plan §11 risk #1 leaves the `<10` ceiling to a future session if Material 10 lands).
- **Next:** BACKLOG has **2 `[ ]` items** after the reorder. Numerical-first is now (1) Tutorial renderer MkDocs migration — **Phase 1 landed; Phase 2 ready** (CI publish to GitHub Pages via `.github/workflows/publish-tutorial.yml`, per plan §9); (2) Data source inventory Phase 4 OPTIONAL — still deferred. Session 67 picks per operator direction; if operator directs MkDocs Phase 2, Session 67 reads plan §9 + plan §11 risks 3/4/5 + the "uv sync drift erratum" in Session 66's close-out before executing. Open plan-drift correction: plan §8.4 `uv sync --extra docs` → should be `uv sync --all-extras` (or enumerate all groups) — fix in-place during Session 67's Phase 2 so the plan stays the authoritative reference for both phases.

### 2026-04-20 — Plan filed: tutorial renderer migration to MkDocs (Session 65)

Planning session against BACKLOG item 2 ("Tutorial renderer: migrate to MkDocs (or equivalent) with copy-button plugin"). Deliverable is the plan document itself; no code changes. Operator pre-claim decisions (captured in §3 of the plan): MkDocs (chosen over Docusaurus); retire `scripts/render_tutorial.sh` (rather than keep two renderers in parallel); CI publication to GitHub Pages eventually (so no rendered artifact lives in the repo); wiki migration deferred and kept separate (Session 64's `scripts/publish_wiki.sh` continues to handle the wiki). Plan is split into **two implementation sessions**: Phase 1 (local MkDocs setup — `mkdocs.yml`, new `docs` optional-dependency group, delete `render_tutorial.sh`, `.gitignore` swap `docs/tutorial.html` → `/site/`) and Phase 2 (GitHub Actions workflow that `mkdocs gh-deploy`s on push to `master`). Technical claims in the plan (Material's `content.code.copy` feature, Material ≥ 9.0.0, MkDocs `exclude_docs:` directive in 1.5+, `gh-deploy` → `gh-pages` branch) verified against live MkDocs + Material documentation at plan-write time; citations in plan §11. Grep inventory (§6) enumerates every file in the repo that mentions `render_tutorial.sh`, `tutorial.html`, `docs/tutorial.md`, `pandoc`, or `mkdocs`, classified into MODIFY/DELETE/NO-CHANGE buckets; historical-record files (`CHANGELOG.md`, `SESSION_NOTES.md`, `docs/wiki/claims-model-starter/Evolution.md`) are explicitly flagged NO-CHANGE per Learning #32. No existing `mkdocs.yml` or `docusaurus.config.js` anywhere in the repo (confirmed by find + ls). Plan scope explicitly excludes tutorial prose rewrites, wiki migration, migration of other `docs/` subdirectories, theme customization beyond Material defaults.

- **Added:** `docs/planning/tutorial-renderer-migration-plan.md` — new plan document with 14 sections (Goal, Problem, Operator decisions, Technical claims with verification URLs, Scope in/out, Evidence-based inventory, Proposed `mkdocs.yml`, Phase 1 work items + completion criteria + verification commands, Phase 2 work items + completion criteria + verification commands, Open considerations, Risks, Change summary by phase, Non-goals, References). Each phase is marked "one session" with explicit STOP boundary. Per-phase verification commands include grep assertions (e.g. `grep -c "content.code.copy" mkdocs.yml` → 1, `grep -c "migrate to MkDocs" BACKLOG.md` → 0 at Phase 2 close) and runtime checks (e.g. `uv run mkdocs build --strict` → exit 0, `curl -sf https://rmsharp.github.io/claims-model-starter/ | grep -q 'md-clipboard'` at Phase 2). Fourth plan in `docs/planning/` (alongside `data-source-inventory-contract-plan.md`, `evolution-page-plan.md`, `resume-from-checkpoint-plan.md`, `scope-b-plan.md`).
- **Verified:** `uv run pytest -q` → **576/576 passing** @ **97.28% coverage** (unchanged from Session 64; no `src/` or `packages/` or `tests/` files touched). `uv run ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged). `uv run mypy` → `Success: no issues found in 61 source files` (unchanged). Plan's technical claims verified against canonical docs at plan-write time (URLs in plan §11; fetched 2026-04-20). No simulated `mkdocs build` run this session — Phase 1 executes the first build.
- **Unchanged intentionally:** no code touched (`src/`, `packages/`, `tests/`, `scripts/`). No `mkdocs.yml` created — that is explicitly Phase 1's deliverable (Learning #18: planning and implementation in separate sessions). No `pyproject.toml` change — Phase 1 adds the `docs` optional-dependency group. `BACKLOG.md` item 2 stays `[ ]` — Phase 2 removes it (planning a migration is not the same as completing it; the line reflects work-still-open). No methodology files touched. No wiki files touched — the Session 64 close-out hook (`scripts/publish_wiki.sh`) therefore does not fire this session. `docs/tutorial.md` untouched — plan scope explicitly excludes prose changes.
- **Next:** BACKLOG still has **2 `[ ]` items**. Numerical order: (1) Data source inventory Phase 4 OPTIONAL — still deferred; (2) Tutorial renderer MkDocs migration — **plan filed, Phase 1 implementation ready for Session 66** if operator chooses that path. Alternate: Session 66 operator can continue skipping item 2 and pick item 1, or file new work; plan sits in `docs/planning/` until implementation starts.

### 2026-04-20 — Wiki publish mechanism — prevent repo-to-wiki drift (Session 64)

Closes BACKLOG item 3 (the follow-up Session 63 split off after its one-shot manual sync). Ships the drift-prevention infrastructure Session 63 identified as missing: a scripted publish step + a session close-out hook that fires when any wiki source file changes. Scope decision (operator-approved): **option (b) from Session 63's three-option menu — script + close-out hook**, not script-only (which failed in 40 sessions) and not CI-on-push (overkill for infrequent changes, would need a PAT secret to push to the separate wiki repo). The script `scripts/publish_wiki.sh` follows the house style established by `scripts/render_tutorial.sh` (bash strict mode, env-var configuration, dependency check, documented exit codes, instruction-rich error messages). Design: idempotent (rsync from `docs/wiki/claims-model-starter/` → `$WIKI_CLONE` (default `~/Development/claims-model-starter.wiki`) → `git add -A` → commit-if-changed → `git push origin master`), so invoking it unconditionally is safe — when source and clone are in parity it exits cleanly with "no changes to publish" in seconds. Multi-layer sanity: verifies the `WIKI_CLONE` target is an actual clone, that its `origin` URL contains `claims-model-starter.wiki` (protects against mis-configured WIKI_CLONE pointing at the wrong repo), that the branch is `master` (GitHub wiki repos only support this legacy branch name), and that the clone has no uncommitted changes via `git status --porcelain` (the authoritative, self-refreshing dirty-check — Session 64 caught that `diff-index --quiet HEAD --` can false-positive on stat-cache staleness). Wiki-clone-not-present is a hard failure with a printed `git clone` command; no auto-clone (one-way-door action on a second repo). Ambient git auth (SSH / credential helper / gh CLI) handles the push; no PAT hard-coded. Commit message on the wiki side cites the repo's source commit SHA for provenance (`docs: sync wiki from model_project_constructor@<sha>`). SESSION_RUNNER.md Phase 3E gains a conditional `Wiki sync check` sub-step naming the guard (`git diff --name-only <previous-HEAD>..HEAD -- docs/wiki/claims-model-starter/`) and pointing at the script; the script's idempotency means a session that touches no wiki files can run it without side effects, so the guard is more of a "when to care" signal than a strict precondition.

- **Added:** `scripts/publish_wiki.sh` — new executable shell script (~95 lines including header comment and blank lines; ~65 lines of actual logic). Matches `scripts/render_tutorial.sh` conventions: `#!/usr/bin/env bash`; `set -euo pipefail`; positional arg validation (rejects any arg — the script is argument-free); `WIKI_CLONE` env-var with default `$HOME/Development/claims-model-starter.wiki`; uses `git rev-parse --show-toplevel` to resolve the repo root so the script works from any working directory; dependency check for `git` + `rsync`; source-directory-presence check; wiki-clone-presence check with a multi-line `cat <<EOF` instruction message; `origin` URL sanity check (`grep -q 'claims-model-starter\.wiki'`); branch check (`master`); clean-state check via `git status --porcelain`; `rsync -a --delete --exclude='.git/'` to preserve the clone's own git metadata; `git add -A` followed by `git diff --cached --quiet` short-circuit for the idempotent no-op path; commit with a provenance-rich message citing the source commit SHA (`git rev-parse --short HEAD`); `git push origin master` with a push-failure path that preserves the local commit and prints the undo command. File is mode `755`.
- **Changed:** `SESSION_RUNNER.md` Phase 3E (Commit) — gains a new paragraph "Wiki sync check" (+3 lines) naming the guard command (`git diff --name-only <previous-HEAD>..HEAD -- docs/wiki/claims-model-starter/`) and pointing at `scripts/publish_wiki.sh`. Explicitly documents that the script is idempotent so unconditional invocation is safe; references the script's header comment for `WIKI_CLONE` setup and ambient-git-auth prerequisites.
- **Removed:** one `BACKLOG.md` line per Learning #26 (delete, do NOT flip to `[x]`) — "Wiki publish mechanism — prevent repo-to-wiki drift" (now shipped). BACKLOG net change: 3 `[ ]` → 2 `[ ]`, 0 `[x]` → 0 `[x]`. Learning #26 Session #35 of application.
- **Verified:** `uv run pytest -q` → **576/576 passing** @ **97.28% coverage** (unchanged from Session 63; no `src/` or `packages/` or `tests/` files touched). `uv run ruff check src/ tests/ packages/ scripts/` → `All checks passed!` (unchanged; ruff does not lint shell scripts but the scan path is CI-matching). `uv run mypy` → `Success: no issues found in 61 source files` (unchanged; 48 + 13 packages). Script happy-path: `scripts/publish_wiki.sh` invoked against the currently-in-parity wiki clone at `~/Development/claims-model-starter.wiki/` (Session 63 left it at `c13cfc4`, fully synced with `docs/wiki/claims-model-starter/`) — exit 0, `no changes to publish`. Script error-path: `WIKI_CLONE=/tmp/nonexistent_wiki scripts/publish_wiki.sh` → exit 1, prints the `git clone` instruction + the `WIKI_CLONE=/path/to/your/clone` override. Mid-implementation bug caught in verification and fixed in-session: the original dirty-clone check used `git diff-index --quiet HEAD --` which returned a false-positive on the first invocation due to stat-cache staleness (a known git quirk — `diff-index` compares cached mtime/size metadata and can flag mismatches where content is identical); replaced with `[ -n "$(git status --porcelain)" ]` which is authoritative AND refreshes the index as a side effect; re-ran the script → clean exit 0. The fix landed in one Edit, before committing, so the original broken version never left the working tree.
- **Unchanged intentionally:** no source code touched (`src/`, `packages/`). No test files touched — the shell script is verified by the two-invocation integration test (happy-path + error-path) during close-out; adding a pytest harness for a shell script that shells out to `git` and `rsync` would require shelling-out machinery, a fixture git repo, and a fixture wiki clone, all for a 65-line idempotent one-shot whose correctness is testable in three seconds via direct invocation. No CI workflow added (`.github/workflows/`) — option (c) was rejected during scope alignment because (1) wiki pushes are infrequent enough that per-push CI is overkill, (2) pushing to a separate GitHub wiki repo from CI requires a PAT secret (the default `GITHUB_TOKEN` scopes don't include it), and (3) the session close-out hook catches drift the same session it's introduced, which is faster feedback than a CI-on-push workflow that could still miss drift if the session doesn't push at all (e.g. WIP commits). No methodology documents edited (`docs/methodology/README.md`, `docs/methodology/PROJECT_CONVENTIONS.md`, etc.) — Session 17 Learning #17 preserved. `SAFEGUARDS.md` untouched. `CLAUDE.md` untouched. Wiki source files (`docs/wiki/claims-model-starter/*.md`) untouched — this session ships infrastructure, not content. The wiki clone at `~/Development/claims-model-starter.wiki/` still at `c13cfc4` (Session 63's sync); the new script ran against it in no-op mode so it was not touched. `--dry-run` flag intentionally not implemented: the script is already idempotent, an operator who wants to preview changes can inspect `git diff` on the repo source before running, and splitting the invocation into sync-and-preview + push-only introduces a window where the wiki clone is in a "committed but not pushed" state that is easy to forget about.
- **Next:** BACKLOG has **2 `[ ]` items** after the removal. Numerical order: (1) Data source inventory Phase 4 OPTIONAL — still deferred unless pilot demand surfaces; (2) Tutorial renderer: migrate to MkDocs. Session 65 picks per operator direction. Optional follow-ups not filed as BACKLOG items unless operator requests: (a) add `scripts/publish_wiki.sh` to `shellcheck` in a pre-commit hook (no pre-commit config currently exists — would be a net-new machinery); (b) consider renaming `scripts/` → a `tools/` or `bin/` directory if more ad-hoc shell utilities appear (currently two files — render_tutorial + publish_wiki — too few for restructuring); (c) document the script in `README.md` alongside `scripts/render_tutorial.sh` (the README doesn't currently list either; consistent omission is acceptable).

### 2026-04-20 — Wiki sync: close ~40-session drift between repo source and live GitHub Wiki (Session 63)

Closes BACKLOG item 3 (operator-filed 2026-04-20 via ghost-commit `1433c74`). **Scope surprise at Phase 0:** the BACKLOG entry named only `Content-Recommendations` as stale, but a full `diff -rq` between `docs/wiki/claims-model-starter/` (repo source-of-truth) and the live wiki clone at `~/Development/claims-model-starter.wiki/` surfaced **15 pages differing plus 1 page missing (`Evolution.md`)** — the live wiki had not been updated since Session 20B (commit `2fa274a`), ~40 sessions behind. **Root cause:** no publish mechanism exists. No CI workflow, no `scripts/publish_wiki.sh`, no session close-out hook — the wiki is a separate git repo (`github.com/rmsharp/claims-model-starter.wiki.git`) and the only way content ever landed on it was by committing + pushing to that repo directly. Session 20A and 20B each did one such push; no session since has. Operator approved full-sync scope this session + filing a new BACKLOG item for a scripted publish mechanism (option 2 + option 3 deferred per pre-claim alignment). **Executed:** 16 files copied from repo source → wiki clone; `diff -rq` verified zero differences post-copy; single wiki-repo commit `c13cfc4 docs: sync wiki source-of-truth from model_project_constructor (Sessions 20B..62)` pushed to `origin/master` (wiki repo). Live GitHub Wiki at `https://github.com/rmsharp/claims-model-starter/wiki/` now in parity with repo source-of-truth. Notable user-visible corrections that were backlogged and are now live: Session 38's Content-Recommendations restructure (139 → 93 lines, evolution-record → current-state description); Session 32's Schema-Reference `host_url` example update to `gitlab.com`; Sessions 20C..62's accumulated Changelog additions grouped by implementation phase; Evolution page (decision-arc narrative, added post-20B per PROJECT_CONVENTIONS §2) is now present.

- **Changed (external, wiki repo):** `github.com/rmsharp/claims-model-starter.wiki.git` at commit `c13cfc4`, `2fa274a..c13cfc4`, +405 −91 across 16 files. 15 pages refreshed (`_Sidebar, Agent-Reference, Architecture-Decisions, Changelog, Content-Recommendations, Contributing, Data-Guide, Getting-Started, Glossary, Home, Intake-Interview-Design, Monitoring-and-Operations, Pipeline-Overview, Schema-Reference, Worked-Examples`); 1 page added (`Evolution.md`). 6 pages were already in parity and required no change (`Development-Workflow, Extending-the-Pipeline, Generated-Project-Structure, Governance-Framework, Security-Considerations, Software-Bill-of-Materials`).
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — the "Content-Recommendations page shows history, not current state" item (scope-narrow as filed; actual gap was 16 files wide).
- **Added:** one new BACKLOG.md line splitting off the prevent-recurrence follow-up — "Wiki publish mechanism — prevent repo-to-wiki drift" — capturing three scope options (shell script only; script + session close-out hook; CI workflow on push to master) with the choice deferred to the implementing session. Net BACKLOG change: 3 `[ ]` → 3 `[ ]`, 0 `[x]` → 0 `[x]` (one removed + one added — line count preserved, semantic content changed: item 3 was a one-shot content fix, now is an infrastructure follow-up). Learning #26 Session #34 of application; Candidate Learning #48 (split bundled BACKLOG lines when one clause ships) 2nd instance.
- **Verified:** `uv run pytest -q` → **576/576 passing** @ **97.28% coverage** (unchanged from Session 62; no `src/` or `packages/` or `tests/` files touched this session). `uv run ruff check` → `All checks passed!` (unchanged). `uv run mypy src/` → `Success: no issues found in 48 source files` (unchanged). `uv run mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (unchanged). External sync verification: `diff -rq docs/wiki/claims-model-starter/ ~/Development/claims-model-starter.wiki/` (excluding `.git`) returned zero differences post-copy. Wiki push verification: `git push origin master` in the wiki clone returned `2fa274a..c13cfc4 master -> master` without rejection.
- **Unchanged intentionally:** No source code modified — this is a documentation-sync deliverable only. No test files, no agent prompts, no schemas, no orchestrator code. No `CLAUDE.md` / `SESSION_RUNNER.md` / `SAFEGUARDS.md` / methodology docs touched. The repo's `docs/wiki/claims-model-starter/*.md` files were the source; they were NOT edited in this session (no drift was found between repo source and expected current state — the repo source IS the current state; the only drift was on the wiki side). `docs/methodology/PROJECT_CONVENTIONS.md` §1 is honoured ("The wiki is the outward-facing documentation"); no convention changes needed. No publish script or CI workflow shipped this session — that is the scope of the new BACKLOG follow-up item; shipping it bundled would violate the operator's durable directive ("one BACKLOG item per session"). The wiki clone at `~/Development/claims-model-starter.wiki/` is a session-local tool, not a committed artifact of this repo; its location outside `model_project_constructor/` is deliberate (it is a separate GitHub wiki repo).
- **Next:** BACKLOG now has 3 `[ ]` items after the remove-and-add: (1) Data source inventory Phase 4 OPTIONAL — still deferred unless pilot demand surfaces; (2) Tutorial renderer: migrate to MkDocs; (3) Wiki publish mechanism — prevent repo-to-wiki drift (NEW this session, replaces the narrow one-shot filing). Session 64 picks per operator direction; the operator's durable directive ("work on backlog items in numerical order; 1 per session") makes numerical-first (1) Phase 4 OPTIONAL (likely still deferred) → (2) MkDocs → (3) wiki publish mechanism.

### 2026-04-20 — Statistical glossary — agent system prompt injection (Session 62)

Ships the Session 61 follow-up: wires a curated subset of `docs/style/statistical_terms.md` into the LLM system prompts of the agents that emit prose. **Scope finding (Phase 0, Learning #47 2nd instance):** the BACKLOG entry named "intake / data / website" (3 agents), but the website agent is a template renderer that talks to GitHub/GitLab via `RepoClient` (no `anthropic_client.py`, no `SYSTEM_*` constants). Scope reduces to intake + data (2 agents). Design (option b from Session 61 handoff — curated subsets inlined as module-level constants; walled-garden decoupling preserved, no cross-package import): intake gets ~7 terms covering the highest-impact business-prose conflations (probability/likelihood, statistical vs practical significance, statistical vs algorithmic bias, statistical vs insurance-peril risk, accuracy vs precision, overfitting, class imbalance) injected into `SYSTEM_INTERVIEWER` only (not `SYSTEM_GOVERNANCE`, which emits regulatory labels rather than statistical prose); data agent gets ~6 terms (class imbalance, data leakage, calibration vs discrimination, statistical vs algorithmic bias, P&C accounting triple frequency/severity/pure premium) injected into `summarize()` and `generate_datasheet()` system strings only (not `generate_primary_queries`, `generate_quality_checks`, or `rank_candidate_tables` — those surfaces produce SQL or 0.0-1.0 relevance scores where statistical-terminology conflations are rare). Each note ends with a pointer to `docs/style/statistical_terms.md` so future amendments propagate via the style-guide tree, not by re-editing prompts. Tests pin the injection points with both positive assertions (the note IS present in the prose surfaces) and negative assertions (the note is NOT present in the SQL / governance surfaces) so future prompt-edits can't silently drop or over-spread the note; probes use key term tokens rather than the full note content so glossary amendments don't break tests.

- **Changed:** `src/model_project_constructor/agents/intake/anthropic_client.py` — existing `SYSTEM_INTERVIEWER` string split into `_INTERVIEWER_BASE` + `_STATISTICAL_TERMS_NOTE` (the note is a new private module-level constant, ~40 lines of bullet-form glossary content with leading `\n\n` separator); `SYSTEM_INTERVIEWER` is now `_INTERVIEWER_BASE + _STATISTICAL_TERMS_NOTE`. Public export name (`SYSTEM_INTERVIEWER`) unchanged so existing imports (including the data-source-discovery probe test shipped in Session 56) continue to work. `SYSTEM_GOVERNANCE` untouched — deliberate negative (pinned by test).
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/anthropic_client.py` — new private module-level constant `_STATISTICAL_TERMS_NOTE` (~30 lines of bullet-form glossary content with leading `\n\n` separator). `summarize()` and `generate_datasheet()` system strings now append the constant; `generate_primary_queries()`, `generate_quality_checks()`, and `rank_candidate_tables()` system strings unchanged — deliberate negatives (each pinned by test).
- **Changed:** `tests/agents/intake/test_anthropic_client.py` — imports `SYSTEM_GOVERNANCE` in addition to `SYSTEM_INTERVIEWER`. Adds `test_system_interviewer_pins_statistical_terms_note` (asserts glossary-path cross-link + 9 key term tokens are present in `SYSTEM_INTERVIEWER`) and `test_system_governance_excludes_statistical_terms_note` (asserts glossary-path cross-link is absent from `SYSTEM_GOVERNANCE`). +2 tests.
- **Changed:** `tests/data_agent_package/test_anthropic_client.py` — adds `TestStatisticalTermsNoteInjection` class with 5 tests: positive pinning on `summarize()` + `generate_datasheet()` system strings (glossary-path cross-link + 9 key term tokens); negative pinning on `generate_primary_queries()`, `generate_quality_checks()`, and `rank_candidate_tables()` system strings (glossary-path cross-link absent). +5 tests.
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — the "Statistical glossary — agent system prompt injection" follow-up added in Session 61.
- **Verified:** `uv run pytest -q` → **576/576 passing** @ **97.28% coverage** (was 569/569 @ 97.28% at Session 61 close-out; +7 tests, coverage unchanged). `uv run ruff check` → `All checks passed!` (unchanged). `uv run mypy src/` → `Success: no issues found in 48 source files` (unchanged). `uv run mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (unchanged). Positive pin check (intake): `uv run python -c "from model_project_constructor.agents.intake.anthropic_client import SYSTEM_INTERVIEWER; assert 'docs/style/statistical_terms.md' in SYSTEM_INTERVIEWER"` — covered by `test_system_interviewer_pins_statistical_terms_note`. Negative pin check (intake governance): covered by `test_system_governance_excludes_statistical_terms_note`. Positive + negative pins on the five data-agent surfaces covered by `TestStatisticalTermsNoteInjection`.
- **Unchanged intentionally:** website agent (`src/model_project_constructor/agents/website/`) — no LLM prompt surface exists (template renderer via `RepoClient` Protocol); Phase 0 scope finding verified by `Glob '**/anthropic_client.py'` returning only intake + data paths, and by `Grep 'anthropic|claude|LLM|system_prompt|system='` against `src/model_project_constructor/agents/website/` returning one false-positive in `fake_client.py` (the fake `RepoClient`, not an LLM client). Intake `SYSTEM_GOVERNANCE` — regulatory-label emitter (cycle_time / risk_tier), not statistical prose; glossary injection would waste tokens without improving classification. Data-agent `generate_primary_queries` / `generate_quality_checks` / `rank_candidate_tables` — SQL-authoring or 0.0-1.0 scoring surfaces; statistical-terminology conflations rare. `docs/style/statistical_terms.md` itself — Session 61's initial seed is the authoritative reference; this session consumes it by pointer (no content edits). `docs/wiki/claims-model-starter/Glossary.md` — Session 61's cross-link is load-bearing; no edits needed. Orchestrator code, data-agent nodes / agent / schemas / llm protocol — prompt-injection is strictly additive inside `anthropic_client.py` module; no wire-protocol change, no schema change, no Protocol signature change. No changes to `CLAUDE.md` / `SESSION_RUNNER.md` / `SAFEGUARDS.md` / methodology docs. No wiki page edits — the glossary's wiki integration (Session 61) is sufficient for human readers; the agent-prompt injection is an agent-internal concern.
- **Next:** BACKLOG now has 2 `[ ]` items after removal. Numerical order: (1) Data source inventory Phase 4 OPTIONAL — still deferred unless pilot demand surfaces; (2) Tutorial renderer: migrate to MkDocs. Session 63 picks per operator direction.

### 2026-04-19 — Statistical terminology glossary — initial seed + amendment process + wiki integration (Session 61)

Ships the initial seed of `docs/style/statistical_terms.md` — a curated, authoritative glossary of ~30 statistical / machine-learning terms the project actively cares about because they are commonly conflated (in casual speech, in LLM-generated prose, or across sub-fields). Initial sections: probability and inference (probability vs likelihood, odds, confidence vs credible intervals, p-value, statistical vs practical significance, Bayes' theorem); hypothesis testing and error types (H₀, Type I / Type II, multiple comparisons); classification metrics (accuracy / precision / recall / specificity / F1, ROC AUC vs PR AUC, calibration vs discrimination); model evaluation and generalization (overfitting / underfitting, train/val/test, cross-validation, class imbalance, data leakage); bias, variance, and fairness (statistical bias vs algorithmic/fairness bias — named explicitly as a separate row to head off the collision; bias-variance trade-off; fairness-metric incompatibility citing Chouldechova 2017 + Kleinberg-Mullainathan-Raghavan 2016); insurance-specific clarifications (statistical risk vs insured peril, frequency vs severity, pure premium, loss ratio vs loss function, hazard ratio, subrogation cross-link). Each entry uses the same Term / Definition / Common confusion 3-column format as the existing wiki Glossary so the two files visually align. Where the definition is contested or non-obvious, inline markdown links cite authoritative sources (ASA p-value statement, etc.). Amendment process documented: when to add (LLM conflation caught at review, contributor/stakeholder conflation observed, new modeling capability adopted), how to add (format, authoritative-source priority order: peer-reviewed texts → professional societies → standards bodies → Wikipedia for uncontested terms; single-purpose PRs; reviewer checklist), how to remove or rewrite. Wiki integration documented: the wiki `## Statistical terms` section becomes a **curated subset** (terms likely to appear in user-facing wiki prose); this file is authoritative; conflicts resolve toward this file. Canonical cross-link format (single blockquote line at top of the wiki section) specified with relative path `../../style/statistical_terms.md`. Sync-check grep command documented. Future integrations (agent-system-prompt injection, lint/CI check) explicitly listed as out-of-scope for Session 61 and filed in BACKLOG. **Scope note:** the original BACKLOG entry bundled "glossary" and "agent-system-prompt injection" as one item; operator scoped this session to the glossary + amendment process + wiki integration, with agent-prompt-injection split off as a follow-up BACKLOG line.

- **Added:** `docs/style/` — new directory, first file. Establishes the style-guide tree as a sibling of `docs/wiki/` (user-facing) and `docs/planning/` (architecture plans); contributor-facing style content that applies across the project's prose (reports, commit messages, planning docs, wiki) lives here.
- **Added:** `docs/style/statistical_terms.md` — new file, ~220 lines. Sections: header + "Why this exists" + "What this file is NOT"; "How to use this file" (contributor / reviewer / agent-prompt-author audiences); 6 glossary sections (probability and inference, hypothesis testing and error types, classification metrics, model evaluation and generalization, bias/variance/fairness, insurance-specific clarifications); amendment process (when to add, how to add or amend, authoritative-source priority order, reviewer responsibilities, how to remove or rewrite); wiki integration (when to update the wiki Glossary alongside this file, canonical cross-link format, sync check at session close); future integrations (agent system-prompt injection, lint/CI check); provenance paragraph naming Session 61 as the seed session with operator-requested scope.
- **Changed:** `docs/wiki/claims-model-starter/Glossary.md` — `## Statistical terms` section gains a one-line blockquote cross-link (+3 lines including blank lines) pointing at `../../style/statistical_terms.md` with the "Curated subset" framing. Existing 5 table rows (Probability, Likelihood, Target variable, Feature, Supervised learning) untouched — they remain the curated subset surfaced to wiki readers; they are consistent with the corresponding entries in `docs/style/statistical_terms.md` (which has additional terms not surfaced in the wiki).
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — the original "Statistical terminology glossary" entry that bundled glossary authoring + agent-prompt injection.
- **Added:** one new BACKLOG.md line splitting off the follow-up — "Statistical glossary — agent system prompt injection" — wiring the glossary (or a curated subset) into the intake / data / website agent system prompts so generated reports use the precise statistical terminology natively. Net BACKLOG change: 3 `[ ]` → 3 `[ ]`, 0 `[x]` → 0 `[x]` (one removed + one added). Learning #26 Session #31 of application.
- **Verified:** `uv run pytest -q` → **569/569 passing** @ **97.28% coverage** (unchanged from Session 60 baseline; docs-only change). `uv run ruff check src/ tests/ packages/` → `All checks passed!` (unchanged). `uv run mypy src/` → `Success: no issues found in 48 source files` (unchanged). `uv run mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (unchanged). Manual verification: `docs/style/statistical_terms.md` renders as valid GitHub-flavored-markdown (tables align, relative links resolve, no unbalanced fences); the wiki Glossary cross-link's relative path `../../style/statistical_terms.md` resolves correctly from `docs/wiki/claims-model-starter/Glossary.md` to `docs/style/statistical_terms.md`. Pre-commit grep `grep -in "probability\|likelihood" docs/style/statistical_terms.md docs/wiki/claims-model-starter/Glossary.md` confirms the two files' definitions of the two highest-priority terms agree on the substantive claim (both pin probability as P(event) and likelihood as L(θ|data), both flag the conflation).
- **Unchanged intentionally:** no code in `src/` or `packages/` touched — this is a documentation-only deliverable. No agent system prompt modified (the BACKLOG entry's original second clause is split off as a follow-up item, per operator's session scope). No `CLAUDE.md` / `SESSION_RUNNER.md` / `SAFEGUARDS.md` / methodology docs touched. No test files touched. No changes to the wiki's `_Sidebar.md` (the Glossary page already exists and is already linked; adding a cross-link inside it doesn't affect sidebar navigation). The wiki Glossary's 5 existing "Statistical terms" rows (Probability, Likelihood, Target variable, Feature, Supervised learning) were kept with their existing wording — they predate this session, they are consistent with the new authoritative file's entries, and rewriting them would produce diff noise without definitional improvement. Other wiki pages (Data-Guide, Worked-Examples, Schema-Reference, Pipeline-Overview, Content-Recommendations) that use statistical terminology were not re-scanned against the new glossary in this session — a future review pass can catch any existing conflations, but such a sweep is explicitly out-of-scope for the "initial seed" deliverable. `docs/wiki/claims-model-starter/Changelog.md` (user-facing wiki changelog) not updated in this session — the style-guide tree is contributor-facing and the wiki changelog's audience is project users; consensus from Learning #17 (methodology README as authoritative for what belongs in which file) supports keeping the user-facing wiki changelog focused on user-observable changes, not contributor-facing style-guide work.
- **Next:** BACKLOG has 3 `[ ]` items. Numerical order is (1) Data source inventory Phase 4 (OPTIONAL, deferred unless pilot demands); (2) Statistical glossary — agent system prompt injection (follow-up split off this session); (3) Tutorial renderer: migrate to MkDocs. Session 62 picks per operator direction. Optional follow-ups not filed as BACKLOG items unless operator requests: (a) review existing wiki pages (Data-Guide, Worked-Examples, Schema-Reference, Pipeline-Overview, Content-Recommendations) against the new glossary to catch existing conflations; (b) extend the glossary with terms from sub-fields the project hasn't yet adopted (causal inference, time-series, survival analysis beyond the single `hazard ratio` entry already included).

### 2026-04-19 — Data source inventory Phase 3 — consumer integration (Session 60)

Ships Phase 3 of the data-source-inventory architectural workstream planned in Session 57 (`docs/planning/data-source-inventory-contract-plan.md`). Phase 3 closes the loop: `DataRequest` gains an optional `data_source_inventory: DataSourceInventory | None = None` field; the `LLMClient.generate_primary_queries` Protocol gains a keyword-only `data_source_inventory` kwarg; `AnthropicLLMClient.generate_primary_queries` renders a truncated+sanitized inventory block into the prompt so the LLM prefers inventory-named tables over inventing ones; `PrimaryQuery.inventory_entries_used: list[str]` (plan §6.4 Q6 default, pinned this phase) records which inventory entries each returned SQL references, giving auditors a provenance trail; `make_generate_queries` in `nodes.py` plumbs the inventory from `DataRequest` through to the LLM; `_assemble_complete_report` in `agent.py` carries `inventory_entries_used` from spec to schema; `intake_report_to_data_request` in the orchestrator adapter gains an optional keyword-only `data_source_inventory` kwarg. Behaviour when the field is absent is byte-identical to pre-Phase-3 (verified: every pre-existing test continues to pass unmodified). Truncation strategy (plan §9 Phase 3 gotcha #1 / §12 Q4): entries are sorted by `relevance_score` (None → 0.0) and the top 20 are emitted with a `... and M more sources truncated` note for the remainder. Sanitization (gotcha #2 / Q5): `description` and `relevance_reason` have control characters stripped (keeping `\n` and `\t`) and are bounded at 2000 characters with an ellipsis on overflow. Empty inventory (`entries=[]`) is treated identically to `None` per §6.3. Plan §11 cross-phase invariant confirmed: status-decision logic at `agent.py:55-61` is unchanged (Learning #21); decoupling guarantee preserved (no `IntakeReport` imports added to `packages/data-agent/`).

- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/schemas.py` — `DataRequest` gains `data_source_inventory: DataSourceInventory | None = None` field (+1 line; forward-reference resolves via `from __future__ import annotations` + Pydantic v2 lazy resolution, no model_rebuild needed). `PrimaryQuery` gains `inventory_entries_used: list[str] = Field(default_factory=list)` (+1 line). Both fields are additive-only with defaults; every pre-existing fixture and call site continues to work without modification.
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/llm.py` — `PrimaryQuerySpec` frozen dataclass gains `inventory_entries_used: list[str] = field(default_factory=list)` (+1 line, +1 `field` import). `LLMClient.generate_primary_queries` Protocol signature extended with keyword-only `data_source_inventory: DataSourceInventory | None = None` (+4 lines including docstring paragraph naming the None / empty-entries preservation rule).
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/anthropic_client.py` — `AnthropicLLMClient.generate_primary_queries` signature extended with keyword-only `data_source_inventory` kwarg; user prompt now includes an inventory block when one is rendered; JSON output shape expanded to request `inventory_entries_used: list[str]` on each primary query (parser accepts missing key → `[]` fallback for pre-Phase-3 / third-party client responses). New module-level constants `MAX_INVENTORY_ENTRIES_IN_PROMPT = 20`, `MAX_INVENTORY_FIELD_CHARS = 2000`. New private helpers `_sanitize_prompt_field(value, max_chars=2000) -> str` (strip control chars keeping `\n`/`\t`; truncate with ellipsis) and `_build_inventory_block(inventory) -> str` (None / empty-entries → empty string; otherwise sort by `relevance_score` desc, emit top N, append truncation note). `_dump_request` updated to strip `data_source_inventory` from the raw JSON dump so the inventory is rendered only via `_build_inventory_block` (no double-copy in the prompt, saves tokens). +~65 lines total.
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/nodes.py` — `make_generate_queries` inner function now passes `data_source_inventory=request.data_source_inventory` to `llm.generate_primary_queries` (+4 lines).
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/agent.py` — `_assemble_complete_report` now threads `inventory_entries_used=list(spec.inventory_entries_used)` from each `PrimaryQuerySpec` to the constructed `PrimaryQuery` (+1 line).
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/cli.py` — `_FakeCLIClient.generate_primary_queries` signature extended with keyword-only `data_source_inventory` kwarg (+4 lines; deterministic `SELECT 1 AS placeholder` output unchanged — the smoke-test fake does not need to simulate inventory-aware SQL generation). `DataSourceInventory` imported from `schemas`.
- **Changed:** `src/model_project_constructor/orchestrator/adapters.py` — `intake_report_to_data_request` signature extended with keyword-only `data_source_inventory: DataSourceInventory | None = None` kwarg (+3 lines signature + docstring paragraph). When provided, it is passed through to `DataRequest.data_source_inventory`; when omitted, `DataRequest.data_source_inventory` is `None` and every pre-Phase-3 caller (including `run_pipeline` → `intake_report_to_data_request(intake, run_id)`) continues to work without modification. `DataSourceInventory` imported from `schemas.v1.data`.
- **Added:** `tests/data_agent_package/test_anthropic_client.py` — 17 new Phase 3 tests (across 3 new classes + helper `_inventory` + helper `_entries_with_producer`). `TestSanitizePromptField` (4 tests: None returns empty; control-char strip keeps `\n`/`\t`; truncation at `MAX_INVENTORY_FIELD_CHARS + 3` chars with trailing `...`; custom bound respected). `TestBuildInventoryBlock` (9 tests: None → empty string; empty entries → empty string; FQNs and column previews rendered; sort by `relevance_score` descending; None `relevance_score` treated as 0.0 / ranked entries precede unranked; truncation at `MAX_INVENTORY_ENTRIES_IN_PROMPT + 5` emits "and 5 more sources truncated"; exactly max entries emits no truncation note; sanitization of control chars + long text; column-preview overflow marker "+4 more" for 12-column entry). `TestGeneratePrimaryQueriesInventoryPrompt` (7 tests: inventory block appears in prompt when present; block title absent when inventory is None; block absent when `entries=[]`; `_dump_request` output does NOT include `data_source_inventory` key; parser extracts `inventory_entries_used` field from canned Claude response; parser defaults to `[]` when canned response omits the key; truncation note reaches the user prompt for 27-entry inventory). +~280 lines of test code.
- **Added:** `tests/data_agent_package/test_inventory_schemas.py` — two new test classes (+~95 lines). `TestDataRequestWithInventory` (3 tests: default `data_source_inventory` is `None`; round-trip through `model_dump_json` / `model_validate_json` preserves the nested inventory; pre-Phase-3 JSON blob without the field still validates). `TestPrimaryQueryInventoryProvenance` (3 tests: default `inventory_entries_used` is `[]`; round-trip preserves the list; pre-Phase-3 JSON blob without the field still validates).
- **Added:** `tests/orchestrator/test_adapters.py` — new class `TestIntakeReportToDataRequestInventory` (3 tests, +~55 lines). `test_default_none_preserves_pre_phase_3_behaviour` (kwarg omitted → field is None). `test_inventory_passed_through` (inventory passed as kwarg → `request.data_source_inventory is inventory`; entry fully_qualified_name `public.claim_events` survives the round-trip). `test_empty_inventory_roundtrips` (empty-entries inventory passed as kwarg → preserved without being rewritten to None).
- **Added:** `tests/agents/data/test_data_agent.py` — 2 new end-to-end tests (+~100 lines). `test_inventory_entries_used_plumbed_end_to_end` — inventory-aware `FakeLLMClient` subclass captures the `data_source_inventory` kwarg it receives; agent assembles a `DataReport` whose `primary_queries[0].inventory_entries_used == ["public.claims"]` exactly matching the seeded spec's list; the inventory object passed through `DataRequest` reaches the LLM unchanged (`captured["inv"] is inventory`); the echoed `report.request.data_source_inventory is inventory`. Pins the cross-phase invariant in plan §11 without requiring a live LLM. `test_inventory_entries_used_defaults_empty_when_inventory_absent` — pre-Phase-3 `FakeLLMClient` (no inventory-aware override) returns a spec without `inventory_entries_used`; assembled `PrimaryQuery.inventory_entries_used == []` and `request.data_source_inventory is None`, pinning backward-compat. The pre-existing `FakeLLMClient` + `ExplodingLLM` definitions in the file were extended to accept the new keyword-only kwarg (signatures match the Protocol; behaviour unchanged).
- **Added:** `packages/data-agent/USAGE.md` — new "Example 5 — Inventory-aware run (consumer integration)" section (~40 lines) showing end-to-end Python usage (load curated JSON → build DataRequest with inventory → run → inspect `inventory_entries_used`). Precedence rules block + truncation-and-sanitization paragraph. Public-API block annotates `DataRequest` as "(optional data_source_inventory field)" and `PrimaryQuery` as "(+ inventory_entries_used)". "Data source inventory" section updated: Phase 3 marked as shipped (no longer "lands in a later phase"); Example 5 linked for the consumer usage path.
- **Added:** `docs/wiki/claims-model-starter/Schema-Reference.md` — `DataRequest` code block + prose updated to include the new `data_source_inventory` field; `PrimaryQuery` code block + prose updated to include the new `inventory_entries_used` field. Precedence rules and the empty-entries-treated-as-None rule mentioned alongside `database_hint`. Section anchors kept stable (removed the hard-coded "(lines 46-60)" / "(lines 83-89)" line numbers to avoid rot).
- **Added:** `docs/wiki/claims-model-starter/Data-Guide.md` — new "Providing a data source inventory" section (~25 lines) at the end of the page. Names the three populated producer classes (curated / automated / interview — `interview` reference deferred); Python-API snippet loading a curated JSON into a `DataSourceInventory` and attaching to a `DataRequest`; precedence + truncation summary.
- **Added:** `docs/wiki/claims-model-starter/Worked-Examples.md` — new "Optional: attaching a data source inventory" subsection (~20 lines) inside the subrogation DataRequest handoff walkthrough. Points at `tests/fixtures/sample_curated_inventory.json` as a concrete example; shows how `PrimaryQuery.inventory_entries_used` would surface the provenance in a real run.
- **Added:** `docs/wiki/claims-model-starter/Pipeline-Overview.md` — Data Agent bullet block updated: input line adds "(optionally a `DataSourceInventory`)"; behavior paragraph describes the new `inventory_entries_used` provenance field; interfaces line adds `discover` subcommand alongside `run` (this was already in the repo since Phase 2 but had not been reflected in Pipeline-Overview).
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — "Data source inventory — Phase 3: Consumer integration" (now shipped). BACKLOG net change: 4 `[ ]` → 3 `[ ]`, 0 `[x]` → 0 `[x]`. Learning #26 Session #30 of application.
- **Verified:** `uv run pytest -q` → **569/569 passing** @ **97.28% coverage** with `Required test coverage of 95% reached` — was 537 @ 97.22% pre-session; +32 new tests (17 in `test_anthropic_client.py`, 6 in `test_inventory_schemas.py`, 3 in `test_adapters.py`, 2 in `test_data_agent.py`, 4 implicit in signature-updated fakes), +0.06% coverage (Phase 3 production code ~70 net lines, tests cover the new helper functions + parse paths + precedence branches + end-to-end plumbing). `uv run ruff check src/ tests/ packages/` → `All checks passed!` (after one manual fix of an `F541` f-string without placeholders in a test assertion). `uv run mypy src/` → `Success: no issues found in 48 source files`. `uv run mypy packages/data-agent/src/` → `Success: no issues found in 13 source files`. Decoupling guarantee test `tests/test_data_agent_decoupling.py` green in full suite — zero `IntakeReport` imports added to `packages/data-agent/`; the orchestrator-side `adapters.py` change is outside the walled garden.
- **Unchanged intentionally:** `DataAgent.run` status-decision logic at `agent.py:55-61` untouched — inventory presence/absence does not add a new terminal status (Learning #21 invariant preserved; plan §11 requirement). `DataAgentState` in `state.py` untouched — the inventory travels on `state["request"].data_source_inventory`, not as a separate state key. `generate_quality_checks` / `summarize` / `generate_datasheet` LLM methods untouched — Phase 3 only extends the primary-query-generation path; quality checks and summarization continue to reason over the echoed `DataRequest` (which now carries the inventory, but the prompts for those stages do not re-render it). `run` CLI subcommand (`model-data-agent run`) untouched — the inventory is carried inside `request.json` when the user provides one; the CLI itself does not grow a dedicated `--inventory` flag because the `DataRequest` JSON schema already supports it (one-way-door avoidance; can be added later if pilot demand surfaces). `discover` CLI subcommand untouched. `ReadOnlyDB` untouched. Phase 1 + Phase 2 source files (`schemas.py` beyond the two new fields, `discovery.py`, `db.py`) untouched — the contract and the reference producer are locked surfaces. `tests/data_agent_package/test_discovery.py` unchanged — Phase 2 producer behavior is untouched by Phase 3 consumer code. `tests/test_data_agent_decoupling.py` unchanged — Phase 3 adds no `IntakeReport` imports to the walled garden. `sample_datareport.json` / `sample_request.json` fixtures unchanged — the new fields are optional with defaults, and the fixtures represent pre-Phase-3 shape (valid input for every version of the consumer). `scripts/run_pipeline.py` untouched — it calls `intake_report_to_data_request(intake, config.run_id)` without the new kwarg, which is the correct default (no inventory wiring yet on the main pipeline path; an orchestrator-level inventory source is a Phase 4 design decision). Plan §9 Phase 3 implemented per scope; the optional live-LLM round-trip against the new prompt is deferred (analogous to B1/B2 in the resume-from-checkpoint plan) — mocked Claude test coverage is sufficient for this phase's success criteria. No YAML loader for curated inventories (Q8 default: JSON only; YAML is a thin wrapper addable later without contract changes).
- **Next:** BACKLOG item 1 — Data source inventory Phase 4 (optional): Intake coupling. Deferred unless pilot demand surfaces; the contract supports it without Phase 4 landing. BACKLOG now has 3 `[ ]` items; numerical order is Phase 4 (optional, deferred) → statistical terminology glossary → MkDocs renderer migration. Optional follow-ups available (not filed as BACKLOG items unless operator requests): (a) live-LLM round-trip against the new inventory-aware prompt to observe whether Claude actually prefers inventory-named tables and returns a well-populated `inventory_entries_used` list — analogous to B1/B2 live-LLM runs; (b) YAML loader for curated inputs; (c) `--curated-inventory <path>` CLI flag on `model-data-agent run`; (d) `DataReport.summary` that cites inventory-provenance by name rather than only via the per-query `inventory_entries_used` list.

### 2026-04-19 — Data source inventory Phase 2 — `information_schema` reference producer (Session 59)

Ships Phase 2 of the data-source-inventory architectural workstream planned in Session 57 (`docs/planning/data-source-inventory-contract-plan.md`). Phase 2 is the first producer: a dialect-agnostic `information_schema` probe (PostgreSQL, SQLite, and most other SQLAlchemy-supported backends via the `Inspector` API) that emits a valid `DataSourceInventory` JSON matching the Phase 1 contract. Ships as both a Python API (`probe_information_schema`) and a CLI subcommand (`model-data-agent discover --db-url ... --output ...`). The producer is fully decoupled — the data agent's graph / nodes / LLM consumers / `DataRequest` are untouched; Phase 3 (consumer integration) is a separate session. Optional `--rank-with-llm` path included: when the caller's LLM object exposes a `rank_candidate_tables` method, the probe populates `DataSourceEntry.relevance_score` and `relevance_reason`; otherwise those fields stay `None`. Hasattr-dispatched rather than Protocol-required so clients that only do query-generation (no ranking) remain valid `LLMClient` implementations. Plan §9 Phase 2 completion criteria all green: `model-data-agent discover --help` shows the subcommand; fake-DB smoke tests write a parseable `DataSourceInventory` JSON; permission-denied / unsupported-dialect paths produce empty-inventory-with-notes rather than raising; all four gate commands (pytest / ruff / mypy-src / mypy-packages) clean.

- **Added:** `packages/data-agent/src/model_project_constructor_data_agent/discovery.py` — new module, `probe_information_schema(db, *, include_schemas, llm, request_context) -> DataSourceInventory` + module-level constants `PRODUCER_ID="information_schema_probe_v1"` / `PRODUCER_VERSION="1.0"`. Calls `db.get_information_schema(schemas=include_schemas)`, builds one `DataSourceEntry` per returned table/view with fully-populated `ColumnMetadata`, PK columns, and FK targets, and wraps them in a `DataSourceInventory` with one `ProducerMetadata` entry (`producer_type="automated"`). Degradation path catches `(SQLAlchemyError, NotImplementedError, RuntimeError)` and returns an empty-entries inventory whose single producer's `notes` names the error — so downstream consumers can distinguish "no tables" from "probe failed" without inspecting return types. Optional LLM ranking via `hasattr(llm, "rank_candidate_tables")` — when present, the returned `TableRanking` dicts are applied via `model_copy(update={...})` to preserve Pydantic's cross-field validator invariants.
- **Added:** `packages/data-agent/src/model_project_constructor_data_agent/llm.py` — new `TableRanking(fully_qualified_name, relevance_score, relevance_reason)` frozen dataclass. Documented as the output shape for optional `rank_candidate_tables` LLM methods; not part of the `LLMClient` Protocol.
- **Added:** `tests/data_agent_package/test_discovery.py` — new file, 21 tests across 6 test classes (~250 lines). `TestProbeHappyPath` (10 tests: inventory return shape, 3 tables + 1 view discovered, producer metadata with correct id+version, entity_kind table-vs-view, PK column detection both as list and as column flag, FK target populated with `namespace.table.column` shape, cross-field validator integrity, request_context preserved, created_at + produced_at coherent, column data_type + nullable captured). `TestProbeIncludeSchemas` (2 tests: schema filter matching the actual schema returns entries; filter matching nothing returns empty entries with a valid producer). `TestProbeDegradation` (4 tests: empty DB returns empty inventory without error; probe-before-connect surfaces as empty-with-notes; `SQLAlchemyError` from a fake DB becomes empty-with-notes; `NotImplementedError` becomes empty-with-notes). `TestProbeRoundTrip` (1 test: probe output round-trips through `model_dump_json` / `model_validate_json`). `TestProbeWithLLMRanking` (4 tests: LLM without `rank_candidate_tables` is ignored with `relevance_score=None`; LLM with the method populates rankings; partial ranking leaves unranked entries `None`; empty-inventory short-circuits the LLM call so it is not invoked).
- **Added:** `tests/data_agent_package/test_cli.py` — 4 new CLI tests (+1 modification): `test_cli_discover_smoke` (writes a valid `DataSourceInventory` JSON against a seeded SQLite DB with 2 tables), `test_cli_discover_rank_with_fake_llm` (`--rank-with-llm --fake-llm` populates `relevance_score=0.9` for the first entry via the deterministic fake stub), `test_cli_discover_include_schemas_filter` (2 assertions — `--include-schemas main` returns entries; `--include-schemas nonexistent` returns empty entries with a valid producer), `test_cli_discover_unreachable_db_errors` (connect-to-dead-postgres fails non-zero without writing the output file). The existing `test_python_dash_m_entrypoint_works` gained one `assert "discover" in result.stdout.lower()` line to pin the new subcommand in `--help` output.
- **Added:** `tests/data_agent_package/test_anthropic_client.py` — 3 new tests for `AnthropicLLMClient.rank_candidate_tables` (+1 helper `_sample_entries`). `test_rank_candidate_tables_parses_json_array` (mocked Claude emits 2 rankings; client parses to `TableRanking` objects with correct scores). `test_rank_candidate_tables_accepts_missing_request_context` (None `request_context` surfaces as "no explicit request context" phrase in the prompt). `test_rank_candidate_tables_non_array_raises` (malformed non-array response raises `LLMParseError`).
- **Added:** `packages/data-agent/USAGE.md` — new "Example 4 — Data-source discovery (`discover` CLI)" section (~50 lines). Three CLI examples (default, `--include-schemas`, `--rank-with-llm` with `--request-context`) + one Python-API example showing `ReadOnlyDB` + `probe_information_schema`. Public-API list expanded from 10 names to 12 (adds `probe_information_schema`, `TableRanking`) and annotates `ReadOnlyDB` with its new `get_information_schema` method.
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/db.py` — `ReadOnlyDB` gains `get_information_schema(schemas: list[str] | None = None) -> list[dict[str, Any]]` (+~60 lines). Delegates to `sqlalchemy.inspect(self._engine)` so PostgreSQL, SQLite, MySQL, and most other supported dialects work transparently; system schemas (`information_schema`, `pg_catalog`) are skipped by default; `schemas=[...]` filters to an allow-list. Returns one dict per table/view with `namespace`, `name`, `entity_kind`, `columns` (list of per-column dicts including PK/FK flags and `foreign_key_target` strings), and `primary_key_columns`. New static helper `_reflect_entity` handles the per-table introspection (columns + PK + FK) with `NoSuchTableError` caught for tables that disappear between discovery and reflection.
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/cli.py` — new `discover` Typer subcommand (+~70 lines) with flags `--db-url` (required), `--output` / `-o` (required), `--include-schemas` (repeatable, default `[]` = all non-system schemas), `--rank-with-llm` (default off), `--request-context` (default None), `--model` (default `DEFAULT_MODEL`), `--fake-llm` (default off). Connects via `ReadOnlyDB`, optionally builds an LLM when `--rank-with-llm`, calls `probe_information_schema`, writes the result as indented JSON. `_FakeCLIClient` gained `rank_candidate_tables` stub (+~15 lines) returning deterministic scores (first entry 0.9, each subsequent 0.1 lower, floor 0.0) so `--fake-llm --rank-with-llm` works end-to-end in CI without a real API key.
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/anthropic_client.py` — `AnthropicLLMClient` gained `rank_candidate_tables(entries, request_context) -> list[TableRanking]` (+~45 lines) following the same system-prompt + user-prompt + `_call_claude` + `_extract_json` pattern as the other methods. Prompt includes each entry's fully-qualified name + entity_kind + column count + first 8 column names (bounded for token-budget safety) and asks Claude for a JSON array of `{fully_qualified_name, relevance_score, relevance_reason}` objects.
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/__init__.py` — added `probe_information_schema` from `discovery` and `TableRanking` from `llm` to the top-level package imports and `__all__` (+3 lines). `from model_project_constructor_data_agent import probe_information_schema` now works.
- **Changed:** `packages/data-agent/USAGE.md` — "Data source inventory" summary updated to reflect Phase 2 shipping (the automated producer is no longer "planned for a future release"; it is the `discover` command + `probe_information_schema`). Consumer integration (Phase 3) still noted as future scope.
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — "Data source inventory — Phase 2: `information_schema` reference producer" (now shipped). BACKLOG net change: 5 `[ ]` → 4 `[ ]`, 0 `[x]` → 0 `[x]`. Learning #26 Session #29 of application.
- **Verified:** `uv run pytest -q` → **537/537 passing** @ **97.22% coverage** with `Required test coverage of 95% reached` — was 509 @ 97.25% pre-session; +28 new tests (21 in `test_discovery.py`, 4 in `test_cli.py`, 3 in `test_anthropic_client.py`), -0.03% coverage delta (essentially flat; `discovery.py` at 100% covered, `db.py` 93% with two unreachable `NoSuchTableError` branches, `anthropic_client.py` 84% with existing live-path-only code untouched). `uv run ruff check src/ tests/ packages/` → `All checks passed!` (after one `ruff --fix` pass for import ordering + one manual pass for 3 `E501` line-too-long fixes in cli.py / discovery.py / test_anthropic_client.py). `uv run mypy src/` → `Success: no issues found in 48 source files`. `uv run mypy packages/data-agent/src/` → `Success: no issues found in 13 source files` (was 12; `discovery.py` is the 13th). Decoupling guarantee test `tests/test_data_agent_decoupling.py` green in full suite — `discovery.py` imports zero intake-side code.
- **Unchanged intentionally:** `DataRequest` unchanged — the `data_source_inventory: DataSourceInventory | None = None` field on `DataRequest` is Phase 3 scope, not Phase 2. `database_hint` unchanged. `DataAgent` graph / nodes / `build_graph` / `generate_primary_queries` prompt untouched — the data agent's runtime path does not yet consume the inventory. `schemas.py` untouched — Phase 1 contract is locked; Phase 2 only CONSUMES the Phase 1 types, never modifies them. `tests/data_agent_package/test_inventory_schemas.py` unchanged — those tests pin the Phase 1 contract; any change would signal a contract mutation not a producer implementation. The `LLMClient` Protocol in `llm.py` unchanged beyond the new `TableRanking` sibling dataclass — `rank_candidate_tables` is a per-implementation method, not a Protocol requirement (keeps query-generation-only LLM clients valid). `_FakeCLIClient` protocol-compatibility unaffected by the new stub. `src/model_project_constructor/orchestrator/adapters.py:63-77` untouched — pipeline-side adapter still passes `database_hint=None`; inventory plumb-through is Phase 3. `generate_datasheet` / `generate_quality_checks` / `summarize` LLM methods untouched. `tests/test_data_agent_decoupling.py` unchanged — Phase 2 stays inside `packages/data-agent/` with zero intake imports (verified: `discovery.py` imports only from `db`, `schemas`, and `sqlalchemy.exc`). No changes to `src/model_project_constructor/schemas/v1/*` — inventory types stay re-exported via Phase 1's existing paths. Plan §9 Phase 2 implemented per scope; the optional `--rank-with-llm` path was included (scope pressure manageable: ~80 LOC across anthropic client + llm dataclass + fake stub) rather than deferred to a follow-up session. No live-DB coverage — fake-SQLite + fake-LLM only per plan §9 Phase 2 scope; live-DB coverage would be a separate Scope-C-style task.
- **Next:** Session 60 implements Phase 3 (consumer integration). Scope per plan §9 Phase 3: add `data_source_inventory: DataSourceInventory | None = None` field to `DataRequest`; extend `LLMClient.generate_primary_queries` Protocol signature to accept `data_source_inventory=None`; update `AnthropicLLMClient.generate_primary_queries` prompt to include a truncated inventory block when present; update `nodes.py` to plumb inventory through `DataAgentState`; decide on `PrimaryQuery.inventory_entries_used: list[str]` (or equivalent) for provenance; update `src/model_project_constructor/orchestrator/adapters.py` to optionally pass inventory (default None); update docs (`USAGE.md`, `Schema-Reference.md`, `Data-Guide.md`, `Worked-Examples.md`, `Pipeline-Overview.md`). Gotchas to forecast: MAX_TOKENS headroom (truncation strategy for large inventories — plan §9 Phase 3 gotchas #1 name `DEFAULT_MAX_TOKENS=4096` as the budget), prompt-injection sanitization of `DataSourceEntry.description` content, backward-compat preservation for every existing test + fixture. BACKLOG now has 4 `[ ]` items; numerical order is Phase 3 → Phase 4 (optional, deferred unless pilot demands) → glossary → MkDocs.

### 2026-04-19 — Data source inventory Phase 1 — contract schema (Session 58)

Ships Phase 1 of the multi-session architectural workstream planned in Session 57 (`docs/planning/data-source-inventory-contract-plan.md`). Phase 1 is the contract itself — four Pydantic types (`DataSourceInventory`, `DataSourceEntry`, `ProducerMetadata`, `ColumnMetadata`) that every future producer emits and every future consumer accepts. No producer or consumer code lands this session; `DataRequest` is unchanged; the data agent's graph / nodes / LLM paths are untouched. The schema is additive-only: it adds 4 new classes + their re-exports, and it does not touch any existing field, method, or call site. Plan §9 Phase 1 completion criteria all green: both `uv run python -c "from model_project_constructor_data_agent import DataSourceInventory"` and `uv run python -c "from model_project_constructor.schemas.v1.data import DataSourceInventory"` succeed; full pytest suite green with a 33-test increase over the 476-test baseline; all four gate commands (pytest / ruff / mypy-src / mypy-packages) clean. Q1/Q8/Q9/Q10 resolved per plan defaults: `relevance_score: float | None`, JSON-only (no YAML loader), schema_version Literal-pinned, `business_domain: str | None` as free text. The cross-field validator on `DataSourceInventory` rejects any entry whose `producer_id` does not resolve to a `ProducerMetadata.producer_id` — the error message names BOTH the dangling values AND the set of known producer_ids so future debuggers see the mismatch in one line (candidate Learning #42).

- **Added:** `packages/data-agent/src/model_project_constructor_data_agent/schemas.py` — four new `StrictBase`-derived classes (+77 lines, 1 new import `model_validator`, 4 new entries in `__all__`). `ColumnMetadata` (7 fields — name / data_type / nullable / description / is_primary_key / is_foreign_key / foreign_key_target). `ProducerMetadata` (5 fields — producer_id / producer_type Literal of 4 values `curated|automated|interview|external_catalog` / produced_at / producer_version / notes). `DataSourceEntry` (20 fields — schema_version Literal["1.0.0"] / name / namespace / source_system / fully_qualified_name / entity_kind Literal of 6 values `table|view|materialized_view|file_dataset|feature_view|other` / columns / primary_key_columns / row_count_estimate / description / business_domain / entity_types / relevance_score / relevance_reason / last_updated_at / refresh_cadence / access_notes / owning_team / producer_id / extra `dict[str, Any]` producer-extension point). `DataSourceInventory` (5 fields — schema_version Literal["1.0.0"] / entries / producers / created_at / request_context — plus `@model_validator(mode="after")` cross-field validator `_producer_ids_resolve` that walks `entries × producers` and raises `ValueError` with both dangling values and known producer_ids).
- **Added:** `tests/data_agent_package/test_inventory_schemas.py` — new file, 33 tests across 6 test classes (+~250 lines). `TestColumnMetadata` (3 tests: minimal construction, full construction with FK, extra-fields forbidden). `TestProducerMetadata` (4 tests: all producer_types parametrized / invalid type rejected / optional fields default / extra forbidden). `TestDataSourceEntry` (9 tests: minimal happy path, all entity_kinds parametrized, invalid entity_kind rejected, columns + primary_key_columns, relevance_score as float, freshness/access fields, `extra` dict accepts producer-specific keys, top-level extra keys still forbidden, schema_version Literal pinning). `TestDataSourceInventory` (7 tests: happy path, empty entries valid, dangling producer_id rejected, multi-producer+multi-entry, schema_version Literal, extra forbidden, serialization round-trip). `TestSampleCuratedFixture` (1 test: fixture validates and all entry producer_ids resolve). Plus 1 identity test that the class is the same object across all three import paths. Helpers `_make_producer` / `_make_entry` / `_make_inventory` mirror the `make_data_request` / `make_data_report` pattern in `tests/schemas/fixtures.py`.
- **Added:** `tests/fixtures/sample_curated_inventory.json` — new file, reference curated-producer payload. 2 entries (`public.claim_events`, `public.subrogation_outcomes`) + 1 `ProducerMetadata` (`producer_id="curated:claims-analytics-team"`, `producer_type="curated"`) + full `ColumnMetadata` per entry (claim_id / event_type / event_timestamp on claim_events with primary_key and composite PK `[claim_id, event_timestamp]`; claim_id / recovered_amount / closed_date on subrogation_outcomes with `is_foreign_key=True` + `foreign_key_target="public.claim_events.claim_id"`). `business_domain="claims"`, `entity_types` populated, `refresh_cadence` and `owning_team` set. Ready for Phase 2 as a fake-producer input and Phase 3 as a consumer-integration test input.
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/__init__.py` — added `ColumnMetadata`, `DataSourceEntry`, `DataSourceInventory`, `ProducerMetadata` to the schema import block and to `__all__` (+8 lines). Top-level `from model_project_constructor_data_agent import DataSourceInventory` now works.
- **Changed:** `src/model_project_constructor/schemas/v1/data.py` — mirrored the re-export; main-package path `from model_project_constructor.schemas.v1.data import DataSourceInventory` now works (+8 lines).
- **Changed:** `src/model_project_constructor/schemas/v1/__init__.py` — mirrored the re-export; package path `from model_project_constructor.schemas.v1 import DataSourceInventory` now works (+8 lines).
- **Changed:** `packages/data-agent/USAGE.md` — Public-API block expanded from 6 schema names to 10 (adds `ColumnMetadata`, `DataSourceEntry`, `DataSourceInventory`, `ProducerMetadata`). New "Data source inventory" section (~25 lines) names the four producer classes (curated, automated, interview, external_catalog) and points to the plan document + sample fixture.
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — "Data source inventory — Phase 1: Contract schema" (now shipped). BACKLOG net change: 6 `[ ]` → 5 `[ ]`, 0 `[x]` → 0 `[x]`. Learning #26 Session #28 of application.
- **Verified:** `uv run pytest -q` → **509/509 passing** @ **97.25% coverage** with `Required test coverage of 95% reached` — was 476 @ 97.19 pre-session; +33 new tests (all in `test_inventory_schemas.py`), +0.06% coverage (the new Pydantic classes add mostly field declarations which don't affect coverage arithmetic much, but `_producer_ids_resolve` contributes 3 new covered lines). `uv run ruff check src/ tests/ packages/` → `All checks passed!` (after one `ruff --fix` pass for import ordering in the four re-export files; `DataSourceEntry`/`DataSourceInventory` sort after `Datasheet` under ruff's case-sensitive isort rule). `uv run mypy src/` → `Success: no issues found in 48 source files`. `uv run mypy packages/data-agent/src/` → `Success: no issues found in 12 source files`. Decoupling guarantee test `tests/test_data_agent_decoupling.py` green (confirmed in full suite; the new schema types add no `IntakeReport` / `schemas.v1.intake` import).
- **Unchanged intentionally:** no producer or consumer code. `DataAgent` graph / nodes / LLM methods / CLI / adapters untouched. `DataRequest` unchanged — the `data_source_inventory: DataSourceInventory | None = None` field on `DataRequest` is Phase 3 scope. `database_hint` unchanged. Existing `tests/schemas/test_data.py` / `tests/data_agent_package/test_anthropic_client.py` / `tests/data_agent_package/test_cli.py` unchanged. `src/model_project_constructor/schemas/registry.py` unchanged — inventory types are nested inside future `DataRequest` (Phase 3), not standalone handoff payloads, so they do NOT belong in the handoff registry. `tests/test_data_agent_decoupling.py` unchanged — Phase 1 stays inside `packages/data-agent/` with no intake imports. Plan document unchanged — Phase 1 implemented per plan §9 scope without refinements to field names or Literal values; the plan's "refinement discretion" (gotcha #9) was available but not exercised because the plan's design was already well-formed.
- **Next:** Session 59 implements Phase 2 (`information_schema` reference producer). Scope per plan §9 Phase 2: `ReadOnlyDB.get_information_schema` method + `probe_information_schema` function in new `discovery.py` + `discover` CLI subcommand with `--db-url` / `--output` / `--include-schemas` / `--rank-with-llm` / `--fake-llm` flags. Fake-DB / fake-LLM coverage only; live-DB deferred. `--rank-with-llm` is OPTIONAL for MVP — if it adds scope pressure, defer to a follow-up session; ranking without LLM (`relevance_score=None`) is valid. BACKLOG now has 5 `[ ]` items; numerical order is Phase 2 → Phase 3 → Phase 4 (optional, deferred unless pilot demands) → glossary → MkDocs.

### 2026-04-19 — Data source inventory contract — architectural plan (Session 57)

Reframes BACKLOG item 1 (previously "Data agent: metadata discovery mode", sized at "one session, maybe 1.5") as a multi-session architectural workstream. Operator guidance (captured verbatim §1.2 of the plan, and saved as durable project memory `feedback_data_source_discovery.md`): "Potential data source discovery needs to be a separate activity since some groups will have their own curated raw data and factors. We will need a clear method of providing data sources for ingestion by this tool stack. I expect over time separate data discovery tools will be created and evolve. This system will need to be adaptable for that." Session 57 is therefore a PLANNING session — the deliverable is the plan document, not code. Per Failure Mode #19 (plan-mode bypass) and #18 (planning-to-implementation bleed), implementation starts in Session 58 (Phase 1). The plan pins four decisions that shape every future phase: (a) discovery is upstream, inventory is the interface, consumption is downstream — the data agent *consumes* inventory, never *produces* it; (b) the contract is pure data (Pydantic schemas serializable to JSON) so curated files, shell scripts, and non-Python tools can all produce inventory without importing the Python package; (c) four first-class producer classes (`curated`, `automated`, `interview`, `external_catalog`) share a single consumer shape — the data agent does not branch on `producer_type`; (d) `DataRequest.database_hint` stays; a new optional `data_source_inventory: DataSourceInventory | None = None` field extends the schema additively, preserving every existing fixture and call site.

- **Added:** `docs/planning/data-source-inventory-contract-plan.md` — 14-section architectural plan (~640 lines of markdown). Sections: (1) Context with operator guidance + current-state table + filing-session grounding; (2) Non-goals; (3) Five architectural decisions (separation, pure-data contract, four producer classes, backward compat, optional absence); (4) Interface-first schema design for `DataSourceInventory`, `DataSourceEntry`, `ProducerMetadata`, `ColumnMetadata` — field-by-field rationale, error contract, versioning strategy; (5) Producer ecosystem (curated, `information_schema` probe with public-API sketch, interview-sourced Phase 4 converter, future external catalogs); (6) Consumer integration (entry point, prompt effect, precedence rules with `database_hint`, audit/observability); (7) Alternatives considered table (6 alternatives weighed and rejected with reasons); (8) Grep-based evidence inventory (4 separate greps, file lists, counts verified fresh Session 57 — 49 files reference `DataRequest`, 14 reference `database_hint`, 7 reference `generate_primary_queries`, 18 reference `ReadOnlyDB`, zero substantive matches for `discover|information_schema|catalog tables` in the data-agent package confirming greenfield); (9) Phased implementation with four phases (Phase 1 contract schema, Phase 2 `information_schema` reference producer, Phase 3 consumer integration, Phase 4 optional intake coupling) — each phase has explicit scope, completion criteria including all four gate commands, session boundary, and forecast gotchas; (10) Impact analysis table per file; (11) Overall verification plan including Learning #21 status-decision invariant check + decoupling-guarantee preservation; (12) 11 open questions / deferred decisions flagged for the phase that decides each; (13) References; (14) Planning session checklist per SESSION_RUNNER.md.
- **Added:** `~/.claude/projects/-Users-rmsharp-Development-model-project-constructor/memory/feedback_data_source_discovery.md` — durable project memory (type `feedback`) capturing operator's architectural guidance verbatim plus mechanical "how to apply" rules (discovery upstream, inventory interface, consumption downstream; never embed discovery inside `DataAgent.run()`; reframe any future "add discovery to X" task as contract-then-reference-impl-then-consumer in separate sessions; future producer tools plug into the contract, not into consumer-facing code). Pointer added to `MEMORY.md` index so the guidance loads into every future session on this project.
- **Changed:** `BACKLOG.md` — replaced the single "Data agent: metadata discovery mode" line with four per-phase lines (Phases 1–4 of the plan). Each per-phase line cites the plan's §9 sub-section so Session 58+ can jump directly to its scope. The per-phase split respects the operator's "1 per session" discipline: Session 58 = Phase 1, Session 59 = Phase 2, etc.
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — "Data agent: metadata discovery mode" (now superseded by the plan document + four per-phase items). Net BACKLOG change: 3 `[ ]` → 6 `[ ]`, 0 `[x]` → 0 `[x]`. Learning #26 Session #27 of application — the shipped-item-removal discipline applied to a plan that supersedes a BACKLOG line is structurally identical to shipping the item (the line is not "waiting work" any more; it is "now scoped into four phases").
- **Verified:** `uv run pytest -q` → **476/476 passing** @ **97.19% coverage** with `Required test coverage of 95% reached` — unchanged from pre-session since no source or test file was edited. `uv run ruff check src/ tests/ packages/` → `All checks passed!`. `uv run mypy src/` → `Success: no issues found in 48 source files`. `uv run mypy packages/data-agent/src/` → `Success: no issues found in 12 source files`. Planning Session Checklist (SESSION_RUNNER.md) complete: plan has file paths and line numbers, grep-based inventory for all affected symbols, per-phase completion criteria with verification commands, explicit session boundaries with STOP markers, close-out executed this turn.
- **Unchanged intentionally:** zero production code edited — no schemas, nodes, graph, LLM methods, CLI, adapters, fixture driver, or test files touched. The plan IS the deliverable; implementation is Session 58's scope. Learning #18 (scope discipline) applied end-to-end: the plan deliberately phases intake coupling (Phase 4) as OPTIONAL rather than bundling it into Phase 3; it keeps `database_hint` rather than proposing a migration; it does not extract a new `packages/data-source-inventory/` package despite the plug-in architecture (extraction revisited post-Phase-3 if demand surfaces). `SYSTEM_INTERVIEWER` (Session 56's prompt extension) untouched — Phase 4's converter reads the qa_pairs it populates but does not modify the interview itself. Learning #21's status-decision invariant (`agent.py:55-61` routes to COMPLETE / INCOMPLETE_REQUEST / EXECUTION_FAILED based on final state) is pinned as a Phase 3 verification requirement, not altered this session. Decoupling guarantee (`tests/test_data_agent_decoupling.py`) preserved — all Phase 1–3 work stays inside `packages/data-agent/`; the Phase 4 interview-sourced converter lives in `src/model_project_constructor/orchestrator/adapters.py` outside the walled garden.
- **Next:** Session 58 implements Phase 1 (contract schema — add `DataSourceInventory`, `DataSourceEntry`, `ProducerMetadata`, `ColumnMetadata` to `packages/data-agent/src/model_project_constructor_data_agent/schemas.py` + round-trip tests + re-exports from the package `__init__.py` and `src/model_project_constructor/schemas/v1/data.py`). Plan §9 Phase 1 pins the scope + completion criteria + gotchas. BACKLOG now has 6 `[ ]` items (Phases 1–4 + glossary + MkDocs migration); numerical order is Phase 1 → Phase 2 → Phase 3 → Phase 4 (optional, deferred unless pilot demands it) → glossary → MkDocs.

### 2026-04-19 — Intake agent: data-source discovery prompts (Session 56)

Closes BACKLOG item 1 (first by numerical order, per operator directive "work on backlog items in numerical order; 1 per session"). The BACKLOG line described the filing motivation: "the agent accepts whatever the stakeholder says about available data at face value." The current `SYSTEM_INTERVIEWER` in `src/model_project_constructor/agents/intake/anthropic_client.py:33-42` directs Claude toward the four intake sections (business problem, proposed solution, model solution, estimated value) and the governance classification, but says nothing about data-source discovery — so whether and how the agent probes for data is entirely up to Claude's improvisation for a given run. Session 56 extends the system prompt with one additional paragraph that (a) directs the agent to probe for CONCRETE data sources when the conversation reaches `model_solution.candidate_features`, (b) names the P&C-claims-domain systems typically present (so Claude has a concrete checklist rather than generic "do you have a data warehouse?" phrasing), and (c) offers help identifying sources if the stakeholder is uncertain. The `MAX_QUESTIONS=20` cap is respected by design: the probe is opportunistic and folds into existing question budget rather than adding a fixed sub-sequence — Learning #21's status-decision invariant (`nodes.py:129-134,142` — `questions_cap_reached` auto-appends when `questions_asked >= MAX_QUESTIONS and not believe_enough_info`) is unchanged, and no new questions are forced.

- **Changed:** `src/model_project_constructor/agents/intake/anthropic_client.py` — `SYSTEM_INTERVIEWER` constant extended from 9 lines to 21 lines (additional ~80 words appended to the existing string). New guidance: probe for named systems, tables, extracts, or feeds rather than accepting "we have the data"; offer to help identify sources if the stakeholder is uncertain; named P&C-claims-domain systems — claims admin (Guidewire ClaimCenter, Duck Creek Claims), policy admin, billing and collections, subrogation recovery tools, fraud / SIU scoring, agent and customer CRM, enterprise data warehouse or lake; surface owning team and refresh cadence when material to feasibility. No other source files touched — the prompt is the single source of guidance for how the agent conducts the interview; nodes.py, state.py, protocol.py, agent.py, fixture.py, graph.py, __main__.py, cli.py all untouched.
- **Added:** one new test in `tests/agents/intake/test_anthropic_client.py` — `test_system_interviewer_pins_data_source_discovery_probes` (35 lines including docstring). Pins the new probe content against drift by asserting (a) `"CONCRETE data sources"` and `"offer to help identify"` phrases are present, and (b) each of the 9 named P&C-claims-domain systems appears as a substring (`Guidewire ClaimCenter`, `Duck Creek Claims`, `policy admin`, `billing and collections`, `subrogation recovery`, `fraud / SIU`, `CRM`, `data warehouse`, `data lake`). The test imports `SYSTEM_INTERVIEWER` directly — which required adding the constant to the existing `from model_project_constructor.agents.intake.anthropic_client import (...)` block — so any future edit that drops a load-bearing system name fails immediately rather than silently.
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — "Intake agent: data source discovery prompts" (now shipped). BACKLOG net change: 4 `[ ]` → 3 `[ ]`, 0 `[x]` → 0 `[x]`. Learning #26 Session #26 of application.
- **Verified:** `uv run pytest -q` → **476/476 passing** (was 475 pre-session; +1 new regression test, -0 test removals). Coverage **97.19%** with `Required test coverage of 95% reached` — floor holds (2.19-point headroom; unchanged from Session 55 because the new test exercises already-covered string-import code paths and the prompt extension is a constant-string edit that adds no new execution branches). `uv run ruff check src/ tests/ packages/` → `All checks passed!`. `uv run mypy src/` → `Success: no issues found in 48 source files`. `uv run mypy packages/data-agent/src/` → `Success: no issues found in 12 source files`.
- **Unchanged intentionally:** no changes to the graph, state, nodes, protocol, schemas, or fixture driver — the deliverable is a prompt change, and every other file would have been scope creep. No changes to `SYSTEM_GOVERNANCE` (the governance-classification sub-prompt is about cycle-time/risk-tier decisions, not data-source probing; data sources surface in the interviewer path). No changes to `MAX_QUESTIONS=20` cap — the probe is opportunistic within the existing budget (Learning #21 invariant preserved). No new fixture files — the fixture-driven test path (`FixtureLLMClient`) does not use `SYSTEM_INTERVIEWER`, it returns canned `NextQuestionResult` values, so fixture-based tests are unaffected by the prompt change. No live-LLM round-trip this session — the test pins prompt content, not LLM output; a live round-trip against the new prompt is a separate, optional exercise (analogous to B1/B2 in the resume-from-checkpoint plan) and would be filed as its own BACKLOG item if a pilot operator wants output-quality evidence.
- **Next:** 3 `[ ]` BACKLOG items remain (numerical order per operator directive): (1) data-agent metadata discovery mode, (2) statistical terminology glossary, (3) tutorial renderer migration to MkDocs with copy-button plugin. Optional plan Phase 4 (UI writes `IntakeReport.json` envelope) stays deferred unless a pilot operator demands the UI → `--resume` workflow. Optional live-LLM round-trip against the new intake prompt (observe whether Claude actually probes named systems when prompted) also available as a pilot follow-up.

### 2026-04-19 — Tutorial UX: split multi-command code blocks for per-block copy (Session 55)

Closes BACKLOG item 1 (first by numerical order, per operator directive "work on backlog items in numerical order; 1 per session"). Session 18's fresh-clone user test (self-assessment at `SESSION_NOTES.md:4983`) filed the motivating pain point: contributors following the tutorial hit "copy-paste UX" friction because several bash blocks bundled multiple commands (install sequence, mutually-exclusive env-loading options, namespace/host-URL variants), forcing manual editing after paste. GFM/pandoc rendering emits one `<pre>` element per fenced block with no per-block copy button, so the only way to give each command its own copy target is to split multi-command blocks into one-command-per-block. Four split sites identified by grep-inventory (fence lines 52 → 66; blocks 26 → 33; net +7 new blocks): (a) Prerequisites install sequence `git clone` + `cd` + `uv sync`, (b) Step 5a "Load the environment" Options A/B/C (direnv, manual export, source wrapper — mutually exclusive), (c) Step 5c `MPC_NAMESPACE` three variants (GitLab top-level, GitLab nested, GitHub org — mutually exclusive), (d) Step 5c self-hosted `MPC_HOST_URL` GitLab vs GitHub Enterprise (mutually exclusive). Non-split sites kept intentionally: YAML fixture body (single file content), `.env` template body (single file content), multi-line `\`-continued single commands (logically one command), Python programmatic-API block (complete recipe users copy as a unit), already-single-command blocks. Renderer migration to MkDocs/Docusaurus for native per-block copy buttons filed as a new BACKLOG item (scope-kept-separate per Session 54 gotcha #3).

- **Changed:** `docs/tutorial.md` — four split sites restructured. Each split replaces one multi-command block with 2–3 single-command blocks, separated by short descriptive prose (no nested list indentation to avoid GFM/pandoc rendering quirks). Install sequence gains three leading sentences ("Clone the repository (substitute your clone URL):", "Enter the project directory:", "Install dependencies:") so the commands stand alone. Env-loading options are preceded by bolded labels (`**Option A — direnv** (if you use it):`, `**Option B — manual export:**`, `**Option C — source a wrapper:**`) so the user sees which variant they're about to copy. Namespace and host-URL variants gain short prose labels naming the platform for each block. No command text changed — only prose formatting around the existing commands.
- **Added:** one new BACKLOG.md line — "Tutorial renderer: migrate to MkDocs (or equivalent) with copy-button plugin". Scope: renderer migration only; tutorial prose stays put. Deferred per Session 54 gotcha #3's scope discipline ("1 per session"). BACKLOG now has the renderer task in its own numbered slot for a future session.
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — "Tutorial UX: split code blocks" (now shipped). BACKLOG net change: 4 `[ ]` → 4 `[ ]`, 0 `[x]` → 0 `[x]` (removed the shipped item, added the renderer-migration follow-up).
- **Verified:** `./scripts/render_tutorial.sh` (Session 54's wrapper) re-rendered `docs/tutorial.md` → `docs/tutorial.html` cleanly (72,652 bytes, up from Session 54's 72,597; +55 bytes reflects the added prose leading each split block). CSS sentinels present (`max-width: 860px`, `border: 1px solid #d0d7de`, `margin: 2.5em` all confirmed in rendered HTML via grep). Post-split rendered code-block count: 33 `<pre>` elements (matches the post-split fence count of 33 blocks; pandoc wraps each fenced block in one `<pre>`). `uv run pytest -q` → **475/475 passing** (unchanged from Session 54 — tutorial-only edits add no Python code paths). Coverage **97.19%** (unchanged). `uv run ruff check src/ tests/ packages/` → `All checks passed!`. `uv run mypy src/` → `Success: no issues found in 48 source files`. `uv run mypy packages/data-agent/src/` → `Success: no issues found in 12 source files`.
- **Unchanged intentionally:** no edits to command semantics — every `git clone` / `cd` / `uv sync` / `export` / etc. kept its exact pre-split text. No edits to the YAML fixture body (lines ~63-172) — it is one file's content, not a command sequence; users paste the whole block into `my_intake.yaml`. No edits to the `.env` template body (single file content with GitLab/GitHub alternatives as comment-toggleable lines — intentionally kept together because users edit the file rather than running two separate shell commands). No edits to the Python programmatic-API block in Step 8 — it is a complete end-to-end recipe users copy as a unit. Session 51's newly-inserted Step 7 "Resuming a partial run" block left intact (already one command per block). `scripts/render_tutorial.sh` unchanged — it handled the new block structure without modification (the CSS on `pre`/`code` elements cascades to the new smaller blocks without tuning). No new BACKLOG items beyond the filed renderer-migration one; Session 55 stayed in scope.
- **Next:** 4 `[ ]` BACKLOG items remain (numerical order per operator directive): (1) intake agent data-source discovery prompts, (2) data-agent metadata discovery mode, (3) statistical terminology glossary, (4) tutorial renderer migration to MkDocs with copy-button plugin. Optional plan Phase 4 (UI writes `IntakeReport.json` envelope) stays deferred unless a pilot operator demands the UI → `--resume` workflow.

### 2026-04-19 — Tutorial HTML render script: `scripts/render_tutorial.sh` (Session 54)

Closes BACKLOG item 1 (first by numerical order, per operator directive "work on backlog items in numerical order; 1 per session"). Session 18's gotcha #5 (`SESSION_NOTES.md:4793`) filed the motivating pain point: plain `pandoc docs/tutorial.md -o tutorial.html` renders with hr/code-block overlap, unbordered tables, and full-viewport body width because pandoc's default stylesheet was designed for prose, not dense technical markdown with rule-separated sections and reference tables. The fix is a shell wrapper that builds an inline `<style>` block via a heredoc-mktemp'd header file and passes it to pandoc via `--include-in-header`. CSS cascade is favorable — the header-included styles follow pandoc's default stylesheet in `<head>`, so same-specificity body-level selectors in the custom styles override the defaults (verified: `max-width: 860px` at line 229 of the rendered output follows pandoc's `max-width: 36em` at line 15). Script defaults to `docs/tutorial.md → docs/tutorial.html` but accepts arbitrary `[INPUT] [OUTPUT]` positional overrides, so the same wrapper works for future tutorials or other GFM documents. Title is extracted from the first `# ` heading of the input so pandoc's `--standalone` mode doesn't warn "this document has no title." Temp header file is cleaned via `trap … EXIT` so failed renders don't leak files into `/tmp`.

- **Added:** `scripts/render_tutorial.sh` (89 lines, `chmod +x`) — shebang `#!/usr/bin/env bash`, `set -euo pipefail`, positional `[INPUT] [OUTPUT]` args with defaults, pandoc-availability check (exits 1 with installation URL if missing), input-file-exists check, title extraction from first H1, heredoc-to-mktemp'd `<style>` block covering BACKLOG-named concerns (body width 860px, hr margins 2.5em, table `border-collapse` with 1px `#d0d7de` borders + padding) plus code-block readability (light-grey `pre` background, 6px `border-radius`, `overflow-x: auto` so long commands don't break layout), `trap` cleanup of the header file. Pandoc invocation uses `--from=gfm --to=html5 --standalone --toc --toc-depth=2 --metadata=title="$TITLE" --include-in-header="$HEADER_FILE" --output="$OUTPUT"`.
- **Added:** `.gitignore` entry `docs/tutorial.html` — the rendered artifact is a local reading convenience, not a checked-in asset. `docs/tutorial.md` remains the source of truth; contributors regenerate on demand via the script.
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — "Add `scripts/render_tutorial.sh` — Wrap the pandoc invocation with inline CSS (body width, hr margins, table borders) so rendering the tutorial to HTML is a one-liner" (now shipped). BACKLOG net change: 5 `[ ]` → 4 `[ ]`, 0 `[x]` → 0 `[x]`.
- **Verified:** smoke-test render of `docs/tutorial.md` → `docs/tutorial.html` (72,597 bytes) via `./scripts/render_tutorial.sh` succeeded with `rendered docs/tutorial.md -> docs/tutorial.html` stdout line. CSS sentinels confirmed in rendered HTML (`max-width: 860px`, `border: 1px solid #d0d7de`, `margin: 2.5em 0` all present). Title inferred correctly (`<title>Tutorial: Running the Model Project Constructor Pipeline</title>`). `git check-ignore -v docs/tutorial.html` confirmed the `.gitignore` rule matches — the rendered artifact does not appear in `git status --short`. `uv run pytest -q` → **475/475 passing** (unchanged — shell script adds no Python code paths). Coverage **97.19%** (unchanged). `uv run ruff check src/ tests/ packages/` → `All checks passed!`. `uv run mypy src/` → `Success: no issues found in 48 source files`. `uv run mypy packages/data-agent/src/` → `Success: no issues found in 12 source files`.
- **Unchanged intentionally:** no changes to `docs/tutorial.md` content (its BACKLOG item — split multi-command code blocks for per-block copy buttons — is a separate item for a future session). No pytest coverage for the shell script (project convention: shell utilities in `scripts/` are smoke-tested, not unit-tested; `scripts/run_pipeline.py` is the precedent — it has integration tests via `tests/scripts/` but no isolated unit tests for the argparse/main scaffolding, and it is Python, not bash). No package-shell for pandoc — the system pandoc on PATH is what contributors use (verified `pandoc 3.1.1` on the author's macOS). `scripts/` directory untouched aside from the new file (`run_pipeline.py` unchanged).
- **Next:** 4 `[ ]` BACKLOG items remain (numerical order per operator directive): (2) tutorial UX — split multi-command code blocks for per-block copy UX + consider MkDocs/Docusaurus for per-block copy buttons, (3) intake agent data-source discovery prompts, (4) data-agent metadata discovery mode, (5) statistical terminology glossary. Optional plan Phase 4 (UI writes `IntakeReport.json` envelope) stays deferred unless a pilot operator demands the UI → `--resume` workflow.

### 2026-04-19 — Data-agent JSON parser tolerates prose around markdown fences (Session 53)

Fixes BACKLOG item 1 (the second finding from Session 51's live-LLM round-trip, `run_id=run_b1_resume_live_1776570556`). `claude-sonnet-4-6` returned the quality-check list wrapped in ` ```json … ``` ` markdown fences, and the data-agent's `_extract_json` helper raised `LLMParseError` because its fence-strip regex required the entire stripped response to be a single fenced block (`^…$` anchors). In practice, models sometimes add prose before or after the fence (an introductory sentence, a trailing explanation) — the anchored regex did not match, the fence passed through to `json.loads`, and the data agent's graph crashed with `EXECUTION_FAILED`. Session 53's fix restructures `_extract_json` into a two-phase parse: try bare `json.loads` first (fast path for already-clean JSON) and, on `JSONDecodeError`, search for a fenced block anywhere in the response and retry with its contents. Fence-search uses `re.search` without anchors so prose before and/or after the fence no longer blocks parsing. The bare-parse error is preserved as the cause of any final `LLMParseError` so debug output names the real decode failure, not a synthetic one.

- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/anthropic_client.py` — `_extract_json` restructured to bare-parse-first + fence-fallback. `_CODE_FENCE` regex loosened from `^\`\`\`(?:json)?\s*\n(.*?)\n\`\`\`\s*$` (anchored, match-only-entire-response) to `\`\`\`(?:json)?\s*\n?(.*?)\n?\`\`\`` (unanchored, searchable; optional newlines on both sides of the fenced content). Module docstring updated to describe the new parse order and cite the Session 51 `run_id` as the tracked bug. Docstring of `_extract_json` itself names the invariant: bare-parse-first, fence-search-fallback, raise only if both fail.
- **Added:** 5 regression tests in `tests/data_agent_package/test_anthropic_client.py` — `test_extract_json_prose_before_fence` (the original bug shape), `test_extract_json_prose_after_fence` (trailing explanation), `test_extract_json_prose_before_and_after_fence` (both sides), `test_extract_json_fence_without_language_tag_and_prose` (``` ``` without `json` tag + leading prose), `test_extract_json_bare_json_still_parses` (fast-path regression: bare JSON with leading/trailing whitespace + bare arrays). Each test of the three originally-broken shapes cites `run_id=run_b1_resume_live_1776570556` in its docstring so future readers can trace the fix to its production evidence.
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — "Data agent JSON parser rejects markdown-fenced output from sonnet-4-6" (now fixed). BACKLOG net change: 6 `[ ]` → 5 `[ ]`, 0 `[x]` → 0 `[x]`.
- **Verified:** `uv run pytest -q` → **475/475 passing** (was 470 pre-session; +5 new regression tests, -0 test removals). Coverage **97.19%** with `Required test coverage of 95% reached` — floor holds (2.19-point headroom; 0.08 drop from Session 52's 97.27 reflects the new helper's nested `json.JSONDecodeError: pass` branch in the fence-fallback path, which is exercised only when a fenced block exists AND its contents are themselves malformed — not reachable in these tests but kept as defensive code). `uv run ruff check src/ tests/ packages/` → `All checks passed!`. `uv run mypy src/` → `Success: no issues found in 48 source files`. `uv run mypy packages/data-agent/src/` → `Success: no issues found in 12 source files`.
- **Unchanged intentionally:** no prompt changes to the data-agent `_call_claude` prompts. The defensive parser-side fix is more robust than a prompt-side fix (model versions drift; no prompt phrasing reliably eliminates fences across all models and temperature settings). `model_project_constructor.agents.data` shim and `DataAgent` graph flow untouched. Sibling `src/model_project_constructor/agents/intake/anthropic_client.py` has its own `_extract_json` variant — NOT touched this session because the intake agent has not been observed emitting prose-around-fence responses and the Session 51 evidence is specific to the data-agent call path. Future session can apply the same fix to the intake parser if a similar bug surfaces; the fix shape is general.
- **Next:** 5 `[ ]` BACKLOG items remain. Natural next candidates: tutorial UX (split multi-command code blocks into individually-copyable blocks), `scripts/render_tutorial.sh` (pandoc wrapper), intake data-source discovery prompts, data-agent metadata discovery mode, statistical terminology glossary. Optional plan Phase 4 (UI writes checkpoint envelope) stays deferred unless a pilot operator demands the UI → `--resume` workflow.

### 2026-04-19 — Status-aware resume demotion: FAILED envelopes re-execute their stage (Session 52)

Fixes BACKLOG item 1 (filed by Session 51's live-LLM round-trip, `run_id=run_b1_resume_live_1776570556`). `determine_resume_point` previously used only `CheckpointStore.has()` existence checks, so a `DataReport.json` on disk with `status=EXECUTION_FAILED` — the halt artifact of a `FAILED_AT_DATA` run — was treated as a completed handoff and the resume point returned `"website"`, handing the FAILED report to the website agent. The symmetric bug existed for `IntakeReport` with `status=DRAFT_INCOMPLETE`. Session 52's fix: `determine_resume_point` loads saved `IntakeReport` / `DataReport` payloads and demotes the resume point to `"intake"` / `"data"` when the saved status is not `"COMPLETE"`. `ResumeInconsistent` contract stays narrow (missing-predecessor cases only) — a FAILED envelope is a legitimate halt artifact, not structural corruption. `RepoProjectResult` is NOT symmetric: its FAILED case is handled by `_handle_already_complete` with opt-in-to-retry UX because the website stage has irreversible side effects (GitLab/GitHub project creation).

- **Changed:** `src/model_project_constructor/orchestrator/pipeline.py` — `determine_resume_point` restructured so that (a) `RepoProjectResult` existence short-circuits before any payload load (keeps the hot path free of deserialization when a terminal result is present; preserves Session 51 CLI tests that seed stub `{"status": "COMPLETE"}` envelopes), (b) `IntakeReport` / `DataReport` status is loaded and checked only at the branch points where it materially affects the resume decision, (c) a non-`COMPLETE` saved payload demotes to the re-execution point for that stage. Added `_is_saved_payload_complete(store, run_id, payload_type)` private helper. Docstring updated to cross-reference plan §11 risk #8 and explain the asymmetry with `RepoProjectResult`.
- **Changed:** `tests/orchestrator/test_pipeline.py` — migrated S1–S5 tests in `TestDetermineResumePoint` from `_touch_checkpoint` (writes empty `{}` JSON) to `_seed_envelope` (writes real `HandoffEnvelope` with valid payloads), because the pure-function tests now exercise a path that loads the payload to check status. S0 and INVALID A/B/C tests stay on `_touch_checkpoint` (they fire before any load attempt). Helpers `_incomplete_data_report` / `_draft_incomplete_intake` pre-existed from `TestHaltBehavior` — reused rather than duplicated.
- **Added:** two regression tests in `TestDetermineResumePoint` — `test_failed_data_report_demotes_to_data` (seeds `IntakeReport` COMPLETE + `DataRequest` + `DataReport` with `status=EXECUTION_FAILED` → asserts resume point is `"data"` not `"website"`) and `test_draft_incomplete_intake_demotes_to_intake` (seeds `IntakeReport` with `status=DRAFT_INCOMPLETE` alone → asserts `"intake"`). Each test cites `run_id=run_b1_resume_live_1776570556` as the original production evidence so future readers can trace the fix to its discovery.
- **Changed:** `docs/planning/resume-from-checkpoint-plan.md` §11 — added risk #8 ("the inverse of #5") documenting the bug, its observation in Session 51's live-LLM round-trip, and the Session 52 fix including the `RepoProjectResult` asymmetry rationale and the `ResumeInconsistent` contract narrowing.
- **Removed:** one BACKLOG.md line per Learning #26 (delete, do NOT flip to `[x]`) — "Resume of FAILED data stage skips re-execution" (now fixed). BACKLOG net change: 7 `[ ]` → 6 `[ ]`, 0 `[x]` → 0 `[x]`.
- **Verified:** `uv run pytest -q` → **470/470 passing** (was 468 pre-session; +2 new regression tests, -0 test removals). Coverage **97.27%** with `Required test coverage of 95% reached` — floor holds (2.27-point headroom; 0.06 drop from Session 51's 97.33 is the new `_is_saved_payload_complete` helper's defensive branch for the impossible-under-normal-run-semantics `request_present` + non-`COMPLETE` intake case). `uv run ruff check src/ tests/ packages/` → `All checks passed!`. `uv run mypy src/` → `Success: no issues found in 48 source files`. `orchestrator/pipeline.py` coverage: 100% on 98 statements + 32 branches (was 92 stmts + 32 branches pre-session).
- **Unchanged intentionally:** `CheckpointStore` API surface untouched. `run_pipeline` execution path untouched (Session 50's Phase 2 wiring still honors `resume_from` as before; status-awareness is entirely inside `determine_resume_point`). CLI-layer `_handle_already_complete` untouched (its opt-in-to-retry UX for FAILED `RepoProjectResult` is the right asymmetric treatment because website has irreversible side effects; auto-retry of website would create a second GitLab project). Plan §5 truth table unchanged (its rows still hold under the strengthened semantics; §11 risk #8 names the new invariant). Second Session 51 finding ("data agent JSON parser rejects markdown-fenced output from sonnet-4-6") remains open — it is a separate bug class in a different module and deserves its own session.
- **Next:** 6 `[ ]` BACKLOG items remain. The data-agent markdown-fence bug (Session 51 finding #2) is the natural follow-up since it was filed together with this one and is in the same subsystem. Other options: tutorial UX (split code blocks), `scripts/render_tutorial.sh`, agent enhancements (intake data-source discovery, data-agent metadata discovery), statistical terminology glossary, or the plan §7.4 OPTIONAL Phase 4 (UI writes checkpoint envelope).

### 2026-04-18 — Resume-from-checkpoint Phase 3: `--resume` CLI flag + OPERATIONS + tutorial (Session 51)

Closes the resume-from-checkpoint plan's critical path (Phases 1–3) per `docs/planning/resume-from-checkpoint-plan.md` §7.3 + §14. `scripts/run_pipeline.py --resume <run_id>` resolves the resume point via `determine_resume_point` (Phase 1) and threads it into `PipelineConfig.resume_from` (Phase 2); `run_pipeline` then loads every envelope before the resume point and re-executes from that point onward. Operator-facing exit codes/messages match plan §7.3.2 verbatim. BACKLOG loses two lines — the "Automated resume-from-checkpoint" item (now shipped) and the "B-3 Web UI bridge" item (per §1.3 supersession: once `--resume` exists, the UI → pipeline handoff becomes "UI writes `IntakeReport.json` envelope, operator invokes `--resume`," with no bespoke `--resume-intake` flag needed). Phase 4 (UI writes the envelope) remains plan-§7.4 OPTIONAL and is deferred unless a pilot operator demands the UI-driven workflow.

- **Added:** `scripts/run_pipeline.py` — `--resume <run_id>` argparse flag (overrides `--run-id` per plan §11 risk #3); three inline helpers (`_resume_preflight` for the S0 "no checkpoints" rejection, `_handle_already_complete` for the S5 COMPLETE/FAILED branching that reads the saved `RepoProjectResult.result.json` to distinguish, `_resolve_resume` orchestrating both + `ResumeInconsistent` translation); `_SKIPPED_STAGES_BY_RESUME_POINT` mapping for the "Skipping: ..." banner. The resumed `run_id` overrides the auto-generated one before the banner prints, so the operator-facing log shows the actual run being continued. Imports `CheckpointStore`, `ResumeInconsistent`, `ResumePoint`, `determine_resume_point` from the orchestrator package (all three are public re-exports landed by Sessions 49/50).
- **Added:** `tests/scripts/test_run_pipeline_resume.py` — 7 tests covering argparse acceptance, `_resume_preflight` rejection (exit 2 + "no checkpoints" stderr message), `ResumeInconsistent` translation (DataReport without DataRequest), `already_complete` COMPLETE → exit 0 with `project_url`, `already_complete` FAILED → exit 2 with retry recipe naming the result-file path, `_resolve_resume` returns "data" for an S2-seeded checkpoint dir, end-to-end `main()` integration that monkeypatches `run_pipeline` to capture the constructed `PipelineConfig` and asserts `config.resume_from == "data"` + `config.run_id == <resumed id>` + the resume banner ("RESUMED from: data" + "Skipping: intake, intake_to_data_adapter") prints to stdout. Loaded via the existing `importlib.util` pattern from `test_run_pipeline_adapter.py` (no package promotion).
- **Changed:** `OPERATIONS.md §5` rewrite — replaces the manual `CheckpointStore.load_payload` recipe with the `--resume` walkthrough as §5.1 (table of disk-state → resume-at → operator-outcome rows; resume banner example; precedence rule that operator-supplied `MPC_NAMESPACE`/`MPC_HOST_URL` always wins per plan §6.4). The original manual snippet is preserved as §5.2 ("Manual fallback (unusual cases)") for one-off REPL-driven recovery work that doesn't fit the `--resume` envelope.
- **Added:** `docs/tutorial.md` Step 7 ("Resuming a partial run") inserted before the existing "Using the orchestrator programmatically" section (renumbered to Step 8). Includes a full `--resume` invocation example, the resume banner shape, and cross-references to OPERATIONS §5 + plan §5 truth table.
- **Changed:** `README.md:126` — extends the runbook reference to namedrop `--resume <run_id>` and point at OPERATIONS §5 (the README has no per-script CLI flag summary, so this paragraph is the natural single-line surface per plan §7.3.1).
- **Removed:** two BACKLOG.md lines per Learning #26 (delete, do NOT flip to `[x]`) — "Automated resume-from-checkpoint" (now shipped via Phases 1–3) and "B-3 (optional): Web UI bridge" (superseded; the UI writes the envelope and the operator invokes `--resume` instead). BACKLOG net change: 7 `[ ]` → 5 `[ ]`, 0 `[x]` → 0 `[x]`.
- **Verified:** `uv run pytest -q` → **468/468 passing** (was 461 pre-session; +7 new CLI tests). Coverage **97.33%** with `Required test coverage of 95% reached` — floor holds (2.33-point headroom). `uv run ruff check src/ tests/ packages/` → `All checks passed!`. `uv run mypy src/` → `Success: no issues found in 48 source files`. CLI smoke: `scripts/run_pipeline.py --resume nonexistent_smoke_test_id` → exit 2 with `Run 'nonexistent_smoke_test_id' has no checkpoints at .orchestrator/checkpoints/nonexistent_smoke_test_id. Start a new run without --resume.` Synthetic round-trip captured: `--run-id resume_smoke_synth --checkpoint-dir /tmp/mpc_resume_smoke` (fake mode) → COMPLETE; second `--resume resume_smoke_synth` on the COMPLETE run → exit 0 + "already complete" message + project URL `https://fake.host.test/data-science/model-drafts/subrogation-pilot`; delete `RepoProjectResult.result.json` then re-`--resume` → banner shows "RESUMED from: website / Skipping: intake, intake_to_data_adapter, data" and the website stage runs alone → COMPLETE.
- **Unchanged intentionally:** `CheckpointStore` API surface (no `load_result` reader added — the CLI's `_handle_already_complete` reaches the result file via `store._result_path` with a `noqa: SLF001`; if a future caller needs symmetric API, that's a separate session). `intake_report_to_data_request` adapter unchanged (S2 still loads the saved `DataRequest` per plan §6.3 instead of re-deriving). LangGraph intra-agent checkpointers (`MemorySaver`/`SqliteSaver`) untouched per plan §10 anti-scope. Plan's Phase 4 (UI writes `IntakeReport.json` envelope) NOT implemented — remains §7.4 OPTIONAL.
- **Live-LLM round-trip captured (Session 51 addendum, 2026-04-18):** Operator authorized a live B1 round-trip post-commit. Run #1: `uv run python scripts/run_pipeline.py --live --host gitlab --llm data --model claude-sonnet-4-6 --db-url sqlite:///does-not-exist.db --run-id run_b1_resume_live_1776570556` halted at `FAILED_AT_DATA` after 58 s of data-agent time as plan §13 predicted, saving `IntakeReport.json` + `DataRequest.json` + `DataReport.json` (with `status=EXECUTION_FAILED`). Cost ~$0.05–0.10. Run #2 (`--resume`) was NOT executed: pre-resume inspection of the saved `DataReport.json` surfaced two bugs (filed as new BACKLOG items, see below) that would have caused the resume to either crash or generate a broken GitLab project; Session 51 stopped the round-trip and filed the findings rather than burn additional LLM cost on already-known-broken paths. The synthetic fake-mode round-trip remains the end-to-end exit-code proof.
- **Findings from the live round-trip — filed to BACKLOG:** (1) `determine_resume_point` returns `"website"` whenever `DataReport.json` exists on disk, regardless of the saved report's `status`. A run that halted at `FAILED_AT_DATA` therefore resumes by *skipping* the data stage and handing the FAILED report to the website agent — the mirror image of plan §11 risk #5, which named the "loaded-data wrongly triggers halt" risk but missed this inverse. (2) `claude-sonnet-4-6` returned the data agent's quality-check list wrapped in ` ```json … ``` ` markdown fences; the data-agent's JSON parser raised `Expecting value: line 1 column 1 (char 0): '```json\n[...'` and the agent graph crashed with `EXECUTION_FAILED`. Either the prompt forbids fences explicitly or the parser strips a fence before `json.loads`. Repro: the run-#1 invocation above.
- **Next:** plan critical path complete (Phases 1–3 shipped across Sessions 49/50/51). Two new BACKLOG items filed from the live round-trip (see above). Future session: pick any of 7 `[ ]` BACKLOG items, or optional plan Phase 4 (UI writes envelope) per plan §7.4.

### 2026-04-18 — Resume-from-checkpoint Phase 2: `run_pipeline` honors `resume_from` (Session 50)

Executes Phase 2 of `docs/planning/resume-from-checkpoint-plan.md` per plan §7.2. Wires `PipelineConfig.resume_from` into `run_pipeline`'s execution path: stages earlier than the resume point load their envelopes from disk via `CheckpointStore.load_payload`; the resume point and every stage after it re-executes. Halt semantics preserved — `FAILED_AT_*` only fires when the stage actually ran (plan §11 risk #5). `PipelineResult.resume_point` is populated on every return path (success, failure, or `already_complete` refusal) so operators can see whether a failed run originated inside a resumed invocation. Phase 3 (`--resume` CLI + OPERATIONS) is a separate session per plan §14.

- **Added:** `src/model_project_constructor/orchestrator/pipeline.py` — `PipelineConfig.resume_from: ResumePoint | None = None` (new field, default preserves pre-Phase-2 behavior); `PipelineResult.resume_point: ResumePoint | None = None` (propagated on every return path); load-or-execute branch logic in `run_pipeline` for the intake, adapter, and data stages; `ValueError` refusal when `resume_from == "already_complete"` (Phase 3 CLI will translate this to an operator-facing exit code); `typing.cast` used on `CheckpointStore.load_payload` results (the registry is the single source of truth for envelope-type mapping, zero runtime cost).
- **Added:** `TestRunPipelineResume` class in `tests/orchestrator/test_pipeline.py` — 6 tests: default-behavior regression under `resume_from=None`, S1/S2/S3 load-or-execute cases, `halt_under_resume_records_resume_point_on_failure` (plan §11 risk #5 regression), and `already_complete_raises_value_error`. The S2 case pins §6.3's "loaded request is ground truth" decision by seeding a tweaked `source_ref` on the disk envelope and asserting the data runner sees the loaded value, not a fresh adapter derivation.
- **Added:** `_seed_envelope` + `_make_resume_config` test helpers for resume-execution tests. `_seed_envelope` writes a real `HandoffEnvelope` (so `load_payload` can resolve the payload through the schema registry), distinct from `_touch_checkpoint` (for pure-function existence-only tests).
- **Verified:** `uv run pytest -q` → **461/461 passing** (was 455 pre-session; +6 new tests). Coverage **97.33%** with `Required test coverage of 95% reached`. `src/model_project_constructor/orchestrator/pipeline.py` → **100% coverage** on the 92 statements + 32 branches (up from 82 stmts + 24 branches pre-session — +10 stmts + 8 branches, all exercised). `uv run ruff check src/ tests/ packages/` → `All checks passed!`. `uv run mypy src/` → `Success: no issues found in 48 source files`.
- **Unchanged intentionally:** `scripts/run_pipeline.py` is untouched — Phase 3 adds the `--resume` CLI flag. `OrchestratorSettings` (`config.py`) untouched. LangGraph intra-agent checkpointers (`MemorySaver`, `SqliteSaver`) untouched per plan §10 anti-scope. BACKLOG "Automated resume-from-checkpoint" line stays open until plan's Phase 3 per §7.3.1.
- **Next:** Phase 3 of `resume-from-checkpoint-plan.md` (§7.3) — add `--resume <run_id>` argparse flag, rewrite `scripts/run_pipeline.py:main()` to resolve the resume point via `determine_resume_point` and construct `PipelineConfig` with the resolved value, OPERATIONS.md §5 rewrite, `docs/tutorial.md` §7, 4–5 new CLI tests, BACKLOG -2 lines, one live resume round-trip documented with its `run_id`.

### 2026-04-18 — Resume-from-checkpoint Phase 1: `determine_resume_point` pure function (Session 49)

Executes Phase 1 of `docs/planning/resume-from-checkpoint-plan.md` per plan §7.1. Lands the pure truth-table function + its exception class + 9 unit tests in isolation — `run_pipeline` is intentionally unchanged so the Phase 1 diff is small and reviewable. Phase 2 (wiring `resume_from` into `run_pipeline` execution) is a separate session per plan §14.

- **Added:** `src/model_project_constructor/orchestrator/pipeline.py` — `ResumePoint` `Literal["intake", "intake_to_data_adapter", "data", "website", "already_complete"]`, `ResumeInconsistent(RuntimeError)` exception with specific error messages naming the missing predecessor envelope, and `determine_resume_point(store, run_id) -> ResumePoint` pure function implementing plan §5's truth table (S0–S5 valid rows + 3 INVALID raise paths). `RepoTarget` (`T`) is deliberately NOT consulted per plan §6.4 — config wins on resume.
- **Added:** re-exports of `ResumePoint`, `ResumeInconsistent`, `determine_resume_point` from `model_project_constructor.orchestrator/__init__.py`.
- **Added:** `TestDetermineResumePoint` class in `tests/orchestrator/test_pipeline.py` — 9 tests, one per truth-table row (6 valid + 3 INVALID). Uses a real `CheckpointStore` with `tmp_path` + a `_touch_checkpoint` helper that writes empty-JSON files at the `has()` / `has_result()` inspection paths (no envelope-construction overhead since the function only reads file existence).
- **Verified:** `uv run pytest -q` → **455/455 passing** (was 446 pre-session; +9 new tests). Coverage **97.31%** with `Required test coverage of 95% reached`. `src/model_project_constructor/orchestrator/pipeline.py` → **100% coverage** on the 82 statements + 24 branches. `uv run ruff check src/ tests/ packages/` → `All checks passed!`. `uv run mypy src/` → `Success: no issues found in 48 source files`.
- **Unchanged intentionally:** `run_pipeline` is untouched — Phase 2 adds `PipelineConfig.resume_from` and the skip-on-resume branch logic; Phase 1 deliberately lands the pure function in isolation. No CLI changes (Phase 3). No UI changes (Phase 4, optional). BACKLOG "Automated resume-from-checkpoint" line stays open until plan's Phase 3 per §7.3.1.
- **Next:** Phase 2 of `resume-from-checkpoint-plan.md` (§7.2) — add `PipelineConfig.resume_from` field, branch `run_pipeline` to load envelopes up to the resume point and re-execute from there, 5–6 new `TestRunPipelineResume` tests.

### 2026-04-18 — Resume-from-checkpoint plan (Session 48)

Planning-workstream deliverable per Learning #18 (plan IS the deliverable; implementation happens in later sessions). `docs/planning/resume-from-checkpoint-plan.md` (768 lines, 17 sections) covers BACKLOG item "Automated resume-from-checkpoint" and explicitly supersedes the B-3 Web UI bridge (scope-b-plan.md:309). Structured in 4 phases — Phase 1 `determine_resume_point` pure function, Phase 2 `run_pipeline` resume branching, Phase 3 `--resume` CLI + OPERATIONS + tutorial (also removes both BACKLOG lines), Phase 4 (optional) UI writes `IntakeReport.json` envelope. Critical path = 3 sessions; Phase 4 optional.

- **Added:** `docs/planning/resume-from-checkpoint-plan.md` — plan with evidence inventory (§3, grep counts for `CheckpointStore`/`PipelineConfig`/`run_pipeline`/CLI flags/`checkpoint_dir`), resume truth table (§5, cases S0–S5 + 3 INVALID keyed off envelope existence on disk), six architecture decisions (§6, notably §6.3 load-vs-re-derive, §6.4 terminal handling), per-phase implementation plans with files/LOC/shape/completion-criteria/verification commands (§7), five operator decisions resolved at review gate (§8), anti-scope (§10), risks (§11), test strategy (§12), per-phase session boundaries (§14), file reference map (§15), sign-off checklist (§16).
- **Resolved at review gate:** §8.1 reject `--resume` with no checkpoints; §8.2 no-op on `COMPLETE`, error on `FAILED`; §8.3 config `RepoTarget` wins over disk; §8.4 flag name is `--resume`; §8.5 Phase 4 is optional (not critical path).
- **Supersedes:** B-3 Web UI bridge from `docs/planning/scope-b-plan.md §7.3`. Phase 3 of this plan removes both the "Automated resume-from-checkpoint" and "B-3" lines from `BACKLOG.md`.
- **Verified:** plan self-consistent against current codebase (file:line citations for `CheckpointStore` at `checkpoints.py:13-16`, `97-100`, `91-95`, `102-105`; `run_pipeline` at `pipeline.py:97-209`, `:114`; `intake_report_to_data_request` at `adapters.py:44-77`; OPERATIONS.md §5 at `:329-353`; scope-b-plan.md §10 at `:619-637`, §14 item 3 at `:721`). Grep counts all re-run in Phase 0 (Learning #19 discipline).
- **Unchanged intentionally:** no production code touched this session (plan-session discipline per Learning #18). BACKLOG.md line for "Automated resume-from-checkpoint" stays open — it closes during Phase 3 per plan §7.3.1.
- **Next:** future session picks up Phase 1 (`determine_resume_point` pure function + 9 truth-table tests). Each of plan's 4 phases is a separate session per §14.

### 2026-04-18 — Coverage floor bump 94% → 95% (Session 47)

Raises the pytest coverage floor by one point. No-cost at current actual coverage (97.27%). Canonical change at `pyproject.toml:61`; five doc mirrors updated so maintainer-facing + audience-facing references stay consistent. Wiki Changelog history-row backfill lands as a follow-up commit once this commit's sha is known (mirrors the Session 20B backfill pattern for `c3943a8`).

- **Changed:** `pyproject.toml:61` — `--cov-fail-under=94` → `95` (canonical).
- **Changed:** `docs/wiki/claims-model-starter/Contributing.md:86-87`, `docs/wiki/claims-model-starter/Changelog.md:141`, `docs/wiki/claims-model-starter/Monitoring-and-Operations.md:91`, `README.md:124` — doc mirrors updated `94` → `95`. README.md "422 tests" string also bumped to "440+" to reduce re-update churn (Session 38 F3 / Session 39 durable-lower-bound pattern).
- **Verified:** `uv run pytest -q` → **446/446 @ 97.27% coverage** with `Required test coverage of 95% reached` in the output. Floor holds with 2.27-point headroom.
- **Unchanged intentionally:** `CHANGELOG.md` / `SESSION_NOTES.md` / `docs/architecture-history/` historical prose (append-only; Learning #32 class); `Evolution.md` historical rows at `:49`, `:236`, `:277` and the earlier floor-bump rows at `Changelog.md:138-139` (historical-record entries in freshness-tracked file); `Contributing.md:135` commit-message example (`93% → 94%` shown as past bump). `wiki/Changelog.md:138-139` history-table row for the 94→95 bump is added in the follow-up commit.
- **Next:** no further floor bump planned; backlog items unchanged.

### 2026-04-18 — Phase 3 of Evolution plan: `wiki/Evolution.md` initial write (Session 46)

Closes the Evolution page + documentation convention plan per plan §7. Full-rewrite `docs/wiki/claims-model-starter/Evolution.md` (305 lines) organized thematically — concept origins / foundational architecture / intake-agent arc / data-agent arc / website-agent arc / platform-abstraction arc / methodology arc / current state — plus §9 chronological index (41 session rows) and a deliberately-omitted appendix. Explicit review gate per plan §4.6 / PROJECT_CONVENTIONS.md §4.7 engaged; operator approved after two rounds of revision (reclassification of Sessions 21/36 into Methodology arc, Sessions 40/41/42 into documentation-reorganization thread; expansion from 195 → 305 lines; addition of timeline table). Closes the full 3-phase Evolution plan (Phase 1 Session 44, Phase 2 Session 45, Phase 3 this session).

- **Added:** `docs/wiki/claims-model-starter/Evolution.md` — 305-line decision-arc narrative with PROJECT_CONVENTIONS.md §4.4 banner (commit `f09ea57`, after Session 45), per-file opener, 8 thematic sections, 41-row chronological index, deliberately-omitted appendix.
- **Added:** Evolution entries in `_Sidebar.md` (Reference section), `Home.md` (wiki-contents list), `Content-Recommendations.md` (Current wiki pages table).
- **Found:** Session 23 (`docs/planning/scope-b-plan.md`, 822 lines) exists in SESSION_NOTES.md but has no CHANGELOG entry — surfaced during timeline construction and documented in Evolution.md §9. Pre-Session-33 drift example; the kind of gap Learning #26's Phase-0 BACKLOG/CHANGELOG/ROADMAP check now prevents.
- **Verified:** plan §7.4 commands all pass — Evolution.md present, banner visible, `Deliberately omitted` grep → 2, wc -l → 305 (within plan's ~300-600 range), Sidebar/Home Evolution counts → 1 each. `uv run pytest -q` → **446/446 @ 97.27% coverage** (unchanged — wiki-only).
- **Unchanged intentionally:** no code changes; no upstream-methodology edits; no append-only-history edits; `docs/planning/evolution-page-plan.md` preserved (plan-doc stays for future reference).
- **Next:** Evolution plan fully shipped; BACKLOG line removed. Future Evolution rewrites follow PROJECT_CONVENTIONS.md §4 discipline.

### 2026-04-18 — Phase 2 of Evolution plan: archive directory + planning-doc move + banner + link updates (Session 45)

Executes Phase 2 of `docs/planning/evolution-page-plan.md` per plan §6. Creates `docs/architecture-history/` and `git mv`s 5 completed plans + `architecture-approaches.pdf` + `initial_purpose.txt` out of the repo root and `docs/planning/`, prepends the PROJECT_CONVENTIONS.md §3 archive banner (dated 2026-04-18) to each moved `.md` + `.txt` file, and updates 16 link references across 8 inward files + 3 outward wiki files. `scope-b-plan.md` (B3 pending) and `evolution-page-plan.md` (Phases 2-3 active) stay in `docs/planning/` per plan §4.2 Framing B tiebreaker. Phase 3 remains — separate session per plan §9.

- **Added:** `docs/architecture-history/` directory with 6 moved files: `architecture-plan.md`, `architecture-approaches.md`, `architecture-approaches.pdf`, `github-gitlab-abstraction-plan.md`, `pilot-readiness-audit.md`, `initial_purpose.txt`. All 5 markdown files + the `.txt` file carry the PROJECT_CONVENTIONS.md §3 archive banner dated 2026-04-18 as the first line.
- **Changed:** 16 link references updated to point at the archive path — `README.md` (4 hits: "authoritative design document" line 9, repo-layout comment line 108, Phase 4B §11 reference line 197, Documents-worth-reading entries lines 201-202); `OPERATIONS.md:6`; `TROUBLESHOOTING.md:6-7`; `ROADMAP.md` (3 hits: lines 5, 31, 80; plus `github-gitlab-abstraction-plan.md` reference at line 82); `CLAUDE.md` (4 `initial_purpose.txt` references at lines 35, 43, 47, 55); `BACKLOG.md:13` (`initial_purpose.txt` reference in the data-agent discovery-mode entry); `docs/wiki/claims-model-starter/Architecture-Decisions.md` (lines 3 and 39); `docs/wiki/claims-model-starter/Changelog.md:5` (phase-structure reference); `docs/wiki/claims-model-starter/Contributing.md:212` (expanded to reference both active and archived plan directories).
- **Changed:** `README.md:108` repo-layout snippet now shows both `docs/planning/` (active plans: `evolution-page-plan.md`, `scope-b-plan.md`) and `docs/architecture-history/` (archived concept-era plans) as sibling directories.
- **Changed:** `ROADMAP.md:81` + `docs/wiki/claims-model-starter/Contributing.md:195` left intentionally unchanged. `ROADMAP.md:81` correctly references `docs/planning/scope-b-plan.md` which stays in `docs/planning/` (B3 pending). Contributing.md:195 refers to WRITING NEW plans ("Cross-module refactors need a written plan in `docs/planning/`"), and active plans still go to `docs/planning/` by convention — the reference is semantically correct post-move.
- **Verified:** `ls docs/architecture-history/` shows all 6 files. `head -1` on each moved file shows the 2026-04-18 banner. Plan §6.5 link-break grep (`grep -rn "docs/planning/" --include="*.md" .` minus SESSION_NOTES + CHANGELOG + scope-b + evolution-page + archive self-references) returns 11 remaining hits, all classified: 5 in upstream-methodology `SESSION_RUNNER.md` (unchanged per plan §4.7 Interpretation A), 2 in `docs/methodology/PROJECT_CONVENTIONS.md` (describe active-plan convention by design), 1 in `docs/methodology/HOW_TO_USE.md` (upstream — do not edit), 1 in `docs/wiki/claims-model-starter/Contributing.md:195` (new-plan reference, correct), 2 in `docs/wiki/claims-model-starter/Changelog.md:117, 122` (historical release-note entries describing past state — see Learning #32 note). pytest 446/446 @ 97.27% coverage unchanged (doc/fs-only, no code touched).
- **Learning #32 validation:** Plan §6.5's "expect hits only in append-only history" criterion met in spirit but surfaced 2 hits at `docs/wiki/claims-model-starter/Changelog.md:117, 122` — freshness-tracked wiki changelog entries that describe past state ("Added: `docs/planning/architecture-plan.md`"). Same failure shape as Session 44's `contributor-facing → 0` conflict in root CHANGELOG: a grep-count criterion ignored that release-note-style historical prose references old paths in description. This is Learning #32's 2nd validation — coined in Session 45's Phase 3C (see SESSION_NOTES.md). Remediation: plan §3.2's outward-wiki inventory should have listed these 2 hits; §6.5 would then have pre-classified them as historical-description rather than link-break.
- **Unchanged intentionally:** no changes to `CHANGELOG.md` historical entries (append-only per plan §3.3), no changes to `SESSION_NOTES.md` historical narrative (append-only), no changes to upstream methodology files (`docs/methodology/README.md`, `HOW_TO_USE.md`, `ITERATIVE_METHODOLOGY.md`, `workstreams/*.md`). No Evolution.md write (Phase 3). No content-shape changes to moved files beyond the banner prepend.
- **Next:** Session 46 executes Phase 3 (`docs/wiki/claims-model-starter/Evolution.md` initial write) per plan §7. Full-rewrite narrative from CHANGELOG classification + SESSION_NOTES rationale extraction + thematic outline. Review gate per plan §4.6 — draft presented before commit.

### 2026-04-18 — Phase 1 of Evolution plan: project conventions + per-file openers + word-tightening (Session 44)

Executes Phase 1 of `docs/planning/evolution-page-plan.md` after operator confirmed all 9 §4 recommendations verbatim. Establishes the project-local maintainer/user documentation convention + Evolution.md update discipline in one new file, adds per-file openers to both CHANGELOG surfaces, and tightens `CHANGELOG.md:8` from "contributor-facing" to "maintainer-facing" to align with the three-surface split. Phases 2 and 3 remain — each is a separate session per plan §9.

- **Added:** `docs/methodology/PROJECT_CONVENTIONS.md` — 97-line project-local convention doc. Four sections: (1) inward vs outward documentation with user-supplied convention sentence; (2) three-surface "what changed" split table (CHANGELOG.md maintainer ledger / wiki/Changelog.md audience summary / wiki/Evolution.md decision arc); (3) planning-doc archive convention (tiebreaker for when a plan moves, banner template, scope boundaries); (4) Evolution.md update discipline (full rewrite, CHANGELOG-as-checklist, SESSION_NOTES-as-rationale, deliberately-omitted appendix, diff-against-prior, explicit review gate).
- **Added:** `CHANGELOG.md` per-file opener blockquote (line 3) pointing at `docs/wiki/claims-model-starter/Changelog.md` and `Evolution.md`.
- **Added:** `docs/wiki/claims-model-starter/Changelog.md` per-file opener blockquote (line 3) pointing at the maintainer ledger and sibling Evolution page.
- **Changed:** `CHANGELOG.md:10` (was line 8 pre-opener) — `"authoritative, contributor-facing record"` → `"authoritative, maintainer-facing record"`.
- **Verified:** `test -f docs/methodology/PROJECT_CONVENTIONS.md` → present (97 lines). `grep -c "maintainer-facing" CHANGELOG.md` → 1 (expected ≥1). pytest 446/446 @ 97.27% coverage unchanged. Both CHANGELOG surfaces show the opener in the first 10 lines.
- **Plan criterion §5.3 note:** `grep -c "contributor-facing" CHANGELOG.md` returned 1, not the expected 0. The remaining hit is at line 25 inside Session 43's append-only CHANGELOG entry describing the Session 43 state. Per plan §3.3 ("CHANGELOG.md historical entries: DO NOT UPDATE — append-only history"), the append-only convention takes precedence over the literal §5.3 grep criterion. Line-8 intent is satisfied; historical accuracy preserved.
- **Unchanged intentionally:** no upstream methodology files touched (README.md, HOW_TO_USE.md, ITERATIVE_METHODOLOGY.md, `workstreams/*.md` — imported framework material per `docs/methodology/README.md:253-255`). No planning-doc moves (Phase 2). No Evolution.md write (Phase 3). No link updates across wiki or inward docs (Phase 2). No changes to `wiki/Changelog.md` content shape (per plan Design Decision 5 — opener only).
- **Next:** Session 45 executes Phase 2 (archive directory + planning-doc move + banner + link updates) per plan §6.

### 2026-04-18 — Evolution page + documentation convention plan (Session 43)

Planning session. Produces `docs/planning/evolution-page-plan.md` — a 3-phase implementation plan for (a) a project-local maintainer/user documentation convention, (b) a physical archive split of completed planning docs, and (c) a new `wiki/Evolution.md` decision-arc narrative that addresses the "why is it like this?" gap between current-state wiki pages and the raw session log.

- **Added:** `docs/planning/evolution-page-plan.md` — ~650-line plan document. Evidence-based inventory (§3, 7 subsections), 9 open decisions awaiting operator sign-off (§4), 3 sequential implementation phases with per-phase completion criteria + verification commands + session boundaries (§5-§7), dependency graph (§9), outstanding risks with mitigations (§11), out-of-scope notes (§12).
- **Added:** BACKLOG.md item pointing at the plan (Phases 1-3 blocked on §4 operator sign-off).
- **Context:** Follow-on from Session 42's out-of-scope planning-doc MAX_QUESTIONS drift finding + multi-turn design discussion settling 5 load-bearing design decisions: (1) inward (maintainer) vs outward (wiki) documentation split with user-supplied convention sentence; (2) three-surface "what changed" split (CHANGELOG.md maintainer ledger / wiki/Changelog.md audience summary / wiki/Evolution.md decision arc); (3) user-triggered full-rewrite Evolution cadence with banner + CHANGELOG-as-checklist + SESSION_NOTES-as-rationale discipline; (4) planning-doc physical move to `docs/architecture-history/` with in-file banner; (5) wiki/Changelog.md content unchanged.
- **Verified:** evidence-based inventory greps completed (6 planning files enumerated; 4 outward-wiki references + ~15 inward references to `docs/planning/` catalogued with update disposition; `contributor-facing` single-hit at `CHANGELOG.md:8` confirmed; `docs/methodology/README.md` upstream-provenance discovered via License section at `:253-255` — drives §4.4 recommendation against editing upstream). pytest 446/446 @ 97.27% unchanged (plan-only, no code or doc behavior change to existing surfaces).
- **Unchanged intentionally:** no phase implementation. Plan is the deliverable; implementation is in subsequent sessions per `SESSION_RUNNER.md` §Planning Sessions. Existing wiki, CHANGELOG, and planning-doc contents unmodified.
- **Next:** Session 44 starts Phase 1 only after operator confirms §4 open decisions.

### 2026-04-18 — MAX_QUESTIONS drift: 4 wiki + TROUBLESHOOTING hits (Session 42)

Closes the BACKLOG "MAX_QUESTIONS drift: 4 additional wiki + TROUBLESHOOTING hits" item seeded by Session 41's end-of-session drift-class sweep. Four paste-ready one-phrase replaces; completes the post-Session-27 `state.py:57` `MAX_QUESTIONS = 10` → `20` lag across the wiki + TROUBLESHOOTING surfaces.

- **Changed:** `docs/wiki/claims-model-starter/Pipeline-Overview.md:11` — `"(max 10 questions)"` → `"(max 20 questions)"` in the ASCII pipeline diagram's intake annotation.
- **Changed:** `docs/wiki/claims-model-starter/Agent-Reference.md:45` — `"(tracked against 10-question cap)"` → `"(tracked against 20-question cap)"` in the `questions_asked` state-shape bullet.
- **Changed:** `docs/wiki/claims-model-starter/Agent-Reference.md:52` — `"**Max 10 questions**"` → `"**Max 20 questions**"` in the intake-agent Behavior list.
- **Changed:** `TROUBLESHOOTING.md:61` — `"10-question cap before converging"` → `"20-question cap before converging"` in the `DRAFT_INCOMPLETE` troubleshooting root-cause list.
- **Verified:** All 4 before-strings confirmed present before edit; re-grep (`10-question|10 question|hard 10|max 10 questions|Max 10 questions`, case-insensitive) against `*.md` after edit shows only historical references remain (CHANGELOG + SESSION_NOTES Session 41 entries; BACKLOG entry now removed). `uv run pytest -q` → **446/446 passing, coverage 97.27%** (unchanged — doc-only; no behavior change). Baseline against `src/model_project_constructor/agents/intake/state.py:57` (`MAX_QUESTIONS = 20`) re-confirmed.
- **Unchanged intentionally:** No ruff/mypy/`-W default` re-run (no code touched); Sessions 37/35 baselines stand. The Session 41 CHANGELOG entry is preserved verbatim — it documents the `protocol.py` fix and the deferral of these 4 hits, so its before-strings are historical context, not drift.
- **Out-of-scope drift noted for follow-up:** Wider case-insensitive grep (`max 10 questions|Max 10 questions|hard 10|10-question|10 question`) across all `.md` surfaced additional MAX_QUESTIONS lag in `docs/planning/architecture-plan.md` (at least 7 hits across §5/§8/§tables) and `docs/planning/pilot-readiness-audit.md:146`. Both are **point-in-time planning artifacts** (the architecture-plan was written pre-Session-27 bump; the pilot-readiness audit captured state on its own write-date), so they were deliberately excluded from the current drift-class scope. If the operator wants the planning docs to reflect current production values, that is a separate (larger) decision — noted here but not added to BACKLOG, since planning-doc freshness is a different convention than wiki/runtime-doc freshness.

### 2026-04-18 — `protocol.py:34` `NextQuestionResult` docstring MAX_QUESTIONS lag fix (Session 41)

Closes the BACKLOG "`protocol.py:34` docstring MAX_QUESTIONS lag" item (source-code twin of Session 40's F1 wiki fix). The `NextQuestionResult` docstring said "the hard **10-question** cap from §4.1" — drift from Session 27's `src/model_project_constructor/agents/intake/state.py:57` bump `MAX_QUESTIONS = 10` → `20`. One-word fix paired with out-of-scope drift-class finding (below).

- **Changed:** `src/model_project_constructor/agents/intake/protocol.py:34` — `"hard 10-question cap from §4.1"` → `"hard 20-question cap from §4.1"`. Verified against `state.py:57` (`MAX_QUESTIONS = 20`).
- **Verified:** `uv run pytest -q` → **446/446 passing, coverage 97.27%** (unchanged — docstring-only; no behavior change). `uv run ruff check src/ tests/ packages/` clean. `uv run mypy src/` clean on 48 source files (CI-matching scope per Learning #18).
- **Unchanged intentionally:** No other `protocol.py` content touched. No fixture or test changes — the docstring is not under test.
- **Out-of-scope drift noted for follow-up:** End-of-session grep `10-question|10 question|hard 10` across `src/` surfaced zero additional source-code hits, but `.md` sweep found 4 more drifts that Sessions 38 and 40 missed (those sweeps used different grep patterns — drift markers, hosts, test counts — not the literal `10-question` string): `docs/wiki/claims-model-starter/Pipeline-Overview.md:11` ("max 10 questions"), `docs/wiki/claims-model-starter/Agent-Reference.md:45` ("10-question cap") + `:52` ("Max 10 questions"), `TROUBLESHOOTING.md:61` ("10-question cap"). Added as a single BACKLOG item ("MAX_QUESTIONS drift: 4 additional wiki + TROUBLESHOOTING hits") for a future micro session. Per FM #2 scope discipline, NOT bundled into this session.

### 2026-04-18 — Wiki freshness sweep: 14 remaining `docs/wiki/claims-model-starter/` pages (Session 40)

Closes the BACKLOG "Wiki freshness sweep: 14 remaining pages" item. Audited the 14 pages not covered by Session 38 (`Architecture-Decisions`, `Changelog`, `Contributing`, `Data-Guide`, `Development-Workflow`, `Extending-the-Pipeline`, `Generated-Project-Structure`, `Glossary`, `Governance-Framework`, `Intake-Interview-Design`, `Monitoring-and-Operations` minus `:91`, `Security-Considerations`, `Software-Bill-of-Materials`, `Worked-Examples`) using Session 38's proven 3-pattern grep inventory (drift markers, stale host refs, stale test counts) plus per-page read. The inventory pass found 0 drift-marker hits across the wiki (Content-Recommendations restructure in Session 38 cleared them all) and confirmed the only `github.example.com/api/v3` reference (`Security-Considerations.md:111`) is the canonical GHE example, not drift. The per-page read surfaced 4 findings: 1 narrative-consistency drift (intake interview question-cap lag) + 1 env-var-name drift (tokens that never existed) + 2 count/length drifts in the Contributing page. 10 of the 14 target pages passed with zero changes.

- **Changed (F1, narrative consistency):** `docs/wiki/claims-model-starter/Intake-Interview-Design.md:94, 96` — "If 10 questions have been asked regardless" → "If 20 questions have been asked regardless", and "reaches question 10 without feeling complete will stop at question 10 anyway" → "reaches question 20 without feeling complete will stop at question 20 anyway". The page's own §2 table (line 63) already said `MAX_QUESTIONS = 20` (correct), but the §3 prose narrative still said "10 questions," a drift from Session 27's `src/model_project_constructor/agents/intake/state.py:57` bump 10 → 20. Verified current source: `state.py:57` is `MAX_QUESTIONS = 20`.
- **Changed (F2, env-var names):** `docs/wiki/claims-model-starter/Worked-Examples.md:232` — `GITLAB_PRIVATE_TOKEN` / `GITHUB_PRIVATE_TOKEN` → `GITLAB_TOKEN` / `GITHUB_TOKEN`. The `_PRIVATE_` form never existed as an env-var name anywhere in the project. Canonical names (per `scripts/run_pipeline.py:22`, `OPERATIONS.md`, `Security-Considerations.md:34-35`, `Monitoring-and-Operations.md:11-12`) are `GITLAB_TOKEN` / `GITHUB_TOKEN`.
- **Changed (F3, test-function count + subdir list):** `docs/wiki/claims-model-starter/Contributing.md:98` — "**120 test functions**" → "**131 test functions**" and added `scripts/` to the enumerated subdirectory list. Snapshot grew from 120 (Session 20B baseline) to 131 as Sessions 21-37 added tests; `tests/scripts/` (containing `test_run_pipeline_adapter.py`) was new since Session 20B wrote the page. Current count: `find tests -name "*.py" | xargs grep -h "^def test_" | wc -l` → 131.
- **Changed (F4, line-count citation):** `docs/wiki/claims-model-starter/Contributing.md:187` — `SESSION_RUNNER.md (306 lines)` → `SESSION_RUNNER.md (321 lines)`. SESSION_RUNNER.md grew from 306 lines (Session 20B) to 321 lines as Sessions 27-38 coined Learnings #21-#30. `SAFEGUARDS.md (183 lines)` and `CLAUDE.md (61 lines)` were also cited on the same page; both are still exact (verified via `wc -l`) and were left unchanged.
- **Verified:** `uv run pytest -q` → **446/446 passing, coverage 97.27%** (unchanged — doc-only). No ruff/mypy re-run (no code touched); Session 37's clean baseline stands. No `-W default` re-run (no sqlite-adjacent code touched); Session 35's 0-warnings baseline stands.
- **Unchanged intentionally:** 10 of the 14 target pages (`Architecture-Decisions`, `Changelog`, `Data-Guide`, `Development-Workflow`, `Extending-the-Pipeline`, `Generated-Project-Structure`, `Glossary`, `Governance-Framework`, `Monitoring-and-Operations` minus `:91`, `Security-Considerations`, `Software-Bill-of-Materials`). Per Session 38's convention: the audience-facing `Changelog.md` is a summary, not a complete log, so its not covering post-Session-20B entries is a deliberate design choice (line 165 of the wiki page documents this) — not drift. `Security-Considerations.md:111`'s `https://github.example.com/api/v3` is the canonical GHE example URL (confirmed by Session 38's analysis) and matches the shape cited elsewhere in the wiki.
- **Out-of-scope drift noted for follow-up:** `src/model_project_constructor/agents/intake/protocol.py:34` docstring for `NextQuestionResult` says "the hard **10-question** cap from §4.1" — the same Session-27 MAX_QUESTIONS lag as F1 but in source code, not wiki scope. Added to BACKLOG as a separate follow-up.

### 2026-04-18 — `Monitoring-and-Operations.md:91` stale test count fixed (Session 39)

Closes the "Monitoring-and-Operations.md staleness sweep" BACKLOG item for its single-line subscope (the micro fix called out in Session 38 gotcha #9). Same phrasing convention Session 38 applied to `Getting-Started.md:34`. The broader 14-page wiki scan remains open in BACKLOG as a separate item.

- **Changed:** `docs/wiki/claims-model-starter/Monitoring-and-Operations.md:91` — CI-jobs table row `| **Tests** | \`pytest -q\` (422+ tests, >94% coverage) |` → `| **Tests** | \`pytest -q\` (440+ tests, >94% coverage) |`. Current actual: 446 tests @ 97.27%; `440+` chosen as a slightly-durable lower bound to reduce re-update churn (same rationale as Session 38's F3).
- **Verified:** `uv run pytest -q` → **446/446 passing, coverage 97.27%** (unchanged — doc-only). No ruff/mypy re-run (no code touched); Session 37's clean baseline stands. No `-W default` re-run (no sqlite-adjacent code touched); Session 35's 0-warnings baseline stands.
- **Unchanged intentionally:** The `>94% coverage` floor in the same row was kept as-is — it is a durable CI lower bound, not a current-state claim, and Session 38's F3 choice preserved the analogous `>94%` phrasing in `Getting-Started.md` for the same reason. The 14 wiki pages outside Session 38's original scope (Architecture-Decisions, Changelog, Contributing, Data-Guide, Development-Workflow, Extending-the-Pipeline, Generated-Project-Structure, Glossary, Governance-Framework, Intake-Interview-Design, Security-Considerations, Software-Bill-of-Materials, Worked-Examples, plus the rest of Monitoring-and-Operations.md) are out of scope per "1 and done"; the BACKLOG item now covers only the broader 14-page scan.

### 2026-04-18 — Wiki freshness sweep: `docs/wiki/claims-model-starter/` (Session 38)

Closes the BACKLOG "Wiki freshness sweep" item. Audited the 6 pages named in the BACKLOG entry (`Home.md`, `Pipeline-Overview.md`, `Getting-Started.md`, `Content-Recommendations.md`, `Agent-Reference.md`, `Schema-Reference.md`). Three findings: 1 major (Content-Recommendations.md restructure) + 2 minor (Schema-Reference.md stale example, Getting-Started.md stale test count). The other 4 in-scope pages passed — the BACKLOG's prioritization of `Content-Recommendations.md` as the primary drift hotspot was accurate.

- **Changed (F1, major):** `docs/wiki/claims-model-starter/Content-Recommendations.md` restructured from evolution record to current-state description. Before: 139 lines in which 7 of 10 "Recommended additions" carried `✅ *shipped in Session 20A/20B*` markers (items #1 Worked Examples, #2 Intake Interview Design, #3 Extending the Pipeline, #4 Schema Reference, #5 Security Considerations, #8 Changelog, #9 Contributing). After: 93 lines, "Known gaps" section lists only genuine future work — FAQ, Comparison with alternatives, Self-hosted deployment guide, Auto-generated schema reference (hand-written → JSON Schema pass), Release-tag-driven changelog refresh, Additional worked example (regression/unsupervised fixture). `Current wiki pages` table (lines 7-27) and `Recommendations for the SBOM page` section (verified still-pending against `Software-Bill-of-Materials.md`) and `Wiki maintenance` section are unchanged. The page opener was tightened from "documents what this wiki contains, what additional content would add value, and prioritized recommendations" to "lists the wiki's current pages (with audiences) and known gaps where a new page would add value" — matches the new shape.
- **Changed (F2, minor):** `docs/wiki/claims-model-starter/Schema-Reference.md:302` — inline comment on `host_url: str` updated from `# "https://gitlab.example.com"` to `# "https://gitlab.com" or self-hosted like "https://gitlab.yourcompany.com"`. The old example was stale after Session 32's `cli.py:39` `GITLAB_DEFAULT_HOST_URL` bump from `gitlab.example.com` to `gitlab.com` (public default).
- **Changed (F3, minor):** `docs/wiki/claims-model-starter/Getting-Started.md:34` — "You should see 422+ tests pass with 97%+ coverage." → "You should see 440+ tests pass with ~97% coverage." Current actual: 446 tests @ 97.27%. Chose `440+` as a slightly-durable lower bound rather than `446` exact to reduce re-update churn.
- **Verified:** `uv run pytest -q` → **446/446 passing, coverage 97.27%** (unchanged — doc-only changes). No ruff/mypy re-run (no code touched); Session 35's baseline stands. No `-W default` re-run (no sqlite-adjacent code touched).
- **Unchanged intentionally:** `Home.md` (44 lines, accurate), `Pipeline-Overview.md` (88 lines, the "(in future) resumed" phrase at line 79 remains accurate — automated resume-from-checkpoint is a genuine BACKLOG item), `Agent-Reference.md` (192 lines, accurate). Cross-references audited: no in-wiki page anchor-links pointed to the old `#X-worked-examples-page`-style headings in Content-Recommendations.md (none existed to begin with), so the restructure breaks nothing.
- **Out-of-scope drift noted for Session 39:** `Monitoring-and-Operations.md:91` has the same "(422+ tests, >94% coverage)" staleness pattern as Getting-Started.md but is outside this session's BACKLOG scope (the 6 named pages). `Changelog.md:31` reference to "422 tests at 97.18%" is historical phase-marker content and appropriately kept.

### 2026-04-18 — GitHub explicit-override test symmetry (Session 37)

Closes the BACKLOG "GitHub explicit-override test symmetry" item. Completes the test-coverage symmetry Session 32 began (Learning #25): both repo-host platforms now have default-path **and** explicit-override test paths in `tests/agents/website/test_cli.py`. Before: GitLab had both (`test_cli_host_gitlab_with_token_invokes_python_gitlab_adapter` at lines 233-263 + `test_cli_host_gitlab_explicit_host_url_override` at lines 266-300), GitHub had only the default-path (`test_cli_host_github_with_token_invokes_pygithub_adapter` at lines 303-338). The asymmetry was noted in Session 32's gotcha #10; this session mirrors the GitLab sibling exactly so future refactors that touch either path trip both tests identically.

- **Added:** `test_cli_host_github_explicit_host_url_override` at `tests/agents/website/test_cli.py:341-377`. Mirrors the GitLab sibling's shape (`--host-url` explicit + monkeypatched adapter + kwargs assertion). Uses `"https://github.example.com/api/v3"` as the canonical GitHub Enterprise example URL (consistent with the pattern in prior session notes and with the GHE URL shape required by the `/api/v3` suffix).
- **Verified:** `uv run pytest -q` → **446/446** passing (up from 445), coverage **97.27%** (unchanged). `uv run ruff check src/ tests/ packages/` clean. `uv run mypy src/ packages/` clean on 60 source files.
- **Unchanged (intentionally):** No production-code changes. `GITHUB_DEFAULT_HOST_URL = "https://api.github.com"` at `cli.py:40` remains the public-GitHub default; the new test only exercises the override branch the existing default-path test doesn't cover.

### 2026-04-17 — `--ci-platform` flag mentioned in `docs/tutorial.md` §3 (Session 36)

Closes the BACKLOG "`docs/tutorial.md` §5 `--ci-platform` mention" item. Placement moved from the BACKLOG-suggested §5 to §3's "Try GitHub CI output" subsection after verification that `scripts/run_pipeline.py` (the only CLI the tutorial invokes across §3, §5, and §6) does **not** expose `--ci-platform` — `scripts/run_pipeline.py:262` hardcodes `ci_platform = host` and never reads the flag. The flag only exists on the standalone website CLI at `src/model_project_constructor/agents/website/cli.py:107-116`. Learning #28 (handoff fix-shape prescriptions as hints, not contracts) applied: honored the handoff's intent (document the flag in the tutorial), corrected the handoff's specific (§3 instead of §5 — §3 is where `--host` is first introduced as the CI-manifest toggle, so the "you can decouple them" note lands at peak relevance).

- **Added:** 2-sentence paragraph at `docs/tutorial.md:272` (end of §3's "Try GitHub CI output" subsection, directly after the `.github/workflows/ci.yml` vs `.gitlab-ci.yml` explanation). Covers (a) the coupling — `scripts/run_pipeline.py` always ties the CI manifest to `--host`, (b) the override path — use the standalone website CLI with `--ci-platform {gitlab,github}` to emit a CI manifest that differs from the repo host (primarily for fake-mode cross-platform testing), (c) the pointer — see `OPERATIONS.md` §4.1 for the website-CLI recipe.
- **Verified:** `uv run pytest -q` 445/445 passing, coverage **97.27%** (unchanged — doc-only). No code touched; no ruff / mypy re-run.
- **Unchanged (intentionally):** `docs/wiki/claims-model-starter/Schema-Reference.md:302` still references `gitlab.example.com` as a fifth operator-facing doc surface; the "Wiki freshness sweep" BACKLOG item covers that gap and kept out of scope here per "1 and done." No change to the §3 "Command-line options" table (lines 257-262) — that table documents `scripts/run_pipeline.py` flags specifically and `--ci-platform` is genuinely not one of them.

### 2026-04-17 — Unclosed-SQLite `ResourceWarning` noise eliminated (Session 35)

Closes the BACKLOG "Fix unclosed SQLite connection warnings" item (prior count "~20 + 1"; Session 34 characterized the actual count as 26 running `uv run pytest -W default`; Session 35's pre-change tracemalloc pass measured 28 — the 2-warning delta is scheduling noise between runs, all of the same class). Two independent root causes, both in test-side lifecycle gaps rather than production defects:

- **Fixed (root cause A — 15 warnings):** `IntakeSessionStore._conn` (a raw `sqlite3.Connection` opened at `src/model_project_constructor/ui/intake/runner.py:84` with `check_same_thread=False` for the FastAPI threadpool) was never closed under test. `IntakeSessionStore.close()` exists and is wired into the FastAPI `lifespan` at `src/model_project_constructor/ui/intake/app.py:79-86`, but `starlette.testclient.TestClient` only fires `lifespan` when used as a context manager (`with TestClient(app) as client:`); `tests/ui/intake/conftest.py` instantiated `TestClient(make_app(fixture))` bare, so `lifespan` never ran. Fix in `conftest.py`: `make_app` is now a yielding fixture that tracks every `FastAPI` app it produces in a list and, on teardown, calls `app.state.store.close()` for each. `make_client`, `subrogation_client`, and the direct `make_client(...)` usages in `test_caps_and_revisions.py:31,:48` inherit the fix via the fixture dependency chain — no test body changes were needed.
- **Fixed (root cause B — 13 warnings):** `ReadOnlyDB._engine` (the SQLAlchemy engine assigned at `packages/data-agent/src/model_project_constructor_data_agent/db.py:37`) was never disposed, so the pooled sqlite3 connections lingered until Python 3.13's stricter GC surfaced them at arbitrary frames (`threading.py:281/303`, `ast.py:279`, `anyio/_backends/_asyncio.py:342`, `rich/protocol.py`, `yaml/reader.py`, etc. — the frames in the warning are NOT the allocation sites). Plus a sibling leak in `tests/agents/data/conftest.py:32` where the `seeded_sqlite_url` fixture's own setup engine was never disposed, and an inline engine in `tests/data_agent_package/test_cli.py:60` with the same gap.
- **Added:** `ReadOnlyDB.close()` method (`db.py:47-54`) — idempotent, safe to call without a prior `connect()`. Calls `self._engine.dispose()` and sets `_engine = None`. No context-manager (`__enter__/__exit__`) methods added — explicit `close()` via try/finally covers every current call site, and YAGNI rules out the extra surface until a caller needs it.
- **Changed:** `packages/data-agent/src/model_project_constructor_data_agent/cli.py:102-108` — wrapped `agent.run(...)` in a `try/finally` so `db.close()` fires even if the run raises. CLI is the owner of the `ReadOnlyDB` lifetime for the `--db-url` path; the agent and its graph treat `db` as borrowed.
- **Changed:** `tests/agents/data/conftest.py` — `seeded_sqlite_url` is now a yielding fixture with `engine.dispose()` in `finally`; `seeded_db` is now a yielding fixture with `db.close()` in `finally`. Both maintain `tmp_path` isolation; teardown order is correct (dependents tear down before dependencies per pytest fixture contract, so `seeded_db.close()` runs before `seeded_sqlite_url.engine.dispose()`).
- **Changed:** `tests/data_agent_package/test_cli.py:58-64` — wrapped the inline `engine.begin()` block in `try/finally` with `engine.dispose()`. One-liner; no test behavior change.
- **Verified:** `uv run pytest -W default --tb=no -q` now prints **0 warnings** (previously 25-28 of class `ResourceWarning: unclosed database`). `uv run pytest -q` 445/445 passing; coverage **97.27%** (up 0.01pp from 97.26% — the new `close()` statements count as covered because both test fixtures and the CLI `try/finally` exercise them). `uv run ruff check src/ tests/ packages/` clean. `uv run mypy src/ packages/` clean on 60 source files. `PYTHONTRACEMALLOC=25 uv run pytest -W default --tb=no -q` re-run after the fix: 0 `unclosed database` hits.
- **Unchanged (intentionally):** `ReadOnlyDB` has no `__enter__/__exit__` — see "No context-manager" note above. Also unchanged: `test_data_agent.py:211` (`unreachable = ReadOnlyDB("sqlite:////nonexistent...")`) — `ReadOnlyDB.connect()` fails inside the agent's node before `_engine` is ever assigned, so no engine leaks; calling `close()` there would be a no-op. Also unchanged: `src/model_project_constructor/agents/intake/graph.py` uses `MemorySaver()` (in-memory, no sqlite); `src/model_project_constructor/agents/website/graph.py` same. The only LangGraph SQLite saver usage is `SqliteSaver` in `ui/intake/runner.py`, which is covered by root cause A.

### 2026-04-17 — F6: `--ci-platform` flag documented in OPERATIONS §4.1 (Session 34)

Closes F6, the smaller half of Session 31's audit findings (F5 shipped in Session 32; F6 was attempted + reverted in Session 33 when a cross-session protocol drift was surfaced instead — see that session's entry). Session 33 captured the verified prose in `SESSION_NOTES.md`; Session 34's work was paste + verify + commit, no redrafting.

- **Added:** 7-line paragraph at end of `OPERATIONS.md` §4.1 (after "...run by CI on every commit." at line 173) documenting `--ci-platform {gitlab,github}`. Covers: (a) default behavior (`--host` selects both the repo host and the CI manifest — `.gitlab-ci.yml` for GitLab, `.github/workflows/ci.yml` for GitHub), (b) override purpose (decouple CI manifest from repo host, primarily for fake-path cross-platform testing), (c) scope (applies to §4.1 / §4.2 / §4.3; `scripts/run_pipeline.py` in §4.4 hardcodes `ci_platform = host` at `:262` and ignores the flag). The end-of-§4.1 placement was chosen in Session 33 over a new §4.5 because the flag applies to all three top-level recipes, not just one; putting it at peak fake-mode relevance (where the use case is primary) and ahead of §4.2/§4.3 for linear readers trades off TOC discoverability for in-flow visibility.
- **Verified:** `uv run pytest -q` 445/445 passing, coverage 97.26% (unchanged — doc-only). `uv run python -m model_project_constructor.agents.website --help` still advertises `--ci-platform` at `cli.py:107-116` unchanged.
- **Unchanged (intentionally):** `docs/tutorial.md §5` still does not mention `--ci-platform` — that remains a separate BACKLOG item (Session 33 promoted it to an explicit `[ ]` entry). Keeping the two doc edits separate respects "1 and done" (one deliverable per session) even though both are micro-scope.

### 2026-04-17 — BACKLOG.md / ROADMAP.md protocol-erosion sweep (Session 33)

Session 33 started F6 (document `--ci-platform` in OPERATIONS §4.x) but surfaced a cross-session protocol drift during close-out bookkeeping. User redirected: ship the hygiene fix alone in this session; resume F6 in Session 34. The F6 OPERATIONS.md edit was reverted mid-session; no other F6 artifact shipped.

- **Fixed:** `BACKLOG.md` was accumulating completed items as `[x]` checked-off bullets rather than removing them per `docs/methodology/README.md:99` ("CHANGELOG.md keeps BACKLOG.md lean") + `CHANGELOG.md:6` ("remove the item from BACKLOG.md and add an entry here"). User caught this when I flipped `[ ]` → `[x]` on the F6 item per the drift pattern Sessions 24-32 had each followed. Swept both origins of accumulation: (a) 8 inline `[x]` items in `## Up Next` (Sessions 24, 26, 27, 28, 29, 30, 31, 32), (b) entire legacy `## Completed` section (23 items grouped by milestone, predating the v2.1 three-file split). Added a file-top note ("**Open work only.** Completed items move to `CHANGELOG.md`... Do not leave checked-off items here.") to prevent recurrence. Classification: **SAFEGUARDS.md failure mode #17 (protocol erosion).** The drift spanned 8 sessions; each incremental violation was individually minor, which is exactly the pattern FM #17 warns against.
- **Fixed:** `ROADMAP.md` was severely stale. Claimed "Current Milestone: M1: Architecture & Pipeline Design" when all 5 build milestones were complete per `docs/planning/architecture-plan.md` §14. "## Planned" listed all 5 phases as future work. "## What's Built" mentioned only methodology and `initial_purpose.txt`, omitting every agent. "## Completed Milestones" read "None yet — project is in initial setup." Rewrote to describe current state accurately: all 5 milestones done (M1 Architecture → M5 Orchestrator + Hardening); 445 tests @ 97.26%; Scope A (Session 22) + B-1 (Session 24) + B-2 (Sessions 26-27) shipped; remaining work is optional B-3 Web UI bridge + post-pilot operator-experience / doc-freshness improvements. New `## Completed Milestones` section preserves the M1-M5 phase summary that lived in BACKLOG's deleted `## Completed` section — this is the only place the milestone-grouped view is recorded (CHANGELOG is chronological per session, not grouped).
- **Added:** Two previously-implicit BACKLOG entries promoted from handoff prose to `[ ]` items — `docs/tutorial.md §5` `--ci-platform` mention (Session 32 gotcha follow-up), GitHub explicit-override test symmetry (Session 32 gotcha #10).
- **Deferred:** F6 (`--ci-platform` in OPERATIONS §4.1) stays in BACKLOG as top-priority Session 34 deliverable. Session 33's drafted prose is captured verbatim in `SESSION_NOTES.md` to preserve work-continuity — Session 34 can paste-and-verify rather than re-draft.
- **Verified:** `uv run pytest -q` 445/445 passing, coverage 97.26% — no code changes, no test regressions. Pure task-tracking hygiene.

### 2026-04-17 — Website CLI GitLab default URL fixed: placeholder → public gitlab.com (Session 32)

- **Fixed:** `GITLAB_DEFAULT_HOST_URL` at `src/model_project_constructor/agents/website/cli.py:39` changed from `"https://gitlab.example.com"` (RFC-2606 placeholder — a non-resolving domain) to `"https://gitlab.com"` (the canonical public GitLab API URL). Closes Session 31's F5 finding. Now consistent with all four other surfaces that declared the default: `orchestrator/config.py:33` (`DEFAULT_GITLAB_URL = "https://gitlab.com"`), `scripts/run_pipeline.py:105,283`, `.env.example:18-19` (prose "defaults to the public host for the selected platform (https://gitlab.com or https://api.github.com)"), and `OPERATIONS.md:24`. Before the fix, an operator running `python -m model_project_constructor.agents.website --host gitlab --private-token X --namespace ...` without `--host-url` would have hit a DNS failure on the RFC-2606 placeholder domain; now it succeeds against public gitlab.com, matching the four sibling surfaces.
- **Changed:** `tests/agents/website/test_cli.py` — restructured `test_cli_host_gitlab_with_token_invokes_python_gitlab_adapter` to mirror the GitHub sibling's default-path shape: omits `--host-url` from the invocation and asserts `_StandinGitLabAdapter.last_init_kwargs == {"host_url": "https://gitlab.com", "private_token": "fake-gitlab-token"}`. This test previously passed `--host-url "https://gitlab.example.com"` explicitly, which meant the CLI's default URL was never pinned by a regression test (Session 31 noted this gap in its F5 write-up).
- **Added:** `test_cli_host_gitlab_explicit_host_url_override` in the same file — preserves explicit-override coverage that the default-path restructuring removed. Passes `--host-url https://gitlab.example.com` and asserts the adapter received that exact URL, confirming the self-hosted-GitLab path still flows through correctly. 444 → 445 tests.
- **Verified:** `uv run pytest -q` 445/445 passing, coverage 97.26%; `uv run ruff check src/ tests/ packages/ scripts/` clean; `uv run mypy` clean on all 60 source files. Fake-mode smoke of the default path (`--host gitlab --fake`) still produces `Status: COMPLETE` (FakeRepoClient has its own hardcoded `https://fake.host.test` base, so the fake path is invariant to this change; unit-test coverage of the adapter kwarg pins the live behavior). No live GitLab.com run performed — matches the unit-test-only verification pattern Session 30 + Session 22 used for the symmetric GitHub fix.
- **Unchanged (intentionally):** `README.md:182`, `OPERATIONS.md:184/189`, `.env.example:21`, `src/model_project_constructor/agents/website/gitlab_adapter.py:50` all still reference `https://gitlab.example.com` — these are **self-hosted-instance example placeholders** (each surrounded by prose explaining "substitute your instance URL") and are correct as-is. Test fixtures that pass `https://gitlab.example.com` as a test input value (`tests/schemas/fixtures.py`, `tests/agents/website/conftest.py`, etc.) are also unchanged — they exercise valid URL-shape handling, not the default.

### 2026-04-17 — OPERATIONS.md §4.2/§4.3 re-audit + reconciliation (Session 31)

- **Audited:** OPERATIONS.md §4.2 (live GitLab) and §4.3 (live GitHub / GitHub Enterprise) recipes against current cli.py, README.md recipes, and adjacent docs. Closes Session 22's "reconcile or deprecate `python -m model_project_constructor.agents.website`" finding (previously recorded in this CHANGELOG's 2026-04-16 Session-22 block and tracked at `BACKLOG.md:53`). Answer: **keep, reconcile.** The CLI is an intended, well-supported entry point — README.md documents it prominently, cli.py docstring identifies it as "the plan's literal verification command," 14 tests in `tests/agents/website/test_cli.py` pin all 3 invocation shapes, and `docs/planning/github-gitlab-abstraction-plan.md` uses it as the Phase D verification command. OPERATIONS.md §4.2/§4.3 had drifted from the canonical README pattern on 4 dimensions (F1-F4 below); all now reconciled.
- **Changed:** `OPERATIONS.md` §4.2 and §4.3 rewritten to mirror `README.md:178-194` style:
  - **F1 (ANTHROPIC_API_KEY over-listed):** Removed `export ANTHROPIC_API_KEY=...` from both §4.2 and §4.3 preambles. The Website Agent + cli.py make no Anthropic calls (`grep -rn anthropic src/model_project_constructor/agents/website/` returns zero matches); §1's env-var table correctly scopes the key to the Intake Agent web UI, live interviews, and the Data Agent. Operators following §4.2/§4.3 will no longer block on an API key they don't need. §4.4 (which runs `--llm data`) correctly retains the export.
  - **F2 (intake.json/data.json provenance gap):** Replaced abstract `intake.json` / `data.json` filenames with the canonical fixture paths (`tests/fixtures/subrogation_intake.json` and `tests/fixtures/sample_datareport.json`), matching §4.1 + README. Added a one-line note on substituting pre-built `IntakeReport` / `DataReport` JSON for non-fixture runs, with schema-version pointers (`schemas.v1.intake` / `schemas.v1.data`).
  - **F3 (cosmetic env exports):** Dropped `export MPC_HOST=...` and `export MPC_HOST_URL=...` preambles from §4.2 and §4.3. cli.py reads zero environment variables (verified by source read); these exports had no effect on the CLI. Kept `"$GITLAB_TOKEN"` / `"$GITHUB_TOKEN"` inline with a prose prerequisite pointing at §1. Added explicit prose: "This CLI is flag-driven — the `MPC_*` env vars in §1 are read by `scripts/run_pipeline.py` (§4.4), not by this entry point."
  - **F4 (GHE recipe incompleteness):** Previous §4.3 exported `MPC_HOST_URL=https://api.github.com # or your GHE API URL` but the invocation omitted `--host-url`, so GHE operators following the recipe verbatim would silently have hit public `api.github.com`. Fix mirrors README: default invocation omits `--host-url` (uses cli.py's `https://api.github.com` default for public GitHub); added a GHE-specific sentence "For GitHub Enterprise, add `--host-url https://github.mycompany.com/api/v3` (or your Enterprise instance's API URL)."
- **Verified:** New §4.2 recipe (with `--fake` substituted for `--private-token` to avoid live credentials) → `Status: COMPLETE` with project URL `https://fake.host.test/data-science/model-drafts/intake-subrogation-001`. New §4.3 recipe → `Status: COMPLETE` with `https://fake.host.test/acme/intake-subrogation-001`. GHE variant (same with `--host-url https://github.mycompany.com/api/v3` added) → also `Status: COMPLETE`. Full pre-flight green: `uv run pytest -q` 444/444 passing, coverage 97.26%; `uv run ruff check src/ tests/ packages/ scripts/` clean; `uv run mypy` clean on all 60 source files. No code changes — doc-only commit.
- **Deferred (new BACKLOG items, F5 + F6):**
  - **F5: cli.py GitLab default URL inconsistency** — `src/model_project_constructor/agents/website/cli.py:39` has `GITLAB_DEFAULT_HOST_URL = "https://gitlab.example.com"` (placeholder). Inconsistent with `orchestrator/config.py:33` + `scripts/run_pipeline.py:105,283` + `.env.example:18-19` + `OPERATIONS.md:24` — all four of those declare the default as `https://gitlab.com`. Operators running `python -m ...website --host gitlab --private-token X --namespace ...` without `--host-url` hit the RFC-2606 placeholder domain. Code-level fix deferred to avoid mixing code + doc edits in one audit session (SAFEGUARDS.md failure mode #8 / #18).
  - **F6: `--ci-platform` flag undocumented in OPERATIONS §4.x** — cli.py:107-116 documents it (defaults to `--host`), README.md:197 mentions it, none of OPERATIONS.md §4.1/§4.2/§4.3 examples reference it. Low-impact onboarding gap.

### 2026-04-17 — Self-hosted GitHub URL override wired through pipeline script (Session 30)

- **Fixed:** `scripts/run_pipeline.py:273-278` live GitHub branch had two related bugs that together meant the `--live --host github` path had never worked. (a) It passed `token=token` to `PyGithubAdapter(...)`, but the adapter's keyword-only parameter is `private_token=` (the kwarg-only signature at `src/model_project_constructor/agents/website/github_adapter.py:66-72`). First invocation would have raised `TypeError: __init__() got an unexpected keyword argument 'token'`. Parallels Session 22's `url=` → `host_url=` fix for the GitLab branch. (b) It never read `MPC_HOST_URL`, so even after the TypeError was noticed, GitHub Enterprise users would silently have hit public `api.github.com` despite `docs/tutorial.md §5c` claiming the env var works for GHE. Fix mirrors the GitLab branch directly below: read `MPC_HOST_URL` with `https://api.github.com` as the default, pass `private_token=token, host_url=host_url`.
- **Added:** 3 new tests in `tests/scripts/test_run_pipeline_adapter.py` (441 → 444; +3). `test_build_website_runner_github_live_threads_host_url` — with `MPC_HOST_URL=https://github.mycompany.com/api/v3`, `PyGithubAdapter` receives `private_token=<GITHUB_TOKEN>` + that exact `host_url`. `test_build_website_runner_github_live_defaults_host_url` — with `MPC_HOST_URL` unset, `host_url` defaults to `https://api.github.com`. `test_build_website_runner_gitlab_live_threads_host_url` — regression guard for the symmetric GitLab branch. All three monkeypatch the adapter classes + `WebsiteAgent` so no live HTTP is made. Pattern follows the existing `importlib.util` module-loading approach since `scripts/` is not a package.
- **Verified:** `uv run pytest -q` 444/444 passing, coverage 97.26%; `uv run ruff check src/ tests/ packages/ scripts/` clean; `uv run mypy` clean on all 60 source files. No adapter change required — `PyGithubAdapter.__init__` already accepted `host_url=` (and already wired it into `Github(auth=..., base_url=host_url)` at `github_adapter.py:72`); the gap was purely in the pipeline script's call site. Not live-tested against a real GitHub Enterprise (no test instance); parallels Session 22's GitLab `host_url=` fix which was also shipped on code-read + unit-test coverage only. Closes the last of Session 22's three code-gap findings (validator + ruff-scope + mypy-scope already landed in Sessions 28/25/29).

### 2026-04-17 — CI mypy coverage extended to `packages/` (Session 29)

- **Changed:** `.github/workflows/ci.yml` typecheck job now runs `uv run mypy` (no explicit path), which picks up the `[tool.mypy] packages = ["model_project_constructor", "model_project_constructor_data_agent"]` declaration in `pyproject.toml`. Previously CI ran `mypy src/` only, so pre-existing errors in `packages/data-agent/` never blocked merges. Structural twin of Session 25's ruff-scope extension: `pyproject.toml` already declared the intent; CI was running a narrower subset.
- **Fixed:** 13 pre-existing mypy errors in `packages/data-agent/src/model_project_constructor_data_agent/` surfaced by the broader scope.
  - `anthropic_client.py:218` — 11 × `union-attr` on `response.content[0].text`. The Anthropic SDK's `ContentBlock` is a 13-variant union; only `TextBlock` has `.text`. Fix: added `from anthropic.types import TextBlock` + `isinstance(block, TextBlock)` guard that raises `LLMParseError` with the actual class name if the first block is not a `TextBlock`. Production behavior is unchanged on the happy path (Claude always returns `TextBlock` for our `messages=[{"role": "user", ...}]` payload); the guard replaces an implicit `AttributeError` with a typed error that the Data Agent's outer try/except already handles.
  - `nodes.py:142` — `arg-type` on `execution_status=status` because `status = "PASSED" if rows else "FAILED"` inferred as `str` not `Literal["PASSED", "FAILED"]`. Fix: explicit `status: Literal["PASSED", "FAILED"] = ...` annotation + `Literal` import.
  - `sql_validation.py:26` — `no-untyped-call` on `parsed[0].get_type()` (sqlparse is untyped in mypy's strict mode). Fix: inline `# type: ignore[no-untyped-call]` — minimal, localized, accurate (3rd-party untyped API).
- **Changed:** `tests/data_agent_package/test_anthropic_client.py` — replaced the `_FakeResponse` dataclass (duck-typed `.text` attr) with real `anthropic.types.TextBlock(text=..., type="text")` instances in `_FakeMessages.create`. Tests now exercise the production `isinstance(block, TextBlock)` guard faithfully.
- **Added:** `test_call_claude_rejects_non_text_block` in `tests/data_agent_package/test_anthropic_client.py` — covers the new defensive branch that raises `LLMParseError("expected TextBlock...")` when the first content block is not a `TextBlock`. 440 → 441 tests.
- **Verified:** `uv run pytest -q` 441/441 passing, coverage 97.26%; `uv run ruff check src/ tests/ packages/ scripts/` clean; `uv run mypy` clean on all 60 source files (`src/` + `packages/data-agent/src/`).

### 2026-04-17 — `MPC_NAMESPACE` URL-prefix validator + docs (Session 28)

- **Added:** `validate_namespace(raw: str) -> str` module-level helper in `src/model_project_constructor/orchestrator/config.py`. Raises `ConfigError` when `MPC_NAMESPACE` starts with `http://` or `https://` (case-insensitive, whitespace-trimmed) with a message that names the received value and the expected path form. Closes a Session 22 operator-experience finding: before this, a URL-form namespace surfaced as a generic `group lookup failed: 404` from the GitLab adapter; now it fails at `[2/5] Building pipeline config...` in `scripts/run_pipeline.py` before any agent runs.
- **Added:** `OrchestratorSettings.namespace: str | None` field, populated from `MPC_NAMESPACE` and validated via `validate_namespace()` in `from_env()`. `None` when unset or whitespace-only. The pipeline script still applies host-specific defaults (`my-org` for GitHub, `data-science/model-drafts` for GitLab); the field lets future refactors thread the value through settings instead of re-reading `os.environ`.
- **Changed:** `scripts/run_pipeline.py` `build_repo_target` now calls `validate_namespace(os.environ.get("MPC_NAMESPACE", <default>))` so the error surfaces in fake mode as well as live mode — fail-fast for any mis-set namespace, not just ones that reach the adapter.
- **Added:** 13 new tests in `tests/orchestrator/test_config.py` (+13 total; 427 → 440). `TestValidateNamespace` direct-function tests (4 group-path acceptances + 4 URL-prefix rejections including mixed-case `HTTPS://` and leading/trailing whitespace). `TestFromEnvDefaults` gains `test_namespace_group_path`, `test_namespace_nested_group_path`, `test_namespace_empty_string_treated_as_unset`, and confirms `namespace is None` on empty env. `TestFromEnvValidation` gains `test_rejects_namespace_with_https_prefix` and `test_rejects_namespace_with_http_prefix`.
- **Changed:** Docs — `.env.example` now carries a `#MPC_NAMESPACE=rmsharp-modelpilot` template line with an inline comment explaining the path-not-URL invariant; `OPERATIONS.md` §1 env-var table gains a `MPC_NAMESPACE` row; `docs/tutorial.md` §5c expands the `MPC_NAMESPACE` section with the path-not-URL guidance + a worked example of the `ConfigError` message the validator raises.
- **Verified:** pytest 440/440 (coverage 97.25%), `uv run ruff check src/ tests/ packages/ scripts/` clean, `uv run mypy src/` clean. Sanity checks: `MPC_NAMESPACE=https://gitlab.com/rmsharp-modelpilot scripts/run_pipeline.py` now raises `ConfigError` at `[2/5]` with the clear message; `MPC_NAMESPACE=rmsharp-modelpilot scripts/run_pipeline.py` still produces `Status: COMPLETE` in fake mode.

### 2026-04-17 — Scope B-2 follow-up: `MAX_QUESTIONS` raised to 20, happy path reaches `COMPLETE` (Session 27)

- **Changed:** `MAX_QUESTIONS` in `src/model_project_constructor/agents/intake/state.py:57` raised from 10 to 20. Session 26's `run_b2_live` against `subrogation_b2.yaml` (10 qa_pairs) produced a rich Claude-drafted report but hit the cap before `believe_enough_info` flipped, forcing `DRAFT_INCOMPLETE` via `nodes.py:129-134`'s `questions_cap_reached` auto-append. Plan §7.2.3 criterion #1 (happy path → `COMPLETE`) therefore required both a higher cap AND a fuller fixture. All cap consumers (`agent.py:29,78`, `nodes.py:24,81,130`) import the constant — the single-line bump is sufficient.
- **Changed:** `tests/fixtures/subrogation_b2.yaml` extended from 10 to 15 qa_pairs. Pre-answers the three gaps Claude flagged in `run_b2_live`: **latency SLA** (250 ms p95 at intake completion; not real-time at FNOL), **average recovery per successful subrogation** (~$7,500 mean / $4,200 median; $30M/yr comes from ~4,000 recoveries), and **fairness / bias plan** (pre-launch disparate-impact audit + quarterly SR 11-7 fairness review; no protected attributes, tenure included as feature). Two padding entries cover retraining cadence (quarterly + PSI/AUC triggers) and incident response (feature-flag fallback + model-incident review). `draft_after` bumped to 15 for documentation consistency (no-op in `--llm both`).
- **Changed:** `tests/fixtures/intake_question_cap.yaml` expanded from 11 to 21 qa_pairs so `FixtureLLMClient` supplies `MAX_QUESTIONS+1` answers — the test's purpose (verify the hard cap stops the interview) requires more answers than the cap. Entry #21 is labeled "should never be asked" to document the invariant.
- **Changed:** `tests/ui/intake/test_runner.py:test_caps_constants_exposed` now references `MAX_QUESTIONS` + `MAX_REVISIONS` symbolically instead of the literal `{"max_questions": 10, ...}`. Future cap changes won't need a test edit.
- **Changed:** Doc references to the literal "10" updated — `README.md:101`, `OPERATIONS.md` §4.4.2, `docs/wiki/claims-model-starter/Intake-Interview-Design.md:63,220`, `docs/wiki/claims-model-starter/Worked-Examples.md:215`. Historical entries in `CHANGELOG.md` / wiki `Changelog.md` / planning docs kept as-is (they describe the state at the time of the change).
- **Verified:** live `run_b2_complete` (`--live --host gitlab --llm both --model claude-opus-4-7 --intake-fixture tests/fixtures/subrogation_b2.yaml`) produced `Status: COMPLETE` at `https://gitlab.com/rmsharp-modelpilot/subrogation-pilot-v3`, with `questions_asked=10`, `missing_fields=[]`, `tier_3_moderate` governance, `target_variable=subrogation_recovery_success`. End-to-end latencies: intake 63s, data 696s, website 5s. Claude flipped `believe_enough_info=true` exactly at turn 10 after receiving the pre-answers. Pytest 427/427 (coverage 97.24%), ruff clean on CI scope, mypy clean on `src/`.

### 2026-04-16 — Scope B-2: scripted-answers intake with real Anthropic (Session 26)

- **Added:** `scripts/run_pipeline.py` `--llm` flag gains a third value `both`, which drives `IntakeAgent.run_scripted(...)` with real Anthropic-generated questions + fixture-supplied answers + a real Anthropic data agent. New `--intake-fixture PATH` flag (required when `--llm=both`, ignored otherwise). `--model` now applies to BOTH intake and data agents (single flag, shared value across stages). New `build_intake_runner` helper mirrors `build_data_runner`'s shape.
- **Added:** Inline `_draft_incomplete_from_exception` adapter in `scripts/run_pipeline.py` (per scope-b-plan §8.4 (a)). Converts exhausted-script `RuntimeError`, rate-limit / parse errors, and pydantic validation failures into a typed `IntakeReport(status="DRAFT_INCOMPLETE")` so the orchestrator halts with `FAILED_AT_INTAKE` instead of crashing. Reason code is `exception_class_name`; full message goes into `missing_fields[0]`.
- **Added:** `tests/scripts/test_run_pipeline_adapter.py` (5 tests; +5 total). Covers the adapter's happy path (`RuntimeError` → `DRAFT_INCOMPLETE`), arbitrary-exception path (simulated rate-limit), the closure's `run_scripted` error path via monkeypatched `IntakeAgent`, `--llm none` fixture fallback, and the fail-fast `SystemExit` when `--llm both` is passed without `--intake-fixture`. Adapter is loaded via `importlib.util` since `scripts/` is not a package.
- **Added:** `tests/fixtures/_b2_failmode.yaml` — a deliberately under-specified fixture (1 `qa_pairs` entry + `draft_after: 99`) that forces `IntakeAgent.run_scripted` to raise the exhausted-answers `RuntimeError`. Consumed by the B2 failure-injection live run.
- **Added:** `OPERATIONS.md` §4.4 extended with the `--llm both` recipe (happy path + failure-injection). `docs/tutorial.md` §6 extended with a `--llm both --intake-fixture` subsection, cost delta vs B1 (~$0.15-0.75/run), and the `FAILED_AT_INTAKE` behavior contract.
- **Verified:** pre-flight (pytest 427/427 with +5 adapter tests, ruff on CI scope, mypy) stays green; the `--llm none` (Scope A) and `--llm data` (Scope B-1) invocations continue to produce their Scope-A / Scope-B-1 checkpoint envelopes (regression check). Plan: `docs/planning/scope-b-plan.md` §7.2 (Phase B2). Scope B-3 (Web UI bridge) remains optional and deferred.

### 2026-04-16 — CI ruff coverage extended to `scripts/` (Session 25)

- **Changed:** `.github/workflows/ci.yml` lint job now runs `uv run ruff check src/ tests/ packages/ scripts/` (previously `src/ tests/ packages/`). Closes a CI gap documented in Session 22: `scripts/` was outside CI's ruff scope, so 10 pre-existing errors (6 × `E402`, 4 × `F541`) never blocked merges.
- **Fixed:** `scripts/run_pipeline.py` — 6 × `E402` ("module level import not at top of file") resolved with per-line `# noqa: E402` on the imports that follow `sys.path.insert`. Kept the `sys.path` preamble intact so the module docstring's claim that the script runs "whether invoked via `uv run python scripts/run_pipeline.py` or directly" stays true. 4 × `F541` ("f-string without any placeholders") auto-fixed by `ruff check --fix`; the affected `print(f"...")` calls had no placeholders and are now plain `print("...")`.
- **Verified:** `uv run ruff check src/ tests/ packages/ scripts/` clean; `uv run python scripts/run_pipeline.py --help` renders; full fake-mode pipeline run (`--run-id run_s25_scripts_ruff`) completes with status `COMPLETE`; pytest 422/422 green.

### 2026-04-16 — Scope B-1: real Anthropic data agent wired into `run_pipeline` (Session 24)

- **Added:** `scripts/run_pipeline.py` now accepts `--llm {none,data}` (default `none`), `--model ID` (default `claude-opus-4-7`), and `--db-url URL` (default `None`). A new `build_data_runner` helper returns either a fixture-serving closure (`--llm none`, unchanged Scope A behavior) or `DataAgent(AnthropicLLMClient(model=...), db=...).run` (`--llm data`). The intake stage stays fixture-driven at this phase — Scope B-2 wires the real intake path.
- **Added:** `OPERATIONS.md` §4.4 documents the Scope B-1 invocation with a model-selection table and a checkpoint-inspection snippet for verifying the data side actually called Claude. `docs/tutorial.md` §6 "Real LLM-backed run" adds the same recipe with model tradeoffs and the optional `--db-url` path; the previous §6 becomes §7.
- **Verified:** pre-flight (pytest 422/422, ruff on CI scope, mypy) stays green; the live `run_b1_live` invocation produces `COMPLETE` against a real GitLab project with Claude-generated SQL distinct from the fixture. Plan: `docs/planning/scope-b-plan.md` §7.1 (Phase B1). Scope B-2 (scripted-answers intake) and optional Scope B-3 (Web UI bridge) remain open.
- **Deviation from plan:** §8.2 recommended `claude-sonnet-4-6`; user chose `claude-opus-4-7` for the first real run to remove "was it the model?" as a confounding variable if output quality is poor. `--model` flag added to the B1 surface (not in original §8.2); default is opus.

### 2026-04-16 — First live end-to-end smoke test (Session 22, Scope A)

- **Fixed:** `scripts/run_pipeline.py:119` passed `url=` to `PythonGitLabAdapter(...)` but the constructor's keyword-only parameter is `host_url=`. `--live --host gitlab` had never successfully run — it would have raised `TypeError: __init__() got an unexpected keyword argument 'url'` on the first keystroke. Phase 4B/5 tests only exercise the adapter's import + protocol conformance, not the script's wiring. Two-character fix: `url=host_url` → `host_url=host_url`.
- **Verified:** after the fix, `uv run python scripts/run_pipeline.py --live --host gitlab --run-id run_live_002` created a real project at `https://gitlab.com/rmsharp-modelpilot/subrogation-pilot` (project ID 81385820, initial commit `3dec542`). Website stage latency: 3,501 ms live vs ~4 ms fake-mode (~875× expected network overhead). All 38 files present, all 10 tier-3-moderate governance artifacts created, regulatory mapping correct (SR_11_7 + NAIC_AIS).
- **Findings (filed in `BACKLOG.md`, unfixed in this session):**
  - `MPC_NAMESPACE` takes a group *path* (`rmsharp-modelpilot`), not a URL (`https://gitlab.com/rmsharp-modelpilot`). `.env.example`, `OPERATIONS.md` §1, and `docs/tutorial.md` §5 all omit this constraint. The GitLab adapter returns a generic `"group lookup failed: 404"` instead of a clearer `"MPC_NAMESPACE must be a group path, not a URL"`.
  - `scripts/` is not ruff-checked in CI (`.github/workflows/ci.yml` lint job scans `src/ tests/ packages/` only). Running ruff locally surfaces 10 `E402` errors in `scripts/run_pipeline.py` — the `sys.path.insert` pattern needs `# noqa: E402` or a refactor.
  - `packages/` is not mypy-checked in CI (typecheck job runs `mypy src/` only) even though `[tool.mypy] packages = [...]` in `pyproject.toml` declares both `model_project_constructor` and `model_project_constructor_data_agent`. Running mypy with the declared packages surfaces 13 errors, mostly in `packages/data-agent/.../anthropic_client.py:218` — Anthropic SDK's content-block union has grown ~8 variants since the code was written, and `block.text` no longer narrows without a type guard. Latent bit-rot.
  - `docs/tutorial.md` §5c says `MPC_HOST_URL="https://github.mycompany.com/api/v3"` works for GitHub Enterprise, but `scripts/run_pipeline.py:109-113` constructs `PyGithubAdapter(token=token)` with no URL argument — GHE users silently hit public `api.github.com`. (Not live-tested in Session 22; confirmed by code reading.)
  - `docs/tutorial.md` §5 and `OPERATIONS.md` §4.2/4.3 document two different live-run commands. The OPERATIONS recipe uses `python -m model_project_constructor.agents.website --intake ... --host ... --private-token ...` — this flow should be reconciled with the script path or marked deprecated.

### 2026-04-16 — Gotcha cleanup (Session 21)

- **Changed:** `CHANGELOG.md` refreshed from Session-0-only stub to full authoritative history covering Phases 1–6, Phases A–D, pilot readiness, tutorial, wiki expansion, and license change.
- **Changed:** `BACKLOG.md` "Up Next" items "Pilot readiness audit" and "Ruff cleanup sweep" removed — both completed in Session 17 and now recorded here.
- **Changed:** `SESSION_NOTES.md` duplicate "What Session 18 Did" block and duplicate Session 17 handoff-evaluation block collapsed.

### 2026-04-16 — Wiki expansion (Sessions 19, 20A, 20B)

- **Added:** 14 initial wiki pages for `claims-model-starter` (Session 19) — Home, Getting Started, Pipeline Overview, Generated Project Structure, Governance Framework, Development Workflow, Data Guide, Agent Reference, Monitoring and Operations, Software Bill of Materials, Architecture Decisions, Glossary, Content Recommendations, plus `_Sidebar` navigation. Commit: `0acdd37`.
- **Added:** Intake Interview Design, Schema Reference, Security Considerations wiki pages (Session 20A). Covers the two intake system prompts verbatim, full Pydantic schema set field-by-field, outbound-network boundaries, and 9-item security review checklist. Commit: `8c96f24`.
- **Added:** Worked Examples, Extending the Pipeline, Changelog (wiki), Contributing wiki pages (Session 20B). Traces subrogation and renewal-profitability end-to-end; documents 4 extension surfaces; derives CI/test conventions from actual config. Commit: `9535665`.
- **Changed:** License updated from Proprietary to **MIT** across `LICENSE`, both `pyproject.toml` files, and the SBOM wiki page. Commit: `f2f2a70`.
- **Changed:** Session 19 close-out formalized with scored self-assessment and 20A/20B handoff plan. Commits: `06ef12f`, `da8d485`.

### 2026-04-16 — End-to-end pipeline script + tutorial (Session 18)

- **Added:** `scripts/run_pipeline.py` — 265-line driver running Intake → Data → Website end-to-end against fixture data with `FakeRepoClient`. `--live` flag switches to real GitLab / GitHub hosts. Tested against both `--host gitlab` and `--host github`. Commit: `4dc2f5d`.
- **Added:** `docs/tutorial.md` — six-step user tutorial (YAML fixture → IntakeReport JSON → pipeline run → checkpoint inspection → live host → programmatic API). Restructured during user testing to start with fixture creation. Commits: `4dc2f5d`, `1613d60`.
- **Fixed:** tutorial install command was missing `--extra ui`. Commit: `883935a`.
- **Changed:** replaced conflated "likelihood" with "probability" in fixtures, tests, and `initial_purpose.txt` where referring to `P(event)` (5 files). Commit: `1613d60`.
- **Added:** BACKLOG items for data-source discovery (intake + data agents), statistical terminology glossary, `render_tutorial.sh`, tutorial copy-UX, SQLite connection warnings. Commits: `1f9c28a`, `9f3399c`.
- **Added:** `.orchestrator/`, `my_intake.yaml`, `my_intake_report.json` to `.gitignore`.
- **Docs:** session-18 close-out handoff notes. Commit: `d831414`.

---

## [0.1.0 — Pilot Ready] — 2026-04-15

Phases 1 through 6 complete. 422 tests at 97.18% coverage. Both GitLab and GitHub adapters pass structural + integration tests. CI green across lint, typecheck, test, and decoupling jobs.

### Pilot readiness audit (Session 17) — 2026-04-15

- **Added:** `docs/planning/pilot-readiness-audit.md` — all 46 `architecture-plan.md §14` acceptance criteria verified against the actual codebase. PILOT-READY declared. Commit: `b9c87c7`.
- **Fixed:** all 62 pre-existing ruff errors across `src/`, `tests/`, `packages/` (56 auto-fixable + 6 manual). Commit: `17f661d`.
- **Fixed:** 3 CI workflow failures — mypy typecheck missing `--extra ui`, decoupling job below coverage floor, CLI tests failing on ANSI color codes (resolved via `click.unstyle`). Commits: `d62efc2`, `b8d8d7e`.
- **Changed:** `BACKLOG.md` marks all 6 phases complete. Commit: `66b44c8`.

### Phase 6 — Production hardening (Session 16) — 2026-04-15

- **Added:** structured logging via `make_logged_runner` — binds `run_id` and `correlation_id` to every stage's log context; emits `agent.start` / `agent.end` / `agent.error` events.
- **Added:** in-memory metrics via `MetricsRegistry` + `make_measured_runner` — run counts, status distribution, per-agent latency; no external metrics backend required.
- **Added:** `OrchestratorSettings.from_env()` — env-var-driven configuration with `require_*` guards that fail fast on missing credentials; `.env.example` template.
- **Added:** `.github/workflows/ci.yml` — four-job CI pipeline (`lint`, `typecheck`, `test`, `decoupling`).
- **Added:** `OPERATIONS.md` production runbook and `TROUBLESHOOTING.md` diagnostics (one walkthrough per `FAILED_AT_*` path).
- **Added:** 54 new tests across 3 new test files.
- **Design:** zero new dependencies. Observability is opt-in via runner composition — `pipeline.py` has no imports from the new logging/metrics modules.
- **Changed:** coverage floor raised 93% → 94%. Commit: `c3943a8`.
- Feature commit: `2060d4a`.

### Phase 5 — Orchestrator (Session 15) — 2026-04-15

- **Added:** `src/model_project_constructor/orchestrator/` package — `pipeline.py` (sequential `run_pipeline` driver per §12), `adapters.py` (sole `IntakeReport ↔ DataRequest` bridge enforcing the §7 decoupling invariant), `checkpoints.py` (`CheckpointStore` with envelope save/load + `save_result` for terminal artifacts).
- **Added:** `PipelineStatus` literal — `COMPLETE` / `FAILED_AT_INTAKE` / `FAILED_AT_DATA` / `FAILED_AT_WEBSITE`. Partial state retained on halt so operators can inspect or resume.
- **Added:** 45 new tests under `tests/orchestrator/` — happy path for both `ci_platform="gitlab"` and `ci_platform="github"` with positive + negative CI-file assertions; halt behavior for each `FAILED_AT_*` path; downstream-agent-not-called guards; adapter inference rules; envelope round-trip; terminal-result filename isolation; run-directory isolation.
- Commit: `b94cb47`.

### Phases A–D — GitHub/GitLab abstraction (Sessions 10–14) — 2026-04-15

- **Phase A (Session 11) — Neutral rename:** `GitLabClient → RepoClient` protocol, `GitLabTarget → RepoTarget`, `GitLabProjectResult → RepoProjectResult`. Widened `project_id: int → str` to accommodate the GitHub `owner/repo` form. Commit: `8c00e1a`.
- **Phase B (Session 12) — GitHub Actions CI template:** `render_github_actions_ci` sibling to the GitLab CI renderer; `ci_platform` plumbed through website agent state and governance manifest. Commit: `9b2ab5e`.
- **Phase C (Session 13) — PyGithub adapter:** `PyGithubAdapter` sibling to `PythonGitLabAdapter`, both implementing the neutral `RepoClient` protocol. `PyGithub` added as dependency (LGPL-3.0; usage is pure API, no redistribution). Commit: `55745ed`.
- **Phase D (Session 14) — Host selection CLI:** website CLI `--host gitlab|github` flag; dual-adapter selection; README mentions GitHub alongside GitLab. Commits: `e9f0d10`, `9f20e95`.
- **Supporting:** abstraction plan document — `f44f8dc` (Session 10). Session close-out handoffs — `0f132e0`, `a869904`, `a7fa214`, `72ac527`, `c996c0d`.
- **Changed:** coverage floor raised 90% → 93%. Commit: `e91c9f2`.

### Phase 4B — Website Agent governance (Session 9) — 2026-04-14

- **Added:** governance artifact scaffolding proportional to `risk_tier` and `cycle_time` per architecture-plan §8. `is_governance_artifact` classifier is the single source of truth for which files are governance-tagged (derives manifest rather than storing in state).
- **Added:** `python-gitlab` adapter (`PythonGitLabAdapter`) implementing the original `GitLabClient` protocol (later renamed to `RepoClient` in Phase A).
- **Added:** retry / exponential backoff on repo operations.
- Commit: `f97b530`.
- Session close-out: `5a20eb8`.

### Phase 4A — Website Agent core (Session 8) — 2026-04-14

- **Added:** Website Agent LangGraph flow — `CREATE_PROJECT`, `SCAFFOLD_BASE`, `INITIAL_COMMITS` nodes.
- **Added:** initial model build scaffolding — `.qmd` files, `src/` module stubs, reasonable defaults clearly marked where human judgment is required.
- Commit: `9887286`.
- Session close-out: `c996c0d`.

### Phase 3B — Intake Agent web UI (Session 7) — 2026-04-14

- **Added:** FastAPI + SSE + HTMX frontend for interactive interview sessions.
- **Added:** SQLite session persistence and resume.
- **Added:** fixture statelessness preserved for testing (fixtures bypass DB).
- Commit: `1c1141a`.
- Session close-out: `41449a8`.

### Phase 3A — Intake Agent core (Session 6) — 2026-04-14

- **Added:** Intake Agent LangGraph flow — expert data-scientist / business-analyst / consultant persona for P&C claims domain.
- **Added:** one-question-at-a-time interview with `MAX_QUESTIONS=10` and `MAX_REVISIONS=3` budgets.
- **Added:** `IntakeReport` generation with `GovernanceMetadata` (risk tier, cycle time, model type) re-derived on every revision.
- **Added:** 6-token review-loop accept vocabulary in `nodes.py`.
- **Added:** fixture-driven CLI.
- Commit: `64b8a99`.
- Session close-out: `081cb20`.

### Phase 2B — Data Agent standalone package (Session 5) — 2026-04-14

- **Changed:** Data Agent refactored into standalone package `packages/data-agent/` with its own `pyproject.toml` — enables reuse as a query-writing tool for analyst teams per `initial_purpose.txt`. Commit: `4982332`.
- **Added:** `AnthropicLLMClient`, Typer CLI, Python API docs. Commit: `aca858a`.
- **Changed:** coverage floor raised 80% → 90%. Commit: `0b30014`.
- Session close-out: `0c0197e`.

### Phase 2A — Data Agent core (Session 4) — 2026-04-14

- **Added:** Data Agent LangGraph flow — query generation, quality checks, data expectation confirmation.
- **Added:** AST-walk decoupling test ensuring the Data Agent has no `intake` imports (enforces the §7 invariant that the only `IntakeReport ↔ DataRequest` bridge is the orchestrator adapter).
- **Added:** `sqlparse`-based SQL parse validation for generated queries.
- **Added:** `DataAgent.run(DataRequest) -> DataReport`.
- Commit: `e526332`.
- Session close-out: `5c73ed0` (also restored `pyproject` readme field, added `README.md`).
- Session 4 claim + `uv` bootstrap: `530bff9`.

### Phase 1 — Architecture plan and payload schemas (Sessions 2–3) — 2026-04-14

- **Added:** `docs/planning/architecture-plan.md` — full pipeline architecture with 14 phases of work planned (6 product phases + 4 abstraction phases + planning/hardening). Commit: `5bf0d8a`.
- **Added:** repo skeleton, v1 payload schemas (`IntakeReport`, `DataRequest`, `DataReport`, plus what later became `RepoTarget` / `RepoProjectResult`), `HandoffEnvelope` with versioning, `schemas/registry.py`. Commit: `f94e211`.

### Early architecture exploration — 2026-04-10

- **Added:** architecture-approaches document with pros/cons for 4 critical features (authored before the canonical architecture-plan). Commit: `4a9840c`.

---

## [0.0.1 — Project bootstrap] — 2026-04-10

Initial commit of methodology framework and empty project scaffolding. Commit: `ff0228e`.

### 2026-04-10 — Project setup (Session 0)

- **Added:** Iterative Session Methodology (v2.1) — `SESSION_RUNNER.md`, `SAFEGUARDS.md`, `SESSION_NOTES.md`, `methodology_dashboard.py`.
- **Added:** Task tracking files — `BACKLOG.md`, `CHANGELOG.md`, `ROADMAP.md`.
- **Added:** Framework reference docs in `docs/methodology/` (`ITERATIVE_METHODOLOGY.md`, `HOW_TO_USE.md`, 5 workstreams).
- **Added:** `CLAUDE.md` with session protocol, pipeline description, agent design principles, and worked examples from `initial_purpose.txt`.
- **Customized:** `SESSION_RUNNER.md` Phase 1 task-to-workstream mapping for project-specific work types (agent building, prompt engineering, schema definition, pipeline integration).
- **Populated:** `ROADMAP.md` with 5 milestones, pipeline overview table, and domain context.
- **Populated:** `BACKLOG.md` with granular tasks broken down by milestone (Architecture, Intake Agent, Data Agent, Website Agent, Integration).
- **Populated:** `SESSION_NOTES.md` with active task, key files, and gotchas for the first real session.
