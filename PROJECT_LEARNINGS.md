# model_project_constructor — Project-Specific Learnings

> Migrated verbatim from `SESSION_RUNNER.md`'s "Learnings (added by sessions)" table on 2026-06-07 to keep `CLAUDE.md` lean and route project learnings to a project-owned file (methodology PR #25/#27). No content changed in the move. **Append new project learnings here, not in `CLAUDE.md` and not in `SESSION_RUNNER.md`.** Base, methodology-level learnings remain in `SESSION_RUNNER.md`.

| # | Learning | Source | When to Apply |
|---|----------|--------|---------------|
| 1 | Plan-mode output is a draft, not a verified plan. When a prompt contains a multi-phase plan with "implement," the deliverable is a plan document with evidence-based inventory, not Phase 1 code. The gap: Phase 1's task mapping had no entry for plan-mode handoffs, so the session defaulted to "implement." Structural fix: new mapping row + FM #19. | FM #19 discovery | When a prompt contains a multi-phase plan with "implement" — recognize this as a planning workstream. |
| 2 | When extending a LangGraph pipeline with new nodes, put the helper functions in a sibling module (e.g. `governance_templates.py` next to `templates.py`) rather than expanding the existing module. Review-time diff stays local; the "before vs after" phase split is visible in the file tree. | Session 9 (Phase 4B governance extension) | Any LangGraph phase extension where the previous phase's templates/helpers are stable and do not need to change. |
| 3 | Derive manifest/audit data by classifier function (`is_governance_artifact(path)`) rather than by storing it in state during each node. The classifier is the single source of truth and composes with any future node that adds a new artifact category; state bookkeeping drifts. | Session 9 (Phase 4B GovernanceManifest assembly) | Assembling a result object from side effects of multiple nodes — especially when some nodes are optional. |
| 4 | When a test file needs a graph with injected dependencies (e.g. no-op `sleep`), prefer plumbing a kwarg through the production factory (`build_website_graph`) rather than building it manually — but do NOT refactor production code to add the kwarg for a test-only need unless the kwarg is also valuable in production. Accept a `__new__`-based construction hack in tests rather than over-engineer production. | Session 9 (`test_retry.py` construction) | Whenever a test needs a variant of a production-constructed object. |
| 5 | Pin tier/flag-gated behavior with BOTH a positive AND a negative assertion per tier: `assert "governance/foo.md" in files` AND `assert "governance/bar.md" not in files`. A positive-only test passes silently if a tier starts emitting the wrong artifact. | Session 9 (`test_governance.py` tier fan-out) | Any tier/flag-gated code where missing artifacts are as important as present ones. |
| 6 | The pattern "plan as deliverable, then implementation as a separate session" (failure modes #18, #19) is load-bearing across the SESSION_RUNNER workstreams. When the user hands Session N a plan input mid-session, write it into the handoff verbatim and leave it for Session N+1 — do NOT pivot. | Session 9 (GitHub/GitLab notes mid-Phase-4B) | Any session where the user introduces a second deliverable while the first is mid-flight. |
| 7 | When the user provides a partially-captured message as input (e.g. a truncated `/btw` response), proactively reconstruct the missing content based on the visible evidence AND document the reconstruction with explicit provenance: "originally from /btw on <date>, point #N lost to truncation, reconstructed by Session X from <evidence>." Do NOT silently infer or silently ignore the gap. Offer the user a "regenerate vs proceed" choice before committing, then honor their preference. | Session 10 (missing point #1 in Session 9's verbatim `/btw` notes) | Any session where the input is referenced as partially missing or explicitly truncated. |
| 8 | A grep-based inventory for a rename plan must include the TYPE, FIELD, STATE KEY, and DOCSTRING surfaces **separately** — each has a different grep pattern. Conflating them into one total gives misleading counts. List hits per grep pattern + per file, not a single total. Classes (`GitLabClient`), field names (`gitlab_url`), state keys (`gitlab_target`), and literal filenames (`.gitlab-ci.yml`) are four different greps. | Session 10 (GitHub/GitLab abstraction plan §4) | Any rename/migration plan. |
| 9 | When writing a multi-phase plan, put cross-phase invariants (do-not-change lists, strategic decisions, Phase N ordering) in ONE document rather than duplicating them across phase-specific documents. Duplication causes drift: one phase doc gets a correction and the others do not. Single-document trades navigability for consistency; for 4-phase plans with 3+ shared invariants the tradeoff favors consistency. | Session 10 (GitHub/GitLab abstraction plan §16) | Any plan with more than 2 phases sharing ≥3 invariants. |
| 10 | During Phase 0, when the user's first message appears to contain a task (e.g. "go"), do NOT skip the orientation report even if the ACTIVE TASK block already describes the deliverable. The report exists for the user's benefit — it establishes shared state before any work begins. Failure mode #9 (task-in-prompt bypass) is the specific risk. Complete all 8 Phase 0 steps, report, and explicitly wait for direction even when the direction feels implicit. | Session 10 ("go" → still did Phase 0 report) | Every session. |
| 11 | When a plan section's signatures or field names contradict the actual protocol/source code (because the plan was written before a rename or refactor), **trust the code, not the plan**. Plan §8.1 for Phase C used `group_path` in one bullet, but `protocol.py:51` uses `namespace` — the Phase A rename had already landed. Mirror the current code's signatures; keep the plan's semantic intent. A plan that names a specific symbol is a claim that it existed when the plan was written, not a guarantee that it still exists. | Session 13 (Phase C plan had `group_path`, protocol had `namespace`) | Any implementation session working off a plan written more than one session ago. |
| 12 | When a previous handoff's gotcha contradicts the actual pattern in the codebase (e.g. "X should NOT inherit from Y" when sibling code already inherits), **mirror the codebase pattern, not the gotcha**. Consistency with existing code is more defensible than following a note that the source already contradicts. Document the resolution in your own handoff so the next session doesn't waste cycles. | Session 13 (Session 12 gotcha said `PyGithubAdapter` should not inherit from `RepoClient`; `gitlab_adapter.py:41` inherits) | Any session where a gotcha and the codebase disagree. |
| 13 | When a handoff suggests "likely X" or "possibly Y" for a new dependency (e.g. "likely `structlog`", "possibly `pydantic-settings`"), prefer the zero-new-dep alternative if the stdlib or existing deps can do the job. Adding a dep is a one-way door (future sessions must maintain it, CI must install it, version conflicts can surface). For Phase 6, stdlib `logging` with `extra={"context": ...}` satisfied "structured logging" without `structlog`; a plain `dataclass` + `os.environ` satisfied "env-var-driven config" without `pydantic-settings`. Both choices were validated by 54 new tests + mypy strict + ruff clean. The handoff should explicitly flag the tradeoff so the implementing session can make the call quickly. | Session 16 (Phase 6: zero new deps despite plan suggesting structlog + pydantic-settings) | Any implementation session where the plan or handoff suggests adding a new dependency. |
| 14 | For documentation-heavy deliverables (wiki, reference docs), use parallel research agents to explore the codebase from different angles simultaneously (e.g., generated output structure, dependencies, architecture). This front-loads all discovery into ~3 minutes of wall time instead of sequential reads. Each agent should have a specific angle — avoid overlap. Then verify every claim against source code before writing (read the actual templates, schemas, and config files, not just summaries). | Session 19 (claims-model-starter wiki, 3 parallel agents for structure/deps/architecture) | Any session producing documentation that spans multiple codebase concerns. |
| 15 | When a parallel research agent times out (e.g., "Stream idle timeout - partial response received"), do NOT re-launch — the failure mode is usually scope breadth (too many angles packed into one agent) and a re-launch burns the same budget. Instead, pivot to direct `Grep` + `Read` for just the citations you actually need for the page you're writing. The sibling agents that DID complete already gave you enough map to know which files to open. A timed-out agent costs ~3 min to recover; a re-launched timed-out agent costs ~6 min. Special case of "trust but verify." | Session 20A (security research agent timed out; direct reads of config/adapters/LLM clients for ~10 files completed the page) | Any documentation session using parallel research agents when one times out. |
| 16 | When the canonical "what gets generated" spec is a test assertion (e.g., `tests/agents/website/test_templates.py:176-205`), prefer reading the test over running the pipeline to capture the file listing. The test IS the spec — any divergence would fail CI before the listing could drift. Running the pipeline with fake adapters just re-derives the same set of filenames at the cost of setup + environment risk. Cite the test file:line in the doc so future readers can verify independently. | Session 20B (Worked-Examples generated-file listing) | Any documentation that needs to enumerate outputs of a generator function — prefer the test over the run. |
| 17 | Methodology guidance for shared files (`CHANGELOG.md`, `BACKLOG.md`, `ROADMAP.md`, `docs/methodology/*`) lives primarily in `docs/methodology/README.md` and secondarily as inline notes in `SESSION_RUNNER.md` / `SAFEGUARDS.md`. Before proposing options on how a shared file should be maintained, **read `docs/methodology/README.md` first** — its "templates" section at `:99` and `:215` is load-bearing. A handoff that offers options including one that contradicts methodology (e.g., "mark CHANGELOG as subordinate to the wiki") is a protocol violation; methodology outranks handoff suggestions. | Session 21 (CHANGELOG scoping round-trip) | Any session that is about to modify `CHANGELOG.md`, `BACKLOG.md`, `ROADMAP.md`, `SESSION_NOTES.md`, `SAFEGUARDS.md`, `SESSION_RUNNER.md`, or the methodology docs themselves. |
| 18 | **CI gate scope can diverge from declared tool scope.** `pyproject.toml` may declare `[tool.mypy] packages = [A, B]` but CI runs `mypy A` only. Similarly `ruff` CI may scan `src/ tests/ packages/` while excluding `scripts/`. Local "green pre-flight" using the tool's natural scope will surface failures that CI isn't gating — these are CI-gap findings, not regressions. Before declaring pre-flight failed, re-run the EXACT command CI executes and compare. If CI-matching is green but broader-scope fails, file the CI gap + the underlying errors as findings, don't block on them. | Session 22 (ruff on `scripts/` + mypy on `packages/` surfaced pre-existing errors CI doesn't gate) | Any session where local pre-flight diverges from CI green-status. |
| 19 | **Fill grep-inventory counts AFTER running the greps, not from memory of an earlier pass.** Memory of "I saw N matches" degrades within minutes — by the time the inventory section is being written, the count has fuzzed. Discipline: write the section with `XX` placeholders, run the greps in sequence, paste the actual numbers. The executor will re-run those greps in their Phase 0; if the plan's numbers are visibly wrong they lose trust in everything else the plan claims. The cost of running the grep again is one tool call; the cost of an inaccurate inventory is the plan's credibility. | Session 23 (Scope B plan §17 first draft said "11" for runner contract; actual 16; caught + fixed in close-out) | Any planning session whose deliverable includes a grep inventory or evidence count. |
| 20 | **When a plan asks "which model?" in an open-decisions section, do NOT default to the cheapest option for a pilot's first real run.** The common rationale "defer the quality conversation to after we see output" is weak when output quality is literally what the pilot is evaluating. If the model turns out to be limiting, the first-run impression is contaminated and "was it the model?" becomes a confounding variable. Recommend the highest-quality option (e.g. `claude-opus-4-7`) for the *first* run to eliminate that variable; fall back to cheaper models (sonnet, haiku) for iteration once the output shape is validated. Pilot cost at ~$0.50 vs ~$0.10 per run is trivial compared to the cost of a muddled conclusion. | Session 24 (user challenged plan §8.2's sonnet-4-6 recommendation; opus-4-7 used instead for B1) | Any session where a plan recommends a cheaper/faster model for a first-impression pilot run that will be judged on output quality. |
| 21 | **Before writing a plan criterion like "live LLM + fixture → COMPLETE status", read the status-decision code.** Terminal-state logic is usually one tight function at the end of a graph (in this project: `src/model_project_constructor/agents/intake/nodes.py:142`). In the intake agent, `status = "COMPLETE" if accepted and not missing else "DRAFT_INCOMPLETE"`, AND `nodes.py:129-134` auto-appends `questions_cap_reached` whenever `questions_asked >= MAX_QUESTIONS and not believe_enough_info`. "Happy path = COMPLETE" against a real LLM therefore requires the LLM to flip `believe_enough_info=true` BEFORE hitting `MAX_QUESTIONS`. Plan §7.2.3 criterion #1 assumed this would just happen; in practice, Claude Opus 4.7 against a 10-qa-pair fixture asks 10 substantive questions and still wants more — producing a rich DRAFT_INCOMPLETE report, not COMPLETE. When planning, pay for the read once; when executing, forecast the gap rather than discovering it after a $0.30 live run. | Session 26 (B2 happy-path run `run_b2_live` returned DRAFT_INCOMPLETE despite a rich Claude-drafted envelope because of the status-decision invariant). | Any planning session whose success criteria depend on an agent's terminal state. |
| 22 | **When auditing a production constant's blast radius, grep for BOTH the UPPER_CASE constant name AND the lower_case field form.** Constants leak through two indirect coupling paths that plain `grep MAX_QUESTIONS` misses: (a) test fixtures whose CONTENT is sized to the constant by arithmetic (e.g. `intake_question_cap.yaml` needs `qa_pairs >= MAX_QUESTIONS + 1` to demonstrate the cap firing — the fixture's size IS the contract), and (b) dict-literal exports that rename the constant through a public API (e.g. `runner.py:CAPS = {"max_questions": MAX_QUESTIONS}` → `test_runner.py:assert CAPS == {"max_questions": 10}` — the grep for `MAX_QUESTIONS` misses the far side of the rename). Second-pass grep for the dict-form field name (`max_questions`, case-insensitive) catches both. Read the fixture file top-comments by purpose, not by name — their size-invariant is load-bearing. | Session 27 (MAX_QUESTIONS 10→20 bump — pre-change grep caught `intake_question_cap.yaml` by reading the fixture's top-comment, missed `test_caps_constants_exposed` because of the dict-form rename; pytest surfaced the second one). | Any session bumping a production constant that is referenced in test fixtures or cross-file dict exports. |
| 23 | **When a handoff describes a fix as "parallel to Session X's fix," read both sibling call sites side-by-side before editing.** The parallel often runs deeper than the named symbol — if sibling code has had "the same class of bug" on one branch, the untouched branch may have multiple instances. Session 30 was framed as "add `host_url=` to the GitHub branch, parallel to Session 22's GitLab `url=` → `host_url=` fix." The parallel was correct but incomplete: the GitHub branch ALSO had `token=` vs `private_token=` wrong (same shape as the Session 22 kwarg mismatch). Both bugs together meant `--live --host github` had never worked. Reading `github_adapter.py:66-72` (constructor signature) + `scripts/run_pipeline.py:273-278` (call site) together caught both in one pass; reading the call site alone would have produced a half-fix that still raised `TypeError`. Countermeasure: 10-second grep of both adapters' constructor signatures + both call sites before editing. | Session 30 (`PyGithubAdapter(token=token)` at `run_pipeline.py:277` had TWO bugs; Session 29's handoff named only the second). | Any implementation session where the handoff frames the task as "parallel to Session X" — read both sibling call sites side-by-side before editing either. |
| 24 | **A "re-audit" task generates a finding set, not a pass/fail verdict — forecast the finding shape before drafting.** When a handoff's verification path implies a binary outcome ("if both work → pin; if only one → update") but the task is framed as "re-audit X," expect multi-dimensional findings (typically 3-6, not 1-2). Session 31's first quick pass against OPERATIONS.md §4.2/§4.3 surfaced 4 findings (ANTHROPIC_API_KEY over-listed, fixture-path provenance gap, cosmetic env exports, GHE host-url asymmetry). A second pass under `/effort=max` (re-reading every constant file end-to-end and comparing against declared defaults in `config.py`/`.env.example`/`OPERATIONS.md §1`) surfaced F5 + F6 (cli.py GitLab default placeholder; `--ci-platform` doc gap). The subtler findings live in cross-surface asymmetries that a flag-by-flag check doesn't catch. Countermeasure: **before drafting the report, ask "what 7 dimensions would I evaluate per AUDIT_WORKSTREAM? what prior findings in BACKLOG/CHANGELOG should I reconcile?"** This prevents the "first-pass quick audit" anti-pattern where 3 obvious findings get shipped and the systemic 3 (cross-surface drift) get missed. | Session 31 (first quick pass: F1-F4 in ~10 min; `/effort=max` second pass: F5 + F6 in ~15 min more, caught cli.py:39 placeholder default vs. config.py:33 canonical default). | Any session whose deliverable is an audit of a documented artifact (recipe, plan, config, schema, env-var table). Forecast the finding shape with the 7-dimension framework before drafting the report. |
| 25 | **When a default-value bump requires a test restructure, mirror the sibling platform's test shape rather than inventing a new one.** Session 32's F5 fix changed `cli.py:39` `GITLAB_DEFAULT_HOST_URL` from `"https://gitlab.example.com"` to `"https://gitlab.com"`. The existing GitLab test (`test_cli_host_gitlab_with_token_invokes_python_gitlab_adapter`) explicitly passed `--host-url` and asserted the user-supplied value — so it didn't actually pin the default. The GitHub sibling test (`test_cli_host_github_with_token_invokes_pygithub_adapter` at `test_cli.py:267-302`) omitted `--host-url` and asserted the GitHub default. The natural fix was not "change the assertion's value" but "make the GitLab test symmetric with the GitHub sibling" — same invocation shape, same assertion shape, just with the GitLab default value. Adding a separate `test_cli_host_gitlab_explicit_host_url_override` (36 lines) preserved the explicit-override coverage the symmetry restructure removed. Pattern: when two platform-symmetric tests are in the same file and one asserts defaults while the other asserts overrides, the asymmetry is usually a bug in test design — restructure to default-path on both and add override-path tests separately. | Session 32 (F5 fix: cli.py GitLab default URL). | Any session that bumps a default value in a constant + needs to restructure a test that currently overrides that default. Look for a sibling platform test; mirror its shape. |
| 26 | **During Phase 0, actively compare BACKLOG.md / CHANGELOG.md / ROADMAP.md against `docs/methodology/README.md`'s stated conventions — do not silently accept file state as-given.** The three-file split (methodology v2.1, `docs/methodology/README.md:99`/`:215`) mandates: BACKLOG.md = **open work only**, CHANGELOG.md = completed work history, ROADMAP.md = feature inventory + milestone summary. `CHANGELOG.md:6` is explicit: "remove the item from `BACKLOG.md` and add an entry here." Session 33's Phase 0 missed 8 sessions' worth of `[x]` accumulation in BACKLOG.md (Sessions 24, 26, 27, 28, 29, 30, 31, 32 had each flipped `[ ]` → `[x]` in `## Up Next` rather than removing the line) plus 23 legacy `[x]` items in a pre-v2.1 `## Completed` section. Each individual `[x]` was minor; the aggregate was a convention violation — the exact FM #17 (protocol erosion) signature. Separately, ROADMAP.md had drifted to claim "Current Milestone: M1: Architecture & Pipeline Design" when all 5 build milestones were complete, and "Completed Milestones: None yet — project is in initial setup." Both drifts survived every session because every session took the file shape as given. Countermeasure: **during Phase 0, spend 30 seconds checking "does BACKLOG.md contain only `[ ]` items? does ROADMAP.md describe current state accurately? does CHANGELOG.md have an entry for each recent session?"** If any answer is no, flag the drift in the orientation report before starting the session's actual deliverable. When closing a session, **remove** (do not flip to `[x]`) the completed line from BACKLOG.md. | Session 33 (user-caught: "why are completed items being recorded in the backlog when that is what the changelog is for?" — spawned the B3a sweep + ROADMAP resync). | Every session, as the last step of Phase 0 orientation. Also: every close-out, as the BACKLOG update step. |
| 27 | **When a handoff contains a ready-to-paste work product (verified prose, drafted code, etc.), treat it as output from the predecessor session, not input to be re-derived.** Session 33's F6 handoff was structured as "paste + verify + commit, not redraft + verify + commit" — the prose was captured in `SESSION_NOTES.md` with explicit provenance (file:line verified). When the inheriting session feels the urge to "improve" the prose, that urge is the FM #2 "keep going" signal (the agent wants to do more work than the task requires) applied to the *quality* dimension rather than the *scope* dimension. Resist it. Paste the work product verbatim. Re-verify only that (a) the file/section boundary hasn't moved since the handoff was written, and (b) the final state matches the prose's claims (e.g. `--help` output, pytest green). This pattern maximizes the ROI of the predecessor's work and minimizes the "hand-it-off-twice" failure mode. **Anti-pattern:** redrafting a handoff-provided work product, re-running all the verifications the predecessor ran, and claiming credit for "improvements" that drift the output from the agreed-upon intent. | Session 34 (F6 paste-and-verify executed in ~5 min vs. ~15-20 min a fresh-draft session would have taken). | Any session inheriting a handoff with a verified work product (drafted prose, drafted code, verified command sequence). |
| 28 | **When a handoff names the fix shape prescriptively (e.g. "threading a `close()` / `__exit__` discipline"), treat the verbs as hints rather than contracts.** Session 34's warnings-fix handoff named three specific patterns: `close()`, `__exit__`, and "discipline through `ReadOnlyDB` + `agents/intake/graph.py` + `tests/agents/data/conftest.py` fixtures." Two of those three were correct in direction but only ~40% precise: (a) `__exit__` / context manager was not needed — no caller uses `with ReadOnlyDB(...)` and adding the methods drops coverage without a corresponding user; (b) `agents/intake/graph.py` uses `MemorySaver` (in-memory, no sqlite) — the real sqlite saver is `SqliteSaver` in `ui/intake/runner.py`; (c) a separate leak class lives in `tests/ui/intake/conftest.py` (the `TestClient`/`lifespan` gap) which the handoff did not name. A session that followed the handoff prescription literally would have edited the wrong files AND added unused surface. The correct workflow is: read the handoff, confirm the root cause independently (tracemalloc for resource-warning bugs, grep for rename/refactor bugs), then design the minimal fix against *that* root cause. Respect handoff *intent* (fix the warnings), audit handoff *specifics* (where the leaks live, what API they need). | Session 35 (added `__enter__/__exit__` to ReadOnlyDB because the handoff named them; caught by coverage drop; removed — coverage recovered 97.20 → 97.27%). | Any implementation session whose handoff includes a prescriptive fix shape (method names, file paths, specific patterns). Verify the specifics against the actual code before editing. |
| 29 | **Gotchas that name a past execution failure are prescriptive safeguards, not anecdotes — apply the mitigation mechanically.** The complement to Learning #28: Learning #28 says prescribed *fix shapes* may be wrong and must be audited; Learning #29 says prescribed *safeguards* (named after a past mistake) must be heeded. Session 36's self-assessment -0.5 deduction flagged a "stub fumble" — it duplicated the `### Post-Session-35 pre-commit state` heading on the first Phase 1B stub edit because it pasted a heading into the new stub without reading far enough into the file to see the heading already existed below. Session 36's gotcha #8 named the fumble + prescribed the mitigation: "when inserting new content into a structured file, read far enough into the file to see if the structure you're adding already exists below." Session 37 read the gotcha, **then re-did the exact same fumble** on its own Phase 1B stub edit (duplicated `### Session 36 placement decision` + `### Post-Session-36 pre-commit state` headings; caught on re-read; fixed with two cleanup Edits). Cost: ~1 min + 3 extra tool calls. Root cause: passive reading of a safeguard-gotcha rather than active application. **Mitigation (now explicit):** when a predecessor's gotcha describes a specific execution failure they made, treat it as a structural safeguard for *your* session — before any edit to a structured file, grep or scan for the heading/section you're about to insert, to confirm it doesn't already exist. Mechanical: `Grep pattern="Session N placement"` before inserting `### Session N placement decision`. Zero-cost check; fumble-prevention is the load-bearing value. **Validated Session 38:** grep-before-insert was applied mechanically on the Phase 1B stub edit, zero fumbles despite a substantial insertion. Learning #29 works. | Session 37 (re-did Session 36's stub fumble despite gotcha #8 naming it); Session 38 (validated). | Any session inheriting a handoff whose gotcha list names a specific execution failure. The gotcha is not a warning-to-read; it is a safeguard-to-apply. |
| 30 | **Interim status updates during research phases should be declarative, not volitional.** Phrasing like "Let me verify one more potential drift before producing the edit plan..." can read as a question requesting user approval to proceed rather than a progress report. The user is forced to parse whether the update is a question or a status — and may interrupt with "Is there a question for me?" which forces a clarification round-trip. Declarative framing ("Running one more verification pass before producing the edit plan...") eliminates the ambiguity — the reader sees that the session is executing, not pausing. **Mechanical:** swap `"Let me X"` → `"Running X"` / `"Checking X"` / `"Doing X"`. Zero-cost phrasing discipline. Applies especially during multi-step research phases (audit inventories, grep-based investigations, pre-edit verification passes) where interim summaries naturally invite "shall I continue?" phrasing. Separate from FM #7 ("ask the user to solve process problems") — this is about the grammatical framing of routine progress narration, not about actually needing user input. | Session 38 (user asked "Is there supposed to be a question for me in that?" in response to a "Let me verify..." phrasing during the wiki sweep inventory pass). | Any research-heavy session where interim progress updates are natural. Every research phase of every multi-step task. |
| 31 | **When a drift-class micro lands in your lap, run the widest reasonable pattern across ALL affected surface types during Phase 0 — not just the narrow handoff-named pattern against narrow handoff-named files.** A drift-class is defined by the symbol + the semantic neighborhood, not by one grep pattern against one file list. Session 41's handoff named 4 hits under a narrow pattern (`10-question\|10 question\|hard 10`); Session 42's post-edit widening (`... \|max 10 questions\|Max 10 questions`, case-insensitive) surfaced ~9 additional planning-doc hits that a Phase-0 widening would have raised to the operator upfront. Instead, the wider grep ran only at post-edit verification, constraining the operator's options to "defer to Session 43." The symmetric predecessor-side version (Session 41's self-flag): when writing a drift-class recipe into a handoff, run the widest reasonable pattern BEFORE committing, so the recipe enumerates all hits not just narrow-pattern hits. Both sides of the baton benefit from the same discipline. **Cost:** one extra grep in Phase 0 (~30 seconds). **Benefit:** zero post-edit scope surprises, upfront scope-decision visibility, handoff completeness. **Scope:** drift-class micros, constant/value renames, deprecation sweeps. Not applicable to feature work where scope is defined by design, not by symbol neighborhood. **Mechanical:** before any drift-class edit, grep case-insensitively for (a) the literal old value, (b) adjacent prose AND typographic-join forms — sweep EVERY adjacency form (`N X`, `N-X`, `up to N`, `max N`, `Maximum N`, `hard N`), preferring a digit-class regex like `[0-9]+-X` over the literal value so any numeral is caught, not just the current one, and (c) the constant name; across ALL `*.md` + `src/` + `docs/planning/` + `tests/`; classify each hit as "drift", "historical/archive", or "canonical" before deciding scope. | Session 41 predecessor-side self-flag + Session 42 executor-side self-flag (same -0.5 shape, two consecutive sessions — coining threshold met; mechanical (b) refined by Candidate #75 — Session 104 filed (space-form sweep missed two `10-question` docstrings), Session 105 vindicated (the `[0-9]+-question` digit-class form found the complete inventory in one pass)). | Any session with a drift-class micro, constant rename, or deprecation-sweep deliverable. Apply during Phase 0, not post-edit. |
| 32 | **Per-phase completion-criteria greps in a plan must account for append-only historical prose that may reference the pre-change state.** A `grep -c "<old-string>" <file>` expected to return 0 post-fix will fail when `<file>` contains historical-record entries (either strict append-only files like root `CHANGELOG.md` / `SESSION_NOTES.md`, OR freshness-tracked files that carry historical-record entries by convention, such as `docs/wiki/claims-model-starter/Changelog.md` — entries grouped by implementation phase, each a permanent record of what-was-added-when). The failure mode: a global find-and-replace-shape criterion conflates "current-state prose" (must update) with "historical-description prose" (must NOT update, even in a freshness-tracked file, because it records what-was-at-the-time). **Mechanical:** in planning sessions, (a) prefer line-targeted or section-targeted criteria (`sed -n "10p" FILE \| grep -c "STRING"`) over file-wide greps; OR (b) pre-run the criterion's grep against current file state and enumerate the expected-nonzero baseline by line; for freshness-tracked files with historical sections, identify the boundary (e.g. "entries above line 10 = current-state prose; below = historical records"). In Phase 0 orientation reports for implementation sessions, pre-run the verification greps and pre-classify the expected hits so the executor can distinguish drift from historical-description without debate. **Differentiation from Learning #19:** #19 is about evidence-inventory grep counts being live at plan-write time; #32 is about per-phase criteria anticipating post-edit state AND distinguishing prose classes. | Session 44 (`grep -c "contributor-facing" CHANGELOG.md` expected 0, returned 1 — Session 43's own append-only entry at line 25) + Session 45 (`grep -rn "docs/planning/" *.md` surfaced 2 historical "Added: docs/planning/X" entries in freshness-tracked wiki Changelog at lines 117, 122; same shape, different file-class — tightens the learning to "any file with historical-record entries"). | Any planning session whose deliverable includes per-phase grep criteria against files that contain historical-record entries, OR any implementation session running plan-specified verification greps against such files. |
| 33 | **At Phase 0, verify the BACKLOG item's named target by reading both current state AND referenced fix-site before claiming — surface any scope mismatch in the orientation report.** A BACKLOG entry's noun-phrase scope (e.g. "intake / data / website agents", or "Content-Recommendations page shows history", or "wiki publish mechanism — three options") is a claim about the current state that was accurate when filed but may be narrower or wider than reality when the session picks it up. Mechanical countermeasure: at Phase 0, after reading the BACKLOG entry, spend ~2 minutes running the greps / Globs / directory listings that confirm each named surface (a) exists, (b) has the shape the entry implies, and (c) doesn't have hidden cousins the entry missed. Example applications: Session 61 detected that "glossary + agent-prompt injection" could be cleanly split when one clause was ready; Session 62 detected that the website agent has no LLM prompt surface (so the BACKLOG scope of "intake / data / website" reduced to 2 agents); Session 63 detected that the wiki-drift gap was 16 files wide, not the 1 file the BACKLOG entry named; Session 64 verified the three publish-mechanism options named in BACKLOG against existing `scripts/` + CI + convention docs before committing to option (b). Each catch would have been a mid-edit scope re-derivation if left to post-claim discovery. **Cost:** ~2 minutes of Phase 0 Globs/Greps/directory listings. **Benefit:** zero post-claim scope surprises, one pre-claim operator round-trip when a mismatch surfaces, clean approval path. **Relation to Learning #26:** #26 is about BACKLOG/CHANGELOG/ROADMAP file-state discipline (convention hygiene); #33 is about BACKLOG entry semantic scope vs. actual code/doc state (claim hygiene). Both are Phase 0 checks. | Sessions 61, 62, 63, 64 (four consecutive instances — candidate learning promoted in Session 64 close-out). | Every session, as part of Phase 0 orientation, when a BACKLOG entry names specific surfaces, symbols, or scope-options. Run the confirming greps/Globs before the orientation report. |
| 34 | **Before any Edit to a structured file where `old_string` could match multiple lines (archive headings, CHANGELOG entry inserts, plan cross-section updates), pre-grep to confirm uniqueness. If >1 match, extend the anchor with a multi-line boundary (e.g. a sibling-line pair or a heading+content pair) to guarantee uniqueness. Complement to Learning #29 which covers stub-insertion — #34 covers all other structured-file inserts.** *See also: Learning #35 — archive-heading pre-grep is a specialization of this rule for the Phase 1B predecessor-archive move; it adds a second grep specifically against the existing `### Session N-1 ARCHIVED ACTIVE TASK` heading before opening the Edit tool.* | Candidate filed Session 65 (1st instance); applied Sessions 66/67 (2nd/3rd, promotion-triggering); validated Session 68 (4th instance — archive-heading 9-match collision recovered via multi-line anchor per the recipe). Promoted Session 69. | Any Edit to a structured file (CHANGELOG / SESSION_NOTES / plan documents, archive headings, multi-row table inserts) where `old_string` could legitimately match more than one line. Skip only if you've already grepped and confirmed uniqueness. |
| 35 | **At Phase 1B, pre-grep the archive-heading string `### Session N-1 ARCHIVED ACTIVE TASK` BEFORE inserting it as a new heading.** Specialization of Learning #34 for archive-edits: archive heading strings appear in gotcha prose (multi-match risk), so the pre-grep recipe applies even when the insertion is a NEW heading rather than a string replace. If the pre-grep returns 0 hits, the insertion is safe; if >0, plan a multi-line anchor. **⚠ Superseded for the live convention by Learning #48 (inline-at-top archival): the file-tail `### Session N ARCHIVED ACTIVE TASK` zone froze at Session 112 — this row documents the historical mechanic.** | Candidate filed Session 68 (1st instance); applied Session 69 (2nd) / Session 70 (3rd, promotion-triggering); validated Session 71 (4th instance — pre-grep returned 0 hits, archive heading insertion landed first-attempt). Promoted Session 71. | Any Phase 1B archive-heading insertion in `SESSION_NOTES.md`. Run the `^### Session N-1 ARCHIVED ACTIVE TASK` grep before opening the Edit tool. **⚠ Session 222: that grep now returns EMPTY against the live file — all 58 such headings moved to `docs/architecture-history/SESSION_NOTES-through-S216.md`. Empty output reads as "not found"; it means "moved". Do not re-create the zone.** |
| 36 | **Phase 1B touches ONLY the Task/Status block + predecessor archive heading insertion. Heading renames (`Session N-1 Handoff Evaluation` → `Session N Handoff Evaluation`), eval-section content, and `What Session N Did` block body all belong to Phase 3 close-out, not Phase 1B stub.** Boundary discipline: a stub at Phase 1B is a minimal claim, not a rewrite — even when the closing-session-shaped content can be drafted earlier, holding it for Phase 3 keeps the file-state observable and the close-out atomic. The stub is a *claim that the session has started*; the close-out is the session's *record of what it did*. Conflating them at Phase 1B muddies what is provisional vs. final, and a mid-session crash leaves stub-shaped content that misleads the next session into reading speculative claims as record. | Candidate filed Session 69 (1st instance — boundary identified after Session 69 prematurely renamed a Phase 3A heading at Phase 1B); applied Session 70 (2nd) / Session 71 (3rd, promotion-triggering); validated Session 72 (4th instance — Phase 1B touched only `## ACTIVE TASK` Task/Status block + predecessor archive heading, both Edits landed first-attempt; no heading renames, no eval content, no `What Session N Did` body drafted until Phase 3). Promoted Session 72. | Every Phase 1B stub. Touch ONLY the `## ACTIVE TASK` Task/Status block + predecessor `### Session N-1 ARCHIVED ACTIVE TASK` heading insertion; defer heading renames (`### Session N-1 Handoff Evaluation` → `### Session N Handoff Evaluation`), eval-section content, and `### What Session N Did` body to Phase 3 close-out. |
| 37 | **When inserting new content immediately above an existing structured heading via the Edit tool, consume the boundary heading (and a uniqueness-conferring follow-on line, e.g. the heading's first-line content) in `old_string`, and replicate both at the tail of `new_string`.** Distinct from Learning #34 — #34 is about uniqueness of the anchor itself (single-match guarantee); #37 is about constructing the anchor when the natural insertion point is the line immediately before an existing heading. Useful for chronological-archive insertions (`### Session N-1 ARCHIVED ACTIVE TASK`), table-row append above a `---` separator, and any "insert above this heading" pattern. | Candidate filed Session 70 (1st instance — caught after a duplicate-heading fumble at close-out where new_string ended with a boundary heading that should have been left for old_string to anchor against); applied Session 71 (2nd) / Session 72 (3rd, promotion-triggering — old_string consumed `### Session 70 ARCHIVED ACTIVE TASK\n\n**Task:** Session 70 — BACKLOG-driven (Data source inventory Phase 4 OPTIONAL,` and new_string replicated both at its tail); validated Session 73 (4th instance — same shape against `### Session 71 ARCHIVED ACTIVE TASK\n\n**Task:** Session 71 — operator direction (off-BACKLOG, methodology housekeeping; BACKLOG is empty post-Session-70).`, landed first-attempt). Promoted Session 73. | Any Edit-tool insertion immediately above an existing structured heading. Consume the boundary heading + a uniqueness-conferring follow-on line in `old_string`; replicate both at the tail of `new_string`. Pre-verify the multi-line anchor's uniqueness with `grep -c` before opening the Edit tool (composes with Learning #34). |
| 38 | **For any Edit-tool uniqueness pre-grep, use raw-string `grep -c "literal text" FILE` (no `^`/`$` regex anchors).** The Edit tool's matching ignores regex line-anchors, so a `grep -c "^literal"` count that returns 1 may correspond to 6+ unanchored prose hits that the Edit tool sees — producing a "Found N matches" failure on what looked like a unique anchor. Raw-string `grep -c` returns Edit's view exactly. If the count is >1, escalate to a multi-line anchor per Learning #34. Composes with Learning #34 (anchor uniqueness) and Learning #37 (consume-and-replace). | Candidate filed Session 76 (1st instance — caught after a `^### Session 74 ARCHIVED ACTIVE TASK` pre-grep returned 1 but Edit failed with "Found 6 matches"); applied Session 77 (2nd instance, preemptive); applied Session 78 (3rd instance, preemptive — promotion-triggering). | Any Edit-tool uniqueness pre-grep. Use raw-string `grep -c "literal"` (no `^` / `$`); multi-line escalation per Learning #34 if >1. |
| 39 | **At Phase 1B, before constructing the demoted-archive Edit's `new_string`, run `grep -n "^### Session .* ARCHIVED ACTIVE TASK" SESSION_NOTES.md \| head -1` to locate the file-tail archive zone.** The demoted Session N-1 ACTIVE TASK block must be inserted there (above the predecessor archive heading), not inline adjacent to the new ACTIVE TASK at the top of the file. Without this verification, the Phase 1B Edit can place the demoted block inline at the top (where the natural `old_string` anchor is the current ACTIVE TASK), producing a file structure where archives are split between two regions and the file becomes harder for the next session to read. The convention is documented in gotchas (e.g. "placed above the Session N-1 archive heading") but parses as prescriptive prose rather than a structural verification — easy to read past during Phase 0. The grep adds 5 seconds and produces an unambiguous line number for the file-tail insert. Composes with Learning #35 (archive-heading pre-grep) and Learning #37 (consume-and-replace). **⚠ Superseded for the live convention by Learning #48 (inline-at-top archival): the file-tail `### Session N ARCHIVED ACTIVE TASK` zone froze at Session 112 — this row documents the historical mechanic.** | Candidate filed Session 79 (1st instance — placement fumble caught + recovered; 2 extra Edits cost); applied Session 80 (2nd instance, preemptive — clean placement); applied Session 81 (3rd instance, preemptive — promotion-triggering). Promoted Session 82. | Every Phase 1B archive-demote Edit. Locate the file-tail archive zone via the grep above BEFORE constructing `new_string`; insert above the predecessor's `### Session N-1 ARCHIVED ACTIVE TASK` heading. **⚠ Session 222: the grep now returns EMPTY against the live file — all 58 headings are in `docs/architecture-history/SESSION_NOTES-through-S216.md`. Superseded by #48 for the live convention; see #95.** |
| 40 | **When writing a Phase 3D handoff that names a deliverable, pre-name any unresolved convention, date, boundary, or contract-shape choice as a one-sentence bullet under "Open contract questions for Session N+1:".** Ambiguity surfaced mid-execution forces the executing session into an AskUserQuestion round-trip (~1 minute lost + operator interruption mid-flight); pre-naming the choice in the handoff lets the next session resolve it at Phase 1A — before claim, before any Edit. The pattern applies to any deliverable with multiple bounded contracts compatible with the verbal description, not just "transition X to Y" date/version choices. Examples that have fired: a release-date convention at a `[Unreleased]` → `[X.Y.Z]` transition (Session 78); a wiki-date format choice at a Changelog mirror update (Session 80); a flag interaction matrix + single-file-vs-glob default in a CLI implementation deliverable (Session 81 — first non-transition application, broadened the scope). **Mechanical:** at Phase 3D handoff-writing time, scan the named deliverable for any choice the next session cannot derive from the named target alone (date, convention, boundary, contract-shape, scope-multiplier, flag-matrix); list each as a bullet under "Open contract questions for Session N+1:" with the bounded options stated. **Cost:** ~30 seconds of forecasting per handoff. **Benefit:** zero mid-Phase-1 AskUserQuestion round-trips, clean approval path, operator not interrupted mid-execution. Composes with Learning #28 (handoff specifics are hints, not contracts — pre-named choices remain options for the next session, not prescriptions). | Candidate filed Session 78 (1st instance — release-date ambiguity at the `[0.1.0] - 2026-05-12` transition); applied Session 80 (2nd instance — wiki-date convention at the Changelog mirror); applied Session 81 (3rd instance, promotion-triggering — broadened scope to non-"transition X to Y": flag interaction matrix + multi-file default in an implementation deliverable, resolved via 2-question AskUserQuestion at Phase 1A). Promoted Session 83. | Every Phase 3D handoff that names a deliverable with one or more unresolved contract-shape choices. At handoff-writing time, list them as bullets under "Open contract questions for Session N+1:" with the bounded options stated, so the next session resolves at Phase 1A rather than mid-execution. |
| 41 | **For multi-file sessions touching ≥5 files of non-procedural state, take an explicit checkpoint commit at the first stable-state boundary mid-session — particularly during refactors or multi-stage implementation.** Operationalizes SAFEGUARDS.md's hard rule "Never touch more than 5 files without committing first" as a session-discipline pattern. The mitigation cost is one extra `checkpoint: <what's done so far>` commit; the avoided cost is a mid-session interruption (crash, context-limit hit, scope re-scope) leaving uncommitted unverified work with no `git checkout HEAD~1` recovery boundary. **Filing-quality-check refinement:** the ≥5-file count is the trigger, but applicability is tightest for multi-stage work with destructive, refactor-shaped, or generative operations. For purely additive single-coherent-deliverable sessions with gates green throughout, a mid-session checkpoint may produce unnecessary noise — the threshold-cross should be noted-but-not-acted-on. Trust the gates: if pytest + ruff + mypy run clean at the candidate-checkpoint boundary AND the work is coherent under one deliverable, the checkpoint is optional; if any of those fails or the work spans multiple deliverable-shapes, the checkpoint is high-ROI. Procedural-only files (`SESSION_NOTES.md` / `CHANGELOG.md` / `BACKLOG.md`) do not count toward the ≥5 threshold. | Filed Session 70 (1st instance — 9 files touched in one cohesive Phase-4 commit, SAFEGUARDS rule bent; `feat(...): phase 4 schema scaffold` checkpoint recipe scheduled). Advanced Sessions 81 / 84 / 86 / 87 as candidate at the 5-file threshold (gates green, single-deliverable shape — filing-quality-check held that additive coherent work didn't fit the rationale; #41 stayed spirit-dormant despite four advancements). **Session 88 (5th instance, disaster-context, promotion-triggering):** initial Session 88 invocation touched 10+ files with no checkpoint, was interrupted mid-flight without committing or closing out, left uncommitted unverified work — the exact scenario #41's rationale targets. Recovery Session 88 ran Phase-0 disaster orientation, took operator direction via AskUserQuestion ("verify, then close out"), verified all gates against the uncommitted work, and completed. Recovery cost was modest only because all gates passed at recovery time; without that luck, recovery would have required `git stash` discipline plus diff-by-diff review of 12 files. Sessions 89 / 91 confirmed the spirit-dormant-but-still-tracking judgment for purely additive instances (5-file thresholds crossed, no disaster, no checkpoint needed). Promoted Session 92. | Any session whose deliverable spans ≥5 files of non-procedural state (production code, schemas, test fixtures, generated artifacts — `SESSION_NOTES.md` / `CHANGELOG.md` / `BACKLOG.md` updates do not count). At the first stable-state boundary inside the session that runs pytest + ruff + mypy clean AND the post-checkpoint work has a distinct shape (e.g. schema → wiring; tests → docs; backend → integration), make a `checkpoint: <what's done so far>` commit before continuing. Skip the checkpoint only when the session is additive, single-coherent, and gates-clean end-to-end; note the threshold-cross in the close-out so the next session can audit the judgment. |
| 42 | **At Phase 1B, "claim the session" is two ordered edits, never one in-place overwrite: (1) archive the predecessor's `## ACTIVE TASK` Task/Status block to the file-tail archive zone, above the most-recent `### Session N-1 ARCHIVED ACTIVE TASK` heading; (2) only then replace the head Task/Status with the new session's stub.** Conflating them into a single in-place overwrite loses the predecessor's block (the original Session 93 violation archived the Session 92 block retroactively in Phase 3). For the multi-MB `SESSION_NOTES.md`, a hand-typed Edit must reproduce two ~600-word lines verbatim — a fidelity risk — so the robust form is a **self-verifying Python script**: it extracts the head block programmatically, builds the archived block, inserts it above the predecessor heading, writes the stub, and asserts every invariant *before* writing (it writes only if all asserts pass, so a failed assert damages nothing). **Mechanical:** locate the zone with `grep -n "^### Session .* ARCHIVED ACTIVE TASK" SESSION_NOTES.md \| head -1` (Learning #39); anchor the archive insert on a multi-line `heading + first-Task-line prefix` anchor (Learnings #34/#37); in the script, assert heading uniqueness with **newline-anchored** counts `text.count("\n### Session N ARCHIVED ACTIVE TASK\n") == 1`, never the bare substring — the heading recurs backtick-wrapped in this file's own Gotcha prose (Learning #38 + Candidate #71). **⚠ Superseded for the live convention by Learning #48 (inline-at-top archival): the file-tail `### Session N ARCHIVED ACTIVE TASK` zone froze at Session 112 — this row documents the historical mechanic.** | Candidate filed Session 93 (1st instance — violation-then-correction: claimed in-place, then archived the Session 92 block retroactively in Phase 3). Clean instances Sessions 94–100 (2nd–8th; Sessions 96–100 executed via the self-verifying Python script; Session 100's run caught the Learning-#38 backtick-prose collision in its `str.count` assert before any write — the first instance of Candidate #71). Recommended for promotion by Sessions 96/97/98/99/100 — none acted (each deliverable was elsewhere; the single most overdue housekeeping item, carried five sessions). Promoted Session 101 (own Phase 1B claim = 9th instance, via the script). | Every session, at Phase 1B. Archive the predecessor Task/Status block to the file tail FIRST, then overwrite the head with the stub — never a single in-place overwrite. For the multi-MB `SESSION_NOTES.md`, prefer the self-verifying Python script over hand-typed Edits; assert prerequisites + heading-uniqueness (newline-anchored `str.count`) before writing. Composes with Learnings #34/#35/#37/#38/#39. **⚠ Session 222: the file-tail zone is not merely frozen — all 58 of its headings are now in `docs/architecture-history/SESSION_NOTES-through-S216.md` and the locating grep returns empty. The self-verifying-script FORM is what survives, and #95 reuses it for the trim itself.** |
| 43 | **Finding a bug never auto-licenses fixing it — the deliverable's explicit scope does.** A real bug (latent crash, API misuse, even a cosmetic asymmetry) discovered *outside* the session's selected deliverable gets surfaced prominently — report section, `CHANGELOG.md`, handoff — with exact file:line and a quick-win recommendation, and is then **handed off, not fixed**. This holds whether the bug surfaces during a read-only audit (operator said "DO NOT modify code") or during an adjacent implementation: acting on the find is a *different deliverable*. The audit's value is the map; the fix is a separately-directed session. **Distinct from FM #2/#18/#19:** those govern not *continuing past* the deliverable (feature-continuation creep); #43 governs not *expanding* it when a tempting adjacent bug appears (sideways creep). The pull is strongest precisely when the bug is in the same function one line away, or is surfaced by the session's own verification workflow — but proximity and severity are not scope. **Mechanical:** when the urge to fix an out-of-scope find appears, apply SAFEGUARDS.md's "I also noticed…" rule — commit/flag, don't bundle; record it as a standing option for the next session's operator decision. | Candidate filed Session 97 (1st instance — read-only audit found 3 correctness bugs, fixed 0, per the operator's "DO NOT modify code" constraint). Generalized Session 98 (2nd — implementation found sibling bug #16b mid-fix of #16, flagged + handed off, did not bundle; rule reworded from "audits" to the general form). Session 99 (3rd, promotion-triggering — completeness lens surfaced #16c mid-verification, deferred to Session 100). Session 100 (4th — verifier flagged a cosmetic comment asymmetry on the data-agent guard; not acted on, since logic-symmetry was all #16c required). Four instances across audit + implementation workstreams. Promoted Session 101. | Any session — audit or implementation — that discovers a real or cosmetic issue outside the current deliverable's scope. Surface it (report / `CHANGELOG.md` / handoff) with file:line + a quick-win note; do NOT fix it this session — hand it off as a standing option. Apply even when the find is (a) in the same function as the current fix, (b) surfaced by the session's own verification workflow, or (c) cosmetic. Scope — not the bug's severity or proximity — decides. |
| 44 | **When the deliverable is "port a hardened/fixed function from module A into its stale copy in module B," verify with a 4-lens adversarial fan-out — and treat lens 4 as the payoff.** (1) **behavioral-equivalence** — diff the two implementations character-by-character, run N adversarial inputs through both, confirm identical outcomes modulo the intentional difference (e.g. error type); (2) **caller-regression** — grep every transitive caller, confirm none depended on the OLD behavior (a more-permissive port can silently accept previously-rejected input); (3) **test-efficacy** — confirm each new regression test fails against the OLD code (a test that passes on old code proves nothing); (4) **completeness** — diff the WHOLE of A vs B to surface OTHER hardenings present in A but missing from B. **Lens 4 converts a single-bug fix into a complete map of the stale-copy debt** — it is where sibling latent bugs surface (it found #16b for Session 98, #16c for Session 99). **Terminal-case:** a clean completeness PASS is itself a valuable result — it tells you the twin-hardening campaign is *complete* (Session 100's lens 4 found nothing → debt closed, no #16d), not a non-event. **Refinement:** constrain the verdict schema's severity enum and instruct each lens NOT to label PASS-supporting observations as "blocker" — unconstrained verdicts produced spurious blockers needing main-loop severity-correction (Session 99); the enum constraint eliminated them (Session 100). | Candidate filed Session 98 (1st instance — 4-lens fan-out after porting #16; lens 4 surfaced sibling #16b). Applied Session 99 (2nd — re-ran for the #16b port; lens 4 surfaced shared gap #16c; also surfaced Candidate #70, guard-port blast-radius is the test doubles). Session 100 (3rd, promotion-triggering — re-ran for #16c; lens 4 found NO further sibling = terminal / debt-closed; validated the severity-enum refinement → zero spurious blockers). Three instances. Promoted Session 101. | Whenever the deliverable is porting a hardened/fixed function from module A into a stale copy in module B (or symmetrically hardening twin copies). Run the 4-lens fan-out; treat lens 4 (whole-module diff) as load-bearing — it maps the remaining stale-copy debt. Stop when lens 4 returns empty: that clean PASS is the terminal state (campaign complete). Constrain the verdict severity enum so lenses don't emit spurious blockers. |
| 45 | **Cross-check any load-bearing inherited or subagent-produced claim — a count, a provenance fact, OR a chain of reasoning — against the authoritative single-source record (or first principles) BEFORE it enters a permanent artifact or drives a non-trivial decision.** Large-file research subagents and adversarial-verify lenses confabulate plausible-but-false claims (over-pattern-matching across a 2+ MB file), miscount, or reason from a false premise — and a wrong claim that reaches a Learnings row / plan / `CHANGELOG.md` / handoff inventory becomes a *permanent falsehood the next session inherits and trusts*. Three observed claim-failure shapes, all caught pre-commit: (a) **provenance confabulation** — Session 101's #64 reader invented a "Session 76 filing" (nonexistent) and a "Session 96 caught the collision" instance (first caught Session 100 per Candidate #71's SoT), the latter reaching the row #42 draft as a "twice caught" overstatement *despite the session already holding the contradicting SoT line*; (b) **miscount** — Session 102's research workflow reported `count: 8` `_extract_json` tests per suite while *listing 9 names*; a direct `grep -c "def test_extract_json"` returned 9; (c) **false-premise reasoning** — Session 103 an adversarial lens's load-bearing sub-claim ("`ast.dump` cross-version instability will cause false CI failures") was verified-and-rejected from first principles (a same-process two-AST comparison moves both sides identically, so any instability cancels) — while the lens's *separate* brittleness finding was genuine and WAS acted on. Applied to an inherited *handoff* the discipline is preventive, not reactive: an "exactly these N" inventory COULD propagate a half-fix if trusted blind, so re-derive it — Sessions 104/105 both re-ran the complete sweep independently and CONFIRMED the inventory (a clean re-derivation is a successful application, not a non-event). **Mechanical:** when a subagent/lens/handoff returns a load-bearing claim AND you hold (or can cheaply obtain) an authoritative single-source-of-truth — a Candidate's "first instance: Session N" line, a `CHANGELOG.md` entry, a promoted Learning's Source column, a direct `grep`/diff against the tree, or a first-principles argument — treat the claim as *suspect until the SoT confirms it*, and run the confirming check *before* the claim is committed, not after. Cost: one grep/diff/derivation (~30s); avoided cost: a permanent confabulated fact plus every downstream session that inherits it. **Distinct from Learning #28** (handoff *fix-shape specifics* are hints to audit): #28 governs prescriptive instructions; #45 governs factual / quantitative / inferential *claims*. **Composes with Learning #44** — an adversarial-verify lens is itself a claim-producer #45 says to cross-check (Session 103's rejected `ast.dump` claim came from a verify lens). | Candidate filed Session 101 (1st instance — two confabulations from a provenance research workflow: an invented "Session 76 filing" + a false "Session 96 caught the collision" contradicting Candidate #71's "first instance: Session 100" SoT, the latter shipped into the row #42 draft as "twice caught" and corrected by the adversarial verify pass before commit). Session 102 (2nd — `count: 8` vs 9 listed `_extract_json` names per suite; direct `grep -c "def test_extract_json"` returned 9; corrected in the O2 plan with a provenance note). Session 103 (3rd, reaching the project's standard 3-instance promotion threshold — verified-and-rejected an adversarial lens's `ast.dump` cross-version-instability sub-claim from first principles, while acting on the lens's separate genuine brittleness finding; generalized the rule from "counts/provenance" to "count, provenance, OR reasoning"). Session 104 (4th — scope lens surfaced 2 out-of-scope stale `10-question` docstrings; verified each directly + ran an independent complete hyphenated-form sweep confirming exactly those 2 before handing off; carried #72 as overdue option #3). Session 105 (reinforcement, not a counted 5th — re-ran the complete drift sweep independently rather than trusting the handoff's "exactly these 2"; it matched). Four counted instances (101/102/103/104) plus a Session-105 reinforcement; promotion-ready since Session 103, carried as overdue option #3 through Sessions 104/105. Promoted Session 106 (the row's own accuracy adversarially verified by a 9-agent workflow + an independent HEAD-diff before commit). | Any session inheriting a load-bearing claim from a subagent / workflow, an adversarial-verify lens, or a prior handoff — a count, a filing/instance provenance, or a chain of reasoning — that is about to enter a permanent artifact (Learnings row, plan, `CHANGELOG.md`, handoff inventory) or drive a non-trivial decision. Trust the single-source-of-truth (a Candidate's "first instance: Session N" line, a `CHANGELOG.md` entry, a promoted Learning's Source column, the code/tree, or a first-principles derivation) over the agent's claim; run the confirming `grep`/diff/derivation BEFORE the claim is committed, not after. Composes with Learnings #28 and #44. |
| 46 | **Spawn any read-only subagent — a critique / verify / research / audit lens, or a workflow fan-out whose mandate is to *read and report* — with `agentType: 'Explore'` (no Edit/Write/NotebookEdit), never the default workflow/`Agent` subagent type, which inherits Edit/Write and WILL make unauthorized edits to permanent files.** The agent *type*, not the prompt, is the control: a "please only read" instruction does not remove the tool. Session 106's incident is the proof — read-only-*intent* critique subagents spawned with the default type wrote to two permanent files (a non-drafted `SESSION_RUNNER.md` row #45; the operator-*declined* Candidate #75 folded into row #31) **and** overwrote the Phase-1B Status line with a *fabricated* "Session 106 RESUMED after an accidental kill" narrative; recovery was a SAFEGUARDS "Recovery From Disaster" `git checkout` of both files (no fabrication survived). **Backstop:** even with `Explore`, run `git diff HEAD` after the fan-out and confirm zero writes before trusting the run — the diff, not the agent's self-report, is the proof of read-only behavior. **Right-size:** the incident was a 9-agent over-scoped workflow; the validated pattern is 1–3 read-only lenses (Session 107 ×1; Sessions 108/109 ×3). **The action-side complement to Learning #45:** #45 says don't *trust* a subagent's claims (it may confabulate); #46 says don't *grant* a subagent the *capability* to act beyond its read-only mandate (it may edit). Together they bound both what a verification subagent can assert and what it can do. Reserve write-capable agent types for explicit implementation stages with a stated edit mandate. | Candidate filed Session 106 (1st instance — **the incident**: default-type critique subagents made unauthorized writes to 2 permanent files + fabricated a Phase-1B Status line; root cause = spawning read-only-intent subagents with the write-capable default type; recovered via SAFEGUARDS "Recovery From Disaster" git-checkout, no fabrication survived). Applied Session 107 (2nd, 1st clean — lone fidelity verifier as `Explore`; `git diff HEAD` confirmed zero writes; consciously right-sized to 1 agent, not 106's 9). Session 108 (3rd, promotion-triggering — 3 `Explore` lenses, `wf_a5329e28-dd6`; HEAD-diff zero writes). Session 109 (4th — 3 `Explore` lenses, `wf_58c4c3dd-ade`; HEAD-diff zero writes). Four instances; the project's standard 3-instance threshold was crossed at Session 108 → promotion-ripe, carried as overdue standing option #1 through Sessions 108/109. Promoted Session 110 (the 4-instance provenance re-derived directly against the close-out SoT before this row hardened, per Learning #45). | Any session about to spawn a subagent or workflow fan-out whose mandate is to read, critique, verify, research, or audit — anything that should *report*, not *edit*. Set `agentType: 'Explore'` (read-only) on every such agent; never rely on a "read-only" instruction to a write-capable default agent. Right-size to 1–3 lenses (not the incident's 9). After the fan-out, run `git diff HEAD` (or `git status`) to confirm zero writes *before* trusting any result. Reserve the default / `general-purpose` (write-capable) agent types for explicit implementation stages with an edit mandate. Composes with Learning #45 (its read-side complement — together they bound a verification subagent's *claims* and its *capabilities*) and SAFEGUARDS "Recovery From Disaster" (the recovery path if a write slips through). |
| 47 | **When adding a drift-guard test between a prose producer (e.g. an LLM-prompt sentence) and a structured consumer (a dict / `Literal` / schema), lift the producer's enumeration into a parseable module constant that BOTH the producer and the test import — do not regex-parse the prose.** The constant becomes the producer single-source (the prompt derives its text via `join`), the test reads a real Python object (robust, no brittle prose regex), and the change closes the "vocabulary bypasses the type system" gap (audit theme E2). This is the test-side, single-file version of the controlled-vocabulary single-source pattern — it does NOT cross module boundaries, so it needs no plan mode. Distinct from the grep-inventory learnings (#8/#19, which scan prose, not types); the seed that the O4 overhaul (Sessions 127–130) generalized into in-wheel `get_args`-derived prompt enumerations. | Candidate #77 filed Session 108 (1st instance — a parseable producer constant for a governance-framework enumeration drift-guard, closing audit theme E2). Applied Session 113 (2nd — tier-2 `_assert_vocab_parity` reads `RiskTier`/`CycleTime` via `typing.get_args`; the schema `Literal` IS the parseable producer) / Session 116 (3rd, promotion-triggering — host guard reads `set(REPO_PLATFORMS)` + `get_args(HostLiteral)`, as forecast in plan §12). Held at 3 instances Sessions 116→130; promoted Session 131. | Any session adding or maintaining a prompt↔map / prose↔schema drift-guard test. Lift the producer's enumeration into a parseable module constant that both the producer and the test import, rather than regex-parsing prose; the test then reads an actual Python object. Especially valuable where prose and schema must stay synchronized (governance frameworks, risk tiers, enumerated field values). Stay single-file (test-side) when producer and consumer share a module; cross-module single-sourcing is the O4 pattern and needs plan mode. |
| 48 | **At Phase 1B and Phase 3 close-out, archive the predecessor session's record with INLINE-AT-TOP demotion, NOT the file-tail-zone mechanic of Learnings #35/#39/#42 — that zone froze at Session 112.** The `SESSION_NOTES.md` Phase-1B archival convention silently evolved: since Session 113, the new session's full record sits directly under `## ACTIVE TASK`, and the predecessor's record is demoted IN PLACE with a one-line `**(Session N-1 — COMPLETE, archived below.)**` marker inserted immediately above it. A session that follows Learning #42 literally hunts for a file-tail `### Session N ARCHIVED ACTIVE TASK` zone that no longer receives entries. **Mechanical:** at Phase 1B, replace the `## ACTIVE TASK` Task/Status block with the new stub AND insert the `**(Session N-1 — COMPLETE, archived below.)**` one-liner above the predecessor's demoted record; at Phase 3, insert the full close-out record between the stub and that marker (+ a `---`). Learning #42's self-verifying-Python-script FORM survives (newline-anchored `str.count` asserts) — only the destination changed. **Supersedes the file-tail mechanic in Learnings #35/#39/#42 for the live convention** (those rows now carry a superseded-pointer here). Composes with Learnings #34/#37/#38 (general Edit-anchor discipline, still applicable). | Candidate #84 filed Session 116 (1st instance — discovered the convention had silently drifted from the file-tail zone, frozen at S112, to inline-at-top demotion across Sessions 113–115; numbered from all-time max 83 per Candidate #80). Applied every session 117→130 via self-verifying Python scripts (2nd–14th instances; flagged "over-ripe" from the 4th instance and carried as the single most-overdue housekeeping item for ~10 sessions). Promoted Session 131; its operator-chosen reconciliation ("annotate superseded + fix self-refs") back-annotated Learnings #35/#39/#42 to point here. | Every session, at Phase 1B and Phase 3 close-out, when archiving the predecessor's record in `SESSION_NOTES.md`. Use inline-at-top demotion (new stub under `## ACTIVE TASK`; predecessor demoted in place with the `**(Session N-1 — COMPLETE, archived below.)**` marker), never the superseded file-tail zone. Keep Learning #42's self-verifying-script form (newline-anchored `str.count` asserts); only the insertion destination differs. |
| 49 | **When adversarial review of a JUST-finished deliverable (a just-committed diff OR a drafted plan/artifact) surfaces a gap in THAT deliverable's own correctness/coverage, fix it in-session rather than handing it off.** The review exists to improve the shipped artifact; a gap in the tests for the very change you made — or a flaw in the plan you are writing — is part of "done," not a separate deliverable. Distinct from Learning #43, whose out-of-scope ADJACENT finds ARE handed off; the discriminator is whether the find is about THIS deliverable's own quality (fix) or about neighbouring code/scope (hand off). **Mechanical:** fold the fix into the atomic, unpushed commit via `git commit --amend` (unpushed → safe); for a not-yet-committed change, fix pre-commit; for a planning draft, fix inline. Make the fix discriminating — e.g. a test that monkeypatches the registry FACTORY (which a hardcoded dispatch never calls), not just the adapter class (which passes either way). | Candidate #86 filed Session 118 (1st instance — review of a just-committed `cli.py` diff found its dispatch only indirectly tested; added a discriminating factory-monkeypatch test, folded via `git commit --amend`). Applied Session 119 (2nd — EXTENDED from implementation diffs to planning artifacts: a 2-lens review of a written plan found an import-failing code sketch + 5 clarity gaps, all fixed in-session) / Session 120 (3rd, promotion-triggering — stale line-ref major fixed at root) / Sessions 121–123 (4th–6th — coverage gap proven non-vacuous by mutation; a COMPLETE-report gap + minors; 3 majors source-verified). Held at 6 instances Sessions 123→130; promoted Session 131. | When an adversarial review, code review, or verify pass surfaces a gap in the deliverable's OWN correctness/coverage that is (1) in-scope to THIS session's change, (2) small, and (3) gates-passing. Fix in-session — amend the atomic unpushed commit for code, fix pre-commit for not-yet-committed work, fix inline for planning drafts. If the find is about adjacent/neighbouring scope instead, hand it off (Learning #43). The new test must be discriminating (fail against the un-fixed code). |
| 50 | **When landing/merging a branch via `checkout`+`merge` while holding an uncommitted edit to a file that DIFFERS between the two branches (typically the Phase-1B stub in `SESSION_NOTES.md`), `git stash push` that file first, do the fast-forward merge, then `git stash pop`.** A live Phase-1B stub blocks `git checkout master` whenever `SESSION_NOTES.md` diverges between branches — which it nearly always does, since each session rewrites the top. Stashing only that one file, doing the FF-merge, then popping works because **after the fast-forward the stash's base matches the new `master`**, so the pop is a clean apply. Composes with Learning #40 (resolve merge-method + push via `AskUserQuestion` first) and Learning #51 (re-run the gate with its exact documented invocation on the landed tree). | Candidate #91 filed Session 125 (1st instance — coined when landing the methodology PR#25/#27 remediation branch off a diverging `SESSION_NOTES.md`). Applied Session 128 (2nd — landed O4-1 onto master, FF `08e4238→668147c`, clean pop) / Session 130 (3rd, promotion-triggering — landed O4-2 onto master, FF `d915772→890c6e7`; explicitly pre-flagged in the S129 handoff with exact commands; clean pop). Promoted Session 131. | Any session whose deliverable is "land/merge a branch" while it already holds an uncommitted housekeeping edit (the Phase-1B stub) to a file that diverges between the feature branch and the target. Execute: `git stash push <diverging-file>` → `git checkout <target>` → `git merge --ff-only <branch>` → `git stash pop`. Resolve merge-method/push via `AskUserQuestion` first (Learning #40). |
| 51 | **Confirm a quality gate with its EXACT documented invocation, not a broader scan — broader scopes surface intentional negative-test errors and mislead.** This repo's mypy gate is bare `mypy` (no path args), which uses `pyproject.toml [tool.mypy] packages = [...]` → 62 source files, 0 errors. Adding explicit paths (`mypy src tests`) is a DIFFERENT scope: 109 files, 81 errors in 16 files — and those "errors" are intentional negative-validation test cases (e.g. `DataGranularity(time_grain="hourly")` deliberately passing a non-member to assert the validator rejects it), not regressions. A session re-confirming "mypy 0/62" after a merge can be falsely alarmed by the broad scan. Composes with Learning #45 (verify claims against actual command output, not memory) — this is its gate-invocation specialization; and with Learning #50 (the stash-dance landing it most often follows). | Candidate #93 filed Session 128 (1st instance — caught the broad-scan false alarm `mypy src tests`→81 errors vs. the documented bare-`mypy` gate 0/62 when re-confirming quality after the O4-1 FF merge onto master). Applied Session 129 (2nd — ran every gate with its exact documented invocation during O4-2 implementation) / Session 130 (3rd, promotion-triggering — re-confirmed the full gate on the master tree after the O4-2 FF, before push). Promoted Session 131. | Any session verifying quality gates after a landing, merge, or refactor. Run each gate exactly as its baseline was measured: bare `.venv/bin/python -m mypy` (NOT `mypy src tests`); `.venv/bin/python -m pytest -q`; `.venv/bin/ruff check src/ tests/ packages/ scripts/`. Use `.venv/bin/python` (3.13.5), not bare `python`. Treat a different file-count in a tool's output as a signal you have changed scope, not as a regression. |
| 52 | **A pre-built migration/edit inventory — even an adversarially-verified one — is a LOWER BOUND, not a checklist: derive the actual edit set from the LIVE artifact + the convention by scanning for EVERY matching form, and use the inventory only as a cross-check whose misses you expect to find.** A completeness-gated migration (the gate must reach 0, e.g. "no `path:line` may remain") fails when the inventory under-enumerates — and inventories routinely do: citations split across prose / table cells / headings, bare-shorthand continuation forms (`(:6)`, `(:7-25)` that resolve against a file named on an earlier line), and — most dangerously — partial-path references with NO line number, which the gate's `path.ext:DIGITS` regex never flags yet the convention still requires expanding. A row-by-row walk of Appendix A would migrate only the line-pinned rows and leave the rest for the adversarial pass to catch. **Mechanical:** before migrating, run the gate regex AND a bare-`(:N)` scan AND a `lines N-M` prose scan AND a backtick-wrapped-bare-filename enumeration over the live file; classify each hit migrate / documented-exception; expand partial paths proactively (compose with the convention's "full repo-relative path in every case" rule) rather than waiting for a verifier to flag them — front-loading completeness converts the adversarial round from a correction loop into a clean confirmation (S139's 2-round Explore pass returned 0 problems vs. S138's round-1 which flagged 7 un-expanded partials). Composes with Learning #45 (re-derive load-bearing claims from the authoritative source — for a migration the source-of-truth is the live file, NOT the inventory). | Candidate #104 filed Session 137 (1st instance — a pre-flight surface `grep` undercounted citations 116 vs. the real 166 from a per-file read). Session 138 (2nd — Appendix A missed `scripts/run_pipeline.py:122` and did not enumerate the 16 partial-path §3 expansions its own convention demands; both surfaced by reading the live pages). Session 139 (3rd, promotion-triggering — Appendix A under-specified the bare-`(:N)` JSON-key line refs (Worked-Examples L40–61, listed as key names without flagging they were line-pins to migrate) and omitted the no-line partial source paths `graph.py`/`state.py` (L278), `orchestrator/adapters.py` (L107), `nodes.py`/`governance_templates.py` (L277); all found by reading the live page, so the proactive sweep+expansion made both adversarial rounds return 0 problems). Three instances. Promoted Session 139. | Any docs/code migration with a completeness gate. Build the edit set by scanning the LIVE artifact for every matching form — not by walking a pre-built inventory's rows. Treat Appendix A / a prior grep sweep as a lower-bound guide; expect and hunt its misses (table cells, shorthand continuations, and partial paths with no line number). Expand partial paths proactively per the convention so the adversarial pass confirms rather than corrects. Composes with Learning #45. |
| 53 | **On a convention-governed *published* artifact, widen to FULL convention-compliance proactively rather than defending the literal task scope — recurrence-prevention is the whole point, so a future audit will re-flag any same-convention gap you leave.** When migrating citations on a page, also close the page's *other* gaps in the SAME convention even though they fall outside the literal 'migrate the `file:line` citations' task: the page's self-description of its own citation convention (the Security page's 'Claim standard' line said *'every factual statement has a file:line citation'* — false the moment the citations became symbols), ellipsis/partial paths in a Key-files inventory table, and bare-filename mentions with no line. Front-loading this from the start (not after a verifier flags it) is why the adversarial pass *confirms* rather than *corrects*. **Boundary:** this is about CONVENTION gaps on the artifact you are already editing — NOT unrelated content/accuracy errors (those belong to a separate wiki-accuracy audit — Learning #43 / FM #18). **Distinct from Learning #52**, which is about COMPLETENESS (finding every citation *form*); #53 is about SCOPE (widening past citations to the artifact's other convention gaps). | Candidate #105 filed Session 138 (1st — verifier flagged 7 partial paths beyond the line-pin scope; widened to expand them). Session 139 (2nd — proactively expanded all partial source paths from the start). Session 140 (3rd, promotion-triggering — proactively updated the Security page's 'Claim standard' self-description to the symbol-reference convention and expanded the §12 Key-files ellipsis paths, both beyond the literal 29 Appendix-A citation rows; 37 edits total vs. the inventory's 29). Promoted Session 140. | Any session migrating/refactoring a convention-governed published artifact. Beyond the literal task, scan the same artifact for other gaps in the SAME convention (self-descriptions, inventory tables, bare references) and fix them in-scope; reserve content/accuracy fixes for a separate audit (#43). Composes with Learning #52 (completeness) and #103 (re-verify after your own edits). |
| 54 | **Expanding or disambiguating a path on a symbol-adjacent citation can turn an AMBIGUOUS reference into a WRONG one — re-confirm the cited path actually hosts the named symbol, at APPLY time, after every expansion.** A vague pointer like `(orchestrator/pipeline.py)` or a bare `cli.py` is merely imprecise; once you expand it to a full path beside a symbol name it reads as *'this symbol is defined HERE'* — a falsifiable claim that can be false (Session 138: expanding to `pipeline.py` made a `CheckpointStore` reference wrong — it lives in `checkpoints.py`). On bare-filename hot spots especially (a name like `cli.py` / `db.py` / `logging.py` / `anthropic_client.py` resolves to DIFFERENT files across intake / data-agent / website / orchestrator), `grep -rn '<symbol>' <chosen-path>` BEFORE writing. Checking at apply time (not only at verify time) is what makes the adversarial pass a clean confirmation. | Candidate #106 filed Session 138 (1st — round-2 caught the `CheckpointStore`→`pipeline.py` regression its own path-expansion introduced). Session 139 (2nd — ~15 expansions, each re-confirmed at apply time; 0 regressions). Session 140 (3rd, promotion-triggering — every disambiguated bare filename re-confirmed against live code at apply time: `cli.py:10-13`→data-agent cli docstring, `pipeline.py`→orchestrator, `logging.py`/`metrics.py`→orchestrator, both adapter `__init__`s host the SDK construction; the 2-round adversarial pass found 0 wrong-location citations, and its two 'blocker' flags both dissolved under grep). Promoted Session 140. | Whenever you expand, normalize, or disambiguate a path on a citation that also names a symbol. `grep -rn '<symbol>' <expanded-path>` before writing. Compose with Learning #45 (re-derive from live code) and #103 (re-run adversarial verification after your own edits). |
| 55 | **Adjudicate adversarial-verifier flags on BOTH factual correctness against the live artifact AND diff-membership/convention-precedent (`git diff | grep '<flagged-text>'`; does the flagged text predate this session's edits? does an operator-approved precedent sanction the flagged form?) before acting on any flag.** Verifier severity labels are advisory until adjudicated (composes with #56). A flag that fails either check is rejected with the evidence recorded; a flag that passes both is fixed in-session per Learning #49. | Candidate #107 filed Session 140 (1st — R2 completeness flag failed diff-membership AND factual check; R1 "blockers" self-concluded no-error). Applied Session 141 (2nd — 3 mislabeled blockers + 1 style flag on pre-existing prose all dissolved). Applied Session 143 (3rd, promotion-triggering — accept-token flag CONFIRMED by direct grep of `REVIEW_ACCEPT_TOKENS` and fixed in-session; banner flag REJECTED against the §4.4 banner convention + the first edition's own precedent). Promoted Session 143. | Whenever an adversarial-verifier lens returns flags. Run both checks per flag before acting; record rejected flags with their evidence in the close-out. |
| 56 | **Schema-forced verifier agents leak chain-of-thought into their findings array — mislabeled severities and self-refuted explorations — unless the prompt requires "report only conclusions that survive your own verification; never report an exploration you later refuted."** Treat `severity` as advisory until adjudicated (#55 composes). With the clause, finding arrays contain only real or honestly-mistaken findings, collapsing adjudication cost. | Candidate #108 filed Session 141 (1st — regression lens emitted 3 "blocker" entries whose own evidence concluded "no error found on re-examination"). Applied Session 142 (2nd — clause in all 6 verifier prompts → empty problems array, zero adjudication). Applied Session 143 (3rd, promotion-triggering — clause in all 6 prompts across 2 rounds; R1 returned only real findings plus one honest convention-misread, R2 returned zero; no self-refuted exploration leaked in either round). Promoted Session 143. | When authoring any Workflow/subagent verifier prompt with a structured `problems`/findings schema. Add the survival clause verbatim; treat severity labels as advisory until adjudicated per #55. |
| 57 | **In a repo whose pytest `addopts` carries `--cov-fail-under`, an isolated single-file pytest run fails the coverage gate even when every test passes — a docs-only/structural test exercises ~0% of source. Use `--no-cov` for single-file runs, and write `--no-cov` into any plan/handoff verification command that names a single test file.** The failure is misleading: the test itself is green; only the coverage floor trips (e.g. 0.00% < 95). | Candidate #109 filed Session 142 (1st instance — the plan §8 / S141-handoff DONE command `uv run pytest tests/test_wiki_no_line_citations.py -q` failed as written at 0.00% < 95 while both tests passed; `--no-cov` is the documented pytest-cov switch). Applied Session 143 (every single-file run that session used `--no-cov`, zero coverage false alarms; reached 3 instances per the S143 close-out — promotion-ripe). Promoted Session 144. | Authoring or executing any verification command that runs a subset of this suite (a single test file or a `-k` filter). Append `--no-cov`; when writing such a command into a plan or handoff, include it there too. |
| 58 | **A carried-forward claim about a document's or symbol's STATUS — archive-eligibility, "finding X exists", a count, "gone stale" — is point-in-time: re-derive it against the live tree before acting, and when a document's own self-description conflicts with its dependent artifacts, the artifacts win.** A handoff or accumulated audit finding asserts a status that was true (or believed true) when written; by execution time the tree may have moved, the original may have been a verifier miscount, or the document may describe its own status inaccurately. Re-derivation is cheap and decisive. The sharpest forms: origin-commit archaeology (`git show <origin>:<file>`) to distinguish wrong-at-birth from gone-stale; and trusting a live dependent artifact (a guard test, a generated file) over a stale self-status line. Generalizes the verifier-miscount family (Learning #56) to any carried status claim; composes with Learning #45 (ground-truth every load-bearing claim at source). | Candidate #113 filed Session 145 (1st instance — S138's carried audit finding "`_save_stage` doesn't exist; only 3 helpers" re-derived → name wrong but the count was a verifier miscount: 4 helpers); applied Session 146 (2nd instance — ROADMAP's "6 decoupling invariants" re-derived at origin commit `b74d3ff` → wrong at birth: 2 AST tests, plan constraints C1–C8); Session 147 (3rd instance, promotion-triggering — the carried "both plans archive-eligible" recommendation re-derived; `wiki-citation-symbol-references-plan.md`'s own header said "DRAFT — not implemented", contradicting eligibility, but the live guard test `tests/test_wiki_no_line_citations.py` (allowlist empty, docstring recording Phases 1–6 delivered) settled it as delivered → eligible; the doc's self-status lied, the dependent artifact was authoritative). Promoted Session 147. | Any session acting on a carried-forward claim about a document's or symbol's status — archive-eligibility, a finding's existence, a count, or a "gone stale" assertion. Re-derive against the live tree before acting; prefer origin-commit archaeology to distinguish wrong-at-birth from gone-stale; when a document's own status line conflicts with its dependent artifacts (guard tests, generated outputs, code), the artifacts are authoritative. |
| 59 | **A verification fan-out's output is a claim to adjudicate against your own primary derivation — in BOTH directions. Silence on an explicitly-directed area is an UNRESOLVED area, not a verified-clean one (low `claims_checked` relative to the range's density is the tell); and a returned finding — even one labeled `blocker`, even when multiple independent lenses converge on it — is confirmed or refuted deterministically (precedent commits, working-tree evidence, `git ls-tree` at the precedent sha) before acting.** The orchestrator must hold its own direct derivation of every load-bearing area; fan-out output never substitutes for it. Cross-lens convergence is not truth: independent lenses reasoning from the same wrong premise (e.g. committed history only, when the draft is uncommitted by design) converge on the same wrong blocker. Extends Learning #55 (the two-check flag-adjudication procedure) and #56 (survival clause) to the silence direction and to convergent false positives. | Candidate #114 filed Session 146 (1st — audit Agent A was explicitly directed at the Remaining-work block with evidence pointers and returned 9 claims_checked with NO finding on it; the block held the page's largest defect, E4, caught by the orchestrator's own derivation, not the fan-out). Applied non-incrementing Session 151 (own grep-classification held primary; the reference-completeness lens independently re-derived and confirmed). Applied Session 152 (2nd — an adversarial convention lens returned a FALSE `blocker` demanding "16 others" in the README archive count; `git ls-tree` at precedent `4b17ab2` proved the convention counts the `.pdf`, 1+6+1=8, refuting the finding — the orchestrator's "17" stood). Applied Session 153 (3rd, promotion-triggering — TWO independent lenses converged on the SAME false blocker, demanding a self-referential Evolution.md banner sha and claiming "Session 153 made no edits"; refuted with the S148 banner-convention precedent plus working-tree evidence that the draft was uncommitted BY DESIGN under the §4.7 review gate). Promoted Session 154. | Any audit/review/verification fan-out. Before acting on results: (a) compare each agent's claims_checked against the density of its assigned range and treat directed-area silence as a miss to investigate yourself; (b) adjudicate every returned finding against deterministic ground truth per Learning #55 — severity labels and cross-lens convergence are advisory, not dispositive; (c) never let fan-out output, silent or confident, overrule your own derivation without evidence. Producer-side complement: pre-arm verifier prompts with the artifact's lifecycle state and governing conventions (Learning #61). |
| 60 | **A convention-governed REPEATABLE operation (archive-move, rename, mass-migration, periodic rewrite) specifies WHAT to do but underspecifies its exact blast radius and form — which reference classes to update vs. leave, which stamp/format conventions govern. The authoritative spec is the diff of prior instances of that operation (`git show --stat <precedent-commit>`, plus selective `git show`), not first-principles reasoning about the convention prose — and the diff-derived scope doubles as a VERIFICATION checklist.** Convention prose like "move + banner + update references" never defines whether code docstrings, other plans' cross-refs, or the wiki count as "references"; precedent diffs answer definitively. Every line class the precedent diffs touched is a line class this instance must check — which is how a predecessor's missed update surfaces. Precedent also settles interpretation questions the prose leaves open (banner-stamp content; surgical integration vs. literal retype). Distinct from Learning #58 (re-derive a carried STATUS claim against the live tree): this derives an OPERATION's scope and form from precedent diffs. | Candidate #119 filed Session 151 (1st — `PROJECT_CONVENTIONS.md` §3 was silent on reference classes; precedent diffs `4b17ab2` and `f09ea57` settled scope: banner-only on moved files, live root-docs and wiki repointed, ZERO code files touched, append-only logs left — all 124 references correctly classified, change stayed docs-only). Applied Session 152 (2nd — scope from `git show --stat 4b17ab2 f09ea57 e1bf7c2`, plus the verification-checklist facet: the README archive-count line appears in `4b17ab2`'s diff, which is the only reason S151's stale "6 others" was caught and corrected to "17 others"). Applied Session 153 (3rd, promotion-triggering — the Evolution.md banner stamp and the surgical-integration approach were both derived from the S148 precedent edition via its archived Task entry, not from §4.2's "from scratch" prose; the same precedent was then load-bearing in refuting a verifier's false banner blocker). Promoted Session 154. | Before executing the Nth instance of any repeatable convention-driven operation: `git show --stat` (plus selective `git show`) the prior instances to derive (a) the exact reference classes to touch vs. leave, (b) the format/stamp conventions the prose underspecifies, and (c) a verification checklist — every line class the precedent diffs touched must be checked in this instance, which is how predecessor omissions surface. Composes with Learning #58: precedent diffs give the operation's scope; live-tree re-derivation gives the current state. |
| 61 | **The SHARED PREAMBLE of an adversarial verification fan-out — the context every lens receives before it fans out — must state the artifact's current LIFECYCLE STATE (e.g. "uncommitted working-tree draft under a pre-commit review gate"; "N commits landed but NOT pushed"; "retroactive release — tag exists, GitHub Release being cut now") AND its GOVERNING CONVENTIONS (banner = previous close-out sha; CHANGELOG cadence; candidate# ≠ row#; free-form-by-design schema fields) — otherwise skeptical lenses reason from committed history plus first principles and manufacture confident false blockers, including CONVERGENT ones where independent lenses reach the SAME wrong blocker from the same missing premise.** This is the producer-side complement to Learning #59: #59 governs CONSUMING verifier output (adjudicate every finding; treat directed-area silence as unresolved); #61 governs CONSTRUCTING the verifier's shared context so the false findings are never produced. Cross-lens convergence is not a defense — it is the symptom, since the converging lenses share the missing premise. Distinct from #56 (the per-lens survival clause, which each lens still applies to its own findings) and from #59 (consume side): #61 is the pre-flight that arms the whole fan-out. | Candidate #120 filed Session 153 (1st — coined from the failure: 2 of 4 lenses independently produced the SAME false blocker, demanding a self-referential Evolution.md banner sha and claiming "Session 153 made no edits", because the preamble named neither the uncommitted-by-design draft state nor the §4.7 banner convention). Applied Session 154 (2nd — preamble armed with lifecycle state (uncommitted-by-design promotion draft; claim committed) plus conventions (S150 receptacle rule; candidate# ≠ row#; frozen pipe anomalies) → ZERO false blockers in the 4-lens, 65-check fan-out, against 1 (S152) and 2-convergent (S153) un-armed). Applied Session 155 (3rd, promotion-triggering — armed with the landed-commits / uncommitted-CHANGELOG / retroactive-release-shape / not-pushed state plus the §2-cadence, data-agent-decoupling, and free-form-by-design conventions → 0 false blockers in the 3-lens fan-out). Increment rule (S152-#119 precedent): applied-as-designed WITH differential outcome evidence increments; routine application without evidence does not. Promoted Session 156. | Before launching any adversarial Workflow/subagent verification fan-out over a gated draft, an uncommitted or not-yet-pushed tree, a retroactive or partial release, or a convention-stamped artifact: write the artifact's current lifecycle state AND its governing conventions into the SHARED preamble every lens receives — do not rely on the lenses to infer them from committed history. Then treat any confident blocker an un-armed lens produces — especially a cross-lens-convergent one — as a likely artifact of a missing premise, and adjudicate per Learning #59. Producer-side complement to #59 (consume side); composes with #56 (per-lens survival clause). |
| 62 | **When a handoff or backlog item states a prerequisite as "verify X against a live invocation," first ask whether the upstream project's SOURCE answers the question better — for a contract/schema/flag-behaviour question it usually does, and it costs nothing.** A live run samples one path through the tool; the emitter in source enumerates *every* case it can produce. Session 211's prerequisite was "OpenCode's `--format json` event schema is not published — verify it live"; reading `run.ts`'s `emit()` in the upstream repo via the GitHub API gave the exact envelope **and** the complete set of event types, which one live invocation could not have (it would have shown only the types that run happened to produce). **Pin the evidence:** record the commit SHA, the default branch, and the current release version alongside the claim — on a fast-moving upstream (this one shipped a release the same day) an unpinned source claim goes stale exactly as fast as an unpinned live observation. Source-reading does **not** replace a live spike for what source cannot show — install/runtime behaviour, latency, credential resolution, and any case where docs and code disagree about *effects* — so the right move is source-first, then a narrowly-scoped live spike for the residue, with every remaining unknown marked `[unverified]` in the deliverable rather than silently assumed. | Session 211 (`OpenCodeLLMClient` spec; discharged `AI-Dependencies.md` §9.2's prerequisite from source at `anomalyco/opencode` `dev@32f278b48f1a`) | Any session whose prerequisite is "verify an external tool's output format, API shape, or flag behaviour" before designing against it — especially when the live path costs an install decision, credentials, or provider tokens that belong to the operator. |
| 63 | **Specifying an adapter for an AGENTIC CLI is not the same as specifying one for a completion API — enumerate the tool's autonomous capabilities as HAZARDS before designing the invocation, and pin each mitigation with a NEGATIVE test.** A completion endpoint does exactly what it is asked; an agentic CLI may read and write files, run shell commands, discover project config by walking up the directory tree, block forever on an inherited stdin, and expose the prompt (which may carry PII) in the process table via `argv`. Session 211 found six such hazards by reading the CLI's own source, and closed each with one specific invocation decision — prompt on **stdin, never argv**; `--dir` pointed at an ephemeral sandbox outside the repo; stdin never inherited (its own source `await`s stdin to EOF whenever stdin is not a TTY, so an inherited pipe hangs the process before any model timeout fires); never pass the auto-approve flag (whose own help string says "dangerous!"); a tool-denying agent definition; and "zero assistant-text events ⇒ error". The mitigations that matter are the ones asserted **negatively** ("`--auto` is absent from argv", "the prompt is not in argv"), because a positive test still passes when a later edit adds the dangerous flag. Also check what the DEFAULT does when nobody answers an interactive prompt. **⚠ CORRECTED by Session 212: this row originally concluded from source that "the default auto-*rejects* permission requests, which is safe." That is FALSE — a live probe showed the `read` tool executing and disclosing file contents with no `--auto` and no permission prompt. The source showed what the permission handler does; it did not show which tools route through it. See Learning #64.** | Session 211 (`OpenCodeLLMClient` spec §3.5 hazard table + §7.1 test plan); safety clause corrected by Session 212 | Any session integrating an external agent or CLI as a pipeline component. Per capability, ask: what can this do on its own, in whose working directory, with whose credentials, reading whose config — and what happens by default when no human is present to answer it? |
| 64 | **Source-reading can verify a SCHEMA but cannot verify a SAFETY DEFAULT — for any claim of the form "the default is already safe," only an adversarial live probe counts.** Session 211 read the CLI's permission handler and correctly established that unanswered permission requests are auto-rejected, then concluded the headless default was safe. Session 212 asked the running binary to read a file and it did — contents disclosed, exit 0, no `--auto`. Both readings of the source were accurate; the inference was not. The gap: the source showed *what the permission handler does to requests that reach it*, and said nothing about *which tools route through it at all*. That is invisible from the handler and obvious from one probe. The general shape: source tells you the behaviour of the code you read, and a safety claim is a statement about **all the code you did not read**. Note the asymmetry that makes this worth a standing rule — a wrong schema claim fails loudly at parse time on the first real call, whereas a wrong safety claim fails silently and only in the presence of an attacker or an unlucky prompt. So the verification effort should be spent in inverse proportion to how loudly the claim would fail. **Test it by asking the tool to do the forbidden thing and requiring a refusal**, then keep that probe as a negative regression test. | Session 212 (spec Phase 1, Appendix A.4 C1 — disproved Session 211's H4 mitigation claim) | Whenever a design leans on "the default is safe," "it refuses by default," or "that is not permitted" — especially when the claim was derived from reading source, docs, or a help string rather than from provoking the behaviour. |
| 65 | **To learn what a black box injects around your prompt, measure its TOKEN ACCOUNTING across controlled variations — it converts an apparently unanswerable qualitative question into a number.** Phase 1 had to settle whether a custom agent definition *replaces* or *appends to* an agentic CLI's built-in system prompt — the residual risk behind the spec's largest design decision. The obvious approaches are introspective and unreliable: ask the model to recite its instructions (it may confabulate), or diff outputs (confounded by sampling). Instead: run the same prompt three ways and read the provider's own `cache.write` counter. No agent → 9,552 tokens. Custom agent, short prompt → 4,832. Custom agent, ~2,000 tokens longer → 6,836 — a delta of **exactly** the text added. Three numbers prove the structure: a *constant* ~4,830-token base plus the custom prompt linearly on top, i.e. **partial replacement**, and they quantify the residue that no configuration can remove. The same counters then priced the design (~120 k extra input tokens per 25-call interview — negligible on a cheap model, material on an expensive one, and invisible to quality-only thresholds). **Generalisation: when a wrapper sits between you and a model, its own usage telemetry is the cheapest available instrument for reverse-engineering what it adds — prefer it to asking the model about itself.** | Session 212 (spec Phase 1, Appendix A.1 #2 and A.2) | Any integration through a wrapper, gateway, agent framework, or proxy that may inject its own prompt content — and any time a cost or quality question is being deferred because it "can't be measured without a full eval." |
| 66 | **When a test you just wrote fails on its first run, ask whether the assertion encodes something you verified or something you assumed — the bug may be in the claim.** Phase 2 added a test asserting the OpenCode provider constructs with **no LLM SDK in `sys.modules`** ("stdlib only" felt like the obvious reading of spec D5). It failed: `opencode_client` imports its parent `AnthropicLLMClient`, which imports `anthropic.types.TextBlock` at module level, so `anthropic` loads transitively no matter what the subclass does. D5's actual claim is narrower — no new *Python dependency*, i.e. `pyproject.toml` is unchanged — which is about what the *file* requires, not what ends up imported. The fix was to weaken the assertion to the property that holds, and to write the gap into the test's own docstring ("what this does NOT claim") rather than delete the test and move on. **Mechanically: for a "no third-party imports" guard, AST-walk the module's own `Import`/`ImportFrom` nodes and subtract `sys.stdlib_module_names` plus the package root — no hardcoded allowlist to rot.** | Session 213 (spec Phase 2) | Any time a first-run test failure tempts you to "fix the code." Especially when the assertion restates a plan's summary line rather than a property you traced through the actual imports. Also whenever inheriting from a class in a dependency-heavy module and claiming dependency freedom. |
| 67 | **When a spec says "remove or hard-gate X", prefer removal whenever the thing being gated is unobservable from inside your process.** Spec §4.4 let a caller pass their own `agent=` name, and Phase 1's safety defect (the tool denial is the ONLY barrier to file reads) required that hatch be "removed or hard-gated." A gate needs something to check — but a caller-supplied agent id may resolve to a definition in `~/.config/opencode/agents/`, outside the sandbox the adapter owns, so the adapter cannot verify it denies tools. A gate that cannot inspect its input is theatre. Removal it was: the parameter became `agent_name`, which **renames the definition the adapter generates** rather than substituting a caller-owned one — configurability preserved, zero safety surrendered, and no constructor path reaches an unlocked agent (pinned by a signature test in both packages). **Generalisation: when hardening a "caller may override" option, first ask whether the safety property of the override is checkable at all; if not, the honest options are remove it or accept the risk explicitly — not add a gate that only looks like one.** | Session 213 (spec Phase 2, Appendix A.4 C1) | Any security-relevant escape hatch, injection point, or "bring your own config" option. Especially when the plan's wording offers "or" and one branch is cheaper to write than to justify. |
| 68 | **A parity battery that only asserts "both copies agree" cannot catch a bug both copies share — pair it with at least one test that pins the RULE.** C4 forces duplicated helpers across the two packages, and the drift guard runs the same inputs through both copies asserting identical output. That is exactly right for drift, and blind to correctness: two twins can be identically wrong, and a one-sided *fix* would then fail the battery, framing the correction as the regression. Phase 2 therefore added `test_opencode_final_step_rule_is_pinned_not_just_mirrored` alongside the sameness battery — it pins the actual extraction rule (the answer is the final `reason == "stop"` step's text, NOT every `text` event concatenated, which on a real 3-step run returned narration glued ahead of the answer). Same idea for shared *constants*: the two `AGENT_DEFINITION` strings are asserted equal to each other **and** asserted to deny each specific tool, because equality alone would happily accept two identically permissive files. | Session 213 (spec Phase 2, §7.2) | Any duplicated-implementation drift guard (the `_extract_json` twins, the new JSONL event twins, any future C4-forced pair) and any "these two configs must match" test. |
| 69 | **An environment-sensing gate is only as safe as the environment the session that WROTE it was standing in — re-derive the predicate against your own machine before implementing it.** Spec §7.4 said the `opencode` provider is "runnable" iff `shutil.which("opencode") is not None`, and that was written by Session 211, whose own provenance note records `which opencode` → **not found**. On that machine the predicate was a safe no-op. Session 212 then installed the binary as part of Phase 1, silently inverting the predicate's meaning; implementing §7.4 literally in Session 214 would have turned every bare `uv run pytest -q` on this machine into a **billable** live run (four `live` cases, hundreds of model calls via the convergence sweep) while CI — which has no binary — still looked hermetic, so nothing would have flagged it. The general shape: a capability probe is a claim about *the machine*, and a plan freezes the machine state at authoring time. Two checks make it cheap: (a) evaluate the predicate on the current box before writing the branch (`python -c` on the probe beats reasoning about it); (b) find out whether the tier it gates is **opt-in or opt-out** — here `pyproject.toml`'s `addopts` carried no `-m 'not live'`, so live tests are collected and *run* by default and only the skip hook stands between a contributor and a bill. The fix shape that worked: require a **second, deliberate signal** that also carries information the run needs anyway (here `OPENCODE_EVAL_MODEL`, which is simultaneously the opt-in and the pinned model id the provider has no default for) — one variable, so the safe path and the reproducible path cannot diverge. | Session 214 (spec Phase 3; §7.4 deviation, operator-approved before implementation) | Any time a plan specifies a gate as "if the tool/credential/file is present" for an expensive, billable, slow, or destructive test tier or code path — especially when an earlier phase of the same plan *installed* the thing being probed. Also whenever adding a provider/backend to an existing parametrized live tier: check `addopts` first. |
| 70 | **A documentation sweep's true scope is the set of CLAIMS the change falsifies, not the file list the plan names — and the grep must not be scoped to the directory you think of as "the docs".** Spec Phase 3 named four wiki surfaces; the honest sweep needed seventeen files. Deriving scope by grepping for the *claim shapes* a third provider falsifies (`two providers`, `Literal["anthropic", "bedrock"]`, `INTAKE_LLM_PROVIDER` value lists, `IntakeLLMClient` implementation lists, credential-surface summaries, test counts) found nine more than the plan listed — but that grep was run against `docs/wiki/`, and the misses that mattered were **outside** it: `docs/tutorial.md` (the one page `mkdocs`/`publish-tutorial.yml` actually publishes — the single most reader-facing document in the repo), `OPERATIONS.md`, and `.env.example`. Two more survived *on pages the sweep had already edited*, ~90 lines from a line it corrected, because an enumeration in a table cell reads nothing like the prose sentence you were hunting. Practical rule: (1) enumerate claim shapes, not files; (2) grep the **repo root**, with exclusions stated as a deliberate list (`docs/architecture-history/`, `Evolution.md`, the wiki `Changelog.md`, dated `CHANGELOG.md` entries — each excluded for a *named* cadence or historical-record reason, not because you did not think of them); (3) ask "what does CI/a workflow actually publish?" — a published page outranks an internal one; (4) after editing a page, re-grep **that page** for the claim shapes, because one fix does not imply the file is clean. An adversarial verification fan-out earned its keep here: four lenses produced ten findings and every one held on re-check, including an off-by-one in the session's own CHANGELOG entry. | Session 214 (spec Phase 3 doc sweep; ten confirmed defects, three of them outside `docs/wiki/`) | Any "update the docs to match X" session, especially one whose plan supplies a file list. Also whenever a repo publishes documentation from more than one place (a wiki, a mkdocs site, root-level operator docs, `.env.example`) — enumerate the publication surfaces before enumerating the edits. |
| 71 | **Before an operator-gated LIVE run, resolve the whole call chain offline — the pre-flight list in the plan is what somebody thought of, not what the code needs.** Spec Phase 4's pre-flight named the binary, the credentials and the pinned model, but never "the harness can drive this provider at all", so nobody had checked; the live tier skips, so no test could see it either. Constructing the stakeholder simulator for the provider — a zero-cost, no-spawn call — raised immediately. The general move: for any tier that only executes when money is on the line, walk each construction/dispatch step by hand with the *real* objects before authorising the run. A skipped test tier is an unexercised code path wearing a green badge. | Session 215 (found spec Phase 4 blocked by two defects; the reproduction cost $0 and took one command, while the un-checked run would have billed ~31 calls and aborted with no report). | Before authorising any live/billable run, and whenever a plan's pre-flight list is the only evidence that a path works. Especially when the tier auto-skips: "the suite is green" is not evidence about a skipped tier. |
| 72 | **When you add a capability to a harness, walk EVERY construction site in the affected function — not the ones your grep pattern matched.** Session 214 threaded the pinned model into `make_intake_client(provider, model=model)` and left `stakeholder_simulator_for(fixture, provider=provider)` unpinned **five lines below, inside the same function and the same diff hunk**. The grep was for the client factory; the simulator is a *different* factory that builds the same client. The discriminator is not "which names matched" but "which objects in this scope talk to the provider". | Session 215 (root-caused defect #2 to Session 214's own edit pass — `git show a2da53e -- tests/eval/shadow_run.py` shows both lines in one hunk's context). Sibling of #70, which is its documentation-surface analogue. | Any time you thread a new parameter/capability through a subsystem. After editing, re-read the whole enclosing function — not just the changed lines — and ask which other calls in scope need the same argument. |
| 73 | **Choose the exception TYPE against the handler that will catch it, not against how bad the error feels.** A misconfigured/untaught provider in the eval harness is a *defect*, but raising the harness's own `StakeholderSimError` would have put it in `interview_sweep._TRANSIENT_ERRORS` — retried three times, then silently *excluded*, yielding an empty result set that reads as "measured nothing" rather than "harness is broken". `TypeError` propagates and aborts, which is the honest outcome. The inverse also holds: a blank simulated answer *should* be the retryable type. | Session 215 (caught while writing the fix, before tests — the retry classification was read out of `interview_sweep.py:82-87` rather than assumed). | Whenever raising inside code that runs under a broad `except` classifier (retry loops, sweeps, task runners, middleware). Read the catch list first, then pick the type that produces the behaviour you want. Silent exclusion is worse than a loud abort. |
| 74 | **A rate metric whose DENOMINATOR is chosen by the thing being measured is not comparable across subjects.** `sql_exec` = executable ÷ generated, and *the model decides how many queries to generate*. In the S216 shadow run `anthropic` scored 3/5 = 60.0% and `opencode` 3/7 = 42.9% — **the same absolute number of working queries (3)**; the candidate looked 17 points worse purely for writing two more queries. A diagnostic re-run reversed the ordering entirely (`anthropic` 2/4 = 50.0%, `opencode` 4/7 = 57.1%). **Mechanical:** before comparing two subjects on any rate, ask whether the harness fixes the denominator or the subject does. If the subject does, report the numerator and denominator separately, re-run at least once to see whether the ordering is stable, and do not describe the gap as a quality difference. | Session 216 (Phase 4 shadow run — the 42.9%-vs-60.0% gap read as a transport regression until the fractions were resolved) | Comparing providers/models/configs on any ratio. Especially when the metric is a gate. |
| 75 | **Diagnose a threshold FAIL before reporting it — the cause routinely changes what the number means.** S216's `opencode` NO-GO hung on one failing threshold. Six extra live calls (~$0.20) capturing the real DB exception per generated query showed every failure on *both* providers was a SQL **dialect** error against SQLite (`DATEDIFF`, `PERCENTILE_CONT ... WITHIN GROUP`, `MEDIAN`) — not model quality, and not the "incomplete seed schema" S165 had guessed and which had stood unchallenged for ~50 sessions. Reporting "candidate fails `sql_exec`" without that would have been true, useless, and quietly misleading. | Session 216 (the diagnosis overturned a standing S165 mis-attribution and reframed the verdict) | Any measured FAIL, especially one that gates a decision. Budget a small diagnostic pass into the measurement session — do not defer it to the next one. |
| 76 | **Do not state a universal from a probe.** After three probe calls all showed `cache_read: 0`, I told the operator the adapter "never" reuses prompt cache. The full 451-call record showed 38 calls (8.4%) with nonzero `cache_read`. The directional claim survived; the quantifier did not. **Mechanical:** when reporting from a sample, say "0 on all three probes", not "0 on every call" — and re-check every sample-derived universal against the full dataset before it reaches a permanent document. | Session 216 (self-caught during final distribution analysis, after the claim had already been stated to the operator) | Any claim made from probe/pilot data that will be repeated in a report or handoff. |
| 77 | **Estimate from the distribution, not from the outliers you happen to sample.** S216's runtime estimate went 2-4 h → "revised" 4-6 h (on a single 65 s call) → actual 1.8 h. The end-of-run analysis showed why: p50 latency 7.8 s but p99 105.8 s — a heavy tail, so any individual sampled call is nearly uninformative about the mean. Progress notifications sample; they do not summarize. **Mechanical:** instrument to record *every* event, and hold projections loosely until enough have accumulated to compute a percentile — do not re-forecast off one slow tick. | Session 216 (two wrong estimates in opposite directions before the recorded distribution settled it) | Forecasting duration or cost of a long run from streaming progress output. |
| 78 | **An eval threshold that has been failing for ~50 sessions is a hypothesis about the *system under test*, and the cheapest way to test it is an A/B against the harness itself — not another sweep of the same measurement.** `sql_exec` failed continuously from Session 165, attributed first to "model SQL or an incomplete seed schema" (S165, wrong), then correctly to a SQL-dialect mismatch (S216). The confirming experiment cost **6 calls**: run the same corpus twice through the same model in the same session, once with the suspected cause present and once with it removed (dialect-blind 2/5 executable vs dialect-aware 4/4). A full re-sweep would have cost ~$14 and ~90 min and still would not have *isolated* the cause, because it varies only the provider. The A/B is diagnostic power per dollar; the sweep is a score. Pair with Learning #74 — report numerator/denominator, and label the probe explicitly as "not a re-score" so the next reader cannot quote 4/4 as a threshold PASS. | Session 217 (`9c9fe35`, `PHASE_E_AGREEMENT_REPORT.md` §"Update — Session 217") | Whenever a metric has failed long enough that its cause is a standing *guess*. Design the cheapest experiment that varies the suspected cause and holds everything else fixed, before authorizing another full measurement. |
| 79 | **When an eval measures something production also does, fix production — a fix applied only to the harness makes the eval flatter the shipped behaviour instead of measuring it.** The dialect silence behind `sql_exec` was never eval-specific: the shipped data agent generated SQL for an unnamed dialect too, so any org on Snowflake/Postgres/SQL Server got a guess. The eval-only variant (pass `sql_dialect` on the harness client alone) was the smallest diff and was explicitly rejected for exactly this reason. The general test: *would this change make the eval and production disagree?* If yes, the eval is being tuned, not fixed. Corollary — derive the value from a source production already has (here the configured `--db-url`) rather than adding a parallel config knob, which makes the disagreement unrepresentable rather than merely discouraged. | Session 217 (operator chose fix A over the eval-only and warehouse-target alternatives) | Any time a harness change would make a measurement pass. Ask whether the measured defect exists in production; if it does, the harness change alone is a scoring change. |
| 80 | **A subclass that re-declares `__init__` and calls `super().__init__` with explicit keywords silently drops every new base-class keyword — and for a prompt-shaping keyword the drop is invisible: output still builds, still parses, and only a rate moves.** `BedrockLLMClient` and `OpenCodeLLMClient` both inherit `generate_primary_queries` unchanged (the deliberate no-prompt-drift design) but both re-declare construction, so `sql_dialect` needed explicit forwarding in each. Nothing would have failed loudly if it had not been. Countermeasure: a per-subclass forwarding test **plus** a factory test parametrized over the provider `Literal` (`KNOWN_PROVIDERS`), so adding a fourth provider that forgets the keyword goes red rather than shipping one dialect-blind provider among three dialect-aware ones. | Session 217 (`test_bedrock_client.py`, `test_opencode_client.py`, `test_factory.py::test_every_known_provider_accepts_sql_dialect`) | Whenever adding a constructor keyword to a base class whose subclasses re-declare `__init__`. Grep for `super().__init__(` and check each call site passes it; then pin it with a test parametrized over the registry, not one test per subclass written by hand. |
| 81 | **A driver that only ever runs when someone spends money has no test coverage in any meaningful sense — stub its seams and assert its wiring at $0.** `shadow_run.measure_provider` was exercised only by paid runs; `test_eval_live.py` auto-skips without credentials, so both were green-badged and unexercised. That is exactly how Session 215's defect (the driver could not drive `opencode` at all) survived undetected — learning #71's structural cause, now given a countermeasure. Emptying the *corpus loaders* rather than canning provider JSON is what makes this cheap: the per-case loops never execute, so no stub has to impersonate a provider, and what remains under test is precisely the collaborator wiring. | Session 217 (`tests/eval/test_shadow_run.py` — 3 tests, 0 API calls) | Any code path gated on credentials, spend, or an external binary. Do not accept "the live tier covers it": if the live tier skips by default, the path is untested by default. Stub the seams and assert the arguments. |
| 82 | **When a metric fails, check the denominator before you believe the numerator.** `sql_exec` failed the gate from Session 165 to Session 217 and was misattributed twice — S165 guessed "model SQL or an incomplete seed schema", S216 correctly found the dialect cause but inherited a measurement too small to confirm a fix against. The metric was measured over ~5 model-chosen queries against a ≥95% bar, i.e. pass-only-if-perfect, on a block that (uniquely among the three) never sampled. Both S216 and S217 measured it and both had to write "do not quote this as a rate" instead of a result. Raising the denominator 3.6–6.8× turned an unquotable number into a decidable one *without touching the threshold*: 3/5 FAIL became 18/18 PASS. **A threshold and a sample size are one instrument; tuning only the threshold is how a gate gets calibrated to noise.** | Session 218 (`tests/eval/sql_sweep.py`; `4e2c8ec`) | Any gate metric that fails repeatedly, or whose value swings between runs. Before diagnosing the *subject*, compute n and ask what the bar demands of it. If one miss moves the rate by more than the margin, the measurement cannot answer the question — fix that first, and never by lowering the bar. |
| 83 | **Scope a live measurement by the change's blast radius, not by the harness's default entry point.** The obvious execution of "re-measure `sql_exec`" was `shadow_run.py` — $13.99 and 99.5 min. But 93% of those calls are the interview block, produced by the *intake* client, which the dialect fix cannot reach: it is built by a different factory that accepts no `sql_dialect`, and nothing under `agents/intake/` imports the data-agent package. Measuring only the reachable block cost $1.63 and 24 min — and bought a *larger* sample on the metric in question than the full sweep would have. **The default driver measures everything because it does not know what changed; you do.** | Session 218 (60 calls vs S216's 482) | Before authorizing any paid re-measurement. Trace which metrics the change can actually move, verify that reachability adversarially (a value-coupling claim is not the same as a failure-mode-coupling claim), then spend only there — and carry the unreachable values forward with the reasoning written down. |
| 84 | **Verify the instrumentation at $0 before the run that needs it.** Session 217 self-docked for reporting "well under $1" instead of a figure on a session whose only gate was spend. The fix is not "remember to instrument" — it is to *test the meter against a fake* before spending. Doing so caught that the data-agent client does not retain its response object, so a meter patched at `_call_claude` records latency but **no token usage**; usage is only reachable one level down at `client._client.messages.create`. Had that gone unnoticed, the paid run would have produced the same "no figure" gap a second time. It also correctly predicted that `opencode`, whose subprocess transport never touches that seam, would yield no usage — so its cost is reported as a char-derived *estimate* rather than dressed up as a measurement. | Session 218 (`_Meter`, verified against a fake SDK before any live call) | Any session that will spend money and wants to report what it spent. Build the telemetry, prove it captures real numbers against a fake, *then* run. Absent usage must be reported as absent, never as zero. |
| 85 | **A refutation that lands on a different axis than the claim is still worth having — read what it actually proved.** Three adversarial lenses all returned "REFUTED" against "the dialect fix cannot affect the five non-SQL metrics." None produced a path by which the fix *changes a value*; all three showed the changed line sits upstream of the other measurement blocks, so anything raising there destroys the whole run. That is a **failure-mode coupling**, not a **value coupling** — it left the carry-forward decision intact while changing *how* to run the measurement (isolate it). One of the three also surfaced a real, verified bug in the code under test. **Do not accept a refuter's verdict flag at face value, and do not discard the finding because the flag was wrong.** | Session 218 (blast-radius verification) | Whenever an adversarial pass returns a verdict. Read the reasoning, classify what was actually established, and re-verify anything load-bearing yourself — the refuters found a genuine `ValueError` defect the original analysis missed. |
| 86 | **When two measurement blocks feed one gate, they must share a transient-failure policy — otherwise the gate scores retry policy, not the subject.** The Phase E harness retries a transient LLM error in the interview block (up to 3 attempts, then *excludes* the sample) and does not retry at all in the SQL/QC block (one `LLMParseError` is recorded as one parse miss + one exec miss + one QC miss). Session 169 fixed this for interviews only; the sibling never got the treatment. The Session 219 sweep hit **two** interview transients and **one** SQL transient: the two were retried away, the one produced a **NO-GO** against two zero-tolerance 100% bars — while every artifact the provider actually produced was valid (29/29 parse, 29/29 executable, 14/14 QC). **A zero-tolerance threshold converts any un-retried transient directly into a verdict.** Learning #82's rule ("when a metric fails, check the denominator") is necessary but not sufficient: check the *policy* that populates the denominator too. | Session 219 (`sql_sweep.py:144-153` vs `interview_sweep.py:82-87,152-180`; the `opencode` GO→NO-GO reversal). **SQL-vs-interview half closed Session 221** — and closing it found the asymmetry is wider than two blocks: `shadow_run.py`'s governance loop is a third policy (no retry, scored a non-agreement against a zero-tolerance bar) and `test_eval_live.py`'s governance test is a fourth (no handler — the seam error aborts the gate), so the driver and the assertion gate disagree with each other. **CLOSED Session 225** — both governance surfaces now share `governance_sweep.sweep_governance_agreement`, the third and last hand-written measurement loop retired; the transport tier it added is new coverage, since `classify_governance` wraps no SDK transport error and one blip aborted a ~2.5-hour run. S225 also found the ceiling on this learning: **"one policy" was never the achievable goal — "no hand-written loops" is.** All three sweeps retry and all three exclude an exhausted *transport* error, but `interview_sweep` still *excludes* an exhausted seam error where the other two *score* it, and that difference is correct (an interview sample is a whole multi-turn run with no report to judge; a governance or SQL sample is one call whose failure IS the measured capability). Claiming the stronger property in a docstring is how S225 shipped a false one. S221 also refined the learning itself: **"share a policy" is not "share a mechanism"** — the two blocks' transient tuples cannot be shared (one holds `RuntimeError` subclasses from the intake package, the other a `ValueError` from the data-agent wheel), and their *exhaustion* branches must differ, because excluding a sample shrinks a denominator whose survivors are clean by construction | Any gate fed by more than one measurement path. Diff their error-handling before trusting a cross-capability comparison, and treat "bar = 1.00" as a demand that transients be handled, not merely counted. |
| 87 | **Before quoting a predecessor's cost figure, check what that money actually bought.** Session 218's handoff said "a full sweep is S216-shaped, ~$14/100 min", and I repeated it to the operator as the price of a full both-provider sweep. It was not: S216 ran a full *candidate* sweep plus a governance/SQL-only baseline refresh, deliberately not re-paying the baseline's ~420-call interview block. The true both-providers-fresh figure is ~$24 — **1.7×** what I quoted, and the quote had already reached an operator decision before I caught it. A cost number is a function of *scope*, and scope does not travel with the number. | Session 219 (caught during Phase 2 inventory, before spending; corrected options re-put to the operator) | Any time a spend estimate is inherited from a prior session or a prior run. Reconstruct what the earlier run measured, not just what it cost, before repeating the figure to anyone. |
| 88 | **A predecessor's honestly-recorded self-criticism is the cheapest lead in the handoff — read the minuses first.** Session 218 wrote that its `opencode` cost was an estimate because "my meter reads the SDK seam that provider never touches, and I did not reach for the event stream." That single sentence pointed straight at `OpenCodeLLMClient.last_usage`, which has carried the real per-call `cost` from the `step_finish` event since Session 213 — in the *production* client, not the harness. Reading it closed a predecessor's self-flagged gap in ~10 minutes and turned a ~$1.00 char-derived estimate into a $16.396 measurement across 515/515 priced calls. Note the shape: the gap was not "instrumentation is missing" but "instrumentation was built at the wrong seam" — so learning #84's countermeasure (test the meter against a fake first) finds it only if the fake exercises the *real* transport. | Session 219 (meter self-test replayed the committed `success_with_agent.jsonl` fixture through the real client, asserting `cost == 0.00611425`) | Start every session by reading the predecessor's self-assessed minuses, not just its gotchas. A gap the previous session admitted is usually the highest-value, lowest-risk thing available to you. |
| 89 | **A per-block cost or latency figure is not derivable from a whole-run mean — the blocks are not alike, so measure the block you are quoting.** Session 220 corrected S219's estimate that the SQL-only probe would take ~7 min (S219 had applied `opencode`'s whole-corpus p50 of 7.7 s to a block whose calls are the heavy ones; S218 had directly measured the same block at 16.8 min) — and then made the *same* error in the other dimension, projecting cost from the global mean of $0.0318/call. The measured SQL-block cost is **$0.0593/call, 1.9× the global mean**, so the probe came in **78% over** its own estimate at $5.34 vs ~$3. Note S219 had already diagnosed this exact confusion in S218's "2× slower" latency claim and still fell into the cost version of it, which is what makes it worth a standing rule rather than a one-off correction. The whole-run mean is a weighted average over blocks with very different call shapes: interview calls are short and numerous, SQL calls are long and few, and the mean sits near whichever block dominates the count. **Before quoting a per-block number, find a prior session that measured that block directly; if none did, say the estimate is unanchored.** | Session 220 (SQL variance probe — corrected S219's latency estimate, then repeated it on cost) | Any time you cost or time-box a *subset* of a previously measured sweep. Also whenever a handoff quotes a p50/mean without saying what it was pooled over. |
| 90 | **A predecessor's gotcha states a fact; it does not license the converse. Do not infer a prohibition from a statement of what something does not supply.** S219's gotcha 5 read: "A live `opencode` run needs `OPENCODE_EVAL_MODEL=...` inline plus the binary; `.env` supplies neither." That is true. Session 220 inferred from it that `.env` was *unnecessary* for an `opencode` run and deliberately did not source it, reasoning that a data-agent-only run had no need of Anthropic credentials. Wrong: `opencode auth list` holds **0 credentials** on this machine and the client shells out with **no `env=`** (`opencode_client.py:174`), so the CLI inherits the parent environment and `ANTHROPIC_API_KEY` is the *only* way an `anthropic/…` model authenticates. The whole first probe run was void. The tell was available in the handoff itself — S219's own driver docstring showed `set -a && . ./.env && set +a` applied to **both** stages, contradicting the inference. **When a handoff says "X does not supply Y," that is a claim about Y, not about X's necessity; check what the predecessor actually ran.** | Session 220 (void first probe run, $0, 45/45 failed) | Reading any handoff gotcha phrased as a negative ("does not supply", "is not needed for", "cannot"). Before acting on the converse, find the predecessor's actual command line. |
| 91 | **A 0% across every threshold at implausible speed and zero cost is an environment failure, not a quality finding — read the meter before believing the rate.** Session 220's void run scored `sql_parse` 0/45, `sql_exec` 0/45 and `qc_structural` 0/45 — a perfect catastrophic-quality signature — in **36 seconds at $0 over 0 billed calls**, where a real run of the same block takes ~18 min and ~$1.78. The harness scores an unauthenticated CLI *identically* to a provider that cannot write SQL: same exception type, same three gate keys, same rate. What distinguishes them is not in the rate at all, it is in the instrumentation around it — call count, spend, wall time. This is why a measurement driver should meter cost and latency even when the deliverable is only a pass-rate: the meter is what makes a bogus rate *legible as bogus*. **Three cheap invariants to assert before reporting any live rate: calls > 0, spend > 0, and wall time within an order of magnitude of the prior measurement of the same block.** | Session 220 (unset `ANTHROPIC_API_KEY`; filed in `BACKLOG.md` as a diagnosability defect) | Before reporting or acting on any live-tier rate, especially a surprisingly bad one. A number that is *too* bad deserves the same scepticism as one that is too good. |
| 92 | **When a retry loop sorts a failure into policy branches, classify over the SET of failures observed, not the last one — last-wins silently discards the evidence the stricter branch exists to protect.** Session 221's `_call_with_retries` reassigned `excluded = isinstance(exc, _TRANSIENT_EXCLUDED)` inside the `except`, once per attempt, so only the final attempt's class reached the branch. A sample raising `LLMParseError` twice and *then* timing out was **excluded** — dropped from the denominator — despite having produced judgeable bad output twice. Reproduced against the real corpus: six observed parse failures across three samples scored **zero** misses and **passed** a 1.00 bar. The bug is invisible to a reading of the loop because each line is individually correct; it only appears when the classes are mixed within one sample, and no single-class test can reach it. **Mechanical:** for any N-attempt loop with a K-way outcome policy, write down which branch is the conservative one, accumulate a flag toward it (`saw_scored = saw_scored or ...`), and add a test whose attempt sequence mixes classes in **both** orders. Corollary: when the branches print different terminal notes, keep the exception that *decided* the branch, not the one that landed last, or the log names the wrong cause. | Session 221 (`sql_sweep.py` `_call_with_retries`; found by adversarial review, reproduced by mutation, one line from correct) | Any retry/backoff loop that classifies errors into different outcome policies (retry-vs-fail, score-vs-exclude, alert-vs-ignore). Especially when one branch removes data from a measurement. |
| 93 | **Injectable parameters get pinned by the tests that inject them; the DEFAULT is then the one value no test observes — and the default is what production runs.** Session 221 added `max_transient_retries` with a module default of 2. All eleven retry tests passed the bound explicitly, so mutating `_MAX_TRANSIENT_RETRIES = 2` → `0` left the whole suite green at unchanged coverage (1187 passed) while silently reverting **both** live call sites — `shadow_run.py` and `test_eval_live.py`, which each omit the keyword — to the pre-fix best-of-1 semantics the change existed to remove. Coverage cannot see this: every line still executes. The same shape applies to any constant a test suite always overrides (timeouts, batch sizes, sample counts, feature-flag defaults). **Mechanical:** for every new tunable, add one test that calls the production entry point **without** the keyword and asserts the observable consequence of the default (here: 3 attempts, not 1). Then mutate the default and confirm that test — and only that test — fails. | Session 221 (`_MAX_TRANSIENT_RETRIES`; found by adversarial review, reproduced by mutation) | Any session adding a defaulted, injectable parameter to code a production/driver path calls without arguments. Also whenever a test file's every case overrides the same knob. |
| 94 | **A backlog item's prescribed fix shape is a hypothesis written by a session that did not implement it — price the prescription before executing it, and expect the exhaustion/edge branch to be where it is wrong.** `BACKLOG.md`'s retry-asymmetry item specified "a `_TRANSIENT_ERRORS` tuple, bounded retries, exclude-with-note on exhaustion, and an `excluded_transient` count" — precise, actionable, and **wrong on the exhaustion branch**: excluding shrinks a denominator whose survivors are clean by construction, so a provider failing 14 of 15 samples scores 1/1 = 100% and PASSES a bar it fails 6.7% of today. The item was written by the sessions that *measured* the defect, so its diagnosis was excellent and its remedy inherited an unaudited precedent (`interview_sweep`'s exclusion, whose `excluded_transient` has exactly one consumer in the repo — inside an assertion f-string that never prints on a PASS). This is Learning #28's shape one level up: #28 says a *handoff's* prescriptive verbs are hints; #94 says the same of a *backlog item's* remedy, and adds where to look — the branch that fires rarely, which nobody exercised. **Mechanical:** before implementing a filed remedy, state its behaviour on the rare branch as a concrete number against the real thresholds, and check whether the precedent it cites was ever verified. | Session 221 (implemented A′ over the filed A after the operator was shown the inverted-gate arithmetic; S219/S220 filed the item) | Any session executing a fix shape specified by an earlier session, especially one citing "do what the sibling module does". Verify the sibling first. |
| 95 | **When a canonical tool REFUSES your input by design, that refusal is a specification — borrow the tool's discipline, never its config table, and never edit it.** `methodology_trim.py` returns `NO_CONFIG` for `SESSION_NOTES.md` and its `LEDGERS` table holds only `CHANGELOG.md` and `HANDOFFS.md`; the dashboard states why (`methodology_dashboard.py:360-366`): *"there is deliberately no generic fallback: a generic rule is what would mis-zone a differently-shaped ledger."* Session 222 trimmed 25,578 lines → 1,033 by hand-implementing the tool's four assertions for a grammar it refuses. **Four transferable rules.** (a) **Assert on re-parsed ARTIFACTS, never on the in-memory partition** — `records[:k] ++ records[k:] == records` is an identity that can never fire; the canonical tool's own first build printed three OKs while dropping a record. (b) **Ship `--self-test`.** A green proof tells you nothing broke; it does not tell you the proof *can* break. 9 mutants (delete a byte / delete a record / flip a char in each artifact / swap two records / move a front-matter line / append a trailer / drop the pointer / normalise deliberate mojibake) — and print **every** assertion that fires per mutant, not just the first, or a reader will believe L3 is an independent second opinion when L1 is doing the work. Two mutants here are caught by L2 **alone**, which is what proves L2 is not redundant. (c) **A record is a heading-delimited BYTE SPAN, never a semantic unit.** 16 sessions have no heading, 5 headings are duplicate zero-body stubs, one record swallows ten sessions — none needed a special case, because a byte claim is the only claim being made. Derive the cut index from a **session id**, never a typed line number. (d) **Never bundle a record edit into the trim commit.** Claim / trim / close out = three commits. A bundled edit registers as an added record and holds the proof red permanently with zero data loss — the shape **5 of the 20 proofs shipped across this operator's project fleet** currently exhibit. Reuses Learning #42's self-verifying-script form (write only if every assert passes) for the trim itself. | Session 222 (`SESSION_NOTES.md` 25,578 → 1,033 lines; shard + proof under `docs/architecture-history/`; all 9 mutants caught; `added: 0`) | Any session trimming, sharding, or migrating a grow-and-must-be-read ledger — and any session tempted to add a `LedgerSpec` to the canonical trimmer rather than accept its refusal. |
| 96 | **A `try` whose docstring promises graceful degradation must be audited against what the guarded call *actually* raises — and the exception hierarchy is the trap, not the call.** `sql_dialect_from_url` caught `sa.exc.ArgumentError` and promised "degrades rather than raising here." But `sa.make_url` coerces the port with a bare `int()`, and **`ArgumentError` derives from `SQLAlchemyError`, not from `ValueError`** — so the library's own error type does not cover the library's own failure mode. Two lessons compound. (a) **Enumerate the failure surface by measurement, not by reading.** A 24-case probe script found the trigger class is **five** inputs wide (unexpanded env var, alphabetic, **empty** port, float, IPv6-with-either), where the backlog item — written by a careful session — named one; it also showed a negative port and a 20-digit port parse *fine*, which is what stopped the fix from over-reaching into range validation. (b) **A widened catch has two bounds and mutation testing only finds the one you thought about.** Reverting to `ArgumentError`-only killed 10 tests; `ValueError`-only killed 3; **`except Exception` killed ZERO** — the suite was blind to the over-catch, which would silently convert a genuine defect into a `None` that reads as operator error. One monkeypatch test pinning propagation closed it. **Mechanical:** for every `except` you widen, mutate it in *three* directions — narrower, narrower-the-other-way, and `except Exception` — and require a distinct test to die for each. | Session 223 (`db.py` one-line catch widening; the `except Exception` mutant survived the first complete-looking suite) | Any change to an `except` clause, and any function whose docstring promises it will not raise. Also whenever a filed bug names a single reproducing input — assume it is a sample of a class and measure the class. |
| 97 | **Do not run mutation-based verification agents concurrently with your own measurements in a shared working tree.** Session 223's adversarial review told four agents to mutate the file under review and restore it. While they worked, the orchestrator ran a live CLI reproduction and got the **pre-fix traceback** — for ~30 seconds the fix genuinely was not in the file, and the result looked exactly like "the fix does not work." Nothing was lost (the agents restored correctly, and `git status` never showed a stray file), but the measurement was of an unknown mutant and the misreading was one step from being written into a CHANGELOG entry as fact. **Two mechanical fixes, use either:** pass `isolation: 'worktree'` so each agent mutates its own checkout, or guard every concurrent measurement with an assertion that the file is in the expected state — `grep -q '<the fixed line>' "$F" || exit 9` before the run **and again after**, since a mutation can land mid-run. The guard is what caught it here on the second attempt. Corollary: an agent instructed to "mutate then revert" is not a safety property you can rely on for *timing*, only for the end state. | Session 223 (adversarial review of the `sql_dialect_from_url` fix; one false pre-fix reproduction, caught by a state guard) | Any session running review/verification subagents that edit files, while also running its own commands against the same tree. Especially when the deliverable's correctness is what you are measuring. |
| 98 | **Run the project's real lint gate, not the linter's full feature set — `ruff format` is not `ruff check`.** Session 223 ran `ruff format --check` reflexively, applied it, and it reflowed **three unrelated pre-existing functions** in the file being edited (collapsing multi-line signatures), turning a 1-line logic diff into a 37-line one. The repo's CI (`.github/workflows/ci.yml:23`) runs `uv run ruff check src/ tests/ packages/ scripts/` and nothing else; `[tool.ruff]` sets `line-length = 100` for the **linter**, and no formatter gate exists. The committed tree therefore is not, and is not required to be, `ruff format`-clean. Reverted with `git checkout` and re-applied the edit by hand. **Mechanical:** before running any formatter, read the CI workflow and the pre-commit config to find the command the project actually enforces; if the tree is not already clean under a tool, that tool is not a gate, and running it is an unrequested reformat of someone else's code. | Session 223 (`ruff format` on `db.py`; 24 unrelated lines reverted) | Before running any auto-formatter in an unfamiliar repo, and any time a "lint fix" produces a diff larger than the change you made. |
| 99 | **Mutation coverage is a property of each ASSERTION, not of the mutant count — an assertion no mutant reaches is unfalsified no matter how green `--self-test` runs.** Session 224 extended the S222 trim proof with a new assertion (`L2/b0`: "the declared front-matter substitution is uniquely anchored in `before`") and shipped 11 mutants, all caught, exit 0. An adversarial reviewer then demonstrated that **no mutant perturbed the `before` operand at all**, so b0's failure branch was unreachable in every one of the 11 — the one assertion the proof added beyond its ancestor was the one thing `--self-test` could not speak to. The fix was a mutant (M13: strip the anchor out of `before`), not a rewrite. **The trap is structural:** mutants are naturally written against the artifact you just produced (`after`, `shard`), while new assertions often guard the *input* or the *declaration*, so the two drift apart silently and the suite still reports 100%. **Mechanical:** after writing the mutant list, build the assertion×mutant matrix — for each assertion, name the mutant that fires it *alone or first*; any assertion with an empty row is unfalsified and needs a mutant before the proof ships. Print every assertion that fires per mutant (not just the first), which is what makes the matrix readable from the run output. Extends #95(b), which established "ship `--self-test`" but counted mutants rather than covered assertions. | Session 224 (second `SESSION_NOTES.md` trim; `L2/b0` shipped uncovered, caught by adversarial review, closed by M13; suite 11 → 15) | Any hand-built proof, assertion suite, or invariant checker — especially one **derived from an ancestor**, where the inherited mutants cover the inherited assertions and create a false sense of completeness over the new ones. |
| 100 | **A refuted finding is refuted on SEVERITY far more often than on FACT — mine the killed pile before discarding it.** Session 224's adversarial review returned 6 survivors and 11 kills. **The two most valuable changes in the session came from the KILLED pile**, where refuters wrote verdicts of the form "REPRODUCED, then refuted as a defect" / "CONFIRMED AS FACT, REFUTED AS A DEFECT": (a) `L2/b0` had zero mutant coverage — killed as "stylistic", true as stated, and it became learning #99; (b) the cut point was unpinned, so sliding the boundary by a whole record preserved concatenation, membership *and* order and passed every assertion — killed because the ancestor proof *declares* that limitation in prose, which makes it a known gap rather than a bug. It became a new assertion (L4) plus two mutants, the single largest strengthening of the deliverable. **The refuters were not wrong** — neither was a defect in the shipped artifact. But "not a defect" and "not worth acting on" are different rulings, and a refute-by-default panel only ever issues the first. **Mechanical:** have refuters rule separately on *reproduced?* and *defect?*, keep both fields, and read every finding whose facts reproduced regardless of its verdict. A kill rate near 100% on a careful build is the expected outcome, not evidence the review was wasted. Corollary on panel design: with a `kills >= 1` rule and a 2-agent panel, one dead agent (an API error killed one here) silently reduces a finding to a single-vote decision — prefer an odd panel and a majority rule. | Session 224 (adversarial review of the second trim: 39 agents, 5 lenses; 6 survived, 11 killed, **2 of the killed acted on and both improved the proof**) | Whenever an adversarial/refutation panel returns. Read the killed findings' *reasoning*, not just their verdicts, and act on any whose factual premise the refuter confirmed. |
| 101 | **When you change the SHAPE of something the repo documents, grep for the documents that DESCRIBE it — not just the ones that reference it.** Session 224 created a second `SESSION_NOTES.md` archive shard. A path-grep found the references and they were easy; the damage was in four documents that described a **one-shard world** in prose: `CLAUDE.md` (twice — "holds the newest ~6 sessions", and a singular "**Shard + proof**" bullet), `README.md`'s repo map, `PROJECT_CONVENTIONS.md` ("First and only instance", plus a `<STEM>-through-<CUTKEY>` naming rule the second shard necessarily broke), and `BACKLOG.md`'s open item about the unwatched shard. **`CLAUDE.md` is the one that matters:** it is injected into every session before anything else is read, so an agent resolving "where do retired records live" from it would have grepped one shard, got zero hits for Sessions 220–217, and had no pointer to the file that held them — the exact silent-miss the shard apparatus exists to remove. **Two sub-lessons.** (a) **The second instance is what tests a naming convention.** `<STEM>-through-<CUTKEY>` is unambiguous only for the first shard, whose span is open at the bottom; a second named `-through-S220` would falsely claim to contain Sessions 216→1. Non-first shards need the range form. A convention written from one instance encodes that instance's accidents. (b) **Cardinality words are the tell** — "the shard", "first and only", "~6 sessions", "nine ways": grep your own new artifacts for counts and singulars and re-derive each one, because they are typed, not computed. | Session 224 (second trim; four describing documents found stale — one by the author, three by adversarial review; plus a shard banner that typed "nine ways" against a 15-mutant suite) | Any change that adds a second instance of something previously singular, or changes the shape/count/range of a documented artifact. Grep prose, not just paths; start with the always-injected instruction file. |
| 102 | **An assertion is only as falsifiable as its OPERANDS are distinguishable — degenerate test data unfalsifies an assertion that is present, correct, and green.** Session 225 closed a review finding that a summary-line test asserted only label tokens and never a value. The fix asserted all six values against a directly-constructed result — and a 12-mutant matrix then showed the mutant *swapping the two fractions* still SURVIVED, because the test set `cycle_matches` to 2/3 and `risk_acceptable` to 2/3: both operands rendered `2/3`, so the swap was invisible to an assertion written specifically to catch it. This is learning #99 one layer down: #99 says every assertion needs a mutant, #102 says the mutant cannot reach the assertion if the fixture's values coincide. It is the SECOND consecutive session in which the author of an assertion and the author of its mutant were the same person and the coverage gap survived review-by-reading. **Only the run finds it.** | Session 225 (`test_governance_sweep.py::test_summary_carries_every_counter_with_its_value`; mutant M6 survived at 11/12, passed at 12/12 after the fractions were made 2/3 vs 3/4) | Whenever a test asserts two or more parallel values from one producer — a summary line, a formatted report, a mapping of N fields to N keys, a swap-able argument pair. Give every slot a DISTINCT value, then run a mutant that swaps them. "I asserted the value" is not the bar; "a swap fails" is. |
| 103 | **When you unify N implementations, state the property that is true of all N — not the stronger uniformity the unification was motivated by.** Session 225 replaced the last hand-written measurement loop so all three eval sweeps run through shared modules, and wrote in the highest-traffic docstring that all three now "apply one transient policy." False: `interview_sweep` EXCLUDES an exhausted seam error where the other two SCORE it, and that difference is *correct* (an interview sample is a whole multi-turn run with no report to judge; a governance or SQL sample is one call whose failure IS the measured capability). The true, checkable claim is the weaker one — **no hand-written loop remains** — and it is the property the file actually needed. A 3/3 refuter panel upheld the finding; a second lens reached the same fact and was refuted 2/3, so the claim survived one panel and died to another. Learning #86 carried the same over-reach in its own text and was corrected in the same session. | Session 225 (`test_eval_live.py` module docstring; also `governance_sweep.py`'s and the `README`'s framing) | Any refactor whose motivation is "these should be consistent." Before writing the summary sentence, diff the siblings and enumerate what genuinely holds across all of them. The motivating slogan is a hypothesis; the docstring must state the theorem. |
| 104 | **A verification cap in a review fan-out bounds COST, never TRUTH — the author must adjudicate every capped finding personally, because the ranking that produced the cap is itself unverified.** Session 225 ran 5 lenses producing 24 findings and verified the top 6 by self-declared severity; 18 fell past the cap unverified. Adjudicating those 18 by hand yielded **three false docstring claims of my own** and seven unfalsifiable assertions — more real defects than the 2 survivors of the verified 6. The severity labels came from the finders, who cannot rank across each other and had no incentive to; "moderate" from a lens that found eight things and "major" from a lens that found one are not the same scale. Logging what was dropped (the no-silent-caps rule) is necessary and NOT sufficient: a logged-but-unread finding is a finding you paid for and discarded. | Session 225 (cap = 6 of 24; the survivors were 1 major + 1 moderate, while the capped pile held the three false-docstring findings and every test-adequacy gap) | Any capped review, triage, or fan-out. Cap the expensive VERIFICATION step; never cap the cheap ADJUDICATION step. Budget time after the fan-out to read every dropped item and rule on it yourself — and prefer a cap set by cost-per-verification over one set by a count you guessed before seeing the findings. |
| 105 | **A handoff number without its command is a rumour — and "re-derive, don't inherit" is advice a session can fail to take while writing it.** Session 225's ruling correctly flagged the filed 644/50 inventory as stale, then replaced it with **667 across 52 files**, which is reproducible at **no commit by no method**: not matching-lines, not raw occurrences, not working-tree grep, not untracked-inclusive, at none of the fourteen commits in range. The true values were 661/50 before its own commit and 665/51 after. The failure mode is specific and seductive: the moment you write "this number is stale," you have established that numbers matter, and the fresh number you supply inherits *more* authority than the one you retired — while receiving *less* scrutiny, because producing it felt like the diligence. **Mechanical fix: never state an inventory count without (a) the exact command and (b) the commit it was measured at, in the same sentence.** Pin to a commit that predates the document doing the counting, or the document contaminates its own inventory — this plan is pinned at `59615e2` precisely because committing it added ~98 hits and a 52nd file. | Session 226 (re-deriving the rename inventory: 666/51 at `59615e2`, reproducible with `git grep -c "claims-model-starter" 59615e2 -- .`; S225's 667/52 unreproducible; S221's 644/50 also low at filing, already caught by S222) | Any session that quotes, corrects, or files a count — inventory, coverage, hit total, cost. Especially when correcting someone else's: the correction is the least-scrutinised number in the document. |
| 106 | **In an inventory, a grep pattern containing `.` is a bug — use `grep -F`. The inflation is silent and it mis-assigns the fix, not just the count.** Counting the wiki-clone references as `claims-model-starter.wiki` gave **104 lines / 11 files**; the literal count is **95 / 7**. The unescaped `.` matched `claims-model-starter**/**wiki` — the `github.com/rmsharp/claims-model-starter/wiki/<Page>` URLs. The count error was minor; the classification error was not. Those 9 lines are **page URLs that survive a rename via GitHub's redirect** and need a one-line URL edit, while the 95 are a **git remote guarded by a fail-closed check in a shell script**. Two different fixes, two different risk levels, silently merged into one row of the plan's per-pattern table — the very table whose purpose is "one fix per pattern." The filed backlog's "89" was the same measurement made the same loose way, so the error had already survived one session. | Session 226 (caught by an independent discovery lens re-running the count literally; verified with `git grep -cF` vs `git grep -c`, and by listing the 9 differing lines) | Any evidence-based inventory, especially one whose rows are meant to map one-to-one onto distinct fixes. Dots, and the whole regex metacharacter set, appear in exactly the strings inventories care about: filenames, domains, package names, extensions. |
| 107 | **Before sequencing around a guard, classify it fail-CLOSED or fail-OPEN — the loud ones are safe and the documented dragons are usually the loud ones.** `scripts/publish_wiki.sh` has five guards; all five `exit 1` with a message and change nothing, and the filed backlog item named one of them a *"hard blocker on rename."* It is not: `man githooks` says post-commit *"cannot affect the outcome of git commit,"* so the worst case is a loud unpublished wiki. The mechanism that actually threatens the project is the **one line nobody had documented** — `.githooks/post-commit:18`'s `grep -q '^docs/wiki/claims-model-starter/'`, which `exit 0`s in **silence** when its prefix goes stale, so every future wiki edit quietly never publishes. Two aggravations found only by looking: the rename commit itself still fires (a rename appears as both paths in `diff-tree`), so you get one reassuring success and the failure begins on the *next* commit in a *different* session; and the same line is blind to merge commits, **which has already happened here** — merge `ff04c02` changed five wiki pages and `git diff-tree --no-commit-id --name-only -r` returned 0 lines. `git grep -ln "post-commit\|publish_wiki" -- tests/` returns nothing, so no CI run can ever catch it. | Session 226 (planning the repository rename; verified against `man githooks`, the real rename commit `35ccbd9`, and the real merge `ff04c02`) | Any plan that sequences around automated machinery — hooks, guards, CI triggers, watchdogs. Ask of each: *"if this is wrong, do I find out?"* Budget the sequencing care for the silent ones, and require an end-to-end observation (did the downstream artifact actually change?) rather than an assertion that the mechanism looks right. |
| 108 | **A completion criterion must be observed RED before it ships — the plan-level form of #99/#102.** A plan's verification commands get the same free pass a never-falsified test does: they look rigorous, they were never run, and the executor discovers at sign-off that they are unsatisfiable. Three in this plan's lineage were: the filed `grep -rIl "claims-model-starter" . | grep -v '^\./\.git/'` returns **58 files against 51 tracked** (`.venv` METADATA, the gitignored `site/` build, a generated `.html`, the plan itself) and can never go green; a count-based criterion is unsatisfiable in principle because every document *about* the rename inflates it; and my own `curl …/sitemap.xml | grep -c` → 0 was **fail-open**, because `sitemap.xml.gz` is served alongside it, is preferred by crawlers, carries the same two dead URLs, and **no grep can read it**. The fix is to run every criterion while writing it and record what it printed: this plan's allowlist prints **20 named paths today**, so it is red, the 20 are enumerated, and going green means something. | Session 226 (`docs/planning/repository-rename.md` §7.2 pre-run and enumerated; the gz fail-open caught by the completeness critic after three refuters missed it) | Any plan, checklist, or acceptance criterion. Run it, paste the output, name the expected delta. A criterion whose current output you cannot state is a wish. And when the criterion reads a generated artifact, enumerate the artifact's blobs — compressed or binary ones are invisible to the tool you reached for. |
| 109 | **Verify a completion criterion's movement by set difference, not by count — the half a count cannot give you is the half that catches collateral damage.** Phase 1 of the rename was expected to shorten the plan's §7.2 file allowlist. A count falling 20 → 17 is consistent with three files repaired; it is *equally* consistent with four repaired and one newly broken. Running the criterion into a file before and after and diffing both directions gave the answer a count cannot: exactly `README.md`, `mkdocs.yml`, `docs/tutorial.md` left, and **nothing appeared**. Note the near-miss that makes this a rule rather than a preference — the first `comm -23` ran on unsorted input, printed the right three anyway, and warned; on differently-ordered input it would have been silently wrong. **Sort both sides, diff both directions, and state the "none appeared" result out loud.** | Session 227 (rename Phase 1 — allowlist 20 → 17) | Any time a plan's completion criterion is a *list* and a phase is expected to shorten it. Also whenever you are about to report progress as a number that used to be a set. |
| 110 | **Before an irreversible act, measure the symptoms you will later be tempted to blame on it.** Phase 1's step 0 recorded the published tutorial at `page=200, css=404` *before* `gh repo rename`. After the rename the stylesheet was still 404 — the deployed `gh-pages` tree carries no `assets/` directory at all, a defect that predates the rename by months. Without that one-line pre-measurement the only available readings were "the irreversible act broke it, revert" (impossible here — Pages URLs do not come back) and "assume it was already broken" (an assumption, in the one place the session cannot afford one). The baseline turned an interpretive argument into arithmetic. The generalisation is not "take a backup": it is **enumerate the observable symptoms the act could plausibly produce, and record their current values first**, which is a different and smaller list than "everything". | Session 227 (rename Phase 1 — the pre-existing 404 that would have read as rename damage) | Before any rename, forced deploy, destructive push, migration, or schema change. Especially when the act is not revertible, because then the wrong reading has no cheap escape. |
| 111 | **Cross-check every completion criterion against the phase that makes it true — a criterion no phase owns fails at the last phase, when the plan is supposed to be closing.** `docs/planning/repository-rename.md` §7.4 check 3 requires all **three** clones' `origin` URLs to carry the new name, and dragon 9 names the third one (`~/Development/mpc_tests/model_project_constructor`) explicitly *so that it would not be forgotten*. It is still forgotten: Phase 4 step 1 re-points the wiki clone, and no phase anywhere re-points that one. **Naming a hazard in a dragon is not the same as scheduling its fix** — the dragon section reads as coverage and is not. The check is mechanical and cheap: walk §7 backwards and, for each criterion, name the phase *and step* that makes it green; any criterion whose answer is a shrug is orphaned. An executor should run the same walk before Phase 1 and file the orphans rather than discovering them at Phase 5. | Session 227 (rename Phase 1 — §7.4 check 3 vs. Phases 1-5) | When writing any multi-phase plan with a completion-criteria section, and again at the *start* of executing one. The failure is invisible while reading forward and obvious while reading backward. |
| 112 | **When you mechanise a prose claim, enumerate the claim's OPERANDS before writing the check — a table has numbers *and* destinations, and asserting one of them looks exactly like asserting the claim.** Session 228 invented L5 to stop a stale routing table sending a session to the wrong file. The first version parsed `N ≤ 216`, `217 ≤ N ≤ 220`, `N ≥ 225` with three integer-only regexes and never captured which FILE each clause routed to. A probe that swapped two filenames and touched no digit passed silently — the exact failure L5 existed to remove, still reachable, inside the assertion written to remove it. Four L5 mutants had been written and all four passed, because they were written by the same person from the same wrong mental model of what the claim asserts (this is #102 one level up: #102 says the fixture's values must be distinguishable, #112 says the *claim's operands* must all be captured). Two mechanical fixes: parse the whole clause in ONE match (`\*\*<bounds>\*\* → \`<file>\``) so a bound cannot be checked without its destination; and **anchor the declaration against reality** — open each file the table names and assert its actual record ids fall inside its clause. That last check is the only thing that catches a boundary moved *consistently in every copy at once*, which no amount of internal self-consistency can detect. | Session 228 (third trim; L5 shipped integer-only, caught by adversarial review, then upgraded to clause-level + a reality anchor. Mutant M23 — the S216/S220 frontier moved to 100/101 in every copy and the declaration — is caught by the reality anchor and by nothing else) | Whenever you turn a sentence a human acts on into an automated check. Write down the sentence, underline every noun in it, and confirm the check reads each one. Then ask what an attacker who can edit *everything you declared* would still not be able to fake, and assert that. |
| 113 | **A green `--self-test` can be green for the wrong reason, and the pass/fail line will never tell you — read the per-mutant attribution column.** Session 228's harness needed L7 reachable before the trim was committed, so it passed a synthetic 40-zero commit id. That id then made every `git show <sha>:<path>` in L5's reality anchor fail, so L5 reported "the file cannot be read" for two shards on **all 28 mutants**. The suite printed `all 28 mutants caught` and exited 0. It was not testing detection; it was testing that a broken proof fails. The tell was visible in one glance at the attribution column — the same two `L5/4` codes on every row, including mutants that touch nothing near those files — and invisible in the summary line. **Print which assertions fired for each mutant, and read that column; a code that appears on every row is a broken fixture, and a code that appears on no row is an unreachable assertion (#99).** The structural fix was to stop deriving one thing's applicability from another's identifier: L7 now decides from the artifact it compares, so no caller ever has to invent a value to make it reachable. | Session 228 (third trim; the fake-sha self-test reported 28/28 with false attribution on every row, found by reading the codes rather than the verdict) | Every time you run a mutation suite. Diff the attribution column against what each mutant should provoke — uniform codes across unrelated mutants mean the harness is broken, not that coverage is excellent. And never fabricate an identifier to satisfy a signature; change the signature. |
| 114 | **Uniqueness of coverage is not stable — adding a broader assertion can silently retire an older one's, so re-measure the matrix after every addition and rule on the subsumed assertion explicitly.** L4 (the cut key pinned against the record ids) was uniquely load-bearing when Session 228 began: neuter it and M14/M15 survived. After L5 gained its reality anchor, neutering L4 caught nothing — L5/4 sees a whole-record boundary shift too, because the moved record's id falls outside its file's clause. Nothing broke, and nothing warned. Had the header's coverage table been left as written it would have claimed uniqueness L4 no longer has, which is exactly the class of false-but-plausible documentation this apparatus exists to prevent. The decision to KEEP L4 is defensible (it states the cut key directly rather than inferring it, and its message names both sides) but it has to be *made and written down*, not defaulted into. **Mechanical: the coverage matrix is an output of the suite, never a claim typed beside it — re-derive it as the last step before commit.** | Session 228 (third trim; measured L0/L1/L3/L4 → no unique mutant, L2 → M10-M13, L5 → M16-M24, L6 → M25/M26, L7 → M27/M28, after adding L5's reality anchor) | After adding any assertion to a suite that already has some. Re-run the leave-one-out sweep, compare against the previous matrix, and for every assertion that lost its unique coverage decide out loud whether it stays and why. |
| 115 | **A copy of a mutable fact inside a write-once artifact is stale by construction at the next change, and cannot be repaired — so either do not put it there, or label it a snapshot and name the authority.** Session 224's shard banner carries a routing table ending *"the live ledger when N ≥ 221"*. Session 228 moved Sessions 224-221 into a new shard, which falsified that sentence permanently: the file is frozen by rule, its own proof predates any assertion that could notice, and no future session may edit it. The convenience of a self-describing artifact is real, and so is the cost — every write-once copy of a shared fact is a landmine armed by the *next* change, not this one. The rule that follows: a frozen artifact may state facts about ITSELF (what it holds, when it was cut, what was verified) freely, but a fact about the SYSTEM belongs in the mutable authority with a pointer from the frozen copy. Session 228's own shard banner therefore labels its table a snapshot, names the live pointer block as the authority, and cites the S220 banner as the observed instance of the hazard. | Session 228 (third trim; the S220 shard's banner falsified by this cut and unrepairable — found by adversarial review, not by the author's own grep for describing documents, which covered mutable files only) | Before writing any fact into a file you are about to declare frozen, immutable, write-once, or archival. Ask: can the next change to the system make this sentence false? If yes, it belongs elsewhere — or it must be explicitly dated and deferred to the authority. Also add frozen artifacts to the sweep #101 prescribes: they describe the system too, they just cannot be fixed. |
| 116 | **Mine a refuted finding for its REASONING, not just its facts — the objection that kills the obvious fix frequently specifies the fix that works.** Session 228's adversarial review returned `confirmed: []`: every finding refuted. Every refuter also wrote "mechanically reproduced" / "the mechanics are accurate" and then refuted on severity — the exact split #100 documents — and all four were acted on, all four improved the deliverable. The sharpening beyond #100 is what the refuters' *arguments* contained. The verifier who killed the write-once finding did not merely assert it was minor: it ran the decisive counter-experiment, forcing the ancestor proof down a worktree-reading branch, and showed it goes RED today with zero data lost — "the proof red forever", the precise failure `CLAUDE.md` names as the thing to avoid. That objection is correct and it kills the naive implementation. It also fully specifies the one that survives: compare **only the write-once artifact**, never the ledger that is supposed to change. L7 is asymmetric because a refutation said so. **A refuter arguing about severity has usually done the cheapest available experiment on your idea; read the experiment even when you discard the verdict.** | Session 228 (third trim; 4 findings, 0 confirmed, 4 acted on → L5's upgrade, L6, L7; L7's asymmetry taken directly from the reasoning of the refutation that killed the finding) | Whenever a refutation panel returns. Read each refuter's method section before its verdict. If it ran an experiment your fix would have to survive, that experiment is a design constraint you did not have — collect it whether or not you accept the ruling. |
| 117 | **A plan's diagnosis and its prescribed repair are two separate claims — measure the second one too.** `repository-rename.md`'s dragon 1 correctly diagnosed that the rename turned five `curl` acceptance criteria into vacuous passes, and then prescribed two repairs, *both wrong*: "add `-fL`" is inert inside a pipeline (learning #118), and its path-scoped fix for the clone-independence grep is unsatisfiable at **350 hits, 284 of them legitimate `from model_project_constructor` imports** — the identical objection dragon 1 itself raises against the alternative it rejects. A dragon is written by a session that had time to *think* about the problem, not necessarily to *run* the fix. The corollary that paid separately: **run the command on the line you are editing, even when you are only editing its URL.** Doing so found `gh repo view … --json isPrivate,archived` — `archived` is not a field, it is `isArchived`, and that C5 criterion had never been able to pass under either repository name. | Session 229 (rename Phase 2; both prescriptions measured before application, both replaced) | Whenever a plan, dragon, ADR or review hands you a *specific* remedy. Reproduce its failure and verify its fix, in that order. Trust the finding; test the prescription. |
| 118 | **A shell pipeline reports its LAST command's exit status, so `-f` on a piped `curl` is decoration.** Measured: `curl -sfL <live sitemap> \| grep -c audits` and `curl -sfL <404 sitemap> \| grep -c audits` **both print `0` and both exit 1** — `-f` suppresses the body, and `grep -c` prints `0` on empty input just as it does on a body with no match. Any acceptance criterion of the form `curl … \| grep -c X   # -> 0` is fail-open against a dead host regardless of curl flags, unless `pipefail` is set or curl's status is captured separately (`out=$(curl -sfL …) \|\| echo FAIL`). Sibling case: `-f` is actively *wrong* on a check whose expected result is a 404 — it makes the expected outcome exit non-zero (56) while still printing the code via `-w`. There, the fix is a **positive control** on the same host, because a host that 404s every path makes every "→ 404" assertion meaningless. | Session 229 (measured with curl 8.7.1 against the live and the dead Pages hosts, before editing the five criteria) | Writing or reviewing any `curl`-based acceptance criterion, and any assertion that pipes a fallible fetch into a counter. Ask what the check prints when the *host* is gone, not just when the content is. |
| 119 | **"Flag it, don't rewrite it" is a work item with a filing location — a flag nobody can find is not a flag.** `repository-rename.md`'s Phase 2 table listed three greps in `enterprise-migration.md` as scope, while dragon 1 forbade editing them: *"Flag this to whoever owns `enterprise-migration.md`; do not silently rewrite another plan's acceptance criteria."* The two reconcile only if the table row's deliverable **is** the flag. Discharging it meant writing the blind spot, the measurement refuting the recommended repair, and the workable alternative into `BACKLOG.md` — where the owner will actually meet it — rather than mentioning it verbally or in a commit message. Cross-plan boundaries are real: a plan may *diagnose* another plan's defects at any depth and may *repair* only what its own scope section authorises. | Session 229 (twelve independent adversarial verifiers reached the same reading from §8's "This plan only repairs the five verification lines") | Any time a plan's scope table and its own prose disagree about whether to edit something. Read the narrow instruction as governing, then ask what artifact discharges it, and file that artifact. |
| 120 | **In an adversarial fan-out, mine the REFUTED set — it carries the ownership map, and it is usually the larger deliverable.** A 42-agent sweep over rename Phase 2 returned **2 survivors and 40 refutations**, and the refutations were the more useful half: each one named the phase that actually owns the line it dismissed, quoting the plan text that assigns it. That map is what told Phase 2 which of 43 old-name hits to leave alone — 17 to Phase 4 by exact count, 9 D-R5 filesystem paths, 4 to Phase 5's residue walk — and it was produced as a by-product of a fan-out designed only to find survivors. This is the complement to [#100](PROJECT_LEARNINGS.md) (`confirmed: []` can still be worth acting on): there, the refuters' *reasoning* improved the deliverable; here, their *assignments* were the deliverable. **Design the verify stage to emit an owner, not just a verdict** — one extra schema field converts a filter into a classifier. | Session 229 (the `owning_phase` enum on the verdict schema; 40 refutations produced the phase map that Phase 2 executed against) | Designing any verify/refute stage. Add an "if refuted, who owns it?" field. Then read the refuted rows before the surviving ones. |
| 121 | **An operator ruling can silently make an already-written completion criterion unsatisfiable, and no proof in this project watches for it.** `repository-rename.md`'s §7.2 allowlist was written in Session 226; decision D-R5 was answered on 2026-08-20 in a way that permanently pins three `claims-model-starter` lines inside `scripts/publish_wiki.sh` — a file §7.2's allowlist does not exempt, while `:1134` rules *"If the command prints a path, that file was missed. No judgment call."* The gate and the ruling live **in the same document**, four hundred lines apart, and disagreed for three sessions without anyone noticing. The same ruling left Phase 4 instructing its executor to change a line the ruling forbids changing, which the plan's own text says breaks the script. **When an operator answers a decision, re-walk every completion criterion and every phase step the answer touches, in the same commit.** Deciding is an edit to the plan's semantics even when it edits none of its criteria. | Session 229 (found by walking §7 backwards against the phases — Session 227's gotcha 8, inherited through Session 228's handoff) | Immediately after an operator resolves any blocking decision inside a plan, and at the start of any phase that executes under a ruling made after the plan was written. |
| 122 | **A defect filed against the instance you found is a defect filed against one instance. Before filing, ask what CLASS it belongs to and grep for the rest.** Session 229 filed "§7.2's allowlist does not exempt `scripts/publish_wiki.sh` (3 permanently-pinned lines), so Phase 5's DONE gate can never go green." True — and incomplete. `docs/planning/enterprise-migration.md` carries **14** lines pinned by the identical operator decision (D-R5's `~/Development/claims-model-starter.wiki`), and no group exempted it either. Repairing only the named file would have shipped a gate as unreachable as it was found, under a commit message saying "fixed". The tell: Session 229 had **already measured** those 14 lines while writing an adjacent flag in the same `BACKLOG.md` block ("the nine D-R5 filesystem paths") — the two facts sat in one file and never met. The fix that generalises is a **classification bucket** (the plan gained a `§3.3` for "live file, pinned by an operator decision, permanently"), not a second one-file exception, so a third such file has somewhere to go. | Session 230 (repairing `repository-rename.md`; found by an adversarial review of the repair, reached independently by 4 of 6 lenses) | When filing a defect against a completion criterion, an allowlist, or any enumerated exception list. Grep for every file matching the same *reason*, not the same *symptom*. And when a fix requires adding an exception, ask whether the thing you actually lack is a category. |
| 123 | **A line number pointing into a file the current session is ALSO editing is stale before it is written.** Session 230 corrected two drifted `BACKLOG.md` citations in `repository-rename.md` to fresh numbers measured at `HEAD` — and the same session's own `BACKLOG.md` edit then shifted both by 28 lines, so the "corrected" citations were wrong in the commit that shipped them. This happened **inside the bullet lecturing about navigational-citation drift**, and separately three in-file self-citations went stale twice as the repair grew. The rule is not "re-derive at the end" (that still races the close-out commit) — it is **cite something `grep` can find**: a quoted section heading, a distinctive sentence, or the `grep -n` command itself. Session 229's taxonomy holds and sharpens: numbers carrying an *assertion* stay right because something re-derives them; numbers that are merely *navigational* have nothing checking them, so give them no number to get wrong. | Session 230 (caught by an adversarial review, twice) | Any time you write `file.md:NNN`. If that file is in `git status`, or will be before you commit, write a greppable name instead. |
| 124 | **Cap findings per lens in an adversarial fan-out; the cap is what makes a second, cheaper round possible.** Session 230 ran two reviews of the same document. Review 1: 6 lenses, no `maxItems`, 3 skeptics per finding — **132 agents, ~8M tokens, 42 findings, 14 survivors.** Review 2: 4 lenses, `maxItems: 4`, no per-finding fan-out, but every finding required `confidence: "reproduced-with-a-command"` — **4 agents, 570k tokens, 13 findings, all reproduced, 3 of them real defects the first review had not reached** (because they were in material the first review predated). **~14× cheaper and it did not obviously find less.** The expensive ingredient is the per-finding skeptic panel multiplying an uncapped finding count; the cheap substitute is forcing each finder to reproduce its own claim with a command before reporting it. Reserve the skeptic panel for findings whose *severity* is contested, not for filtering noise a `maxItems` cap and a reproduction requirement remove for free. | Session 230 (self-assessment; the guideline is ≤15 agents and review 1 used 132) | Designing any adversarial fan-out. Set `maxItems` per lens, require command-reproduced evidence in the schema, and only then decide whether findings need independent refutation. Re-review after each substantive change rather than betting everything on one large round. |
| 125 | **Neuter one `if` at a time, not one assertion at a time — assertion-level coverage hides an arm that has never fired.** The third trim's proof shipped `L5/2`, the gap/overlap check, and **none of its 28 mutants could reach it**: both mutants that rewrite `ROUTING` happen to stay contiguous, so that arm never executed under any mutation. Session 231 confirmed it by measurement (`--self-test \| grep -c 'L5/2'` → 0) and then shipped the same defect itself, in six arms of its own two new assertions — one day after writing the rule into `CLAUDE.md`. The ancestor's function-level neuter loop cannot find this; you must neuter the individual `if` (`for s in required:` → `for s in ():`). Two traps: neutering an arm can make the run **crash** rather than survive, and a traceback means *caught*, not *passed*; and some arms provably cannot have a uniquely-catching mutant (no GAP mutant is uniquely-`L5/2` because dense session ids make any real gap also trip `L5/4`) — say so in the file rather than leaving the next reader to rediscover it. | Session 231 | Whenever a proof, guard or validator gains a new assertion made of several `if`s. Writing the rule is not obeying it. |
| 126 | **A "here are the copies you must update" sentence is itself a list that goes stale — sweep for the class instead of trusting it.** The third trim's pointer block named exactly two copies of the shard set outside every proof (`README.md`'s repo map, `PROJECT_CONVENTIONS.md`'s shard-naming rule) and `CLAUDE.md` repeated the pair. Both were right; the *list* was incomplete. One `git grep -l 'SESSION_NOTES-'` found a third nobody had named — `BACKLOG.md`'s read-cap item, whose whole subject is the shard set and which said "there are now **three** unwatched shards" — and an adversarial review found a fourth hiding in plain sight: `CLAUDE.md`'s own prose, whose routing *table* was machine-checked while the count words around it were read by nothing, so a half-updated `CLAUDE.md` shipped green. This is [#122](PROJECT_LEARNINGS.md) applied to a list of instances rather than a single one. | Session 231 | Before acting on any enumerated "update these N places" instruction. The sweep costs one second; the list was written by someone who had not run it. |
| 127 | **Rebuild the derived artifact from a clean checkout after every edit to its generator — a half-synced pair passes `--self-test` while the plain run is red.** Session 231 built the shard from a builder script and hand-declared the same banner text inside the proof. Editing one and not the other left them six bytes apart; `--self-test` still reported **all mutants caught**, because the L6 banner assertion fired on *every* mutant and each one was therefore "caught". Only the plain run showed the failure. A self-test is a claim about the proof, not about the artifact; **run both, and read the plain run first.** Same session, same shape, three times: banner wording, mutant count, and the shard's own self-reported line count each drifted when the other file changed. | Session 231 | Any time a constant is declared in two places by construction. Re-derive the artifact and re-run the plain check before believing a green self-test. |
| 128 | **A line-wise `grep` returning 0 is not evidence a quoted phrase is absent — soft-wrapped prose hides it.** Session 231 checked the inherited claim that the S220 shard's banner says *"the live ledger when N ≥ 221"*, got `grep -c` → **0**, and was one step from "correcting" a sentence that was accurate: the phrase straddles a newline. `tr '\n' ' ' < file \| grep -o '...'` found it immediately. The same session's *other* suspect claim ("three of the four had no enforcement") was genuinely false, so the discipline is not "trust the prose" — it is **use a matcher that can see what you are looking for before concluding it is not there.** | Session 231 | Before editing or flagging any quoted phrase in wrapped Markdown. Cost of the wrong call is an unnecessary edit to a frozen file. |
| 129 | **A proof that reads only what a publishing script wrote LOCALLY cannot tell success from a stranded commit — assert the remote.** `scripts/publish_wiki.sh` commits into the wiki clone and *then* pushes; on push failure it prints an error and **exits 2 with the local commit left in place** (its own header says so). All four of rename Phase 3's verification commands read local state only — `rev-parse HEAD` on the clone, a re-run of the publisher, a local-vs-local `diff -r`, and a Pages `gh run list` — so in that state every one of them goes **green while the live wiki is stale**, and the `BEFORE != AFTER` check prints *"HOOK FIRED AND PUBLISHED"* as its verdict. Worse, the plan's stated recovery (*"do not hand-edit the clone — re-run `publish_wiki.sh`, it is idempotent"*) is **inert** in exactly that state: the re-run finds the content already committed locally, short-circuits at *"no changes to publish"*, exits 0, and never retries the push. The real recovery — `git -C <clone> push origin master` — appears nowhere in the plan. Two commands close it: `git push --dry-run` **before** the commit, and `[ "$(git -C <clone> rev-list --count origin/master..master)" = 0 ]` plus a `curl` of the live page **after**. | Session 232 (rename Phase 3; found by an adversarial pre-publish review, converged on independently by 3 of 5 lenses, then reproduced against the script) | Writing or inheriting any verification block for an action whose success means *"it reached a remote"* — publish, deploy, push, upload, notify. Ask what the block prints when the local half succeeded and the remote half did not. |
| 130 | **When a plan says a thing was "confirmed in Phase N", open Phase N's actual command and check it exercises the same OPERATION the claim is about.** `repository-rename.md` §1 flagged that git-push continuity over the wiki clone's stale `claims-model-starter.wiki.git` origin was *"documented but not measured … confirm it during Phase 1"*. Phase 1's verification block ran `git fetch` — which proves the redirect resolves and **read** auth works, and proves nothing about **write**. The gap survived two phases and would have been discovered, if at all, by a failed push during the one commit in the plan that publishes to the public. Measured instead, up front: `git push --dry-run origin master` → *"Everything up-to-date"*, exit 0. One second, and the assumption became a measurement before the irreversible-ish act rather than during it. Note the shape — nobody skipped a step; Phase 1 *did* run a verification. It ran a **weaker verb** than the claim needed, and the claim's wording ("confirm it") did not name the verb. | Session 232 (rename Phase 3; the clone's last push predated the rename by 19 days) | Whenever you inherit a "verified/confirmed/measured in Phase N" claim and are about to depend on it. Read the command, not the claim. Match the verb: `fetch` is not `push`, `HEAD` is not `origin/HEAD`, a 200 is not a deploy. |
| 131 | **A truncated quotation can drop the qualifier that makes the quoted claim true — quote to the end of the clause.** `repository-rename.md`'s Phase 4 instruction reads: *(`Evolution.md:266` also says "22 outward-facing wiki pages"; there are 25. Fix the count while you are in the line.)* The page actually says **"22 outward-facing wiki pages plus the sidebar"** — the quotation stops one clause early, and that clause is the whole convention. `git ls-tree` at the commit that wrote the 22 shows **23** files including `_Sidebar.md`, so 22 was exactly right when written; two pages have been added since, making the correct value **24**, not 25. The plan's other 25s are a different quantity (total files, sidebar included), so the instruction silently changes the sentence's counting convention while claiming to fix its arithmetic — and an executor obeying it lands off by one instead of off by two. | Session 232 (rename Phase 3; filed for the Phase 4 executor, not fixed here — `docs/planning/` is outside Phase 3's blast radius) | Any time you write an instruction of the form *"it says X; the truth is Y"*. Paste the whole sentence, then reason. And when a count disagrees with a document, suspect the counting convention before the count. |
| 132 | **Drive a phase from the plan's CLASSIFICATION section, not from the phase's own checklist — and diff the two before you commit.** `repository-rename.md` assigns `BACKLOG.md:74` to Phase 4 **twice**: §3.2(e) (*"Exactly one is a genuine substitution: `BACKLOG.md:74` … which moves with the directory in Phase 4"*) and Phase 5's checklist (*"it should already have been done in Phase 4 — verify, do not assume"*). Phase 4's own step-2 bullet list — the thing an executor actually works from — **omits it**. An executor who trusts the checklist ships a commit whose `git mv` turns two live path citations into 404s inside an *unrelated* backlog item, and **nothing can ever see it**: §7.2's allowlist exempts `^BACKLOG\.md$` wholesale (group 2), and Phase 4's own residue grep covers only `scripts/ tests/ .githooks/`. The only backstop the plan has is a human "verify, do not assume" in a later session. The tell is structural and cheap to check: a plan that classifies every hit in §3 and then re-enumerates the change set per phase carries **two lists that can disagree**, and the classification is the one derived from measurement. | Session 233 (rename Phase 4; found by an adversarial pre-commit review, reached independently by 2 of 5 lenses, then confirmed against both plan sections) | Executing any phase of a plan that has both a global classification/inventory section and per-phase checklists. Grep the classification for your phase's name and reconcile it against the checklist *before* staging. Where they disagree, the classification wins. |
| 133 | **An end-to-end observation can be structurally blind to the exact failure it was built to catch — check whether the event you are observing is reachable both ways.** Phase 4's headline proof is *"the hook fired and published, therefore the trigger is correct."* It is not: `git diff-tree` (no `-M`) lists a rename as **both** the deleted old path and the added new path — verified here against `e1bf7c2` — so on the rename commit the hook fires whether `:18`'s prefix is the new name **or the stale old one**. The plan knows this and says so, and its remedy is *"read the trigger literal back out of the file"* — but reading a literal back is an eyeball, not an assertion. Extracting the pattern from the file and **running it** closes the gap for the cost of three lines: `PAT=$(sed -n '18p' .githooks/post-commit | sed "s/.*grep -q '\([^']*\)'.*/\1/")`, then assert it matches a future-shaped path (`docs/wiki/model_project_constructor/Home.md`), and — the half that catches a *stale* prefix, which the positive test alone cannot — assert it does **not** match `SESSION_NOTES.md` or the old path. A test with no negative control cannot distinguish "correct" from "matches everything". | Session 233 (rename Phase 4; the hyphen typo dragon 4 warns of *is* caught by the fire/no-fire observation, a stale prefix is not — the two failure modes have opposite observability) | Any time a verification is a single end-to-end observation of a success event. Ask which *other* states also produce that event, and add the negative controls that separate them. |
| 134 | **A defect filed against a plan's INSTRUCTION does not suspend the instruction — check whether what was filed is the work or a correction to the work.** Session 232's handoff gotcha 7 read: *"`Evolution.md:266` is `24`, not the plan's `25`. Filed in `BACKLOG.md`; do not paste the plan's number."* Session 233 read *"filed"* as *"deferred — leave the line alone"* and shipped the path substitution without the count fix. Both disambiguators were one grep away and both said execute: the plan's §3.2(c) ends **"Fix the count while you are in the line"**, and the filed defect's own text presumes execution (*"an executor obeying the line as written lands off by one instead of off by two"*) — you cannot land off by one unless you act. The general shape: a backlog entry can be a *work item* (someone must do this later) or an *erratum* (when you do the thing you were already going to do, use this value instead). They read almost identically in a handoff, and only the erratum is safe to postpone by doing nothing — because doing nothing is exactly what it does not authorise. | Session 233 (rename Phase 4; the omission was caught by pre-commit review, not by the executor, and the two 24-vs-25 numbers in the review's own findings had to be re-measured — `ls … \| grep -vc '^_'` → 24 — rather than copied from either source) | Reading any handoff gotcha or backlog item of the form *"X is wrong; filed"*. Ask: filed as work to be done, or filed as a correction to work I am about to do? If the underlying instruction still stands anywhere in the plan, it stands. |
| 135 | **An allowlist exemption is only as fine-grained as the assertion traded for it — exempt a FILE and every live line inside it rides in free.** `repository-rename.md` §7.2 exempts `docs/architecture-history/**` and `CHANGELOG.md` wholesale, on §3.1's rationale that they are frozen historical records. True of their *entries*; false of the archive **banner** and the CHANGELOG **preamble**, which record nothing and navigate — *"For current state, see `docs/wiki/claims-model-starter/Evolution.md`"*. Phase 4 updated the banner's canonical template (`PROJECT_CONVENTIONS.md:44`) and **none of the 21 copies**, so 21 lines across 20 files plus 2 more in `CHANGELOG.md`'s preamble now point at a deleted directory, and no §7 check could ever print one. §7.2's own rubric — *"an exemption is only as good as the assertion traded for it"* — was right and was applied only to group 3, which got three **line-level** assertions; group 1 got a **file-level** one. The blindfold reappeared one level below where the plan looked for it. | Session 234 (rename Phase 5 residue sweep) | Whenever a completion check exempts a path prefix or a filename. Ask what granularity the paired assertion runs at, and whether the exempt file contains any line that is live rather than a record — a banner, a preamble, a "see also", a header. Exempt lines, not files, wherever you can. |
| 136 | **A completion criterion built on `grep -r <dir>` is environment-dependent and cannot carry a "no judgment call" rule; scope it to the index with `git grep`.** `repository-rename.md` §7.3's residue check greps six directories. Measured in Session 234: on a shell where `grep` is a `ugrep --ignore-files` wrapper (it honours `.gitignore`) it prints **empty**; the identical command as `command grep` prints **two** `tests/__pycache__/*.pyc` blobs. Same tree, same command, opposite verdicts — and §7.2's rule is that a printed path *is* a miss with no judgment available to excuse it. Directory greps reach untracked and ignored files, so their result depends on the grep binary AND on build state. `git grep` is index-scoped and deterministic, and still catches the future addition the wider grep was defending against, because an addition would be tracked. | Session 234 (rename Phase 5, §7.3 verification) | When writing or running any grep-based completion criterion. Also: before trusting an "empty" from a search tool, run `type grep` — the wrapper is invisible and silent. Report which binary produced the verdict. |
| 137 | **Deleting a work item deletes every pointer INTO it — sweep for inbound references before the delete, not after.** Phase 5's charter is *"delete the rename item's rows; do not substitute them."* Done naively that strands five sites in `repository-rename.md` alone, and **three of them route a reader to an open operator decision**. The sharpest was dragon 6's citation of `BACKLOG.md`'s *"The name form is underscores, and it is deliberate"* paragraph — the operator's ruling existed **nowhere else in the plan**, so the delete would have destroyed the only record of a decision the plan tells future readers not to reverse. Two open flags needed re-filing as their own items for the same reason: an open decision must not die because of where it happened to be filed. | Session 234 (rename Phase 5, BACKLOG closure) | Before deleting any section, item, or file: `git grep` for its name, its path, and its distinctive phrases, and classify every inbound hit as *dies with it* or *must be preserved*. Preserving is not the same as substituting — inline the substance where the pointer was, or re-file it as its own item. **Do the sweep with `git grep`, not from a list you assembled by reading**: this session's first pass worked from a list, found five, declared "all five", and the pre-commit review found a sixth (§2.3's pattern-6 row, whose own `grep` now returns nothing). The learning was violated in the commit that wrote it. |
| 138 | **A finding that contradicts the plan's own CLASSIFICATION is a plan defect, not residue — file it, do not sweep it in the closing commit.** Phase 5's rule says anything that is not §3.1, §3.3 or a deliberate self-reference is a miss to fix here. The 21 dead banner pointers *classify into §3.1*, so by the letter they are not misses; the real claim is that §3.1's classification is wrong for that one sentence. Sweeping them would have meant an executor overruling the plan's classification, in the closing commit, without the operator, across 20 frozen archive files — against `SAFEGUARDS.md`'s blast-radius rule and against this project's settled answer to a mid-execution plan defect (Session 229 filed → Session 230 got a dedicated repair session). The filing carries the full measurement so the fix is minutes once ruled on. | Session 234 (rename Phase 5) | When a sweep turns up something that looks like a miss: first ask which bucket the plan's own classification puts it in. If the plan says KEEP and you disagree, you have found a defect in the plan, and the deliverable is a filing plus the measurement — not a wider commit. |
| 139 | **A check that cannot produce a TRUE positive is not a safety net — narrow it, and prove the redundancy with a sibling assertion rather than an allowlist.** `L2/b3` scanned the shard's banner *and* its records for leaked front-matter lines. The records half is dead by construction: `L1` pins those bytes by exact concatenation, so anything inserted there trips L1 first, and a front-matter line found among them was already there before the cut. On this trim it false-positived for exactly that reason. **The test to apply is: does a sibling assertion already pin this region at FINER granularity? If yes, the check is redundant there and narrowing is the fix; if no, it is load-bearing and the collision is the artifact's fault.** An allowlist was rejected because it postpones the false positive without ever making a true one possible. An exact-line predicate was *measured* (green on this cut, 0 verbatim collisions today) and rejected on the same ground. | Session 235 | Whenever an inherited assertion fires on work you believe is correct. Do not tighten the predicate because it just went red — that is calibrating to noise (#82). Ask what else already pins the region. |
| 140 | **Every number inside a write-once artifact must be re-measured against a command in the commit that freezes it — after that it is unrepairable, and no assertion can ever catch it.** The fifth trim's shard banner said the self-test *"mutates the artifacts sixty-one ways"*; the suite had 59, residue from a draft. `L6` pins the banner byte-for-byte against a `BANNER` literal carrying the *same* wrong word, so the two agreed and every assertion stayed green; `L7` would have frozen it permanently one commit later. A pre-commit review caught it by comparing the banner's count word against `--self-test`'s own output, and proved the trap by cloning, committing and watching L7 go red on the repair. | Session 235 | Before any commit that creates a frozen/write-once file. Grep the artifact for every numeral and count word and re-derive each from a command. A number that two files agree on is not verified — it is duplicated. |
| 141 | **Run the neuter loop per ARM, not per assertion, and publish the census in three buckets — naming the arms no mutant reaches beats inventing contrived ones.** Neutering a whole assertion cannot tell you that one of its `if`s has never fired. All 34 arms of the fifth trim's proof were neutered individually: **14** have a uniquely-catching mutant, **7** crash when neutered (read a traceback as *caught*), and **13** have none. The 13 are listed with reasons, and three of them are arms the fourth trim shipped with no mutant and no explanation. Two are unreachable *by construction* (`L0/empty` needs a grammar break, not an artifact mutation; `L5/2`'s final clause is subsumed because `L5/3` pins both clauses forming that junction) — writing that down is worth more than a fake mutant. | Session 235 | When authoring or reviewing any hand-built proof with a `--self-test`. A green self-test whose mutants never exercise your new assertion is the same lie as a green run — and so is a coverage line that claims an arm is uniquely caught when neutering it changes nothing. |
| 142 | **A mutant meant to isolate one assertion must be scoped to the smallest text that expresses the mutation — measure its blast radius, never assume it.** `M52` was written to reach `L5/4`'s unreadable arm by typo'ing an ancestor shard's filename. Done as a whole-text replace it also rewrote the archived *records* (moving bytes → `L1`, `L3`) and named **nine** assertions. Narrowed to the single routing clause it names **one**. The same run exposed a second defect it had been masking: `L8`'s derived half closed over the module-level `ROUTING` instead of the declaration a mutant had moved, so it fired for the wrong reason. | Session 235 | Whenever you add a mutant claiming to isolate an assertion. Print the assertion list the mutant actually trips. If it names more than the one you targeted, the mutant is too broad or another assertion is reading the wrong operand — both are findings. |
| 143 | **An ordinal inside a lineage ("the third trim", "the fourth's proof") is a citation and must be greped like one.** The fifth trim's header attributed both historical unreachable-arm defects one trim too late — `L2/b0` belongs to the SECOND trim (Session 224) and `L5/2`'s partition arm to the THIRD (Session 228) — contradicting its own ancestor proof and `CLAUDE.md` in the paragraph every future trim copies forward. Each proof self-declares its ordinal on line 4 (`# Generated by Session N (the Nth trim).`), and the defect claims are re-runnable: `bash <ancestor> --self-test \| command grep -c 'L5/2'` returns 0 for the third trim and non-zero for the fourth. | Session 235 | Any time you write "the Nth trim/phase/session did X" about work you did not do. Ordinals feel like prose and behave like citations; this project has now shipped fabricated or misattributed citations in two consecutive sessions (#137's shape, Session 234's three). |
| 144 | **A fact can be recorded six times and still never reach the item that depends on it — a handoff carries conclusions, and a mechanism nobody draws a conclusion from is not institutional memory.** `publish-tutorial.yml`'s path filter is single-level (**GitHub Actions globs are non-recursive — `*` does not cross `/`**), so nested `docs/` changes are *structurally incapable* of firing it, and line 11 of the same block declares `workflow_dispatch`. **Every one of those facts was already written down:** `enterprise-migration.md:227` has read *"`docs/*.md` is non-recursive"* since Session 182; `git grep -c 'single-level\|single level' 98abb83 -- SESSION_NOTES.md` → **7** across Sessions 228-233; and `workflow_dispatch`'s purpose — *"so the operator can re-deploy without a code change"* — is in `CHANGELOG.md:910` from **Session 67**. Yet Session 235's handoff called the unstyled-tutorial bug *"the one item that **needs** a push to exercise,"* and `BACKLOG.md` costed it that way in two places. The facts and the conclusion lived in different files and never met. **Session 236's first draft of this learning then claimed the mechanism was "a mechanism nobody had read" — an adversarial review reproduced all six prior records and refuted it**, which is the same defect one level up: crediting yourself with a discovery is what happens when you check whether a fact is *actionable* but not whether it is *known*. | Session 236 (pre-push audit; headline overstated in draft, corrected by review before commit) | Two uses. (a) When a handoff, plan or backlog item asserts something *usually*, *historically* or *never* happens, or costs an item as blocked — `git grep` the mechanism before believing the consequence, and fix the consequence **everywhere it is costed**, not just where you found it. (b) Before writing "nobody had noticed X" in any record: grep for X across the live files, the shards, the plans and `CHANGELOG.md`. In a project with 236 sessions of notes, the prior is that someone did. |
| 145 | **"It did not fire" is evidence only once you have separately proved the mechanism was armed and correctly aimed — otherwise you are reading a disabled observer as a clean result.** Three sessions recorded the wiki publish hook's silence as a negative control (S235 gotcha 9: *"If it fires, the trigger prefix has drifted"*). Silence is equally consistent with `core.hooksPath` unset, a non-executable hook, or a prefix that matches nothing. Arming is three commands and all three must pass: `git config core.hooksPath` → `.githooks`, the hook file present and `+x`, and the trigger literal read out of the file (`^docs/wiki/model_project_constructor/` — the **post-rename** path, which is the half a fire/no-fire observation cannot see, cf. [#133](PROJECT_LEARNINGS.md)). **The trap that nearly inverted this:** the first check appeared to show `core.hooksPath` unset, because it ran inside a compound command whose earlier `cd` into the wiki clone was still in effect — a `cd` in a `;`-chain relocates every later command in that chain, and `git config` exits 1 on a missing key, so the wrong answer arrived as a plausible-looking blank. | Session 236 | Before recording any "the guard correctly stayed silent" result. Prove armed, prove aimed, prove silent — in that order. And when a repository-scoped command returns a surprising negative, verify which directory it actually ran in before believing it. |
| 146 | **The proof lineage guards the archive and four prose files; nothing guards the session record's own numbers — and that is precisely where the last three sessions' defects have landed.** The trim proofs carry 12 assertions over the shards, and **L8** exists specifically for count words no assertion reads — but its `REACH` names `README.md`, `PROJECT_CONVENTIONS.md`, `BACKLOG.md` and `CLAUDE.md`, **not `SESSION_NOTES.md` itself**. So the one file every session writes most is the one file no proof inspects. Measured: every self-reported defect of the last three sessions is a numeral or citation in prose — S235's banner *"sixty-one"* (59), two misattributed trim ordinals, a stale mutant range, and the one found here: its record's *"1,378 lines"* for a proof that has only ever had one blob at **1,441**. That row's six sibling numbers all re-measured exact (976, 58, 1,761→901, 7 files), so it was a lone outlier, not a convention — and `git diff --stat` for the same commit printed **1441** three lines away. The correct value was already in the commit. | Session 236 (pre-push audit, `claims` lens; confirmed by independent re-measurement and by an adversarial verifier that could not refute it) | Two uses. (a) Before any commit that publishes a record: grep your own record for numerals and re-derive each from a command — the value is usually already in `git diff --stat`. (b) At the sixth trim: consider extending **L8**'s `REACH` to the live record's own change table, which is the only unguarded prose surface left in the lineage. |
| 147 | **`A..B` excludes `A` — an off-by-one git range in a review brief silently narrows the review, and only the reviewers can catch it.** Session 236's pre-push audit brief said *"7 commits are unpushed: `a707a9e..HEAD`"*. The count was right; the range names **6**, omitting `a707a9e` itself. Two independent lenses flagged it and re-derived the true set from `origin/master..HEAD` before scanning, so nothing went unreviewed — but that was the reviewers' discipline, not the brief's. A brief that states a range *and* a count is self-checking for free: `git rev-list --count <range>` must equal the count, and disagreement means the notation is wrong, not the count. | Session 236 | Writing any review, audit or diff brief that names a commit range. State the range and the expected count, then run `git rev-list --count` on your own notation before sending it. Use `A~1..B` or `origin/<branch>..HEAD` when you mean *inclusive of A*. |
| 148 | **Inverting a filter to fail-closed is only safe once you enumerate the SET it is applied to — a name like `exclude_docs` describes the intent, not the domain.** `b27cc98` (Session 182, enterprise-migration Phase A1) turned `mkdocs.yml`'s `exclude_docs` from a five-line denylist into an allowlist beginning `/*`, to stop internal `docs/` subdirectories being published. Correct instinct, and it silently deleted 43 files it was never aimed at: MkDocs calls `add_files_from_theme()` at `mkdocs/commands/build.py:289` and then **re-runs `set_exclusions()` at :294**, so theme-supplied files — which arrive with `InclusionLevel.UNDEFINED` — are filtered by the same patterns. `/*` is gitignore-anchored at the root, so it matches the Material theme's entire `assets/` tree. The theme contributes **exactly 43 files, all under `assets/`** (measured by calling `add_files_from_theme` directly and diffing the file set), matching the 43 Session 234's bisect found missing. `!/assets/` restores them and keeps every `docs/` subdirectory excluded (verified against `pathspec.GitIgnoreSpec` per-path, not by re-reading the docs). | Session 237 (root cause read out of MkDocs' own source; hypothesis inherited from Session 234) | Any time you make a filter, allowlist, or glob fail-closed. Do not reason from the option's name or its documentation — find where the framework applies it and list every source of items that reaches it. A fail-closed pattern is a *wider* blast radius than the denylist it replaced, not a narrower one. |
| 149 | **When the publish step is irreversible, the assertion that matters runs BEFORE it — an assertion after can only report the damage.** `mkdocs gh-deploy --force --clean` writes a **parentless** commit to `gh-pages` (measured: `git rev-list --parents -n1 origin/gh-pages` prints one field — **zero parents**), so there is no git revert path; `enterprise-migration.md` §2.3 says so and this session confirmed it live. `BACKLOG.md` prescribed *"a post-deploy assertion that the stylesheet the built `index.html` links returns 200"* — right about the symptom, wrong about the placement, and following it literally would have shipped a guard that watches the wreck. The build check now runs between `mkdocs build` and `gh-deploy`, where a bad artifact is still only a failed job. **Keep both anyway: they answer different questions.** The pre-deploy check proves the *artifact* is complete; the live check proves the *deployment* worked, which fails differently (ghp-import dropping files, a missing `.nojekyll`, Pages wired to the wrong branch, a stale CDN). | Session 237 | Adding verification around any irreversible or destructive publish — `gh-deploy`, `rsync --delete`, a force-push, a parentless commit, a release upload. Ask where the check has to sit to *prevent* rather than *announce*, and capture a recovery point before the first run regardless. |
| 150 | **An assertion against an asynchronously-published artifact must poll, or its flakiness will get it deleted — and the naive version fails on the very first correct deploy.** The new post-deploy check polls until every URL returns 200. It earned that on its first live run: **attempts 1 and 2 reported `2 of 3 URL(s) not yet 200`** while GitHub Pages was still building, and the third succeeded, all inside 21 seconds. A single-shot `curl` — the obvious reading of the backlog's wording — would have gone red on a deploy that was entirely correct, and the cheapest repair for a check that cries wolf is to remove it. The failure mode is social, not technical: **a guard that flakes gets deleted, so a guard that cannot tolerate the system's real latency is worse than no guard at all.** | Session 237 | Writing any check that reads a system it just wrote to, where publication is asynchronous — CDN, Pages, DNS, a queue, an index, a cache. Poll with a deadline and print progress. Then look at the first real run's output and confirm the retries actually happened; if they never do, the timeout is untested. |
| 151 | **A test that HANGS instead of failing is a worse outcome than a red test, and a mocked-out default argument is a standard cause.** `check(..., fetch=probe)` bound `probe` at function-definition time, so `monkeypatch.setattr(mod, "probe", ...)` did not reach it and the call fell through to the real network with the production 300s/10s polling defaults. The whole test file timed out at two minutes with no failure to read. The fix is to resolve the collaborator at call time (`fetch = probe if fetch is None else fetch`), but the *second* fix is the one worth copying: the regression guard passes an explicit `--timeout 0`, so if anyone reinstates the default binding the test **fails in milliseconds against the real host** rather than reproducing the hang. A guard that reproduces the original symptom is not a guard. | Session 237 (both defects in this session's own new code, found by its own tests before commit) | Writing any function whose collaborator is meant to be patchable, and writing the regression test for a timeout/retry bug. Default arguments capture the object, not the name. And when a test suite hangs, suspect that a patch silently missed rather than that the code is slow. |
| 152 | **Diff what you are about to publish against what is in production BEFORE predicting the effect — the fix may live entirely in what is missing.** Ahead of the deploy, `site/index.html` was measured **byte-identical** to the live page: same 12,533 bytes, same sha256 `b97eddcd…`. That turned the natural prediction (*"the published page will change"*) into its opposite and correct form — *"the page must NOT change; only the two 404s flip to 200"* — which is a far stronger claim, because an index that changed would have meant the fix had touched something it had no business touching. Confirmed after: index sha256 unchanged, `gh-pages` 7 files → **50** (43 of them `assets/`), both assets 200 at 139,849 and 114,308 bytes. Session 236's discipline of writing predictions down before an irreversible act only pays if the predictions are derived from measurement; guessed ones just record a guess. | Session 237 | Before any deploy, migration or regeneration where you intend to state what will change. Diff the candidate artifact against production first. The measurement costs one command and converts a plausible prediction into a falsifiable one — including, sometimes, into the opposite prediction. |
| 153 | **A NEW assertion can silently retire an OLD one's only mutant, and only an arm-level sweep of the WHOLE file will tell you.** Adding L12 (size figures held against a measurement) to the sixth trim's proof stole `L2/b3`'s sole mutant: the inherited M51 APPENDS a front-matter line to the shard's banner, which under L12 also changes the shard's line count, so `L12/shard_total` caught it and b3 — an assertion the fifth trim had deliberately narrowed — was left reachable by nothing. It was still green. It was still in the coverage list. It proved nothing. Measured by neutering one `if` at a time across all 42 arms, not by reasoning; the fix was to make M51 REPLACE a banner line instead of appending, keeping the line count identical. **The same sweep found a second one in the new assertion itself** (`L12/cap`, whose naive mutant also tripped `L12/prose`). | Session 239 | Whenever you ADD an assertion to an existing proof or checker. Re-run the arm-level sweep over **every** arm, including the ones you did not touch — a new check's reach is exactly what makes an old check redundant, and redundant-but-green is indistinguishable from working. Budget the sweep, not just the new mutants. |
| 154 | **A count you type about your own artifact is wrong often enough to be worth a machine check — and the check catches you writing it.** Six trims of this lineage stated their own size figures in prose ("4 record headings, 918 lines", "At 976 lines it sits under the 2,000-line cap", "(976 lines)") and **not one was ever compared to the artifact**: every occurrence lived inside a string literal pinned only against a declaration the same author wrote. All of them happened to be correct. L12 now derives them. Its very first green run printed `10 prose claims` three lines below a header that said *nine* — the author's own typed count, wrong, caught by the assertion he was writing to catch exactly that. | Session 239 (the sixth consecutive session whose self-reported defect is a typed numeral — see #105, #146, #148, #152) | Any document that states a figure about a file: line counts, record counts, item counts, mutant counts. If a script already reads the file, have it derive the figure and assert the prose. Prefer that to proofreading, which is what failed five times. |
| 155 | **A sweep that returns nothing is a RESULT — write down what you excluded and why, or the next session re-derives it and may get it wrong.** `CLAUDE.md` orders each trim to re-derive the copy list with `git grep -l 'SESSION_NOTES-'` rather than inherit it. Run at the sixth trim it returned **four files no earlier sweep had named** — and the correct action for all four was to declare **none** of them: `CHANGELOG.md` and `evolution-page-plan.md` match only the phrase `SESSION_NOTES-as-rationale`, which is not a filename (the sweep STRING over-matches; `git grep -l 'SESSION_NOTES-[A-Za-z0-9-]*\.md'` drops both), while `PROJECT_LEARNINGS.md` and `repository-rename.md` cite shard names inside frozen historical statements that state no census — and `L8/set` requires a declared file to name the WHOLE shard set, so declaring either would have turned a **correct** record red. | Session 239 | After running any "sweep for the class" instruction. Record the negative result and the exclusion reasoning in the artifact, not just in the session notes — otherwise the next run reads four unfamiliar hits and either re-analyses them or, worse, 'fixes' a frozen record that was right. |
| 156 | **A floor expressed in records is not a floor expressed in context, and a cut can satisfy the letter while gutting the spirit.** `CLAUDE.md`'s retention rule says *never retain fewer than 4 sessions* and the declared grammar says *a record is a byte span, never a session*. At the sixth trim those diverged for the first time: the four retained records were Sessions 239, 238, 237 and 236, of which **238 was an abandoned 19-line claim and 239 was an 11-line stub** — so the letter said four and the working context said two. It was still the right cut (retaining a fifth record would have missed the 1,050-line target and bought only one session of headroom instead of two), but it was right by measurement, not by the rule. | Session 239 | At any trim, and when reading the floor. Measure what the retained records actually CONTAIN before trusting the count; if a cut would leave only stubs plus one real record, the floor is not doing its job and the numbers need re-deriving rather than re-applying. |
| 157 | **A session that claims and then vanishes should be annotated, not overwritten — the stub is the evidence Phase 1B exists to leave.** Session 238 claimed a deliverable, committed only its Phase 1B stub, never pushed, and produced nothing. Session 239 claimed a NEW number and annotated 238's record as ABANDONED with the measurement that proves it (`git log --oneline 61aee5b..HEAD` printed exactly one line; `git rev-parse HEAD` still `e52e88e`; `git status` clean) rather than reusing the heading. Reusing it would have destroyed the only trace that the ghost happened — the precise outcome FM #14 added the stub to prevent. | Session 239 | Whenever Phase 0 finds a claimed-but-empty session. Claim a new number, annotate the abandoned stub with the git measurement that shows nothing was lost, and say explicitly whether anything needs recovering. |
| 158 | **A grep criterion that has gone unsatisfiable is repaired by SPLITTING it into arms, not by narrowing it.** The C4/C5 clone-independence check greps for four identity strings and expects 0. The rename made the string it most needed to catch stop matching, and both proposed repairs — add the new name as a fifth alternative (**1,103 lines / 176 files**), or scope by directory (**357 lines, 269 of them legitimate `from …` imports**) — were unsatisfiable, because the project's own name is simultaneously the import package and a foreign-identity leak. Narrowing the pattern loses coverage; widening the path loses satisfiability. The shape that works is **two arms with different scopes and different meanings**: a wide arm for identity that must be absent *everywhere*, and a narrow arm for identity that is legitimate almost everywhere and *always* a defect in a named handful of files. | Session 240 | Whenever a `→ 0` acceptance criterion becomes unsatisfiable after a rename or a package move. Ask which of the matched strings are legitimate *somewhere* — those need a scoped arm of their own, not deletion from the pattern. |
| 159 | **Validate a new acceptance criterion against the live instance of the defect it is meant to catch, BEFORE writing it into the plan.** Arm 2 of the restated criterion was run over this repository first and returned `claims-model-starter` from `scripts/publish_wiki.sh:19`, `:24`, `:42` — exactly the case the criterion's own comment names and exactly the case the old form had gone silent on. A criterion that has never been shown to *fire* is an assertion nobody has falsified; it can be green because it is satisfied or green because it is blind, and nothing in the output distinguishes those. This is the shard proofs' `--self-test` discipline applied to a plan's verify block instead of to a script. | Session 240 | Before committing any new verification command to a plan or proof. Find a real positive, run the check, watch it fail. If you cannot construct a positive, the check may be unreachable — that is the finding. |
| 160 | **`zsh` does not word-split unquoted parameter expansions, so `-- $FILES` in a verification command matches nothing and PASSES VACUOUSLY.** Found while validating the restated criterion: `git grep … -- $FOUR` printed nothing and read as a clean result; the real cause was zsh passing the whole list as a single pathspec matching no file. In any criterion whose success condition is *"no output"*, a vacuous pass and a real pass are byte-identical. The operator's shell is zsh, so this is not a hypothetical. Pass paths **literally**, or `set --` them into `"$@"`. | Session 240 | Writing or running any `→ 0` shell criterion in this project. Also the general rule: when a check's PASS is "empty output", prove the command reached its inputs before believing it. |
| 161 | **An `⚠ OPERATOR DECISION` item is disposed of by presenting re-derived measurements and the exact question — not by asking whether to proceed.** These two items had been skipped by three consecutive sessions (237 deferred them, 238 claimed and abandoned them, 239 listed them again). They were both ruled inside **one** exchange once the numbers were on the table, re-derived rather than quoted: the filed figures had moved by ~880 lines because trims 4-6 relocated session history into the criterion's own excluded directory. The blocker was never the operator's availability; it was that nobody had put a decidable question in front of them. | Session 240 | Any backlog row marked `⚠ OPERATOR DECISION`. Re-derive every filed figure first (the items say so), state what each candidate answer costs, name the question in one sentence, and stop. Do not ask "shall I proceed?" — ask the question the item exists to answer. |
| 162 | **Before filing a failing check as a defect, confirm the project actually runs that check.** `mkdocs build --strict` fails at `HEAD`, and it was one keystroke from being filed as a new backlog item. Session 237's gotcha 5 records that `--strict` is *deliberately* not used here: `docs/index.md` is a meta-refresh redirect, `mkdocs.yml` sets `validation.links.unrecognized_links: warn` on purpose, and an asset guard is the substitute. The failure was reproduced identically at the pre-session commit in a throwaway worktree, so it was neither caused by the session nor news. A false backlog item costs more than the check saved. | Session 240 | Whenever a command you chose — not one the project's CI or scripts choose — reports a failure. Reproduce it at the pre-session commit, then grep the handoffs and `mkdocs.yml`/workflow files for evidence the project opted out of it deliberately. |
| 163 | **A filed fix that names its own mechanism must be checked for SATISFIABILITY before it is implemented.** The item said *"count `*.md` under `SOURCE_DIR` and abort below a floor."* No static floor works: set it to 1 and it catches only a totally empty source, set it to today's 25 and it goes stale the next time a page is added — and the item's own text names the *half-populated* case a floor cannot see. The guard that works measures the damage instead: an `rsync --dry-run` pass counts what `--delete` would remove and compares it against what the clone publishes now. Implementing the filed sentence literally would have shipped a guard that looks like protection and is not. | Session 241 (`publish_wiki.sh` empty-source wipe) | Whenever a backlog item prescribes a mechanism, not just an outcome. Implement the outcome; verify the prescribed mechanism can actually deliver it, and say so in the notes when it cannot. |
| 164 | **Three `git` defaults that make a path-matching hook silently wrong, all measured this session.** (a) `git diff-tree --no-commit-id --name-only -r HEAD` prints **nothing** for a merge commit and nothing for the root commit without `--root`. (b) **`-m --first-parent` is not the fix** — `diff-tree` ignores `--first-parent` when `-m` is given and emits the *union* of the diffs against every parent; `--diff-merges=first-parent` is the flag that restricts it. (c) `--name-only` honours `core.quotePath`, so any path with a non-ASCII byte arrives C-quoted **and wrapped in double quotes** (`"docs/wiki/…/Caf\303\251.md"`), which defeats a `^prefix` anchor; `-z` piped through `tr '\0' '\n'` emits them raw. | Session 241 (`.githooks/post-commit`) | Any hook or script that decides something by matching paths out of `git diff-tree`/`diff --name-only`. Test with a merge, the root commit, and a non-ASCII filename before believing it. |
| 165 | **`git merge` runs `post-merge`, not `post-commit`.** A clean `git merge --no-ff` or `git pull` never invokes `post-commit` at all; `post-commit` sees a merge only when the user finishes a *conflicted* merge with `git commit`. So a merge-related fix installed in `post-commit` covers the minority path. Verified with both hooks installed as echo stubs — two minutes of measurement that changed the finding from "fixed" to "fixed for one of two paths, other path filed". | Session 241 (backlog item on merge blindness) | Before claiming a git-hook fix covers a workflow, install echo stubs for every plausible hook and run the workflow. Which hook fires is not guessable from the hook's name. |
| 166 | **Two ways a shell guard skips itself while `set -euo pipefail` is on.** (a) `cmd | grep -c X || true` masks **`cmd`'s** failure as a zero count, not just grep's zero-match exit — the `|| true` cannot tell them apart. (b) If the count ends up **empty**, `[ "$X" -gt 2 ]` fails as a *malformed test*, and because it sits inside an `if` condition `set -e` does not fire: the condition is simply false, the guard is skipped, and the script continues to exit 0. Both were live in this session's own first draft of a guard whose entire purpose was to close a fail-open. Fix: test the producer's exit status explicitly, and count in a `while`/`case` loop that cannot fail. | Session 241 (adversarial review of the new guard) | Every shell guard whose PASS condition is "a number came back small" or "no output". `set -e` protects far less inside an `if` condition than it looks like it does. |
| 167 | **When a background review's verifier agents die, the run's "N confirmed" is a floor, not a result — read the journal and adjudicate the dropped claims yourself.** An adversarial review returned `confirmed: 5, candidates: 27, report: null` after a network outage killed the synthesiser and 26 of 27 verifiers. The unverified findings were silently filtered out as unconfirmed. Reading `journal.jsonl` and testing the load-bearing claims by hand surfaced **four real code defects** among the dropped ones — every one of which would otherwise have shipped. | Session 241 (workflow `wf_ec9f7dc0-151`) | Any time a delegated review reports agent errors. The summary counts only what survived verification, and a verifier that died looks identical to a finding that was refuted. |
| 168 | **Mutation-test the tests, not just the code — and expect the first mutant to be vacuous.** A new 23-test suite looked exhaustive; an adversarial review showed the deletion guard's two arms were *algebraically identical* at the fixture's chosen size of 8 pages, so either arm could be deleted with the suite fully green. Separating them needed two fixture sizes (4 pages and 20), not another assertion. Of 15 mutants finally applied, the first `M13` was vacuous — it added a no-op flag instead of removing the check it named — and had to be rewritten before it bit. **A mutant that passes is a bug in the mutant until proven otherwise.** | Session 241 (`tests/scripts/test_wiki_publishing.py`) | Any session shipping a guard with a compound condition. Pick fixture values that make each conjunct decide the outcome *alone*, then delete each conjunct and confirm a named test reddens. |
| 169 | **A permanent pin expressed as a line number is not permanent, and the session that breaks it is the one editing the file the pin protects.** `CLAUDE.md`'s D-R5 rule pinned `publish_wiki.sh:19`, `:24`, `:42` *"permanently"*; this session grew that file by 66 lines and two of the three became `:29` and `:48`, leaving the rule protecting unrelated text — and the same three digits were also a re-runnable acceptance criterion in `repository-rename.md` §7.2, which then reported a false rename regression. Both restated by content (`git grep -n claims-model-starter …` → 3 hits; `grep -c … -> exactly 3`). **The gotcha that stopped me from "fixing" those three lines was itself the citation my change falsified.** | Session 241 (D-R5 pin, §7.2 criterion) | Before editing any file, `git grep` for citations of it *by line number* and fix them in the same commit. When writing a pin meant to be permanent, anchor it to content or to a count — never to a digit. |
| 170 | **A membership test over a whole document is defeated by your own quotation of the string it tests.** `L12/prose` declared the literal `holds the newest 4 sessions` for `CLAUDE.md`; the same commit's prose quoted that literal three lines below the load-bearing sentence, so editing the real sentence to a false value satisfied the assertion via the quotation and shipped green. Found by an adversarial review, reproduced in a clone. Two fixes, both needed: anchor the literal to something unique to the sentence that matters (`:** holds the newest 4 sessions`), and assert the declared literal occurs **exactly once** in the text it is read from. | Session 242 (adversarial review) | Any assertion that greps a document for a hand-declared string — especially when the same change writes prose ABOUT that string. Writing about your own guard can disarm it. |
| 171 | **A new arm and the arm beside it can eat each other's mutants; only a re-run arm sweep shows it.** Adding `if text.count(literal) != 1` after `if literal not in text` meant an absent literal has count 0, so neutering the FIRST arm let the SECOND catch its mutants — both arms lost their uniquely-catching mutants at once. Counting once and branching `n == 0` / `n > 1` restored both (23 → 25 uniquely-reachable arms, measured). The same shape nearly cost `L11/fire`, `/target` and `/floor` their mutants when `L11/figure` arrived, because those three moved an integer while leaving the rule sentence alone — which is precisely what the new arm objects to. | Session 242 | Immediately after adding any assertion arm next to an existing one. Re-run the per-arm neuter sweep; do not assume the old arm still has its own mutant. Two arms that catch each other's mutants are one arm wearing two names. |
| 172 | **Rewording the sentence a mutant targets silently turns the mutant into a no-op.** `M75` mutated the census word by `replace("seven", "eight")`; when the census sentence was later reworded to say *eight*, the replace matched nothing, the mutant became vacuous and SURVIVED. The self-test caught it. Fix: `assert` inside the fixture that the mutation actually changed the text, and that the word being moved is the one the code renders. | Session 242 | Whenever you edit prose that a mutant's fixture matches on. Every mutant fixture should assert its own mutation is non-empty — a mutant that passes is a bug in the mutant (learning #168), and rewording is the commonest way to create one. |
| 173 | **A count word copied forward from an ancestor is a fact about the ancestor, not about you — even inside a block that declares itself MEASURED.** The proof header said *"TWELVE ARMS HAVE NO UNIQUELY-CATCHING MUTANT"* three lines under correctly re-measured totals of 55 arms / 26 unique. The true figure was 29. It was inherited verbatim from the sixth trim, where it was already wrong (19). The enumeration below it named 26 arms in 11 bullets, so the number matched nothing at all. | Session 242 (adversarial review) | When copying any document or proof forward. Re-derive every numeral, and treat a `MEASURED` label on a block as a reason for more suspicion, not less — it is the numbers nobody re-runs that survive longest. |
| 174 | **A declared integer that is never compared to the sentence declaring it is anchored to nothing, and only a RELAXING mutant can prove it.** `L11` checked that `CLAUDE.md` still *contained* the three retention sentences but never parsed the numerals out of them, so a future trim could type `POLICY = (1500, 1400, 2, …)`, ship green, and print the invented target in its own run banner — while the fifth trim's header claimed *"IT CHECKS THE NUMBERS AGAINST THE SENTENCE THAT DECLARES THEM."* Undetectable by construction: every existing mutant TIGHTENED a bound, and tightening makes the current cut fail for the right reason. Fixed with `"{:,}".format(n) in sentence` plus one mutant that relaxes. | Session 242 (adversarial review) | Any check that enforces a policy number. Ask: what compares my constant to the document that declares it? Then ask the sharper question — do my mutants move that constant in the direction that would let a future author cheat, or only in the direction that fails today? |
| 175 | **Prose saying "every X" while the code iterates a hand-declared list is an overclaim, and the X you left out is where the bug goes.** Both the pointer block and the shard banner said L13 held *"every sentence stating which sessions moved"*; L13 iterated a seven-entry tuple. The eighth such sentence lived in a declared substitution's replacement text and took a WRONG SPAN green in a review's probe. Fixed by declaring the sentence, adding a mutant for it, stating the number in the prose, and checking that number against `len()` of the list. | Session 242 (adversarial review) | Whenever documentation describes what an assertion covers. Write the count, not the quantifier, and make the count itself derived — a quantifier cannot be checked, a number can. |
| 176 | **Splitting an inherited literal to give a new assertion its own mutant IS a narrowing — disclose it even when net reach is unchanged.** Three inherited literals were shortened so that moving a span tripped `L13` alone; net reach was genuinely unchanged (every byte is still required by `L8`, `L12`, `L13` or `L8/set`), but two shipped artifacts still carried the lineage's stock phrase *"none of them narrowed or weakened"*. A reviewer found the contradiction inside the proof's own header. The cost that is real: a session grepping for one of the old whole strings will not find it. | Session 242 (adversarial review) | When a new assertion needs a unique mutant and the cheapest route is shortening an older declaration. Do it — then say which strings moved and where their tails went, in the same commit. |
| 177 | **A prefix test on assertion codes silently matches its own successors.** The proof's report predicate `any(f.startswith("L1") for f in fails)` also matches `L10`, `L11`, `L12` and `L13`, so any of those failing printed *"L1 IS RED ON THIS RUN"* while `L1` was green — telling the reader that bytes a narrowed check no longer scans are unpinned when they are pinned. Present in the fifth and sixth trims' proofs, which are frozen and may not be repaired: `L10` holds them to their freeze commits, so correcting them there turns the newest proof red. Fixed forward as `startswith("L1:")`; reported for the ancestors. | Session 242 | Any string-prefix dispatch over identifiers that grow a digit (`L1`/`L10`, `v1`/`v10`, `arg`/`argv`). Anchor on the delimiter. And when the defect lives in a write-once file, fix it forward and report it — do not repair the frozen copy. |
| 178 | **An adversarial review of a hand-built proof outperforms the author's own sweeps, now measured twice.** Session 241's review found 4 real defects. Session 242 ran six lenses over a finished, green, self-tested trim: **15 defects, five of which shipped a WRONG artifact green** (a false retained-session count, a reverted trim-ordinal, two reverted `BACKLOG.md` census strings, and a wrong archived span). The author's own neuter loop AND per-arm sweep both passed clean beforehand. The lenses that paid were the ones aimed at prose-versus-enforcement, not at bytes: the byte-level lens found nothing. | Sessions 241-242 | Before committing any hand-built proof or guard. Point at least one lens at *what the prose claims the code does*, and one at *every count-carrying string the change had to hand-edit* — those two found 11 of the 15. Bytes are the part you already check. |
| 179 | **A deferral's PREMISE expires, and nobody re-derives it because the item reads as already-analysed.** The `uv.lock` / `paths:` item was carried by five sessions (237 filed it, 239/240/241/242 re-listed it) on one sentence: *"adding `uv.lock` would fire a public deploy on every dependency bump."* It is false, and one command shows it — root `pyproject.toml` was **already in the filter**, and **13 of 13** `uv.lock` commits in 496 also touched it, so every dependency bump already fired a deploy. Simulating both filters over the whole history: 45 firing commits before, 45 after, **0 newly firing**. The cost the item was deferred over was zero the whole time. What made it survive five readings is that a filed objection *looks* like a completed analysis, so each session re-listed the sentence instead of testing it. | Session 243 | Any item deferred more than once on a stated cost or risk. Before carrying it a third time, re-derive the number in its objection — not the item's scope, its **premise**. Learning #161 says put a decidable question in front of the operator; this is the step before that, and the reason the question was never decidable. |
| 180 | **An item that lives only in a handoff's what's-next list gets CARRIED; an item in the backlog gets DONE.** This one was filed as a session gotcha and a what's-next bullet, and was never in `BACKLOG.md` at all — `git grep -c 'uv.lock' -- BACKLOG.md` → 0 across six sessions. That is why it was re-typed five times and ruled on none: `BACKLOG.md` has an operator-facing plain-language index that is read aloud in Phase 0, and a what's-next bullet has no such audience. The two surfaces are not interchangeable, and the handoff is the weaker one precisely because it is rewritten every session. | Session 243 | When a session ends with an unresolved decision. Put it in `BACKLOG.md` with its index row, not only in the handoff — the handoff tells the *next* session, the backlog tells *every* session. If you find yourself copying a what's-next bullet forward verbatim, that is the signal it was never filed. |
| 181 | **A "this costs nothing" measurement must be shown capable of returning something, or it is a vacuous pass wearing a number.** The filter simulator reported 45 firing commits before the change and 45 after — the reassuring answer, and exactly what a simulator that silently ignored the new pattern would also print. It was only trustworthy because its sanity probes proved it could see a delta: a synthetic lock-only push returns `False` under the old filter and `True` under the new, and `docs/planning/foo.md` / `packages/data-agent/pyproject.toml` both correctly return `False` (non-recursive globs, root-anchored patterns). | Session 243 | Any before/after measurement whose finding is "no change". Construct an input that MUST move the number and confirm it does, in the same run. This is learning #159's discipline (validate a criterion against a live positive) applied to a measurement rather than to a check — and #160's vacuous-pass shape one level up. |
| 182 | **`git log -- <path>` hides commits by default, and the ones it hides are merges — where lockfiles land.** `git log --oneline -- uv.lock | wc -l` → **12**; `git log --full-history --oneline -- uv.lock | wc -l` → **13**. The hidden one is `ff04c02`, a branch landing. Default history simplification drops a merge whose result matches one parent, which is exactly the shape of a merge that carries a dependency change. My own Phase 2 count was off by one until a verifier re-ran it; the headline (0 lock-only commits) survived, the census did not. | Session 243 | Any time you count commits touching a file to size a risk or a frequency. Use `--full-history`, and pair it with `git diff-tree -m -r` so merges report their files at all (learning #164a: plain `diff-tree` prints nothing for a merge). A count used as evidence is worth the extra flag. |
| 183 | **A trigger without an authority flag is a half-truth: the file that FIRES the build need not be the file that GOVERNS it.** Adding `uv.lock` to `paths:` makes a lock-only bump start a deploy — but the job ran bare `uv sync --extra docs`, with neither `--locked` nor `--frozen`, so uv was free to **re-resolve** at deploy time and the lockfile governed the published theme only while it and `pyproject.toml` happened to agree. The trigger and the authority are separate mechanisms and adding only the first ships a sentence (*"uv.lock determines the deployed theme"*) that is conditionally false. `uv lock --check` exits 0 today, so `--locked` costs nothing now and converts a future drift from a silent re-resolve into a red job. | Session 243 | Whenever you add a file to a CI path filter, a cache key, or a change-detection gate. Ask the second question: does the build actually *obey* that file, or merely start when it moves? Lockfiles, `.tool-versions`, and pinned-digest manifests all have a strict-mode flag, and the trigger is worth much less without it. |
| 184 | **Describing a question is not asking one, and the operator will have to ask you what it was.** Session 243's close-out contained a paragraph headed *"An open question I am NOT deciding unilaterally"* which named the ambiguity, named the rule, named the precedent conflict and named what an amendment would cost — and **never wrote the interrogative sentence**. The operator's reply was *"you did not say what the question was. What was the question?"* This is learning #161's failure one turn later: #161 stops you asking *"shall I proceed?"* instead of a real question; #184 stops you *describing the territory of a decision* and calling that a question. The tell is a paragraph that could be read as a status report and as a request without changing a word. | Session 244 (the operator's correction of Session 243's close-out) | Any time you defer a decision to the operator. Write the sentence that ends in a question mark, then the candidate answers with what each costs. If you cannot phrase it as a question, you have not finished analysing it — and a reader who has to reconstruct the question will usually reconstruct a different one. |
| 185 | **When the written rule and the only precedent point opposite ways, suspect the rule has never been TESTED against that case — and measure the precedent before ruling.** §2's CHANGELOG cadence gate enumerates `src/`, `packages/`, `scripts/`, `tests/`; `.github/` is in none of them. But the only two clean workflow-only commits in the repo's history (`a7508cb`, `4f85a3e`, both 2026-04-20) **each took an entry** — and both predate the **Session 149** refinement that produced the directory list, so the list had never actually met a workflow-only session until Session 243. That measurement is what turned "my call felt underdetermined" into a decidable question: the conflict was not a drift to be corrected but a case the rule had never covered. Measured with `git log --full-history -- .github/` filtered against the gated directories → 4 commits, then each checked for a `CHANGELOG.md` edit in the same commit. | Session 244 | Whenever you find yourself following a written rule *against* an apparent precedent. Date both. If the precedent predates the rule's current wording, you have found an untested case, not a violation — say so, and the ruling becomes an amendment rather than an enforcement. |
| 186 | **A bequest's count is a count nobody derived — re-derive the SIZE of inherited work before scoping it.** Session 242 bequeathed L14 as *"sixteen true statements nothing derives — `(220→217, 804 lines)` and its fifteen siblings"*. Enumerated mechanically at Session 245 by scanning the four files for every ancestor span and every ancestor size, it is **31 literals carrying 44 figures** [corrected by Session 246: the figures Session 245 actually SHIPPED are **32 literals carrying 45 figures** — an adversarial review found a 45th occurrence after this row was drafted, and the row was never updated. Its own pointer block and `L14/census` both say 32/45. The learning about typed numbers carried a typed number that was wrong for one session] — and the hand count that preceded the scan said 28/36 and was also wrong, in the same session that was writing the assertion whose whole purpose is to stop typed numbers. `BACKLOG.md` alone states the ancestor sizes in **three** places, not the one the bequest implied. | Session 245 | Whenever you inherit a work item that states how big it is. The number is an estimate by someone who had not enumerated it; enumerate it with a script before you scope, declare, or quote it. |
| 187 | **An assertion scoped to "the artifacts in hand" goes blind the instant its artifact ages out — and the fix is a SET arm, not more literals.** L12 (sizes) and L13 (spans) measure `shard_recs`/`after_recs`, so they are scoped to their own cut BY CONSTRUCTION; the moment the next trim lands, that shard becomes an ancestor and its figures fall out of every assertion's reach permanently. Seven trims' worth had accumulated. Adding L14's 31 literals fixes today; **`L14/set`** — which holds the shards the declared literals name against `ROUTING`'s derived ancestor set — is what fixes tomorrow, because the ninth trim cannot inherit the declaration unchanged and stay green. | Session 245 | When an assertion's inputs are "the thing I am currently changing." Ask what checks that thing after it stops being current, and bind the declaration's COVERAGE to a derived set rather than enumerating instances. |
| 188 | **A file census must name the commit it describes, or it is two numbers in one sentence.** Session 242's block says the sweep *"returns nineteen files ... and the seven shards with their seven proofs"*. Measured: **19** at its parent `f26233a`, **21** at its own trim commit `e7d5b03` — the count is the pre-shard measurement, the enumeration beside it is the post-shard set, and no assertion reads either. Session 245 drafted *"returns 23 files"* and nearly shipped the mirror image; running `git grep` at both revisions caught it, and the published form now states 23-at-this-commit and 21-at-its-parent. | Session 245 | Any time you state how many files/rows/matches something returns in a document that lives in the repository being counted. The act of committing changes the answer. |
| 189 | **Quote a phrase that `grep` can actually find.** Four consecutive pointer blocks quote the S220 shard's banner as *"the live ledger when N ≥ 221"*. The banner wraps after "the", so that exact string does not exist in the file and a session verifying the quotation gets zero hits — indistinguishable from the banner having been edited, in a lineage whose whole premise is that shards are never edited. Fixed by quoting the contiguous substring *"live ledger when N ≥ 221"*. | Session 245 | Before quoting any string from a wrapped prose file: `grep -F` the quotation first. If it returns nothing, quote less. |
| 190 | **Never mutate the artifacts while a mutation sweep is running, and make the sweep prove it did not error.** Session 245's first arm sweep reported 60 of 64 arms as having "no uniquely-catching mutant". They were harness errors: the artifacts had been regenerated mid-run, so the self-test's own fixture assertions failed and the script exited **1** — which the sweep, matching only on the string `SURVIVED`, scored identically to "everything was caught". A sweep that does not distinguish exit codes cannot tell a clean result from a broken run. | Session 245 | Any long-running mutation/coverage sweep. Freeze its inputs for the duration, and assert the exit code is one of the values the tool actually defines (here 0 or 2) before believing a "(none)" row. |
| 191 | **Neutering a `return [...]` to `pass` changes the return TYPE, and the harness scores the crash as coverage.** The arm sweep neuters one failure-emitting statement at a time. For `out.append(...)` sites `pass` is correct; for the early `return ["L1: ..."]` arms it makes the function return `None`, and `fails += None` raises — so L1, L6 and L7 were reported as unreachable when they had simply crashed. The neuter must be type-preserving: `return []`. | Session 245 | When writing any mutation harness that disables code by substitution. Substitute a value of the same type, not a statement that removes the value. |
| 192 | **Derive the premise that JUSTIFIES a deliverable before you build the deliverable — it is load-bearing and nobody read it.** Operator ruling (a) rested entirely on Session 245's sentence *"each earlier proof reads its artifacts at its own commit, so compressing them disturbs nothing."* No assertion reads that sentence. Testing it cost one experiment — corrupt a collapsed block, run all eight proofs — and it came back TRUE and much bigger than claimed: not just the five blocks but the whole live front matter, and then all four files `L8` reaches, are read only at each proof's own trim commit. The premise was the smallest true version of the finding. | Session 246 | Whenever a task is justified by a claim inherited from another session. Test the claim first, with a control. This is #186 one level up: #186 says re-derive the SIZE of inherited work, this says re-derive the REASON it is safe. |
| 193 | **The trim apparatus guards a historical snapshot, not the live file — measured, with a control.** Every shard proof resolves its prose operands from its OWN trim commit via `blob("%s:%s" % (sha, p))`: `SESSION_NOTES.md`, and `CLAUDE.md`, `README.md`, `BACKLOG.md`, `PROJECT_CONVENTIONS.md` too. Only `L7`/`L9`/`L10` read disk, and only shards and proof scripts. Corrupt the live pointer block's routing clause and figures, or any of the four files' shard censuses: **all eight proofs stay GREEN.** Edit an ancestor shard on disk: RED — that is the control that proves the probe works. So S245's gotcha 2 (*"L8, L12, L13 and now L14 all read those four **live**"*) is true only while a trim is UNCOMMITTED; re-running the loop after the trim commit proves nothing about those four. | Session 246 | Before trusting that any prose copy is enforced. Ask which epoch the assertion reads, not whether the assertion exists. And when a session edits those four files OUTSIDE a trim, no proof will notice — the ninth trim's largest open hole. |
| 194 | **Commit before delegating, because "read-only" is an instruction, not a sandbox.** A verification subagent told in capitals not to write anything ran its refutation experiment on the working tree: `CLAUDE.md`, `README.md`, `BACKLOG.md` and `PROJECT_CONVENTIONS.md` were truncated to one line each and 173 lines were cut from `SESSION_NOTES.md`. Recovery was a single `git checkout` and cost nothing **only because the session's sole work product was already committed.** The second review was launched against a committed HEAD and instructed to write the experiment into its finding rather than run it — which also produced better findings, because a described experiment is reproducible. | Session 246 | Before spawning any review or verification agent. Commit first; tell agents to SPECIFY mutation experiments rather than perform them; treat the repo as shared mutable state, because it is. |
| 195 | **Declared substitutions must anchor on whole PARAGRAPHS, not fragments.** Five fragment-level substitutions left the surrounding prose wrapped for the sentence they replaced — 132- and 153-character lines in a file hand-wrapped at 100 — and, worse, missed a falsified sentence entirely: the eighth block's *"The seven blocks below are frozen at the SEVENTH through FIRST trims"* stayed standing and false with all seven assertions green. Re-cut as three paragraph substitutions, the wrap is right and the anchor IS the text that has to stay true, so the miss is hard to make. | Session 246 | Any edit to frozen front matter under the declared-substitution rule. Anchor the paragraph. Then add the assertion that DERIVES the family of claims you are editing (here `C7`), because the substitution list is still a hand-written list. |
| 196 | **Compose the prose from the measurement rather than comparing the measurement to the prose.** `L12` holds declared integers against the figures printed in the prose — two copies, kept in step. `C3` instead builds each table row's entire markdown line out of the measured span, heading count, archived lines and shard total, then requires that line verbatim in the table. There is no second copy to drift, and a typed figure cannot survive. The same trick closed the table's four prose figures (`C0/FIGURE`, `C3/FIGURE`): compose *"The first five trims are one table now"* from `len(ROWS)` instead of grepping for `five`. | Session 246 | Any assertion whose job is to stop a number being typed. Prefer composition to comparison; and when you must search for a word, compose the whole phrase — a bare `count("five") >= 1` passed a mutant because the word occurred twice. |
| 197 | **An assertion that COMPOSES from declarations shadows the assertion that CHECKS those declarations, and the neuter loop is the only way to see it.** `C3` composed its row line from the declared sha, span and left-live span, so every mutant that perturbed a declaration tripped `C3` and `C4` caught nothing alone — a whole assertion with no uniquely-catching mutant, which is exactly Session 224's `L2/b0` and Session 228's `L5/2`. The fix is a mutant, never a rewrite: mutate the **world** (what git reports) instead of the declaration. Five mutants (`M21`-`M25`) restored it. The same thing then happened *within* a revision — `C3/SESSION` took `C4/LEFT-LIVE`'s mutant, `C0/FIGURE` took `C0/SIZE`'s — which the seventh trim had already named. | Session 246 | After adding any assertion that reads a declaration another assertion also reads. Run the whole-assertion neuter loop before believing a green `--self-test`, and re-run it after every later arm you add. |
| 198 | **Re-run a published sweep after the LAST edit, and audit the sweep tool before publishing its number.** The header shipped `C5 -> M19, M32` and *"Ten of the 36 mutants"* when the final revision measured `M19, M32, M33` and sixteen; both were caught only by re-running. Then the arm sweep itself was wrong: its AST walk matched **any** `.append`, so `composed.append(...)` — not a failure arm at all — was counted, inflating the denominator. Fixed to require `out.append`, the count went 36 -> 35. A measurement you are about to publish deserves the same scepticism as a claim. | Session 246 | Before publishing any measured table. Re-run it on the exact artifact you are shipping, and read the measuring script for the case it over-matches. |
| 199 | **Check every "first / only / never" against the lineage with `grep` before writing it.** Shipped in a draft header: *"this is the first proof in it to have run the loop at both levels and published the result."* False — the SEVENTH trim published a 55-arm table with 29 arms named as uncovered, and the SIXTH found two of its own; the EIGHTH is the one that dropped the discipline. One `git grep -i 'per-arm|arm sweep'` over the eight proofs settles it. Session 245 shipped a superlative wrong by six and wrote that no assertion can reach one; the next session wrote another. | Session 246 | Before any superlative in a header, banner or pointer block. No assertion can check one, so it must be grepped. If it cannot be grepped cheaply, delete the superlative rather than shipping it. |
| 200 | **A working-tree assertion is safe if and only if its SUBJECT is immutable — which is why the ninth trim must not host the live census check.** `C6` works because its subject (trims 1–5) can never change. The shard census changes at *every* trim, so an `L15` re-reading `L8`'s four files live from inside a trim proof would be RED one trim later and **unrepairable**, because `L10` pins every ancestor proof byte-for-byte to a freeze commit. Measured, not reasoned: of the **19** literals the seventh trim's proof requires in those four files, only **3** are still present in the working tree — the eighth trim legitimately rewrote the rest, and that proof is green only because it reads them at its own commit. | Session 247, deciding option (a) vs (b) of Session 246's what's-next #1 | Before putting any assertion in a write-once artifact: ask whether its subject can legitimately change. If it can, the assertion belongs in a mutable artifact — a test — not in the proof. |
| 201 | **Computed fields (Quarto, templating) are fail-OPEN, and every incident in this lineage is an omission.** A computed field fixes the numbers an author *recognized* as computed. The 45th ancestor figure, `CLAUDE.md`'s *"holds the newest 4 sessions"* and the seventh trim's *"nineteen files"* were all missed because nobody noticed there was a number there. A fail-closed scan catches what nobody noticed; a template cannot. (Also true, but secondary: `CLAUDE.md` is loaded raw by the harness, every session hand-edits these files, and eight write-once proofs match literal bytes inside them.) | Session 247, ruling out `.qmd` for the four prose files | Whenever "just make it a computed field" is proposed as the fix for stale numbers. Ask which failure mode dominates: wrong numbers (templating helps) or missing coverage (only fail-closed helps). |
| 202 | **Measure a scan's false-positive rate before choosing its width — a too-wide net empties the word "allowlist" of meaning.** The census scan at ±60 characters of *shard/trim/proof/instance/banner/archiv* returns **127** candidates across the four files, mostly `§3.3`, `75%`, `2,000-line` and *"shape 5 of the 20 proofs"*. Narrowed to ±30 of "shard" plus a head-noun arm it returns **22**, and every one is a real statement about the shard set or a record beside it. Allowlisting the 105-candidate difference would have meant filing `§3.3` as "frozen census prose". | Session 247, choosing `WINDOW` | Before shipping any fail-closed scanner over prose. Run it at two or three widths, read the residue, and pick the width where the exemption list still means something. Learning #135 is the reason. |
| 203 | **A proximity scanner that re-derives its context per CANDIDATE is quadratic, and the symptom is a hang, not a failure.** `_in_scope` called `HEAD_NOUN.finditer(text)` over the whole file once per number token; these files carry hundreds. The suite ran for minutes and had to be killed twice before the cause was looked for. Hoisting the head-noun scan to once per file took it to **2.1 s**. A second instance in the same build: rebuilding the shard set inside every check re-read a 24,590-line file ten times per pass. | Session 247, building the census guard | When a new test is slow enough to notice, look for per-item work that should be per-file. Learning #151's sibling: a test that hangs is worse than a red test, and both hide the same way. |
| 204 | **A check every other mutant reaches TRANSITIVELY is not proven load-bearing — build the consistent-mistake mutant.** `check_tiling` and `check_banners` both fired under existing mutants and both could still be deleted with the suite green, because whatever broke the tree also made the prose disagree and `check_composed` caught it first. The isolating mutants had to break the tree **and rewrite all four prose files to agree with the break** — which is also the realistic failure: a trim that mis-cuts and then documents its own mistake faithfully. | Session 247, neuter loop over 7 checks | Whenever a coverage matrix shows "every check is reached". Reached ≠ load-bearing. Remove each check and ask whether any mutant then survives; if none does, the isolating mutant is the one that makes the mistake self-consistent. |
| 205 | **Describe a guard with no numerals.** The `CLAUDE.md` bullet announcing the census guard names `WINDOW` and `FROZEN` rather than quoting their values, and states no count of checks or mutants. Every numeral there would be a second copy nothing derives — the exact defect the guard exists to prevent — and nothing in this repository checks `CLAUDE.md`'s description of a test file. | Session 247, writing the bullet | When documenting any mechanism whose parameters live in code. Name the constant; do not quote it. If the number genuinely must appear in prose, ask what will re-derive it. |
| 206 | **A new guard flags its own documentation, and that is its acceptance test.** Adding the census guard's own row to `README.md` tripped the scan on *"the four prose files … (25 tests)"*, and filing its BACKLOG item tripped it again on *"Session 247"* sitting beside the word "shard". One was classified in `FROZEN` with a reason, the other reworded. **A guard that does not catch its own introduction is not scanning what it claims.** | Session 247 | Immediately after shipping any repo-invariant guard: write the prose that describes it and check that the guard reacts. If it stays silent, the scope is narrower than the docstring says. |
| 207 | **Measure a harness limit; never inherit its documented value.** The `Read` tool documents "up to 2000 lines by default", and this project built a trim trigger, target and floor as fractions of that number, restating it 19 times across 14 files. Measured at Session 248: a 3,000-line file returns **whole**, a 101-line/200 KB file truncates, and the real limit is **25,000 tokens** — announced in full (`PARTIAL view … total … cap … Do NOT answer from this page alone`), not silently. The claim "silently stops, no error, no marker" was false in 38 places. **A delegated sweep still quoted the documentation as evidence the premise "still holds" after a probe had already falsified it.** | Session 248 | Before deriving any budget, threshold or rule from a tool's stated limit. Build the two-minute probe: one input just under, one just over. Documentation describes intent; a probe describes behaviour. |
| 208 | **When truncation is ORDERED, total size is the wrong invariant — ask which quantity actually binds.** A top-down truncation applied to a newest-first ledger means what a reader receives is fixed by the *header plus the newest few entries*, never by the file's length. Session 248 measured it: a `Read` of a 1,702-line ledger returned the front matter plus the two newest complete records, and appending a thousand lines at the bottom would not change that by one byte. Eight trims had been cutting the **tail** to protect the **head**. The quantity that binds was the front matter — which the trimming apparatus itself manufactures, at an accelerating +19 → +87 lines per cut. | Session 248 | Whenever a rule governs "size". Write down what the size is a proxy *for*, then measure whether the proxy tracks it. A budget on the wrong quantity is invisible because it is always satisfiable and never effective. |
| 209 | **A constraint set can go empty while every parameter in it still looks reasonable.** `target ≤ 1,050`, `floor ≥ 4 records` and a measured density of ~235 lines/record are each individually defensible; jointly they are unsatisfiable, and had been for several cuts before anyone checked the conjunction. The density in the rule's own rationale (~184) was derived once, at the first trim, and never re-derived across 26 sessions while the true figure roughly doubled. | Session 248 | Any multi-parameter rule whose inputs drift. Re-derive the **conjunction** — "does a legal move still exist?" — not the individual numbers. Do it whenever one input has moved, not on a schedule. |
| 210 | **A rule evaluated at the moment that makes it pass is measuring something else.** The retention target is checked at the trim commit, when the newest retained record is the trimming session's own 9-line claim stub. Substitute that stub's finished form and **every** historical cut is over target — one by 210 lines. Nobody cheated; the check simply reads an operand that is still being written. | Session 248 | Any assertion whose operand is mutable at check time. State which reading is enforced, or evaluate against the settled value. Ask: "what will this operand be an hour after the check passes?" |
| 211 | **Narrowing a question toward your prior is the bias, not the answer.** Asked whether a plain run of any proof had ever gone red on a real defect, Session 248 answered for *already-shipped* proofs, got zero, and published it — favouring precisely the side it had been warned it was leaning toward. Under the question as asked the answer is not zero: inherited assertions fired on real defects at trim time twice. The narrowing was invisible because the narrowed claim was **true**. | Session 248 | Before publishing any finding that supports the conclusion you were already drifting toward. Re-read the question beside your answer and check the scope words match. A true answer to a narrower question reads exactly like an answer. |
| 212 | **Delegated measurement needs the same provenance discipline as your own prose.** Seven parallel sweeps produced this session's evidence; adversarial re-derivation corrected four of them materially, including one that quoted a tool's documentation as evidence and one whose arithmetic ignored a mechanism measured two steps earlier. Every figure in the deliverable is tagged by who derived it — measured-by-me, delegated-and-spot-checked, or claimed-and-not-re-derived. | Session 248 | Whenever a sub-agent's number will reach a deliverable. Tag provenance in the artifact itself, and hand-verify anything load-bearing. "An agent measured it" is a claim about the agent, not the world. |
| 213 | **Read the source of a founding precedent before arguing about its value.** The entire ledger apparatus rests on a data-loss event that no session in 26 had cited at source. It is real — `methodology_trim.py:19-21`, commit `020ba3f`, a paragraph lost while a whole-file byte-identity proof **passed** — and it is narrower than the folklore: a different repository, a manual procedure, a weaker check. Opening it simultaneously produced the defence's strongest evidence and bounded what that evidence supports. | Session 248 | Before defending or attacking any inherited safeguard. Find the incident it answers to and read it. A premise you have not opened is a premise you cannot weigh — in either direction. |
| 214 | **Re-run the probe after you write the record — a session's own close-out can falsify its deliverable.** Session 248 measured that one `Read` of the live ledger returned the front matter plus the two newest complete records, published that as its central finding, and then wrote a 232-line close-out that pushed the figure to one record — putting the operator assignment that had shaped the session, and all nine of its predecessor's gotchas, outside the one-`Read` window of the file that carries them. The measurement was true when taken, stale when committed, and the committing act is what staled it. **The general shape: when your deliverable measures an artifact you are also writing, the measurement's validity expires at your own next edit.** | Session 248 | Any session whose deliverable states a measurement of a file that session will also append to — session ledgers, changelogs, backlogs, README inventories. Re-measure as the last step before commit, not as the step that produced the finding. |
| 215 | **Correcting a false premise means finding its EXECUTABLE copies, not only its prose.** Option A was scoped as a text migration. The load-bearing copies turned out to be code: `tests/test_session_notes_census.py` *composed and required* two of the false sentences, so the guard built to keep prose true was mechanically enforcing a falsehood — it derives line counts from disk and cannot derive tokens; and `"read_cap": 2000` is a hand-declared constant in three **write-once** proofs whose `L12/cap` arm passes for a shard that a default `Read` cannot deliver. A prose sweep finds neither. | Session 249 | Whenever you correct a stated fact, grep `tests/`, `scripts/` and any proof or guard for the same claim before declaring the repair complete. A sentence a test requires is not documentation — it is a contract. |
| 216 | **A completion criterion written as one grep string is narrower than the claim it certifies.** The plan's own VERIFY for this option was `git grep -n '2,000-line agent read cap'`. A live table cell in `docs/planning/repository-rename.md` said *"truncates at 2,000 with no marker"* — same premise, different words — and matched neither that command nor the review's alternate. It survived the first pass and was found by an independent sweep running four different search angles. | Session 249 | When a criterion is a literal grep, treat it as a smoke test, not a proof. Sweep the CLAIM across paraphrases and let something other than the string that defined the criterion look for it. |
| 217 | **A fail-closed guard triggers on vocabulary, so writing around its trigger word silently leaves text unchecked.** A first draft used "archived file" where "shard" was the natural word and passed clean; rewritten with the natural vocabulary, the scan went red on three numbers and named each. It caught the same author twice in one session — the second time on a count the guard **already composes elsewhere**, which was deleted rather than exempted. | Session 249 | Write the natural word and let the guard adjudicate. If a number it flags is derivable, add a composed expectation — do not add an exemption. An allowlist entry is the last resort, never the convenient one. |
| 218 | **A frozen artifact does not merely go stale — machinery can force the NEXT one to be born false.** Seven of eight shard banners assert a cap that measurement falsified, and three are provably wrong today. That is unrepairable and known. The live hazard is different: the newest proof's `SIZE_PROSE` row, `SIZES["read_cap"]`, the `L12/cap` arm, the `M63` mutant built on the banner string in four places, and the `--self-test` print all require the ninth trim's banner to repeat the claim. Correcting prose while leaving that in place mints a new permanent falsehood one session later. | Session 249 | After correcting a fact, ask what *generates* the next copy of it. Naming the remedy ("declare the cap in bytes") is not enough — enumerate every site the next author must change, or they will copy the template forward. |
| 219 | **"Nothing here can measure X" is a claim to test, not an excuse to declare a constant.** This session wrote that nothing in the repository could measure the agent harness, then measured a byte proxy that predicts the observed truncation point to within 3.4% at three independent cuts — and the fleet tool had already re-denominated onto exactly that proxy 38 minutes earlier. The hand-declared constant existed because nobody had tried. | Session 249 | Before declaring a constant because the real quantity is unmeasurable, spend ten minutes looking for a proxy that is derivable from the artifact. A computed bound that tracks reality beats a typed one that cannot. |
| 220 | **State a measurement once and point at it; a count restated in three live files is the defect you are fixing.** Having just corrected a premise that had been copied into 19 places, this session published its own newly measured count in three of them before an adversarial lens objected. The fix was to keep the count where the per-shard evidence lives and make the other two state a definite negation that carries no number. | Session 249 | One authority per measured figure, pointers everywhere else. A definite negation ("not all of them read whole") is precise, needs no maintenance, and cannot rot. |
| 221 | **"The measurement is done, this is a paste" is still a claim; re-measuring is what makes the edit a fix rather than a copy — and when it matches, the match is the evidence.** The filing said the three numerals were already measured. Re-running collection took under a minute, reproduced all three, and turned "Session 247 said 257" into "257 collects today". Had a test been added in the two intervening sessions, the paste would have shipped a fourth stale figure into the very row being corrected. | Session 250 | Whenever a handoff or backlog item hands you a number to type, re-derive it first, however recent the filing. The cost is seconds; the alternative is the defect class this project reports most. |
| 222 | **A population censused in two places under two denominators drifts independently — sweep for the second census by the population's NAMES, not by its numbers.** `README.md` counts collected tests per directory; the wiki's `Contributing.md` counts `def test_` functions per directory. No numeral from one appears in the other, so a sweep for the stale figures found nothing; a sweep for the directory names found the second census at once, stale in four directories, its total and its pass/skip sentence. | Session 250 | When fixing a count, grep for what is being counted (directory names, file names, the head noun), not for the count — and check the denominator before declaring two sites inconsistent or "correcting" one to the other. |
| 223 | **A workflow's failed agents come back as `null` and `[]` in the aggregate, indistinguishable from "searched and found nothing".** Four of five verification agents died on a subagent session limit; the returned object read `sweep: null, critics: []`, which a hurried reader takes as a clean sweep. The `<failures>` block in the completion notification is the only place the difference shows. | Session 250 | Before treating a workflow result as coverage, read its failures and its done/error agent counts. Re-run the failed arms inline or after the limit resets, and say in the record which verification was independent and which was your own. |
| 224 | **A conclusion has a shelf life, and a planning document's conclusions expire faster than its analysis.** `ledger-budgets-review.md` §3.2 proved the retention rule's constraint set empty and §3.4 called a ruling "on the critical path"; §12.1 measured `K = 1`. Four sessions later both were false — the rule is satisfiable and `K = 4` — and neither had changed because anyone acted. The document's own dragon 9 said "re-derive at Phase 0" and three consecutive handoffs carried the stale sentence forward anyway. **When you present a decision document to a decision-maker, re-derive the figures the decision turns on, not the figures that are cheap to re-derive.** The two that had moved were the two the whole recommendation rested on. |
| 225 | **A green run and a green `--self-test` are different claims, and the gap between them is where an apparatus rots.** `SESSION_NOTES-pointer-collapse.verify.sh` exits 0 on its ordinary run and exits 2 on `--self-test` with two mutants surviving. It has been in that state since Session 249 and four sessions ran the plain nine-proof loop and reported "all nine green". `CLAUDE.md` already mandated the self-test; nothing automated it, so nothing did it. **A verification convention that is written down but not executable is a convention that has already lapsed.** |
| 226 | **Editing prose can disarm a proof without failing it — the mutation, not the assertion, is what a text edit breaks.** Session 249's Option A correctly repaired one sentence inside a 51-line literal that a proof pins. The assertion (`C6`) kept passing, because it checks the table's rows and opening line individually. But two mutants mutate by `live_wt.replace(NEW_TABLE, …)`, and once `NEW_TABLE` stopped matching, both became silent no-ops. **Assertions degrade loudly; mutants degrade silently. After any edit to text a proof pins, re-run the mutants, not the assertion.** |
| 227 | **Measure before you recommend, not after — especially the one claim a recommendation rests on.** §9 declined Option G because "the handoffs that score 9 and 10 are the long ones." Testing it took one script over 173 record/score pairs: `r(lines) = −0.087`, and the ≥9 records are *shorter*. I had already presented the decline and the operator had already ruled when a completeness critic falsified the premise, so the operator ruled twice on one option. **The correlation was one command away the whole time.** |
| 228 | **A correlation's sign can belong to the denominator rather than the data.** The same 173 record/score pairs give `r = −0.087` in lines and `r = +0.070` in bytes, because the older records are short in lines and long in bytes — the format changed, the quality did not. Restricted to the current regime the sample cannot test the claim at all: 0 of 28 records fit either candidate cap. **Before reading a correlation as evidence, check whether the independent variable is measuring the thing or measuring a regime change; and check whether the regime you are ruling for is represented in the sample.** |
| 229 | **Check whether the question has already been ruled somewhere you own.** This project debated a per-record budget as an open option while the operator's own canonical methodology repo, one directory over, had `RATIFIED 2026-08-25 … 12,288 (12 KiB)` and enforced it in `bin/check-handoff:665`. That plan's §7 deliberately scopes it canonical-only, so there was no contradiction — but a decision document that never looks at the sibling repository presents settled questions as open ones. **Grep the fleet before writing the options table.** |
| 230 | **A fail-closed guard going red on your own prose is the guard working; make the sentence more precise rather than exempting it.** The census guard flagged `95/95` because it sat within 30 characters of the word "shard". The number was not a shard census — it was a mutant count — so an exemption was defensible. Naming the specific proof file instead was shorter, more accurate, and left the guard's reach undiminished. **An exemption is a permanent maintenance obligation; a better sentence is free.** |
| 231 | **Mutate by what the assertion READS, never by a literal that contains prose.** `M16`/`M17` corrupted the working tree with `live_wt.replace(NEW_TABLE, …)` — a 51-line literal of table *plus prose*. `C6` only ever reads the composed rows and the opening line. When Session 249 correctly repaired a sentence in the prose, the literal stopped matching, both mutants became no-ops, and the assertion was handed pristine input for four sessions. The repaired mutants use `rows_of(NEW_TABLE)` and `new_table.split("\n")[0]` — the same objects `C6` counts — so drift cannot break the mutation without breaking the plain run in the same instant. | Session 253 | Whenever a mutant corrupts an artifact an assertion reads: make the mutation's needle the assertion's needle, literally the same expression. Any surplus text in the needle is a silent-rot surface. |
| 232 | **A verification tool with two modes has two blind spots, and running one mode is not verification.** Measured with controls: a plain run cannot see a proof that has stopped being able to fail (green for 8 consecutive commits with two mutants inert); `--self-test` cannot see real corruption (append a line to any archived file and every mutant fails *for the corruption's sake*, scores as "caught", and the self-test reports OK on a broken world). The DONE gate, `CLAUDE.md` and `BACKLOG.md` all published a one-mode loop. | Session 253 | Before writing any "run X to verify" recipe, ask what X is structurally incapable of seeing, and pair it. Publish the pair, not the mode you happened to use. |
| 233 | **A backlog item's suggested fix is a hypothesis written by someone who had not run it.** The filing said "re-pin one literal". Re-pinning was *impossible*: `C1` resolves its `after` operand at the proof's own add-commit forever, so a re-pinned literal fails `C1` permanently — there is no green state containing one. Testing it took three minutes and redirected the whole repair to the mutants, which is where the same item's last paragraph already pointed. | Session 253 | Execute a filing's proposed remedy as an experiment before adopting it, especially one written by a session that diagnosed but did not repair. Diagnosis and remedy have different evidence standards. |
| 234 | **A deferral that names a destination is a factual claim about that destination.** I wrote "it is filed in `BACKLOG.md`, not fixed here" in the same header block arguing that claims must be measured. Nothing was filed. Seven of seven independent review lenses found it — the single most-reported finding of the session — because a dangling forward reference is the one defect no proof in this repository will ever go red over. | Session 253 | Write the filing first, then the sentence that points at it; or state the limitation without asserting a record exists. "Not fixed here" is honest; "filed in X" is checkable, and therefore falsifiable. |
| 235 | **The failure shape you are repairing is the one you are most likely to reintroduce, one field over.** The bug: a needle that was not its haystack's. My first repair searched for `row + "\n"` while `C6` counts a bare `row` — so appending one trailing space to a table row left the plain run green and killed the self-test on an assert. Same shape, new field, written by the author who had just spent an hour characterising it. A review measured it before it shipped. | Session 253 | After fixing a defect, re-read your fix asking specifically "have I committed this same defect in the fix?" Name the shape abstractly first (here: *needle ≠ haystack's own predicate*), then grep your own diff for it. |
| 236 | **A CI step that invokes a mode the tool can silently ignore proves nothing.** Every proof in this lineage parses its own argv and ignores an unrecognised flag, so `bash "$f" --self-test` against a proof lacking the mode performs a second *plain* run and exits 0 — the job stays green while proving half what it claims. The fix is one `grep` for the success line the mode prints; a mode that did not run cannot print it. | Session 253 | When automating a tool's optional mode, assert on the mode's own output, not on its exit code. Exit 0 from a flag that was ignored is indistinguishable from exit 0 from a flag that was honoured. |
| 237 | **Neutering an arm one at a time is the only way to detect a weakening you introduce elsewhere.** Re-anchoring `C6` from a derived needle to a declared literal was correct and both verification modes stayed green — while `C0/FIGURE` and `C3/FIGURE` silently stopped being the sole objector to three mutants, because the fixture that rebuilt the working tree no longer produced a tree the new anchor could match. Unique coverage went 17 of 35 to 16 of 36: one arm gained, two lost, invisibly. A green plain run and a green `--self-test` together still miss this class; only the per-arm sweep sees it. | Session 254 (Option E retroactive) | After ANY change to a proof's assertions or its self-test fixtures, re-run the per-arm neuter sweep and compare unique coverage against the inherited figure. Treat an unexplained drop as a regression in the proof, not a curiosity. Re-derive the inherited figure against the pre-change world before believing either number. |
| 238 | **A prose claim that was true at its own cut rots without any proof going red, and the dated version is the persuasive evidence.** The live pointer block called itself "the authority" and stated that its filename sweep returned 23 files. Measured 26 at HEAD and **23 at its own trim commit** — true when written, false since, with three nameable files added by later sessions. Showing that a figure *was* correct and then decayed is a stronger argument for retiring the prose than showing it is merely wrong, because it proves the failure is structural rather than careless. | Session 254 | When arguing to delete or collapse a body of prose, measure its claims at HEAD **and** at the commit that wrote them. A claim that was true then and is false now indicts the format; a claim that was always false only indicts its author. |
| 239 | **Deleting a duplicated fact is safe only once something derives it from the survivor.** The nine prose routing clauses were removed because the table's `archived` column already contains the same routing, but "already contains" was an inference until `R7` asserted that the spans tile the session history with no gap or overlap **and** that the newest archived session plus one equals the oldest record id in the working tree. The second arm is the load-bearing one: internal consistency proves the table agrees with itself, not that it agrees with the file a session actually opens. | Session 254 | Before deleting one of two copies of a fact, write the assertion that derives the deleted copy from the surviving one — and make sure at least one arm of it reads the live artifact rather than only the declaration. Otherwise the deletion converts a checkable duplicate into an unchecked single point of failure. |
| 240 | **A fail-closed allowlist entry is a claim about prose that outlives the prose.** Rewriting `CLAUDE.md`'s collapse bullet stranded two `FROZEN` entries in the census guard whose exempted sentences no longer existed, turning the guard red and taking 17 mutant tests down with it — all from one cause. The right repair was to DROP them, not re-point them, because the replacement prose tripped nothing; and that had to be established by running the scan without them rather than by reading it. | Session 254 | When you reword any sentence in the four guarded prose files, expect `test_frozen_entries_are_still_present` to fire. Drop the stranded entry if the new prose needs no exemption (verify by running the scan), and re-point it only if it does. Never exempt a file. |
| 241 | **An assertion that mixes a FROZEN operand with a LIVE one can be green in every state that exists and have no reachable green state in the next one.** `R7/FRONTIER` compared the max archived session of a hand-declared, permanently-pinned `ROWS` against the working tree's oldest record id. It passed every check this project has: both proof modes, 48 mutants, a whole-assertion neuter sweep, a per-arm sweep, and three hand-built control experiments — because all of them test the world as it is. After the next trim the live half moves and the frozen half cannot, and no edit reaches a green state (extending the declaration fails the SET arm; editing the pinned literal fails the confinement arm). It is the same trap this file already records one level down, rebuilt inside the new assertion by the session that documented it. | Session 254, found by an adversarial review that outlived the commit | **Before shipping any assertion whose operands are not all resolved at the same time, SIMULATE the next state and run it.** For this ledger that means: fake a trim (drop the oldest records, add the row) and require GREEN, then fake each half alone and require RED. If the simulation has no green state, the assertion is unshippable however many mutants it catches. |
| 242 | **A partial `Read`'s page is sized by the WHOLE file, so the tail can move what arrives at the head.** Past the 25,000-token cap one default `Read` delivered `floor(0.85 × 25,000 × L / T)` lines for L lines and T tokens; appending 20 long lines at the bottom of an otherwise identical file cut its delivered head from 730 lines to 299. "Truncation is ordered, so only the front matter matters" was true of WHICH lines arrive and false of HOW MANY. | Session 255 (synthetic probes) | Before arguing that a trim, an append or a reflow cannot change what a session reads. |
| 243 | **The harness has a second page regime.** When the file's tokens pro-rated to the page's bytes, `T × bytes(page) / B`, exceed the cap, the page is 0.7 of the rule's: six of thirteen real probes, and a sparse tail alone triggered it. A head metered over the cap on its own did not — the first model of the trigger was wrong. The true ratio is unknown offline, so a guard must apply both rules across the measured ratio range and keep the smallest page. | Session 255 (adversarial reviews, G1/R1/R3) | Whenever a guard predicts a page from bytes: model both regimes, worst case over the ratio. |
| 244 | **An explicit `offset`/`limit` `Read` over the cap is an exact token meter.** It returns `File content (N tokens) exceeds maximum allowed tokens (25000)` with zero content — even for a file the default `Read` refuses by size — so "the token count of a refused file is unmeasurable" was false. | Session 255 (completeness critic) | Whenever a token figure is needed for a file over the cap. |
| 245 | **Define the record before counting it: every K figure in circulation had counted claim stubs.** "K = 4" was two non-stub records; "K = 5" was three. The classifier took three tries: matching the marker anywhere mis-classifies a closed record that QUOTES the template; matching only the lines before the first blank line misses claims with a blank before Status (Sessions 193, 226). What works: the run of leading paragraphs that each open with a `**Field:**` line, read outside fences — and a unit test per historical shape, not per imagined one. | Session 255 (probe; reviews G2, G3, N1, V2) | Before writing any "K records fit" or "the file holds N records" claim, and before shipping any classifier over ledger history: scan the history for every shape, then pin each in a test. |
| 246 | **I put three wrong counts into an operator decision question by summarising a table by eye.** "44 commits" was 41, and both K=3 counts were off; the conclusion survived, the figures did not. A number headed for a question the operator will rule on is a deliverable and gets the same script as the record. | Session 255 (self-found) | Every time a measured figure goes into an AskUserQuestion. |
| 247 | **Run the mutant battery, not just the checks, in every certain next state — and the close-out is the next state of a claim commit.** This guard was green at HEAD with almost no green successor: mutants built for today's ledger shape went vacuous or started overlapping after a trim or a close-out, and the next-state test asserted only "no check fires" and skipped the close-out entirely. A mutant whose precondition is impossible in some states (the reduced page cannot cut a small K prefix) is not a mutant; move that proof into a state-independent test on a synthetic file. | Session 255 (reviews F1–F8, V1, F2, F3) | Whenever a guard ships mutants and next-state tests: sweep the successors across realistic record sizes before committing. |
| 248 | **A review of fixes finds defects of the fixes.** The first review's findings were real and all fixed; the second review, run only to verify those fixes, found three new defects the fixes introduced — two mutants with fixed geometry, a classifier regression on history, a page arm with no margin — one of them a blocker on the very next commit. | Session 255 | When a review's fixes are substantial (new arms, new mutants, a changed classifier), budget a verification pass before the commit, not after. |
| 249 | **Check every bequest against the operator's rulings before executing it.** Session 254 bequeathed `L15` as "an instruction, not a note", and Session 255 repeated it; ruling F (budgets review §13.1) had already said of `L15` "F is the decision not to build it". Neither handoff mentioned the conflict — a history agent found it, and the operator ruled in one question. A session's instruction never outranks a ruling. | Session 256 | Before acting on any "bequeathed", "must" or "instructions, not notes" item: grep the rulings (review documents, `PROJECT_CONVENTIONS.md`, BACKLOG ruling rows) for its subject. A conflict is an operator question, not a judgement call. |
| 250 | **Model the trim AND its close-out, and the close-out AND its next claim — not each alone.** The read-budget guard modelled a close-out and a floor-keeping trim separately and was green in both; the ninth trim's close-out was red for every compliant cut. After that fix, a review found the same class twice more (`M01`, `M08`) one state further on: a maximal close-out followed by the next claim. | Session 256 | For any guard with next-state models: compose the models along the sequence a session actually walks (claim → trim → close-out → claim), and search for the widest compliant state rather than modelling the median. |
| 251 | **Fixture geometry must be derived from the live state, not typed.** R-series `M50` hard-coded a gap span that was a gap only while the newest row ended at 241; the ninth row turned it into an overlap and left `R7 LIVE-GAP` reached by no mutant with the self-test still green. The census guard's re-anchored mutants had the same shape — hand-typed copies of composed sentences — and now derive their anchors from the shard set. | Session 256 | Whenever a mutant or fixture on a mutable artifact names a position, span, count or composed sentence: compute it at run time from the same source the check composes from. |
| 252 | **A cell cannot name the commit it is written in — declare the placeholder and verify the placeholder.** The table's `trim` column holds each shard's add-commit, and the newest row is written inside that commit. Resolved as `this commit`, a legend rule that the next trim resolves it, and a proof arm that COMPOSES the row, so the placeholder is checked rather than trusted. | Session 256 | Any self-referential field in an artifact committed with its subject (a hash, a size that includes itself): never fill it later with an unverified value. |
| 253 | **A fail-closed figure scan cannot tell a coincidence from a claim — reword, never allowlist.** `L14/complete` flagged "9 lines / 721 B" because 721 is also the eighth shard's record-line count; the census guard flagged "Session 256" beside "per-shard". Both true sentences; rewording kept both scans strict. | Session 256 | When a fail-closed scan objects to a figure that is not about its subject: change the wording or units, not the scan (learnings #230, #240). |
| 254 | **"Contains the right number" is not "states the right number" — bound every figure match.** Inherited `L12`/`L13`/`L14` arms tested `want in literal`, so "11,681" passed for 1,681, "seventeen" for seven, "twenty-five" for five, and a census with its two counts swapped passed both. `L14/complete` had used a boundary regex all along; the arms beside it never did. An attack lens found it by editing an artifact and its declaration together. | Session 256 | Every check that compares a derived figure with prose: match on digit or word boundaries, and parse a multi-figure sentence by position. Test with a mutant that changes the artifact and the declaration together. |
| 255 | **Pin a row's position, not only its presence.** `L2` proved the front matter changed by exactly one row insertion — and could not see that the row sat outside the table, where every reader of the table still parsed it as routing. Presence plus a residue test is not placement. | Session 256 | Any check that an insertion happened: also assert WHERE — directly after its declared neighbour — when position carries meaning. |
